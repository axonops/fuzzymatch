---
phase: 07-phonetic-algorithms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - soundex.go
  - soundex_test.go
  - soundex_bench_test.go
  - soundex_fuzz_test.go
  - dispatch_soundex.go
  - phonetic_cross_validation_test.go
  - phonetic_codes_golden_test.go
  - testdata/cross-validation/phonetic/vectors.json
  - testdata/golden/phonetic-codes.json
  - testdata/golden/_staging/soundex.json
  - scripts/gen-phonetic-cross-validation.py
  - tests/bdd/features/soundex.feature
  - tests/bdd/steps/algorithms_steps.go
  - monge_elkan.go
  - monge_elkan_test.go
  - props_test.go
  - example_test.go
  - llms.txt
  - llms-full.txt
  - Makefile
  - docs/cross-validation.md
autonomous: true
requirements: [PHON-01]
tags: [phonetic, soundex, cross-validation, knuth-census-variant, foundation]
must_haves:
  truths:
    - "User can call SoundexCode(s) and receive a deterministic 4-char Knuth/Census variant code (NOT SQL/MySQL variant); SoundexCode(\"Tymczak\") == \"T522\"; SoundexCode(\"Ashcraft\") == SoundexCode(\"Ashcroft\") == \"A261\"."
    - "User can call SoundexScore(a, b) and receive 0.0 / 1.0; identity (a == b) returns 1.0; both-empty returns 1.0."
    - "User can run `go test -run TestPhonetic_CrossValidation/Soundex -race ./...` and observe byte-stable agreement with the committed dual-pin (jellyfish==1.2.1 + Metaphone==0.6) corpus."
    - "User can run `make regen-phonetic-cross-validation` (with `jellyfish==1.2.1 Metaphone==0.6` installed) and the script regenerates vectors.json byte-identically; the script REFUSES to run on any other pin per the Phase 6 RapidFuzz pattern."
    - "User can run `go test -run TestPhoneticCodesGolden/Soundex -race ./...` and the cross-platform byte-stable code golden file diffs identically on linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64."
    - "Root `go.mod` MUST remain zero-non-stdlib-`require` lines (verified by `make verify-deps-allowlist`) — `jellyfish` and `Metaphone` are developer-toolchain only."
    - "`testing/quick` for property tests; native `go test -fuzz` for fuzz; no testify in root tests."
    - "Source-Origin Statement at top of `soundex.go` (Phase 4 WR-01 format) cites Russell 1918/Knuth TAOCP Vol. 3 §6.4 + lists `jellyfish==1.2.1` (BSD-2) cross-validation + negative-attribution lines for `xrash/smetrics`, `tilotech/go-phonetics`, `UjjwalAyyangar/go-jellyfish`."
    - "`permittedMongeElkanInner` map gains `AlgoSoundex: true` (14 → 15 entries); `monge_elkan_test.go` panic-test `rejected` slice shrinks from 9 → 8 in lockstep — both mutations in the SAME COMMIT as soundex.go ships per CONTEXT.md §4."
    - "`llms.txt` + `llms-full.txt` synced WITHIN this plan for `SoundexCode` and `SoundexScore` — NOT deferred to finalisation."
    - "Cross-platform determinism gate via `testdata/golden/phonetic-codes.json` (byte-stable encoded strings) — NEW determinism gate value type for Phase 7; 07-01 creates the file with Soundex section; 07-02..07-04 append their sections."
    - "OQ-1 RESOLUTION LOCKED 2026-05-15: scripts/gen-phonetic-cross-validation.py dual-pins JELLYFISH_VERSION = \"1.2.1\" AND METAPHONE_VERSION = \"0.6\" — Metaphone (oubiwann's BSD-licensed Andrew Collins port) carries Double Metaphone because jellyfish 1.x deprecated double_metaphone (RESEARCH.md key finding 1). This is a USER-AUTHORIZED amendment to CONTEXT.md §1's implicit single-pin assumption."
  artifacts:
    - path: "soundex.go"
      provides: "SoundexCode(s string) string + SoundexScore(a, b string) float64; ASCII byte-scan + 4-byte stack-allocated result buffer; Knuth/Census H/W skip rule (NOT SQL separator rule); non-ASCII silent skip per CONTEXT.md §5"
      contains: "func SoundexCode"
      min_lines: 60
    - path: "soundex_test.go"
      provides: "Literature reference vector tests (Tymczak variant gate + Ashcraft/Ashcroft H/W pair + Robert/Rupert/Rubin Knuth canonical + Smith/Honeyman/Lloyd/empty edges); identity / both-empty / one-empty pins; numerical-regression pin (SoundexCode/SoundexScore outside cross-validation corpus)"
    - path: "soundex_bench_test.go"
      provides: "Allocation-aware benchmarks at short/medium/long ASCII sizes; `var sink string` + `sink == \"\"` gate for dead-code-elimination; budget < 500 ns, 0 allocations"
    - path: "soundex_fuzz_test.go"
      provides: "FuzzSoundex with ASCII regime seeds + non-ASCII silent-skip seeds (Müller, Café, 中文, 🎉hello, \\xff\\xfe); invariants: no panic / no NaN / no Inf / range [0,1] / identity / charset [A-Z0-9]"
    - path: "dispatch_soundex.go"
      provides: "var _ = func() bool { dispatch[AlgoSoundex] = SoundexScore; return true }() — package-level side-effect registration (no init())"
    - path: "scripts/gen-phonetic-cross-validation.py"
      provides: "Dual-pin (jellyfish==1.2.1 + Metaphone==0.6) corpus generator with per-entry variant_divergence flag + divergent_jellyfish_value field; writes vectors.json._metadata.{jellyfish_version, metaphone_version, python_version, regenerated_at, script_sha256}; refuses to run on pin mismatch"
    - path: "testdata/cross-validation/phonetic/vectors.json"
      provides: "Initial corpus committed with Soundex section (15 entries per CONTEXT.md §1 — ASCII names + Tymczak Knuth/Census gate + Ashcraft/Ashcroft H/W pair + at-least-one Knuth-vs-SQL marked entry); DM/NYSIIS/MRA sections appended by 07-02..07-04"
    - path: "phonetic_cross_validation_test.go"
      provides: "TestPhonetic_CrossValidation loader with 4 t.Run sub-tests; 07-01 creates with active Soundex sub-test only; 07-02..07-04 enable their sub-tests; dual version-pin preamble checks (jellyfish AND metaphone)"
    - path: "testdata/golden/phonetic-codes.json"
      provides: "NEW byte-stable phonetic-code determinism gate per CONTEXT.md §7; Soundex section (8-12 entries) created here; 07-02..07-04 append their sections"
    - path: "phonetic_codes_golden_test.go"
      provides: "Separate cross-platform code-golden loader (string equality, not float-bit equality); per CONTEXT.md §7 recommendation"
    - path: "testdata/golden/_staging/soundex.json"
      provides: "10 staging-golden Score entries (per Phase 2-6 norm 8-12) to be merged into algorithms.json by plan 07-05"
    - path: "monge_elkan.go"
      provides: "permittedMongeElkanInner map gains `AlgoSoundex: true` + phonetic-tier comment line per CONTEXT.md §4 — lockstep with monge_elkan_test.go"
      contains: "AlgoSoundex: true"
    - path: "monge_elkan_test.go"
      provides: "panic-test rejected slice shrinks 9→8 (AlgoSoundex removed); permittedSanity slice grows; new TestMongeElkanScore_BinaryInner_Soundex with three cases (one-matches=0.5 / both-match=1.0 / neither=0.0)"
    - path: "props_test.go"
      provides: "Five-invariant property tests for SoundexScore (Identity / Symmetric / Range / NoNaN / NoInf) + PropSoundex_CodeCharset (^[A-Z][0-9]{3}$)"
    - path: "example_test.go"
      provides: "ExampleSoundexCode + ExampleSoundexScore (runnable godoc examples)"
    - path: "tests/bdd/features/soundex.feature"
      provides: "≥ 4 scenarios — Scenario Outline literature ref vectors (Robert/Tymczak/Ashcraft/Smith/empty), identity, both-empty, Tymczak Knuth/Census variant gate"
    - path: "tests/bdd/steps/algorithms_steps.go"
      provides: "Append Soundex step.Step registrations + new lastCode state field + shared `^the code should be \"([^\"]*)\"$` step"
    - path: "Makefile"
      provides: "regen-phonetic-cross-validation target with pip install hint for BOTH pins (`pip install --user jellyfish==1.2.1 Metaphone==0.6`) + .PHONY entry"
      contains: "regen-phonetic-cross-validation"
    - path: "docs/cross-validation.md"
      provides: "New 'Phonetic cross-validation' section documenting dual-pin rationale (RESEARCH.md key finding 1), variant_divergence flag mechanism, Makefile regenerate command, Go-side TestPhonetic_CrossValidation entry point"
    - path: "llms.txt"
      provides: "### Soundex (Russell 1918 / Knuth TAOCP §6.4) section with SoundexCode + SoundexScore entries"
    - path: "llms-full.txt"
      provides: "Full Phase 7 Soundex algorithm-surface block (godoc verbatim)"
  key_links:
    - from: "soundex.go"
      to: "dispatch[AlgoSoundex]"
      via: "dispatch_soundex.go's `var _ = func() bool { dispatch[AlgoSoundex] = SoundexScore; return true }()`"
      pattern: "dispatch\\[AlgoSoundex\\] = SoundexScore"
    - from: "phonetic_cross_validation_test.go"
      to: "testdata/cross-validation/phonetic/vectors.json"
      via: "os.ReadFile + json.Unmarshal + per-entry t.Run dispatch on entry.Algorithm; dual version-pin preamble check"
      pattern: "JellyfishVersion.*1\\.2\\.1"
    - from: "scripts/gen-phonetic-cross-validation.py"
      to: "jellyfish.soundex + variant-divergence detection"
      via: "JELLYFISH_VERSION pin assertion + per-entry hand-curated KNUTH_EXPECTED override table for SQL-vs-Knuth divergent inputs"
      pattern: "JELLYFISH_VERSION = \"1.2.1\""
    - from: "monge_elkan.go permittedMongeElkanInner"
      to: "monge_elkan_test.go panic-test fixture"
      via: "lockstep mutation per CONTEXT.md §4 — map gains AlgoSoundex, rejected slice removes AlgoSoundex"
      pattern: "AlgoSoundex: true"
    - from: "phonetic_codes_golden_test.go"
      to: "testdata/golden/phonetic-codes.json"
      via: "os.ReadFile + json.Unmarshal + per-entry t.Run; string equality (not float-bit equality)"
      pattern: "TestPhoneticCodesGolden"
---

<objective>
Land the Phase 7 cross-validation foundation: the dual-pin (jellyfish==1.2.1 + Metaphone==0.6) Python corpus generator, the committed `vectors.json` corpus, the Go loader test (with Soundex sub-test active and DM/NYSIIS/MRA stubs to be enabled by 07-02..07-04), the new byte-stable `phonetic-codes.json` cross-platform determinism golden file (per CONTEXT.md §7) and its loader, the Makefile target, the `docs/cross-validation.md` extension — PLUS the first algorithm itself: Soundex (Knuth/Census variant). Wires `dispatch_soundex.go`, mutates `permittedMongeElkanInner` (14 → 15) and `monge_elkan_test.go` panic-fixture (9 → 8 rejected) in lockstep, ships `TestMongeElkanScore_BinaryInner_Soundex`, adds two new `ExampleXxx` entries, syncs llms.txt + llms-full.txt in-plan. Closes PHON-01.

Purpose: plan 07-01 IS the foundation. Plans 07-02 / 07-03 / 07-04 depend on this plan landing because they extend the same `vectors.json` / `phonetic-codes.json` files, register their step definitions against the same accumulator, and mutate the same `permittedMongeElkanInner` map in lockstep. Soundex is the simplest of the four algorithms and the natural fit for the foundation plan — it lets the cross-validation infrastructure prove itself end-to-end before the harder algorithms (Double Metaphone in 07-02) start.

Output: 1 new algorithm file (soundex.go ~70 LOC) + 3 companion test files + 1 dispatch wiring file; 1 new Python generator script; 1 new corpus JSON; 1 new code-golden JSON; 2 new Go test loaders; 1 new BDD feature; 1 new staging golden; 1 new docs section; modifications to monge_elkan.go + monge_elkan_test.go + props_test.go + example_test.go + tests/bdd/steps/algorithms_steps.go + Makefile + llms.txt + llms-full.txt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-phonetic-algorithms/07-CONTEXT.md
@.planning/phases/07-phonetic-algorithms/07-RESEARCH.md
@.planning/phases/07-phonetic-algorithms/07-VALIDATION.md
@.planning/phases/07-phonetic-algorithms/07-PATTERNS.md
@.claude/skills/algorithm-correctness-standards/SKILL.md
@.claude/skills/algorithm-licensing-standards/SKILL.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/performance-standards/SKILL.md
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/go-testing-standards/SKILL.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From algoid.go (lines 156-175 — already reserved Phase 7 slots):
```go
AlgoSoundex          AlgoID = 23 // Russell 1918 / Knuth TAOCP §6.4
AlgoDoubleMetaphone  AlgoID = 24 // Philips 2000
AlgoNYSIIS           AlgoID = 25 // Taft 1970 / Knuth TAOCP §6.4
AlgoMRA              AlgoID = 26 // Moore 1977 / NBS Tech Note 943
```
(Confirm exact numeric indices via algoid.go's numAlgorithms constant; do NOT renumber.)

From algoid.go dispatch table — already declares the slot as a `(a, b string) float64` function pointer; dispatch_soundex.go's job is to register `SoundexScore` into that slot.

From monge_elkan.go (lines 294-314 — current 14-entry permittedMongeElkanInner map):
```go
var permittedMongeElkanInner = map[AlgoID]bool{
    // Character tier (9) + Q-gram (4) + Gestalt (1 — RatcliffObershelp).
    // Phase 7 will additively add 4 phonetic AlgoIDs.
}
```

From monge_elkan_test.go (lines 313-323 — current 9-entry rejected slice):
```go
rejected := []fuzzymatch.AlgoID{
    fuzzymatch.AlgoMongeElkan,
    fuzzymatch.AlgoTokenSortRatio,
    fuzzymatch.AlgoTokenSetRatio,
    fuzzymatch.AlgoPartialRatio,
    fuzzymatch.AlgoTokenJaccard,
    fuzzymatch.AlgoSoundex,         // Phase 7 reserved — 07-01 removes
    fuzzymatch.AlgoDoubleMetaphone, // Phase 7 reserved — 07-02 removes
    fuzzymatch.AlgoNYSIIS,          // Phase 7 reserved — 07-03 removes
    fuzzymatch.AlgoMRA,             // Phase 7 reserved — 07-04 removes
}
```

From errors.go: `ErrInvalidAlgoID` is declared at package level (consumed by Phase 8 Scorer; not invoked by direct-call phonetic surfaces).

Public function signatures to ship (Phase 7 final state — this plan ships Soundex):
```go
func SoundexCode(s string) string                         // 4-char Knuth/Census variant
func SoundexScore(a, b string) float64                    // binary 0.0 / 1.0
func DoubleMetaphoneKeys(s string) (primary, secondary string) // 07-02
func DoubleMetaphoneScore(a, b string) float64                  // 07-02
func NYSIISCode(s string) string                                // 07-03 (6-char Taft truncation)
func NYSIISScore(a, b string) float64                            // 07-03
func MRACode(s string) string                                    // 07-04
func MRACompare(a, b string) (matched bool, simScore int)        // 07-04 — NBS-943 raw counter
func MRAScore(a, b string) float64                                // 07-04
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-validation foundation — dual-pin script + corpus + Go loader + code-golden + Makefile + docs + BDD step registrations + state fields</name>
  <files>scripts/gen-phonetic-cross-validation.py, testdata/cross-validation/phonetic/vectors.json, phonetic_cross_validation_test.go, testdata/golden/phonetic-codes.json, phonetic_codes_golden_test.go, Makefile, docs/cross-validation.md, tests/bdd/steps/algorithms_steps.go</files>
  <read_first>
    - scripts/gen-token-ratio-cross-validation.py (Phase 6 RapidFuzz pin analog — exact structural template)
    - testdata/cross-validation/token-ratios/vectors.json (corpus + `_metadata` shape)
    - token_ratio_cross_validation_test.go (Go loader pattern — type declarations, per-entry t.Run, version-pin preamble check)
    - algorithms_golden_test.go (cross-platform code-golden loader pattern — adapt from float-bit equality to string equality)
    - testdata/golden/algorithms.json (`_metadata` schema, entry shape)
    - Makefile (existing `regen-token-ratio-cross-validation` target at lines 241-246; `.PHONY` list at lines 28-29)
    - docs/cross-validation.md (existing token-ratios section — mirror structure for the new Phonetic section)
    - tests/bdd/steps/algorithms_steps.go (existing AlgorithmContext state struct lines 46-51; per-algorithm step function pattern lines 53-66; InitializeScenario registration pattern lines 991-1100)
    - .planning/phases/07-phonetic-algorithms/07-RESEARCH.md §4 (jellyfish-has-no-DM finding + Metaphone==0.6 recommendation)
    - .planning/phases/07-phonetic-algorithms/07-CONTEXT.md §1 (LOCKED corpus-size table; `variant_divergence` mechanism)
    - .planning/phases/07-phonetic-algorithms/07-CONTEXT.md §7 (LOCKED phonetic-codes.json schema)
    - .planning/phases/07-phonetic-algorithms/07-PATTERNS.md (scripts/, phonetic_cross_validation_test.go, phonetic-codes.json sections)
    - .claude/skills/determinism-standards/SKILL.md (golden-file conventions, NO map iteration on output, cross-platform byte-stability)
  </read_first>
  <behavior>
    - scripts/gen-phonetic-cross-validation.py declares `JELLYFISH_VERSION = "1.2.1"` AND `METAPHONE_VERSION = "0.6"` at top; asserts `jellyfish.__version__ == JELLYFISH_VERSION` on import; asserts `subprocess.check_output(['pip', 'show', 'Metaphone']).decode()` contains the pinned version (the `Metaphone` PyPI package does not expose `__version__` reliably); refuses to run on either mismatch.
    - Script writes `vectors.json._metadata = {jellyfish_version, metaphone_version, python_version, regenerated_at: ISO-8601, script_sha256}`; entries array with `{algorithm: "Soundex"|"DoubleMetaphone"|"NYSIIS"|"MRA", input, code|primary+secondary, variant_divergence?: bool, divergent_jellyfish_value?: string}`.
    - 07-01 commits the Soundex section of vectors.json with 15 entries per CONTEXT.md §1 — ASCII names + Tymczak/Ashcraft Knuth-Census discriminators + at-least-one Knuth-vs-SQL marked entry (with `variant_divergence: true`). DM/NYSIIS/MRA sections empty arrays in this commit; 07-02..07-04 append.
    - phonetic_cross_validation_test.go preamble asserts `c.Metadata.JellyfishVersion == "1.2.1"` AND `c.Metadata.MetaphoneVersion == "0.6"`; t.Run sub-tests per algorithm; 07-01 implements Soundex sub-test asserting against `entry.Code` (the Knuth-expected value — recorded directly, with the jellyfish value preserved in `divergent_jellyfish_value` for transparency); DM/NYSIIS/MRA sub-tests stubbed with `t.Skip("enabled by plan 07-NN")` markers.
    - testdata/golden/phonetic-codes.json created per CONTEXT.md §7 schema with `_metadata.purpose = "Cross-platform byte-stable phonetic code determinism gate"` + `regenerated_at` ISO timestamp + Soundex section (8-12 entries covering literature gates: Robert→R163, Tymczak→T522, Ashcraft→A261, Smith→S530, plus empty edge).
    - phonetic_codes_golden_test.go separate file per CONTEXT.md §7 recommendation; switch on entry.Algorithm dispatches to Soundex (initially) then DM/NYSIIS/MRA branches (stubs).
    - Makefile `regen-phonetic-cross-validation` target with two-pin pip install hint (`pip install --user jellyfish==1.2.1 Metaphone==0.6`); added to .PHONY.
    - docs/cross-validation.md "Phonetic cross-validation" section appended documenting: (1) why dual-pin (jellyfish has NO Double Metaphone per RESEARCH.md key finding 1); (2) `variant_divergence` flag mechanism (Soundex Knuth/SQL split — defensive; NYSIIS Knuth/modified split — load-bearing); (3) regenerate command + pip install hint; (4) Go entry point `TestPhonetic_CrossValidation`.
    - tests/bdd/steps/algorithms_steps.go AlgorithmContext extended with `lastCode string` (Soundex/NYSIIS/MRA shared); shared step `^the code should be "([^"]*)"$` registered; Soundex-specific `^I compute the Soundex code of "([^"]*)"$` + `^I compute the Soundex score between "([^"]*)" and "([^"]*)"$` registered. lastDMPrimary/lastDMSecondary/lastMRAMatched/lastMRASim fields ADDED but unused until 07-02/07-04.
  </behavior>
  <action>
    Create `scripts/gen-phonetic-cross-validation.py` by mirroring scripts/gen-token-ratio-cross-validation.py's structure. Replace RAPIDFUZZ_VERSION with TWO pin constants `JELLYFISH_VERSION = "1.2.1"` and `METAPHONE_VERSION = "0.6"`. Pin assertion for jellyfish uses `jellyfish.__version__`; pin assertion for Metaphone uses subprocess `pip show Metaphone | grep Version`. Hand-curate the Soundex 15-entry input list per CONTEXT.md §1 corpus-size table (ASCII names + Tymczak Knuth/Census gate + Ashcraft/Ashcroft H/W pair + at least one Knuth-vs-SQL divergent entry). Include hand-curated `KNUTH_EXPECTED` override dict for known SQL-vs-Knuth divergent inputs (per RESEARCH.md §1.1 the Soundex divergence is "defensive insurance with no expected actual divergences for canonical inputs" — keep the schema but expect every entry to have `variant_divergence: false`; still include the override dict scaffolding for when 07-03 NYSIIS arrives where divergences ARE expected). Write output as canonical JSON (encoding/json-style: 2-space indent, alphabetical key ordering within each entry where stable, single trailing LF). DM/NYSIIS/MRA generation functions stubbed with empty entry lists (07-02..07-04 fill in).

    Create `testdata/cross-validation/phonetic/vectors.json` by running the script once. Commit the output with Soundex section populated, DM/NYSIIS/MRA sections as empty arrays. `_metadata.jellyfish_version` = "1.2.1", `_metadata.metaphone_version` = "0.6".

    Create `phonetic_cross_validation_test.go` mirroring `token_ratio_cross_validation_test.go`. Type declarations per PATTERNS.md "phonetic_cross_validation_test.go" section: phoneticEntry (single Code field for Soundex/NYSIIS/MRA + Primary/Secondary fields for DM, omitempty on both), phoneticMetadata (jellyfish_version + metaphone_version + python_version + regenerated_at + script_sha256), phoneticCorpus (version + metadata + entries array). TestPhonetic_CrossValidation preamble: assert version==1, JellyfishVersion=="1.2.1", MetaphoneVersion=="0.6". Per-entry `switch e.Algorithm` dispatch — Soundex branch asserts `fuzzymatch.SoundexCode(e.Input) == e.Code` (the Knuth-expected value); DM/NYSIIS/MRA branches call `t.Skip("enabled by plan 07-NN")`.

    Create `testdata/golden/phonetic-codes.json` per CONTEXT.md §7 schema. Populate Soundex section with 8-12 entries: Robert→R163, Rupert→R163, Rubin→R150, Tymczak→T522, Ashcraft→A261, Ashcroft→A261, Pfister→P236, Smith→S530, Honeyman→H555, Lloyd→L300, ""→"" (empty edge). `_metadata` block contains `purpose` string + `regenerated_at` ISO timestamp. Sort entries by `(algorithm, input)` for deterministic stability.

    Create `phonetic_codes_golden_test.go` as a SEPARATE file per CONTEXT.md §7 recommendation. Type declarations per PATTERNS.md "phonetic_codes_golden_test.go" section. TestPhoneticCodesGolden loads the JSON; per-entry t.Run dispatches via `switch e.Algorithm` — Soundex branch asserts string equality `fuzzymatch.SoundexCode(e.Input) == e.Code`. DM/NYSIIS/MRA branches stubbed with `t.Skip(...)`.

    Update `Makefile`: add `regen-phonetic-cross-validation` target mirroring `regen-token-ratio-cross-validation` at lines 241-246. Install hint contains `pip install --user jellyfish==1.2.1 Metaphone==0.6`. Append target to .PHONY list at lines 28-29.

    Update `docs/cross-validation.md`: append new "## Phonetic cross-validation" section after the existing token-ratios section. Documents dual-pin rationale (RESEARCH.md key finding 1 — jellyfish 1.x has no Double Metaphone module), variant_divergence flag mechanism (Soundex defensive vs NYSIIS load-bearing), regenerate command + pip install hint with both pins, and Go entry point `TestPhonetic_CrossValidation`.

    Update `tests/bdd/steps/algorithms_steps.go`: extend `AlgorithmContext` struct with new fields `lastCode string`, `lastDMPrimary string`, `lastDMSecondary string`, `lastMRAMatched bool`, `lastMRASim int` (all populated by later plans; lastCode used by Soundex steps now). Add new step functions `iComputeTheSoundexCodeOf(s string)` writing to ctx.lastCode, `iComputeTheSoundexScoreBetween(a, b string)` writing to ctx.lastScore. Add shared step function `theCodeShouldBe(want string)` (used by Soundex/NYSIIS/MRA). Register the three steps in `InitializeScenario` with regexes `^I compute the Soundex code of "([^"]*)"$`, `^I compute the Soundex score between "([^"]*)" and "([^"]*)"$`, `^the code should be "([^"]*)"$`.
  </action>
  <verify>
    <automated>cd /Users/johnny/Development/fuzzymatch &amp;&amp; go build ./... &amp;&amp; go vet ./... &amp;&amp; go test -run 'TestPhonetic_CrossValidation/Soundex|TestPhoneticCodesGolden/Soundex' -race -shuffle=on -count=1 ./... &amp;&amp; grep -q regen-phonetic-cross-validation Makefile &amp;&amp; grep -q 'Phonetic cross-validation' docs/cross-validation.md &amp;&amp; grep -q 'JELLYFISH_VERSION = "1.2.1"' scripts/gen-phonetic-cross-validation.py &amp;&amp; grep -q 'METAPHONE_VERSION = "0.6"' scripts/gen-phonetic-cross-validation.py</automated>
  </verify>
  <acceptance_criteria>
    - Satisfies VALIDATION.md rows 07-01-08, 07-01-09, 07-01-10, 07-01-13.
    - `scripts/gen-phonetic-cross-validation.py` contains both pin constants verbatim AND a runtime assertion that refuses to run on mismatch (verified by grep + manual review of the pin assertion).
    - `testdata/cross-validation/phonetic/vectors.json` exists with `_metadata.jellyfish_version == "1.2.1"`, `_metadata.metaphone_version == "0.6"`, Soundex array of 15 entries, DM/NYSIIS/MRA arrays empty.
    - `testdata/golden/phonetic-codes.json` exists with Soundex section ≥ 8 entries including Tymczak→T522 and Ashcraft→A261; DM/NYSIIS/MRA sections empty.
    - `go test -run TestPhonetic_CrossValidation/Soundex -race ./...` passes; DM/NYSIIS/MRA sub-tests skip.
    - `go test -run TestPhoneticCodesGolden/Soundex -race ./...` passes; other sub-tests skip.
    - `make regen-phonetic-cross-validation` is a documented target (`grep -q regen-phonetic-cross-validation Makefile`); .PHONY list includes it.
    - `docs/cross-validation.md` has a "Phonetic cross-validation" section explaining the dual pin AND the variant_divergence mechanism.
    - `tests/bdd/steps/algorithms_steps.go` AlgorithmContext has the 5 new state fields and the 3 new Soundex steps registered; project builds; existing BDD tests still pass.
  </acceptance_criteria>
  <done>The cross-validation foundation is committed and the Soundex sub-tests (corpus + code-golden) both pass; DM/NYSIIS/MRA sub-tests are stubbed for plans 07-02..07-04 to enable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Soundex (algorithm + tests + bench + fuzz + dispatch + ME wiring + props + example + BDD + llms.txt + staging golden)</name>
  <files>soundex.go, soundex_test.go, soundex_bench_test.go, soundex_fuzz_test.go, dispatch_soundex.go, monge_elkan.go, monge_elkan_test.go, props_test.go, example_test.go, tests/bdd/features/soundex.feature, testdata/golden/_staging/soundex.json, llms.txt, llms-full.txt</files>
  <read_first>
    - soundex.go (the file being created — confirm doesn't exist; expected non-existent)
    - ratcliff_obershelp.go (Source-Origin Statement format at lines 1-115; identity short-circuit at lines 144-146 + 170-172; Apache header at lines 1-13; package-level godoc structure)
    - dispatch_ratcliff_obershelp.go (exact 39-line analog for dispatch_soundex.go)
    - ratcliff_obershelp_test.go (literature reference-vector test pattern at lines 128-146; both-empty + one-empty at lines 65-96; variant-gate pin pattern lines 161-169)
    - token_jaccard_bench_test.go (allocation-aware bench pattern; `var sink string` dead-code-elimination gate at lines 58-71; ReportAllocs)
    - token_jaccard_fuzz_test.go (fuzz invariant block at lines 75-96 — extend with CODE CHARSET invariant per CONTEXT.md §5)
    - monge_elkan.go lines 271-314 (permittedMongeElkanInner map current state)
    - monge_elkan_test.go lines 313-323 (rejected slice; 9 entries — 5 base + 4 Phase 7 reserved)
    - monge_elkan_test.go lines 386-389 (per-inner permitted sanity-loop pattern — for new TestMongeElkanScore_BinaryInner_Soundex)
    - props_test.go lines 1391-1451 (RatcliffObershelp Five-invariant block — but ADD `_Symmetric` for phonetic since they ARE symmetric)
    - example_test.go lines 179-194 (RatcliffObershelp ExampleXxx pattern)
    - testdata/golden/_staging/ratcliff_obershelp.json (staging-golden schema and canonical marshalling — entries sorted alphabetically by name)
    - tests/bdd/features/ratcliff_obershelp.feature (exact analog feature file structure — Feature header + Scenario Outline + identity + both-empty + variant gate)
    - llms.txt (existing structure; identify section ordering for new `### Soundex` heading insertion)
    - llms-full.txt (existing per-algorithm block format)
    - .planning/phases/07-phonetic-algorithms/07-RESEARCH.md §1.1 (Soundex variant choice, output charset, jellyfish parity)
    - .planning/phases/07-phonetic-algorithms/07-RESEARCH.md §3.1 (RV-S1..RV-S12 12-vector catalogue)
    - .planning/phases/07-phonetic-algorithms/07-RESEARCH.md §6.1 (Per-algorithm implementation notes — 60-80 LOC, [4]byte stack buffer, 0 allocations budget)
    - .planning/phases/07-phonetic-algorithms/07-RESEARCH.md §"Code Examples" Example 1 (SoundexCode skeleton — illustrative reference)
    - .planning/phases/07-phonetic-algorithms/07-CONTEXT.md §5 (non-ASCII silent-skip godoc warning canonical text — copy verbatim into soundex.go package-level godoc)
    - .planning/phases/07-phonetic-algorithms/07-CONTEXT.md §4 (lockstep monge_elkan mutation discipline)
    - .planning/phases/07-phonetic-algorithms/07-PATTERNS.md (Source-Origin Statement, dispatch_<algo>.go, identity-short-circuit, stack-allocated result buffer, fuzz invariants + charset extension, per-plan llms.txt sync sections)
    - .claude/skills/algorithm-correctness-standards/SKILL.md (primary-source citation, identity short-circuit + both-empty 1.0 convention)
    - .claude/skills/algorithm-licensing-standards/SKILL.md (fresh-implementation discipline, attribution format, negative-attribution conventions)
    - .claude/skills/determinism-standards/SKILL.md (no map iteration on output, no init() — but Soundex has no maps or init paths)
    - .claude/skills/performance-standards/SKILL.md (allocation budgets: < 500 ns, 0 allocations for Soundex)
    - .claude/skills/go-coding-standards/SKILL.md (no testify in root tests; stdlib `testing` only)
    - .claude/skills/go-testing-standards/SKILL.md (coverage targets ≥ 95% overall / ≥ 90% per file / 100% on public API; property-test conventions; fuzz seed conventions)
  </read_first>
  <behavior>
    - `SoundexCode(s string) string` returns 4-char Knuth/Census variant code; first letter uppercased preserved + 3 digits zero-padded; H/W skip without resetting lastGroup (NOT separators); vowels (AEIOU + Y) ARE separators (reset lastGroup to sentinel); same-group consonants collapse to single digit; non-ASCII runes SILENTLY skipped per CONTEXT.md §5; empty input returns "".
    - `SoundexScore(a, b string) float64` returns 1.0 if a == b (identity short-circuit covers both-empty), 1.0 if `SoundexCode(a) == SoundexCode(b)` and the code is non-empty, else 0.0. Both-empty returns 1.0 per algorithm-correctness-standards convention.
    - Literature reference vectors (RV-S1..RV-S12 from RESEARCH.md §3.1) ALL PASS in `soundex_test.go`:
      - `Robert → R163`, `Rupert → R163`, `Rubin → R150`, `Ashcraft → A261`, `Ashcroft → A261`, `Tymczak → T522` (LOAD-BEARING Knuth/Census gate vs SQL T520), `Pfister → P236`, `Honeyman → H555`, `Smith → S530`, `"" → ""`.
    - Identity test (`TestSoundex_BothEmpty`) and one-empty test (`TestSoundex_OneEmpty`) pass.
    - Tymczak load-bearing pin test (`TestSoundexCode_TymczakVariantGate`) asserts T522 verbatim with a comment explaining the SQL T520 divergence per CONTEXT.md §1 LOCKED.
    - Allocation-aware benchmark `BenchmarkSoundexCode_ASCII_Short` etc. with `b.ReportAllocs()`; sink-gate prevents compiler DCE; budget < 500 ns, 0 allocations.
    - `FuzzSoundex` seeds cover ASCII regime (Robert/Tymczak/Ashcraft) + non-ASCII silent-skip regime (Müller/Café/中文/🎉hello) + invalid UTF-8 (\xff\xfe); invariants: no panic / no NaN / no Inf / range [0,1] / identity / CODE charset `[A-Z0-9]`.
    - `dispatch_soundex.go` registers `SoundexScore` into `dispatch[AlgoSoundex]` via the `var _ = func() bool {...}()` Phase-2-canonical form (no `init()`).
    - `permittedMongeElkanInner` map gains `AlgoSoundex: true` with the new "// Phonetic tier (Phase 7):" comment line; the map's leading comment is updated from "14 entries" to "15 entries". `monge_elkan_test.go` `rejected` slice shrinks from 9 → 8 entries (AlgoSoundex removed); `permittedSanity` slice grows correspondingly.
    - `TestMongeElkanScore_BinaryInner_Soundex` asserts three cases per CONTEXT.md §4: `MongeElkanScore("alpha beta", "alpha gamma", AlgoSoundex, opts) == 0.5` (one matches), `MongeElkanScore("alpha beta", "alpha beta", AlgoSoundex, opts) == 1.0` (both match), `MongeElkanScore("alpha", "gamma", AlgoSoundex, opts) == 0.0` (neither).
    - `props_test.go` gains five-invariant block (Identity / Symmetric / Range / NoNaN / NoInf) for SoundexScore plus a `PropSoundex_CodeCharset` asserting `^[A-Z][0-9]{3}$` or empty.
    - `ExampleSoundexCode` and `ExampleSoundexScore` appended to `example_test.go`; both compile and produce expected output (`R163`, `1.0`).
    - `tests/bdd/features/soundex.feature` has ≥ 4 scenarios per CONTEXT.md §6-prior: Scenario Outline literature ref vectors (Robert/Tymczak/Ashcraft/Smith/empty), identity (a == b), both-empty, Tymczak Knuth/Census variant gate.
    - `testdata/golden/_staging/soundex.json` contains 10 entries (per planner OQ resolution): both-empty / identity / Tymczak self-match / Ashcraft-Ashcroft pair / Schmidt-Smith Soundex match / Robert-Rupert match / Robert-Smith non-match / Smith-Jones non-match / Catherine-Katherine + 1 edge.
    - `llms.txt` gains `### Soundex (Russell 1918 / Knuth TAOCP §6.4)` heading + two function lines; `llms-full.txt` gains the full Phase 7 Soundex algorithm-surface block (godoc verbatim).
  </behavior>
  <action>
    Create `soundex.go` (60-80 LOC per RESEARCH.md §6.1). Apache header (13 lines from `ratcliff_obershelp.go:1-13`). Package-level godoc paragraph that COPIES VERBATIM the canonical non-ASCII silent-skip warning text from CONTEXT.md §5 ("Non-ASCII input handling: this algorithm operates on ASCII letters [A-Za-z] only..."). Source-Origin Statement (Phase 4 WR-01 format per `ratcliff_obershelp.go:67-77`): primary source = Knuth TAOCP Vol. 3 §6.4 + Russell-Odell 1918 algorithmic origin; cross-validation = `jellyfish==1.2.1` (BSD-2); negative-attribution list = `xrash/smetrics`, `tilotech/go-phonetics`, `UjjwalAyyangar/go-jellyfish`. Implementation: ASCII byte-scan with stack-allocated `var result [4]byte` per RESEARCH.md §6.1; group-encoding table `soundexGroup(c byte) byte` returning the 1-6 group for B/F/P/V→1, C/G/J/K/Q/S/X/Z→2, D/T→3, L→4, M/N→5, R→6; sentinel `soundexNoGroup` and `soundexVowelGroup` constants. H/W skip rule (continue without changing lastGroup). Vowels (AEIOU + Y) reset lastGroup to `soundexNoGroup` (NOT to vowel-group — vowels act as separators). Identity short-circuit at top of `SoundexScore` before any allocation. Both-empty returns 1.0 (covered by identity). NO `init()`. NO map iteration on output paths.

    Create `dispatch_soundex.go` (~30 LOC) mirroring `dispatch_ratcliff_obershelp.go` exactly: Apache header + file-doc comment + package declaration + `var _ = func() bool { dispatch[AlgoSoundex] = SoundexScore; return true }()`.

    Create `soundex_test.go` (`package fuzzymatch_test`). File-doc paragraph following PATTERNS.md "<algo>_test.go family" lines 15-26. Stdlib `testing` only; NO testify. Tests: `TestSoundex_BothEmpty`, `TestSoundex_OneEmpty`, `TestSoundexCode_KnuthReferenceVectors` (table-driven covering RV-S1..RV-S12 from RESEARCH.md §3.1 — Robert/Rupert/Rubin/Ashcraft/Ashcroft/Tymczak/Pfister/Smith/Honeyman/Lloyd/empty), `TestSoundexCode_TymczakVariantGate` (LOAD-BEARING — fail message explains the SQL T520 divergence), `TestSoundexScore_Reflexivity`, `TestSoundexScore_HW_HandlingPairs` (Ashcraft/Ashcroft cross-match).

    Create `soundex_bench_test.go` following `token_jaccard_bench_test.go` lines 15-71 pattern. Benchmarks at three sizes: ASCII-short (`"Robert"`), ASCII-medium (~30 chars), ASCII-long (~50 chars). For Code benchmarks use `var sink string; if sink == "" { b.Fatal(...) }` gate; for Score benchmarks use `var sink float64; if sink < 0 { b.Fatal(...) }`. NO `Pathological_*` fixtures per CONTEXT.md §6-prior.

    Create `soundex_fuzz_test.go` following `token_jaccard_fuzz_test.go` lines 50-96 pattern. `FuzzSoundex(f *testing.F)` with 10 seed pairs per the PATTERNS.md "<algo>_fuzz_test.go family" `f.Add` seed list (ASCII regime + non-ASCII regime + invalid UTF-8 + empty). f.Fuzz invariants block: not-panic (implicit), `math.IsNaN(score) == false`, `math.IsInf(score, 0) == false`, `score >= 0.0 && score <= 1.0`, `score(x,x) == 1.0`, and the EXTENDED CODE CHARSET invariant per CONTEXT.md §5: for each rune of `SoundexCode(a)`, assert `(c >= 'A' && c <= 'Z') || (c >= '0' && c <= '9')`.

    Modify `monge_elkan.go` `permittedMongeElkanInner` map (lines 294-314): add new comment line `// Phonetic tier (Phase 7):` followed by `AlgoSoundex: true, // Russell 1918 / Knuth TAOCP §6.4`. Update the map's leading comment from "14 entries" to "15 entries (... + 1 phonetic-tier — Phase 7 plan 07-01)". DO NOT touch the other 14 entries.

    Modify `monge_elkan_test.go` (lines 313-323): remove `fuzzymatch.AlgoSoundex` from the `rejected` slice (9 → 8 entries). Add `fuzzymatch.AlgoSoundex` to the `permittedSanity` slice at lines 370-385. Append new test function `TestMongeElkanScore_BinaryInner_Soundex` per the PATTERNS.md "monge_elkan_test.go — 4 new binary-inner ME composition tests" template (three sub-cases: one_matches=0.5, both_match=1.0, neither=0.0); use `fuzzymatch.DefaultNormalisationOptions()` for opts.

    Append to `props_test.go` five-invariant block for SoundexScore (Identity / Symmetric / Range / NoNaN / NoInf) using `testing/quick`. Phonetic algorithms ARE symmetric — KEEP the Symmetric invariant (unlike RO which dropped it). Add `PropSoundex_CodeCharset` asserting the regex `^([A-Z][0-9]{3})?$` (empty OR letter+3 digits) holds for arbitrary input.

    Append to `example_test.go`: `ExampleSoundexCode` (input "Robert", expected output `R163`) and `ExampleSoundexScore` (inputs "Robert"/"Rupert", expected output `1.0`).

    Create `tests/bdd/features/soundex.feature` following `tests/bdd/features/ratcliff_obershelp.feature` structure. ≥ 4 scenarios: Scenario Outline "literature reference vectors" (Robert/Tymczak/Ashcraft/Smith/empty); Scenario "identical strings score 1.0"; Scenario "both-empty strings score 1.0"; Scenario "Tymczak Knuth/Census variant gate".

    Create `testdata/golden/_staging/soundex.json` with 10 entries (planner OQ resolution — middle of 8-12 range): version=1, entries array sorted alphabetically by `name` field; covers both-empty / identity / Tymczak self-match / Ashcraft-Ashcroft pair / Schmidt-Smith Soundex match / Robert-Rupert match / Robert-Smith non-match / Smith-Jones non-match / Catherine-Katherine / 1 edge case. Canonical-marshal via 2-space indent + single trailing LF (per `assertGoldenStaging` helper convention).

    Update `llms.txt`: insert new `### Soundex (Russell 1918 / Knuth TAOCP §6.4)` heading after the Phase 6 token-based section (or in alphabetical-by-AlgoID order if that's the convention); under it list `- func SoundexCode(s string) string` and `- func SoundexScore(a, b string) float64`.

    Update `llms-full.txt`: append the full Phase 7 Soundex algorithm-surface block (godoc verbatim — from soundex.go's package-level godoc + function godocs).
  </action>
  <verify>
    <automated>cd /Users/johnny/Development/fuzzymatch &amp;&amp; go build ./... &amp;&amp; go vet ./... &amp;&amp; go test -race -shuffle=on -count=1 -run 'TestSoundex|TestMongeElkanScore_BinaryInner_Soundex|TestMongeElkan_PanicsOnNonPermittedInner|TestProp_Soundex|TestLLMsTxt' ./... &amp;&amp; go test -run none -bench BenchmarkSoundex -benchmem -benchtime=1x ./... &amp;&amp; go test -fuzz=FuzzSoundex -fuzztime=10s ./...</automated>
  </verify>
  <acceptance_criteria>
    - Satisfies VALIDATION.md rows 07-01-01, 07-01-02, 07-01-03, 07-01-04, 07-01-05, 07-01-06, 07-01-07, 07-01-11, 07-01-12, 07-01-14.
    - `fuzzymatch.SoundexCode("Tymczak") == "T522"` (Knuth/Census gate — load-bearing).
    - `fuzzymatch.SoundexCode("Ashcraft") == fuzzymatch.SoundexCode("Ashcroft") == "A261"` (H/W-handling pair).
    - `fuzzymatch.SoundexCode("Robert") == fuzzymatch.SoundexCode("Rupert") == "R163"` (Knuth p. 393 canonical pair).
    - `fuzzymatch.SoundexScore("Robert", "Rupert") == 1.0` AND `fuzzymatch.SoundexScore("Robert", "Smith") == 0.0`.
    - `soundex.go` package-level godoc contains the EXACT canonical non-ASCII silent-skip warning paragraph from CONTEXT.md §5.
    - `soundex.go` Source-Origin Statement names Knuth TAOCP §6.4 + Russell-Odell 1918 + `jellyfish==1.2.1` (BSD-2) cross-validation + 3 MIT-Go-port negative-attribution lines.
    - `permittedMongeElkanInner` map has 15 entries (was 14); `rejected` slice in `monge_elkan_test.go` has 8 entries (was 9); both verified by `grep -c 'AlgoSoundex' monge_elkan.go` returning >= 1 and `go test -run TestMongeElkan_PanicsOnNonPermittedInner -race ./...` passing.
    - `TestMongeElkanScore_BinaryInner_Soundex` passes three sub-tests (one_matches=0.5 / both_match=1.0 / neither=0.0).
    - `BenchmarkSoundexCode_ASCII_Short` reports `0 allocs/op` and ns/op < 500 (warning if > 500 but green if 0 allocs).
    - `FuzzSoundex` corpus passes 10-second fuzz run with no panics + all invariants satisfied including code charset.
    - `tests/bdd/features/soundex.feature` has ≥ 4 scenarios and `make test-bdd` passes.
    - `testdata/golden/_staging/soundex.json` exists with 10 entries.
    - `llms.txt` contains `SoundexCode` and `SoundexScore` lines under a `### Soundex` heading; `llms-full.txt` contains the full Soundex block; `TestLLMsTxt` (sync-check meta-test) passes.
  </acceptance_criteria>
  <done>Soundex ships fresh-implemented from Knuth TAOCP §6.4 with all 12 literature reference vectors green, the Knuth/Census variant gate locked, dispatch wired into AlgoSoundex, Monge-Elkan permitted-inner extended in lockstep, cross-validation against `jellyfish==1.2.1` corpus green, byte-stable code golden file green, and all documentation synced in-plan. PHON-01 closed.</done>
</task>

</tasks>

<verification>
- `make check` (golangci-lint v2 + vet + race + vulncheck + gosec + markdownlint + tidy-check + coverage-check) green.
- `make test-bdd` green including the new `soundex.feature`.
- `make verify-determinism` green — `phonetic-codes.json` Soundex section diffs byte-identically on the CI matrix.
- `make verify-deps-allowlist` green — root `go.mod` STILL has zero non-stdlib `require` lines beyond `golang.org/x/text`.
- `make regen-phonetic-cross-validation` (developer-side, optional) regenerates `vectors.json` byte-identically when `jellyfish==1.2.1 Metaphone==0.6` are installed.
- `go test -run TestPhonetic_CrossValidation/Soundex -race -shuffle=on -count=1 ./...` green; DM/NYSIIS/MRA sub-tests skip cleanly.
- `go test -run TestPhoneticCodesGolden/Soundex -race -shuffle=on -count=1 ./...` green.
- `bench.txt` regeneration deferred to plan 07-05 finalisation (per CONTEXT.md §6-prior / RESEARCH.md §"Open Questions").
</verification>

<success_criteria>
PHON-01 closed:
1. `SoundexCode(s string) string` and `SoundexScore(a, b string) float64` are public, Knuth/Census-variant correct, deterministic, and free of GPL/LGPL provenance.
2. Tymczak→T522 gate, Ashcraft/Ashcroft→A261 H/W pair, Robert/Rupert→R163 canonical pair all pass.
3. Cross-validation infrastructure (dual-pin script + `vectors.json` + Go loader + Makefile + docs) ships with Soundex sub-test active; DM/NYSIIS/MRA sub-tests stubbed for plans 07-02..07-04.
4. Cross-platform byte-stable `phonetic-codes.json` ships with Soundex section; loader test green.
5. `permittedMongeElkanInner` map mutated 14→15 entries IN LOCKSTEP with `monge_elkan_test.go` panic-fixture shrink 9→8 entries; `TestMongeElkanScore_BinaryInner_Soundex` green.
6. `llms.txt` + `llms-full.txt` synced WITHIN this plan (NOT deferred); `TestLLMsTxt` green.
7. Allocation budget < 500 ns, 0 allocations met; fuzz green; BDD green.
8. OQ-1 RESOLUTION LOCKED 2026-05-15 recorded in plan SUMMARY: dual-pin `jellyfish==1.2.1 + Metaphone==0.6` per RESEARCH.md key finding 1 (USER-AUTHORIZED amendment to CONTEXT.md §1's implicit single-pin assumption).
</success_criteria>

<output>
After completion, create `.planning/phases/07-phonetic-algorithms/07-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md` with:
- Status: COMPLETE
- Closes: PHON-01
- OQ-1 RESOLUTION LOCKED 2026-05-15: dual-pin `JELLYFISH_VERSION = "1.2.1"` AND `METAPHONE_VERSION = "0.6"` per RESEARCH.md key finding 1 — Metaphone (oubiwann's BSD-licensed Andrew Collins port) carries Double Metaphone because jellyfish 1.x has no DM module. USER-AUTHORIZED amendment to CONTEXT.md §1's implicit single-pin assumption.
- OQ-2 RESOLUTION LOCKED: phonetic-codes.json ships as a SEPARATE file (not embedded into algorithms.json) per CONTEXT.md §7 recommendation — schema is string-valued, algorithms.json is float-valued.
- Patterns established for plans 07-02..07-04: dual-pin assertion preamble in cross_validation_test.go; `t.Skip` stubs for not-yet-implemented sub-tests; lockstep permittedMongeElkanInner + panic-fixture mutation in same commit; per-plan llms.txt sync.
- Reference vectors: Tymczak→T522, Ashcraft→Ashcroft→A261, Robert→Rupert→R163, etc.
</output>
