---
phase: 07-phonetic-algorithms
plan: 05
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - testdata/golden/algorithms.json
  - bench.txt
  - examples/identifier-similarity/main.go
  - examples/identifier-similarity/main_test.go
  - examples/phonetic-keys/main.go
  - examples/phonetic-keys/main_test.go
  - tests/bdd/features/monge_elkan_phonetic_inner.feature
  - ai_friendly_test.go
autonomous: true
requirements: [PHON-01, PHON-02, PHON-03, PHON-04]
tags: [phonetic, finalisation, golden-merge, identifier-similarity-extension, phonetic-keys-example]
must_haves:
  truths:
    - "User can read `testdata/golden/algorithms.json` and find entries for all four phonetic algorithms (Soundex / DoubleMetaphone / NYSIIS / MRA) merged from the four `_staging/*.json` files shipped in plans 07-01..07-04; the merged file diffs byte-identically across the CI matrix (linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64)."
    - "User can run `make bench` and observe `bench.txt` regenerated full-replace including the new phonetic benchmarks; benchstat regression detection green vs Phase 6 baseline on carry-forward benchmarks (no >10% regression)."
    - "User can run `go run ./examples/identifier-similarity` and observe a 23-column table (was 19 after Phase 6) with the new `Soundex / DblMetaph / NYSIIS / MRA` score columns; golden stdout fixture regenerated and `go test ./examples/identifier-similarity/...` green."
    - "User can run `go run ./examples/phonetic-keys` and observe the new educational table demonstrating `SoundexCode / DoubleMetaphoneKeys / NYSIISCode / MRACode + MRACompare` on a curated English-surname set; golden stdout fixture in `main_test.go` green."
    - "User can run `make test-bdd` and observe the new `monge_elkan_phonetic_inner.feature` BDD scenarios covering ME-over-{Soundex, DM, NYSIIS, MRA} binary-inner composition (per CONTEXT.md §4)."
    - "`ai_friendly_test.go` walks `go/ast` and confirms every Phase 7 exported symbol (SoundexCode, SoundexScore, DoubleMetaphoneKeys, DoubleMetaphoneScore, NYSIISCode, NYSIISScore, MRACode, MRACompare, MRAScore — 9 new exported symbols) has an entry in both `llms.txt` and `llms-full.txt`. Already synced per-plan in 07-01..07-04; this plan ADDS the meta-test enforcement (or extends it if pre-existing)."
    - "Coverage targets met: ≥ 95% overall, ≥ 90% per file, 100% on public API surface — verified by `make coverage` and the existing coverage-check CI gate."
    - "Root `go.mod` remains zero-non-stdlib-`require` lines (verified by `make verify-deps-allowlist`)."
    - "All four PHON-01..PHON-04 requirements MARKED COMPLETE in REQUIREMENTS.md traceability table — note: ROADMAP/REQUIREMENTS update is a separate /gsd-execute-phase concern; this plan's responsibility is the GROUND TRUTH that all four algorithms work end-to-end in the merged catalogue."
  artifacts:
    - path: "testdata/golden/algorithms.json"
      provides: "Merged golden file extended from 19 algorithms (Phase 6) to 23 algorithms × ≈ 180 entries; the four _staging/{soundex,double_metaphone,nysiis,mra}.json files merged via the existing TestGolden_Algorithms_Merge pattern"
      contains: "Soundex"
    - path: "bench.txt"
      provides: "Full-replace regeneration including 4 new phonetic benchmarks (Soundex/DM/NYSIIS/MRA Code + Score + MRACompare); benchstat detects no >10% regression vs Phase 6 baseline on carry-forward benchmarks"
    - path: "examples/identifier-similarity/main.go"
      provides: "Extended from 19 to 23 columns by appending the 4 phonetic score columns to the `algorithms` slice per CONTEXT.md §8 + PATTERNS.md `examples/identifier-similarity/main.go` section"
      contains: "fuzzymatch.SoundexScore"
    - path: "examples/identifier-similarity/main_test.go"
      provides: "Golden stdout `want` constant regenerated to include the 4 new columns; existing TestExample_Output + TestExample_ColumnWidths shape unchanged"
    - path: "examples/phonetic-keys/main.go"
      provides: "NEW educational program demonstrating SoundexCode + DoubleMetaphoneKeys + NYSIISCode + MRACode + MRACompare on a curated English-surname set per CONTEXT.md §8; output format is string-valued (encoded keys), not float-valued"
      min_lines: 30
    - path: "examples/phonetic-keys/main_test.go"
      provides: "Golden stdout `want` constant + TestExample_Output + TestExample_ColumnWidths mirroring the identifier-similarity test shape"
    - path: "tests/bdd/features/monge_elkan_phonetic_inner.feature"
      provides: "BDD scenarios covering ME-over-{Soundex, DM, NYSIIS, MRA} binary-inner composition per CONTEXT.md §4 — minimum 4 scenarios (one per phonetic inner) demonstrating MongeElkanScore('alpha beta', 'alpha gamma', AlgoXxx, opts) == 0.5"
    - path: "ai_friendly_test.go"
      provides: "Meta-test walks go/ast across the package and confirms every exported symbol has an entry in BOTH llms.txt AND llms-full.txt; extended (or created) here to cover Phase 7's 9 new exported symbols"
  key_links:
    - from: "testdata/golden/algorithms.json"
      to: "testdata/golden/_staging/{soundex, double_metaphone, nysiis, mra}.json"
      via: "Existing TestGolden_Algorithms_Merge pattern; canonical-marshal merge with single trailing LF; entries sorted by (algorithm, name) for byte-identical cross-platform output"
      pattern: "Soundex.*DoubleMetaphone.*NYSIIS.*MRA"
    - from: "examples/identifier-similarity/main.go"
      to: "fuzzymatch.SoundexScore, DoubleMetaphoneScore, NYSIISScore, MRAScore"
      via: "Append 4 entries to the algorithms slice; algoWidth=13 unchanged (DblMetaph fits)"
      pattern: "fuzzymatch.SoundexScore"
    - from: "examples/phonetic-keys/main.go"
      to: "fuzzymatch.SoundexCode, DoubleMetaphoneKeys, NYSIISCode, MRACode, MRACompare"
      via: "Side-by-side table over curated name slice; demonstrates all 5 code/key surfaces"
      pattern: "fuzzymatch.MRACompare"
    - from: "ai_friendly_test.go"
      to: "llms.txt + llms-full.txt"
      via: "go/ast walk over exported symbols asserting both files contain entries for each"
      pattern: "TestLLMsTxt"
---

<objective>
Finalise Phase 7: merge the four `_staging/{soundex, double_metaphone, nysiis, mra}.json` files into `testdata/golden/algorithms.json` (extending it from 19 algorithms × 144 entries to 23 algorithms × ≈ 180 entries via the existing TestGolden_Algorithms_Merge pattern), regenerate `bench.txt` full-replace including the 4 new phonetic benchmarks (benchstat detecting no >10% regression on Phase 6 carry-forward), extend `examples/identifier-similarity/main.go` from 19 columns to 23 columns (Soundex / DblMetaph / NYSIIS / MRA) per CONTEXT.md §8 with golden stdout fixture regenerated, ship the new `examples/phonetic-keys/main.go` educational program + companion `main_test.go` per CONTEXT.md §8 demonstrating the encoded-key surfaces standalone, ship the new `tests/bdd/features/monge_elkan_phonetic_inner.feature` covering ME-over-{Soundex, DM, NYSIIS, MRA} binary-inner composition per CONTEXT.md §4, and confirm `ai_friendly_test.go` enforces llms.txt + llms-full.txt sync for the 9 new Phase 7 exported symbols already synced per-plan. Closes the per-algorithm portion of Phase 7 (PHON-01..PHON-04 closed by 07-01..07-04 respectively; this plan ships the catalogue-wide artifacts that depend on all four algorithms existing).

Purpose: per CONTEXT.md §8 + RESEARCH.md §"Per-algorithm Implementation Notes" + PATTERNS.md "examples/identifier-similarity/main.go" / "examples/phonetic-keys/main.go" sections, the finalisation plan exists to materialise the catalogue-wide artifacts: merged golden, regenerated bench baseline, extended identifier-similarity example (the user-facing "look — 23 algorithms!" demo), new phonetic-keys example program (the user-facing "look — encoded keys!" demo), and the ME-phonetic-inner BDD coverage. All artifacts depend on plans 07-01..07-04 completing; this plan runs in Wave 3 strictly after Wave 2.

Output: 1 merged golden file (algorithms.json); 1 regenerated bench baseline; 2 modified example files (identifier-similarity main.go + main_test.go); 2 new example files (phonetic-keys main.go + main_test.go); 1 new BDD feature; 1 modified meta-test (ai_friendly_test.go — extension or creation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-phonetic-algorithms/07-CONTEXT.md
@.planning/phases/07-phonetic-algorithms/07-RESEARCH.md
@.planning/phases/07-phonetic-algorithms/07-VALIDATION.md
@.planning/phases/07-phonetic-algorithms/07-PATTERNS.md
@.planning/phases/07-phonetic-algorithms/07-01-SUMMARY.md
@.planning/phases/07-phonetic-algorithms/07-02-SUMMARY.md
@.planning/phases/07-phonetic-algorithms/07-03-SUMMARY.md
@.planning/phases/07-phonetic-algorithms/07-04-SUMMARY.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/performance-standards/SKILL.md
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/go-testing-standards/SKILL.md

<interfaces>
Post-07-04 catalogue state (this plan finalises):
- 23 algorithms in dispatch table (Phases 1-6 = 19 + 4 phonetic from 07-01..07-04)
- 18 entries in permittedMongeElkanInner (FINAL)
- 4 _staging/*.json files awaiting merge into algorithms.json
- 9 new exported symbols (SoundexCode, SoundexScore, DoubleMetaphoneKeys, DoubleMetaphoneScore, NYSIISCode, NYSIISScore, MRACode, MRACompare, MRAScore) — all synced into llms.txt + llms-full.txt per-plan

From examples/identifier-similarity/main.go post-Phase 6:
- 19 columns; algoWidth = 13 (longest label fits)
- Phase 7 appends 4 columns: Soundex / DblMetaph / NYSIIS / MRA (DblMetaph is 9 chars, fits in 13)
- main_test.go has a `want` const fixture for golden stdout comparison
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Merge _staging/* into algorithms.json + regenerate bench.txt + finalise meta-tests</name>
  <files>testdata/golden/algorithms.json, bench.txt, ai_friendly_test.go</files>
  <read_first>
    - testdata/golden/algorithms.json (existing 19-algorithm × 144-entry file from Phase 6)
    - testdata/golden/_staging/soundex.json (from plan 07-01 — 10 entries)
    - testdata/golden/_staging/double_metaphone.json (from plan 07-02 — 10 entries)
    - testdata/golden/_staging/nysiis.json (from plan 07-03 — 10 entries)
    - testdata/golden/_staging/mra.json (from plan 07-04 — 10 entries)
    - algorithms_golden_test.go (search for TestGolden_Algorithms_Merge or equivalent — Phase 6 finalisation pattern; the existing merge logic the planner reuses)
    - bench.txt (existing Phase 6 baseline)
    - ai_friendly_test.go (search for TestLLMsTxt — Phase 5/6 sync-check meta-test; this plan extends or confirms coverage)
    - llms.txt + llms-full.txt (post-07-04 state — all 9 Phase 7 exported symbols already present per per-plan sync)
    - .claude/skills/determinism-standards/SKILL.md (canonical marshal: 2-space indent + LF trailing + alphabetical key ordering)
    - .claude/skills/performance-standards/SKILL.md (benchstat regression threshold >10% fails; bench.txt full-replace convention)
  </read_first>
  <behavior>
    - testdata/golden/algorithms.json grows from 19 algorithms × ~144 entries to 23 algorithms × ~180 entries (the four staging files contribute 10 entries each).
    - Entries within each algorithm group sorted by `name`; algorithms sorted alphabetically; canonical marshal (2-space indent + single trailing LF) — byte-identical across the CI matrix.
    - The existing TestGolden_Algorithms_Merge (or equivalent) test passes; if the Phase 6 merge logic auto-discovers staging files, the merge happens by simply committing the new staging files (already present from 07-01..07-04) and re-running the test. If the merge is manual, this task performs it explicitly.
    - bench.txt regenerated full-replace via `make bench` (or `go test -run none -bench . -benchmem -count=10 ./... > bench.txt`); includes the 4 new phonetic benchmarks plus carry-forward Phase 2-6 benchmarks. benchstat -compare with the previous Phase 6 baseline detects no >10% regression on carry-forward.
    - ai_friendly_test.go walks `go/ast` over the package's exported symbols; confirms each appears in BOTH llms.txt AND llms-full.txt. Phase 7's 9 new exports (SoundexCode, SoundexScore, DoubleMetaphoneKeys, DoubleMetaphoneScore, NYSIISCode, NYSIISScore, MRACode, MRACompare, MRAScore) all pass — they were synced per-plan in 07-01..07-04 already, so this task's job is to verify the sync coverage, NOT to add the entries (already added).
  </behavior>
  <action>
    Run the existing golden-merge regeneration path (Phase 6 finalisation pattern). If `TestGolden_Algorithms_Merge` auto-merges from `_staging/`, simply ensure all four 07-01..07-04 staging files are present and re-run the test — the test FAILS on first run with a clear diff, and the regeneration step writes the merged `algorithms.json`. If the merge is manual: read each _staging/*.json + the existing algorithms.json; concatenate the entries arrays; sort by `(algorithm, name)`; canonical-marshal via encoding/json MarshalIndent("", "  ") with a single trailing LF; write back to algorithms.json.

    Regenerate `bench.txt`: run `make bench` (or directly `go test -run none -bench . -benchmem -count=10 ./...` — match Phase 6 invocation in Makefile) and capture the output. Commit `bench.txt` full-replace including the new BenchmarkSoundex / BenchmarkDoubleMetaphone / BenchmarkNYSIIS / BenchmarkMRA entries. Compare against the Phase 6 baseline (via `git diff bench.txt` or `benchstat <old> <new>`): assert no >10% regression on carry-forward benchmarks (Levenshtein / Hamming / Jaro / etc.).

    Verify `ai_friendly_test.go` `TestLLMsTxt` covers Phase 7 — read the file; if it walks `go/ast` and asserts coverage in llms.txt + llms-full.txt, simply run it and confirm green. If it needs extending to cover the 3-public-function MRA surface (where the catalogue expects per-symbol entries for MRACode, MRACompare, MRAScore), update the walk logic. The test must pass on the post-07-04 state without modification IF Phase 5/6 already implemented the go/ast walk; this task confirms that's the case.
  </action>
  <verify>
    <automated>cd /Users/johnny/Development/fuzzymatch &amp;&amp; go test -race -shuffle=on -count=1 -run 'TestGolden_Algorithms|TestLLMsTxt|TestPhoneticCodesGolden|TestPhonetic_CrossValidation' ./... &amp;&amp; make verify-determinism &amp;&amp; test -s bench.txt &amp;&amp; grep -q BenchmarkSoundex bench.txt &amp;&amp; grep -q BenchmarkDoubleMetaphone bench.txt &amp;&amp; grep -q BenchmarkNYSIIS bench.txt &amp;&amp; grep -q BenchmarkMRA bench.txt</automated>
  </verify>
  <acceptance_criteria>
    - Satisfies VALIDATION.md rows 07-05-01, 07-05-02, 07-05-06.
    - `testdata/golden/algorithms.json` contains entries for all 4 phonetic algorithms (Soundex / DoubleMetaphone / NYSIIS / MRA) merged from `_staging/*.json`.
    - `TestGolden_Algorithms` (or the existing merge-verifier) passes.
    - `make verify-determinism` passes — algorithms.json + phonetic-codes.json diff byte-identically across the CI matrix.
    - `bench.txt` contains the 4 new phonetic benchmarks; `grep` confirms each.
    - benchstat regression check (against Phase 6 baseline) reports no >10% regression on carry-forward benchmarks.
    - `TestLLMsTxt` (ai_friendly_test.go) passes covering all 9 new Phase 7 exported symbols.
  </acceptance_criteria>
  <done>algorithms.json merged with all 4 phonetic algorithms; bench.txt regenerated with 4 new benchmarks; ai_friendly_test.go meta-test confirms llms.txt + llms-full.txt sync coverage. Cross-platform determinism gates hold.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend identifier-similarity example (19 → 23 columns) + ship new phonetic-keys example + BDD coverage for ME-phonetic-inner composition</name>
  <files>examples/identifier-similarity/main.go, examples/identifier-similarity/main_test.go, examples/phonetic-keys/main.go, examples/phonetic-keys/main_test.go, tests/bdd/features/monge_elkan_phonetic_inner.feature</files>
  <read_first>
    - examples/identifier-similarity/main.go (current 19-column state; the `algorithms` slice at lines 110-130 per PATTERNS.md; the file-doc text mentioning "nineteen Phase 2 + 3 + 4 + 5 + 6 ... algorithms")
    - examples/identifier-similarity/main_test.go (current `want` const fixture at lines 42-51 per PATTERNS.md; the TestExample_Output + TestExample_ColumnWidths shapes)
    - tests/bdd/features/soundex.feature (Phase 7 plan 07-01 — analog for new monge_elkan_phonetic_inner.feature)
    - tests/bdd/features/double_metaphone.feature (Phase 7 plan 07-02)
    - tests/bdd/features/nysiis.feature (Phase 7 plan 07-03)
    - tests/bdd/features/mra.feature (Phase 7 plan 07-04)
    - monge_elkan_test.go (the four TestMongeElkanScore_BinaryInner_{Soundex, DoubleMetaphone, NYSIIS, MRA} tests already shipped — the BDD scenarios mirror these)
    - .planning/phases/07-phonetic-algorithms/07-CONTEXT.md §8 (identifier-similarity extension LOCKED; new phonetic-keys example program)
    - .planning/phases/07-phonetic-algorithms/07-CONTEXT.md §4 (ME-phonetic-inner binary composition fixture)
    - .planning/phases/07-phonetic-algorithms/07-PATTERNS.md examples/identifier-similarity/main.go section (append 4 entries to algorithms slice; update file-doc text; algoWidth=13 unchanged because DblMetaph fits)
    - .planning/phases/07-phonetic-algorithms/07-PATTERNS.md examples/phonetic-keys/main.go section (new program; column layout; curated name slice)
    - examples/identifier-similarity/main_test.go (the os.Pipe stdout-capture pattern for TestExample_Output)
  </read_first>
  <behavior>
    - examples/identifier-similarity/main.go updated:
      - File-doc text updated from "nineteen Phase 2 + 3 + 4 + 5 + 6 character-based, gestalt, q-gram, and token-based algorithms" → "twenty-three Phase 2 + 3 + 4 + 5 + 6 + 7 character-based, gestalt, q-gram, token-based, and phonetic algorithms".
      - `algorithms` slice at lines 110-130 appended with 4 new entries: `{"Soundex", fuzzymatch.SoundexScore}, {"DblMetaph", fuzzymatch.DoubleMetaphoneScore}, {"NYSIIS", fuzzymatch.NYSIISScore}, {"MRA", fuzzymatch.MRAScore}` per CONTEXT.md §8 + PATTERNS.md.
      - `algoWidth = 13` constant UNCHANGED (longest new label `DblMetaph` is 9 chars, fits).
    - examples/identifier-similarity/main_test.go `want` constant regenerated by running `go run ./examples/identifier-similarity` and pasting the new stdout — the test structure (TestExample_Output + TestExample_ColumnWidths) unchanged.
    - examples/phonetic-keys/main.go NEW program per CONTEXT.md §8 + PATTERNS.md. Apache header (13 lines). Package main. File-doc paragraph documenting the educational purpose. Curated `names` slice: `[]string{"Robert", "Rupert", "Tymczak", "Schmidt", "Smith", "Catherine", "Katherine", "Brown", "Browne", "Pacheco", "Byrne", "Boern"}`. main() prints a header row (Name | Soundex | DM-pri | DM-sec | NYSIIS | MRA) then iterates `names`: for each, prints SoundexCode + DoubleMetaphoneKeys (split into pri/sec) + NYSIISCode + MRACode. Also demonstrates MRACompare per RESEARCH.md Open Question 7 recommendation (yes, include MRACompare) — separate section at end of output: "MRACompare examples:" + a few pairs with `(matched, sim)` printed.
    - examples/phonetic-keys/main_test.go: TestExample_Output captures stdout via os.Pipe (analog from identifier-similarity main_test.go); asserts byte-equal to a `want` const; TestExample_ColumnWidths verifies column alignment per the format string.
    - tests/bdd/features/monge_elkan_phonetic_inner.feature has minimum 4 scenarios per CONTEXT.md §4 — one per phonetic inner — each asserting `MongeElkanScore("alpha beta", "alpha gamma", AlgoXxx, opts) == 0.5` with `Xxx ∈ {Soundex, DoubleMetaphone, NYSIIS, MRA}`. Plus optionally 2 more scenarios covering "both tokens match" and "neither matches" composition cases per RESEARCH.md.
  </behavior>
  <action>
    Update `examples/identifier-similarity/main.go`:
      1. Edit file-doc paragraph: replace "nineteen Phase 2 + 3 + 4 + 5 + 6 character-based, gestalt, q-gram, and token-based algorithms" with "twenty-three Phase 2 + 3 + 4 + 5 + 6 + 7 character-based, gestalt, q-gram, token-based, and phonetic algorithms".
      2. Append 4 entries to the `algorithms` slice at the documented location: `{"Soundex", fuzzymatch.SoundexScore}, {"DblMetaph", fuzzymatch.DoubleMetaphoneScore}, {"NYSIIS", fuzzymatch.NYSIISScore}, {"MRA", fuzzymatch.MRAScore}`.
      3. Add a comment line above the appended entries: `// Phase 7 phonetic tier (binary 0/1 scores):`.

    Update `examples/identifier-similarity/main_test.go`:
      1. Run `go run ./examples/identifier-similarity` from a fresh shell after step 1 above is committed.
      2. Capture the new stdout output (table now has 23 columns).
      3. Replace the existing `want` const with the captured output (preserving the const declaration syntax).
      4. Verify `go test ./examples/identifier-similarity/...` passes.

    Create `examples/phonetic-keys/main.go` (new program — minimum 30 LOC). Apache header (13 lines from analog). Package main godoc paragraph documenting the educational purpose ("demonstrates the four Phase 7 phonetic encoding functions on a curated set of English-language surname samples; complements the score-focused identifier-similarity example by showing the encoded-key surfaces standalone"). Imports `fmt`, `strings`, `github.com/axonops/fuzzymatch`. Curated `var names = []string{"Robert", "Rupert", "Tymczak", "Schmidt", "Smith", "Catherine", "Katherine", "Brown", "Browne", "Pacheco", "Byrne", "Boern"}` per RESEARCH.md §3 reference vectors. main() prints:
      1. Header `Name | Soundex | DM-pri | DM-sec | NYSIIS | MRA` formatted via `fmt.Printf("%-12s %-8s %-8s %-8s %-8s %-8s\n", ...)`.
      2. Separator row `strings.Repeat("-", 60)`.
      3. For each name: call SoundexCode + DoubleMetaphoneKeys (destructure to pri, sec) + NYSIISCode + MRACode; print formatted row.
      4. Blank line + "MRACompare examples:" header.
      5. A short list of 3-4 pairs: `MRACompare("Byrne", "Boern")`, `MRACompare("Smith", "Smyth")`, `MRACompare("Catherine", "Katherine")`, `MRACompare("Ad", "ZachariahMontgomery")` — for each, print `<a> vs <b>: matched=<bool> sim=<int>`.

    Create `examples/phonetic-keys/main_test.go` (new). Mirror examples/identifier-similarity/main_test.go structure:
      1. `want` const with the captured stdout (run `go run ./examples/phonetic-keys` and paste).
      2. `TestExample_Output` captures stdout via os.Pipe; asserts byte-equal to want.
      3. `TestExample_ColumnWidths` asserts column-width assumptions hold (or skip this test if columns aren't fixed-width — the format string IS fixed-width so this test should be straightforward).
    Verify `go test ./examples/phonetic-keys/...` passes.

    Create `tests/bdd/features/monge_elkan_phonetic_inner.feature` (new). Feature header: `Feature: Monge-Elkan composition with phonetic inner algorithms`. Minimum 4 scenarios (one per phonetic inner) covering the CONTEXT.md §4 fixture pattern:
      - Scenario "Monge-Elkan over Soundex — one token matches": Given inputs "alpha beta" and "alpha gamma" with inner AlgoSoundex, When MongeElkanScore is computed, Then the score should be exactly 0.5.
      - Scenario "Monge-Elkan over Double Metaphone — one token matches": same shape with AlgoDoubleMetaphone.
      - Scenario "Monge-Elkan over NYSIIS — one token matches": same shape with AlgoNYSIIS.
      - Scenario "Monge-Elkan over MRA — one token matches": same shape with AlgoMRA.
    Optional 2 extra scenarios: "Monge-Elkan over Soundex — both tokens match" (1.0) and "Monge-Elkan over Soundex — neither token matches" (0.0) per the unit-test fixture.

    NOTE: the existing tests/bdd/steps/algorithms_steps.go already supports `MongeElkanScore` step verbs from Phase 6 + the binary-inner-composition step pattern; if the existing steps don't handle the `with inner AlgoXxx` parameter, extend `algorithms_steps.go` with a step `^I compute the Monge-Elkan score between "([^"]*)" and "([^"]*)" with inner ([A-Za-z]+)$` mapping the inner-name string to a fuzzymatch.AlgoID via a name lookup table.
  </action>
  <verify>
    <automated>cd /Users/johnny/Development/fuzzymatch &amp;&amp; go build ./... &amp;&amp; go vet ./... &amp;&amp; go test -race -shuffle=on -count=1 ./examples/... &amp;&amp; go test -race -shuffle=on -count=1 ./... &amp;&amp; make test-bdd &amp;&amp; grep -q 'fuzzymatch.SoundexScore' examples/identifier-similarity/main.go &amp;&amp; grep -q 'fuzzymatch.MRACompare' examples/phonetic-keys/main.go &amp;&amp; test -f tests/bdd/features/monge_elkan_phonetic_inner.feature</automated>
  </verify>
  <acceptance_criteria>
    - Satisfies VALIDATION.md rows 07-05-03, 07-05-04, 07-05-05.
    - `examples/identifier-similarity/main.go` has 23 columns (was 19); 4 new entries in `algorithms` slice; file-doc text updated.
    - `examples/identifier-similarity/main_test.go` `want` const regenerated; `go test ./examples/identifier-similarity/...` passes.
    - `examples/phonetic-keys/main.go` exists; demonstrates SoundexCode + DoubleMetaphoneKeys + NYSIISCode + MRACode + MRACompare; `go run ./examples/phonetic-keys` produces a table of curated names + their encoded forms + MRACompare examples.
    - `examples/phonetic-keys/main_test.go` exists with golden stdout `want` const; `go test ./examples/phonetic-keys/...` passes.
    - `tests/bdd/features/monge_elkan_phonetic_inner.feature` exists with minimum 4 scenarios (one per phonetic inner) per CONTEXT.md §4.
    - `make test-bdd` green including the new feature file.
    - `go test ./...` green including all per-algorithm + meta + cross-validation + golden tests from plans 07-01..07-04.
  </acceptance_criteria>
  <done>identifier-similarity extended to 23 columns; new phonetic-keys example program ships with golden stdout fixture; ME-phonetic-inner BDD coverage shipped; all per-plan llms.txt sync verified by ai_friendly_test.go; PHON-01..PHON-04 fully closed end-to-end at the catalogue-wide artifact level.</done>
</task>

</tasks>

<verification>
- `make check` green — full quality gate (golangci-lint v2 + vet + race + vulncheck + gosec + markdownlint + tidy-check + coverage-check enforcing ≥95% overall / ≥90% per file / 100% public API).
- `make test-bdd` green including soundex.feature + double_metaphone.feature + nysiis.feature + mra.feature + monge_elkan_phonetic_inner.feature (all 5 Phase 7 BDD features).
- `make verify-determinism` green — algorithms.json + phonetic-codes.json both diff byte-identically on the CI matrix (linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64).
- `make verify-deps-allowlist` green — root `go.mod` zero non-stdlib `require` lines beyond `golang.org/x/text`.
- `make bench` runs the new phonetic benchmarks; benchstat detects no >10% regression vs Phase 6 baseline.
- `make regen-phonetic-cross-validation` regenerates `vectors.json` byte-identically when both pins installed (developer-side verification).
</verification>

<success_criteria>
Phase 7 complete:
1. PHON-01 (Soundex) closed by plan 07-01.
2. PHON-02 (Double Metaphone) closed by plan 07-02 with `algorithm-licensing-reviewer` sign-off + `--depth=deep` review recorded.
3. PHON-03 (NYSIIS) closed by plan 07-03 with Knuth+Taft citation + Taft-1970 truncation gate + variant_divergence mechanism handling ~40-60% of NYSIIS corpus.
4. PHON-04 (MRA) closed by plan 07-04 with three public surfaces (MRACode + MRACompare `(bool, int)` + MRAScore) + threshold table at var + Pitfall 7.C clamp documented + PropMRA_ThresholdMonotonic green.
5. testdata/golden/algorithms.json contains 23 algorithms × ~180 entries; cross-platform byte-stable.
6. testdata/golden/phonetic-codes.json contains entries for all 4 phonetic algorithms; cross-platform byte-stable.
7. bench.txt regenerated full-replace including 4 new phonetic benchmarks.
8. examples/identifier-similarity/main.go extends to 23 columns; main_test.go regenerated.
9. examples/phonetic-keys/main.go + main_test.go ship as new educational example program.
10. tests/bdd/features/monge_elkan_phonetic_inner.feature covers ME-over-{Soundex, DM, NYSIIS, MRA} binary-inner composition.
11. ai_friendly_test.go confirms all 9 Phase 7 exported symbols synced to llms.txt + llms-full.txt (per-plan sync from 07-01..07-04 already complete).
12. permittedMongeElkanInner at FINAL 18 entries; monge_elkan_test.go rejected slice at FINAL 5 entries; all 4 TestMongeElkanScore_BinaryInner_<Algo> tests green.
13. Coverage targets met (≥ 95% / ≥ 90% / 100% public API).
14. Root `go.mod` still zero-non-stdlib-require-lines beyond `golang.org/x/text`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-phonetic-algorithms/07-05-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md` with:
- Status: COMPLETE — Phase 7 finalised
- Closes (catalogue-wide artifacts): all 4 of PHON-01, PHON-02, PHON-03, PHON-04 end-to-end
- Catalogue state: 23 algorithms × ~180 algorithms.json entries; permittedMongeElkanInner at FINAL 18; rejected slice at FINAL 5
- Examples shipped: identifier-similarity extended (19 → 23 columns); new phonetic-keys educational program
- BDD coverage: 5 new feature files (soundex, double_metaphone, nysiis, mra, monge_elkan_phonetic_inner)
- bench.txt regenerated; benchstat no-regression check passed
- Coverage targets met (≥ 95% overall / ≥ 90% per file / 100% public API)
- Cross-platform determinism: algorithms.json + phonetic-codes.json both byte-stable on the CI matrix
- Ready for Phase 8 (Composite Scorer)
</output>
