---
phase: 07-phonetic-algorithms
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - double_metaphone.go
  - double_metaphone_test.go
  - double_metaphone_bench_test.go
  - double_metaphone_fuzz_test.go
  - dispatch_double_metaphone.go
  - phonetic_cross_validation_test.go
  - phonetic_codes_golden_test.go
  - testdata/cross-validation/phonetic/vectors.json
  - testdata/golden/phonetic-codes.json
  - testdata/golden/_staging/double_metaphone.json
  - scripts/gen-phonetic-cross-validation.py
  - tests/bdd/features/double_metaphone.feature
  - tests/bdd/steps/algorithms_steps.go
  - monge_elkan.go
  - monge_elkan_test.go
  - props_test.go
  - example_test.go
  - llms.txt
  - llms-full.txt
autonomous: true
requirements: [PHON-02]
tags: [phonetic, double-metaphone, licensing-load-bearing, deep-review]
must_haves:
  truths:
    - "User can call DoubleMetaphoneKeys(s) and receive a deterministic (primary, secondary string) tuple of ASCII chars from `[A-Z0]` (the `0` represents the 'th' sound)."
    - "DoubleMetaphoneKeys(\"Schmidt\") == (\"XMT\", \"SMT\") (Germanic gate)."
    - "DoubleMetaphoneKeys(\"Smith\") == (\"SM0\", \"XMT\") (Germanic — shared XMT with Schmidt)."
    - "DoubleMetaphoneScore(\"Schmidt\", \"Smith\") == 1.0 (XMT cross-match)."
    - "DoubleMetaphoneKeys(\"Catherine\") == DoubleMetaphoneKeys(\"Katherine\") == (\"K0RN\", \"KTRN\") (Greek gate — exact match)."
    - "DoubleMetaphoneKeys(\"Pacheco\")[0] contains \"PXK\" (Romance/Spanish gate)."
    - "At least one Slavic-origin vector (Wojcik or Sczepanski) passes the language-branch checklist."
    - "At least one Chinese-origin vector (Cheung or Wong) passes the language-branch checklist."
    - "DoubleMetaphoneScore(a, b) == 1.0 iff ANY of {primary_a == primary_b, primary_a == secondary_b, secondary_a == primary_b, secondary_a == secondary_b} where the matched key is non-empty."
    - "Root `go.mod` remains zero-non-stdlib-`require` lines."
    - "Source-Origin Statement at top of double_metaphone.go EXTENDS Phase 4 WR-01 format per CONTEXT.md §3 with TWO additional lines: (1) rule-table provenance line citing SWI-Prolog double_metaphone.c URL; (2) MIT-Go-port negative-attribution line listing 5 forbidden Go ports."
    - "algorithm-licensing-reviewer sign-off recorded in PR description; --depth=deep code review completed on plan 07-02 PR per CONTEXT.md §3 — LOAD-BEARING licensing gate for Phase 7."
    - "permittedMongeElkanInner map gains AlgoDoubleMetaphone: true (15 → 16); monge_elkan_test.go panic-test rejected slice shrinks 8 → 7 in lockstep."
    - "llms.txt + llms-full.txt synced WITHIN this plan for DoubleMetaphoneKeys and DoubleMetaphoneScore."
    - "testdata/golden/phonetic-codes.json appended with DoubleMetaphone section (8-12 entries asserting primary+secondary string equality)."
    - "DM cross-validation corpus appended to testdata/cross-validation/phonetic/vectors.json with 40 entries spanning all 5 language branches; loader asserts count >= 7 per major branch and >= 4 Chinese-origin."
  artifacts:
    - path: "double_metaphone.go"
      provides: "DoubleMetaphoneKeys(s string) (primary, secondary string) + DoubleMetaphoneScore(a, b string) float64; 350-450 LOC imperative state machine; ~200 conditional branches; 5 language-origin branches; 4-char key truncation"
      contains: "func DoubleMetaphoneKeys"
      min_lines: 300
    - path: "double_metaphone_test.go"
      provides: "Mandatory language-branch checklist test (TestDoubleMetaphoneKeys_LanguageBranches); RV-DM1..RV-DM20 literature catalogue; identity / both-empty / one-empty; XMT cross-match pin; four-way key matching coverage"
    - path: "double_metaphone_bench_test.go"
      provides: "Allocation-aware benchmarks at three ASCII sizes; budget < 2 µs, ≤ 2 allocations"
    - path: "double_metaphone_fuzz_test.go"
      provides: "FuzzDoubleMetaphone with ASCII + non-ASCII silent-skip seeds; CHARSET invariant on BOTH keys: ^[A-Z0]{0,4}$"
    - path: "dispatch_double_metaphone.go"
      provides: "var _ = func() bool { dispatch[AlgoDoubleMetaphone] = DoubleMetaphoneScore; return true }()"
    - path: "scripts/gen-phonetic-cross-validation.py"
      provides: "Updated with active DM generation; imports metaphone (Andrew Collins port); 40 DM entries with branch tags"
    - path: "testdata/cross-validation/phonetic/vectors.json"
      provides: "DM section populated with 40 entries each carrying branch field"
    - path: "phonetic_cross_validation_test.go"
      provides: "DM sub-test ENABLED with branch-count guard; per-entry primary+secondary equality"
    - path: "testdata/golden/phonetic-codes.json"
      provides: "DoubleMetaphone section appended (8-12 entries with primary + secondary fields)"
    - path: "phonetic_codes_golden_test.go"
      provides: "DM branch activated — asserts both primary AND secondary key equality"
    - path: "testdata/golden/_staging/double_metaphone.json"
      provides: "10 staging-golden Score entries to be merged by plan 07-05"
    - path: "monge_elkan.go"
      provides: "permittedMongeElkanInner gains AlgoDoubleMetaphone: true (15 → 16 entries)"
      contains: "AlgoDoubleMetaphone: true"
    - path: "monge_elkan_test.go"
      provides: "rejected slice shrinks 8 → 7; new TestMongeElkanScore_BinaryInner_DoubleMetaphone with three cases"
    - path: "props_test.go"
      provides: "Five-invariant property tests for DoubleMetaphoneScore + PropDoubleMetaphone_KeyCharset"
    - path: "example_test.go"
      provides: "ExampleDoubleMetaphoneKeys + ExampleDoubleMetaphoneScore"
    - path: "tests/bdd/features/double_metaphone.feature"
      provides: "≥ 6 scenarios — minimum 1 per language branch + Schmidt-Smith XMT cross-match scenario"
    - path: "tests/bdd/steps/algorithms_steps.go"
      provides: "Append DM step.Step registrations + new shared step `^the keys should be ... and ...$`"
    - path: "llms.txt"
      provides: "### Double Metaphone (Philips 2000) heading + DoubleMetaphoneKeys + DoubleMetaphoneScore lines"
    - path: "llms-full.txt"
      provides: "Full Phase 7 DM algorithm-surface block"
  key_links:
    - from: "double_metaphone.go Source-Origin Statement"
      to: "Philips 2000 C/C++ Users Journal + SWI-Prolog/packages-nlp double_metaphone.c"
      via: "rule-table provenance line + MIT-Go-port negative-attribution line per CONTEXT.md §3"
      pattern: "github.com/SWI-Prolog/packages-nlp"
    - from: "dispatch_double_metaphone.go"
      to: "dispatch[AlgoDoubleMetaphone]"
      via: "var _ = func() bool {...}() registration"
      pattern: "dispatch\\[AlgoDoubleMetaphone\\] = DoubleMetaphoneScore"
    - from: "phonetic_cross_validation_test.go DoubleMetaphone branch"
      to: "testdata/cross-validation/phonetic/vectors.json DM entries"
      via: "switch e.Algorithm case 'DoubleMetaphone' asserts primary+secondary equality; branch-count guard >= 7 per major branch, >= 4 Chinese-origin"
      pattern: "DoubleMetaphoneKeys"
    - from: "monge_elkan.go permittedMongeElkanInner"
      to: "monge_elkan_test.go panic-test fixture"
      via: "lockstep mutation per CONTEXT.md §4"
      pattern: "AlgoDoubleMetaphone: true"
---

<objective>
Land the load-bearing licensing-risk plan of Phase 7: Double Metaphone (Philips 2000) implemented end-to-end from primary source + the SWI-Prolog public-domain C reference redistribution. ~350-450 LOC imperative state machine across 5 language-origin branches (Germanic / Slavic / Romance / Greek / Chinese-origin); ~200 conditional rules. Returns primary + secondary keys (4 chars each, charset `[A-Z0]` where `0` is the conventional 'th' sound digit). Mandatory language-branch checklist test gates the implementation (Germanic Schmidt/Smith XMT-match + Greek Catherine=Katherine + Romance Pacheco PXK + ≥1 Slavic + ≥1 Chinese-origin). Source-Origin Statement EXTENDS Phase 4 WR-01 format with (1) rule-table provenance line citing the SWI-Prolog public-domain C source URL, (2) MIT-Go-port negative-attribution line naming 5 forbidden ports. PR description records `algorithm-licensing-reviewer sign-off:` line AND `--depth=deep code review:` line per CONTEXT.md §3 LOCKED. Closes PHON-02.

Purpose: this is the licensing-gated plan. The phase's discipline risk is licensing (Double Metaphone fresh-transcription without copying from the 5 MIT-Go ports — Pitfall 5's primary attractive-nuisance), not security or correctness alone. The `--depth=deep` review is targeted here because the cost-where-the-risk-lives principle applies. Cross-validation reference is the BSD-licensed `metaphone==0.6` (oubiwann's Andrew Collins port) — NOT jellyfish, which has NO Double Metaphone module per RESEARCH.md key finding 1.

Output: 1 new ~400-LOC algorithm file + 3 companion test files + 1 dispatch wiring file; updates to scripts/gen-phonetic-cross-validation.py (DM section activated); appended entries to vectors.json (40 DM entries with branch tags) + phonetic-codes.json (DM section) + _staging/double_metaphone.json; new BDD feature; appended step registrations + new DM-specific keys-equality step; appended Five-invariant block + PropDoubleMetaphone_KeyCharset; 2 new ExampleXxx; monge_elkan.go + monge_elkan_test.go mutated 15→16 / 8→7 in lockstep + TestMongeElkanScore_BinaryInner_DoubleMetaphone; llms.txt + llms-full.txt synced in-plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-phonetic-algorithms/07-CONTEXT.md
@.planning/phases/07-phonetic-algorithms/07-RESEARCH.md
@.planning/phases/07-phonetic-algorithms/07-VALIDATION.md
@.planning/phases/07-phonetic-algorithms/07-PATTERNS.md
@.planning/phases/07-phonetic-algorithms/07-01-SUMMARY.md
@.claude/skills/algorithm-licensing-standards/SKILL.md
@.claude/skills/algorithm-correctness-standards/SKILL.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/performance-standards/SKILL.md
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/go-testing-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
From soundex.go (Phase 7 plan 07-01 — analog and reference for file structure):
- Apache header 13 lines + package godoc with non-ASCII silent-skip warning copied from CONTEXT.md §5
- Source-Origin Statement Phase 4 WR-01 format

From algoid.go: AlgoDoubleMetaphone AlgoID = 24 already reserved (slot 24 in dispatch table).

From monge_elkan.go post-07-01: permittedMongeElkanInner has 15 entries (with AlgoSoundex newly added).

From monge_elkan_test.go post-07-01: rejected slice has 8 entries (AlgoDoubleMetaphone, AlgoNYSIIS, AlgoMRA, plus 5 base token-tier rejected).

Target signatures (this plan ships DM):
- func DoubleMetaphoneKeys(s string) (primary, secondary string)  // 4 chars each; charset [A-Z0]
- func DoubleMetaphoneScore(a, b string) float64                   // binary 0.0 / 1.0

Key match rule (RESEARCH.md §1.2 + docs/requirements.md §7.4.2 line 660):
Score == 1.0 iff ANY pair (primary_a or secondary_a) matches ANY pair (primary_b or secondary_b) AND the matched key is non-empty.

Language-branch checklist (CONTEXT.md §3 mandatory):
- Germanic: Schmidt → (XMT, SMT); Smith → (SM0, XMT); Score(Schmidt, Smith) == 1.0
- Slavic: ≥1 vector (e.g. Wojcik)
- Romance: Pacheco's primary or secondary contains PXK
- Greek: Catherine == Katherine == (K0RN, KTRN) exact
- Chinese-origin: ≥1 vector (e.g. Cheung)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Double Metaphone end-to-end (algorithm + tests + bench + fuzz + dispatch + cross-validation + golden + ME wiring + props + example + BDD + llms.txt sync)</name>
  <files>double_metaphone.go, double_metaphone_test.go, double_metaphone_bench_test.go, double_metaphone_fuzz_test.go, dispatch_double_metaphone.go, monge_elkan.go, monge_elkan_test.go, props_test.go, example_test.go, tests/bdd/features/double_metaphone.feature, tests/bdd/steps/algorithms_steps.go, testdata/golden/_staging/double_metaphone.json, scripts/gen-phonetic-cross-validation.py, testdata/cross-validation/phonetic/vectors.json, testdata/golden/phonetic-codes.json, phonetic_cross_validation_test.go, phonetic_codes_golden_test.go, llms.txt, llms-full.txt</files>
  <read_first>
    - soundex.go (Phase 7 plan 07-01 — exact structural analog for Apache header + Source-Origin Statement format; the EXTENDED template here adds two MORE lines)
    - dispatch_soundex.go (Phase 7 plan 07-01 — exact analog for dispatch wiring)
    - soundex_test.go (Phase 7 plan 07-01 — analog for unit test structure and file-doc paragraph)
    - soundex_bench_test.go (Phase 7 plan 07-01 — analog for `var sink` gate pattern)
    - soundex_fuzz_test.go (Phase 7 plan 07-01 — analog with charset invariant)
    - ratcliff_obershelp.go (Phase 4 WR-01 format reference at lines 1-115)
    - strcmp95.go lines 17-32 (alternate Source-Origin Statement canonical reference)
    - monge_elkan.go lines 294-314 (permittedMongeElkanInner — now 15 entries after 07-01)
    - monge_elkan_test.go lines 313-323 (rejected slice — now 8 entries after 07-01)
    - props_test.go (Phase 7 plan 07-01 Soundex five-invariant block — exact analog)
    - tests/bdd/features/soundex.feature (Phase 7 plan 07-01 — analog feature structure; DM extends to ≥ 6 scenarios)
    - tests/bdd/steps/algorithms_steps.go (Phase 7 plan 07-01 already added lastDMPrimary + lastDMSecondary state fields)
    - scripts/gen-phonetic-cross-validation.py (Phase 7 plan 07-01 — DM section currently stubbed; activate here)
    - testdata/cross-validation/phonetic/vectors.json (Phase 7 plan 07-01 — DM array empty; populate with 40 entries)
    - testdata/golden/phonetic-codes.json (Phase 7 plan 07-01 — Soundex section present; append DM here)
    - phonetic_cross_validation_test.go (Phase 7 plan 07-01 — DM sub-test stubbed with t.Skip; activate here)
    - phonetic_codes_golden_test.go (Phase 7 plan 07-01 — DM branch stubbed with t.Skip; activate here)
    - testdata/golden/_staging/soundex.json (Phase 7 plan 07-01 — exact analog for staging schema)
    - .planning/phases/07-phonetic-algorithms/07-RESEARCH.md §1.2 (DM authoritative variant + jellyfish-has-no-DM)
    - .planning/phases/07-phonetic-algorithms/07-RESEARCH.md §2 (Double Metaphone Deep Dive — rule-table provenance + 5 language branches + MIT-Go-port negative attribution table + Source-Origin Statement template)
    - .planning/phases/07-phonetic-algorithms/07-RESEARCH.md §3.2 (RV-DM1..RV-DM20 reference vector catalogue)
    - .planning/phases/07-phonetic-algorithms/07-RESEARCH.md §6.2 (Per-algorithm implementation notes — 350-450 LOC, two strings.Builder, < 2 µs ≤ 2 allocs budget)
    - .planning/phases/07-phonetic-algorithms/07-RESEARCH.md §7 Pitfall 7.D (language-branch coverage)
    - .planning/phases/07-phonetic-algorithms/07-CONTEXT.md §3 (LOCKED single-plan + checklist + --depth=deep review + algorithm-licensing-reviewer sign-off)
    - .planning/phases/07-phonetic-algorithms/07-CONTEXT.md §5 (non-ASCII silent-skip canonical text)
    - .planning/phases/07-phonetic-algorithms/07-PATTERNS.md (double_metaphone.go Source-Origin Statement EXTENDED template; per-plan llms.txt sync section)
    - .claude/skills/algorithm-licensing-standards/SKILL.md (Source origin statement format; fresh-implementation discipline; negative-attribution; "For Double Metaphone specifically: Lawrence Philips' 2000 publication has no patent claim... Clear")
    - .claude/skills/algorithm-correctness-standards/SKILL.md
    - .claude/skills/fuzzymatch-review-protocol/SKILL.md (agent gate sequence; algorithm-licensing-reviewer is the gating reviewer here)
    - https://github.com/SWI-Prolog/packages-nlp/blob/master/double_metaphone.c (the canonical public-domain C reference — consulted ONLY for rule-table STRUCTURE READING; NO code copied, NO variable names or comment phrasing derived per algorithm-licensing-standards fresh-implementation discipline)
    - `metaphone==0.6` PyPI package (BSD-licensed Andrew Collins port — consulted ONLY for reference vectors via the cross-validation corpus)
  </read_first>
  <behavior>
    - DoubleMetaphoneKeys(s string) (primary, secondary string) returns two strings each truncated to 4 chars max, each consisting of chars from [A-Z0] (where 0 represents the 'th' sound digit per Philips 2000). Position-by-position imperative state machine over uppercased ASCII bytes; two strings.Builder outputs. Language-origin "mode" flags (isSlavoGermanic, isGermanic, isItalianGreek, etc.) detected in initial pre-scan. Non-ASCII runes SILENTLY skipped per CONTEXT.md §5. Empty input returns ("", "").
    - DoubleMetaphoneScore(a, b string) float64: identity short-circuit when a == b returns 1.0; otherwise computes both keys for each side; returns 1.0 iff ANY non-empty key of a matches ANY non-empty key of b (4-way match: pp / ps / sp / ss); else 0.0. Both-empty returns 1.0 (covered by identity).
    - Mandatory language-branch checklist test (TestDoubleMetaphoneKeys_LanguageBranches) — Scenario-Outline-style table — passes all 5 cases per CONTEXT.md §3:
      - Germanic: Schmidt → (XMT, SMT) AND Smith → (SM0, XMT) AND Score(Schmidt, Smith) == 1.0 (XMT cross-match)
      - Slavic: at least one vector (RV-DM10 Wojcik OR RV-DM11 Sczepanski)
      - Romance: Pacheco's primary or secondary contains "PXK" (RV-DM4)
      - Greek: Catherine → (K0RN, KTRN) AND Katherine → (K0RN, KTRN) — exact match (RV-DM7 + RV-DM8)
      - Chinese-origin: at least one vector (RV-DM12 Cheung OR RV-DM13 Wong)
    - Bench BenchmarkDoubleMetaphoneKeys_ASCII_Short within budget < 2 µs, ≤ 2 allocations per RESEARCH.md §6.2.
    - FuzzDoubleMetaphone invariants include CHARSET on BOTH keys: ^[A-Z0]{0,4}$.
    - dispatch_double_metaphone.go wires DoubleMetaphoneScore (NOT DoubleMetaphoneKeys) into dispatch[AlgoDoubleMetaphone] via the canonical var _ = func() bool {...}() pattern (no init()).
    - permittedMongeElkanInner gains AlgoDoubleMetaphone: true (15 → 16); monge_elkan_test.go rejected slice shrinks 8 → 7; TestMongeElkanScore_BinaryInner_DoubleMetaphone asserts three cases (one_matches=0.5 / both_match=1.0 / neither=0.0).
    - scripts/gen-phonetic-cross-validation.py DM section activated; imports metaphone (already pinned to 0.6 from 07-01); produces 40 entries each with branch field; testdata/cross-validation/phonetic/vectors.json DM section populated.
    - phonetic_cross_validation_test.go DM sub-test ENABLED with per-entry primary+secondary byte-equality; branch-count guard >= 7 major branches, >= 4 Chinese-origin.
    - testdata/golden/phonetic-codes.json DM section appended (8-12 entries with primary + secondary fields per CONTEXT.md §7 schema); phonetic_codes_golden_test.go DM branch activated.
    - testdata/golden/_staging/double_metaphone.json ships 10 staging-golden Score entries.
    - tests/bdd/features/double_metaphone.feature has ≥ 6 scenarios.
    - tests/bdd/steps/algorithms_steps.go appends DM step registrations + the new shared `^the keys should be "([^"]*)" and "([^"]*)"$` step.
    - props_test.go gains five-invariant block + PropDoubleMetaphone_KeyCharset.
    - example_test.go gains ExampleDoubleMetaphoneKeys + ExampleDoubleMetaphoneScore.
    - llms.txt + llms-full.txt synced in-plan with the two new function lines / block.
  </behavior>
  <action>
    Create `double_metaphone.go` (300-450 LOC per RESEARCH.md §6.2). Apache header (13 lines from soundex.go analog). Package-level godoc copying VERBATIM the canonical non-ASCII silent-skip warning text from CONTEXT.md §5. Source-Origin Statement following the EXTENDED template per CONTEXT.md §3 + RESEARCH.md §2.3 — Phase 4 WR-01 format PLUS TWO additional lines:
      1. Primary source: `Philips, L. (2000). "The double-metaphone search algorithm." C/C++ Users Journal, 18(6):38-43.`
      2. Rule-table provenance line: `Rule table derived fresh from: Philips 2000 + the public-domain C reference preserved at https://github.com/SWI-Prolog/packages-nlp/blob/master/double_metaphone.c (Maurice Aubrey's perl-port-derived bug fixes; long-stable URL). Consulted for rule-table STRUCTURE READING only — no code copied, no variable names or comment phrasing derived.`
      3. Cross-validation source: `metaphone==0.6` (BSD-licensed Andrew Collins Python port — oubiwann/metaphone)
      4. Negative-attribution line: `MIT-licensed Go ports NOT consulted (verified by diff during code review): CalypsoSys/godoublemetaphone (MIT), deezer/double-metaphone-go (MIT), tilotech/go-phonetics (MIT), UjjwalAyyangar/go-jellyfish (MIT), any other Go port.`
      5. Closing line: `algorithm-licensing-reviewer sign-off: <recorded in PR description per CONTEXT.md §3 LOCKED>`
    Implementation: imperative state machine. Pre-scan determines language-origin flags (isSlavoGermanic, isGermanic, isItalianGreek, etc.) based on patterns: leading WJ-/SZC- (Slavic), SCH- (Germanic), CHRIS-/CATH-/KATH- (Greek), CHE-/CHI-/CHO- before vowel-N (Chinese-origin), -ez/-ado/-illo/CH-after-vowel (Romance). Main loop dispatches via large `switch s[i]` on current letter with sub-conditions for surrounding context (look-ahead/behind up to 4 positions). Two `strings.Builder` outputs (primary + secondary). Key truncation = 4 chars; stop once both reach 4. Identity short-circuit at top of DoubleMetaphoneScore. Both-empty handled by identity. NO init(). NO map iteration on output paths.

    Create `dispatch_double_metaphone.go` (~30 LOC) mirroring `dispatch_soundex.go` from plan 07-01: Apache header + file-doc + `var _ = func() bool { dispatch[AlgoDoubleMetaphone] = DoubleMetaphoneScore; return true }()`.

    Create `double_metaphone_test.go` (`package fuzzymatch_test`). Tests: `TestDoubleMetaphone_BothEmpty`, `TestDoubleMetaphone_OneEmpty`, `TestDoubleMetaphoneKeys_LanguageBranches` (Scenario-Outline-style table covering all 5 mandatory branches per CONTEXT.md §3), `TestDoubleMetaphoneKeys_LiteratureReferenceVectors` (table-driven RV-DM1..RV-DM20 from RESEARCH.md §3.2), `TestDoubleMetaphoneScore_SchmidtSmithXMTCrossMatch` (load-bearing — fails with explicit message naming XMT cross-match if it breaks), `TestDoubleMetaphoneScore_FourWayKeyMatching` (covers pp / ps / sp / ss match cases).

    Create `double_metaphone_bench_test.go` following `soundex_bench_test.go` pattern. Benchmarks at three ASCII sizes. Code benchmarks gate on `var sinkP, sinkS string` and check both non-empty.

    Create `double_metaphone_fuzz_test.go` following `soundex_fuzz_test.go` pattern. FuzzDoubleMetaphone seeds cover all 5 language-branch fixtures + non-ASCII silent-skip seeds (Müller/Café/中文/🎉hello) + empty + invalid UTF-8. f.Fuzz invariants: not-panic / no NaN / no Inf / range [0,1] / identity / charset [A-Z0] on BOTH primary and secondary.

    Update `scripts/gen-phonetic-cross-validation.py`: activate the DM generation function. Add `import metaphone`. Hand-curate 40-entry DM input list per RESEARCH.md §3.2 with branch tagging: 7+ Germanic (Schmidt, Smith, Schwartz, Knaufmann, Mueller, Goebel, Vogel, ...); 7+ Slavic (Wojcik, Sczepanski, Dvorak, ...); 7+ Romance (Pacheco, Bologna, Catania, Jaramillo, Cabrillo, ...); 7+ Greek (Catherine, Katherine, Athens, Christopher, ...); 4+ Chinese-origin (Cheung, Wong, Hong, Chen, ...). For each, call `metaphone.doublemetaphone(s)` and write `{algorithm: "DoubleMetaphone", input, primary, secondary, branch}`. Re-run; commit updated vectors.json.

    Update `phonetic_cross_validation_test.go`: replace the DM `t.Skip` stub with active sub-test. Switch dispatch case "DoubleMetaphone" asserts `primary, secondary := fuzzymatch.DoubleMetaphoneKeys(e.Input)` then both equality checks. Add preamble branch-count guard: build `map[string]int` of branch → count; assert count >= 7 for Germanic / Slavic / Romance / Greek and count >= 4 Chinese-origin (the map is iterated for ASSERTION only, NOT for ordered output — DET-03 honoured).

    Update `testdata/golden/phonetic-codes.json`: append DM section (8-12 entries with primary + secondary fields per CONTEXT.md §7 schema) — Schmidt→(XMT,SMT), Smith→(SM0,XMT), Catherine→(K0RN,KTRN), Katherine→(K0RN,KTRN), Pacheco→(PXK,PSK), Wojcik, Cheung, Christopher, empty edge. Update `phonetic_codes_golden_test.go` DM branch (replace t.Skip with active assertion of both keys).

    Create `testdata/golden/_staging/double_metaphone.json` (10 entries — staging Score entries): both-empty / identity / Schmidt-Smith XMT match / Catherine-Katherine exact / Schmidt-Mueller non-match / Pacheco-Pachecho match / 4 more edges.

    Modify `monge_elkan.go`: append `AlgoDoubleMetaphone: true, // Philips 2000` to permittedMongeElkanInner map; update leading comment from "15 entries" to "16 entries (... + 2 phonetic-tier — Phase 7 plans 07-01, 07-02)". Modify `monge_elkan_test.go`: remove `fuzzymatch.AlgoDoubleMetaphone` from `rejected` slice (8 → 7); add to `permittedSanity`. Append `TestMongeElkanScore_BinaryInner_DoubleMetaphone` per the PATTERNS.md template (three sub-cases — one_matches=0.5 / both_match=1.0 / neither=0.0).

    Append to `props_test.go` five-invariant block for DoubleMetaphoneScore plus `PropDoubleMetaphone_KeyCharset` asserting regex `^[A-Z0]{0,4}$` holds for BOTH keys.

    Append to `example_test.go`: `ExampleDoubleMetaphoneKeys` (input "Schmidt", output `XMT SMT`) and `ExampleDoubleMetaphoneScore` (inputs "Schmidt"/"Smith", output `1.0`).

    Create `tests/bdd/features/double_metaphone.feature` per CONTEXT.md §3 mandatory checklist — ≥ 6 scenarios covering each language branch + Schmidt-Smith XMT cross-match + identity + both-empty.

    Update `tests/bdd/steps/algorithms_steps.go`: add `iComputeTheDoubleMetaphoneKeysOf(s string)` writing to ctx.lastDMPrimary + ctx.lastDMSecondary; `iComputeTheDoubleMetaphoneScoreBetween(a, b string)` writing to ctx.lastScore; new `theKeysShouldBe(wantPrimary, wantSecondary string)` shared step asserting against ctx.lastDMPrimary + ctx.lastDMSecondary. Register three step regexes in InitializeScenario.

    Update `llms.txt`: insert new `### Double Metaphone (Philips 2000)` heading after Soundex; under it list two function lines.

    Update `llms-full.txt`: append the full Phase 7 DM algorithm-surface block (godoc verbatim).

    PR description MUST include the two lines per CONTEXT.md §3 + VALIDATION.md row 07-02-14:
      - `algorithm-licensing-reviewer sign-off: <reviewer-id or commit-sha after manual diff vs the 5 named MIT-Go ports>`
      - `--depth=deep code review: <reviewer-id>`
  </action>
  <verify>
    <automated>cd /Users/johnny/Development/fuzzymatch &amp;&amp; go build ./... &amp;&amp; go vet ./... &amp;&amp; go test -race -shuffle=on -count=1 -run 'TestDoubleMetaphone|TestMongeElkanScore_BinaryInner_DoubleMetaphone|TestMongeElkan_PanicsOnNonPermittedInner|TestProp_DoubleMetaphone|TestPhonetic_CrossValidation/DoubleMetaphone|TestPhoneticCodesGolden/DoubleMetaphone|TestLLMsTxt' ./... &amp;&amp; go test -run none -bench BenchmarkDoubleMetaphone -benchmem -benchtime=1x ./... &amp;&amp; go test -fuzz=FuzzDoubleMetaphone -fuzztime=10s ./... &amp;&amp; grep -q 'SWI-Prolog/packages-nlp' double_metaphone.go &amp;&amp; grep -q 'CalypsoSys/godoublemetaphone' double_metaphone.go</automated>
  </verify>
  <acceptance_criteria>
    - Satisfies VALIDATION.md rows 07-02-01 through 07-02-15.
    - `fuzzymatch.DoubleMetaphoneKeys("Schmidt") == ("XMT", "SMT")`, `("Smith") == ("SM0", "XMT")`, `("Catherine") == ("Katherine") == ("K0RN", "KTRN")`.
    - `fuzzymatch.DoubleMetaphoneKeys("Pacheco")` has either primary or secondary containing "PXK".
    - `fuzzymatch.DoubleMetaphoneScore("Schmidt", "Smith") == 1.0` (XMT cross-match).
    - `double_metaphone.go` Source-Origin Statement contains the SWI-Prolog URL (`grep -q 'SWI-Prolog/packages-nlp' double_metaphone.go`) AND the 5 MIT-Go-port negative-attribution names (verified by grep for each: CalypsoSys/godoublemetaphone, deezer/double-metaphone-go, tilotech/go-phonetics, UjjwalAyyangar/go-jellyfish).
    - `permittedMongeElkanInner` has 16 entries; `rejected` slice has 7 entries; `TestMongeElkan_PanicsOnNonPermittedInner` passes; `TestMongeElkanScore_BinaryInner_DoubleMetaphone` passes three sub-tests.
    - `BenchmarkDoubleMetaphoneKeys_ASCII_Short` reports ≤ 2 allocs/op and ns/op < 2000.
    - `FuzzDoubleMetaphone` 10-second run passes with no panics + charset invariant holding.
    - `tests/bdd/features/double_metaphone.feature` has ≥ 6 scenarios; `make test-bdd` passes.
    - `TestPhonetic_CrossValidation/DoubleMetaphone` passes with the branch-count guard (>= 7 / >= 4) asserting before per-entry checks.
    - `TestPhoneticCodesGolden/DoubleMetaphone` passes with primary+secondary equality assertions.
    - `llms.txt` contains `### Double Metaphone (Philips 2000)` heading + two function lines; `TestLLMsTxt` passes.
    - PR description includes both required lines (`algorithm-licensing-reviewer sign-off:` and `--depth=deep code review:`).
  </acceptance_criteria>
  <done>Double Metaphone ships fresh-implemented from Philips 2000 + SWI-Prolog C reference (structure-only consultation), all 5 language-branch checklist gates green, dispatch wired, Monge-Elkan permitted-inner extended in lockstep, dual-pin cross-validation (Metaphone==0.6) green with branch-count guard, byte-stable code golden green, llms.txt synced in-plan, PR description records both licensing gate lines. PHON-02 closed.</done>
</task>

</tasks>

<verification>
- `make check` green (golangci-lint v2 + vet + race + vulncheck + gosec + markdownlint + tidy-check + coverage-check).
- `make test-bdd` green including the new `double_metaphone.feature` (≥ 6 scenarios).
- `make verify-determinism` green — `phonetic-codes.json` DM section diffs byte-identically on the CI matrix.
- `make verify-deps-allowlist` green — root `go.mod` still zero non-stdlib `require` lines beyond `golang.org/x/text`.
- `make regen-phonetic-cross-validation` (developer-side, optional) regenerates `vectors.json` byte-identically when `jellyfish==1.2.1 Metaphone==0.6` installed.
- `go test -run 'TestPhonetic_CrossValidation/DoubleMetaphone|TestPhoneticCodesGolden/DoubleMetaphone' -race ./...` green.
- Plan 07-02 PR description contains `algorithm-licensing-reviewer sign-off: <...>` AND `--depth=deep code review: <...>` lines.
</verification>

<success_criteria>
PHON-02 closed:
1. `DoubleMetaphoneKeys(s)` returns the canonical Philips 2000 primary + secondary 4-char keys with charset `[A-Z0]`; `DoubleMetaphoneScore(a, b)` returns binary 0.0/1.0 via 4-way key matching.
2. All 5 mandatory language-branch reference vectors pass per CONTEXT.md §3 LOCKED checklist.
3. Source-Origin Statement extended with rule-table provenance line + 5-MIT-Go-port negative-attribution line.
4. `algorithm-licensing-reviewer` sign-off recorded in PR description; `--depth=deep` code review completed.
5. Cross-validation against `metaphone==0.6` Python port passes byte-stable; branch-count guard enforces minimum coverage per major branch.
6. `permittedMongeElkanInner` mutated 15→16; `rejected` slice shrunk 8→7; `TestMongeElkanScore_BinaryInner_DoubleMetaphone` green.
7. `llms.txt` + `llms-full.txt` synced in-plan.
8. Allocation budget < 2 µs, ≤ 2 allocations met; fuzz green; BDD ≥ 6 scenarios green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-phonetic-algorithms/07-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md` with:
- Status: COMPLETE
- Closes: PHON-02
- Source-Origin Statement audit trail: Philips 2000 + SWI-Prolog/packages-nlp double_metaphone.c (structure-only consultation) + metaphone==0.6 cross-validation + 5 MIT-Go-port negative-attribution lines
- algorithm-licensing-reviewer sign-off recorded: <commit-sha or reviewer-id>
- --depth=deep code review completed: <reviewer-id>
- Language-branch checklist green: Germanic ✓ / Slavic ✓ / Romance ✓ / Greek ✓ / Chinese-origin ✓
</output>
