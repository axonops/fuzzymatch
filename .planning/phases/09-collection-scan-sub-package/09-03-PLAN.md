---
phase: 09
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - scan/scan.go
  - scan/scan_test.go
autonomous: true
requirements: [SCAN-01, SCAN-04]
tags: [scan, phase-9, check-body, within-group, cross-group, threshold-boost, naive]

must_haves:
  truths:
    - "scan.Check(items, cfg) wires validateCheck as its first step and returns its error on failure (Plan 09-02 integration)"
    - "scan.Check within-group pass: for each group (in sorted-key order), iterate all i<j pairs and emit a KindWithinGroup Warning when cfg.Scorer.Match(itemA.Name, itemB.Name) returns true"
    - "scan.Check cross-group pass: when cfg.CompareAcrossGroups == true, iterate all cross-group pairs (groupA != groupB) and emit a KindAcrossGroups Warning when cfg.Scorer.Score(a, b) >= effectiveCrossGroupThreshold where effectiveCrossGroupThreshold = math.Min(1.0, cfg.Scorer.Threshold() + cfg.CrossGroupThresholdBoost)"
    - "Cross-group identical-name suppression: when cfg.CompareIdenticalAcrossGroups == false (the default), pairs where normalisedNameA == normalisedNameB across groups are suppressed (per SCAN-04)"
    - "scan.Check tokenises and normalises every Item.Name ONCE per Check invocation; the raw name is passed to Scorer.Match / Scorer.Score (which re-normalises internally per Phase 8 §3); the normalised name is used for identical-name comparison and forward-references for Plan 09-04's bucket keys"
    - "Each emitted Warning carries Scores populated from cfg.Scorer.ScoreAll(a, b) only on emission (lazy population — paid for only when the warning is kept)"
    - "scan.Check returns the unsorted []Warning + nil error in this plan; sort + completeness assertion lands in Plan 09-06"
    - "No bucketing yet — both passes use naive nested loops (O(N²) within group, O(N×M) cross-group). Plan 09-04 introduces bucket dispatch."
    - "scan.Check has zero goroutines, zero channels, zero mutexes"
  artifacts:
    - path: "scan/scan.go"
      provides: "Check body: validation gate → normalise pass → within-group pass → cross-group pass (naive)"
      contains: "func Check(items []Item, cfg Config) ([]Warning, error)"
    - path: "scan/scan_test.go"
      provides: "Black-box unit tests covering within-group + cross-group + threshold-boost + clamp + identical-name suppression default"
  key_links:
    - from: "scan.Check"
      to: "validateCheck (Plan 09-02)"
      via: "first-step call returning error on failure"
      pattern: "if err := validateCheck\\(items, cfg\\); err != nil"
    - from: "scan.Check cross-group pass"
      to: "cfg.Scorer.Score (raw composite)"
      via: "function call with effectiveCrossGroupThreshold local comparison"
      pattern: "if cfg\\.Scorer\\.Score\\(a, b\\) >= effectiveCrossGroupThreshold"
    - from: "scan.Check within-group pass"
      to: "cfg.Scorer.Match"
      via: "function call (threshold applied internally)"
      pattern: "if cfg\\.Scorer\\.Match\\(a, b\\)"
    - from: "scan.Check normalisation pass"
      to: "cfg.Scorer.NormalisationOptions() (Plan 09-01)"
      via: "Scorer accessor returning (opts, applied)"
      pattern: "opts, applied := cfg\\.Scorer\\.NormalisationOptions\\(\\)"
user_setup: []
---

<objective>
Replace the scan.Check stub from Plan 09-01 with the full naive implementation of the within-group + cross-group passes: validation gate (Plan 09-02 hook), normalise-once pass over Items, within-group naive nested loop using cfg.Scorer.Match, cross-group naive nested loop using cfg.Scorer.Score with the effective-threshold clamp arithmetic and the cross-group identical-name suppression default.

Purpose: Establishes the inner loop, the dual-threshold model, and the Scorer-boundary discipline (raw names to Scorer; normalised names for local comparison). Naive-only at this stage so the diff is small and the PropCheck_BucketEquivalentToNaive gate in Plan 09-04 has a clean reference implementation. Suppression (SilenceLint + SuppressedPairs) lands in Plan 09-05; sort + completeness assertion in Plan 09-06.

Output: scan.Check returns correct (but unsorted, un-bucketed, only-partially-suppressed) []Warning slices for the within-group and cross-group cases. SCAN-04's "cross-group identical-name suppression default" is delivered in this plan because it is a default-on behaviour driven by Config.CompareIdenticalAcrossGroups and operates on the normalised names already available in this layer. Per-item SilenceLint and global SuppressedPairs (SCAN-03) are deferred to Plan 09-05's suppression module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Plan inputs
@.planning/phases/09-collection-scan-sub-package/09-CONTEXT.md
@.planning/phases/09-collection-scan-sub-package/09-RESEARCH.md
@.planning/phases/09-collection-scan-sub-package/09-PATTERNS.md
@.planning/phases/09-collection-scan-sub-package/09-VALIDATION.md
@.planning/phases/09-collection-scan-sub-package/09-01-SUMMARY.md
@.planning/phases/09-collection-scan-sub-package/09-02-SUMMARY.md

# Skills
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/performance-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

# Source files (analogs and consumers)
@scorer.go
@normalise.go
@tokenise.go
@scan/scan.go
@scan/errors.go
@scan/validate.go

<interfaces>
<!-- Scorer methods scan.Check consumes (root pkg): -->

```go
// scorer.go:352-389 — Score returns the weighted composite in [0.0, 1.0]
func (s *Scorer) Score(a, b string) float64

// scorer.go:398-400 — Match returns Score(a, b) >= s.threshold
func (s *Scorer) Match(a, b string) bool

// scorer.go:503-523 — ScoreAll returns per-algorithm map
func (s *Scorer) ScoreAll(a, b string) map[AlgoID]float64

// scorer.go:447-449 — Threshold accessor
func (s *Scorer) Threshold() float64

// New in Plan 09-01:
func (s *Scorer) NormalisationOptions() (opts NormalisationOptions, applied bool)
```

<!-- Plan 09-02 validation entry-point (internal to scan package): -->
```go
func validateCheck(items []Item, cfg Config) error
```

<!-- Normalise + Tokenise (root pkg, consumed once per Check): -->
```go
func Normalise(s string, opts NormalisationOptions) string  // normalise.go:156
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing scan_test.go cases for within + cross-group + clamp + identical-name (TDD RED)</name>
  <files>scan/scan_test.go</files>
  <read_first>
    - .planning/phases/09-collection-scan-sub-package/09-RESEARCH.md lines 162-242 (architecture diagram with within/cross flow) + lines 490-525 (Pitfall 6 boost clamp) + lines 488-498 (Pitfall 3 Match-vs-Score)
    - .planning/phases/09-collection-scan-sub-package/09-PATTERNS.md scan/scan.go "Pattern 4 — Two-step normalise + parallel arrays" + scan/scan_test.go section
    - .planning/phases/09-collection-scan-sub-package/09-CONTEXT.md §"Domain Boundary" (SCAN-04 default behaviour for cross-group identical)
    - scorer.go:398-400 (Match implementation — returns Score(a,b) >= s.threshold)
    - scan/scan.go (Plan 09-01 — Check stub + DefaultConfig)
    - scan/validate.go (Plan 09-02 — validateCheck entry-point)
  </read_first>
  <behavior>
    Write failing tests in scan/scan_test.go (extending the file from Plan 09-01). All tests use stdlib testing only.

    - Test 1 (TestCheck_WithinGroup_SingleMatch): items[0]={user_id, login}, items[1]={userId, login} → 1 warning with Kind == KindWithinGroup, NameA/NameB in sorted-by-input-order, GroupA == GroupB == "login", Score == DefaultScorer().Score("user_id", "userId"), Scores map non-empty
    - Test 2 (TestCheck_WithinGroup_NoMatch_BelowThreshold): items[0]={apple, fruit}, items[1]={ orange, fruit } → 0 warnings (composite Score < 0.85 default threshold)
    - Test 3 (TestCheck_WithinGroup_ThreeItems_PairwiseMatches): items[0]={user_id, login}, items[1]={userId, login}, items[2]={user_name, login} → 1 warning for user_id↔userId; user_name has lower similarity (verify warning count == 1 or 2 depending on Default thresholds — assert by recomputing Score and matching against ≥ Threshold)
    - Test 4 (TestCheck_CrossGroup_Disabled_NoEmissions): items[0]={user_id, login}, items[1]={user_id, profile} (identical name, different groups), cfg.CompareAcrossGroups == false → 0 warnings (cross-group pass skipped)
    - Test 5 (TestCheck_CrossGroup_Enabled_IdenticalNameSuppressed): same items + cfg.CompareAcrossGroups == true + cfg.CompareIdenticalAcrossGroups == false (DefaultConfig) → 0 warnings (identical-name suppressed by default per SCAN-04)
    - Test 6 (TestCheck_CrossGroup_Enabled_IdenticalNameNotSuppressed): same items + cfg.CompareIdenticalAcrossGroups == true → 1 warning with Kind == KindAcrossGroups, NameA == NameB == "user_id"
    - Test 7 (TestCheck_CrossGroup_NonIdenticalSimilar_Emitted): items[0]={user_id, login}, items[1]={userId, profile}, cfg.CompareAcrossGroups == true → 1 warning with Kind == KindAcrossGroups (effective threshold = 0.85 + 0.05 = 0.90; the pair Score should exceed this for any reasonable similarity composite — verify by recomputing Score)
    - Test 8 (TestCheck_CrossGroup_BoostClamp_BlocksEmission): construct a Scorer with WithThreshold(0.95) and a Config with CrossGroupThresholdBoost == 0.20 → effective threshold = min(1.0, 0.95 + 0.20) = 1.0; on items[0]={user_id, login}, items[1]={user_id, profile} (identical-name allowed via CompareIdenticalAcrossGroups=true), the Score is 1.0 (Match on identical input) — verify a single warning emits because Score >= 1.0 holds for identical Names; but for items[0]={user_id, login}, items[1]={userId, profile} (similar but not identical), 0 warnings emit because Score < 1.0
    - Test 9 (TestCheck_CrossGroup_BoostZero_SameAsWithinThreshold): Scorer threshold 0.85, boost 0.0 → cross-group emits whenever Score >= 0.85 (same as within); verify by constructing a pair that scores in [0.85, 0.90) and confirming it emits in cross-group (which would NOT emit if boost were 0.05)
    - Test 10 (TestCheck_WithinGroup_GroupKeyedIteration): three groups (login, profile, audit) with two items each, all within-group pairs similar → expect 3 warnings, one per group (within-group only; cross-group disabled)
    - Test 11 (TestCheck_NoItems_NoWarnings): items == nil, valid Config → returns (nil-or-empty, nil) — both representations acceptable; document choice (recommendation: return empty slice `[]Warning{}` not nil, for consumer-friendly len() == 0 semantics — or document the nil contract clearly. Decide and pin in test.)
    - Test 12 (TestCheck_SingleItem_NoWarnings): items has length 1 → 0 warnings (no pairs to compare)
    - Test 13 (TestCheck_DefaultConfig_DoesNotEnableCrossGroup): cfg := DefaultConfig(s); items with cross-group identical names; cfg.CompareAcrossGroups remains false → 0 warnings
    - Test 14 (TestCheck_ScoresPopulatedOnEmission): on a within-group match, the Warning.Scores map is non-nil, has len > 0, and contains AlgoID keys consistent with s.Algorithms() (every algorithm with non-zero weight has an entry)
    - Test 15 (TestCheck_RawNameNotNormalised_PassedToScorer): construct a Scorer with WithNormalisation(opts that lowercase + strip underscores); pass items with mixed-case names; verify the Warning.NameA/NameB are the RAW input strings (NOT the normalised forms) — this confirms scan.Check doesn't double-normalise (Pitfall 5)
    - Test 16 (TestCheck_NormalisationOptionsRead_FromScorer): construct a Scorer with WithoutNormalisation; pass two items where normalised forms would be equal but raw forms differ ("user_id" vs "USER_ID"); within-group pass; verify the pair score is computed on raw forms (Scorer ignores normalisation), so the test asserts behaviour rather than internal state.
    - Test 17 (TestCheck_CrossGroup_UsesScoreNotMatch): construct a Scorer with WithThreshold(0.50) → cross-group with boost 0.30 → effective threshold 0.80; verify a pair scoring 0.55 (above within threshold 0.50) does NOT emit in cross-group (Score 0.55 < 0.80); the same pair within-group emits. This is the load-bearing Pitfall 3 test.

    Run `go test ./scan/...` and confirm tests fail (Check still returns the (nil, nil) stub from Plan 09-01 when validateCheck passes — all assertions about warning counts will fail).
  </behavior>
  <action>
    Edit scan/scan_test.go (extend the file from Plan 09-01 with the 17 new tests above). Use scan_test (black-box) package — same as in Plan 09-01.

    Helper functions to add at the top of the file:
    - newDefaultScorer() *fuzzymatch.Scorer — returns fuzzymatch.DefaultScorer()
    - newScorerWithThreshold(t float64) *fuzzymatch.Scorer — convenience for tests 8/9/17 (build with NewScorer + DefaultScorerOptions but override threshold)
    - newConfigDefault(s) and newConfigCross(s) — DefaultConfig variants
    - assertWarningKind(t, w, want), assertWarningNames(t, w, wantA, wantB) — small helpers to reduce verbosity

    For Test 8's clamp case, document the expected behaviour explicitly in the test comment block citing Pitfall 6.

    For Test 17, the Pitfall 3 test: construct items={"different_field_A", g1}, {"different_field_B", g2} where Default scorer composite scores around 0.55-0.70; assert that with threshold 0.50 + boost 0.30 (effective 0.80), the cross-group pair does NOT emit. Pick concrete strings whose Default scorer composite is in this range — the test can pre-compute via `s.Score(...)` and skip if the composite falls outside the test's assumed band.

    Tests should use t.Parallel() unless they need shared Scorer state.

    Confirm with `go test -run TestCheck_ ./scan/...` — all 17 tests FAIL because Check still returns (nil, nil) for non-nil-Scorer inputs.
  </action>
  <verify>
    <automated>bash -c 'go test -run TestCheck_ ./scan/... 2>&1 | grep -E "FAIL|--- FAIL" | wc -l | awk "\$1 < 1 { exit 1 }"'</automated>
  </verify>
  <acceptance_criteria>
    - scan/scan_test.go extended with 17 new TestCheck_* functions
    - All 17 tests FAIL under current stub-only Check (TDD RED)
    - Tests use stdlib testing only; t.Parallel() where appropriate
    - Test 8 + Test 17 explicitly document the Pitfall 3 / Pitfall 6 semantics they enforce
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace scan.Check stub with the within + naive cross-group body (TDD GREEN)</name>
  <files>scan/scan.go</files>
  <read_first>
    - .planning/phases/09-collection-scan-sub-package/09-RESEARCH.md lines 488-528 (Pitfall 3 + Pitfall 4 + Pitfall 5 + Pitfall 6) + lines 200-242 (architecture flow) + Pattern 4 (group-by + dispatch shape, though no bucket yet)
    - .planning/phases/09-collection-scan-sub-package/09-PATTERNS.md "scan/scan.go" Pattern 4 (parallel arrays) + Pattern 5 (completeness assertion is forward-reference; landing in Plan 09-06)
    - scan/validate.go (Plan 09-02 — validateCheck entry-point)
    - scan/scan.go (Plan 09-01 — current stub + DefaultConfig)
    - scorer.go:352-400 (Score + Match exact contracts; the boundary between raw and normalised input is at scorer.go:357-360)
    - normalise.go:156 (Normalise signature)
  </read_first>
  <behavior>
    All 17 TestCheck_* tests from Task 1 PASS.

    Key semantic guarantees verifiable by tests:
    - validation gate fires first (errors propagate unmodified)
    - within-group pass uses Match (not Score) — threshold = scorer.Threshold()
    - cross-group pass uses Score (not Match) — effective threshold = min(1.0, scorer.Threshold() + boost)
    - cross-group identical-name (normalised) suppressed when CompareIdenticalAcrossGroups == false
    - raw Item.Name strings appear in Warning.NameA/NameB; the Scorer re-normalises internally
    - Warning.Scores populated via ScoreAll only on emission (lazy)
    - groups iterated in sorted key order; items within a group iterated in input-index order
    - empty items → returns empty (or nil) []Warning + nil error
  </behavior>
  <action>
    Edit scan/scan.go. Replace the existing Check stub body with the full implementation. The function signature remains `func Check(items []Item, cfg Config) ([]Warning, error)`.

    Body structure (mirror 09-RESEARCH.md architecture diagram lines 162-242):

    (1) Validation gate:
        if err := validateCheck(items, cfg); err != nil { return nil, err }

    (2) Early exit on degenerate inputs:
        if len(items) < 2 { return nil, nil }

    (3) Read normalisation policy from Scorer (Plan 09-01 accessor):
        normOpts, applyNormalisation := cfg.Scorer.NormalisationOptions()

    (4) Pre-compute normalised names (parallel array per Pitfall 5):
        normalisedNames := make([]string, len(items))
        for i, item := range items {
            if applyNormalisation {
                normalisedNames[i] = fuzzymatch.Normalise(item.Name, normOpts)
            } else {
                normalisedNames[i] = item.Name
            }
        }

    (5) Build group → []itemIndex map AND sorted group-key slice:
        groupIndices := make(map[string][]int, len(items))
        for i, item := range items {
            groupIndices[item.Group] = append(groupIndices[item.Group], i)
        }
        sortedGroups := make([]string, 0, len(groupIndices))
        for g := range groupIndices {
            sortedGroups = append(sortedGroups, g)
        }
        sort.Strings(sortedGroups)
        // The group → []int slices retain natural input-index order
        // because the for-loop walks items[] in slice order. No need
        // to sort the index slices.

    (6) warnings buffer:
        warnings := make([]Warning, 0)

    (7) Within-group naive pass (sorted-group order, then i<j naive):
        for _, group := range sortedGroups {
            idx := groupIndices[group]
            for i := 0; i < len(idx); i++ {
                for j := i + 1; j < len(idx); j++ {
                    a, b := items[idx[i]], items[idx[j]]
                    if cfg.Scorer.Match(a.Name, b.Name) {
                        // Emit warning. Warning is constructed using RAW
                        // a.Name and b.Name (Pitfall 5); Scorer re-normalises.
                        warnings = append(warnings, Warning{
                            Kind:   KindWithinGroup,
                            NameA:  a.Name, NameB:  b.Name,
                            GroupA: a.Group, GroupB: b.Group,
                            TagA:   a.Tag,  TagB:   b.Tag,
                            Score:  cfg.Scorer.Score(a.Name, b.Name),
                            Scores: cfg.Scorer.ScoreAll(a.Name, b.Name),
                        })
                    }
                }
            }
        }

    (8) Cross-group naive pass (when CompareAcrossGroups):
        if cfg.CompareAcrossGroups {
            effectiveThreshold := math.Min(1.0, cfg.Scorer.Threshold() + cfg.CrossGroupThresholdBoost)
            for gi := 0; gi < len(sortedGroups); gi++ {
                for gj := gi + 1; gj < len(sortedGroups); gj++ {
                    idxA := groupIndices[sortedGroups[gi]]
                    idxB := groupIndices[sortedGroups[gj]]
                    for _, i := range idxA {
                        for _, j := range idxB {
                            a, b := items[i], items[j]
                            // Cross-group identical-name suppression (D-07 default per SCAN-04):
                            if !cfg.CompareIdenticalAcrossGroups && normalisedNames[i] == normalisedNames[j] {
                                continue
                            }
                            score := cfg.Scorer.Score(a.Name, b.Name)
                            if score >= effectiveThreshold {
                                warnings = append(warnings, Warning{
                                    Kind:   KindAcrossGroups,
                                    NameA:  a.Name, NameB:  b.Name,
                                    GroupA: a.Group, GroupB: b.Group,
                                    TagA:   a.Tag,  TagB:   b.Tag,
                                    Score:  score,
                                    Scores: cfg.Scorer.ScoreAll(a.Name, b.Name),
                                })
                            }
                        }
                    }
                }
            }
        }

    (9) Return warnings without sorting (sort lands in Plan 09-06):
        return warnings, nil

    Update Check's godoc:
    - Document that Check returns warnings in implementation-order (NOT sorted) in this plan version — flag explicitly that Plan 09-06 lands the deterministic sort
    - Document the Match-vs-Score discipline (within-group uses Match; cross-group uses Score; explain why)
    - Document the boost clamp at 1.0 with a worked example (boost 0.5 + threshold 0.85 → effective 1.0, NOT 1.35)
    - Document the cross-group identical-name suppression default (the SCAN-04 surface)
    - Document that suppression composition (SilenceLint + SuppressedPairs) lands in Plan 09-05; cross-group identical-name is the only suppression rule active in this plan version
    - Concurrency: safe for concurrent invocation on disjoint inputs (no shared mutable state)

    Add necessary imports to scan/scan.go: "math", "sort", "github.com/axonops/fuzzymatch".

    Run `go test -race -shuffle=on -count=1 -run TestCheck_ ./scan/...` and confirm all 17 tests PASS.

    Run `go vet ./scan/...` and `golangci-lint run ./scan/...` — clean.
  </action>
  <verify>
    <automated>go test -race -shuffle=on -count=1 -run TestCheck_ ./scan/...</automated>
  </verify>
  <acceptance_criteria>
    - scan/scan.go Check function body lands; godoc updated with Match-vs-Score + clamp + identical-name suppression note + "sort lands in Plan 09-06"
    - All 17 Task-1 tests PASS under race + shuffle
    - No bucketing in this plan (naive O(N²) within group, O(N×M) cross-group)
    - No suppression composition (only the SCAN-04 identical-name default)
    - No goroutines, channels, mutexes
    - `go vet ./scan/...` and `golangci-lint run ./scan/...` clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Coverage gate + agent reviews + commit</name>
  <files>(no new files — coverage + reviews + commit)</files>
  <read_first>
    - .claude/skills/fuzzymatch-review-protocol/SKILL.md
    - .claude/skills/determinism-standards/SKILL.md (sortedGroups slice + no map iteration on output path)
  </read_first>
  <action>
    Quality gate:
    (a) `go test -race -shuffle=on -count=1 -cover ./scan/...` — scan/scan.go file coverage ≥ 90%
    (b) `make check` — full gate green
    (c) Manual race/shuffle verification: run the suite 5 times consecutively; all green

    Reviewers (in order):
    (i) code-reviewer on the diff — focus: Match-vs-Score discipline, parallel-arrays Scorer-boundary cleanliness, no double-normalisation, map iteration discipline (sortedGroups slice ordering)
    (ii) determinism-reviewer — focus: sortedGroups is the only map→slice step; verify no iteration of `groupIndices` map on output paths; warnings appended in deterministic order (group-key sorted × index order)
    (iii) security-reviewer — focus: Tag never escapes into errors (no errors here, but verify the warning emission carries TagA/TagB by-value)
    (iv) algorithm-performance-reviewer — focus: ScoreAll called only on emission (not every comparison); Score called once per emitted warning, separately from Match (the Match → Score double-call is intentional because Match doesn't expose the score); flag if this is judged a perf concern but defer optimisation to v1.x per YAGNI
    (v) go-quality — final gate

    Commit (single atomic feat commit):
    feat(scan): naive within + cross-group passes with effective-threshold clamp and identical-name default (SCAN-01, SCAN-04)

    Body:
    - Plan 09-03 lands the naive implementation; bucket dispatch follows in Plan 09-04
    - 17 new TestCheck_* tests added; all green under -race -shuffle
    - SCAN-04 identical-name suppression default delivered
    - Match-vs-Score discipline pinned (Pitfall 3); boost clamp at 1.0 (Pitfall 6); no double-normalisation (Pitfall 5)
    - Reviewer verdicts: code-reviewer / determinism-reviewer / security-reviewer / algorithm-performance-reviewer / go-quality
  </action>
  <verify>
    <automated>bash -c 'set -e; go test -race -shuffle=on -count=1 -cover -coverprofile=/tmp/scan_cover_03.out ./scan/... && go tool cover -func=/tmp/scan_cover_03.out | grep "scan.go" | grep -v _test.go | awk "{print \$3}" | sed s/%// | awk "\$1 < 90.0 { exit 1 }" && make check'</automated>
  </verify>
  <acceptance_criteria>
    - scan/scan.go ≥ 90% line coverage
    - 17 TestCheck_* tests + the 5 Plan-09-01 tests + the 22 Plan-09-02 tests all PASS
    - make check green
    - Five reviewers green; verdicts recorded in 09-03-SUMMARY.md
    - One commit landed: feat(scan): naive within + cross-group passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer items[] → Check inner loop | Item.Name carries adversarial strings; bounded by Scorer.Score O(N²) at per-pair tier |
| Consumer Tag any → Warning.TagA/TagB | Tag is by-value passed; documented in Item.Tag godoc; never logged or stringified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Denial of Service | Naive O(N²) within-group on huge groups | accept (this plan) | Bucket optimisation lands in Plan 09-04. For now, document expected complexity in Check godoc. PERF-05 ≤ 2s/10k budget is met only after Plan 09-04. |
| T-09-03-02 | Tampering | Map iteration on groupIndices map producing non-deterministic warning order | mitigate | sortedGroups slice iterated; groupIndices map only used for lookup. determinism-reviewer verifies. |
| T-09-03-03 | Information Disclosure | Score / ScoreAll double-call leaking timing | accept | The Scorer's internal float-determinism is cross-platform stable; per-pair timing leaks consumer input length already (existing root-pkg surface). |
| T-09-03-04 | Tampering | Scorer.Match internally uses scorer.Threshold(); if downstream code mutates Scorer state, results diverge | mitigate | Scorer is immutable post-NewScorer (Phase 8). scan/scan.go godoc reiterates this guarantee. |
</threat_model>

<verification>
- All 17 TestCheck_* tests + 22 Plan-09-02 tests + 5 Plan-09-01 tests PASS under `go test -race -shuffle=on -count=1 ./scan/...`
- scan/scan.go file coverage ≥ 90%
- `make check` green
- 5 reviewer verdicts recorded
- One commit landed on worktree branch
</verification>

<success_criteria>
- scan.Check produces correct (unsorted) []Warning for within-group + cross-group passes
- SCAN-04 cross-group identical-name suppression default delivered
- Match-vs-Score discipline + boost clamp + no-double-normalisation correctly implemented (Pitfalls 3/5/6)
- No bucketing yet (Plan 09-04); no SilenceLint or SuppressedPairs (Plan 09-05); no sort or completeness assertion (Plan 09-06)
- Plan 09-04 can wire its bucket dispatch into Check's group iteration loop
</success_criteria>

<output>
After completion, create `.planning/phases/09-collection-scan-sub-package/09-03-SUMMARY.md` capturing:
- scan/scan.go Check body LOC + final coverage % for the file
- Five reviewer verdicts
- Confirmation of Pitfall 3 / Pitfall 5 / Pitfall 6 test coverage
- One commit SHA
</output>
