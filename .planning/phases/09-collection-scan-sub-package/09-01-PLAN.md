---
phase: 09
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scan/scan.go
  - scan/kind.go
  - scan/errors.go
  - scan/doc.go
  - scan/example_test.go
  - scan/scan_test.go
  - scorer.go
  - scorer_test.go
  - docs/requirements.md
  - llms.txt
  - llms-full.txt
  - CHANGELOG.md
autonomous: false
requirements: [SCAN-01, SCAN-06]
tags: [scan, phase-9, foundation, spec-override, sentinel-errors, kind, warning, config, accessor]

must_haves:
  truths:
    - "scan/ sub-package compiles cleanly under the root module (no separate go.mod) with zero new non-stdlib runtime dependencies"
    - "scan.Item, scan.Config, scan.Warning, scan.Kind, scan.DefaultConfig, and scan.Check are publicly exported with godoc"
    - "scan.Kind has constants KindWithinGroup and KindAcrossGroups and a String() method returning CamelCase forms"
    - "scan.Warning.Scores has type map[fuzzymatch.AlgoID]float64 (SPEC OVERRIDE per D-01) with explicit godoc back-reference to 09-CONTEXT.md §1"
    - "scan.ErrNilScorer, scan.ErrInvalidItem, scan.ErrInvalidConfig are exported with four-section godoc per Phase 8.5 Q4 template"
    - "Scorer.NormalisationOptions() (NormalisationOptions, bool) is a new public accessor on *fuzzymatch.Scorer returning (s.normaliseOpts, s.applyNormalisation)"
    - "docs/requirements.md §12.1 + §12.4 + §15 + line 2010 + line 2061 amended to rename WarningKind→Kind, Scores map type→map[AlgoID]float64, default-0.05 statement migrated to DefaultConfig godoc, line 1389 phrasing updated"
    - "api-ergonomics-reviewer sign-off recorded in this plan's commit trailer (referenced from CHANGELOG)"
    - "scan.Check is a function-symbol stub returning (nil, ErrNilScorer) when cfg.Scorer is nil; full body lands in Plan 09-03"
  artifacts:
    - path: "scan/scan.go"
      provides: "Item struct, Config struct, Warning struct, Check stub, DefaultConfig helper, package design-notes block"
      contains: "type Item struct; type Config struct; type Warning struct; func Check(items []Item, cfg Config) ([]Warning, error); func DefaultConfig(s *fuzzymatch.Scorer) Config"
    - path: "scan/kind.go"
      provides: "Kind type + KindWithinGroup + KindAcrossGroups + String()"
      contains: "type Kind int; KindWithinGroup Kind = iota + 1; KindAcrossGroups; func (k Kind) String() string"
    - path: "scan/errors.go"
      provides: "Three scan sentinel errors with four-section godoc"
      contains: "var ErrNilScorer; var ErrInvalidItem; var ErrInvalidConfig"
    - path: "scan/doc.go"
      provides: "Package-level godoc landing page"
      contains: "Package scan is the Layer-3 collection-scan sub-package"
    - path: "scan/example_test.go"
      provides: "ExampleKind_String + ExampleDefaultConfig runnable examples"
    - path: "scan/scan_test.go"
      provides: "Type-assertion smoke tests + ErrNilScorer assertion against Check stub"
    - path: "scorer.go"
      provides: "Scorer.NormalisationOptions() accessor"
      contains: "func (s *Scorer) NormalisationOptions() (NormalisationOptions, bool)"
    - path: "docs/requirements.md"
      provides: "Spec amendments for D-01 (Scores map type) + D-02 (Kind rename) + D-04 (default-0.05 location) + D-03 (errors.Join phrasing) + Scorer.NormalisationOptions in §8"
  key_links:
    - from: "scan/scan.go Warning.Scores field"
      to: "fuzzymatch.AlgoID enum"
      via: "import alias"
      pattern: "Scores\\s+map\\[fuzzymatch\\.AlgoID\\]float64"
    - from: "scan/scan.go DefaultConfig"
      to: "fuzzymatch.Scorer parameter"
      via: "function parameter type"
      pattern: "func DefaultConfig\\(s \\*fuzzymatch\\.Scorer\\) Config"
    - from: "scan/errors.go sentinels"
      to: "consumer errors.Is discrimination"
      via: "errors.New + four-section godoc"
      pattern: "errors\\.Is\\(err, scan\\.ErrInvalidItem\\)"
    - from: "scorer.go Scorer.NormalisationOptions()"
      to: "scan/bucket.go and scan/suppress.go (Plans 09-04/05)"
      via: "public accessor returning (NormalisationOptions, bool)"
      pattern: "func \\(s \\*Scorer\\) NormalisationOptions\\(\\)"

user_setup: []
---

<objective>
Land the Phase 9 scan sub-package foundation — public types (Item, Config, Warning, Kind), three sentinel errors with four-section godoc, the Scorer.NormalisationOptions() accessor (resolves 09-RESEARCH.md Open Question 1), the DefaultConfig opinionated helper, package doc, and the SPEC OVERRIDE amendments to docs/requirements.md §12.1 + §12.4 + §15 + line 2010 + line 2061 — in lockstep, in one cluster of commits.

Purpose: Phase 9's eight subsequent plans all depend on these declarations and on the new Scorer.NormalisationOptions() accessor. The SPEC OVERRIDE pattern from Phase 7 Plan 07-02 and Phase 8 Plan 08-03 is followed verbatim: spec amendment and code declaration land together so the api-ergonomics-reviewer sign-off recorded in the PR/commit is the canonical record.

Output: A compilable scan/ skeleton plus a new Scorer accessor, ready for Plan 09-02 (validation pre-flight) to build against. No Check business logic yet — Check is a stub that returns (nil, ErrNilScorer) when cfg.Scorer == nil and (nil, nil) otherwise. The empty body is intentional; it gives downstream plans a callable shape without a half-finished algorithm landing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 9 inputs
@.planning/phases/09-collection-scan-sub-package/09-CONTEXT.md
@.planning/phases/09-collection-scan-sub-package/09-RESEARCH.md
@.planning/phases/09-collection-scan-sub-package/09-PATTERNS.md
@.planning/phases/09-collection-scan-sub-package/09-VALIDATION.md

# Prior phase context
@.planning/phases/08-composite-scorer/08-CONTEXT.md
@.planning/phases/08-composite-scorer/08-03-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-13-SUMMARY.md

# Project skills
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/documentation-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

# Source files the plan modifies or models on
@scorer.go
@warn_kind.go
@errors.go
@validate.go
@algoid.go

<interfaces>
<!-- Key types and contracts the executor consumes directly. -->

From scorer.go (immutable, post-NewScorer):
```go
type Scorer struct {
    threshold              float64
    algorithmsAlgoIDSorted []scorerEntry
    applyNormalisation     bool                  // unexported — see Task 2
    normaliseOpts          NormalisationOptions  // unexported — see Task 2
    // ... other fields
}

// Existing accessors (model for the new NormalisationOptions method):
func (s *Scorer) Threshold() float64              // scorer.go:447-449
func (s *Scorer) Algorithms() []ScorerAlgorithm   // scorer.go:466-472
func (s *Scorer) Score(a, b string) float64       // scorer.go:352-389
func (s *Scorer) ScoreAll(a, b string) map[AlgoID]float64  // scorer.go:503-523
func (s *Scorer) Match(a, b string) bool          // scorer.go:398-400
```

From algoid.go:
```go
type AlgoID int                  // algoid.go:47
func (a AlgoID) String() string  // CamelCase per Phase 8.5 Q6b
const AlgoIDAny AlgoID = -2       // not used in scan
```

From normalise.go:
```go
type NormalisationOptions struct { ... }                    // normalise.go:83
func DefaultNormalisationOptions() NormalisationOptions     // normalise.go:106
func Normalise(s string, opts NormalisationOptions) string  // normalise.go:156
```

From errors.go (four-section godoc template — copy verbatim for scan/errors.go):
```go
// ErrEmptyScorer indicates NewScorer was called without any algorithm
// option — the option slice contained zero WithAlgorithm /
// With*Algorithm entries by the time validation ran. A Scorer with no
// algorithms has no meaningful composite to compute.
//
// Pass at least one WithAlgorithm option (or use DefaultScorer() for
// the opinionated six-algorithm composition).
//
// Returned by NewScorer (Phase 8) after the missing-threshold check
// passes and the option-validation pipeline finds cfg.entries empty.
//
// Discriminate via errors.Is(err, fuzzymatch.ErrEmptyScorer); never
// match the error message string.
var ErrEmptyScorer = errors.New("fuzzymatch: scorer has no algorithms ...")
```

From warn_kind.go (Kind/String pattern — mirror verbatim for scan/kind.go):
```go
type WarnKind int
const (
    WarnEmptyInput WarnKind = iota + 1
    WarnUnequalLength
    // ...
)
func (k WarnKind) String() string {
    switch k {
    case WarnEmptyInput:
        return "EmptyInput"
    // ...
    default:
        return fmt.Sprintf("WarnKind(%d)", int(k))
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land scan/ skeleton (scan.go, kind.go, errors.go, doc.go) + smoke tests</name>
  <files>scan/scan.go, scan/kind.go, scan/errors.go, scan/doc.go, scan/example_test.go, scan/scan_test.go</files>
  <read_first>
    - .planning/phases/09-collection-scan-sub-package/09-CONTEXT.md §1 (D-01 Scores map type, D-02 Kind rename), §2 (D-04 DefaultConfig opinionated default), §3 (D-06 in-line completeness assertion reference)
    - .planning/phases/09-collection-scan-sub-package/09-PATTERNS.md "scan/scan.go", "scan/kind.go", "scan/errors.go", "scan/doc.go", "scan/example_test.go" sections
    - .planning/phases/09-collection-scan-sub-package/09-RESEARCH.md lines 651-740 (sentinel godoc + Kind String + DefaultConfig templates)
    - errors.go:15-35 (file-header godoc) + errors.go:157-170 (ErrEmptyScorer four-section template — copy structure)
    - warn_kind.go:38-97 (Kind type + iota + 1 + constants) + warn_kind.go:117-144 (String switch with allocating default)
    - algoid.go:47-... (AlgoID enum reference)
    - normalise.go:83-156 (NormalisationOptions type + Normalise signature — referenced by scan godoc)
    - scorer.go:15-74 (design-notes block pattern)
    - scorer.go:549-559 (DefaultScorerOptions opinionated-default godoc pattern)
    - docs/requirements.md §12 lines 1266-1473 (scan spec for context only — amendments land in Task 3)
  </read_first>
  <behavior>
    - Test 1: scan/ package compiles cleanly: `go build ./scan/...` exits 0
    - Test 2: scan.KindWithinGroup.String() returns "WithinGroup"; scan.KindAcrossGroups.String() returns "AcrossGroups"; an undefined Kind(99).String() returns "Kind(99)"
    - Test 3: errors.Is(scan.ErrNilScorer, scan.ErrNilScorer) == true; the three sentinels are distinct values (errors.Is(ErrNilScorer, ErrInvalidItem) == false)
    - Test 4: scan.Check(nil, scan.Config{Scorer: nil}) returns (nil, err) where errors.Is(err, scan.ErrNilScorer) == true
    - Test 5: scan.DefaultConfig(fuzzymatch.DefaultScorer()) returns a Config with CrossGroupThresholdBoost == 0.05, CompareAcrossGroups == false, CompareIdenticalAcrossGroups == false, SuppressedPairs == nil, and Scorer != nil
    - Test 6: scan.Warning.Scores has Go type `map[fuzzymatch.AlgoID]float64` (verified by `reflect.TypeOf(scan.Warning{}.Scores).String() == "map[fuzzymatch.AlgoID]float64"`)
    - Test 7: ExampleKind_String emits "WithinGroup\nAcrossGroups\n" — Output comment matches
    - Test 8: ExampleDefaultConfig builds a Config from fuzzymatch.DefaultScorer() and prints its CrossGroupThresholdBoost — exact stdout pinned via `// Output:`
  </behavior>
  <action>
    Create six files under scan/ following the analogs in 09-PATTERNS.md:

    (a) scan/scan.go — package scan declaration. Apache 2.0 header. File-header design-notes block (mirror scorer.go:15-74) explicitly listing the SPEC OVERRIDE notes for D-01 (Scores map type) and D-02 (Kind rename) with back-references to 09-CONTEXT.md §1. Define type Item struct {Name, Group string; SilenceLint bool; Tag any}. Define type Config struct {Scorer *fuzzymatch.Scorer; CompareAcrossGroups bool; CrossGroupThresholdBoost float64; CompareIdenticalAcrossGroups bool; SuppressedPairs [][2]string}. Define type Warning struct {Kind Kind; NameA, NameB string; GroupA, GroupB string; TagA, TagB any; Score float64; Scores map[fuzzymatch.AlgoID]float64}. The Warning.Scores godoc carries the SPEC OVERRIDE block citing D-01 verbatim. Define func DefaultConfig(s *fuzzymatch.Scorer) Config returning the opinionated defaults from 09-RESEARCH.md lines 731-739, with godoc citing D-04 + SPEC OVERRIDE for the 0.05 default location. Define func Check(items []Item, cfg Config) ([]Warning, error) as a stub: if cfg.Scorer == nil return (nil, ErrNilScorer); else return (nil, nil). Document the stub status in the func godoc explicitly ("Phase 9 Plan 09-03 lands the full body; this stub is intentional foundation scaffolding"). British English ("normalise", "behaviour", "synchronise") throughout godoc.

    (b) scan/kind.go — Apache 2.0 header. Package scan. Mirror warn_kind.go verbatim with rename Kind for scan. Type Kind int. Constants KindWithinGroup Kind = iota + 1; KindAcrossGroups. Function (k Kind) String() string with switch covering both constants returning "WithinGroup" / "AcrossGroups" and a default branch returning fmt.Sprintf("Kind(%d)", int(k)). Type-level godoc carries the SPEC OVERRIDE note citing D-02 with back-reference to 09-CONTEXT.md §1.

    (c) scan/errors.go — Apache 2.0 header. Package scan. File-header godoc following errors.go:15-35 (state convention: messages start with "scan:" prefix, lowercase, no trailing punctuation; sentinels are flat errors.New values). Declare three sentinels with four-section godoc (What / Common causes / Resolution / Example errors.Is snippet) per Phase 8.5 Q4 template (errors.go:157-170 is the closest analog). The sentinels are: ErrNilScorer (text "scan: Config.Scorer is required"), ErrInvalidItem (text "scan: invalid Item"), ErrInvalidConfig (text "scan: invalid Config"). ErrInvalidItem's godoc MUST explicitly note "When validation collects multiple invalid items, the returned error is errors.Join of multiple wrapped ErrInvalidItem values; errors.Is(err, scan.ErrInvalidItem) still discriminates because errors.Is walks Unwrap() []error (Go 1.20+)" — this is the D-03 / D-06 forward reference.

    (d) scan/doc.go — Apache 2.0 header. Package-level godoc (mirror tokenise.go top-of-file pattern + 09-PATTERNS.md scan/doc.go template lines 470-492). State the three-layer architecture position, typical usage example, link to docs/scan.md and docs/requirements.md §12.

    (e) scan/example_test.go — Package scan_test. Two runnable examples: ExampleKind_String (prints both constants on separate lines, `// Output:` block) and ExampleDefaultConfig (constructs DefaultConfig from fuzzymatch.DefaultScorer(), prints CrossGroupThresholdBoost via fmt.Printf("%.2f\n", cfg.CrossGroupThresholdBoost) so the output is byte-stable; `// Output: 0.05`).

    (f) scan/scan_test.go — Package scan_test. Tests: TestKind_String (table-driven covering KindWithinGroup, KindAcrossGroups, Kind(99) fallback); TestSentinels_Distinct (asserts errors.Is(ErrNilScorer, ErrInvalidItem) == false, errors.Is(ErrInvalidItem, ErrInvalidConfig) == false, all three errors.Is identity); TestCheck_NilScorer (calls scan.Check(nil, scan.Config{Scorer: nil}) and asserts errors.Is(err, scan.ErrNilScorer) and len(warnings) == 0); TestDefaultConfig_Defaults (constructs from fuzzymatch.DefaultScorer() and asserts every field); TestWarning_ScoresTypeIsAlgoID (uses reflect to assert the type literal string).

    Stdlib `testing` only — NO testify (CLAUDE.md root-test rule).
  </action>
  <verify>
    <automated>go test -race -shuffle=on -count=1 ./scan/... -run "TestKind_String|TestSentinels_Distinct|TestCheck_NilScorer|TestDefaultConfig_Defaults|TestWarning_ScoresTypeIsAlgoID|ExampleKind_String|ExampleDefaultConfig"</automated>
  </verify>
  <acceptance_criteria>
    - scan/ directory exists with six files (scan.go, kind.go, errors.go, doc.go, example_test.go, scan_test.go)
    - `go build ./scan/...` exits 0 with no warnings
    - `go vet ./scan/...` clean
    - All 5 Test* functions PASS; both Example functions emit matching Output blocks
    - Warning.Scores field type is exactly `map[fuzzymatch.AlgoID]float64` (verified by reflect-based test)
    - DefaultConfig returns CrossGroupThresholdBoost == 0.05 and CompareIdenticalAcrossGroups == false
    - All three sentinel godocs have the four-section structure (What / Common causes / Resolution / Example)
    - SPEC OVERRIDE notes present in Warning.Scores godoc (D-01) and Kind type godoc (D-02), each back-referencing 09-CONTEXT.md §1
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Scorer.NormalisationOptions() accessor on *fuzzymatch.Scorer (resolves Open Question 1)</name>
  <files>scorer.go, scorer_test.go</files>
  <read_first>
    - .planning/phases/09-collection-scan-sub-package/09-RESEARCH.md §"Open Questions" 1 (lines 860-877) — Option A recommendation with rationale
    - .planning/phases/09-collection-scan-sub-package/09-PATTERNS.md "scorer.go (MODIFIED)" section lines 1063-1099 (verbatim accessor template)
    - scorer.go:115-128 (applyNormalisation + normaliseOpts struct field godoc — unexported, by-value)
    - scorer.go:447-449 (Threshold() accessor — exact pattern to mirror)
    - scorer.go:466-472 (Algorithms() accessor — concurrency-safety godoc reference)
    - scorer.go:60-66 (immutability godoc + concurrent-safe guarantees referenced from new method godoc)
    - normalise.go:83 (NormalisationOptions struct definition — return type)
  </read_first>
  <behavior>
    - Test 1: A Scorer constructed via fuzzymatch.DefaultScorer() returns NormalisationOptions() == (DefaultNormalisationOptions(), true) — applied == true
    - Test 2: A Scorer constructed via fuzzymatch.NewScorer(opts..., fuzzymatch.WithoutNormalisation()) returns NormalisationOptions() == (DefaultNormalisationOptions(), false) — applied == false (the opts value is the zero-config default; the bool flag distinguishes)
    - Test 3: A Scorer constructed with fuzzymatch.WithNormalisation(customOpts) returns NormalisationOptions() == (customOpts, true) byte-for-byte
    - Test 4: Concurrent invocation: 100 goroutines call s.NormalisationOptions() simultaneously and all observe identical results (race-detector clean under `go test -race`)
    - Test 5: The returned NormalisationOptions is by-value — mutating the returned struct does not affect the Scorer's internal options (subsequent NormalisationOptions() call still returns the original values)
  </behavior>
  <action>
    Append a new method to scorer.go immediately after Algorithms() (anchor: scorer.go:472). Copy the signature template from 09-PATTERNS.md lines 1078-1097 verbatim, adapting the British-English spelling ("Normalisation" not "Normalization"):

    Function signature: `func (s *Scorer) NormalisationOptions() (opts NormalisationOptions, applied bool)`. Implementation is two lines: `return s.normaliseOpts, s.applyNormalisation`. The godoc block MUST cover (per D-01/D-02 sign-off precedent): (1) what the method returns; (2) the returned options are by-value (immutability respect); (3) typical use by the scan sub-package per spec §12.3 + §12.5 + 09-CONTEXT.md §4; (4) concurrency-safe (Scorer immutable after NewScorer per scorer.go:60-66). Document in the godoc that the spec amendment lands in docs/requirements.md §8 in Task 3.

    Add unit tests to scorer_test.go (package fuzzymatch_test): TestScorer_NormalisationOptions_Default (calls DefaultScorer().NormalisationOptions(), asserts (DefaultNormalisationOptions(), true)); TestScorer_NormalisationOptions_WithoutNormalisation (constructs via NewScorer with WithThreshold(0.85) + WithAlgorithm + WithoutNormalisation, asserts applied == false); TestScorer_NormalisationOptions_WithCustom (constructs with WithNormalisation(custom), asserts byte-for-byte equality of the returned NormalisationOptions); TestScorer_NormalisationOptions_ByValue (calls NormalisationOptions(), mutates the returned struct's fields, calls NormalisationOptions() again, asserts the second call returns the original values — proves by-value semantics).

    Run `go test -race -shuffle=on -count=1 ./... -run TestScorer_NormalisationOptions` and verify all four tests PASS with the race detector clean.
  </action>
  <verify>
    <automated>go test -race -shuffle=on -count=1 ./... -run "TestScorer_NormalisationOptions_Default|TestScorer_NormalisationOptions_WithoutNormalisation|TestScorer_NormalisationOptions_WithCustom|TestScorer_NormalisationOptions_ByValue"</automated>
  </verify>
  <acceptance_criteria>
    - scorer.go contains func (s *Scorer) NormalisationOptions() (opts NormalisationOptions, applied bool) immediately after Algorithms()
    - Method body is a single return statement reading s.normaliseOpts and s.applyNormalisation
    - Method godoc has at least 12 lines covering: purpose, return-by-value immutability, scan sub-package consumer, concurrency-safe, spec amendment reference
    - All 4 tests added to scorer_test.go pass under `go test -race -shuffle=on -count=1 ./...`
    - `go vet ./...` clean; `grep -c "func (s \*Scorer) NormalisationOptions" scorer.go` returns 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Amend docs/requirements.md §12.1 / §12.4 / §15 / line 2010 / line 2061 + add Scorer.NormalisationOptions() to §8 (SPEC OVERRIDE lockstep)</name>
  <files>docs/requirements.md, llms.txt, llms-full.txt, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/09-collection-scan-sub-package/09-CONTEXT.md §"Specific Ideas" lines 201-207 (the five exact spec-amendment locations)
    - .planning/phases/09-collection-scan-sub-package/09-RESEARCH.md lines 837-841 ("Deprecated / outdated" enumeration) + lines 1109-1134 (SPEC OVERRIDE inline-note template)
    - .planning/phases/09-collection-scan-sub-package/09-PATTERNS.md "docs/requirements.md (MODIFIED)" lines 1103-1134
    - docs/requirements.md lines 1266-1473 (entire §12 in its current pre-amendment state) — read once via the file Read tool
    - docs/requirements.md line 212 + 1022 + 1079 (Phase 8 SPEC OVERRIDE precedent for ScoreAll → reuse exact tone and phrasing)
    - llms.txt (current head — confirm Phase 9 placeholder absence)
    - llms-full.txt scan section (currently scaffold)
    - CHANGELOG.md [Unreleased] section
  </read_first>
  <behavior>
    - Test 1: grep -nc "WarningKind" docs/requirements.md returns 0 (all references renamed to Kind in scan-package context; the root WarnKind type is untouched)
    - Test 2: grep -n "Scores map\[AlgoID\]float64" docs/requirements.md §12.1 returns at least one hit (D-01 amendment applied)
    - Test 3: grep -n "errors.Join" docs/requirements.md §12.1 returns at least one hit (D-03 line 1389 phrasing updated)
    - Test 4: docs/requirements.md §8 contains a new accessor entry for Scorer.NormalisationOptions() with signature and one-paragraph description
    - Test 5: docs/requirements.md contains at least two "SPEC OVERRIDE (Phase 9)" inline notes in §12.1 (for D-01 and D-02) each citing 09-CONTEXT.md §1
    - Test 6: markdownlint-cli2 docs/requirements.md exits 0
    - Test 7: CHANGELOG.md [Unreleased] section under "Added" has one entry for scan sub-package scaffolding and one entry for Scorer.NormalisationOptions accessor; "Changed" lists the SPEC OVERRIDE amendments
    - Test 8: llms.txt and llms-full.txt contain entries for scan.Kind, scan.Item, scan.Config, scan.Warning, scan.Check, scan.DefaultConfig, scan.ErrNilScorer, scan.ErrInvalidItem, scan.ErrInvalidConfig, and Scorer.NormalisationOptions
  </behavior>
  <action>
    Edit docs/requirements.md using the Edit tool, ONE edit per amendment:

    (1) §12.1 lines 1306-1322: rename `WarningKind` → `Kind` throughout the type declaration, constants, and the String() method signature. Insert above the type a `> **SPEC OVERRIDE (Phase 9):** Type renamed from WarningKind to Kind per 09-CONTEXT.md §1 D-02 — the package-scoped name is unambiguous at the call site (scan.KindWithinGroup) and avoids accidental symmetry with root's WarnKind. api-ergonomics-reviewer signed off on this override in plan 09-01.` block.

    (2) §12.1 line 1328: the Warning struct `Kind WarningKind` field renames to `Kind Kind`.

    (3) §12.1 line 1337: change `Scores map[string]float64` → `Scores map[AlgoID]float64` and add an inline `// SPEC OVERRIDE (Phase 9): Scores is map[AlgoID]float64; see 09-CONTEXT.md §1 D-01. Extends the Phase 8 ScoreAll override at §8.3 + §8.6.` comment immediately above the field.

    (4) §12.1 line 1355-1360 (the CrossGroupThresholdBoost block): replace the existing "Default: 0.05" sentence with a forward reference to DefaultConfig: "Zero-value is 0.0 (no boost). The opinionated default 0.05 is supplied by `scan.DefaultConfig(s *fuzzymatch.Scorer) Config`. SPEC OVERRIDE (Phase 9): default-0.05 location migrated to DefaultConfig per 09-CONTEXT.md §2 D-04."

    (5) §12.1 line 1389: change "wrapped with the offending item's index if any item has an empty Name" → "every offending index, joined via errors.Join — caller's errors.Is(err, scan.ErrInvalidItem) discriminates because errors.Is walks Unwrap() []error". Cite 09-CONTEXT.md §2 D-03 + §3 D-06.

    (6) §12.4 + §12.6 + §15 + line 1639 (`features/scan.feature`): rename every `WarningKind` reference encountered to `Kind`.

    (7) §19 line 2010 + line 2061: rename `WarningKind` → `Kind` in the phase-plan REQUIREMENTS row and the Scan acceptance criteria.

    (8) §8 (Scorer accessors): add a new method entry under the Scorer accessor list following the Threshold/Algorithms pattern. Signature: `func (s *Scorer) NormalisationOptions() (opts NormalisationOptions, applied bool)`. One-paragraph description: "Returns the NormalisationOptions stored at construction time and a boolean indicating whether the Scorer applies normalisation. Used by the scan sub-package to build token buckets and canonicalise SuppressedPairs entries using the same normalisation pipeline the Scorer uses for scoring. Safe for concurrent use; the Scorer is immutable after NewScorer."

    Update llms.txt: under the "Scan sub-package (planned for Phase 9)" section, replace the scaffold note with concrete entries for the 9 new exported symbols + Scorer.NormalisationOptions. Update llms-full.txt with full godoc-style signatures for each (mirror the format used for Validate / Warning / WarnKind in Phase 8.5 Plan 17b output).

    Update CHANGELOG.md [Unreleased]: under "### Added" add bullets for `scan` sub-package public surface (Item, Config, Warning, Kind, KindWithinGroup, KindAcrossGroups, DefaultConfig, Check stub, ErrNilScorer, ErrInvalidItem, ErrInvalidConfig) and `Scorer.NormalisationOptions() (NormalisationOptions, bool)` accessor. Under "### Changed" add a bullet for the docs/requirements.md §12 SPEC OVERRIDE amendments (D-01 + D-02 + D-04 + D-03 line 1389 phrasing). Reference 09-CONTEXT.md §1 / §2 / §3.

    Verify all amendments with: `grep -nc "WarningKind" docs/requirements.md` (expect 0 in §12 context — root WarnKind is unaffected since it lives in §11.5); `grep -n "SPEC OVERRIDE (Phase 9)" docs/requirements.md` (expect ≥ 2 hits in §12.1); `markdownlint-cli2 docs/requirements.md llms.txt llms-full.txt CHANGELOG.md` exits 0.
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -v "^#" docs/requirements.md | grep -c "WarningKind" | xargs -I{} test {} -eq 0 && grep -n "Scores map\[AlgoID\]float64" docs/requirements.md && grep -n "errors.Join" docs/requirements.md | grep -q "§12\|line 1389\|every offending index" && grep -n "NormalisationOptions" docs/requirements.md && grep -c "SPEC OVERRIDE (Phase 9)" docs/requirements.md | xargs -I{} test {} -ge 2 && markdownlint-cli2 docs/requirements.md llms.txt llms-full.txt CHANGELOG.md'</automated>
  </verify>
  <acceptance_criteria>
    - docs/requirements.md §12 has zero "WarningKind" tokens after the rename sweep (verified by `grep -v '^#' docs/requirements.md | grep -c WarningKind` returning 0; comments are filtered to avoid self-invalidation)
    - docs/requirements.md §12.1 contains the new Scores map type, the new line-1389 phrasing, and the migrated 0.05 default location
    - docs/requirements.md §8 has a new entry for Scorer.NormalisationOptions() with signature + description
    - At least 2 inline "SPEC OVERRIDE (Phase 9)" notes in §12.1
    - llms.txt + llms-full.txt updated with the 9 scan symbols + Scorer.NormalisationOptions
    - CHANGELOG.md [Unreleased] has Added + Changed entries
    - `markdownlint-cli2 docs/requirements.md llms.txt llms-full.txt CHANGELOG.md` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Final quality gate + agent reviews + commit cluster</name>
  <files>(no new files — quality gate + reviews + commits)</files>
  <read_first>
    - .claude/skills/fuzzymatch-review-protocol/SKILL.md (mandatory reviewer order)
    - .claude/skills/go-coding-standards/SKILL.md (British English, no testify in root, error message convention)
    - .planning/phases/09-collection-scan-sub-package/09-CONTEXT.md §"Specific Ideas" (api-ergonomics-reviewer sign-off requirement)
    - CHANGELOG.md (drafted in Task 3)
  </read_first>
  <action>
    Run the full local quality gate:

    (a) `make verify-deps-allowlist` — confirms root go.mod still has zero non-stdlib runtime require lines (scan/ adds no deps; only stdlib + fuzzymatch's own pkg)
    (b) `make verify-license-headers` — confirms all 6 new scan/ files carry the Apache-2.0 header
    (c) `go test -race -shuffle=on -count=1 ./...` — full root test suite plus scan/ plus scorer_test.go new tests
    (d) `go vet ./...` — clean
    (e) `golangci-lint run --timeout=5m ./...` — clean (no new lint findings)
    (f) `markdownlint-cli2 docs/requirements.md llms.txt llms-full.txt CHANGELOG.md docs/scan.md` — clean
    (g) `make verify-llms-sync` — clean (advisory llms-full.txt OK; strict llms.txt MUST be green)

    Then spawn the mandatory reviewers in order per fuzzymatch-review-protocol skill:
    (i) api-ergonomics-reviewer on the foundation surface (D-01 Scores type, D-02 Kind rename, Scorer.NormalisationOptions name and shape, DefaultConfig opinionated-helper pattern). Record verdict + any conditions in this plan's SUMMARY.md and in the commit message trailer.
    (ii) code-reviewer on the diff (focus: SPEC OVERRIDE inline godoc, four-section sentinel godoc, accessor immutability).
    (iii) security-reviewer (focus: Tag any field not leaking into error messages; ErrInvalidItem index in message is consumer-owned data, already in scope).
    (iv) go-quality (final automated gate).
    (v) commit-message-reviewer on the draft commits (skip issue-ref findings per project memory `project_no_github_issues.md`).

    Stage commits in atomic groups (use `git add <file>` per group, not `git add -A`):
    (1) feat(scan): foundation skeleton — Item/Config/Warning/Kind + 3 sentinels + DefaultConfig + Check stub + scan/doc.go + example_test.go + scan_test.go [Task 1 output]
    (2) feat(scorer): add NormalisationOptions accessor — resolves Open Question 1 for scan sub-package [Task 2 output]
    (3) docs(spec): amend docs/requirements.md §8 + §12.1 + §12.4 + §15 + lines 2010, 2061 in lockstep with D-01 / D-02 / D-03 / D-04 SPEC OVERRIDE — api-ergonomics-reviewer signed off [Task 3 output, includes llms.txt/llms-full.txt/CHANGELOG]

    NEVER use `--no-verify`. Pre-commit hooks must pass. If a hook fails, fix the underlying issue and create a NEW commit (never amend).
  </action>
  <verify>
    <automated>bash -c 'set -e; make verify-deps-allowlist && make verify-license-headers && go test -race -shuffle=on -count=1 ./... && go vet ./... && make verify-llms-sync && markdownlint-cli2 docs/requirements.md llms.txt llms-full.txt CHANGELOG.md docs/scan.md && git log --oneline | head -5 | grep -E "(feat\(scan\)|feat\(scorer\)|docs\(spec\))"'</automated>
  </verify>
  <acceptance_criteria>
    - Full make check green (test + vet + lint + markdownlint + llms-sync + deps-allowlist + license-headers)
    - Five mandatory reviewers run with verdicts recorded in 09-01-SUMMARY.md
    - api-ergonomics-reviewer sign-off explicit on D-01 + D-02 + Scorer.NormalisationOptions naming + DefaultConfig opinionated-helper shape
    - Three commits landed in order on the worktree branch; commit-message-reviewer green (or noted issue-ref skip)
    - `git status` clean (no uncommitted changes)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 9 scan sub-package foundation + SPEC OVERRIDE amendments:
    - scan/ sub-package with 6 files: scan.go (Item/Config/Warning/Check stub/DefaultConfig), kind.go (Kind + String), errors.go (3 sentinels), doc.go, example_test.go (2 examples), scan_test.go (5 tests)
    - scorer.go: new Scorer.NormalisationOptions() (NormalisationOptions, bool) accessor + 4 unit tests
    - docs/requirements.md: amended §12.1 + §12.4 + §15 + §8 + lines 2010, 2061 for D-01 (Scores map[AlgoID]float64) + D-02 (WarningKind→Kind) + D-04 (0.05 default location) + D-03 (errors.Join phrasing)
    - llms.txt + llms-full.txt + CHANGELOG.md synced
    - Three atomic commits on worktree branch; api-ergonomics-reviewer sign-off recorded
  </what-built>
  <how-to-verify>
    1. Spot-check the scan/ surface compiles in a fresh shell:
       ```
       go build ./scan/...
       go test -race -shuffle=on -count=1 ./scan/...
       ```
       Expect: zero failures.

    2. Spot-check the Scorer.NormalisationOptions accessor:
       ```
       go doc -all ./... | grep -A 15 "func .Scorer. NormalisationOptions"
       ```
       Expect: signature + multi-line godoc with "Used by the scan sub-package" and "Safe for concurrent use".

    3. Confirm the SPEC OVERRIDE amendments are visible:
       ```
       grep -c "SPEC OVERRIDE (Phase 9)" docs/requirements.md
       ```
       Expect: ≥ 2 hits.

    4. Confirm sentinel godoc structure: open scan/errors.go in your editor and verify each sentinel has four paragraphs (What / Common causes / Resolution / Example).

    5. Confirm the api-ergonomics-reviewer sign-off is recorded in 09-01-SUMMARY.md or the third commit's body (whichever was chosen).

    6. Run `git log -3 --oneline` and verify three commits with feat(scan) / feat(scorer) / docs(spec) prefixes.

    7. Confirm root go.mod is untouched: `grep -c require go.mod` returns the same count as before this plan (zero non-stdlib runtime requires).

    Approve only if all 7 checks pass.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Plan 09-02, or describe any issues for follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → scan public API | Item.Name, Item.Group, Item.Tag, Config fields are untrusted consumer input |
| docs/requirements.md spec → executor | spec text is authoritative; executor must not paraphrase the SPEC OVERRIDE phrasing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Information Disclosure | scan.Warning.Tag in error messages | mitigate | Item.Tag never appears in error wrapping; only the int index is included. Documented in Item.Tag godoc. Test TestSentinels_Distinct + future TestCheck_ErrInvalidItem_NoTagLeakage gates. |
| T-09-01-02 | Tampering | docs/requirements.md spec drift | mitigate | All amendments in a single docs(spec) commit; markdownlint-cli2 gate; grep -nc WarningKind gate on commented-filtered file ensures rename completeness |
| T-09-01-03 | Information Disclosure | Scorer internal options leaking through NormalisationOptions() | accept | The fields are immutable post-NewScorer; returning by-value is the documented contract; no secret state in NormalisationOptions |
| T-09-01-04 | Denial of Service | scan.Check stub returning nil for arbitrary input | accept | Stub is intentional Phase 9 Plan 09-03 scaffold; downstream plans land validation + bucketing; the Wave 1 contract is "compilable + ErrNilScorer triggers" only |
</threat_model>

<verification>
- `make verify-deps-allowlist` green (zero non-stdlib runtime requires in root go.mod)
- `make verify-license-headers` green (Apache-2.0 header on all 6 new scan/ files)
- `go test -race -shuffle=on -count=1 ./...` all pass including new tests
- `go vet ./...` clean
- `golangci-lint run --timeout=5m ./...` clean
- `markdownlint-cli2 docs/requirements.md llms.txt llms-full.txt CHANGELOG.md docs/scan.md` exits 0
- `make verify-llms-sync` green (llms.txt strict; llms-full.txt advisory)
- `grep -v '^#' docs/requirements.md | grep -c WarningKind` returns 0
- `grep -c "SPEC OVERRIDE (Phase 9)" docs/requirements.md` returns ≥ 2
- api-ergonomics-reviewer + code-reviewer + security-reviewer + go-quality + commit-message-reviewer verdicts recorded in 09-01-SUMMARY.md
- Three commits landed: feat(scan), feat(scorer), docs(spec)
</verification>

<success_criteria>
- scan/ sub-package compiles cleanly under the root module with zero new non-stdlib runtime dependencies
- scan.Item, scan.Config, scan.Warning, scan.Kind, scan.DefaultConfig, scan.Check stub, three sentinels exported
- Scorer.NormalisationOptions() (NormalisationOptions, bool) accessor exists and is concurrent-safe
- docs/requirements.md §12.1 + §12.4 + §15 + §8 + lines 2010, 2061 amended for D-01 / D-02 / D-03 / D-04
- llms.txt + llms-full.txt + CHANGELOG.md updated
- api-ergonomics-reviewer sign-off recorded in plan SUMMARY/commit trailer
- Full make check green
- Plan 09-02 has a callable scan.Check function and the Scorer.NormalisationOptions accessor it depends on
</success_criteria>

<output>
After completion, create `.planning/phases/09-collection-scan-sub-package/09-01-SUMMARY.md` capturing:
- Files created (6 in scan/) + files modified (scorer.go, scorer_test.go, docs/requirements.md, llms.txt, llms-full.txt, CHANGELOG.md)
- api-ergonomics-reviewer verdict + any conditions imposed
- The exact value of bucketThreshold = 50 (forward-reference only; no change in this plan)
- Confirmation that the Scorer.NormalisationOptions accessor was added in this plan (consumed by Plans 09-04 and 09-05)
- Three commit SHAs on the worktree branch
</output>
