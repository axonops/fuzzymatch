---
phase: 09
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - scan/validate.go
  - scan/validate_test.go
autonomous: true
requirements: [SCAN-01, SCAN-06]
tags: [scan, phase-9, validation, errors-join, d-03, d-04, d-05, d-06, pre-flight]

must_haves:
  truths:
    - "validateCheck(items, cfg) returns ErrNilScorer immediately when cfg.Scorer == nil (P1 fail-fast)"
    - "validateCheck returns ErrInvalidConfig immediately when CrossGroupThresholdBoost is NaN, ±Inf, < 0, or > 1 (P2 fail-fast per D-04)"
    - "validateCheck collects ALL empty-Name indices into errors.Join wrapped ErrInvalidItem values, walking items[] in slice order (P3 collect-all per D-03)"
    - "validateCheck collects ALL duplicate (Name, Group) indices into errors.Join wrapped ErrInvalidItem values (P3 collect-all per D-06)"
    - "validateCheck collects ALL empty-string SuppressedPairs entries into errors.Join wrapped ErrInvalidConfig values (P4 collect-all per D-05)"
    - "Self-pairs in SuppressedPairs (a == b after lookup) are silently kept (D-05 explicit semantics)"
    - "For ANY accumulated error path, errors.Is(err, scan.ErrInvalidItem) == true on the joined value (Go 1.20+ Unwrap() []error walk)"
    - "Error messages include the offending index (and for duplicates, the first-seen index) so callers can pinpoint failures without re-walking"
  artifacts:
    - path: "scan/validate.go"
      provides: "Internal validation pipeline P1..P4 with errors.Join discipline"
      contains: "func validateCheck(items []Item, cfg Config) error; func validateConfigFields(cfg Config) error; func validateItems(items []Item) error; func validateSuppressedPairs(pairs [][2]string) error"
    - path: "scan/validate_test.go"
      provides: "Unit tests covering every P1..P4 path plus errors.Is/errors.Join discrimination"
  key_links:
    - from: "scan/validate.go validateItems"
      to: "scan.ErrInvalidItem sentinel"
      via: "fmt.Errorf with %w wrap"
      pattern: "fmt\\.Errorf\\(.*: %w.*, ErrInvalidItem\\)"
    - from: "scan/validate.go validateSuppressedPairs"
      to: "scan.ErrInvalidConfig sentinel"
      via: "fmt.Errorf with %w wrap"
      pattern: "fmt\\.Errorf\\(.*: %w.*, ErrInvalidConfig\\)"
    - from: "scan/validate.go validateItems duplicate detection"
      to: "internal itemKey map (input-position seen[k] → first-seen index)"
      via: "map[itemKey]int built during walk"
      pattern: "seen\\[k\\]"
user_setup: []
---

<objective>
Land scan/validate.go: the internal pre-flight pipeline P1..P4 enforcing the Phase 8.5 Q2 framework (parameters strict, comparison data lenient) on the scan sub-package's surface. Three coupled errors.Join collect-all paths (D-03 empty Name, D-05 empty SuppressedPairs entry, D-06 duplicate (Name, Group)) plus two fail-fast checks (P1 nil Scorer, P2 CrossGroupThresholdBoost range).

Purpose: D-06's "in-line completeness assertion becomes a hard invariant" guarantee (rather than a tiebreaker) hinges on duplicate (Name, Group) being rejected at the door. Without P3's duplicate-detection pass, the sort key in Plan 09-06 cannot be proven unique by construction. The Phase 8.5 Q4 errors.Join discipline is locked in this plan and reused unchanged across the three validation paths.

Output: A pure validation function that scan.Check will invoke as its first step in Plan 09-03. No business logic yet — Check stub from Plan 09-01 still returns (nil, nil) when validation passes; Plan 09-03 lands the within/cross-group passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Plan inputs
@.planning/phases/09-collection-scan-sub-package/09-CONTEXT.md
@.planning/phases/09-collection-scan-sub-package/09-RESEARCH.md
@.planning/phases/09-collection-scan-sub-package/09-PATTERNS.md
@.planning/phases/09-collection-scan-sub-package/09-VALIDATION.md
@.planning/phases/09-collection-scan-sub-package/09-01-SUMMARY.md

# Prior phase context
@.planning/phases/08.5-review-remediation-gate/08.5-13-SUMMARY.md

# Project skills
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

# Source files (analogs)
@validate.go
@scorer.go

# Plan 09-01 declarations (consumer scope)
@scan/scan.go
@scan/errors.go

<interfaces>
<!-- From scan/scan.go (declared in Plan 09-01): -->

```go
type Item struct {
    Name        string
    Group       string
    SilenceLint bool
    Tag         any
}

type Config struct {
    Scorer                       *fuzzymatch.Scorer
    CompareAcrossGroups          bool
    CrossGroupThresholdBoost     float64
    CompareIdenticalAcrossGroups bool
    SuppressedPairs              [][2]string
}

// From scan/errors.go (declared in Plan 09-01):
var ErrNilScorer       = errors.New("scan: Config.Scorer is required")
var ErrInvalidItem     = errors.New("scan: invalid Item")
var ErrInvalidConfig   = errors.New("scan: invalid Config")
```

<!-- From the analog validate.go (root pkg): -->

```go
// validatePathologicalThreshold = 65_536 — the consumer-supplied
// input-size ceiling for Validate (NOT used here; scan inherits this
// via the Scorer's per-pair calls in Plan 09-03).

// sort.SliceStable on a complete-key comparator — the discipline scan
// uses for the final warning sort (Plan 09-06), NOT for error
// accumulation (errors.Join keeps natural index order).
```

<!-- Errors.Join semantics (Go 1.20+): -->
```go
// errors.Is(joined, target) returns true if any err in joined's
// Unwrap() []error matches target via errors.Is. Discrimination across
// multiple wrapped ErrInvalidItem values is preserved.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the validate_test.go failing-test suite for P1..P4 (TDD RED)</name>
  <files>scan/validate_test.go</files>
  <read_first>
    - .planning/phases/09-collection-scan-sub-package/09-CONTEXT.md §2 (D-03 empty Name + D-04 Config boost validation + D-05 SuppressedPairs validation + D-06 duplicate (Name, Group))
    - .planning/phases/09-collection-scan-sub-package/09-RESEARCH.md lines 162-186 (validation flow diagram) + lines 317-356 (errors.Join Pattern 2 template) + lines 757-792 (full validateCheck pipeline)
    - .planning/phases/09-collection-scan-sub-package/09-PATTERNS.md scan/validate.go section (lines 281-353) — pipeline ordering + errors.Join template + "no post-sort" note
    - scan/scan.go (Plan 09-01 — Item, Config, Warning, Check stub)
    - scan/errors.go (Plan 09-01 — three sentinels with four-section godoc)
    - validate.go:60-73 (analog: validatePathologicalThreshold constant pattern)
    - errors.go:213-240 (ErrInternalInvariantViolated reference for understanding the four-section godoc echo)
  </read_first>
  <behavior>
    Write failing tests in scan/validate_test.go covering every locked behaviour. All tests use stdlib testing only (no testify in root). Tests fail because scan/validate.go does not yet exist.

    - Test 1 (TestValidate_NilScorer_ReturnsErrNilScorer): cfg.Scorer == nil → returns ErrNilScorer directly (sentinel identity, not wrap)
    - Test 2 (TestValidate_NaNBoost_ReturnsErrInvalidConfig): CrossGroupThresholdBoost == math.NaN() → errors.Is(err, ErrInvalidConfig) == true; message contains "NaN" or "invalid"
    - Test 3 (TestValidate_PosInfBoost_ReturnsErrInvalidConfig): CrossGroupThresholdBoost == math.Inf(+1) → ErrInvalidConfig
    - Test 4 (TestValidate_NegInfBoost_ReturnsErrInvalidConfig): CrossGroupThresholdBoost == math.Inf(-1) → ErrInvalidConfig
    - Test 5 (TestValidate_NegativeBoost_ReturnsErrInvalidConfig): CrossGroupThresholdBoost == -0.1 → ErrInvalidConfig
    - Test 6 (TestValidate_OverflowBoost_ReturnsErrInvalidConfig): CrossGroupThresholdBoost == 1.5 → ErrInvalidConfig
    - Test 7 (TestValidate_ZeroBoost_OK): CrossGroupThresholdBoost == 0.0 → no boost-related error
    - Test 8 (TestValidate_UnitBoost_OK): CrossGroupThresholdBoost == 1.0 → no boost-related error
    - Test 9 (TestValidate_SingleEmptyName_CollectsViaJoin): items[3].Name == "" → errors.Is(err, ErrInvalidItem) == true; err.Error() contains "index 3"
    - Test 10 (TestValidate_MultipleEmptyNames_CollectsAllViaJoin): items[1] and items[4] and items[7] have empty Name → err.Error() mentions all three indices; errors.Is(err, ErrInvalidItem) still true; the wrapped errors are exactly 3 (count via errors.As/Unwrap walk)
    - Test 11 (TestValidate_DuplicateNameGroup_CollectsViaJoin): items[0] = {Name:"user_id", Group:"login"}, items[2] = {Name:"user_id", Group:"login"} → errors.Is(err, ErrInvalidItem); err.Error() contains "index 2" and "duplicate (Name, Group) of index 0"
    - Test 12 (TestValidate_DuplicatePlusEmptyName_CollectsBoth): mixed input with both kinds — both errors appear in the joined output
    - Test 13 (TestValidate_DifferentGroups_NoDuplicate): items[0] = {Name:"user_id", Group:"login"}, items[1] = {Name:"user_id", Group:"profile"} → no duplicate error (Group disambiguates)
    - Test 14 (TestValidate_EmptySuppressedPair_CollectsViaJoin): SuppressedPairs = [["a","b"], ["","x"]] → errors.Is(err, ErrInvalidConfig); err.Error() contains "SuppressedPairs[1]"; both sides ("" left and "" right) tested
    - Test 15 (TestValidate_MultipleEmptySuppressedPairs_CollectsAll): three empty entries at indices 1, 3, 5 → all appear in joined output
    - Test 16 (TestValidate_SelfPairInSuppressed_OK): SuppressedPairs = [["abc","abc"]] → returns nil (D-05 self-pair silently kept)
    - Test 17 (TestValidate_FailFastOrder_NilScorerBeforeItemsCheck): cfg.Scorer == nil AND items[0].Name == "" → returns ErrNilScorer (NOT ErrInvalidItem); P1 fires first per the pipeline order
    - Test 18 (TestValidate_FailFastOrder_BoostBeforeItemsCheck): cfg.Scorer != nil, cfg.CrossGroupThresholdBoost == math.NaN, items[0].Name == "" → returns ErrInvalidConfig (boost) NOT ErrInvalidItem; P2 fires before P3
    - Test 19 (TestValidate_EmptyItems_NoError): items == nil and cfg.Scorer != nil and cfg.CrossGroupThresholdBoost in range → returns nil (empty slice is valid — zero-warning Check)
    - Test 20 (TestValidate_GoodInput_NoError): typical valid configuration → returns nil
    - Test 21 (TestValidate_JoinedError_DiscriminatesAllSentinels): in a Phase mixing P3 and P4 errors (skip — pipeline order means P3 collect happens before P4 so if P3 errors exist, the function returns at P3 boundary; document this in P3/P4 boundary godoc and verify test). Actually: each Phase returns its joined error before moving on. Test that within P3, items errors join cleanly; within P4 SuppressedPairs errors join cleanly. Cross-phase mixing is NOT supported by the pipeline (validate this in TestValidate_FailFastOrder_P3BeforeP4).
    - Test 22 (TestValidate_FailFastOrder_ItemsBeforeSuppressedPairs): valid Scorer + valid boost + items with empty Name + SuppressedPairs with empty string → returns the items-errors join (P3) without checking SuppressedPairs (P4). Document this is fail-fast between phases, collect-all within a phase.

    Run `go test ./scan/...` and confirm all 22 tests FAIL with "undefined: validateCheck" or similar — TDD RED phase verified.
  </behavior>
  <action>
    Create scan/validate_test.go in the scan_test package (black-box). Import "errors", "fmt", "math", "strings", "testing", "github.com/axonops/fuzzymatch", "github.com/axonops/fuzzymatch/scan".

    Define a helper newGoodScorer() *fuzzymatch.Scorer = fuzzymatch.DefaultScorer() (used by every test that needs a valid Scorer). Define newConfig(s *fuzzymatch.Scorer) scan.Config returning scan.DefaultConfig(s).

    For each of the 22 tests above, write a t.Parallel() Test* function with table-driven cases where natural. Use stdlib testing only — no testify. Pattern for errors.Is assertions:

    if !errors.Is(err, scan.ErrInvalidItem) {
        t.Fatalf("want errors.Is(ErrInvalidItem), got %v", err)
    }

    For "joined error contains N wrapped sentinels of kind X" assertion, walk via:

    type unwrapper interface { Unwrap() []error }
    if u, ok := err.(unwrapper); ok {
        wraps := u.Unwrap()
        count := 0
        for _, w := range wraps {
            if errors.Is(w, scan.ErrInvalidItem) {
                count++
            }
        }
        if count != want { t.Fatalf(...) }
    } else {
        // Single error case — assert message
    }

    For "err.Error() contains 'index N'" assertion, use strings.Contains(err.Error(), "index 3"). For TestValidate_DuplicateNameGroup_CollectsViaJoin, assert both "index 2" and "of index 0" appear.

    DO NOT call scan.Check — these tests target the internal validateCheck function. Since validateCheck is unexported, tests live in package scan (internal test package). Rename the file accordingly: scan/validate_test.go declares `package scan`, NOT `package scan_test`. Use a helper exposed via export_test.go IF needed; the simpler approach is for validate_test.go to be `package scan` (internal) and call validateCheck directly.

    Re-confirm by running `go test ./scan/... -run TestValidate_` — all 22 tests must FAIL with build error or test failure (validateCheck not defined yet).
  </action>
  <verify>
    <automated>bash -c 'go test -run TestValidate_ ./scan/... 2>&1 | grep -E "undefined: validateCheck|FAIL|cannot find" >/dev/null && exit 0 || (echo "Expected test failures (TDD RED phase) but suite passed — investigate"; exit 1)'</automated>
  </verify>
  <acceptance_criteria>
    - scan/validate_test.go exists with package scan (internal) and 22 Test* functions
    - Every test fails (TDD RED) — either build error "undefined: validateCheck" or test failures
    - File uses stdlib testing only; no testify; British English in test docs/comments
    - File compiles when the underlying validateCheck function is provided (Task 2)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scan/validate.go (TDD GREEN — make all 22 tests pass)</name>
  <files>scan/validate.go</files>
  <read_first>
    - .planning/phases/09-collection-scan-sub-package/09-RESEARCH.md lines 757-792 (full validateCheck + validateConfigFields template)
    - .planning/phases/09-collection-scan-sub-package/09-RESEARCH.md lines 325-356 (validateItems errors.Join Pattern 2 — drop-in)
    - .planning/phases/09-collection-scan-sub-package/09-PATTERNS.md scan/validate.go section (Pipeline-ordering godoc + errors.Join template + sort-order discussion)
    - validate.go (root pkg) — accumulate-then-emit pattern + sort.SliceStable usage (NOT used here per "no post-sort" decision)
    - scorer.go:130-200 — NewScorer validation pipeline order (analog for scan's P1..P4 order)
    - scan/validate_test.go (just-written, Task 1)
  </read_first>
  <behavior>
    All 22 tests from Task 1 must PASS:
    - validateCheck called with cfg.Scorer == nil returns ErrNilScorer (identity, no wrap)
    - validateCheck called with NaN/Inf/out-of-range CrossGroupThresholdBoost returns an error where errors.Is(err, ErrInvalidConfig) and the message includes the offending value
    - validateCheck called with items[].Name == "" or duplicate (Name, Group) returns errors.Join of one or more ErrInvalidItem wraps; each wrap mentions its index and (for duplicates) the first-seen index
    - validateCheck called with SuppressedPairs[i][0] == "" or [i][1] == "" returns errors.Join of one or more ErrInvalidConfig wraps mentioning SuppressedPairs[i]
    - Fail-fast order is preserved: P1 (Scorer nil) → P2 (Config fields) → P3 (Items) → P4 (SuppressedPairs); within P3 and P4, collect-all
    - Self-pairs in SuppressedPairs (e.g. ["abc","abc"]) silently kept (no error) — D-05 explicit
    - Empty items slice (len == 0) is valid input (returns nil after P1/P2 pass)
    - The internal map[itemKey]int never escapes the function (map iteration discipline)
  </behavior>
  <action>
    Create scan/validate.go with Apache 2.0 header. Package scan. Imports: "errors", "fmt", "math".

    Define unexported type itemKey struct { Name string; Group string } at package scope (used by validateItems).

    Implement four functions:

    (1) func validateCheck(items []Item, cfg Config) error — top-level entry point. Comment block (12-15 lines) documenting the P1..P4 pipeline order using the bullet structure from scorer.go:136-148. Body:
        if cfg.Scorer == nil { return ErrNilScorer }                // P1 fail-fast (return sentinel directly, not wrapped)
        if err := validateConfigFields(cfg); err != nil { return err }  // P2 fail-fast
        if err := validateItems(items); err != nil { return err }       // P3 collect-all
        if err := validateSuppressedPairs(cfg.SuppressedPairs); err != nil { return err }  // P4 collect-all
        return nil

    (2) func validateConfigFields(cfg Config) error — P2 fail-fast field validation. Currently validates only CrossGroupThresholdBoost. Body uses 09-RESEARCH.md lines 781-790 verbatim:
        b := cfg.CrossGroupThresholdBoost
        if math.IsNaN(b) || math.IsInf(b, 0) || b < 0.0 || b > 1.0 {
            return fmt.Errorf("scan: CrossGroupThresholdBoost=%v is invalid (must be in [0.0, 1.0], finite, non-NaN): %w", b, ErrInvalidConfig)
        }
        return nil
    Note: D-04 explicitly enumerates NaN, ±Inf, < 0, > 1 as invalid. The math.IsInf(b, 0) check covers both +Inf and -Inf.

    (3) func validateItems(items []Item) error — P3 collect-all. Body uses 09-RESEARCH.md lines 325-356 verbatim:
        var errs []error
        seen := make(map[itemKey]int, len(items))
        for i, item := range items {
            if item.Name == "" {
                errs = append(errs, fmt.Errorf("scan: invalid item at index %d: empty name: %w", i, ErrInvalidItem))
                continue
            }
            k := itemKey{Name: item.Name, Group: item.Group}
            if first, ok := seen[k]; ok {
                errs = append(errs, fmt.Errorf("scan: invalid item at index %d: duplicate (Name, Group) of index %d: %w", i, first, ErrInvalidItem))
                continue
            }
            seen[k] = i
        }
        return errors.Join(errs...)
    Add a 6-line godoc explaining that walking items in slice order gives ascending-index error ordering by construction, so no post-sort step is needed (anti-pattern: don't iterate `seen` map on the output path — already complied by reading via direct lookup not range).

    (4) func validateSuppressedPairs(pairs [][2]string) error — P4 collect-all. Mirror validateItems shape but simpler (no duplicate detection; D-05 keeps self-pairs silently):
        var errs []error
        for i, p := range pairs {
            if p[0] == "" || p[1] == "" {
                errs = append(errs, fmt.Errorf("scan: invalid SuppressedPairs[%d]: empty string in pair: %w", i, ErrInvalidConfig))
            }
        }
        return errors.Join(errs...)
    Add godoc noting D-05's silent self-pair keep — explicitly state "self-pairs (p[0] == p[1]) are valid; they are harmless because Check never emits self-warnings."

    Run `go test -race -shuffle=on -count=1 -run TestValidate_ ./scan/...` and verify all 22 tests PASS.

    Run `go vet ./scan/...` and confirm clean. Run `golangci-lint run ./scan/...` — clean (no new findings; the package is small enough to be lint-clean trivially).
  </action>
  <verify>
    <automated>go test -race -shuffle=on -count=1 -run TestValidate_ ./scan/...</automated>
  </verify>
  <acceptance_criteria>
    - scan/validate.go exists with validateCheck + 3 helpers
    - All 22 Task-1 tests PASS (TDD GREEN)
    - `go vet ./scan/...` clean
    - `golangci-lint run ./scan/...` clean
    - The itemKey type is unexported; the seen map is never iterated on an output path
    - Pipeline-order godoc cites P1..P4 with their phase labels
    - validateSuppressedPairs godoc explicitly notes D-05 self-pair keep
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Coverage gate + agent reviews + commit cluster</name>
  <files>(no new files — coverage + reviews + commits)</files>
  <read_first>
    - .claude/skills/fuzzymatch-review-protocol/SKILL.md (reviewer order)
    - .claude/skills/go-testing-standards/SKILL.md (≥95% overall / ≥90% per file / 100% public API gate)
    - .claude/skills/determinism-standards/SKILL.md (no map iteration on output paths — confirm seen map compliance)
  </read_first>
  <action>
    Quality gate:

    (a) `go test -race -shuffle=on -count=1 -cover ./scan/...` — confirm scan/validate.go coverage ≥ 90% per file
    (b) `make check` — full gate (test + vet + lint + markdownlint + deps + license)
    (c) `make verify-llms-sync` — green (no exported symbols added in this plan; llms.txt unchanged)

    Reviewers (in order):
    (i) code-reviewer on scan/validate.go + scan/validate_test.go — focus: errors.Join discipline, fail-fast vs collect-all clarity, godoc completeness
    (ii) security-reviewer — focus: Tag field never appears in errors; no panic on consumer input; integer index in error message is bounded
    (iii) determinism-reviewer — confirms seen map is never iterated on the output path; errors are emitted in walk order (which is ascending index by construction)
    (iv) go-quality — final gate

    Commit (single atomic feat commit):
    feat(scan): pre-flight validation pipeline P1..P4 with errors.Join collect-all (D-03 + D-04 + D-05 + D-06)

    Include in commit body:
    - Pipeline order documented per CONTEXT.md §2 (D-03/D-04/D-05/D-06 references)
    - 22 tests added; coverage on scan/validate.go ≥ 90%
    - Reviewer verdicts: code-reviewer ✓ / security-reviewer ✓ / determinism-reviewer ✓ / go-quality ✓
  </action>
  <verify>
    <automated>bash -c 'set -e; go test -race -shuffle=on -count=1 -cover -coverprofile=/tmp/scan_cover.out ./scan/... && go tool cover -func=/tmp/scan_cover.out | grep validate.go | awk "{print \$3}" | sed s/%// | awk "\$1 < 90.0 { exit 1 }" && make check'</automated>
  </verify>
  <acceptance_criteria>
    - scan/validate.go ≥ 90% line coverage
    - All 22 Task-1 tests PASS under race + shuffle + cover
    - make check green
    - Four reviewers green; verdicts recorded in 09-02-SUMMARY.md
    - One commit landed: feat(scan): pre-flight validation pipeline
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer Items[] → validateItems | Item.Name and Item.Group are untrusted; emptiness, duplicates, and adversarial length all surface here |
| consumer Config.SuppressedPairs → validateSuppressedPairs | Strings are untrusted; emptiness is caught |
| consumer Config.CrossGroupThresholdBoost → validateConfigFields | Float is untrusted; NaN/Inf/range rejected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Denial of Service | validateItems map allocation on huge inputs | accept | Map grows to O(len(items)); bounded by available memory. Documented in CONTEXT.md threat model (DoS via input size is consumer responsibility per spec §12.6). |
| T-09-02-02 | Tampering | Joined error message exposing Item.Tag | mitigate | Error format strings never reference item.Tag; only int index. Verified by code-reviewer + grep in CI. |
| T-09-02-03 | Information Disclosure | Error messages include consumer-supplied Name / Group implicitly via duplicate-detection note | accept | The Name + Group are already consumer-owned data; their appearance in error messages mirrors the existing fuzzymatch.Validate Warning pattern. |
| T-09-02-04 | Tampering | Map iteration leaking into error order | mitigate | The map `seen` is read via direct lookup (`seen[k]`) only; the for-loop walks items in slice order. Documented in validateItems godoc + determinism-reviewer verifies. |
</threat_model>

<verification>
- All 22 Test* functions in scan/validate_test.go PASS under `go test -race -shuffle=on -count=1 ./scan/...`
- scan/validate.go file coverage ≥ 90% (per-file floor)
- `make check` green (test + vet + lint + markdownlint + deps + license)
- code-reviewer, security-reviewer, determinism-reviewer, go-quality verdicts recorded in 09-02-SUMMARY.md
- One commit landed on the worktree branch: feat(scan): pre-flight validation pipeline
</verification>

<success_criteria>
- scan/validate.go implements validateCheck + validateConfigFields + validateItems + validateSuppressedPairs
- All four sentinels (ErrNilScorer + ErrInvalidConfig + ErrInvalidItem) reachable through their D-NN paths
- errors.Is discrimination works against joined errors for ErrInvalidItem and ErrInvalidConfig
- Self-pairs in SuppressedPairs silently accepted (D-05)
- Pipeline order matches CONTEXT.md §2: P1 → P2 → P3 → P4
- Plan 09-03 can call validateCheck as its first step in Check
</success_criteria>

<output>
After completion, create `.planning/phases/09-collection-scan-sub-package/09-02-SUMMARY.md` capturing:
- scan/validate.go LOC + test count + coverage
- The four reviewer verdicts
- One commit SHA
- Confirmation that Plan 09-03 can now wire validateCheck into Check's entry
</output>
