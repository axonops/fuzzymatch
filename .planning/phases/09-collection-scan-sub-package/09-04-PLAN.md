---
phase: 09
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - scan/bucket.go
  - scan/scan.go
  - scan/scan_internal_test.go
  - scan/props_test.go
  - scan/scan_bench_test.go
  - bench.txt
autonomous: false
requirements: [SCAN-02, PERF-05]
tags: [scan, phase-9, bucket, optimisation, property-test, benchmark, d-08, empirical-validation, perf-05]

must_haves:
  truths:
    - "scan/bucket.go declares a private const bucketThreshold = 50 (or empirically-validated alternative) with a comment citing the BenchmarkScanCheck_BucketVsNaive_GroupSize wall-clock crossover, the date, and 'Plan 09-04' (per D-08)"
    - "scan.Check dispatches to bucket-filtered candidate enumeration when a group's len > bucketThreshold; otherwise stays on the naive nested loop from Plan 09-03"
    - "The bucket consumes pre-Normalised, pre-Tokenised names — every Item.Name is Normalise()+Tokenise() at most ONCE per Check invocation (Pitfall 7)"
    - "PropCheck_BucketEquivalentToNaive asserts that for randomly-generated []Item with at least one group of size > bucketThreshold, bucket-Check's warning set equals naive-Check's warning set (after sort)"
    - "PropCheck_DeterministicAcrossRuns asserts that two consecutive Check calls on the same []Item produce identical (post-sort) results"
    - "BenchmarkScanCheck_BucketVsNaive_GroupSize sweeps group sizes 10, 25, 50, 75, 100, 200 at 10,000 total items per variant; bench.txt records the result"
    - "BenchmarkScanCheck_DefaultScorer_10k is recorded in bench.txt for the PERF-05 < 2s budget"
    - "If the empirical crossover differs materially from 50 on darwin/arm64, bucketThreshold is updated AND the in-source comment is updated AND a checkpoint:human-verify task confirms the new constant"
    - "Map iteration discipline preserved: the bucket map[string][]int is read via direct lookup; candidate-set slices are sorted before use; the final warning order is unchanged from the naive baseline (same up to post-sort)"
  artifacts:
    - path: "scan/bucket.go"
      provides: "Token-bucket: bucketThreshold const + tokeniseAll + buildBucket + bucketCandidates helpers"
      contains: "const bucketThreshold; type tokenBucket map[string][]int; func tokeniseAll; func buildBucket; func bucketCandidates"
    - path: "scan/scan.go"
      provides: "Updated Check dispatching naive↔bucket based on group size"
      contains: "if len(idx) <= bucketThreshold"
    - path: "scan/props_test.go"
      provides: "PropCheck_BucketEquivalentToNaive + PropCheck_DeterministicAcrossRuns property tests"
    - path: "scan/scan_internal_test.go"
      provides: "Tests for canonicalPair / tokenBucket / bucketCandidates helpers (unexported)"
    - path: "scan/scan_bench_test.go"
      provides: "BenchmarkScanCheck_BucketVsNaive_GroupSize{10,25,50,75,100,200} + BenchmarkScanCheck_DefaultScorer_10k"
    - path: "bench.txt"
      provides: "Updated baseline including the new scan benchmarks"
  key_links:
    - from: "scan/bucket.go bucketThreshold const"
      to: "scan/scan.go dispatch site"
      via: "package-private constant"
      pattern: "if len\\(idx\\) <= bucketThreshold"
    - from: "scan/bucket.go tokeniseAll"
      to: "fuzzymatch.Tokenise + Scorer.NormalisationOptions"
      via: "two-step Normalise then Tokenise per Open Question 2"
      pattern: "fuzzymatch\\.Tokenise\\(normalisedName, fuzzymatch\\.DefaultTokeniseOptions\\(\\)\\)"
    - from: "PropCheck_BucketEquivalentToNaive"
      to: "scan.Check via internal helpers (naiveCheck vs bucketCheck)"
      via: "package-internal test that exercises both paths"
      pattern: "PropCheck_BucketEquivalentToNaive"
user_setup: []
---

<objective>
Land the token-bucket optimisation for scan.Check's within-group and cross-group passes per spec §12.5 and D-08. Property-test the bucket implementation against the naive baseline (the SCAN-02 load-bearing gate). Add a six-point empirical-validation benchmark sweep for bucketThreshold and update the constant if the wall-clock crossover differs materially from the spec's hypothesis (50).

Purpose: Closes PERF-05 (< 2s/10k items) and SCAN-02 (bucket equivalent to naive). Plan 09-03's naive implementation cannot meet the PERF-05 budget on large groups; the bucket prunes the candidate set to items sharing at least one token. The empirical-validation step is non-optional per D-08 — the spec's 50 is a starting hypothesis; the benchmark is load-bearing.

Output: A bucketed scan.Check that produces the same warning set as the naive baseline (post-sort), achieves the PERF-05 budget for realistic inputs, and ships with three load-bearing tests (PropCheck_BucketEquivalentToNaive + PropCheck_DeterministicAcrossRuns + the empirical-validation benchmark sweep in bench.txt). The bucketThreshold constant carries the wall-clock crossover provenance in its in-source comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Plan inputs
@.planning/phases/09-collection-scan-sub-package/09-CONTEXT.md
@.planning/phases/09-collection-scan-sub-package/09-RESEARCH.md
@.planning/phases/09-collection-scan-sub-package/09-PATTERNS.md
@.planning/phases/09-collection-scan-sub-package/09-VALIDATION.md
@.planning/phases/09-collection-scan-sub-package/09-03-SUMMARY.md

# Skills
@.claude/skills/performance-standards/SKILL.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/algorithm-correctness-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

# Source files (analogs)
@scorer.go
@tokenise.go
@normalise.go
@validate.go
@scorer_bench_test.go
@scorer_test.go

# Plan 09-03 implementation (consumer scope)
@scan/scan.go
@scan/validate.go

<interfaces>
<!-- From tokenise.go: -->

```go
func DefaultTokeniseOptions() TokeniseOptions  // tokenise.go:105
func Tokenise(s string, opts TokeniseOptions) []string  // tokenise.go:179
```

<!-- Plan 09-01 Scorer accessor: -->

```go
func (s *Scorer) NormalisationOptions() (opts NormalisationOptions, applied bool)
```

<!-- Anti-pattern reference (spec + RESEARCH.md): -->

```go
// Each Item.Name MUST be tokenised at most once per Check invocation.
// Pre-compute tokenisedNames [][]string at Check entry; bucket build
// reads from this slice. (Pitfall 7)
```

<!-- Open Question 2 resolution per RESEARCH.md Option (a): -->

```go
// Normalise first using the Scorer's normalisation options; then
// Tokenise the normalised form with DefaultTokeniseOptions. The bucket
// keys mirror what the Scorer actually sees. The accessor was added in
// Plan 09-01.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write PropCheck_BucketEquivalentToNaive + PropCheck_DeterministicAcrossRuns property tests (TDD RED)</name>
  <files>scan/props_test.go, scan/scan_internal_test.go</files>
  <read_first>
    - .planning/phases/09-collection-scan-sub-package/09-RESEARCH.md lines 561-580 (SCAN-02 load-bearing gate) + lines 657-665 (property test list)
    - .planning/phases/09-collection-scan-sub-package/09-PATTERNS.md scan/props_test.go section (lines 636-665) + scorer_test.go:832-982 reference
    - scorer_test.go (search for "PropScorer_DeterministicAcrossRuns" + "PropScorer_WeightSumOne") to confirm the testing/quick + quick.Config{MaxCount: N} pattern
    - .planning/phases/09-collection-scan-sub-package/09-CONTEXT.md §"Claude's Discretion" (token-bucket internal data layout — PropCheck_BucketEquivalentToNaive is the gate)
  </read_first>
  <behavior>
    Write failing property tests in scan/props_test.go (package scan_test). All tests use stdlib testing + testing/quick.

    - PropCheck_BucketEquivalentToNaive: generator produces a synthetic []Item with at least one group containing > 50 items (so the bucket path is exercised). For each generated input, run scan.Check with the default Scorer twice — once with bucketing disabled (forced naive — internal test hook) and once with bucketing enabled (production path). Sort both result slices by (Kind, NameA, NameB, GroupA, GroupB) (in this plan, sort is not yet in Check; the property test sorts locally for comparison). Assert the two slices are identical (length + element-by-element equality on the Warning value, ignoring Scores map iteration order via key-sorted comparison).

    - PropCheck_DeterministicAcrossRuns: generator produces []Item; run scan.Check twice on the same input; assert equal (after local sort). The test exercises the production path (bucketing on for large groups, naive for small).

    Both tests use a custom quick.Generator for []Item that produces:
    - 50-200 items
    - 3-10 distinct group names (chosen from a fixed alphabet so the result is reproducible per seed)
    - Item names drawn from a moderate-similarity bag (e.g. base names like "user_id", "userId", "user_name", "userName", "userid" with small mutations to ensure some pairs cross the 0.85 threshold)
    - SilenceLint always false (per-item suppression is Plan 09-05's scope)
    - Tag nil
    - NO duplicate (Name, Group) — generator must enforce D-06 so validateCheck doesn't reject the input

    Generator must be deterministic given the same random source — testing/quick controls the seed.

    PropCheck_NoSelfWarnings (added now per Pitfall 8): with the same generator, assert no Warning has (NameA == NameB AND GroupA == GroupB AND TagA == TagB) — i.e. no (i, i) emissions.

    Since bucketing is not yet implemented, both PropCheck_BucketEquivalentToNaive and PropCheck_DeterministicAcrossRuns will pass trivially because Check has only one code path. The property tests are nonetheless written now to lock the contract. To make PropCheck_BucketEquivalentToNaive meaningfully gate the bucket implementation in Task 2, expose an internal test hook via scan/scan_internal_test.go (or an export_test.go file) that toggles a package-private `forceNaivePath` bool — when true, the bucket dispatch is suppressed and the naive path runs unconditionally. The hook lives in production-test-only code (build tag _test.go).

    For TDD RED: scan/props_test.go and the internal hook compile against the Plan-09-03 surface and PASS (since the bucket path doesn't exist yet — both production and forced-naive run the same code). The behaviour test value comes in Task 2 where the bucket path is added and the property must still hold.

    Additionally, write scan/scan_internal_test.go (internal package) with three smaller deterministic unit tests:
    - Test_bucketThreshold_HasComment (compile-time inspection — verify the constant is declared in scan/bucket.go; we cannot test the comment content from Go code, but we can use reflect or build-tag check; alternatively defer to a manual review item)
    - Test_tokeniseAll_OncePerItem (build a synthetic []Item, call tokeniseAll, assert returned slice length equals len(items) and each tokens slice has the expected count from Tokenise)
    - Test_canonicalPair_LexOrder (assert canonicalPair("z","a") == canonicalPair("a","z") and both have first element "a")

    The internal tests target functions that DON'T yet exist (canonicalPair lands in Plan 09-05; tokeniseAll lands in Task 2 of this plan; bucketThreshold lands in Task 2). Tests fail with build errors — TDD RED.
  </behavior>
  <action>
    Create scan/props_test.go (package scan_test, imports testing/quick, fuzzymatch, scan). Write three property tests + the custom generator.

    Create scan/scan_internal_test.go (package scan, imports testing, etc.). Write the three small unit tests targeting unexported helpers.

    Create OR extend scan/export_test.go (package scan) declaring:

    var ForceNaivePath = &forceNaivePath  // pointer for test toggling

    Where forceNaivePath is a package-private bool defined in scan/scan.go (or wherever bucket dispatch lives in Task 2 — defer field declaration to Task 2 if necessary).

    Pattern for property tests follows scorer_test.go:854-866. quick.Config{MaxCount: 100} is the default; specify it explicitly so the test discoverable. For PropCheck_BucketEquivalentToNaive, use MaxCount: 50 (each invocation runs Check twice; budget is generous).

    Run `go test ./scan/...` and confirm the internal tests FAIL with "undefined: tokeniseAll" / "undefined: canonicalPair" / "undefined: bucketThreshold" build errors. The property tests should compile and pass (because Check has only one code path so far) — the gate value comes after Task 2.
  </action>
  <verify>
    <automated>bash -c 'go build ./scan/... 2>&1 | grep -E "undefined: tokeniseAll|undefined: canonicalPair|undefined: bucketThreshold|undefined: forceNaivePath" >/dev/null && exit 0 || (echo "Expected undefined-symbol build errors (TDD RED)"; exit 1)'</automated>
  </verify>
  <acceptance_criteria>
    - scan/props_test.go with 3 property tests + custom []Item generator
    - scan/scan_internal_test.go with 3 unit tests
    - scan/export_test.go exposing forceNaivePath for the bucket-equivalence test (or noted as deferred to Task 2)
    - Internal tests fail with "undefined" build errors (TDD RED for Task 2 to fix)
    - Property tests are well-formed (compile after Task 2 lands the missing identifiers)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scan/bucket.go + wire bucket dispatch into scan.Check (TDD GREEN)</name>
  <files>scan/bucket.go, scan/scan.go</files>
  <read_first>
    - .planning/phases/09-collection-scan-sub-package/09-CONTEXT.md §5 (D-08 LOCKED — private const + comment + empirical validation)
    - .planning/phases/09-collection-scan-sub-package/09-RESEARCH.md lines 405-422 (Pattern 4 naive↔bucket dispatch) + lines 393-403 (canonicalPair which lives in Plan 09-05 — DO NOT add it here) + Pitfall 4 (SuppressedPairs normalisation — informs Tokenise+Normalise pipeline) + Pitfall 5 (no double-normalisation) + Pitfall 7 (tokenise once per Item)
    - .planning/phases/09-collection-scan-sub-package/09-PATTERNS.md scan/bucket.go section (lines 357-410)
    - .planning/phases/09-collection-scan-sub-package/09-RESEARCH.md Open Question 2 RECOMMENDATION (a): Normalise first using Scorer's options; Tokenise the normalised form with DefaultTokeniseOptions
    - tokenise.go:179 (Tokenise signature) + tokenise.go:105 (DefaultTokeniseOptions)
    - validate.go:60-73 (private const + comment template)
    - scan/scan.go (Plan 09-03 — Check body with naive passes)
  </read_first>
  <behavior>
    - bucketThreshold is a private const in scan/bucket.go starting at value 50 (per D-08 hypothesis; updated empirically in Task 3 if benchmark differs materially)
    - tokeniseAll(items []Item, normOpts NormalisationOptions, applyNormalisation bool) [][]string returns one slice of tokens per Item, in input order; Normalise applies first (when applyNormalisation), then Tokenise(normalised, DefaultTokeniseOptions())
    - buildBucket(indices []int, tokens [][]string) tokenBucket builds map[token][]itemIndex from the supplied item indices, using the supplied tokenised forms
    - bucketCandidates(srcIdx int, bucket tokenBucket, tokens [][]string) []int returns the deduplicated, sorted slice of item indices that share at least one token with items[srcIdx], EXCLUDING srcIdx itself (no self-pairing per Pitfall 8)
    - scan.Check dispatches: when len(groupIndices[g]) > bucketThreshold (AND forceNaivePath == false), use bucketCandidates for that group; otherwise use naive nested loop from Plan 09-03
    - PropCheck_BucketEquivalentToNaive (Task 1) PASSES when run with the new dispatch — bucket and forced-naive paths produce identical warning sets
    - PropCheck_DeterministicAcrossRuns PASSES under both paths
    - PropCheck_NoSelfWarnings PASSES
    - Test_tokeniseAll_OncePerItem + Test_canonicalPair_LexOrder (canonicalPair NOT in this plan — see note below) + Test_bucketThreshold_HasComment PASS
    - For cross-group, the bucket dispatch operates against the union of two group's indices (effectively one "supergroup" containing both) — but the candidate enumeration still respects the cross-group constraint (item indices from different source groups). The dispatch condition for cross-group is similarly len(idxA)+len(idxB) > bucketThreshold.
  </behavior>
  <action>
    Create scan/bucket.go with Apache 2.0 header. Package scan. Imports: "sort", "github.com/axonops/fuzzymatch".

    Declarations:

    (1) Private constant with the D-08 comment (verbatim per CONTEXT.md):
        // bucketThreshold is the group-size cutoff at which the token-bucket
        // optimisation overtakes the naive nested-loop comparison. Empirically
        // validated on darwin/arm64 by BenchmarkScanCheck_BucketVsNaive_GroupSize
        // at $WALL_TIME / Plan 09-04. (Initial value carried from the
        // hypothesis in docs/requirements.md §12.5; the wall-clock token
        // is filled in by Task 3 after the benchmark sweep.)
        const bucketThreshold = 50

    (2) type tokenBucket map[string][]int

    (3) func tokeniseAll(items []Item, normOpts NormalisationOptions, applyNormalisation bool) [][]string — for each item, Normalise(item.Name, normOpts) (if applyNormalisation) and Tokenise(result, DefaultTokeniseOptions()). Note: signature uses fuzzymatch.NormalisationOptions exported type to avoid importing more than needed.

        Sub-decision: this plan uses the same accessor handed in via the caller because tokeniseAll is package-internal — pass (NormalisationOptions, bool) as parameters rather than dereferencing the Scorer inside tokeniseAll. Cleaner unit-testable shape.

    (4) func buildBucket(indices []int, tokens [][]string) tokenBucket — for each idx in indices, walk tokens[idx] and append idx to bucket[token] (deduplicating via a small inner check to avoid pushing the same index twice if a name has repeated tokens).

    (5) func bucketCandidates(srcIdx int, bucket tokenBucket, tokens [][]string) []int — for each token in tokens[srcIdx], gather bucket[token]; deduplicate; remove srcIdx; sort by ascending index.
        Implementation: use a `seen` set (map[int]struct{}) sized at len(tokens[srcIdx])*8 to deduplicate; then sort the result slice. Map iteration is contained within the function (read-only output is the sorted slice, not the map).

    Modify scan/scan.go:

    - Add a package-private var forceNaivePath bool (declared in scan.go or a small dispatch.go file). Plumb the bucket-dispatch decision through:

      For within-group pass:
        if !forceNaivePath && len(idx) > bucketThreshold {
            // bucket path
            bucket := buildBucket(idx, tokenised)
            for _, i := range idx {
                candidates := bucketCandidates(i, bucket, tokenised)
                for _, j := range candidates {
                    if j <= i { continue }                 // dedupe pair order
                    a, b := items[i], items[j]
                    if cfg.Scorer.Match(a.Name, b.Name) {
                        warnings = append(warnings, Warning{
                            Kind: KindWithinGroup, ...
                        })
                    }
                }
            }
        } else {
            // existing naive pass from Plan 09-03
        }

      For cross-group pass:
        if !forceNaivePath && (len(idxA) + len(idxB)) > bucketThreshold {
            // build bucket over idxA ∪ idxB; for each src in idxA, find
            // candidates and emit if the candidate is in idxB; or build
            // a per-side bucket and intersect. The simpler implementation:
            // build buckets only over idxA, then for each j in idxB walk
            // tokens[j] and gather bucket[token] from A — symmetric.
            // Choose the simpler shape; document in comments.
        } else {
            // existing naive pass from Plan 09-03
        }

    Plumb tokenisedNames [][]string through Check's body alongside normalisedNames so both buckets and any future Plan-09-05 suppression composition (Plan 09-05 adds the canonical-pair lookup using normalisedNames; bucket lookup uses tokenisedNames).

    Update Check's godoc to document the dispatch threshold and the bucket-equivalence property test gate.

    Run the test suite:
    - go test -race -shuffle=on -count=1 -run "TestCheck_|PropCheck_|Test_tokeniseAll_|Test_canonicalPair_|Test_bucketThreshold_" ./scan/... — all PASS. NOTE: canonicalPair is not part of this plan — remove Test_canonicalPair_LexOrder from scan/scan_internal_test.go (defer to Plan 09-05) OR skip the test with t.Skip noting "canonicalPair lands in Plan 09-05".
    - Run an additional manual loop: 5× consecutive runs of `go test -race -shuffle=on -count=1 ./scan/...` — all green (catches flaky property tests).
  </action>
  <verify>
    <automated>go test -race -shuffle=on -count=1 -run "TestCheck_|PropCheck_|Test_tokeniseAll_|Test_bucketThreshold_" ./scan/...</automated>
  </verify>
  <acceptance_criteria>
    - scan/bucket.go exists with bucketThreshold const + tokenBucket type + tokeniseAll + buildBucket + bucketCandidates
    - scan/scan.go Check dispatches naive ↔ bucket based on len(idx) > bucketThreshold && !forceNaivePath
    - All Plan-09-03 tests still PASS; all Plan-09-04 property + internal tests PASS
    - 5× consecutive race+shuffle runs all green (no flaky behaviour)
    - `go vet ./scan/...` + `golangci-lint run ./scan/...` clean
    - canonicalPair is NOT introduced in this plan (defer to Plan 09-05)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Write BenchmarkScanCheck_BucketVsNaive_GroupSize sweep + PERF-05 budget benchmark</name>
  <files>scan/scan_bench_test.go</files>
  <read_first>
    - .planning/phases/09-collection-scan-sub-package/09-CONTEXT.md §5 D-08 (the exact sweep: 10/25/50/75/100/200 at 10,000 total items)
    - .planning/phases/09-collection-scan-sub-package/09-RESEARCH.md lines 562 + line 855 (Assumption A8 — single-platform darwin/arm64 value)
    - .planning/phases/09-collection-scan-sub-package/09-PATTERNS.md scan/scan_bench_test.go section (lines 557-587)
    - scorer_bench_test.go (entire file) — header docs + b.ReportAllocs + sink-gate + construction-outside-ResetTimer pattern
    - .claude/skills/performance-standards/SKILL.md (allocation-budget discipline)
  </read_first>
  <behavior>
    - 7 new Benchmark* functions in scan/scan_bench_test.go:
      - BenchmarkScanCheck_BucketVsNaive_GroupSize10
      - BenchmarkScanCheck_BucketVsNaive_GroupSize25
      - BenchmarkScanCheck_BucketVsNaive_GroupSize50
      - BenchmarkScanCheck_BucketVsNaive_GroupSize75
      - BenchmarkScanCheck_BucketVsNaive_GroupSize100
      - BenchmarkScanCheck_BucketVsNaive_GroupSize200
      - BenchmarkScanCheck_DefaultScorer_10k (PERF-05 — < 2s budget; recorded informationally per CONTEXT.md not a CI gate)
    - All benchmarks call b.ReportAllocs()
    - Construction (items slice + Scorer + cfg) outside b.ResetTimer()
    - All benchmarks use a sink-gate (`var sink []scan.Warning` package-private) to defeat compiler DCE
    - The bucket-sweep benchmarks construct 10,000 items total, partitioned into groups of the size in the name (so 1000 groups of size 10, 400 groups of size 25, etc.). Each group's item names follow the moderate-similarity pattern from Task 1 (so bucketing is meaningful).
    - The PERF-05 benchmark uses a single group with mixed similarity distribution at 10k items.
  </behavior>
  <action>
    Create scan/scan_bench_test.go with Apache 2.0 header. Package scan_test. Imports: "testing", "fmt", "github.com/axonops/fuzzymatch", "github.com/axonops/fuzzymatch/scan".

    Header godoc block (mirror scorer_bench_test.go:14-58):
    - Documents each benchmark's budget / purpose
    - States the sink-gate convention
    - Documents the D-08 empirical-validation methodology

    Helper:
    - func buildBenchItems(totalItems, groupSize int) []scan.Item — partitions totalItems into ceil(totalItems/groupSize) groups, named "group_0", "group_1", ...; item names follow base patterns with mutations so some pairs cross the 0.85 threshold within groups

    Implement the 6 bucket-sweep benchmarks by sharing a helper:

    func benchBucketVsNaive(b *testing.B, groupSize int) {
        b.ReportAllocs()
        items := buildBenchItems(10000, groupSize)
        cfg := scan.DefaultConfig(fuzzymatch.DefaultScorer())
        b.ResetTimer()
        for i := 0; i < b.N; i++ {
            w, err := scan.Check(items, cfg)
            if err != nil { b.Fatal(err) }
            sink = w
        }
    }

    func BenchmarkScanCheck_BucketVsNaive_GroupSize10(b *testing.B)  { benchBucketVsNaive(b, 10) }
    ... etc through 200

    Implement BenchmarkScanCheck_DefaultScorer_10k similarly using one group of 10,000 (forces bucket path for sure).

    Run the sweep:
    `go test -bench=BenchmarkScanCheck_ -benchmem -count=3 -run=^$ ./scan/...`

    Inspect benchstat output. Record the results in this plan's SUMMARY. Determine the empirical crossover from the benchmark output.

    Then run:
    `go test -bench=BenchmarkScanCheck_BucketVsNaive_GroupSize -benchmem -count=5 -run=^$ ./scan/... | tee /tmp/scan_bench.txt`
    `benchstat /tmp/scan_bench.txt`

    Identify the wall-clock crossover (the group size at which the per-Check duration is approximately tied between bucket and naive — though both run via the production dispatch in this implementation; the comparison is more about whether the bucket is HELPING at each group size relative to the per-pair cost).

    NOTE: A more discriminating crossover sweep would compare bucket-only vs naive-only at each group size. To do that, exploit the forceNaivePath hook from Task 2: write TWO benchmark variants per group size:
      - BenchmarkScanCheck_Naive_GroupSize10 — sets *scan.ForceNaivePath = true, runs Check, restores false
      - BenchmarkScanCheck_Bucket_GroupSize10 — leaves *scan.ForceNaivePath = false

    Or simpler: add one helper that toggles forceNaivePath via the export_test hook and produces two sub-benchmarks per group size (b.Run("naive", ...), b.Run("bucket", ...)). Pick the cleaner shape; document in the helper.

    Record results — see Task 4 for the constant-update decision step.

    Run all root tests still PASS after benchmark code lands: `go test -race -shuffle=on -count=1 ./scan/... ./...`.
  </action>
  <verify>
    <automated>go test -bench=BenchmarkScanCheck_BucketVsNaive_GroupSize -benchmem -benchtime=1x -count=1 -run=^$ ./scan/...</automated>
  </verify>
  <acceptance_criteria>
    - 7 benchmarks landed in scan/scan_bench_test.go
    - Each calls b.ReportAllocs and uses construction-outside-ResetTimer + sink-gate
    - Benchmark sweep runs cleanly on darwin/arm64 (the user's machine)
    - Initial results captured in this plan's task notes for Task 4 to inspect
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Bucket optimisation lands, but the bucketThreshold constant's value is empirically gated per D-08.

    Tasks 1-3 produced:
    - scan/bucket.go with bucketThreshold = 50 (provisional)
    - scan/scan.go dispatch wiring
    - 3 property tests + internal helper tests (PropCheck_BucketEquivalentToNaive is the SCAN-02 gate)
    - 7 new benchmarks including the 6-point sweep over group sizes 10/25/50/75/100/200

    The benchmark sweep has run (Task 3); benchstat output is available locally.
  </what-built>
  <how-to-verify>
    1. Run the benchmark sweep with high count for confidence:
       ```
       go test -bench=BenchmarkScanCheck_BucketVsNaive_GroupSize -benchmem -count=5 -run=^$ ./scan/... | tee /tmp/scan_bench.txt
       benchstat /tmp/scan_bench.txt
       ```

    2. Inspect the per-operation timing across the 6 group sizes (10, 25, 50, 75, 100, 200). Identify the **wall-clock crossover** — the group size at which the bucket-enabled run becomes meaningfully faster than naive (or, if the test used b.Run sub-benchmarks for naive vs bucket, the size at which the bucket variant overtakes naive).

    3. Compare the crossover to the spec's hypothesis of 50:
       - If the crossover is within ±10 of 50 (i.e. 40-60), **leave the constant at 50**.
       - If the crossover differs materially (>±10, e.g. 30 or 80), **update scan/bucket.go**:
         - Change `const bucketThreshold = 50` to `const bucketThreshold = <new value>`
         - Update the inline comment to read:
           ```
           // bucketThreshold is the group-size cutoff at which the token-bucket
           // optimisation overtakes the naive nested-loop comparison.
           // Empirically validated on darwin/arm64 by
           // BenchmarkScanCheck_BucketVsNaive_GroupSize at $YYYY-MM-DD $WALL_TIME /
           // Plan 09-04. (Previous hypothesis: 50; observed crossover at group size N
           // with bucket=Xms vs naive=Yms per Check at 10,000 total items.)
           ```

    4. If the constant changed, re-run the test suite to confirm all property tests still pass with the new threshold:
       ```
       go test -race -shuffle=on -count=1 ./scan/...
       ```

    5. Confirm the PERF-05 budget:
       ```
       go test -bench=BenchmarkScanCheck_DefaultScorer_10k -benchmem -count=3 -run=^$ ./scan/...
       ```
       The reported `ns/op` should correspond to < 2,000,000,000 ns (2s) per Check on darwin/arm64. If it doesn't, flag the gap (acceptable if very close; an order-of-magnitude miss requires investigation).

    6. Approve when:
       - The constant value is finalised (50 or empirically-justified alternative) with the inline comment carrying the wall-clock provenance
       - All tests still pass
       - PERF-05 budget either met or the gap documented in SUMMARY for follow-up

    Reply with "approved" once verified, or describe any issues for follow-up.
  </how-to-verify>
  <resume-signal>Type "approved <value>" where <value> is the final bucketThreshold (e.g. "approved 50" or "approved 65"), or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Commit bench.txt update + agent reviews + atomic commit</name>
  <files>bench.txt</files>
  <read_first>
    - bench.txt (current baseline)
    - .claude/skills/fuzzymatch-review-protocol/SKILL.md
  </read_first>
  <action>
    Re-run all scan benchmarks at -count=10 for a stable baseline:
    ```
    go test -bench=BenchmarkScanCheck_ -benchmem -count=10 -run=^$ ./scan/... | tee /tmp/scan_bench_final.txt
    ```

    Append the cleaned output (or merge if scan benches already exist) to bench.txt. Pattern: follow the existing bench.txt structure (headed sections per benchmark group with the date + git SHA on the top line).

    Reviewers:
    (i) algorithm-licensing-reviewer — focus: token-bucket implementation is fresh-written from first principles; no GPL/LGPL-derived code; no patent encumbrance on the data-structure pattern (textbook inverted-index optimisation is unencumbered); attribution adequate in scan/bucket.go top-of-file comment
    (ii) algorithm-performance-reviewer — focus: D-08 empirical-validation discipline; bucket builds once per group; tokeniseAll once per Check; ScoreAll on emission only; map iteration discipline; benchmark coverage adequate
    (iii) algorithm-correctness-reviewer — focus: PropCheck_BucketEquivalentToNaive seed coverage, generator correctness (no duplicate (Name, Group)); proves the bucket implementation is mathematically equivalent to naive
    (iv) determinism-reviewer — focus: bucketCandidates returns a SORTED slice (not map iteration); tokenBucket map never iterated on output; PropCheck_DeterministicAcrossRuns covers the production path
    (v) code-reviewer + go-quality — standard final pass
    (vi) commit-message-reviewer

    Commit (atomic):
    perf(scan): token-bucket optimisation with empirical bucketThreshold validation (SCAN-02, PERF-05, D-08)

    Body:
    - bucketThreshold = <value> (50 or empirically-updated) with wall-clock provenance in in-source comment
    - PropCheck_BucketEquivalentToNaive (SCAN-02 gate) + PropCheck_DeterministicAcrossRuns + PropCheck_NoSelfWarnings
    - 6-point BenchmarkScanCheck_BucketVsNaive_GroupSize sweep + BenchmarkScanCheck_DefaultScorer_10k (PERF-05)
    - Reviewer verdicts recorded
  </action>
  <verify>
    <automated>bash -c 'set -e; go test -race -shuffle=on -count=1 ./scan/... && grep -q "BenchmarkScanCheck_BucketVsNaive_GroupSize" bench.txt && grep -q "BenchmarkScanCheck_DefaultScorer_10k" bench.txt && make check'</automated>
  </verify>
  <acceptance_criteria>
    - bench.txt contains entries for all 7 new benchmarks
    - All tests + linters green via `make check`
    - 6 reviewer verdicts recorded in 09-04-SUMMARY.md (algorithm-licensing-reviewer, algorithm-performance-reviewer, algorithm-correctness-reviewer, determinism-reviewer, code-reviewer + go-quality, commit-message-reviewer)
    - One commit landed: perf(scan): token-bucket optimisation
    - bucketThreshold value + comment finalised with empirical provenance
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer items[] → tokeniseAll | Item.Name strings tokenised; pathological inputs bounded by Tokenise's complexity (existing root-pkg surface) |
| Consumer items[] → bucket build | Adversarial inputs with no token overlap fall back to candidate-empty (no DoS amplification) |
| forceNaivePath test hook → production code | Test-only var exposed via export_test.go; cannot be set from outside the scan package in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Denial of Service | Adversarial inputs that defeat bucketing (all items share one token) | accept | Worst-case complexity == naive O(N²); documented in Check godoc. The bucket is a candidate-filter optimisation, not a complexity-class change. |
| T-09-04-02 | Tampering | Bucket dispatch via forceNaivePath in production | mitigate | forceNaivePath is exposed only via export_test.go (test-only file) and is unreachable from non-test consumer code. |
| T-09-04-03 | Information Disclosure | Bucket map iteration leaking input order | mitigate | bucketCandidates always sorts its output slice. tokenBucket map is never iterated; only direct lookup. Property test verifies determinism. |
| T-09-04-04 | Denial of Service | Pathologically large token counts per Item (e.g. 50,000-token name) | accept | Existing Validate surface flags inputs > 64 KiB; scan inherits this implicitly per the per-pair Scorer.Score call. The bucket build cost is O(sum of token counts), bounded by total input bytes. |
</threat_model>

<verification>
- All Plan 09-04 tests + Plan 09-03 tests + Plan 09-02 tests + Plan 09-01 tests PASS under `go test -race -shuffle=on -count=1 ./scan/...`
- PropCheck_BucketEquivalentToNaive PASSES at MaxCount=50 (SCAN-02 gate)
- PropCheck_DeterministicAcrossRuns PASSES at MaxCount=100
- 6-point bucket-vs-naive benchmark sweep runs cleanly; bucketThreshold value finalised with in-source wall-clock provenance comment
- PERF-05 BenchmarkScanCheck_DefaultScorer_10k recorded in bench.txt
- `make check` green
- 5 reviewer verdicts recorded
- One commit landed on worktree branch
</verification>

<success_criteria>
- SCAN-02 closed: PropCheck_BucketEquivalentToNaive passes
- PERF-05 budget recorded (10k items in < 2s on darwin/arm64; or documented gap)
- bucketThreshold constant carries the wall-clock provenance per D-08
- bucket map iteration discipline observed (sorted-slice candidate output; no map range on output paths)
- Plan 09-05 can wire suppression composition into the production Check loop (both naive and bucket paths)
</success_criteria>

<output>
After completion, create `.planning/phases/09-collection-scan-sub-package/09-04-SUMMARY.md` capturing:
- Final bucketThreshold value with wall-clock crossover + benchmark output excerpt
- All benchmark numbers (per-op timing for each group size; PERF-05 result)
- 5 reviewer verdicts
- Two test counts: total scan/ tests, property test seed counts
- One commit SHA
- Confirmation that PERF-05 < 2s budget is met (or documented gap)
</output>
