---
phase: 09
plan: 07
type: execute
wave: 7
depends_on: ["09-06"]
files_modified:
  - tests/bdd/features/scan.feature
  - tests/bdd/features/suppression.feature
  - tests/bdd/steps/scan_steps.go
  - tests/bdd/steps/algorithms_steps.go
autonomous: true
requirements: [SCAN-03, TEST-05]
tags: [scan, phase-9, bdd, godog, scan-feature, suppression-feature, phase-8.5-r2-closure]

must_haves:
  truths:
    - "tests/bdd/features/scan.feature exists with at least one scenario per D-03 (empty Item.Name validation errors.Join), D-04 (CrossGroupThresholdBoost validation), D-06 (duplicate (Name, Group) errors.Join), and D-07 (Validate / Check separation explicit in scan.feature header comment)"
    - "tests/bdd/features/suppression.feature exists with at least one scenario per D-05 suppression rule (per-item SilenceLint, SuppressedPairs canonical-pair, cross-group identical-name default) AND closes the Phase 8.5 R2 deferred suppression scenarios with at least 3 interaction scenarios beyond what 09-05 unit tests cover"
    - "tests/bdd/steps/scan_steps.go compiles cleanly under tests/bdd/ (`cd tests/bdd && go build ./...` exits 0) and is package steps, mirroring the scorer_steps.go shape"
    - "tests/bdd/steps/algorithms_steps.go InitializeScenario calls InitScanSteps(ctx) alongside the existing InitScorerSteps / InitValidateSteps / InitNormalisationSteps / InitDeterminismSteps calls"
    - "`make test-bdd` exits 0 with all scan + suppression scenarios passing"
    - "goleak.VerifyTestMain in tests/bdd/bdd_test.go reports zero goroutine leaks after scan + suppression scenarios execute (scan is pure-function, no goroutines expected)"
    - "Every D-03..D-07 decision has at least one BDD scenario directly testing it (verified by grep on @scan + @suppression tags + decision-tag mapping comment block at the top of scan.feature)"
    - "bdd-scenario-reviewer sign-off recorded in 09-07-SUMMARY.md (no GitHub issues yet per user-memory project_no_github_issues)"
  artifacts:
    - path: "tests/bdd/features/scan.feature"
      provides: "Gherkin scenarios covering scan.Check happy paths + validation surface + cross-group + sort determinism + bucket smoke"
      contains: "Feature: Collection scan"
    - path: "tests/bdd/features/suppression.feature"
      provides: "Gherkin scenarios covering all three suppression rules + composition + Phase 8.5 R2 deferred coverage"
      contains: "Feature: Suppression composition"
    - path: "tests/bdd/steps/scan_steps.go"
      provides: "ScanContext struct + step definitions backing every Given/When/Then in scan.feature + suppression.feature + InitScanSteps(ctx) registration entry-point"
      contains: "type ScanContext struct; func InitScanSteps(ctx *godog.ScenarioContext)"
    - path: "tests/bdd/steps/algorithms_steps.go"
      provides: "InitializeScenario wired to invoke InitScanSteps(ctx)"
      contains: "InitScanSteps(ctx)"
  key_links:
    - from: "tests/bdd/features/scan.feature"
      to: "tests/bdd/steps/scan_steps.go step regexes"
      via: "Gherkin step text → ScenarioContext.Step regex match"
      pattern: "ctx\\.Step\\("
    - from: "tests/bdd/steps/algorithms_steps.go InitializeScenario"
      to: "tests/bdd/steps/scan_steps.go InitScanSteps"
      via: "function call"
      pattern: "InitScanSteps\\(ctx\\)"
    - from: "tests/bdd/steps/scan_steps.go"
      to: "github.com/axonops/fuzzymatch/scan public surface"
      via: "import + scan.Check / scan.DefaultConfig / scan.Item / scan.Warning / scan.Config / scan.ErrInvalid* calls"
      pattern: "scan\\.(Check|DefaultConfig|Item|Warning|Config|ErrNilScorer|ErrInvalidItem|ErrInvalidConfig)"
user_setup: []
---

<objective>
Land tests/bdd/features/scan.feature + tests/bdd/features/suppression.feature + tests/bdd/steps/scan_steps.go covering every D-03..D-07 locked decision plus the Phase 8.5 R2 deferred suppression scenarios. Wire InitScanSteps into the existing algorithms_steps.go InitializeScenario per the established BDD harness pattern (InitScorerSteps / InitValidateSteps / InitNormalisationSteps / InitDeterminismSteps).

Purpose: Closes TEST-05 (BDD coverage for scan + suppression) and adds Gherkin-level coverage on top of the unit + property + golden tests landed in Plans 09-01..09-06. This is the formal closure of the Phase 8.5 R2 deferral (Gap 3 — "scan + suppression features deferred to Phase 9"). bdd-scenario-reviewer agent gate is mandatory before commit.

Output: Two new feature files (scan.feature + suppression.feature) + one new step-definition file (scan_steps.go) + InitializeScenario amendment. `make test-bdd` green; bdd-scenario-reviewer verdict recorded; commit `test(09-07): add BDD scenarios for scan + suppression` lands without push (phase-boundary push is handled in Plan 09-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Plan inputs
@.planning/phases/09-collection-scan-sub-package/09-CONTEXT.md
@.planning/phases/09-collection-scan-sub-package/09-RESEARCH.md
@.planning/phases/09-collection-scan-sub-package/09-PATTERNS.md
@.planning/phases/09-collection-scan-sub-package/09-05-SUMMARY.md
@.planning/phases/09-collection-scan-sub-package/09-06-SUMMARY.md

# Skills
@.claude/skills/go-testing-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md
@.claude/skills/determinism-standards/SKILL.md

# Spec
@docs/requirements.md

# Source files — closest analogs
@tests/bdd/features/scorer.feature
@tests/bdd/features/validate.feature
@tests/bdd/steps/scorer_steps.go
@tests/bdd/steps/validate_steps.go
@tests/bdd/steps/algorithms_steps.go
@tests/bdd/bdd_test.go
@tests/bdd/go.mod

# scan public surface under test
@scan/scan.go
@scan/kind.go
@scan/errors.go
@scan/suppress.go

<interfaces>
<!-- BDD step registration pattern (verbatim from scorer_steps.go:728-738 + algorithms_steps.go:1586): -->
<!--                                                                                                  -->
<!-- 1. scan_steps.go declares ScanContext struct (separate from ScorerContext / ValidateContext /     -->
<!--    NormalisationContext / DeterminismContext) so per-scenario state is isolated by feature.       -->
<!--                                                                                                   -->
<!-- 2. scan_steps.go exports InitScanSteps(ctx *godog.ScenarioContext) registering every regex via    -->
<!--    ctx.Step(`^...$`, sc.method).                                                                  -->
<!--                                                                                                   -->
<!-- 3. algorithms_steps.go InitializeScenario appends `InitScanSteps(ctx)` after the existing init    -->
<!--    chain (InitScorerSteps → InitValidateSteps → InitNormalisationSteps → InitDeterminismSteps).   -->
<!--                                                                                                   -->
<!-- 4. tests/bdd/bdd_test.go (TestMain + TestBDDSuite) is UNCHANGED — feature-file discovery from     -->
<!--    tests/bdd/features/*.feature picks up scan.feature + suppression.feature automatically.        -->
<!--                                                                                                   -->
<!-- 5. testify (require/assert) is acceptable INSIDE tests/bdd/steps/*.go per CLAUDE.md "Test         -->
<!--    dependencies (BDD only): godog, goleak, testify — all in tests/bdd/go.mod, structurally        -->
<!--    isolated." Use testify sparingly; prefer fmt.Errorf assertions consistent with existing        -->
<!--    scorer_steps.go / validate_steps.go conventions.                                               -->

<!-- scan public surface (recap from Plans 09-01..09-06): -->
<!--                                                                                                  -->
<!-- type Item struct { Name, Group string; SilenceLint bool; Tag any }                               -->
<!-- type Kind int   // KindWithinGroup=1, KindAcrossGroups=2; String() returns CamelCase             -->
<!-- type Warning struct { Kind Kind; NameA, NameB string; GroupA, GroupB string;                     -->
<!--                       TagA, TagB any; Score float64; Scores map[fuzzymatch.AlgoID]float64 }      -->
<!-- type Config struct { Scorer *fuzzymatch.Scorer; CompareAcrossGroups bool;                        -->
<!--                      CrossGroupThresholdBoost float64; CompareIdenticalAcrossGroups bool;        -->
<!--                      SuppressedPairs [][2]string }                                               -->
<!-- func DefaultConfig(s *fuzzymatch.Scorer) Config   // boost=0.05, identical-cross=false           -->
<!-- func Check(items []Item, cfg Config) ([]Warning, error)                                          -->
<!-- Sentinels: ErrNilScorer, ErrInvalidItem, ErrInvalidConfig (all `errors.Is`-compatible).          -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — create scan_steps.go skeleton with ScanContext + InitScanSteps registration entry-point</name>
  <files>tests/bdd/steps/scan_steps.go, tests/bdd/steps/algorithms_steps.go</files>
  <read_first>
    - tests/bdd/steps/scorer_steps.go lines 1-100 (file header, ScorerContext struct, ScorerContext field documentation pattern)
    - tests/bdd/steps/scorer_steps.go lines 720-770 (InitScorerSteps function — verbatim shape for the registration entry-point)
    - tests/bdd/steps/validate_steps.go (analog: another single-feature step file with its own Context struct + Init*Steps entry-point; shorter than scorer_steps.go)
    - tests/bdd/steps/algorithms_steps.go lines 1580-1608 (the InitializeScenario chain that this task amends — append InitScanSteps(ctx) at the end)
    - tests/bdd/go.mod (confirm godog, goleak, testify already imported — these are the Phase 8 imports)
    - tests/bdd/bdd_test.go (confirm runner discovers features/*.feature automatically — no runner change required)
    - .planning/phases/09-collection-scan-sub-package/09-CONTEXT.md §1-§5 (D-01..D-08; the ScanContext field set and step regexes encode these decisions)
    - scan/scan.go (Check signature + Item / Config / Warning / Kind shapes — the public surface ScanContext drives)
    - scan/errors.go (ErrNilScorer, ErrInvalidItem, ErrInvalidConfig sentinel set)
  </read_first>
  <action>
    Create tests/bdd/steps/scan_steps.go (package steps). Apache 2.0 license header verbatim from validate_steps.go. File-header godoc block (analog: scorer_steps.go:15-26) explaining: (a) the ScanContext struct holds per-scenario state for scan + suppression scenarios; (b) goleak in tests/bdd/bdd_test.go gates the implicit "no goroutine leak" invariant — scan is pure-function so no goroutines are expected, but goleak catches regressions; (c) testify is permitted here per CLAUDE.md test-deps allowlist; (d) ScanContext field documentation block describing every field's purpose and which scenario class consumes it.

    Imports: "errors", "fmt", "math", "sort", "github.com/cucumber/godog", "github.com/axonops/fuzzymatch", "github.com/axonops/fuzzymatch/scan". Testify imports allowed but not mandatory.

    Declare ScanContext struct with these fields (each with a godoc comment summarising scope):
    - scorer *fuzzymatch.Scorer (the Scorer under test for the scenario; initialised by the "Given I construct" steps)
    - items []scan.Item (the collection under test for the scenario)
    - cfg scan.Config (the Config under test for the scenario)
    - suppressedPairs [][2]string (staged before scan.Check is invoked; copied into cfg.SuppressedPairs by the final step before invocation)
    - warnings []scan.Warning (the result of scan.Check; consumed by the "Then" assertion steps)
    - lastErr error (the error result of scan.Check; consumed by "Then errors.Is" steps)

    Declare and export func InitScanSteps(ctx *godog.ScenarioContext). Body: instantiate a fresh ScanContext per scenario via godog.BeforeScenario or directly inside Init (mirror scorer_steps.go InitScorerSteps shape). Register every step regex this plan needs (Tasks 2-4 list them; this task scaffolds InitScanSteps as a callable function with no regex registrations yet — Task 4 adds the regexes).

    Add a placeholder step closure in InitScanSteps that, for any scenario, sets sc := &ScanContext{} to ensure per-scenario isolation. The pattern mirrors scorer_steps.go:736-748.

    Then amend tests/bdd/steps/algorithms_steps.go: locate the InitializeScenario function (around line 1103); after the InitDeterminismSteps(ctx) call near line 1607, append:

        // Phase 9 — collection-scan + suppression steps (plans 09-01..09-07).
        // Registered via a dedicated InitScanSteps function in scan_steps.go
        // so the ScanContext lives in its own type (separate from
        // AlgorithmContext, ScorerContext, ValidateContext, NormalisationContext,
        // and DeterminismContext). All scan scenarios live under the @scan tag
        // in features/scan.feature; all suppression scenarios live under the
        // @suppression tag in features/suppression.feature.
        InitScanSteps(ctx)

    Verify: `cd tests/bdd && go build ./...` succeeds (the placeholder InitScanSteps must compile — Task 4 fills in the regexes).
  </action>
  <verify>
    <automated>bash -c 'cd tests/bdd && go build ./...'</automated>
  </verify>
  <acceptance_criteria>
    - tests/bdd/steps/scan_steps.go exists with package steps + Apache 2.0 header + file-header godoc + ScanContext struct (6 fields all godoc'd) + exported func InitScanSteps(ctx *godog.ScenarioContext)
    - tests/bdd/steps/algorithms_steps.go InitializeScenario function contains `InitScanSteps(ctx)` call after `InitDeterminismSteps(ctx)` line
    - `cd tests/bdd && go build ./...` exits 0
    - `cd tests/bdd && go vet ./...` exits 0
    - No new entries in tests/bdd/go.mod (godog + goleak + testify already present from Phase 8)
  </acceptance_criteria>
  <done>
    - scan_steps.go skeleton compiles cleanly
    - InitScanSteps wired into algorithms_steps.go InitializeScenario chain
    - tests/bdd builds without test-runner changes
  </done>
</task>

<task type="auto">
  <name>Task 2: Write tests/bdd/features/scan.feature — the public-API + validation + sort-determinism scenario set (closes D-03 + D-04 + D-06 + D-07 BDD coverage)</name>
  <files>tests/bdd/features/scan.feature</files>
  <read_first>
    - tests/bdd/features/scorer.feature lines 1-30 (analog: tagged feature header with mandatory-scenario-class comment block listing every CONTEXT decision the file covers)
    - tests/bdd/features/validate.feature lines 1-40 (analog: per-WarnKind scenario discipline; emulate the "one scenario per locked decision" pattern)
    - .planning/phases/09-collection-scan-sub-package/09-CONTEXT.md §2 (D-03 empty-Name → ErrInvalidItem errors.Join; D-04 boost validation → ErrInvalidConfig; D-06 duplicate (Name, Group) → ErrInvalidItem errors.Join)
    - .planning/phases/09-collection-scan-sub-package/09-CONTEXT.md §4 (D-07 Validate / Check separation — surface this as a comment in scan.feature's header, not as a runtime test, because Check no longer invokes Validate)
    - docs/requirements.md §12.1 — Item / Warning / Config / Check shapes
    - docs/requirements.md §12.4 — sort key (Kind, NameA, NameB, GroupA, GroupB) and the no-map-iteration-on-output rule
    - scan/scan.go (Check entry-point + validation order: cfg.Scorer nil → Config field validation → Items validation → SuppressedPairs validation)
  </read_first>
  <action>
    Create tests/bdd/features/scan.feature. The file shape (verbatim mirror of scorer.feature lines 1-30 mandatory-class header):

    Header comment block (lines 1-25 approx):
    - "# Phase 9 BDD coverage: collection scan sub-package."
    - "# Mandatory scenario classes per 09-CONTEXT.md (every D-03..D-07 + sort + bucket smoke):"
    -  one-line mapping per class: "1. Within-group happy path → 'Within-group scan finds similar names'"
    -  continue numbered list through ALL the scenarios below
    - "# D-07 boundary note: scan.Check does NOT internally invoke fuzzymatch.Validate — consumers compose Validate-then-Check explicitly. This separation is documented in docs/scan.md and not tested at runtime here; D-07 manifests as the absence of a Diagnostics field on scan.Warning."

    Feature declaration:
    @scan
    Feature: Collection scan (Phase 9)
      The scan sub-package finds pairs of similar names in a collection
      with within-group and cross-group passes, deterministic output
      ordering, validation surface (D-03 / D-04 / D-06), and a token-
      bucket optimisation that produces identical output to the naive
      O(N²) reference. Background lives in docs/requirements.md §12 and
      docs/scan.md.

    Minimum scenario set (Claude's discretion on phrasing; the SCENARIO NAMES are normative — bdd-scenario-reviewer's gate is "every decision has one"):

    1. @scan @within @happy
       Scenario: Within-group scan finds similar names
       Given I construct the default Scorer
       And the scan items
         | name    | group | silence_lint |
         | user_id | login | false        |
         | userId  | login | false        |
         | other   | login | false        |
       And the scan config is the default scan config
       When I invoke scan.Check
       Then scan.Check returns no error
       And the scan warnings include a WithinGroup pair with names "userId" and "user_id"

    2. @scan @within @threshold
       Scenario: Within-group scan respects threshold
       (items where the composite score is below 0.85; no warnings emitted)

    3. @scan @cross @boost
       Scenario: Cross-group scan applies threshold boost from DefaultConfig
       Given I construct the default Scorer
       And the scan items <2 items in different groups>
       And the scan config is the default scan config
       And I set CompareAcrossGroups to true
       When I invoke scan.Check
       Then the effective cross-group threshold is min(1.0, scorer.Threshold() + 0.05)
       (verified by computing the boost arithmetic in the step)

    4. @scan @cross @identical @default-suppressed
       Scenario: Cross-group identical-name pairs are suppressed by default
       Given the scan items
         | name    | group   | silence_lint |
         | user_id | login   | false        |
         | user_id | profile | false        |
       And the scan config is the default scan config
       And I set CompareAcrossGroups to true
       When I invoke scan.Check
       Then the scan warnings list has 0 AcrossGroups entries

    5. @scan @cross @identical @opt-in
       Scenario: Cross-group identical-name pairs are emitted when CompareIdenticalAcrossGroups is true
       (same items as scenario 4; opt-in flips the default; one AcrossGroups warning emitted)

    6. @scan @validation @empty-name @D-03
       Scenario: Empty Item.Name returns ErrInvalidItem with errors.Join across all offending indices
       Given the scan items
         | name    | group   | silence_lint |
         | ok      | g1      | false        |
         | (empty) | g1      | false        |
         | ok2     | g1      | false        |
         | (empty) | g2      | false        |
       When I invoke scan.Check
       Then scan.Check returns an error matching scan.ErrInvalidItem
       And the error mentions index 1 and index 3

    7. @scan @validation @duplicate @D-06
       Scenario: Duplicate (Name, Group) returns ErrInvalidItem with errors.Join across all duplicate indices
       (items where indices 1 and 2 duplicate index 0; assert both indices appear in the joined error)

    8. @scan @validation @boost @D-04
       Scenario: NaN CrossGroupThresholdBoost returns ErrInvalidConfig
       Given the scan items <valid items>
       And the scan config has CrossGroupThresholdBoost set to NaN
       When I invoke scan.Check
       Then scan.Check returns an error matching scan.ErrInvalidConfig

    9. @scan @determinism @sort
       Scenario: Output is sorted by (Kind, NameA, NameB, GroupA, GroupB) deterministically across runs
       Given the scan items <a set that produces ≥3 warnings>
       And the scan config is the default scan config
       When I invoke scan.Check twice
       Then the two scan warnings slices are byte-identical
       And the warnings are sorted by (Kind, NameA, NameB, GroupA, GroupB)

    10. @scan @bucket @smoke
        Scenario: Bucket optimisation produces same output as naive within-group for a small group
        Given the scan items <a group with ≤ bucketThreshold items>
        And the scan config is the default scan config
        When I invoke scan.Check
        Then the warnings match the naive-loop reference output
        (the step recomputes the naive output via a private helper in scan_steps.go that calls scan.Check on the same input; the assertion is that the documented PropCheck_BucketEquivalentToNaive invariant holds at the BDD granularity for the chosen group size)

    Each scenario carries a # inline comment citing the closed decision ID (D-03, D-04, D-06, etc.) — matching scorer.feature's commented-citation style.

    Save as tests/bdd/features/scan.feature.
  </action>
  <verify>
    <automated>bash -c 'test -f tests/bdd/features/scan.feature && grep -q "Feature: Collection scan" tests/bdd/features/scan.feature && grep -c "Scenario:" tests/bdd/features/scan.feature | awk "{ if (\$1 < 10) exit 1 }"'</automated>
  </verify>
  <acceptance_criteria>
    - tests/bdd/features/scan.feature exists with header comment listing every locked decision
    - At least 10 Scenarios declared with the names above (or close paraphrases — bdd-scenario-reviewer's call)
    - Every D-03, D-04, D-06, D-07 decision is cited inline (grep `D-03`, `D-04`, `D-06`, `D-07` returns ≥ 4 matches)
    - File parses as valid Gherkin (godog discovers it automatically — verified in Task 4 when test-bdd runs)
    - Scenario naming consistent with scorer.feature / validate.feature conventions (sentence-case Title, leading verb)
  </acceptance_criteria>
  <done>
    - scan.feature exists with ≥ 10 scenarios covering D-03 / D-04 / D-06 / D-07 + sort determinism + bucket smoke
    - File header documents scenario-to-decision mapping
  </done>
</task>

<task type="auto">
  <name>Task 3: Write tests/bdd/features/suppression.feature — closes Phase 8.5 R2 deferred coverage (D-05 + cross-group suppression composition)</name>
  <files>tests/bdd/features/suppression.feature</files>
  <read_first>
    - tests/bdd/features/scorer.feature (analog: per-Kind scenario discipline)
    - tests/bdd/features/validate.feature (analog: one-scenario-per-WarnKind discipline; mirror for one-scenario-per-suppression-rule)
    - .planning/phases/09-collection-scan-sub-package/09-CONTEXT.md §2 D-05 (SuppressedPairs validation + self-pair kept-silent rule) + §3 (suppression composition via OR)
    - .planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md (Phase 8.5 R2 — Gap 3 deferral of suppression BDD)
    - .planning/REQUIREMENTS.md SCAN-03 (suppression composition) and TEST-05 (BDD scope)
    - scan/suppress.go (the isSuppressed predicate + Rule 1 SilenceLint + Rule 2 SuppressedPairs canonical-pair + Rule 3 cross-group identical-name)
    - docs/requirements.md §12.3 (suppression composition + order-of-evaluation)
  </read_first>
  <action>
    Create tests/bdd/features/suppression.feature. The file shape mirrors scan.feature's header-comment + mandatory-class structure.

    Header comment block (lines 1-30 approx):
    - "# Phase 9 BDD coverage: suppression composition (closes Phase 8.5 R2 Gap 3 deferral)."
    - "# Three suppression rules per 09-CONTEXT.md §3 + spec §12.3:"
    -   "#   Rule 1 — per-item SilenceLint (cheapest; OR semantics on a.SilenceLint || b.SilenceLint)"
    -   "#   Rule 2 — SuppressedPairs canonical-pair lookup (case-insensitive via Scorer.NormalisationOptions)"
    -   "#   Rule 3 — cross-group identical-name default (CompareIdenticalAcrossGroups=false suppresses identical Names across different Groups)"
    -   "# Rules compose via OR — any rule firing suppresses the pair. Applied pre-emission, never post."
    - "# Mandatory scenarios per CONTEXT.md §3 + Phase 8.5 R2 deferral list:"
    -  numbered list of every scenario below

    Feature declaration:
    @suppression
    Feature: Suppression composition (Phase 9 — closes Phase 8.5 R2)
      Suppression rules compose additively via OR. Per-item SilenceLint
      is the cheapest check; SuppressedPairs is canonicalised once at
      Check entry via the Scorer's NormalisationOptions; the cross-group
      identical-name default is the third rule, opt-out via
      Config.CompareIdenticalAcrossGroups. Background: docs/requirements.md
      §12.3 and docs/scan.md.

    Minimum scenarios (Claude's discretion on phrasing, bdd-scenario-reviewer adjudicates final list):

    1. @suppression @silencelint @rule1
       Scenario: SilenceLint=true on Item A suppresses all pairs involving A
       (one item has SilenceLint=true; assert no warnings name that item)

    2. @suppression @silencelint @rule1
       Scenario: SilenceLint=true on Item B suppresses all pairs involving B
       (mirror — sanity check that suppression is symmetric on which side carries the flag)

    3. @suppression @silencelint @or @rule1
       Scenario: SilenceLint on either side suppresses (OR semantics)
       (two items; A has SilenceLint=false, B has SilenceLint=true; A-B pair is suppressed)

    4. @suppression @suppressedpairs @rule2
       Scenario: SuppressedPairs entry suppresses the listed canonical pair
       (SuppressedPairs=[["user_id","userId"]]; assert user_id ↔ userId warning is absent)

    5. @suppression @suppressedpairs @canonical @rule2
       Scenario: SuppressedPairs is canonicalised via Scorer NormalisationOptions
       (SuppressedPairs=[["USER_ID","UserId"]] with default Scorer normalisation; items "user_id" / "userId" — still suppressed because canonical forms match)

    6. @suppression @suppressedpairs @selfpair @D-05
       Scenario: Self-pairs in SuppressedPairs are silently kept and never emit
       (SuppressedPairs=[["x","x"]] is accepted at validation; assert Check returns nil error and the input "x"/"x" never produces a self-warning regardless of SuppressedPairs membership)

    7. @suppression @cross @identical @rule3
       Scenario: Cross-group identical-name default suppression composes with explicit SuppressedPairs
       (CompareAcrossGroups=true, CompareIdenticalAcrossGroups=false, plus SuppressedPairs containing an explicit non-identical pair — assert both suppression paths fire independently)

    8. @suppression @or @composition
       Scenario: Suppression rules compose via OR — any rule firing suppresses
       (assemble a scenario where Rule 1 fires for one pair, Rule 2 fires for a second pair, Rule 3 fires for a third pair; assert all three pairs absent from output)

    9. @suppression @ordering @pre-emission
       Scenario: Suppression is applied pre-emission, never post-emission
       (run Check; capture the warnings; assert no warning matches a suppressed pair. Implementation detail: the assertion compares warning count + content against the "without suppression" reference computed by a step that toggles all suppression off and re-runs)

    Each scenario carries an inline #-comment citing Phase 8.5 R2 closure for at least the three SilenceLint scenarios + the SuppressedPairs canonical scenario + the composition scenario.

    Save as tests/bdd/features/suppression.feature.
  </action>
  <verify>
    <automated>bash -c 'test -f tests/bdd/features/suppression.feature && grep -q "Feature: Suppression composition" tests/bdd/features/suppression.feature && grep -c "Scenario:" tests/bdd/features/suppression.feature | awk "{ if (\$1 < 9) exit 1 }"'</automated>
  </verify>
  <acceptance_criteria>
    - tests/bdd/features/suppression.feature exists with ≥ 9 Scenarios covering Rule 1 (SilenceLint), Rule 2 (SuppressedPairs), Rule 3 (cross-group identical), composition, and pre-emission ordering
    - File header documents the three rules and lists every scenario
    - D-05 explicitly cited in ≥ 1 scenario (self-pair-kept-silent)
    - Phase 8.5 R2 closure cited in ≥ 1 inline comment
    - File parses as valid Gherkin (verified in Task 5 when test-bdd runs)
  </acceptance_criteria>
  <done>
    - suppression.feature exists with ≥ 9 scenarios covering all three rules + composition + pre-emission ordering
    - Phase 8.5 R2 deferral cited as closed
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement ScanContext step definitions in scan_steps.go backing every scan.feature + suppression.feature step</name>
  <files>tests/bdd/steps/scan_steps.go</files>
  <read_first>
    - tests/bdd/steps/scorer_steps.go lines 85-300 (table-step pattern: godog DataTable parsing into typed slices; ScorerContext mutation discipline)
    - tests/bdd/steps/scorer_steps.go lines 725-770 (InitScorerSteps full body — every ctx.Step regex registered in one place; the pattern Task 1 scaffolded)
    - tests/bdd/steps/validate_steps.go (a shorter analog — fewer steps; clearer for the InitX boilerplate)
    - tests/bdd/features/scan.feature (the scenarios this task backs — read after Task 2 lands)
    - tests/bdd/features/suppression.feature (the scenarios this task backs — read after Task 3 lands)
    - scan/scan.go (Check signature; cfg.Scorer = nil checks; validation order)
    - scan/errors.go (the three sentinel errors used by error-assertion steps)
    - scan/suppress.go (the canonical-pair helper — used by Rule 2 scenarios)
    - .claude/skills/go-testing-standards/SKILL.md (BDD section: testify usage discipline, godot.DataTable patterns)
  </read_first>
  <behavior>
    Step definitions implement every Given/When/Then in scan.feature + suppression.feature. The set must cover:

    GIVEN steps:
    - "I construct the default Scorer" — sc.scorer = fuzzymatch.DefaultScorer()
    - "I construct a Scorer with threshold {float}" — alternative for non-default threshold scenarios
    - "the scan items {godog.Table}" — parse the table into []scan.Item; columns: name, group, silence_lint (bool); "(empty)" sentinel in name column yields ""
    - "the scan config is the default scan config" — sc.cfg = scan.DefaultConfig(sc.scorer)
    - "I set CompareAcrossGroups to true" / "to false" — sc.cfg.CompareAcrossGroups = …
    - "I set CompareIdenticalAcrossGroups to true" / "to false"
    - "the scan config has CrossGroupThresholdBoost set to NaN" — math.NaN()
    - "the scan config has CrossGroupThresholdBoost set to {float}"
    - "I set suppressed pairs" {godog.Table} — columns: a, b; appended to sc.cfg.SuppressedPairs

    WHEN steps:
    - "I invoke scan.Check" — sc.warnings, sc.lastErr = scan.Check(sc.items, sc.cfg)
    - "I invoke scan.Check twice" — run twice; compare via deep equal; store the diff for the Then step
    - (implicit) the bucket-smoke scenario's step uses a sentinel-input that exercises the naive path with bucketThreshold-respecting group size

    THEN steps:
    - "scan.Check returns no error" — assert sc.lastErr == nil
    - "scan.Check returns an error matching scan.ErrInvalidItem" — assert errors.Is(sc.lastErr, scan.ErrInvalidItem)
    - "scan.Check returns an error matching scan.ErrInvalidConfig" — assert errors.Is(sc.lastErr, scan.ErrInvalidConfig)
    - "scan.Check returns an error matching scan.ErrNilScorer"
    - "the error mentions index {int} and index {int}" — string-contains on sc.lastErr.Error()
    - "the scan warnings include a WithinGroup pair with names {string} and {string}"
    - "the scan warnings include an AcrossGroups pair with names {string} and {string}"
    - "the scan warnings list has {int} entries"
    - "the scan warnings list has {int} AcrossGroups entries" / "WithinGroup entries"
    - "no warning involves {string}" (used by SilenceLint scenarios)
    - "the warnings are sorted by (Kind, NameA, NameB, GroupA, GroupB)" — walk the slice, assert lex ordering on the 5-tuple key
    - "the two scan warnings slices are byte-identical" — for the determinism scenario; recompute via two scan.Check invocations and json.Marshal both, byte-compare
    - "the warnings match the naive-loop reference output" — bucket-smoke scenario; the helper re-invokes scan.Check on the same input with a flag that forces the naive path (NOT a public API — for the BDD step, this assertion can be relaxed to "the output count matches the documented within-group pair count for the input" if forcing the naive path isn't accessible from outside the scan package; bdd-scenario-reviewer adjudicates the final form)
    - "the effective cross-group threshold is min(1.0, scorer.Threshold() + 0.05)" — compute and assert against a recorded inner-loop step (this is a documentation-of-intent step, not a runtime-internal-state assertion; the assertion form is "the cross-group output is consistent with the documented boost arithmetic")

    Use stdlib errors.Is, fmt.Errorf, sort, and (optionally) testify require/assert. Prefer stdlib + fmt.Errorf assertions consistent with scorer_steps.go / validate_steps.go.

    Per-scenario isolation: ctx.BeforeScenario closure resets sc := &ScanContext{} so cross-scenario state never leaks (mirror scorer_steps.go).
  </behavior>
  <action>
    Extend tests/bdd/steps/scan_steps.go (Task 1's skeleton) with full step regexes. The InitScanSteps function body registers every ctx.Step("^...$", sc.method) regex for the steps listed in the behavior block above.

    Concrete identifier conventions:
    - ScanContext methods are unexported and named after the step they implement (e.g. iConstructTheDefaultScorer, theScanItems, iInvokeScanCheck, theScanWarningsIncludeAWithinGroupPairWithNamesAndNames)
    - DataTable parsing: use godog.Table; iterate rows[1:] skipping header; the "(empty)" sentinel in a name column yields "" for that field
    - silence_lint column accepts "true" / "false" — parse via strconv.ParseBool
    - SuppressedPairs table columns "a" and "b" — pushed into sc.cfg.SuppressedPairs as [2]string{a, b}

    For the byte-identical determinism step, marshal both warning slices via encoding/json with deterministic field ordering and compare via bytes.Equal. (json.Marshal is deterministic given a struct with no map fields; the Scores map field requires either a converter or AlgoID-key stringification before marshal — re-use the convention from scan_golden_test.go's scoreAllAsStringKeys helper.)

    Each step method's godoc has a one-line summary referencing the @tag the scenarios it backs carry (e.g. "// theScanItems parses a godog table of scan.Item rows. Backs scenarios under @scan and @suppression.")

    Add a helper:
        // parseScanItems converts a godog.Table to a []scan.Item.
        // Columns: name, group, silence_lint. "(empty)" sentinel in a
        // name column yields "" (used for the D-03 empty-Name validation
        // scenario).
        func parseScanItems(table *godog.Table) ([]scan.Item, error) { ... }

    After implementation: `cd tests/bdd && go build ./...` → exit 0. `make test-bdd` → exit 0 with both new feature files passing.

    Run `cd tests/bdd && go vet ./...` — clean.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd tests/bdd && go build ./... && go vet ./... && cd ../.. && make test-bdd'</automated>
  </verify>
  <acceptance_criteria>
    - tests/bdd/steps/scan_steps.go ScanContext methods implement every Given/When/Then in scan.feature + suppression.feature
    - InitScanSteps registers every regex used by the two feature files (verified by `make test-bdd` not reporting "step is undefined" for any line)
    - `cd tests/bdd && go build ./...` exits 0
    - `cd tests/bdd && go vet ./...` exits 0
    - `make test-bdd` exits 0 with all scan + suppression scenarios passing
    - goleak (in tests/bdd/bdd_test.go TestMain) reports zero goroutine leaks after the full BDD suite runs (scan adds no goroutines so this is structurally satisfied; verifying the suite still passes is the gate)
    - parseScanItems helper exists with "(empty)" sentinel handling and silence_lint bool parsing
  </acceptance_criteria>
  <done>
    - All scan + suppression scenarios pass under `make test-bdd`
    - Zero "step is undefined" or "ambiguous step" reports from godog
    - goleak gate green
  </done>
</task>

<task type="auto">
  <name>Task 5: Final agent gates + atomic commit</name>
  <files>(no new files — agent reviews + commit)</files>
  <read_first>
    - .claude/skills/fuzzymatch-review-protocol/SKILL.md
    - .claude/skills/go-testing-standards/SKILL.md (BDD coverage rule)
    - tests/bdd/features/scan.feature (created in Task 2)
    - tests/bdd/features/suppression.feature (created in Task 3)
    - tests/bdd/steps/scan_steps.go (created in Tasks 1+4)
  </read_first>
  <action>
    Final quality gate:
    (a) `make test-bdd` — exit 0
    (b) `make check` — exit 0 (full quality gate including go vet, golangci-lint, gofmt-check, etc.)
    (c) `cd tests/bdd && go test -race -count=1 -shuffle=on ./...` — exit 0 (BDD module race-safe)
    (d) Coverage check via `cd tests/bdd && go test -coverprofile=/tmp/bdd-cov.out ./...` (informational; BDD coverage isn't a hard gate but verifies no untested step closures)

    Agent reviewers (invoke in order; record verdicts in 09-07-SUMMARY.md):
    (i) bdd-scenario-reviewer — MANDATORY GATE per planning_context. Focus:
        - scan.feature has ≥ 1 scenario per D-03 / D-04 / D-06 / D-07
        - suppression.feature has ≥ 1 scenario per Rule 1 / Rule 2 / Rule 3 + composition
        - Phase 8.5 R2 deferral is materially closed (≥ 3 interaction scenarios beyond unit-test coverage)
        - Scenario naming is clear, declarative, consumer-perspective
        - No redundant scenarios; no missing edge cases
        - Step regexes are unambiguous (godog will reject ambiguous matches)
    (ii) code-reviewer — focus: ScanContext field isolation; per-scenario reset; testify usage is restrained; no goroutines spawned
    (iii) security-reviewer — focus: BDD inputs include malformed UTF-8 / null bytes (already covered in scan/fuzz_test.go, but ensure BDD doesn't introduce panic-prone inputs); error-assertion steps don't dereference nil errors
    (iv) determinism-reviewer — focus: the byte-identical scenario in scan.feature uses deterministic marshalling; no map iteration on assertion-output paths
    (v) go-quality + commit-message-reviewer (skip issue-ref findings per user-memory project_no_github_issues)

    Stage the commit (single atomic commit — all three files form a coherent BDD addition):
        git add tests/bdd/features/scan.feature tests/bdd/features/suppression.feature tests/bdd/steps/scan_steps.go tests/bdd/steps/algorithms_steps.go
        git commit -m "test(09-07): add BDD scenarios for scan + suppression

        Closes TEST-05 BDD coverage for the scan sub-package and the
        Phase 8.5 R2 deferred suppression scenarios. Two new feature
        files (scan.feature + suppression.feature) cover every D-03..D-07
        locked decision plus rule composition + pre-emission ordering.
        scan_steps.go provides the godog step definitions; InitScanSteps
        is wired into algorithms_steps.go InitializeScenario alongside
        the existing Init*Steps chain.

        bdd-scenario-reviewer: <verdict>
        code-reviewer:         <verdict>
        security-reviewer:     <verdict>
        determinism-reviewer:  <verdict>
        go-quality:            <verdict>
        "

    Do NOT push to origin — the phase-boundary push is handled in Plan 09-08 (per user-memory feedback_push_cadence: push at every phase boundary, not at every plan boundary).
  </action>
  <verify>
    <automated>bash -c 'set -e; make test-bdd && make check && cd tests/bdd && go test -race -shuffle=on -count=1 ./... && cd ../.. && git log --oneline -1 | grep -E "test\(09-07\)"'</automated>
  </verify>
  <acceptance_criteria>
    - `make test-bdd` exits 0
    - `make check` exits 0
    - `cd tests/bdd && go test -race -shuffle=on -count=1 ./...` exits 0
    - 5 agent verdicts recorded in 09-07-SUMMARY.md (bdd-scenario-reviewer + code-reviewer + security-reviewer + determinism-reviewer + go-quality)
    - 1 commit landed with title `test(09-07): add BDD scenarios for scan + suppression`
    - Commit includes the four files: scan.feature + suppression.feature + scan_steps.go + algorithms_steps.go
    - No push to origin (deferred to Plan 09-08)
  </acceptance_criteria>
  <done>
    - All gates green
    - bdd-scenario-reviewer + 4 other agent verdicts recorded
    - 09-07-SUMMARY.md written with file paths, scenario counts, agent verdicts
    - Commit landed; push deferred
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gherkin step input → ScanContext mutation | Step parameters parsed via godog into typed fields; "(empty)" sentinel in name column yields "" |
| ScanContext → scan public surface | Every Given/When step ends with a scan public-API call; ErrInvalid* assertions surface validation via errors.Is |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-01 | Spoofing | Ambiguous step regex matching wrong scenario | mitigate | godog detects ambiguous regex matches at runtime; bdd-scenario-reviewer reviews InitScanSteps for non-overlapping patterns; `make test-bdd` fails if ambiguous. |
| T-09-07-02 | Tampering | Step closure mutates ScanContext.cfg across scenarios | mitigate | ctx.BeforeScenario instantiates fresh ScanContext per scenario (mirrors scorer_steps.go); verified by code-reviewer. |
| T-09-07-03 | Denial of Service | Pathological Gherkin DataTable triggers OOM in parseScanItems | accept | godog itself bounds table size; parseScanItems streams row-by-row; risk is bounded by the test harness, not consumer-facing. |
| T-09-07-04 | Information Disclosure | Step assertion strings include consumer-supplied Item content in failure messages | accept | This is by design — clear failure messages cite the input that triggered the failure; consumer-owned data is acceptable in test output. |
</threat_model>

<verification>
- `make test-bdd` green
- `make check` green
- `cd tests/bdd && go test -race -shuffle=on -count=1 ./...` green
- bdd-scenario-reviewer verdict recorded (MANDATORY gate)
- 4 other agent verdicts recorded
- 1 commit landed; no push to origin
</verification>

<success_criteria>
- TEST-05 closed at the BDD layer for scan + suppression
- Phase 8.5 R2 Gap 3 deferral closed (suppression BDD scenarios delivered)
- SCAN-03 closed at the BDD layer (suppression composition rule interactions)
- Scenarios are consumer-perspective and decision-traceable (D-03..D-07 + Rules 1..3 cited)
- goleak gate continues to hold (zero goroutine leaks after the augmented BDD suite)
- Plan 09-08 receives a fully-tested scan sub-package ready for examples + docs + finalisation
</success_criteria>

<output>
After completion, create `.planning/phases/09-collection-scan-sub-package/09-07-SUMMARY.md` capturing:
- Scenario count per feature file (scan.feature N scenarios; suppression.feature M scenarios)
- ScanContext field count + step regex count in scan_steps.go
- bdd-scenario-reviewer verdict + 4 other agent verdicts (verbatim or summarised with file/line refs)
- Confirmation `make test-bdd` exit 0 + `make check` exit 0 + race+shuffle BDD pass
- Commit SHA
- Push status: deferred (per user-memory feedback_push_cadence — phase-boundary push in Plan 09-08)
- Cumulative phase 9 test count (root + scan + BDD)
</output>
