# Phase 05 Deferred Items

Out-of-scope discoveries logged during plan 05-05 finalisation execution per gsd-executor.md scope-boundary rule.

## Resolved (auto-fixed during plan 05-05 finalisation)

- **`gofmt -s` violations across seven Phase 5 files** — pre-existing from plans 05-01 / 05-02 / 05-04 (commits prior to 47ad37e). Files affected: `props_test.go`, `qgram_jaccard.go`, `qgram_jaccard_fuzz_test.go`, `qgram_jaccard_test.go`, `sorensen_dice.go`, `sorensen_dice_fuzz_test.go`, `tversky.go`. The violations were canonical-form godoc continuation indentation: gofmt's strict-form rule requires tab-indented continuation lines under bullet headers, and the prior plans landed those godoc blocks with space indentation. RESOLVED here per Rule 3 (blocking — `make check` would not pass without fmt-check passing): `gofmt -s -w` applied to the seven files; behaviour unchanged (whitespace only). Same pattern as Phase 4 plan 04-05's `strcmp95.go` resolution.

- **`gocyclo` warnings on `cosineFromQGramMaps` (cosine.go:303, complexity 13) and `tverskyFromQGramMaps` (tversky.go:338, complexity 11)** — pre-existing from plans 05-03 and 05-04 respectively. Both helpers fold the empty-multiset short-circuit + smaller-side intersection iteration + sort-key-canonicalised dot-product (cosine) / asymmetric weighting (tversky) into a single function for performance and locality reasons. The complexity is intrinsic to the algorithm: reducing it would either split the function (extra allocation + extra function-call overhead on the hot path, against PERF-04 budget) or hide essential branches behind helpers (against the determinism-reviewer's auditable-iteration discipline). RESOLVED here per the established project precedent: `//nolint:gocyclo` directive added to both function declarations with an inline rationale comment, mirroring `algoid.go::String()`, `damerau_osa.go::damerauOSADP`, `damerau_osa.go::damerauOSADistanceRuneSlices`, `damerau_full.go::damerauFullDP`, `damerau_full.go::damerauFullDistanceRuneSlices`. `make lint` exits 0.

- **staticcheck ST1005 on two error strings in `tests/bdd/steps/algorithms_steps.go`** — pre-existing from plan 05-04 Tversky scenario step bindings. Two `fmt.Errorf` messages began with capital letters (`"Tversky asymmetry gate FAILED…"`, `"Tversky scores not equal…"`) which Go convention forbids — error strings should not be capitalised since they are typically chained or wrapped. RESOLVED here per Rule 3 (blocking — `golangci-lint run ./...` from the BDD module would not exit 0 without the fix): both error messages downcased to `"tversky asymmetry gate FAILED…"` / `"tversky scores not equal…"`. Behaviour and meaning preserved.

- **Phase 5 dispatch closures uncovered (`dispatch_qgram_jaccard.go`, `dispatch_sorensen_dice.go`, `dispatch_cosine.go`, `dispatch_tversky.go` all 66.67% < 90% per-file floor)** — pre-existing from plans 05-01..05-04. Phase 2-4 dispatch files use direct function references (`dispatch[X] = XScore`, single statement = 100% coverage); Phase 5 dispatch files use closure wrappers (`dispatch[X] = func(a,b string) float64 { return XScore(a,b,3) }`) because the q-gram algorithms need to bind default `n=3` (and α=β=1.0 for Tversky). The closure body never got exercised by tests, leaving 1 of 3 statements uncovered → 66.67%. RESOLVED here per Rule 3 (blocking — `make coverage-check` would not pass without the fix): added `DispatchInvokeForTest` helper to `export_test.go` and extended each of the four existing `TestDispatch_*Registered` tests to invoke the dispatched closure and assert it returns the same score as the underlying `*Score` function. All four dispatch files now report 100% coverage; overall coverage 97.1%.

## Outstanding (none)

No advisory items remained outstanding at the close of Phase 5. The two items above were both auto-fixed under Rule 3 because they were blocking the `make check` gate; the established project pattern (gofmt application + `//nolint:gocyclo` with rationale) was the minimal-impact remediation.
