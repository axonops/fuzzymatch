---
phase: 06-token-based-algorithms
plan: 05
type: execute
wave: 5
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - monge_elkan.go
  - monge_elkan_test.go
  - monge_elkan_bench_test.go
  - monge_elkan_fuzz_test.go
  - dispatch_monge_elkan.go
  - testdata/golden/_staging/monge_elkan.json
  - tests/bdd/features/monge_elkan.feature
  - tests/bdd/steps/algorithms_steps.go
  - props_test.go
  - example_test.go
  - llms.txt
  - llms-full.txt
autonomous: true
requirements: [TOKEN-01]
must_haves:
  truths:
    - "User can call MongeElkanScore(a, b, inner, opts) for the asymmetric Monge-Elkan similarity, where inner is one of 14 permitted AlgoIDs (9 character-tier + 4 q-gram tier + AlgoRatcliffObershelp per OQ-4 RESOLUTION)"
    - "User can call MongeElkanScoreSymmetric(a, b, inner, opts) for the arithmetic mean of the two directions"
    - "MongeElkanScore('user create', 'usr creating', AlgoJaroWinkler, DefaultNormalisationOptions()) returns the per-token-max-mean derived score (formula reproduced in test comment per RESEARCH.md §1b: max(JW(user, usr) = 0.875, JW(user, creating)) = 0.875; max(JW(create, usr), JW(create, creating)) = JW(create, creating); ME = average)"
    - "MongeElkanScore is asymmetric: MongeElkanScore(A, B, inner, opts) ≠ MongeElkanScore(B, A, inner, opts) on at least one explicit pair (RV-ME-asym fixture)"
    - "MongeElkanScoreSymmetric(a, b) == MongeElkanScoreSymmetric(b, a) for all inputs (the arithmetic mean of the two directions makes the symmetric variant truly symmetric)"
    - "Calling MongeElkanScore with inner = AlgoMongeElkan panics with 'fuzzymatch: AlgoID MongeElkan not permitted as Monge-Elkan inner metric' (self-recursion gate per RESEARCH.md Pitfall 4)"
    - "Calling MongeElkanScore with any token-tier inner (TokenSortRatio, TokenSetRatio, PartialRatio, TokenJaccard) panics with the documented message (token-on-token meaningless)"
    - "Calling MongeElkanScore with any Phase 7 phonetic inner (Soundex, DoubleMetaphone, NYSIIS, MRA) panics — those are reserved for Phase 7's additive permitted-inner expansion"
    - "AlgoMongeElkan dispatch slot is registered with the symmetric variant + AlgoJaroWinkler default inner + DefaultNormalisationOptions per CONTEXT §4 LOCKED"
    - "BenchmarkMongeElkan_Pathological_1000Tokens (1000-token both-sides input) lands and is measurable"
    - "AlgoMongeElkan participates in the standard PropAlgorithmScore_Symmetric property test set without exemption (the dispatch wraps the symmetric variant per CONTEXT §4)"
  artifacts:
    - path: "monge_elkan.go"
      provides: "MongeElkanScore (asymmetric) + MongeElkanScoreSymmetric (symmetric) public surface, plus the unexported permittedMongeElkanInner allow-list (14 entries per OQ-4 RESOLUTION) declared at PACKAGE SCOPE (not init)"
      contains: "func MongeElkanScore"
    - path: "dispatch_monge_elkan.go"
      provides: "Dispatch wrapper binding MongeElkanScoreSymmetric(a, b, AlgoJaroWinkler, DefaultNormalisationOptions()) per CONTEXT §4 LOCKED"
    - path: "testdata/golden/_staging/monge_elkan.json"
      provides: "8-12 staging entries to be merged into algorithms.json by plan 06-06 (hand-derived per CONTEXT §1b LOCKED)"
    - path: "tests/bdd/features/monge_elkan.feature"
      provides: "Godog scenarios — canonical reference vectors AND explicit Scenario: MongeElkanScore is direction-sensitive (asymmetric pair fixture)"
    - path: "monge_elkan_bench_test.go"
      provides: "Standard ASCII/Unicode benchmarks + BenchmarkMongeElkan_Pathological_1000Tokens (DoS-T3 fixture)"
      contains: "BenchmarkMongeElkan_Pathological_1000Tokens"
  key_links:
    - from: "monge_elkan.go"
      to: "dispatch[inner]"
      via: "innerFn := dispatch[inner]; innerFn(tokA, tokB) per (i, j) in tokensA × tokensB iteration — safe because the allow-list gate above guarantees inner is in permittedMongeElkanInner AND that slot is non-nil at this point in the package's lifecycle"
      pattern: "dispatch\\[inner\\]"
    - from: "monge_elkan.go"
      to: "permittedMongeElkanInner"
      via: "if !permittedMongeElkanInner[inner] { panic(...) } gate at the top of MongeElkanScore (and re-check in MongeElkanScoreSymmetric via call-through to MongeElkanScore in both directions)"
      pattern: "permittedMongeElkanInner\\[inner\\]"
    - from: "dispatch_monge_elkan.go"
      to: "dispatch[AlgoMongeElkan]"
      via: "var _ = func() bool { dispatch[AlgoMongeElkan] = func(a, b string) float64 { return MongeElkanScoreSymmetric(a, b, AlgoJaroWinkler, DefaultNormalisationOptions()) }; return true }()"
      pattern: "MongeElkanScoreSymmetric\\(a, b, AlgoJaroWinkler"
---

<objective>
Land Monge-Elkan — the most complex Phase 6 algorithm. Two public functions (`MongeElkanScore` asymmetric and `MongeElkanScoreSymmetric` symmetric per CONTEXT §4 LOCKED), a strict 14-entry `permittedMongeElkanInner` package-scope allow-list (9 character + 4 q-gram + AlgoRatcliffObershelp per OQ-4 RESOLUTION LOCKED — see context block below), exhaustive panic-test for every non-permitted AlgoID, the dispatch wrapper binding the symmetric variant + JaroWinkler default inner per CONTEXT §4, asymmetric reference vectors hand-derived per CONTEXT §1b, asymmetry property test (mirrors Tversky α≠β pattern from Phase 5), and the DoS-vector three-part godoc block + Pathological_1000Tokens bench fixture per CONTEXT §5 LOCKED. Closes TOKEN-01.

Purpose: Monge-Elkan is the only Phase 6 algorithm that takes a parameter (inner AlgoID), uses the existing dispatch table for inner-metric resolution, has BOTH symmetric and asymmetric variants, and has the most surface-area for misconfiguration (the allow-list + panic discipline is the load-bearing safety surface). Phase 7's phonetic algorithms will EXTEND this allow-list ADDITIVELY (4 new entries → 18 total).

Output: 5 new `.go` files (algorithm + dispatch + test + bench + fuzz), 1 new BDD feature, 1 new staging golden; modifications to props_test.go (ME property block — symmetric variant in standard set + asymmetry test for direct-call ME), example_test.go (2 examples — asymmetric + symmetric), tests/bdd/steps/algorithms_steps.go (ME steps with the inner AlgoID parameter — parameter-rich pattern mirrors Tversky's), llms.txt (1 new section), llms-full.txt (1 new Phase 6 algorithm-surface block).

NO modifications to `token_ratio_cross_validation_test.go` — Monge-Elkan is NOT in the cross-validation corpus per CONTEXT §1b LOCKED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-token-based-algorithms/06-CONTEXT.md
@.planning/phases/06-token-based-algorithms/06-RESEARCH.md
@.planning/phases/06-token-based-algorithms/06-PATTERNS.md
@.planning/phases/06-token-based-algorithms/06-VALIDATION.md
@.planning/phases/06-token-based-algorithms/06-01-SUMMARY.md
@.planning/phases/06-token-based-algorithms/06-02-SUMMARY.md
@.planning/phases/06-token-based-algorithms/06-03-SUMMARY.md
@.planning/phases/06-token-based-algorithms/06-04-SUMMARY.md
@.claude/skills/algorithm-correctness-standards/SKILL.md
@.claude/skills/algorithm-licensing-standards/SKILL.md
@.claude/skills/performance-standards/SKILL.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/go-testing-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md
@token_indel.go
@token_sort_ratio.go
@token_set_ratio.go
@partial_ratio.go
@token_jaccard.go
@dispatch_token_sort_ratio.go
@dispatch_token_set_ratio.go
@dispatch_partial_ratio.go
@dispatch_token_jaccard.go
@tversky.go
@tversky_test.go
@dispatch_tversky.go
@qgram_jaccard.go
@dispatch_qgram_jaccard.go
@jarowinkler.go
@dispatch_jarowinkler.go
@tokenise.go
@normalise.go
@algoid.go
@errors.go
@props_test.go
@example_test.go
@llms.txt
@llms-full.txt
@tests/bdd/features/qgram_jaccard.feature
@tests/bdd/steps/algorithms_steps.go
@testdata/golden/_staging/qgram_jaccard.json

<interfaces>
<!-- Key types and contracts the executor needs. -->

From algoid.go:
- All 23 AlgoID constants — the panic test walks the entire enum
- `var dispatch [numAlgorithms]func(a, b string) float64` — Monge-Elkan reads `dispatch[inner]` after the allow-list gate

From normalise.go:
- `type NormalisationOptions struct { ... }`
- `func DefaultNormalisationOptions() NormalisationOptions`
- The signature `MongeElkanScore(a, b string, inner AlgoID, opts NormalisationOptions) float64` — opts threads through but Monge-Elkan does NOT call Normalise itself; opts is passed to make the API consistent with Phase 8's Scorer option `WithMongeElkanAlgorithm(weight, inner)` which will pass NormalisationOptions through

From tokenise.go:
- `func Tokenise(s string, opts TokeniseOptions) []string` — non-nil empty slice on empty input
- `func DefaultTokeniseOptions() TokeniseOptions`

From errors.go (Phase 1 — already declared):
- `var ErrInvalidAlgoID = errors.New("fuzzymatch: invalid AlgoID")` — used by Phase 8's Scorer option, NOT by direct calls (direct calls panic per CONTEXT §3 LOCKED)

From tversky.go (parameter-rich algorithm precedent):
- `func TverskyScore(a, b string, n int, alpha, beta float64) float64` — file-header layout for an algorithm with multiple parameters and an asymmetric variant (when α ≠ β); `props_test.go` `TestProp_TverskyScore_AsymmetricWhenAlphaNeqBeta` is the asymmetry property-test template

From dispatch_qgram_jaccard.go:
- `var _ = func() bool { dispatch[AlgoQGramJaccard] = func(a, b string) float64 { return QGramJaccardScore(a, b, 3) }; return true }()` — closure-with-default-param pattern; Monge-Elkan's dispatch wraps `MongeElkanScoreSymmetric(a, b, AlgoJaroWinkler, DefaultNormalisationOptions())` similarly

From all 14 permitted-inner-AlgoID dispatch files (already landed by Phase 5):
- `dispatch[AlgoLevenshtein]`, `dispatch[AlgoDamerauLevenshteinOSA]`, `dispatch[AlgoDamerauLevenshteinFull]`, `dispatch[AlgoHamming]`, `dispatch[AlgoJaro]`, `dispatch[AlgoJaroWinkler]`, `dispatch[AlgoStrcmp95]`, `dispatch[AlgoSmithWatermanGotoh]`, `dispatch[AlgoLCSStr]`, `dispatch[AlgoQGramJaccard]`, `dispatch[AlgoSorensenDice]`, `dispatch[AlgoCosine]`, `dispatch[AlgoTversky]`, `dispatch[AlgoRatcliffObershelp]` — all guaranteed non-nil at the point Monge-Elkan calls `dispatch[inner](tokA, tokB)`
</interfaces>

<recorded_resolutions>
Plan SUMMARY.md must record:

- **OQ-4 RESOLUTION LOCKED <date>:** `permittedMongeElkanInner` includes `AlgoRatcliffObershelp` (Phase 4 character-tier algorithm). Total 14 entries: 9 character-tier (`AlgoLevenshtein`, `AlgoDamerauLevenshteinOSA`, `AlgoDamerauLevenshteinFull`, `AlgoHamming`, `AlgoJaro`, `AlgoJaroWinkler`, `AlgoStrcmp95`, `AlgoSmithWatermanGotoh`, `AlgoLCSStr`) + 4 q-gram tier (`AlgoQGramJaccard`, `AlgoSorensenDice`, `AlgoCosine`, `AlgoTversky`) + AlgoRatcliffObershelp. CONTEXT §3's list of 13 omitted AlgoRatcliffObershelp; RESEARCH.md OQ-4 recommended including it because RatcliffObershelp is a character-tier algorithm that fits the Monge-Elkan inner-metric semantics. Excluding it would be arbitrary. The exhaustive panic-test asserts these 14 are permitted AND that the remaining 9 (`AlgoMongeElkan` self / 4 token-tier / 4 phonetic) panic with the documented message.
- **Permitted-inner allow-list LOCKED <date>:** Declared at PACKAGE SCOPE in `monge_elkan.go` (NOT in `init()` per DET-13 / Phase 5 §5 LOCKED) as `var permittedMongeElkanInner = map[AlgoID]bool{...}` literal with one line per entry, each with an inline comment citing the AlgoID's primary source (mirrors `tversky.go` permitted-AlgoID-style if any; otherwise the planner-defined comment style). The map is the single source of truth: when Phase 7 lands, planners ADD 4 entries (`AlgoSoundex`, `AlgoDoubleMetaphone`, `AlgoNYSIIS`, `AlgoMRA`) and update the panic-test fixture from 9 rejected → 5 rejected.
- **Direct-call panic format LOCKED <date>:** `panic("fuzzymatch: AlgoID " + inner.String() + " not permitted as Monge-Elkan inner metric")` — verbatim format including the canonical AlgoID name from `String()`. The Phase 8 Scorer option `WithMongeElkanAlgorithm(weight, inner)` will instead return `ErrInvalidAlgoID` (already declared in `errors.go`) — direct-call panic discipline per CONTEXT §3.
- **Asymmetric reference vector LOCKED <date>:** At least one hand-derived `(a, b, inner)` triple where `MongeElkanScore(a, b, inner, opts) ≠ MongeElkanScore(b, a, inner, opts)` — the asymmetry gate per RESEARCH.md §"Specific Ideas" "Monge-Elkan asymmetric test pair" + Pattern 7 derivation. Recommended pair: `("user create", "usr creating", AlgoJaroWinkler)` with the per-token-max derivation embedded in the test comment per RESEARCH.md §1b LOCKED template (`tokens(A) = ["user","create"]; tokens(B) = ["usr","creating"]; max_inner(user, *) = JW("user","usr") ≈ 0.875; max_inner(create, *) = JW("create","creating") ≈ 0.95; ME(A,B) = (0.875+0.95)/2 = 0.9125`).
- **Symmetric variant in standard property-set LOCKED <date>:** AlgoMongeElkan dispatch wraps the SYMMETRIC variant per CONTEXT §4, so `AlgoMongeElkan` participates in `PropAlgorithmScore_Symmetric` without exemption. The DIRECT-CALL `MongeElkanScore` (asymmetric) gets its own `TestProp_MongeElkanScore_AsymmetricWhenTokenCountAsymmetric` property test mirroring the Tversky α≠β template.
</recorded_resolutions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land MongeElkanScore + MongeElkanScoreSymmetric + permittedMongeElkanInner allow-list + dispatch wrapper + companions + property tests + 2 examples + BDD + staging golden + per-plan llms sync</name>
  <files>monge_elkan.go, monge_elkan_test.go, monge_elkan_bench_test.go, monge_elkan_fuzz_test.go, dispatch_monge_elkan.go, props_test.go, example_test.go, tests/bdd/features/monge_elkan.feature, tests/bdd/steps/algorithms_steps.go, testdata/golden/_staging/monge_elkan.json, llms.txt, llms-full.txt</files>
  <read_first>
    - tversky.go (parameter-rich algorithm file-header; asymmetric variant when α ≠ β; the closest analog for Monge-Elkan's parameter-rich + asymmetric-variant surface)
    - tversky_test.go (asymmetric reference vector pattern; panic test for invalid params)
    - dispatch_tversky.go (closure-with-default-params dispatch wiring — the file-level godoc rationale for binding defaults is the template Monge-Elkan's dispatch wrapper godoc mirrors)
    - dispatch_qgram_jaccard.go (closure-with-default-param dispatch wiring — simpler analog)
    - jarowinkler.go (the DEFAULT inner metric — read its public function signature `JaroWinklerScore(a, b string) float64` to confirm)
    - normalise.go (NormalisationOptions and DefaultNormalisationOptions — Monge-Elkan's signature accepts opts as a passthrough for Phase 8 Scorer compatibility, even though MongeElkanScore itself does NOT call Normalise)
    - tokenise.go DefaultTokeniseOptions
    - errors.go (`ErrInvalidAlgoID` — declared in Phase 1; Monge-Elkan's direct call panics, but Phase 8's Scorer option will return this error — the godoc cross-references)
    - algoid.go lines 1-310 (full AlgoID enum + dispatch[] declaration; the panic test walks all 23 AlgoIDs)
    - token_sort_ratio.go (Phase 6 algorithm file-header layout)
    - token_set_ratio.go (DoS godoc three-part block format precedent)
    - props_test.go LCSStr block (lines 1140-1280) AND Tversky asymmetric block (lines 2375-2434) — the asymmetric property-test template that Monge-Elkan's `TestProp_MongeElkanScore_AsymmetricWhenTokenCountAsymmetric` mirrors
    - example_test.go Tversky examples (search for `ExampleTverskyScore` lines 280-310) for the symmetric+asymmetric dual-example pattern
    - tests/bdd/features/qgram_jaccard.feature + token_sort_ratio.feature + token_set_ratio.feature (BDD scenario formats)
    - tests/bdd/steps/algorithms_steps.go lines 881-911 (Tversky parameter-rich step pattern with float params; Monge-Elkan uses the AlgoID enum string instead — implementer derives the regex)
    - testdata/golden/_staging/qgram_jaccard.json + tversky.json (staging-golden shape for parameter-rich algorithms)
    - llms.txt + llms-full.txt (per-plan sync format established in plans 06-01 / 06-02 / 06-03 / 06-04)
    - .planning/phases/06-token-based-algorithms/06-RESEARCH.md Pattern 7 + Example 5 (full Monge-Elkan asymmetric formula + symmetric variant + permitted-inner allow-list godoc + the verbatim Go body template for both functions)
    - .planning/phases/06-token-based-algorithms/06-RESEARCH.md Pitfall 4 (self-reference infinite-recursion gate; exhaustive panic test walking all 23 AlgoIDs)
    - .planning/phases/06-token-based-algorithms/06-CONTEXT.md §3 LOCKED (allow-list discipline; panic on direct-call invalid inner; Scorer returns ErrInvalidAlgoID)
    - .planning/phases/06-token-based-algorithms/06-CONTEXT.md §4 LOCKED (dispatch wrapper binds Symmetric + JaroWinkler default + DefaultNormalisationOptions)
    - .planning/phases/06-token-based-algorithms/06-CONTEXT.md §5 LOCKED (DoS godoc three-part block format)
    - .planning/phases/06-token-based-algorithms/06-CONTEXT.md §6 (per-plan llms.txt + llms-full.txt sync REINFORCED)
    - .claude/skills/algorithm-correctness-standards/SKILL.md (primary source: Monge & Elkan 1996 KDD'96 §3; mathematical invariants — symmetric variant participates in PropAlgorithmScore_Symmetric; asymmetric direct-call has its own asymmetry property test)
    - .claude/skills/determinism-standards/SKILL.md (no transcendental floats; left-to-right reduction with explicit parenthesisation; no goroutines)
    - .claude/skills/algorithm-licensing-standards/SKILL.md (Source-Origin Statement; Monge-Elkan is academic algorithm — no patent encumbrance, public-domain formula)
  </read_first>
  <behavior>
    - **Dispatch & defaults:**
      - `dispatch[AlgoMongeElkan]("user create", "usr creating") == MongeElkanScoreSymmetric("user create", "usr creating", AlgoJaroWinkler, DefaultNormalisationOptions())` (verified via DispatchInvokeForTest)
    - **Asymmetric variant (`MongeElkanScore`):**
      - `MongeElkanScore("alpha", "alpha", AlgoJaroWinkler, opts) == 1.0` (a == b identity short-circuit; Tokenise NOT invoked)
      - `MongeElkanScore("", "", AlgoJaroWinkler, opts) == 1.0` (both Tokenise outputs empty → both-empty identity convention — STANDARD catalogue convention)
      - `MongeElkanScore("hello", "", AlgoJaroWinkler, opts) == 0.0` (one-empty)
      - `MongeElkanScore("user create", "usr creating", AlgoJaroWinkler, opts)` returns the per-token-max-mean derived from JaroWinkler inner — exact value computed in test comment per RESEARCH.md §1b LOCKED template
      - **Asymmetry RV-ME-asym fixture:** at least one explicit pair where `MongeElkanScore(a, b, AlgoJaroWinkler, opts) ≠ MongeElkanScore(b, a, AlgoJaroWinkler, opts)` — the asymmetry gate; without it the directional-max could be silently averaged in the wrong direction
    - **Symmetric variant (`MongeElkanScoreSymmetric`):**
      - `MongeElkanScoreSymmetric(a, b, inner, opts) == (MongeElkanScore(a, b, inner, opts) + MongeElkanScore(b, a, inner, opts)) / 2.0` for any (a, b, inner, opts) — pinned by a property test
      - `MongeElkanScoreSymmetric(a, b, inner, opts) == MongeElkanScoreSymmetric(b, a, inner, opts)` for all inputs (truly symmetric — arithmetic mean of the two directions is order-independent)
    - **Allow-list panic discipline (RESEARCH.md Pitfall 4):**
      - `MongeElkanScore("a b", "c d", AlgoMongeElkan, opts)` panics with `"fuzzymatch: AlgoID MongeElkan not permitted as Monge-Elkan inner metric"` (self-reference)
      - `MongeElkanScore("a b", "c d", AlgoTokenSortRatio, opts)` panics (token-on-token meaningless)
      - `MongeElkanScore("a b", "c d", AlgoTokenSetRatio, opts)` panics
      - `MongeElkanScore("a b", "c d", AlgoPartialRatio, opts)` panics
      - `MongeElkanScore("a b", "c d", AlgoTokenJaccard, opts)` panics
      - `MongeElkanScore("a b", "c d", AlgoSoundex, opts)` panics (Phase 7 — not yet permitted)
      - `MongeElkanScore("a b", "c d", AlgoDoubleMetaphone, opts)` panics
      - `MongeElkanScore("a b", "c d", AlgoNYSIIS, opts)` panics
      - `MongeElkanScore("a b", "c d", AlgoMRA, opts)` panics
      - 14 permitted inners (9 char + 4 q-gram + AlgoRatcliffObershelp per OQ-4) all return a score in `[0, 1]` without panic — exhaustive panic-test walks all 23 AlgoIDs
    - **DoS-T3 bench:**
      - `BenchmarkMongeElkan_Pathological_1000Tokens` (1000-token both-sides input) lands and runs to completion (timing baseline lands in `bench.txt` after plan 06-06)
    - **Property-test set membership:** `AlgoMongeElkan` participates in `PropAlgorithmScore_Symmetric` (the dispatch wraps the Symmetric variant per CONTEXT §4 — no exemption needed); `MongeElkanScore` (direct-call asymmetric) has its own `TestProp_MongeElkanScore_AsymmetricWhenTokenCountAsymmetric` property test mirroring Tversky α≠β
  </behavior>
  <action>
    Create `monge_elkan.go` (Apache-2.0 header → block-comment with primary-source citation: Monge, A. E., Elkan, C. P. (1996). "The field matching problem: algorithms and applications." Proceedings of the Second International Conference on Knowledge Discovery and Data Mining: 267-270, §3 — fresh implementation from primary source, with the formula reproduction `ME(A, B, sim) = (1/|A|) · Σ_{i=1..|A|} max_{j=1..|B|} sim(A[i], B[j])` per RESEARCH.md Pattern 7; symmetric variant `ME_sym(A, B, sim) = (ME(A, B, sim) + ME(B, A, sim)) / 2.0` per CONTEXT §4 LOCKED → `Algorithm — MongeElkanScore / MongeElkanScoreSymmetric:` block describing tokenise → per-token-max → average; asymmetric variant produces direction-sensitive scores; symmetric variant averages both directions → `Conventions:` block (a == b → 1.0 identity; both-Tokenise-empty → 1.0; one-Tokenise-empty → 0.0) → `Inner-metric allow-list:` block enumerating the 14 permitted inner AlgoIDs with one bullet per entry citing the AlgoID's primary source (e.g. `AlgoLevenshtein` — Levenshtein 1965; ...; `AlgoRatcliffObershelp` — Ratcliff & Metzener 1988 per OQ-4 RESOLUTION LOCKED `<date>` referencing this plan SUMMARY); explicitly note the EXCLUSIONS: `AlgoMongeElkan` (self-recursion infinite loop per RESEARCH.md Pitfall 4); `AlgoTokenSortRatio` / `AlgoTokenSetRatio` / `AlgoPartialRatio` / `AlgoTokenJaccard` (token-on-token meaningless); Phase 7 phonetic algorithms (`AlgoSoundex`, `AlgoDoubleMetaphone`, `AlgoNYSIIS`, `AlgoMRA`) — Phase 7 will EXTEND the allow-list ADDITIVELY → `Source-origin discipline:` (Primary source: Monge & Elkan 1996 §3; Cross-validation: NONE — hand-derived RV-ME1..RV-ME6 reference vectors in monge_elkan_test.go per CONTEXT §1b LOCKED; the RapidFuzz cross-validation corpus does NOT include Monge-Elkan entries because RapidFuzz's ME default inner may not match ours; GPL/LGPL provenance: none; Code copied verbatim: none) → `Implementation discipline:` (NO init for the allow-list — declared at package scope per DET-13 / Phase 5 §5 LOCKED; allow-list gate at the TOP of MongeElkanScore — calls panic per CONTEXT §3 LOCKED; symmetric variant calls MongeElkanScore in both directions, so the symmetric path also surfaces the panic on invalid inner; identity short-circuit BEFORE Tokenise — saves Tokenise allocation on identical inputs; tokeniser-divergence note per OQ-1; left-to-right reduction with explicit parenthesisation per DET-06 — `sumOfMax / float64(len(tokensA))` for asymmetric; `(asymmetricAB + asymmetricBA) / 2.0` for symmetric — explicit parenthesisation; NO transcendentals; NO map iteration on output paths — both inner loops iterate the input slice which is already deterministically ordered) → DoS-vector three-part block per CONTEXT §5 LOCKED:
    ```
    // Complexity:
    //
    //   O(|A|·|B|·cost(inner))
    //
    //   where |A|, |B| are the post-tokenisation token counts and
    //   cost(inner) is the inner metric's per-comparison complexity
    //   (e.g. Jaro-Winkler is O(min(m,n)) per token comparison).
    //
    // DoS notice:
    //
    //   On inputs with > 1,000 tokens per side this performs ~10^6 inner-
    //   metric comparisons. With Jaro-Winkler inner (the default), each
    //   comparison is O(token_length) — total cost on 1000-token inputs
    //   approximates 10^7 character operations. In untrusted-input
    //   contexts (HTTP request body, file uploads, user-submitted
    //   identifiers), pre-validate token-count ceilings before calling.
    //   See BenchmarkMongeElkan_Pathological_1000Tokens for measured
    //   timings on 1000×1000-token inputs.
    ```
    → `package fuzzymatch`.

    Declare the allow-list at PACKAGE SCOPE (NOT in init() per DET-13):
    ```go
    // permittedMongeElkanInner enumerates the AlgoIDs valid as
    // Monge-Elkan inner metrics. Declared at package scope (per DET-13 /
    // Phase 5 §5 LOCKED — NO init()-time table builds). Phase 7 will
    // ADD AlgoSoundex / AlgoDoubleMetaphone / AlgoNYSIIS / AlgoMRA
    // additively (4 new entries → 18 total). The allow-list is the
    // single source of truth — when Phase 7 lands, planners ADD entries
    // here AND update the panic-test fixture in monge_elkan_test.go.
    var permittedMongeElkanInner = map[AlgoID]bool{
        // Character tier (9):
        AlgoLevenshtein:            true, // Levenshtein 1965
        AlgoDamerauLevenshteinOSA:  true, // Damerau 1964 OSA variant
        AlgoDamerauLevenshteinFull: true, // Lowrance-Wagner 1975
        AlgoHamming:                true, // Hamming 1950
        AlgoJaro:                   true, // Jaro 1989
        AlgoJaroWinkler:            true, // Winkler 1990 (DEFAULT inner)
        AlgoStrcmp95:               true, // Winkler 1994
        AlgoSmithWatermanGotoh:     true, // Smith-Waterman 1981 + Gotoh 1982
        AlgoLCSStr:                 true, // Wagner-Fischer 1974

        // Q-gram tier (4):
        AlgoQGramJaccard: true, // Ukkonen 1992 + Jaccard 1912
        AlgoSorensenDice: true, // Dice 1945 + Sørensen 1948
        AlgoCosine:       true, // Salton & McGill 1983
        AlgoTversky:      true, // Tversky 1977

        // Gestalt tier (1) — OQ-4 RESOLUTION LOCKED <date>:
        AlgoRatcliffObershelp: true, // Ratcliff & Metzener 1988 — character-tier per OQ-4

        // EXPLICITLY NOT permitted (verified by exhaustive panic test):
        // AlgoMongeElkan: self-recursion infinite loop (RESEARCH.md Pitfall 4)
        // AlgoTokenSortRatio / AlgoTokenSetRatio / AlgoPartialRatio / AlgoTokenJaccard:
        //   token-on-token meaningless (the inner metric receives single tokens
        //   from the outer Tokenise; re-tokenising single tokens is a no-op /
        //   identity-equivalent at best, recursive at worst)
        // Phase 7 phonetic (AlgoSoundex / AlgoDoubleMetaphone / AlgoNYSIIS /
        //   AlgoMRA): added in Phase 7 ADDITIVELY (4 new entries → 18 total)
    }
    ```

    `func MongeElkanScore(a, b string, inner AlgoID, opts NormalisationOptions) float64` body structure (mirrors RESEARCH.md Example 5 verbatim in structure, NOT in code):
    1. `if !permittedMongeElkanInner[inner] { panic("fuzzymatch: AlgoID " + inner.String() + " not permitted as Monge-Elkan inner metric") }` (allow-list gate — TOP of function per CONTEXT §3 LOCKED)
    2. `if a == b { return 1.0 }` (identity short-circuit BEFORE Tokenise)
    3. `tokensA := Tokenise(a, DefaultTokeniseOptions()); tokensB := Tokenise(b, DefaultTokeniseOptions())`
    4. `if len(tokensA) == 0 && len(tokensB) == 0 { return 1.0 }` (both-Tokenise-empty identity)
    5. `if len(tokensA) == 0 || len(tokensB) == 0 { return 0.0 }` (one-Tokenise-empty)
    6. `innerFn := dispatch[inner]` (safe — allow-list above guarantees inner is in permittedMongeElkanInner AND the dispatch slot is non-nil because all 14 algorithms have landed by the time Phase 6 plan 06-05 runs; the package-init order via `var _ = func() bool` registrations guarantees Monge-Elkan can call dispatch[inner] at any time after package load)
    7. `var sumOfMax float64` — accumulator for `Σ max_{j} inner(A[i], B[j])`
    8. Outer loop: `for _, tokA := range tokensA { var maxSim float64; for _, tokB := range tokensB { s := innerFn(tokA, tokB); if s > maxSim { maxSim = s } }; sumOfMax += maxSim }` (explicit if/else for max — NOT builtin `max`; left-to-right accumulation per DET-06)
    9. `return sumOfMax / float64(len(tokensA))` (single division; explicit float64() conversion)

    NOTE on `opts NormalisationOptions`: the parameter is currently unused in the function body — it's accepted for Phase 8 Scorer compatibility (`WithMongeElkanAlgorithm(weight, inner)` will pass NormalisationOptions through). Document this in the godoc explicitly: "The opts parameter is accepted for forward-compatibility with the Phase 8 Scorer option WithMongeElkanAlgorithm; MongeElkanScore itself does NOT call Normalise — Tokenise(s, DefaultTokeniseOptions()) lowercases via TokeniseOptions.Lowercase = true. Pre-Normalise inputs explicitly if you want NFC / diacritic stripping before tokenisation."

    `func MongeElkanScoreSymmetric(a, b string, inner AlgoID, opts NormalisationOptions) float64` body:
    `return (MongeElkanScore(a, b, inner, opts) + MongeElkanScore(b, a, inner, opts)) / 2.0` (explicit parenthesisation; the panic on invalid inner surfaces from the first MongeElkanScore call; the divide-by-2 on a sum is exact in IEEE-754 for finite values)

    Imports: `// no imports needed beyond the package` (or just remove the import block; everything is in the same `package fuzzymatch`).

    Create `dispatch_monge_elkan.go` mirroring `dispatch_tversky.go` rationale-rich godoc (cite CONTEXT §4 LOCKED — symmetric variant + JaroWinkler default + DefaultNormalisationOptions; reference Phase 8's `WithMongeElkanAlgorithm(weight, inner)` as the user-controllable surface):
    ```go
    var _ = func() bool {
        dispatch[AlgoMongeElkan] = func(a, b string) float64 {
            return MongeElkanScoreSymmetric(a, b, AlgoJaroWinkler, DefaultNormalisationOptions())
        }
        return true
    }()
    ```

    Create `monge_elkan_test.go` (`package fuzzymatch_test`, stdlib `testing` only):
    - Table-driven `TestMongeElkanScore` covering ≥ 6 hand-derived cases per CONTEXT §1b LOCKED (RV-ME1..RV-ME6) with the per-token-max derivation embedded in the test comment per the RESEARCH.md §1b template:
      - **RV-ME1**: `("user create", "usr creating", AlgoJaroWinkler) → exact value` — the canonical asymmetric example with full derivation: `tokens(A) = ["user","create"]; tokens(B) = ["usr","creating"]; max_inner(user, *) = JW("user","usr") ≈ 0.875; max_inner(create, *) = JW("create","creating") ≈ 0.95; ME(A,B) = (0.875+0.95)/2 ≈ 0.9125`
      - **RV-ME2**: identical token sets → 1.0 (e.g. `("alpha beta", "alpha beta", AlgoJaroWinkler) → 1.0`)
      - **RV-ME3**: disjoint token sets → low score (`("alpha beta", "gamma delta", AlgoJaroWinkler) → ?` — derivation in test comment)
      - **RV-ME4**: partially-overlapping with token-count asymmetry — e.g. `("alpha", "alpha beta gamma", AlgoLevenshtein) → ?` (forces |A| < |B| so direction matters)
      - **RV-ME5**: Unicode token (lowercase ASCII at corpus level — Tokenise lowercases) — e.g. `("café", "cafe", AlgoLevenshtein)` (or similar)
      - **RV-ME6 (asymmetry RV-ME-asym fixture LOCKED)**: pair `(a, b, inner)` where `MongeElkanScore(a, b, inner, opts) ≠ MongeElkanScore(b, a, inner, opts)` — KEYSTONE asymmetry gate. Pin both directions and assert inequality with a tolerance (e.g. `assert |MongeElkanScore(a, b, inner, opts) - MongeElkanScore(b, a, inner, opts)| > 0.01`). Recommended: `("alpha", "alpha beta gamma", AlgoLevenshtein)` — the directional max produces clearly different averages
    - `TestMongeElkanScoreSymmetric` covering: identity (`("alpha", "alpha", AlgoJaroWinkler) → 1.0`); explicit `MongeElkanScoreSymmetric(a, b, ...) == (MongeElkanScore(a, b, ...) + MongeElkanScore(b, a, ...)) / 2.0` pin; symmetry pin (`MongeElkanScoreSymmetric(a, b) == MongeElkanScoreSymmetric(b, a)` for the RV-ME-asym pair)
    - `TestMongeElkan_PanicsOnNonPermittedInner` (RESEARCH.md Pitfall 4 KEYSTONE — exhaustive panic test):
      - Build a `rejected []fuzzymatch.AlgoID` slice with all 9 NON-permitted AlgoIDs: `AlgoMongeElkan` (self), `AlgoTokenSortRatio`, `AlgoTokenSetRatio`, `AlgoPartialRatio`, `AlgoTokenJaccard`, `AlgoSoundex`, `AlgoDoubleMetaphone`, `AlgoNYSIIS`, `AlgoMRA`
      - For each, run a `t.Run(inner.String(), ...)` sub-test that wraps the call in `defer func() { if r := recover(); r == nil { t.Fatalf("MongeElkanScore(*, *, %s, opts) did not panic", inner) } }()` then calls `_ = fuzzymatch.MongeElkanScore("a b", "c d", inner, fuzzymatch.DefaultNormalisationOptions())`
      - Additionally: build a `permitted []fuzzymatch.AlgoID` slice with all 14 permitted AlgoIDs and assert each returns a value in `[0, 1]` without panic (representative subset of 4-5 per CONTEXT §6 Discretion would also be acceptable; recommended is the exhaustive subset for clarity)
    - `TestMongeElkan_PanicMessageFormat`: pin the exact panic message string format per CONTEXT §3 LOCKED — `"fuzzymatch: AlgoID MongeElkan not permitted as Monge-Elkan inner metric"` (replace MongeElkan with the actual AlgoID name from `String()`)
    - `TestMongeElkanScore_DispatchRegistration`: call `fuzzymatch.DispatchInvokeForTest(int(fuzzymatch.AlgoMongeElkan), "user create", "usr creating")` and assert it equals `fuzzymatch.MongeElkanScoreSymmetric("user create", "usr creating", fuzzymatch.AlgoJaroWinkler, fuzzymatch.DefaultNormalisationOptions())` (verifies the dispatch closure binds the LOCKED defaults correctly)

    Create `monge_elkan_bench_test.go`:
    - `BenchmarkMongeElkanScore_ASCII_Short` (10 chars, JaroWinkler inner) / `_ASCII_Medium` (50 chars) / `_ASCII_Long` (200 chars) / `_Unicode_Short`
    - `BenchmarkMongeElkanScoreSymmetric_ASCII_Short` (the symmetric variant — 2x cost since it calls the asymmetric in both directions; the bench number lets benchstat track the symmetric overhead)
    - `BenchmarkMongeElkan_Pathological_1000Tokens` per CONTEXT §5 LOCKED — fixture: 1000-token both-sides input. Construct via `manyA := strings.Repeat("alpha beta gamma delta ", 250); manyB := strings.Repeat("epsilon zeta eta theta ", 250)` (or similar — 1000 tokens per side; use `strings.Trim` to remove trailing space; the per-token-max iteration is the dominant cost). The bench calls `MongeElkanScoreSymmetric(manyA, manyB, AlgoJaroWinkler, DefaultNormalisationOptions())` — bench reports allocs and times
    - File-header godoc states the per-input-size budget per .claude/skills/performance-standards "Monge-Elkan: < 10 µs (dominated by inner-metric × token-count²)"; pathological budget is informational (not bounded — the DoS fixture is descriptive, not a regression gate)

    Create `monge_elkan_fuzz_test.go`:
    - TWO Fuzz targets — `FuzzMongeElkanScore` and `FuzzMongeElkanScoreSymmetric` — each with ≥ 8 programmatic `f.Add` seeds. The fuzz body must coerce the `inner AlgoID` parameter via a helper that walks the permitted-inner allow-list (mirrors `fuzzCoerceN` for q-grams) so the documented panic path is NOT exercised by the harness — the panic contract is unit-tested separately by `TestMongeElkan_PanicsOnNonPermittedInner`. Bodies assert no panic + score in `[0.0, 1.0]` + no NaN + no ±Inf

    Append to `props_test.go`:
    - `// --- Monge-Elkan property tests (plan 06-05)` divider
    - `TestProp_MongeElkanScore_RangeBounds` (asymmetric direct call; uses a fixed inner = AlgoJaroWinkler), `_NoNaN`, `_NoInf`, `_NoNegativeZero` — NO `_Symmetric` for the direct-call asymmetric variant (use the Tversky asymmetry-test pattern instead)
    - `TestProp_MongeElkanScore_AsymmetricWhenTokenCountAsymmetric` mirroring `TestProp_TverskyScore_AsymmetricWhenAlphaNeqBeta`: premise checks `len(tokensA) != len(tokensB)` AND inner-metric overlap; conclusion is `MongeElkanScore(A, B, inner, opts) != MongeElkanScore(B, A, inner, opts)` (with a small tolerance for floating-point closeness — the asymmetry holds in the limit but property tests should accept marginal edge cases without false-failure)
    - `TestProp_MongeElkanScoreSymmetric_RangeBounds`, `_Identity`, `_Symmetric`, `_NoNaN`, `_NoInf`, `_NoNegativeZero` — the symmetric variant participates in the standard symmetric set (no exemption per CONTEXT §4)
    - **AlgoMongeElkan participates in `TestProp_DispatchScore_Symmetric`** (or whatever the project's standard symmetric-set property test is — the dispatch wraps the symmetric variant per CONTEXT §4 so AlgoMongeElkan needs NO exemption from the symmetric-set membership). Verify by reading the existing `props_test.go` or `algoid_test.go` for the standard symmetric-set fixture and ensuring `AlgoMongeElkan` is included (or the planner adds it explicitly if the fixture is a per-AlgoID list)

    Append `ExampleMongeElkanScore` and `ExampleMongeElkanScoreSymmetric` to `example_test.go`. The asymmetric example should demonstrate the asymmetry inline (mirror `ExampleTverskyScore` with `// Symmetric case` + `// Asymmetric case` comment structure) — show the asymmetric pair returning different scores in the two directions, then the symmetric variant averaging them. Use `// Output:` block with the exact computed values.

    Create `tests/bdd/features/monge_elkan.feature` mirroring the q-gram feature structure with the asymmetric extension per CONTEXT §6:
    - Hashed-line header citing Monge & Elkan 1996 KDD'96 §3 as primary source
    - `Feature: Monge-Elkan (asymmetric per-token-max-mean with 14 permitted inner AlgoIDs + symmetric variant)`
    - `Scenario Outline: Canonical reference vectors (asymmetric, AlgoJaroWinkler inner)` with at least 4 Examples rows
    - `Scenario Outline: Canonical reference vectors (symmetric, AlgoJaroWinkler inner)` with the symmetric-variant outputs
    - `Scenario: identical strings score 1.0`
    - `Scenario: both-empty strings score 1.0`
    - `Scenario Outline: one-empty string scores 0.0`
    - `Scenario: MongeElkanScore is direction-sensitive` (asymmetry KEYSTONE — `When I compute MongeElkanScore("alpha", "alpha beta gamma", AlgoLevenshtein) ... Then the score differs from MongeElkanScore("alpha beta gamma", "alpha", AlgoLevenshtein)`)
    - `Scenario: MongeElkanScoreSymmetric is symmetric` (symmetric variant)
    - `Scenario: dispatch wraps the symmetric variant with AlgoJaroWinkler default inner` (verify dispatch behaviour)
    - `Scenario Outline: non-permitted inner AlgoIDs panic` with 9 Examples rows (the 9 rejected AlgoIDs)
    - Tag every scenario `@token`

    Append step methods to `tests/bdd/steps/algorithms_steps.go` (after the TokenJaccard block from plan 06-04). Step methods include the inner AlgoID parameter — parameter-rich pattern mirrors Tversky's `algorithms_steps.go:892-905` step methods with float params, but ME uses the AlgoID enum string. Recommended step regex format: `^I compute the MongeElkan score between "([^"]*)" and "([^"]*)" with inner Algo([A-Za-z0-9]+)$` — the step body resolves the inner AlgoID by switching on the matched name (e.g. `case "JaroWinkler": innerID = fuzzymatch.AlgoJaroWinkler`). Implementer chooses the exact regex but must support all 14 permitted AlgoIDs. Add matching `InitializeScenario` registrations.

    Create `testdata/golden/_staging/monge_elkan.json` with 8-12 entries in the canonical shape covering the symmetric variant (since dispatch wraps symmetric — the golden file's `algorithm: "MongeElkan"` invokes the symmetric path through dispatch). Cover identity, both-empty, one-empty, partial-overlap, asymmetric pair (the symmetric average), Unicode-safe.

    Update `llms.txt` — append a new section after the TokenJaccard section (added in plan 06-04):
    ```
    ### Monge-Elkan (Monge & Elkan 1996) per-token-max with pluggable inner metric

    - func MongeElkanScore(a, b string, inner AlgoID, opts NormalisationOptions) float64
    - func MongeElkanScoreSymmetric(a, b string, inner AlgoID, opts NormalisationOptions) float64
    ```

    Update `llms-full.txt` — append a new `### Phase 6 algorithm surface (token tier — Monge-Elkan)` block describing the asymmetric formula + symmetric variant + the 14-entry allow-list + DoS notice + Phase 7 forward-compatibility note (the allow-list will additively grow to 18 entries when phonetic algorithms land); include `go` snippets for both functions with their godoc; reference vectors inline (e.g. `MongeElkanScore("user create", "usr creating", AlgoJaroWinkler, DefaultNormalisationOptions()) ≈ 0.9125`); explicit panic-discipline note.
  </action>
  <verify>
    <automated>go test -run 'TestMongeElkan|TestProp_MongeElkan' -race -shuffle=on -count=1 ./... && go test -run TestLLMsTxt ./... && cd tests/bdd && go test -race -count=1 ./... && go test -run none -bench BenchmarkMongeElkan -benchtime=1x ./..</automated>
  </verify>
  <done>
    MongeElkanScore + MongeElkanScoreSymmetric are exported, dispatch[AlgoMongeElkan] is registered with the symmetric variant + AlgoJaroWinkler default inner + DefaultNormalisationOptions per CONTEXT §4 LOCKED (verified via TestMongeElkanScore_DispatchRegistration), all tests + property tests + BDD + fuzz + bench compile-and-run; the exhaustive panic-test walks all 9 rejected AlgoIDs AND a representative-subset of 4-5 permitted AlgoIDs; the asymmetry RV-ME-asym fixture is pinned in unit tests AND BDD AND staging-golden (the symmetric variant's golden file is what's committed; asymmetric direct-call test is unit-only); the three-part DoS godoc block + Pathological_1000Tokens bench fixture per CONTEXT §5 LOCKED; permittedMongeElkanInner is declared at PACKAGE SCOPE (not init) with 14 entries per OQ-4 RESOLUTION LOCKED; per-plan llms.txt + llms-full.txt sync verified; AlgoMongeElkan participates in the standard symmetric property-test set without exemption.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 6 algorithms are pure functions accepting opaque strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | D (algorithmic-complexity DoS — DoS-T3 per VALIDATION.md) | MongeElkanScore on 1000-token vs 1000-token inputs | mitigate | Three-part DoS godoc block (Complexity formula + DoS notice paragraph naming the 10^6 inner-metric-comparison cost + dedicated `BenchmarkMongeElkan_Pathological_1000Tokens` fixture per CONTEXT §5 LOCKED). Bench numbers commit to `bench.txt` after plan 06-06; benchstat regression detection > 10% fails CI. Consumers in untrusted-input contexts must pre-validate token-count ceilings — documented in the godoc DoS notice. |
| T-06-05-02 | E (elevation of privilege — wrong inner metric leading to incorrect security-sensitive scores) | MongeElkanScore with malformed inner AlgoID | mitigate | Strict 14-entry `permittedMongeElkanInner` allow-list at PACKAGE SCOPE per CONTEXT §3 LOCKED + DET-13. Direct calls panic with the documented message on any non-permitted AlgoID (including `AlgoMongeElkan` self-reference per RESEARCH.md Pitfall 4 — prevents stack overflow). Phase 8's `WithMongeElkanAlgorithm(weight, inner)` returns `ErrInvalidAlgoID` for the same input (already declared in `errors.go` Phase 1). The exhaustive panic test walks all 9 rejected AlgoIDs AND 4-5 representative permitted ones — a regression that loosens the allow-list surfaces as 9 separate test failures. |
| T-06-05-03 | T (tampering — silent direction averaging of asymmetric ME) | MongeElkanScore returning the wrong direction | mitigate | KEYSTONE asymmetry gate RV-ME-asym fixture pinned in (a) `TestMongeElkanScore` (RV-ME6 — explicit `(a, b, inner)` triple where the two directions produce inequal scores with a tolerance), (b) `TestProp_MongeElkanScore_AsymmetricWhenTokenCountAsymmetric` property test, (c) BDD scenario "MongeElkanScore is direction-sensitive". A regression that silently averages or swaps directions surfaces as 3 separate test failures. The symmetric variant `MongeElkanScoreSymmetric` has its own `TestProp_MongeElkanScoreSymmetric_Symmetric` property test confirming the arithmetic-mean construction is order-independent. |
</threat_model>

<verification>
- `go test -run 'TestMongeElkan|TestProp_MongeElkan' -race -shuffle=on -count=1 ./...` exits 0
- `go test -run TestLLMsTxt ./...` exits 0
- `cd tests/bdd && go test -race -count=1 ./...` exits 0
- `make fmt-check && make vet && make lint && make verify-license-headers && make verify-deps-allowlist` exit 0
- `go test -run none -bench BenchmarkMongeElkan -benchtime=1x ./...` runs to completion (numbers commit in plan 06-06)
- The exhaustive panic test exits 0 on all 9 non-permitted AlgoIDs (each triggers the documented `panic` message and recovers via `defer recover()`)
</verification>

<success_criteria>
- TOKEN-01 closed: MongeElkanScore + MongeElkanScoreSymmetric ship with the LOCKED 14-entry allow-list (9 character + 4 q-gram + AlgoRatcliffObershelp per OQ-4); reference vectors match Monge-Elkan 1996; dispatch wraps the symmetric variant with AlgoJaroWinkler default inner per CONTEXT §4
- 6 hand-derived reference vectors RV-ME1..RV-ME6 with formula derivation in test comments per CONTEXT §1b LOCKED
- Direct-call panic discipline + exhaustive panic test walking all 9 rejected AlgoIDs (plus 4-5 representative permitted) per RESEARCH.md Pitfall 4 + CONTEXT §3 LOCKED
- DoS-vector three-part godoc block + Pathological_1000Tokens bench fixture per CONTEXT §5 LOCKED
- AlgoMongeElkan participates in the standard symmetric property-test set without exemption (dispatch wraps Symmetric)
- Direct-call asymmetric `MongeElkanScore` has its own asymmetry property test mirroring Tversky α≠β
- KEYSTONE asymmetry gate pinned in unit test + BDD + property test (3 surfaces)
- `permittedMongeElkanInner` declared at PACKAGE SCOPE (NOT in init) per DET-13
- OQ-4 RESOLUTION LOCKED recorded in plan SUMMARY (AlgoRatcliffObershelp INCLUDED — 14 entries total)
- Per-plan llms.txt + llms-full.txt sync verified
- Coverage floors maintained: ≥ 95% overall, ≥ 90% per file, 100% on public API surface
</success_criteria>

<output>
After completion, create `.planning/phases/06-token-based-algorithms/06-05-SUMMARY.md` documenting:
- File-by-file changes
- The five `LOCKED <date>` records (OQ-4 RESOLUTION / Permitted-inner allow-list / Direct-call panic format / Asymmetric reference vector / Symmetric variant in standard property-set)
- The 6 RV-ME1..RV-ME6 reference vectors with their per-token-max derivations
- The exhaustive panic test fixture (9 rejected AlgoIDs)
- Reference vector numbers for the staging-golden file
- Pathological-bench timing on developer hardware (informational; baseline lands in plan 06-06)
- Phase 7 forward-compatibility note: when Phase 7 lands, planners ADD AlgoSoundex / AlgoDoubleMetaphone / AlgoNYSIIS / AlgoMRA to the allow-list AND update the panic-test fixture (rejected: 9 → 5; permitted: 14 → 18)
</output>