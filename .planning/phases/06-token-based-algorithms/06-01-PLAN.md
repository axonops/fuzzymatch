---
phase: 06-token-based-algorithms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - token_indel.go
  - token_indel_test.go
  - export_test.go
  - token_sort_ratio.go
  - token_sort_ratio_test.go
  - token_sort_ratio_bench_test.go
  - token_sort_ratio_fuzz_test.go
  - dispatch_token_sort_ratio.go
  - token_ratio_cross_validation_test.go
  - testdata/cross-validation/token-ratios/vectors.json
  - testdata/golden/_staging/token_sort_ratio.json
  - scripts/gen-token-ratio-cross-validation.py
  - tests/bdd/features/token_sort_ratio.feature
  - tests/bdd/steps/algorithms_steps.go
  - props_test.go
  - example_test.go
  - llms.txt
  - llms-full.txt
  - Makefile
  - docs/cross-validation.md
autonomous: true
requirements: [TOKEN-02]
must_haves:
  truths:
    - "User can call TokenSortRatioScore(a, b) and receive a deterministic float64 in [0.0, 1.0]"
    - "User can run `go test -run TestTokenRatios_CrossValidation -race ./...` and observe byte-stable agreement with the committed RapidFuzz reference corpus on TokenSortRatio entries"
    - "User can run `make regen-token-ratio-cross-validation` (with rapidfuzz==3.14.5 installed) and the script regenerates vectors.json byte-identically"
    - "Calling TokenSortRatioScore(s, s) on identical inputs returns 1.0 without invoking Tokenise (identity short-circuit)"
    - "lcsLen('abc','axc') == 2 (subsequence) AND LongestCommonSubstring('abc','axc') has length 1 (substring) — kernel divergence pinned in regression test"
  artifacts:
    - path: "token_indel.go"
      provides: "Unexported lcsLen / indelRatio / lcsLenRunes / indelRatioRunes shared kernel for the three Indel-based ratios"
      contains: "func lcsLen"
    - path: "token_indel_test.go"
      provides: "Kernel-level unit + invariant + lcsstr-divergence tests via export_test.go re-exports"
    - path: "token_sort_ratio.go"
      provides: "TokenSortRatioScore(a, b string) float64 — sorted-join → indelRatio composition"
      contains: "func TokenSortRatioScore"
    - path: "dispatch_token_sort_ratio.go"
      provides: "Direct registration into dispatch[AlgoTokenSortRatio]"
    - path: "token_ratio_cross_validation_test.go"
      provides: "TestTokenRatios_CrossValidation loader asserting per-entry byte-stable agreement with the committed RapidFuzz corpus"
    - path: "scripts/gen-token-ratio-cross-validation.py"
      provides: "Pinned-RapidFuzz Python generator for the four-score-per-entry corpus"
    - path: "testdata/cross-validation/token-ratios/vectors.json"
      provides: "Committed RapidFuzz reference corpus (≥ 20 entries spanning identity / both-empty / one-empty / token-reordered / subset / partial-substring / Unicode / TokenSet-three-way-max edge / pathological-length pair)"
    - path: "testdata/golden/_staging/token_sort_ratio.json"
      provides: "8-12 staging entries to be merged into algorithms.json by plan 06-06"
    - path: "tests/bdd/features/token_sort_ratio.feature"
      provides: "Godog scenarios — canonical reference vectors, identity, both-empty, one-empty, symmetry, sorted-token equivalence"
    - path: "Makefile"
      provides: "regen-token-ratio-cross-validation target + .PHONY entry"
      contains: "regen-token-ratio-cross-validation"
    - path: "docs/cross-validation.md"
      provides: "Contributor-facing note documenting the RapidFuzz version pin, lowercasing reconciliation, and OQ-1 tokeniser-divergence scope (corpus restricted to whitespace-only lowercase ASCII inputs)"
  key_links:
    - from: "token_sort_ratio.go"
      to: "token_indel.go"
      via: "indelRatio call after Tokenise → sort.Strings → strings.Join"
      pattern: "indelRatio\\(\\[\\]byte\\(joinedA\\)"
    - from: "dispatch_token_sort_ratio.go"
      to: "dispatch[AlgoTokenSortRatio]"
      via: "var _ = func() bool { dispatch[AlgoTokenSortRatio] = TokenSortRatioScore; return true }()"
      pattern: "dispatch\\[AlgoTokenSortRatio\\] = TokenSortRatioScore"
    - from: "token_ratio_cross_validation_test.go"
      to: "testdata/cross-validation/token-ratios/vectors.json"
      via: "os.ReadFile + json.Unmarshal + per-entry t.Run for token_sort sub-test (TokenSet/PartialRatio entries are LOADED but their assertions are gated on plans 06-02/06-03 landing — Wave-1 loader asserts only TokenSortRatio entries; later waves extend the loop)"
      pattern: "testdata/cross-validation/token-ratios/vectors.json"
    - from: "scripts/gen-token-ratio-cross-validation.py"
      to: "rapidfuzz.fuzz.token_sort_ratio"
      via: "pinned-version assert + .lower() reconciliation per OQ-1"
      pattern: "RAPIDFUZZ_VERSION = \"3.14.5\""
    - from: "export_test.go"
      to: "lcsLen / indelRatio / lcsLenRunes / indelRatioRunes"
      via: "var LCSLenForTest = lcsLen ; var IndelRatioForTest = indelRatio ; ..."
      pattern: "LCSLenForTest"
---

<objective>
Land the shared `token_indel.go` foundation kernel (Wagner-Fischer 1974 LCS-subsequence DP, two-row optimisation, stack-allocated `[64]int` buffer when `min(m,n) ≤ 50`), the simplest Indel-consumer (TokenSortRatio), and the entire RapidFuzz cross-validation infrastructure (Python generator, pinned `rapidfuzz==3.14.5`, committed `vectors.json` carrying all four scores per entry per OQ-2/OQ-3, Makefile target, Go loader test). Closes TOKEN-02. Records OQ-1, OQ-2, OQ-3, RapidFuzz version pin, and stack-buffer threshold resolutions in plan SUMMARY.

Purpose: this plan establishes the load-bearing foundation for Wave 2 (TokenSet, PartialRatio, TokenJaccard) and Wave 3 (Monge-Elkan). The shared kernel + cross-validation infrastructure must land FIRST so subsequent algorithms compose against a single source of LCS truth and so their cross-validation entries are pre-seeded in the corpus from day one.

Output: 9 new `.go` files (token_indel + token_sort_ratio + dispatch + cross-validation loader + companions), 1 new Python script, 1 new corpus file, 1 new BDD feature, 1 new staging golden, 1 new docs page (`docs/cross-validation.md`); modifications to export_test.go (4 re-exports), Makefile (target + .PHONY), props_test.go (TokenSortRatio property block), example_test.go (1 example), tests/bdd/steps/algorithms_steps.go (TokenSortRatio steps), llms.txt (1 new section), llms-full.txt (1 new Phase 6 algorithm-surface block).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-token-based-algorithms/06-CONTEXT.md
@.planning/phases/06-token-based-algorithms/06-RESEARCH.md
@.planning/phases/06-token-based-algorithms/06-PATTERNS.md
@.planning/phases/06-token-based-algorithms/06-VALIDATION.md
@.claude/skills/algorithm-correctness-standards/SKILL.md
@.claude/skills/algorithm-licensing-standards/SKILL.md
@.claude/skills/performance-standards/SKILL.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/go-testing-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md
@q_gram.go
@qgram_jaccard.go
@dispatch_lcsstr.go
@dispatch_qgram_jaccard.go
@lcsstr.go
@levenshtein.go
@tokenise.go
@algoid.go
@errors.go
@export_test.go
@props_test.go
@example_test.go
@llms.txt
@llms-full.txt
@Makefile
@scripts/gen-ratcliff-obershelp-cross-validation.py
@ratcliff_obershelp_test.go
@algorithms_golden_test.go
@testdata/golden/_staging/qgram_jaccard.json
@tests/bdd/features/qgram_jaccard.feature
@tests/bdd/steps/algorithms_steps.go

<interfaces>
<!-- Key types and contracts the executor needs. Extracted directly from the codebase so no exploration is required. -->

From algoid.go (lines 129-139, 280-308):
- `type AlgoID int` with reserved slots `AlgoMongeElkan`, `AlgoTokenSortRatio`, `AlgoTokenSetRatio`, `AlgoPartialRatio`, `AlgoTokenJaccard` (lines 129-154 — DO NOT renumber)
- `func (id AlgoID) String() string` — already returns `"TokenSortRatio"` etc.
- `var dispatch [numAlgorithms]func(a, b string) float64` — package-private dispatch table; the unique writer for each slot is `dispatch_<algo>.go`

From tokenise.go (lines 105-112, 143-200):
- `type TokeniseOptions struct { Lowercase bool; SplitCamelCase bool; SplitConsecutiveUpper bool; SeparatorChars string }`
- `func DefaultTokeniseOptions() TokeniseOptions` — returns `Lowercase: true, SplitCamelCase: true, SplitConsecutiveUpper: true, SeparatorChars: "_-.:/ \t\n\r"`
- `func Tokenise(s string, opts TokeniseOptions) []string` — non-nil empty slice on empty input

From normalise.go (DefaultNormalisationOptions):
- `type NormalisationOptions struct { Lowercase, StripSeparators, SplitCamelCase, NFC, StripDiacritics bool; SeparatorChars string }`
- `func DefaultNormalisationOptions() NormalisationOptions`

From levenshtein.go (line 57-60):
- `const maxStackInputLen = 64` — DO NOT redeclare; reference from token_indel.go

From normalise.go:
- `func isASCII(s string) bool` — DO NOT redeclare; reference from token_indel.go via `isASCIIBytes(b []byte) bool` if needed (or call a fresh byte-aware helper inside token_indel.go — implementer's choice but document it)

From q_gram.go:
- `func extractQGrams(s string, n int) map[string]int` — UNRELATED to LCS but the file-header layout is the canonical template

From lcsstr.go:
- `func LongestCommonSubstring(a, b string) string` — DISTINCT from `lcsLen`; the regression test in `token_indel_test.go` MUST pin the divergence (`lcsLen("abc","axc")==2` vs `LongestCommonSubstring("abc","axc")` of length 1)

From export_test.go (lines 115-129):
- `var ExtractQGramsForTest = extractQGrams` — pattern for re-exporting unexported helpers

From qgram_jaccard.go (lines 144-156):
- The identity-short-circuit-then-empty-guard pattern: `if a == b { return 1.0 }` then `if a == "" || b == "" { return 0.0 }`
</interfaces>

<recorded_resolutions>
Plan SUMMARY.md must record (per CONTEXT.md §6 "Established Patterns" `OQ-N RESOLUTION LOCKED <date>` discipline):

- **OQ-1 RESOLUTION LOCKED <date>:** Tokeniser-divergence handled by (a) restricting the cross-validation corpus to whitespace-only lowercase ASCII inputs, (b) the generator script calls `.lower()` on every input before passing to RapidFuzz, (c) `docs/cross-validation.md` documents the divergence prominently. The fuzzymatch TokenSortRatio godoc explicitly notes that the algorithm uses `Tokenise(s, DefaultTokeniseOptions())` (camelCase-aware) — for inputs without identifier-style boundaries the behaviour matches RapidFuzz; for inputs with mixed identifier styles the project's tokenisation produces semantically richer splits.
- **OQ-2 RESOLUTION LOCKED <date>:** Single combined `vectors.json` file with all four scores per entry (`token_sort_ratio`, `token_set_ratio`, `partial_ratio_bytes`, `partial_ratio_runes`). Per-entry sub-tests via `t.Run(e.Name+"/<surface>", ...)`. Mirrors Phase 3/4 structure. The Wave-1 loader asserts only TokenSortRatio entries; plans 06-02/06-03 extend the loop to assert TokenSetRatio / PartialRatio surfaces as those algorithms land.
- **OQ-3 RESOLUTION LOCKED <date>:** `partial_ratio_runes` is always included in every entry — for ASCII inputs the value matches `partial_ratio_bytes` (the rune path's separate code path is exercised even on ASCII to catch path-specific regressions).
- **RapidFuzz version pin LOCKED <date>:** `RAPIDFUZZ_VERSION = "3.14.5"` (current stable per PyPI registry as of 2026-04-07; verify before committing the script header). Recorded in script header AND in vectors.json `_metadata.rapidfuzz_version` AND in `docs/cross-validation.md`.
- **Stack-buffer threshold LOCKED <date>:** `var prevArr, currArr [65]int` with the stack path active when `min(m, n) <= 64` (mirrors `levenshtein.go`'s `maxStackInputLen` = 64; CONTEXT §2's `[64]int` at `min(m,n) ≤ 50` is an inherited recommendation that the planner finalises at 64/64 to share `maxStackInputLen` with the Phase 2 budget). Heap fallback `make([]int, m+1)` for longer inputs.
</recorded_resolutions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land token_indel.go shared kernel + export_test.go re-exports + token_indel_test.go (kernel + lcsstr-divergence regression)</name>
  <files>token_indel.go, token_indel_test.go, export_test.go</files>
  <read_first>
    - q_gram.go (file-header layout, unexported-helper-with-export_test re-export pattern, Source-Origin Statement block, DET-03 / DET-06 godoc reaffirmation, allocation-budget block)
    - q_gram_test.go (kernel-only test pattern via re-exports; stdlib `testing` only with `package fuzzymatch_test`)
    - lcsstr.go lines 100-300 (substring DP — DISTINCT from subsequence DP; pin the divergence in the regression test)
    - levenshtein.go lines 1-60 (file-header layout, `maxStackInputLen` constant location, two-row DP discipline)
    - export_test.go lines 115-129 (`var ExtractQGramsForTest = extractQGrams` re-export pattern + plan-introducer / consumer-plan godoc note)
    - .claude/skills/algorithm-correctness-standards/SKILL.md (primary-source citation block, formula documentation, fresh-implementation discipline)
    - .claude/skills/algorithm-licensing-standards/SKILL.md (Source-Origin Statement format)
    - .claude/skills/determinism-standards/SKILL.md (no map iteration on output paths, no transcendental floats — DET-06 limits to +,-,*,/, math.Sqrt/Abs/Min/Max only)
    - .claude/skills/performance-standards/SKILL.md (two-row DP, stack-allocated buffer pattern, b.ReportAllocs discipline)
    - .planning/phases/06-token-based-algorithms/06-CONTEXT.md §2 (LOCKED foundation file decision)
    - .planning/phases/06-token-based-algorithms/06-RESEARCH.md Pattern 2 + Example 1 (Wagner-Fischer DP recurrence and concrete `lcsLen` body)
    - .planning/phases/06-token-based-algorithms/06-RESEARCH.md Pitfall 6 (lcsLen vs LongestCommonSubstring divergence — regression test mandatory)
  </read_first>
  <behavior>
    - `lcsLen([]byte("abc"), []byte("axc")) == 2` (subsequence "ac")
    - `lcsLen([]byte("abc"), []byte("abc")) == 3` (identity)
    - `lcsLen([]byte(""), []byte("abc")) == 0`
    - `lcsLen([]byte(""), []byte("")) == 0`
    - `lcsLen([]byte("AGCT"), []byte("AGCTAGCT")) == 4` (subsequence "AGCT")
    - `indelRatio([]byte(""), []byte("")) == 1.0` (both-empty identity convention)
    - `indelRatio([]byte("abc"), []byte("")) == 0.0` (one-empty)
    - `indelRatio([]byte(""), []byte("abc")) == 0.0` (one-empty)
    - `indelRatio([]byte("abc"), []byte("abc")) == 1.0` (identity)
    - `indelRatio([]byte("abc"), []byte("axc")) == 2.0*2.0/(3.0+3.0) == 0.6666666666666666`
    - `lcsLenRunes([]rune("café"), []rune("cafe")) == 3` (subsequence "caf")
    - `indelRatioRunes` honours the same edge-case conventions on rune slices
    - Regression test pins lcsLen('abc','axc') == 2 AND `len(LongestCommonSubstring("abc","axc")) == 1` (Pitfall 6 divergence gate)
    - Allocation budget: `lcsLen` on `min(m,n) <= 64` performs zero heap allocations (verified by `b.ReportAllocs()` in plan 06-01-03's bench task); on longer inputs ≤ 2 heap allocations (one per DP row, both reused via swap)
  </behavior>
  <action>
    Create `token_indel.go` declaring four unexported helpers (no public surface in v1 — see CONTEXT.md "Deferred Ideas"):

    Signatures (Claude's discretion per CONTEXT §6 — recommended):
    - `func lcsLen(a, b []byte) int`
    - `func indelRatio(a, b []byte) float64`
    - `func lcsLenRunes(a, b []rune) int`
    - `func indelRatioRunes(a, b []rune) float64`

    File-header layout copies the q_gram.go template verbatim in structure (Apache-2.0 header → `Source:` block citing Wagner & Fischer 1974 J. ACM 21(1):168-173 → `Algorithm — lcsLen(a, b):` block reproducing the LCS-subsequence recurrence (NOT the substring recurrence — the godoc must EXPLICITLY contrast with `lcsstr.go`'s substring DP per Pitfall 6) → `Determinism (DET-03 + DET-06):` block → `Source-origin discipline:` block (primary source Wagner-Fischer 1974; cross-validation RapidFuzz 3.14.5 via plan 06-01-04; no GPL/LGPL provenance; no code copied verbatim) → `Implementation discipline:` block (no init, no map iteration, integer arithmetic + one final division; explicit if/else max NOT builtin `max`; `maxStackInputLen` referenced from levenshtein.go — DO NOT redeclare) → `Allocation budget:` block (0 alloc when `min(m,n) <= 64`; ≤ 2 allocs on longer inputs) → `package fuzzymatch`.

    Implementation discipline:
    - Two-row DP with the inner loop over the SHORTER side (always swap so `m <= n` before allocating; matches RESEARCH.md Example 1)
    - Stack-allocated `var prevArr, currArr [65]int` when `m <= maxStackInputLen` (= 64 per Phase 2); slice into `prevArr[:m+1]` and `currArr[:m+1]`
    - Heap fallback: `prev := make([]int, m+1); curr := make([]int, m+1)` when `m > 64`
    - Inner-loop body uses explicit `if prev[i] > curr[i-1] { curr[i] = prev[i] } else { curr[i] = curr[i-1] }` — NOT builtin `max` (per qgram_jaccard.go canonical pattern lines 240-246, for determinism-reviewer auditability)
    - `indelRatio` body: `if sum := len(a)+len(b); sum == 0 { return 1.0 }; if len(a) == 0 || len(b) == 0 { return 0.0 }; lcs := lcsLen(a, b); return 2.0 * float64(lcs) / float64(sum)` — explicit left-to-right parenthesisation per DET-06 (per lcsstr.go LCSStrScore lines 213-216)

    Append four `var <Name>ForTest = <unexported>` re-exports to `export_test.go` (after the Phase 5 q-gram block at lines 115-129): `LCSLenForTest`, `IndelRatioForTest`, `LCSLenRunesForTest`, `IndelRatioRunesForTest`. Each gets a godoc paragraph naming plan 06-01 as the introducing plan and plans 06-02 / 06-03 / 06-05 as future consumers.

    Create `token_indel_test.go` (`package fuzzymatch_test`, stdlib `testing` only — NO testify per .claude/skills/go-coding-standards) with:
    - Table-driven `TestLCSLen` covering identity / both-empty / one-empty / Wagner-Fischer §3 worked example ("AGCT"/"AGCTAGCT" → 4) / asymmetric-length pairs / the Pitfall 6 fixture ("abc"/"axc" → 2)
    - Table-driven `TestIndelRatio` covering identity → 1.0 / both-empty → 1.0 / one-empty → 0.0 / "abc"/"axc" → 0.6666666666666666
    - `TestLCSLenRunes` and `TestIndelRatioRunes` covering "café"/"cafe" → lcs 3, ratio 0.75
    - `TestLCSLen_DistinctFromLCSStr` (RESEARCH.md Pitfall 6 — keystone gate): asserts `LCSLenForTest([]byte("abc"), []byte("axc")) == 2` AND `len(LongestCommonSubstring("abc", "axc")) == 1` in the SAME test function so the divergence cannot regress silently

    Float comparisons use `math.Abs(got-want) > 1e-12` (per .claude/skills/go-testing-standards "Float comparisons use a small tolerance, never `==`"). All test cases use `t.Run(name, ...)` for per-case isolation per `q_gram_test.go` precedent.
  </action>
  <verify>
    <automated>go test -run 'TestLCSLen|TestIndelRatio|TestLCSLenRunes|TestIndelRatioRunes|TestLCSLen_DistinctFromLCSStr' -race -shuffle=on -count=1 ./...</automated>
  </verify>
  <done>
    token_indel.go compiles, declares the four unexported helpers with the canonical file-header layout including primary-source citation + Source-Origin Statement + DET-03/DET-06 + alloc budget; export_test.go has the four `*ForTest` re-exports; token_indel_test.go's seven tests all pass; the Pitfall-6 regression test (`TestLCSLen_DistinctFromLCSStr`) is part of the suite.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land TokenSortRatio (algorithm + dispatch + companions + property tests + example + BDD + staging golden + per-plan llms sync)</name>
  <files>token_sort_ratio.go, token_sort_ratio_test.go, token_sort_ratio_bench_test.go, token_sort_ratio_fuzz_test.go, dispatch_token_sort_ratio.go, props_test.go, example_test.go, tests/bdd/features/token_sort_ratio.feature, tests/bdd/steps/algorithms_steps.go, testdata/golden/_staging/token_sort_ratio.json, llms.txt, llms-full.txt</files>
  <read_first>
    - qgram_jaccard.go (file structure, identity short-circuit, Source-Origin Statement block, dispatched-byte-path-only convention)
    - qgram_jaccard_test.go (table-driven canonical reference vectors, Phase-2/3/4/5 idiom, stdlib `testing` only)
    - qgram_jaccard_bench_test.go (ASCII Short/Medium/Long + Unicode Short benchmarks, sink+gate pattern, b.ReportAllocs)
    - qgram_jaccard_fuzz_test.go (one Fuzz target, programmatic f.Add seeds, range/no-NaN/no-Inf assertions)
    - dispatch_lcsstr.go (no-params direct dispatch registration via `var _ = func() bool { ... }()`)
    - tokenise.go DefaultTokeniseOptions (Lowercase: true, separator set)
    - props_test.go lines 1140-1280 (LCSStr property block) and 1508-1670 (Q-Gram Jaccard property block) — accumulator append pattern
    - example_test.go lines 201-310 (q-gram examples — `// Output:` block format, `fmt.Printf("%.4f\n", ...)` pattern)
    - tests/bdd/features/qgram_jaccard.feature (full feature file structure — primary-source header, `Scenario Outline: Canonical reference vectors`, identity / both-empty / one-empty / symmetry scenarios)
    - tests/bdd/steps/algorithms_steps.go lines 442-475 (Q-Gram Jaccard step methods — append-only) and the matching `InitializeScenario` block at lines 820-833
    - testdata/golden/_staging/qgram_jaccard.json (8-12 entries, `{"name": "...", "algorithm": "...", "a": "...", "b": "...", "expected_score": ...}` shape)
    - llms.txt lines 100-130 (per-algorithm `### <Name>` heading + bulleted func signature list)
    - llms-full.txt lines 155-215 (per-plan `### Phase 5 algorithm surface (...)` block — full godoc-style snippet)
    - .planning/phases/06-token-based-algorithms/06-RESEARCH.md Pattern 4 / Example 3 (TokenSortRatio sorted-join + indelRatio composition body)
    - .planning/phases/06-token-based-algorithms/06-CONTEXT.md §6 (Per-plan llms.txt + llms-full.txt sync REINFORCED — must land in same commit)
    - .planning/phases/06-token-based-algorithms/06-PATTERNS.md "Algorithm test files / bench files / fuzz files / per-plan accumulator" sections
    - .claude/skills/go-testing-standards/SKILL.md (BDD scenario coverage discipline, fuzz-per-public-function rule, coverage floors)
    - .claude/skills/algorithm-correctness-standards/SKILL.md (primary-source citation, mathematical invariants, reference vectors, edge cases)
  </read_first>
  <behavior>
    - `TokenSortRatioScore("alpha beta", "beta alpha") == 1.0` (sorted tokens identical → sorted-joined identical → indelRatio == 1.0)
    - `TokenSortRatioScore("hello world", "hello world") == 1.0` (a == b identity short-circuit; Tokenise NOT invoked)
    - `TokenSortRatioScore("", "") == 1.0` (both-empty via identity short-circuit)
    - `TokenSortRatioScore("hello", "") == 0.0` (one-empty)
    - `TokenSortRatioScore("", "hello") == 0.0` (one-empty)
    - `TokenSortRatioScore("fuzzy wuzzy was a bear", "wuzzy fuzzy was a bear") == 1.0` (token-reordered)
    - `TokenSortRatioScore(a, b) == TokenSortRatioScore(b, a)` for all inputs (symmetric — Tokenise is deterministic; sort.Strings is deterministic; indelRatio is symmetric)
    - Returned value always in `[0.0, 1.0]` (range-bounds invariant)
    - Identical-string fuzz inputs return 1.0 (no NaN, no ±Inf, no panic)
    - `dispatch[AlgoTokenSortRatio]("alpha beta", "beta alpha") == 1.0` (dispatch registration verified via DispatchInvokeForTest in dispatch unit test)
  </behavior>
  <action>
    Create `token_sort_ratio.go` (Apache-2.0 header → block-comment with primary-source citation: RapidFuzz documentation https://rapidfuzz.github.io/RapidFuzz/Usage/fuzz.html for the engineering provenance + Wagner & Fischer 1974 for the underlying LCS DP — per algorithm-correctness-standards "For algorithms without an academic primary source ... cite the canonical modern reference (RapidFuzz documentation URL) and note the engineering provenance explicitly"; include the Indel-formula equivalence proof reference to RESEARCH.md Pattern 3 → `Algorithm — TokenSortRatioScore(a, b):` block describing tokenise → sort → join → indelRatio → `Conventions:` block (both-empty → 1.0 via identity; identical → 1.0; one-empty → 0.0) → `Source-origin discipline:` (Primary source: RapidFuzz docs + Wagner-Fischer 1974; Cross-validation: RapidFuzz 3.14.5 via testdata/cross-validation/token-ratios/vectors.json; GPL/LGPL provenance: none; Code copied verbatim: none — fresh transcription from RapidFuzz `_split_sequence` + `token_sort_ratio` Python source structure only) → `Implementation discipline:` (NO init; NO map iteration; integer arithmetic in kernel + one final division in indelRatio; identity short-circuit BEFORE Tokenise to avoid the `make([]string, 0, 4)` allocation on identical inputs; tokeniser-divergence note per OQ-1 — uses `Tokenise(s, DefaultTokeniseOptions())` (camelCase/lowercase aware), NOT Python str.split() — for whitespace-only-lowercase-ASCII inputs the behaviour matches RapidFuzz, for inputs with mixed identifier styles the project tokenisation produces semantically richer splits) → `package fuzzymatch`.

    `func TokenSortRatioScore(a, b string) float64` body follows RESEARCH.md Example 3 verbatim in structure (not code): identity short-circuit `if a == b { return 1.0 }`; both-Tokenised-empty guard returning 1.0; one-Tokenised-empty guard returning 0.0; `sort.Strings(tokensA); sort.Strings(tokensB)`; `joinedA := strings.Join(tokensA, " "); joinedB := strings.Join(tokensB, " ")`; `return indelRatio([]byte(joinedA), []byte(joinedB))`. Imports: `sort`, `strings`. NO public Runes variant (per CONTEXT §6 — Tokenise handles UTF-8 internally).

    Create `dispatch_token_sort_ratio.go` mirroring `dispatch_lcsstr.go` exactly: file-header with the canonical `var _ = func() bool { ... }()` rationale; package-level idiom `var _ = func() bool { dispatch[AlgoTokenSortRatio] = TokenSortRatioScore; return true }()`. NO closure (signature is already `func(a, b string) float64`).

    Create `token_sort_ratio_test.go` (`package fuzzymatch_test`, stdlib `testing` only):
    - Table-driven `TestTokenSortRatioScore` covering ≥ 8 cases: identity (`"hello"`/`"hello"`); both-empty; one-empty A; one-empty B; token-reordered (`"fuzzy wuzzy was a bear"` vs `"wuzzy fuzzy was a bear"` → 1.0); subset (`"alpha beta"` vs `"alpha beta gamma"` < 1.0 — exact value computed by hand from sorted-joined strings); disjoint (`"abc"` vs `"xyz"` low score); Unicode (`"café société"` vs `"cafe societe"` — values diverge between byte path and what RapidFuzz reports under its case-preserving processor; the exact value is the cross-validated one from vectors.json). Each test case has its expected_score derived from the sorted-joined-strings → indelRatio computation with the derivation in the test comment per algorithm-correctness-standards "Reference Vectors" — at least 3 canonical examples with citation noted.
    - `TestTokenSortRatioScore_DispatchRegistration` calling `fuzzymatch.DispatchInvokeForTest(int(fuzzymatch.AlgoTokenSortRatio), "alpha beta", "beta alpha")` and asserting `== 1.0` to exercise the closure body.

    Create `token_sort_ratio_bench_test.go` mirroring `qgram_jaccard_bench_test.go`: `BenchmarkTokenSortRatioScore_ASCII_Short` (10 chars) / `_ASCII_Medium` (50 chars) / `_ASCII_Long` (200 chars) / `_Unicode_Short`. Each uses `b.ReportAllocs()` + `var sink float64` + `sink < 0` gate. File-header godoc states the per-input-size allocation-budget table per .claude/skills/performance-standards "Token Sort / Set / Jaccard Ratio: < 5 µs, ≤ 4 allocations".

    Create `token_sort_ratio_fuzz_test.go` mirroring `qgram_jaccard_fuzz_test.go`: one `FuzzTokenSortRatioScore` with ≥ 8 programmatic `f.Add` seeds (canonical reference vector / identity / both-empty / one-empty / orthogonal / invalid UTF-8 / multi-byte UTF-8 / long-input). Body asserts no panic + score in `[0.0, 1.0]` + no NaN + no ±Inf. Optional on-disk seed corpus at `testdata/fuzz/FuzzTokenSortRatioScore/seed-001`.

    Append to `props_test.go` (after the Phase 5 q-gram block, at the end of the file):
    - `// --- TokenSortRatio property tests (plan 06-01)` divider
    - `TestProp_TokenSortRatioScore_RangeBounds`, `_Identity` (skip empty input — Tokenise can produce empty token list), `_Symmetric`, `_NoNaN`, `_NoInf`, `_NoNegativeZero` — each via `testing/quick` per the LCSStr block template at lines 1140-1280

    Append to `example_test.go` (per Phase 5 q-gram convention at lines 201-216): `ExampleTokenSortRatioScore` calling `TokenSortRatioScore("fuzzy wuzzy was a bear", "wuzzy fuzzy was a bear")` with `fmt.Printf("%.4f\n", ...)` and `// Output:` comment block.

    Create `tests/bdd/features/token_sort_ratio.feature` mirroring `tests/bdd/features/qgram_jaccard.feature` structure: hashed-line header citing RapidFuzz docs as primary engineering source AND Wagner-Fischer 1974 as the LCS DP source; `Feature: TokenSortRatio (Indel-formula similarity over sorted-joined tokens)`; `Scenario Outline: Canonical reference vectors` with at least 4 Examples rows (identity / token-reordered / subset / disjoint); `Scenario: identical strings score 1.0`; `Scenario: both-empty strings score 1.0`; `Scenario Outline: one-empty string scores 0.0` (Examples for both A-empty and B-empty); `Scenario: token-sort score is symmetric`. Tag every scenario `@token`.

    Append step methods to `tests/bdd/steps/algorithms_steps.go` (after the existing Phase 5 Tversky block, ~line 911): `iComputeTheTokenSortRatioScoreBetween`, `iComputeTheSecondTokenSortRatioScoreBetween`, `bothTokenSortRatioScoresShouldBeEqual` — same shape as `iComputeTheQGramJaccardScoreBetweenWithN` at lines 442-475 minus the `n` parameter. Add the matching `ctx.Step(...)` registrations in `InitializeScenario` (alongside the Phase 5 entries at lines 820-833).

    Create `testdata/golden/_staging/token_sort_ratio.json` with 8-12 entries in the canonical `{"version": 1, "entries": [{"name": "TokenSortRatio_<id>", "algorithm": "TokenSortRatio", "a": "...", "b": "...", "expected_score": ...}, ...]}` shape (per `_staging/qgram_jaccard.json`). Cover identity, both-empty, one-empty, token-reordered, subset, disjoint, Unicode (without expected RapidFuzz parity — these are project-locked values for cross-platform determinism, NOT RapidFuzz cross-validation values which live in vectors.json).

    Update `llms.txt` — append a new section before the `### Normalisation` heading:
    ```
    ### TokenSortRatio (RapidFuzz Indel formula via Tokenise → sort → join)

    - func TokenSortRatioScore(a, b string) float64
    ```

    Update `llms-full.txt` — append a new `### Phase 6 algorithm surface (token tier — TokenSortRatio)` block (mirroring the Phase 5 algorithm-surface blocks at lines 155-215) with: a paragraph describing the Indel formula equivalence + tokeniser-divergence note + per-plan llms-sync discipline reference; a fenced `go` snippet showing the `TokenSortRatioScore(a, b string) float64` signature with godoc; reference vector inline (e.g. `TokenSortRatioScore("fuzzy wuzzy was a bear", "wuzzy fuzzy was a bear") = 1.0`).
  </action>
  <verify>
    <automated>go test -run 'TestTokenSortRatio|TestProp_TokenSortRatio' -race -shuffle=on -count=1 ./... && go test -run TestLLMsTxt ./... && cd tests/bdd && go test -race -count=1 ./... && go test -run none -bench BenchmarkTokenSortRatioScore -benchtime=1x ./..</automated>
  </verify>
  <done>
    TokenSortRatioScore is exported, registered in dispatch[AlgoTokenSortRatio] (verified via TestTokenSortRatioScore_DispatchRegistration), passes the table-driven unit tests, the six property tests, the BDD scenarios, the fuzz harness compile-and-add, the bench harness compile-and-run; staging-golden JSON committed; llms.txt + llms-full.txt sync verified by `ai_friendly_test.go::TestLLMsTxt`; no testify in any new root `_test.go`; no GPL/LGPL provenance; AxonOps Apache-2.0 header on every new `.go` file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land RapidFuzz cross-validation infrastructure (Python generator + pinned vectors.json + Go loader test + Makefile target + docs/cross-validation.md)</name>
  <files>scripts/gen-token-ratio-cross-validation.py, testdata/cross-validation/token-ratios/vectors.json, token_ratio_cross_validation_test.go, Makefile, docs/cross-validation.md</files>
  <read_first>
    - scripts/gen-ratcliff-obershelp-cross-validation.py (full Apache-2.0 hashed-line header, module docstring with usage / requirements / determinism notes, `_MIN_PYTHON_VERSION = (3, 7)` gate, `CASES = [...]` ordered list, `score_case` function, `_check_python_version` gate, `main` function writing JSON with `json.dump(..., indent=2, sort_keys=False)` + trailing LF)
    - scripts/gen-swg-cross-validation.py (alternative pinned-version-assertion analog for `biopython.__version__`)
    - testdata/cross-validation/ratcliff-obershelp/vectors.json (committed-corpus shape — `version`, `python_version`, `entries` top-level keys; per-entry `name`, `a`, `b`, `<scoring_field>`)
    - ratcliff_obershelp_test.go lines 283-356 (`TestRatcliffObershelp_CrossValidation` corpus-loader struct + version-gate + per-entry t.Run pattern)
    - Makefile lines 26-30 (.PHONY line — must add `regen-token-ratio-cross-validation`) and lines 213-228 (`regen-ratcliff-obershelp-cross-validation` target template — copy structure with rapidfuzz-specific install instructions)
    - .planning/phases/06-token-based-algorithms/06-RESEARCH.md Pattern 9 (full generator script template) and Pattern 10 (full Go loader template)
    - .planning/phases/06-token-based-algorithms/06-RESEARCH.md Pitfall 1 (tokeniser-divergence avoidance — corpus restricted to whitespace-only lowercase ASCII)
    - .planning/phases/06-token-based-algorithms/06-CONTEXT.md §1 LOCKED (RapidFuzz cross-validation mandate + script header version-pin + JSON _metadata)
    - .planning/phases/06-token-based-algorithms/06-VALIDATION.md "Manual-Only Verifications" row 1 (RapidFuzz install + version-pin self-verification flow)
    - tokenise.go DefaultTokeniseOptions (Lowercase: true) — informs the generator's `.lower()` reconciliation
    - .claude/skills/algorithm-licensing-standards/SKILL.md (RapidFuzz is MIT — vectors are reference-vector cross-validation, not code copy; document in script header)
  </read_first>
  <behavior>
    - `make regen-token-ratio-cross-validation` (with `pip install --user rapidfuzz==3.14.5` installed) produces a byte-identical `vectors.json` on repeated runs
    - The generator script refuses to run if `rapidfuzz.__version__ != "3.14.5"` and exits with a clear error message
    - The generator script refuses to run on Python < 3.7 and exits with a clear error message (mirrors `_check_python_version` from gen-ratcliff-obershelp-cross-validation.py)
    - `vectors.json` contains a top-level `_metadata` block with `rapidfuzz_version`, `python_version`, `regenerated_at` (ISO timestamp)
    - `vectors.json` contains ≥ 20 `entries`, each with fields `name`, `a`, `b`, `token_sort_ratio`, `token_set_ratio`, `partial_ratio_bytes`, `partial_ratio_runes` (all four scores per entry per OQ-2 / OQ-3)
    - Every corpus input string is lowercase ASCII with whitespace-only word boundaries (OQ-1 tokeniser-divergence avoidance — the script's `_assert_corpus_is_tokenise_safe` gate enforces this and exits nonzero on violation; the input is also `.lower()`'d before being passed to RapidFuzz to reconcile case-folding)
    - `go test -run TestTokenRatios_CrossValidation -race ./...` exits 0 with the Wave-1 loader asserting `token_sort_ratio` agreement on EVERY entry within `epsilon = 1e-9`; `token_set_ratio` / `partial_ratio_bytes` / `partial_ratio_runes` sub-tests are present but skipped via `t.Skip("plan 06-02 will land TokenSetRatioScore — see VALIDATION.md row 06-02-01")` until plans 06-02 / 06-03 land — the loader is structurally complete on Wave 1 but its assertion loop body is gated per-surface
    - The Go loader's failure message includes `c.RapidFuzzVersion` and `c.PythonVersion` for triage (mirrors `ratcliff_obershelp_test.go` line 351-352 pattern)
    - `docs/cross-validation.md` exists and documents: the pinned RapidFuzz version (3.14.5), the lowercasing reconciliation, the OQ-1 corpus restriction (whitespace-only lowercase ASCII), the regeneration command (`make regen-token-ratio-cross-validation`), and the `pip install` instruction
  </behavior>
  <action>
    Create `scripts/gen-token-ratio-cross-validation.py` mirroring `scripts/gen-ratcliff-obershelp-cross-validation.py` structurally:
    - Apache-2.0 hashed-line header (lines 1-14 verbatim — same AxonOps copyright and licence text)
    - Module docstring documenting: purpose (RapidFuzz cross-validation corpus generator for TokenSort / TokenSet / PartialRatio bytes / PartialRatio runes — four scores per entry); reference implementation (`rapidfuzz.fuzz.token_sort_ratio` / `token_set_ratio` / `partial_ratio` — MIT licensed; used SOLELY for reference-vector cross-validation per algorithm-licensing-standards; no rapidfuzz code copied into fuzzymatch implementation); CRITICAL — RAPIDFUZZ_VERSION pin gate; tokeniser-divergence note (corpus restricted to whitespace-only lowercase ASCII inputs per RESEARCH.md Pitfall 1 / OQ-1; inputs are `.lower()`'d before passing to RapidFuzz to reconcile RapidFuzz's case-preserving default with fuzzymatch's lowercasing Tokenise); usage; requirements (Python 3.7+, rapidfuzz==3.14.5); determinism (CASES list owns deterministic ordering; Python 3.7+ dict insertion-order; json.dump with sort_keys=False)
    - `RAPIDFUZZ_VERSION = "3.14.5"` constant
    - `_MIN_PYTHON_VERSION = (3, 7)` constant
    - Imports: `import json, os, sys` then `import rapidfuzz` followed by `assert rapidfuzz.__version__ == RAPIDFUZZ_VERSION, f"rapidfuzz version mismatch: installed {rapidfuzz.__version__}, script pinned to {RAPIDFUZZ_VERSION} — run: pip install --user rapidfuzz=={RAPIDFUZZ_VERSION}"`
    - `from rapidfuzz import fuzz`
    - `CASES = [...]` ordered list of dicts `{"name": "...", "a": "...", "b": "..."}` — ≥ 20 entries spanning ALL of: identity_short, both_empty, one_empty_a, one_empty_b, tokens_reordered, subset_a_in_b, subset_b_in_a, partial_short_in_long, partial_disjoint, tokenset_diff_dominates (TokenSet three-way max edge where the diff_ab vs diff_ba ratio wins per RESEARCH.md Pattern 5 critical landmine 4), tokenset_subset_shortcircuit (one set is subset of the other → expected 1.0 via short-circuit), partial_left_tail_wins (Pitfall 3 Region-1 fixture: `("abc", "ab")` — though restricted to whitespace-or-not — see notes below), partial_right_tail_wins (Pitfall 3 Region-3 fixture: `("abc", "bc")`), partial_middle_wins, long_short_mismatch (small pathological pair), unicode_lowercase_ascii_safe (multi-token input where tokenise+rapidfuzz agree under .lower() reconciliation), and the standard ASCII medium / ASCII long entries
    - `_assert_corpus_is_tokenise_safe(case)` helper that fails with a clear error if `case["a"]` or `case["b"]` contains uppercase letters / `_` / `-` / `.` / camelCase boundaries — calls `sys.exit(2)` on violation. NOTE: PartialRatio is character-level (no Tokenise), but the constraint applies because the SAME corpus serves TokenSort / TokenSet which DO Tokenise. PartialRatio inputs that are not "Tokenise-safe" are still allowed in the corpus IFF they only feed `partial_ratio_bytes` and `partial_ratio_runes` and are explicitly marked with a `"partial_only": true` flag in the case dict (the `_assert_corpus_is_tokenise_safe` helper skips the check when this flag is set). This lets us include `("abc", "bc")` Region-3 fixtures without violating the Tokenise-safety invariant for the TokenSort / TokenSet entries — the generator emits a sentinel value (e.g. `null`) for `token_sort_ratio` and `token_set_ratio` on partial-only entries, and the Go loader skips those sub-tests via the per-surface `e.TokenSortRatio == nil` (or sentinel) check.
    - `def score_case(case)` returns the entry dict with `name`, `a`, `b`, `token_sort_ratio`, `token_set_ratio`, `partial_ratio_bytes`, `partial_ratio_runes` (each computed via `fuzz.<name>(case["a"].lower(), case["b"].lower()) / 100.0` — divided by 100 because RapidFuzz returns a 0-100 score; project surface is 0.0-1.0) — for `partial_only: true` entries the token_sort and token_set fields are emitted as `null` (json.dumps preserves Python None as JSON null)
    - `_check_python_version()` gate (mirror gen-ratcliff-obershelp-cross-validation.py)
    - `main()` builds the corpus dict `{"version": 1, "_metadata": {"rapidfuzz_version": RAPIDFUZZ_VERSION, "python_version": sys.version.split()[0], "regenerated_at": datetime.now(timezone.utc).isoformat()}, "entries": [score_case(c) for c in CASES]}` and writes to `testdata/cross-validation/token-ratios/vectors.json` with `json.dump(corpus, f, indent=2, sort_keys=False)` + trailing `f.write("\n")`
    - `if __name__ == "__main__": _check_python_version(); main()`

    Run the script locally (developer side) to produce the initial committed `vectors.json` — both files committed in the same commit. The corpus must be byte-stable on repeated runs (verified by re-running and diffing).

    Add `Makefile` target after line 228 (after `regen-ratcliff-obershelp-cross-validation`):
    ```
    # Regenerates testdata/cross-validation/token-ratios/vectors.json by
    # invoking the rapidfuzz-based generator script. Developer-only — NOT
    # included in `make check`. The committed JSON is the verification
    # fixture; CI does NOT require Python at test time. Re-run this target
    # when rapidfuzz is bumped or when new test cases are added to
    # scripts/gen-token-ratio-cross-validation.py.
    #
    # Requires: python3 + rapidfuzz==3.14.5
    #   python3 -m pip install --user rapidfuzz==3.14.5
    regen-token-ratio-cross-validation:
            @if ! command -v python3 >/dev/null 2>&1; then \
              echo "python3 not found; install Python 3.7+ and run: python3 -m pip install --user rapidfuzz==3.14.5"; \
              exit 1; \
            fi
            python3 scripts/gen-token-ratio-cross-validation.py
    ```
    Add `regen-token-ratio-cross-validation` to the `.PHONY:` line at lines 26-29 (alongside the existing two cross-validation targets). Also add the corresponding entry to `makefile_targets_test.go` if that meta-test enumerates targets explicitly (read the file first; only modify if needed).

    Create `token_ratio_cross_validation_test.go` (`package fuzzymatch_test`, stdlib `testing` only) with the structure transcribed from `ratcliff_obershelp_test.go` lines 283-356 (do NOT copy — fresh transcription per fresh-implementation discipline):
    - Top-of-file godoc explaining: corpus loader for the four-score-per-entry RapidFuzz cross-validation corpus; tolerance `epsilon = 1e-9`; per-entry sub-tests via `t.Run(e.Name+"/<surface>", ...)`; failure message includes `c.RapidFuzzVersion`, `c.PythonVersion`, and the field name for triage; the loader is the Wave-1 surface — TokenSetRatio / PartialRatio sub-tests skip with `t.Skip("plan 06-NN will land ...; see VALIDATION.md row ...")` until those plans land
    - `type tokenRatioEntry struct { Name string `json:"name"`; A string `json:"a"`; B string `json:"b"`; TokenSortRatio *float64 `json:"token_sort_ratio"`; TokenSetRatio *float64 `json:"token_set_ratio"`; PartialRatioBytes *float64 `json:"partial_ratio_bytes"`; PartialRatioRunes *float64 `json:"partial_ratio_runes"`; PartialOnly bool `json:"partial_only,omitempty"` }` — pointer-to-float64 lets us distinguish JSON `null` (skipped sub-test) from `0.0` (legitimate score)
    - `type tokenRatioCorpus struct { Version int `json:"version"`; Metadata struct { RapidFuzzVersion string `json:"rapidfuzz_version"`; PythonVersion string `json:"python_version"`; RegeneratedAt string `json:"regenerated_at"` } `json:"_metadata"`; Entries []tokenRatioEntry `json:"entries"` }`
    - `func TestTokenRatios_CrossValidation(t *testing.T)`:
      - `const epsilon = 1e-9`
      - `path := filepath.Join("testdata", "cross-validation", "token-ratios", "vectors.json")`
      - `os.ReadFile` + `json.Unmarshal` + corpus version / `_metadata.rapidfuzz_version` non-empty / Python version >= 3.7 gates (mirror RO test lines 305-344)
      - For each entry, run four `t.Run(e.Name+"/token_sort", ...)`, `t.Run(e.Name+"/token_set", ...)`, `t.Run(e.Name+"/partial_bytes", ...)`, `t.Run(e.Name+"/partial_runes", ...)` sub-tests:
        - `token_sort` sub-test: if `e.TokenSortRatio == nil` then `t.Skip("partial_only entry — TokenSortRatio not computed")`; else `got := fuzzymatch.TokenSortRatioScore(e.A, e.B)`; assert `math.Abs(got - *e.TokenSortRatio) <= epsilon` with the failure message including delta, tolerance, rapidfuzz_version, python_version
        - `token_set` sub-test: `t.Skip("plan 06-02 will land TokenSetRatioScore — see VALIDATION.md row 06-02-01")` (Wave-1 placeholder; plan 06-02 removes the Skip and adds the assertion body)
        - `partial_bytes` sub-test: `t.Skip("plan 06-03 will land PartialRatioScore — see VALIDATION.md row 06-03-01")`
        - `partial_runes` sub-test: `t.Skip("plan 06-03 will land PartialRatioScoreRunes — see VALIDATION.md row 06-03-02")`
      - Imports: `encoding/json`, `math`, `os`, `path/filepath`, `strconv`, `strings`, `testing`, `github.com/axonops/fuzzymatch`

    Create `docs/cross-validation.md` (a new contributor-facing doc) documenting:
    - **RapidFuzz pin:** the project pins `rapidfuzz==3.14.5` (current stable as of 2026-04-07 per PyPI). Bumping requires: (1) updating `RAPIDFUZZ_VERSION` in `scripts/gen-token-ratio-cross-validation.py`, (2) updating the install hint in `Makefile`'s `regen-token-ratio-cross-validation` target, (3) running `make regen-token-ratio-cross-validation`, (4) committing the regenerated `vectors.json`, (5) running `go test -run TestTokenRatios_CrossValidation -race ./...` — any score drift between the new RapidFuzz version and the project implementation surfaces as a test failure and requires algorithm-correctness-reviewer review.
    - **Tokeniser divergence (OQ-1 RESOLUTION LOCKED):** RapidFuzz tokenises via Python `str.split()` (whitespace-only, case-preserving); fuzzymatch uses `Tokenise(s, DefaultTokeniseOptions())` (camelCase / snake_case / kebab-case / dot-case aware AND lowercasing). To make cross-validation tractable, the corpus is restricted to inputs where the two tokenisations agree — pure whitespace-separated lowercase ASCII text. The generator script's `_assert_corpus_is_tokenise_safe` gate enforces this. PartialRatio inputs (which are character-level, no tokenise) MAY use any inputs but are flagged with `"partial_only": true` so they don't pollute TokenSort / TokenSet entries. The script also calls `.lower()` on inputs before passing to RapidFuzz to reconcile RapidFuzz's case-preserving default with fuzzymatch's lowercasing Tokenise.
    - **Why RapidFuzz, not fuzzywuzzy:** fuzzywuzzy's pure-Python and C-extension paths produce different scores for the same library call (RapidFuzz issue lineage). The Indel formula is canonically documented by RapidFuzz at https://rapidfuzz.github.io/RapidFuzz/Usage/distance/Indel.html and is mathematically equivalent to the spec's `2·LCS/(|a|+|b|)` (proof in 06-RESEARCH.md Pattern 3).
    - **Empty-token-set deviation (TokenSetRatio only):** RapidFuzz's `token_set_ratio` returns 0 (not 100) when either token-set is empty (bug-for-bug compat with fuzzywuzzy issue #110). fuzzymatch's TokenSetRatioScore matches this deviation (per plan 06-02 LOCKED). Other tokenised algorithms (TokenJaccard, MongeElkan) follow the standard both-empty → 1.0 convention.
    - **Regeneration command:** `make regen-token-ratio-cross-validation` (developer-only; CI does NOT require Python at test time — the committed JSON is the verification fixture).
  </action>
  <verify>
    <automated>go test -run TestTokenRatios_CrossValidation -race -shuffle=on -count=1 ./... && grep -q '^regen-token-ratio-cross-validation:' Makefile && grep -q 'regen-token-ratio-cross-validation' Makefile && test -f testdata/cross-validation/token-ratios/vectors.json && test -f scripts/gen-token-ratio-cross-validation.py && test -f docs/cross-validation.md && python3 -c 'import json; c = json.load(open("testdata/cross-validation/token-ratios/vectors.json")); assert c["version"] == 1; assert c["_metadata"]["rapidfuzz_version"] == "3.14.5"; assert len(c["entries"]) >= 20; print("ok")'</automated>
  </verify>
  <done>
    Generator script + Makefile target + committed corpus JSON + Go loader + docs page all land in the same commit; `TestTokenRatios_CrossValidation` is green with TokenSortRatio asserting on every entry and TokenSet/PartialRatio sub-tests skipping with explicit "plan 06-NN will land" messages; no rapidfuzz dependency added to root `go.mod` (verified by `make verify-deps-allowlist`); the corpus carries `_metadata.rapidfuzz_version == "3.14.5"`, `_metadata.python_version` non-empty, ≥ 20 entries; `docs/cross-validation.md` documents OQ-1 / pinning / regeneration.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 6 algorithms are pure functions accepting opaque strings; no untrusted input crosses a process / network / filesystem boundary at the algorithm layer |

## STRIDE Threat Register

Token-based algorithms accept opaque string inputs; the only security-relevant concerns are algorithmic-complexity DoS classes per CONTEXT.md §5 LOCKED. This plan introduces the foundation kernel + TokenSortRatio (linear-time composition: tokenise + sort + join + LCS DP); no DoS-relevant complexity beyond LCS DP `O(m·n)` which is the standard / accepted Wagner-Fischer 1974 cost. The Pathological-input bench fixtures land in plans 06-02 (TokenSet), 06-03 (PartialRatio), and 06-05 (Monge-Elkan). For TokenSortRatio specifically: the sorted-joined string lengths are at most `len(a)+len(b)`, so the LCS DP cost remains `O((|a|+|b|)^2)` in the worst case — same complexity class as Levenshtein on the raw strings, no novel DoS vector.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | D (denial-of-service via algorithmic complexity) | `lcsLen` on very long inputs | accept | LCS DP is `O(m·n)` worst-case; the kernel is not exposed publicly (no `LCSLen` public function) so consumers cannot accidentally invoke it on adversarial inputs; downstream algorithms (TokenSortRatio in this plan; TokenSetRatio / PartialRatio / MongeElkan in later plans) document their own DoS notices per CONTEXT §5 LOCKED. The pure-function library has no input-validation rejection on long input — caller controls input size. |
| T-06-01-02 | T (tampering) — corpus drift | `testdata/cross-validation/token-ratios/vectors.json` | mitigate | Committed corpus has `_metadata.rapidfuzz_version` field; the Go loader test asserts the field is non-empty AND that Python version >= 3.7. Generator script asserts `rapidfuzz.__version__ == "3.14.5"` at run time. Tampering attempts surface as: (a) loader test failure with the field value in the message, (b) `make regen-token-ratio-cross-validation` refusing to run on the wrong rapidfuzz version. |
</threat_model>

<verification>
- `go test -run 'TestLCSLen|TestIndelRatio|TestLCSLen_DistinctFromLCSStr|TestTokenSortRatio|TestProp_TokenSortRatio|TestTokenRatios_CrossValidation' -race -shuffle=on -count=1 ./...` exits 0
- `go test -run TestLLMsTxt ./...` exits 0 (per-plan llms.txt + llms-full.txt sync gate)
- `cd tests/bdd && go test -race -count=1 ./...` exits 0 (TokenSortRatio scenarios pass)
- `make fmt-check && make vet && make lint` exit 0 across all new/modified files
- `make verify-deps-allowlist` exits 0 (root go.mod still has only `golang.org/x/text` runtime dep — rapidfuzz is Python-developer-toolchain only)
- `make verify-license-headers` exits 0 (every new `.go` and `.py` file has the AxonOps Apache-2.0 header)
- `python3 -c 'import json; c = json.load(open("testdata/cross-validation/token-ratios/vectors.json")); assert len(c["entries"]) >= 20'` exits 0
</verification>

<success_criteria>
- TOKEN-02 closed: TokenSortRatioScore matches RapidFuzz `token_sort_ratio` byte-identically on the committed cross-validation corpus (within `epsilon = 1e-9`); the algorithm has unit + property + fuzz + bench + BDD coverage; AlgoTokenSortRatio dispatch slot wired
- Shared `token_indel.go` foundation kernel exists with the four unexported helpers; consumers in plans 06-02 / 06-03 / 06-05 can compose against it without touching its internals
- Cross-validation infrastructure (script + corpus + Makefile target + Go loader + `docs/cross-validation.md`) is fully wired and Wave-1-extensible: plans 06-02 / 06-03 only need to remove `t.Skip` calls from the existing per-surface sub-tests
- Per-plan llms.txt + llms-full.txt sync verified by `ai_friendly_test.go::TestLLMsTxt`
- Recorded resolutions for OQ-1 / OQ-2 / OQ-3 / RapidFuzz version pin / stack-buffer threshold are written into the plan SUMMARY.md as `OQ-N RESOLUTION LOCKED <date>` lines
- Apache-2.0 file header on every new `.go` and `.py` file (`make verify-license-headers` passes)
- Coverage floors maintained: ≥ 95% overall, ≥ 90% per file, 100% on public API surface
- No new entries added to root `go.mod` (only `golang.org/x/text` remains)
</success_criteria>

<output>
After completion, create `.planning/phases/06-token-based-algorithms/06-01-SUMMARY.md` documenting:
- File-by-file changes (token_indel.go + token_sort_ratio.go + companions + cross-validation infra)
- The five `OQ-N RESOLUTION LOCKED <date>` lines (OQ-1 tokeniser-divergence handling; OQ-2 single combined corpus; OQ-3 partial_ratio_runes always-included; RapidFuzz version pin; stack-buffer threshold)
- Architectural decisions consumed by Wave 2/3 plans (kernel signatures, export_test re-exports, corpus structure, loader skip-message format)
- Reference vector numbers for the staging-golden file
- Any deferred items for plan 06-06 (e.g., final `bench.txt` numbers)
</output>