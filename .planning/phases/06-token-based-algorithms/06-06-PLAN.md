---
phase: 06-token-based-algorithms
plan: 06
type: execute
wave: 6
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05"]
files_modified:
  - algorithms_golden_test.go
  - testdata/golden/algorithms.json
  - examples/identifier-similarity/main.go
  - examples/identifier-similarity/main_test.go
  - bench.txt
  - cross_algorithm_consistency_test.go
autonomous: true
requirements: [TOKEN-01, TOKEN-02, TOKEN-03, TOKEN-04, TOKEN-05]
must_haves:
  truths:
    - "User can run `make verify-determinism` and observe testdata/golden/algorithms.json diffing byte-identically across the cross-platform CI matrix (linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64) — extended with all 5 Phase 6 algorithm entries"
    - "User can run `go run ./examples/identifier-similarity` and see the rendered table with the 5 new Phase 6 columns (TokenSort / TokenSet / Partial / TokenJaccard / MongeElkan) appended after the 14 existing Phase 1-5 columns"
    - "User can run `make bench` and observe the regenerated bench.txt baseline including all 5 Phase 6 algorithm benchmarks + 3 pathological fixtures (TokenSet / PartialRatio / MongeElkan) — benchstat regression detection green vs Phase 5 carry-forward benchmarks"
    - "All 5 Phase 6 algorithms (TokenSort / TokenSet / Partial / TokenJaccard / MongeElkan) appear in the dispatch table; AlgoIDs() returns a stable list including their entries; algorithms.json contains entries for all 5"
    - "Cross-algorithm consistency tests pin at least 2 cross-algorithm invariants for the Phase 6 surface (e.g. TokenJaccard vs Q-Gram Jaccard divergence on `('a a b', 'a b')`; TokenSetRatio empty-set deviation pinned against TokenJaccard's both-empty 1.0)"
  artifacts:
    - path: "testdata/golden/algorithms.json"
      provides: "Cross-platform determinism golden file extended with all 5 Phase 6 algorithm entries (merged from staging files via TestGolden_Algorithms_Merge)"
    - path: "algorithms_golden_test.go"
      provides: "Updated stagingFiles slice with the 5 new Phase 6 staging-file paths appended"
      contains: "_staging/monge_elkan.json"
    - path: "examples/identifier-similarity/main.go"
      provides: "Rendered identifier-similarity table extended from 14 to 19 columns (5 Phase 6 algorithms appended)"
    - path: "bench.txt"
      provides: "Full-replaced benchstat baseline including all Phase 6 benchmarks + 3 pathological fixtures"
    - path: "cross_algorithm_consistency_test.go"
      provides: "≥ 2 new cross-algorithm consistency tests pinning Phase 6 cross-algorithm invariants"
  key_links:
    - from: "algorithms_golden_test.go"
      to: "testdata/golden/_staging/{monge_elkan,partial_ratio,token_jaccard,token_set_ratio,token_sort_ratio}.json"
      via: "stagingFiles slice append → TestGolden_Algorithms_Merge reads and merges into algorithms.json (alphabetical by file path)"
      pattern: "_staging/(monge_elkan|partial_ratio|token_jaccard|token_set_ratio|token_sort_ratio)\\.json"
    - from: "examples/identifier-similarity/main.go"
      to: "fuzzymatch.{TokenSortRatioScore,TokenSetRatioScore,PartialRatioScore,TokenJaccardScore,MongeElkanScoreSymmetric}"
      via: "5 new entries appended to the algorithms slice at lines 87-105 with the dispatch-default form for each"
      pattern: "fuzzymatch\\.(TokenSort|TokenSet|Partial|TokenJaccard|MongeElkan)"
    - from: "bench.txt"
      to: "Benchmark{TokenSortRatio,TokenSetRatio,PartialRatio,TokenJaccard,MongeElkan}*"
      via: "make bench full-replace including the 3 pathological fixtures (BenchmarkTokenSetRatio_Pathological_AsymmetricSetCardinalities, BenchmarkPartialRatio_Pathological_LongShortMismatch_*, BenchmarkMongeElkan_Pathological_1000Tokens)"
      pattern: "Benchmark.*_Pathological_"
---

<objective>
Finalise Phase 6 by merging the 5 per-algorithm staging-golden files into the canonical `testdata/golden/algorithms.json` (cross-platform determinism gate), extending the `examples/identifier-similarity/` example with 5 new columns, full-replacing `bench.txt` to baseline all Phase 6 benchmarks (including the 3 LOCKED pathological fixtures), and pinning ≥ 2 new cross-algorithm consistency tests for the Phase 6 surface. Verifies all 5 Phase 6 algorithm requirement IDs (TOKEN-01..TOKEN-05) are committed and documented.

Purpose: this is the Phase 6 close-out plan. Per CONTEXT §6 LOCKED "Established Patterns": staging golden → finalisation merge happens here; identifier-similarity 14-to-19 column extension happens here; bench.txt full-replace happens here. NO new exported symbols (per-plan llms.txt + llms-full.txt sync was already done in plans 06-01..06-05; this plan does NOT touch llms.txt / llms-full.txt). NO new algorithm files. NO modifications to algorithm sources or test files.

Output: modifications to `algorithms_golden_test.go` (5 staging-file paths appended to `stagingFiles` slice), `testdata/golden/algorithms.json` (regenerated via `TestGolden_Algorithms_Merge -update`), `examples/identifier-similarity/main.go` (5 new column entries appended to the `algorithms` slice), `examples/identifier-similarity/main_test.go` (column-count assertion / golden-output update if any), `bench.txt` (full-replace via `make bench`), `cross_algorithm_consistency_test.go` (≥ 2 new cross-algorithm tests pinning Phase 6 invariants).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-token-based-algorithms/06-CONTEXT.md
@.planning/phases/06-token-based-algorithms/06-RESEARCH.md
@.planning/phases/06-token-based-algorithms/06-PATTERNS.md
@.planning/phases/06-token-based-algorithms/06-VALIDATION.md
@.planning/phases/06-token-based-algorithms/06-01-SUMMARY.md
@.planning/phases/06-token-based-algorithms/06-02-SUMMARY.md
@.planning/phases/06-token-based-algorithms/06-03-SUMMARY.md
@.planning/phases/06-token-based-algorithms/06-04-SUMMARY.md
@.planning/phases/06-token-based-algorithms/06-05-SUMMARY.md
@.claude/skills/algorithm-correctness-standards/SKILL.md
@.claude/skills/algorithm-licensing-standards/SKILL.md
@.claude/skills/performance-standards/SKILL.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/go-testing-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md
@algorithms_golden_test.go
@cross_algorithm_consistency_test.go
@examples/identifier-similarity/main.go
@examples/identifier-similarity/main_test.go
@bench.txt
@token_indel.go
@token_sort_ratio.go
@token_set_ratio.go
@partial_ratio.go
@token_jaccard.go
@monge_elkan.go
@dispatch_monge_elkan.go
@testdata/golden/_staging/monge_elkan.json
@testdata/golden/_staging/partial_ratio.json
@testdata/golden/_staging/token_jaccard.json
@testdata/golden/_staging/token_set_ratio.json
@testdata/golden/_staging/token_sort_ratio.json
@testdata/golden/algorithms.json

<interfaces>
<!-- Key types and contracts the executor needs. -->

From algorithms_golden_test.go (lines 162-202):
- `func TestGolden_Algorithms_Merge(t *testing.T)` — reads `stagingFiles` slice, merges entries, sorts alphabetically by Name, sanity-checks no duplicate Names, writes to `testdata/golden/algorithms.json` via `assertGolden(t, "algorithms.json", file)` when `-update` flag is set
- The current `stagingFiles` slice contains 14 entries (Phase 1-5 algorithms); Phase 6 adds 5

From examples/identifier-similarity/main.go (lines 87-105):
- `var algorithms = []struct { name string; fn func(a, b string) float64 }{...}` — 14 existing entries; Phase 6 appends 5 new entries
- `pairWidth = 32`, `algoWidth = 13` (lines 109-110) — column-width constants; the planner verifies the 13-char width accommodates the new labels (recommended labels: "TokenSort", "TokenSet", "Partial", "TokenJac", "MongeElk" — all ≤ 13 chars)

From the 5 algorithm files (landed in plans 06-01..06-05):
- `func TokenSortRatioScore(a, b string) float64` — direct dispatch shape
- `func TokenSetRatioScore(a, b string) float64` — direct dispatch shape
- `func PartialRatioScore(a, b string) float64` — byte-path dispatch (the rune surface is NOT in the example — the example uses byte path for column consistency)
- `func TokenJaccardScore(a, b string) float64` — direct dispatch shape
- `func MongeElkanScoreSymmetric(a, b string, inner AlgoID, opts NormalisationOptions) float64` — needs a wrapper closure binding the LOCKED defaults: `func(a, b string) float64 { return fuzzymatch.MongeElkanScoreSymmetric(a, b, fuzzymatch.AlgoJaroWinkler, fuzzymatch.DefaultNormalisationOptions()) }` (matches CONTEXT §4 LOCKED dispatch wrapper)

From cross_algorithm_consistency_test.go (Phase 4 / Phase 5 precedent):
- File-level pattern: each test asserts a cross-algorithm invariant (e.g. "Levenshtein vs DamerauLevenshtein OSA discriminator on 'ca' / 'abc'" or "Cosine vs Sørensen-Dice equivalence on identical inputs"); Phase 6 adds 2-4 new tests pinning Phase 6 cross-algorithm invariants
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge 5 Phase 6 staging-golden files into algorithms.json + extend identifier-similarity example with 5 new columns + cross-algorithm consistency tests</name>
  <files>algorithms_golden_test.go, testdata/golden/algorithms.json, examples/identifier-similarity/main.go, examples/identifier-similarity/main_test.go, cross_algorithm_consistency_test.go</files>
  <read_first>
    - algorithms_golden_test.go lines 150-202 (`TestGolden_Algorithms_Merge` `stagingFiles` slice + sort + duplicate-name check + `assertGolden` with `-update` flag handling — append-only modification)
    - examples/identifier-similarity/main.go lines 80-110 (the `algorithms` slice + the column-width constants `pairWidth = 32`, `algoWidth = 13`)
    - examples/identifier-similarity/main_test.go (any column-count assertion or golden-output check that needs updating with the 5 new columns)
    - cross_algorithm_consistency_test.go (Phase 4 / Phase 5 cross-algorithm invariant pattern — read 2-3 existing tests for the file-header layout and assertion style)
    - testdata/golden/_staging/monge_elkan.json + partial_ratio.json + token_jaccard.json + token_set_ratio.json + token_sort_ratio.json (all landed in plans 06-01..06-05; verify they are alphabetically-sortable and have no Name collisions with existing Phase 1-5 entries)
    - testdata/golden/algorithms.json (current state — Phase 5 baseline; Phase 6 extends additively)
    - All 5 algorithm files (token_sort_ratio.go / token_set_ratio.go / partial_ratio.go / token_jaccard.go / monge_elkan.go) for the public function signatures consumed by the example
    - .planning/phases/06-token-based-algorithms/06-CONTEXT.md §6 (Established Patterns: staging golden → finalisation merge; identifier-similarity 14-to-19 column extension; bench.txt full-replace)
    - .planning/phases/06-token-based-algorithms/06-PATTERNS.md "examples/identifier-similarity/main.go modification (plan 06-06 finalisation)" section (the exact append pattern for the 5 new entries)
  </read_first>
  <behavior>
    - `TestGolden_Algorithms_Merge` runs successfully without the `-update` flag (file is byte-stable after the merge)
    - `testdata/golden/algorithms.json` contains entries for all 5 Phase 6 algorithms (TokenSort, TokenSet, Partial, TokenJaccard, MongeElkan) merged alphabetically by Name; no duplicate Names across the merged staging files
    - `make verify-determinism` exits 0 (cross-platform diff via the CI matrix — locally just runs the golden test)
    - `examples/identifier-similarity/main.go`'s `algorithms` slice contains 19 entries (14 existing + 5 new); `go run ./examples/identifier-similarity` runs to completion and renders the table with the 5 new columns appended after the 14 existing columns
    - `examples/identifier-similarity/main_test.go` (if it has any column-count or golden-output assertions) is updated to expect 19 columns and the new column labels
    - `cross_algorithm_consistency_test.go` contains ≥ 2 new tests pinning Phase 6 cross-algorithm invariants (recommended: TokenJaccard-vs-QGramJaccard set-vs-multiset divergence; TokenSetRatio empty-set deviation pinned against TokenJaccard's both-empty 1.0)
    - All Phase 6 algorithm requirement IDs (TOKEN-01..TOKEN-05) are committed: each algorithm appears in dispatch[], in algorithms.json, in the identifier-similarity example, and in `cross_algorithm_consistency_test.go` for at least one cross-algorithm assertion
  </behavior>
  <action>
    **Step 1 — Append 5 new staging-file paths to the `stagingFiles` slice in `algorithms_golden_test.go`:**

    Locate the `stagingFiles` slice at lines 162-178 of `algorithms_golden_test.go`. Insert 5 new entries in alphabetical order:
    - `"_staging/monge_elkan.json"` (between `_staging/lcsstr.json` and `_staging/qgram_jaccard.json`)
    - `"_staging/partial_ratio.json"` (between `_staging/qgram_jaccard.json` and `_staging/ratcliff_obershelp.json`)
    - `"_staging/token_jaccard.json"` (between `_staging/tversky.json` and end-of-list)
    - `"_staging/token_set_ratio.json"` (after `_staging/token_jaccard.json`)
    - `"_staging/token_sort_ratio.json"` (after `_staging/token_set_ratio.json`)

    The exact alphabetical order between Phase 1-5 entries and the 5 Phase 6 entries is determined by string sort — the existing slice is already sorted alphabetically; the executor inserts each Phase 6 entry at the correct sorted position. After insertion, the slice has 19 entries total (14 existing + 5 new).

    Run `go test -run TestGolden_Algorithms_Merge -update -count=1 ./...` to regenerate `testdata/golden/algorithms.json` with the merged entries. Verify the merged JSON has entries for all 5 Phase 6 algorithm Names sorted with the rest. Then run `go test -run TestGolden_Algorithms_Merge -race -shuffle=on -count=1 ./...` (without `-update`) and confirm it passes (file is byte-stable). The duplicate-Name sanity check at lines 195-198 must pass.

    **Step 2 — Append 5 new columns to `examples/identifier-similarity/main.go`:**

    Locate the `algorithms` slice at lines 87-105 of `examples/identifier-similarity/main.go`. Append 5 new entries after `{"Tversky", func(a, b string) float64 { return fuzzymatch.TverskyScore(a, b, 3, 1.0, 1.0) }}` (the existing line 104):

    ```go
    {"TokenSort", fuzzymatch.TokenSortRatioScore},
    {"TokenSet", fuzzymatch.TokenSetRatioScore},
    {"Partial", fuzzymatch.PartialRatioScore}, // byte path; rune path not exposed in the example for column consistency
    {"TokenJac", fuzzymatch.TokenJaccardScore},
    {"MongeElk", func(a, b string) float64 { return fuzzymatch.MongeElkanScoreSymmetric(a, b, fuzzymatch.AlgoJaroWinkler, fuzzymatch.DefaultNormalisationOptions()) }}, // matches CONTEXT §4 LOCKED dispatch wrapper
    ```

    All 5 column labels are ≤ 13 chars (matching the existing `algoWidth = 13` constant — no width adjustment needed). The total slice grows from 14 to 19 entries.

    Verify `go run ./examples/identifier-similarity` runs to completion locally and the rendered table includes the 5 new columns with correctly-aligned scores.

    **Step 3 — Update `examples/identifier-similarity/main_test.go`:**

    Read the existing test file. If it has any of the following assertions, update them to reflect the 19-column shape:
    - Column-count assertion (e.g. `if len(algorithms) != 14 { ... }` → change to `19`)
    - Golden-output check (if any — the example's stdout is compared against a fixture; regenerate the fixture with the 5 new columns)
    - Specific column-label expectation (if any names are pinned in test fixtures, add the 5 new labels)

    If no test changes are needed (the test is purely a smoke test that runs `main()` and asserts no panic), no modification is required — just verify `go test ./examples/identifier-similarity/...` exits 0.

    **Step 4 — Append ≥ 2 new cross-algorithm consistency tests to `cross_algorithm_consistency_test.go`:**

    Read the existing file for the test pattern. Append a new section `// --- Phase 6 cross-algorithm invariants (plan 06-06)` divider followed by ≥ 2 tests:

    1. **`TestCrossAlgo_TokenJaccard_VsQGramJaccard_SetVsMultisetDivergence`**: Pin the LOCKED set-vs-multiset semantic divergence (per plan 06-04 SUMMARY's "Set vs Multiset LOCKED" record). The test computes `tokenJaccard := TokenJaccardScore("a a b", "a b")` AND `qgramJaccard := QGramJaccardScore("a a b", "a b", 1)` and asserts:
       - `tokenJaccard == 1.0` (set semantics — left-side tokens deduplicate to {a, b}; intersection 2, union 2)
       - `qgramJaccard < 1.0` (multiset semantics — left-side q-grams have count "a":2, "b":1; right has "a":1, "b":1; intersection cardinality differs from union)
       - The test docstring cites plan 06-04's "Set vs Multiset LOCKED" record and RESEARCH.md Pattern 8

    2. **`TestCrossAlgo_TokenSetRatio_EmptyDeviation_PinnedAgainstTokenJaccard`**: Pin the LOCKED empty-set deviation (per plan 06-02 SUMMARY's "DEVIATION LOCKED" record). The test computes `tokenSet := TokenSetRatioScore("", "")` AND `tokenJaccard := TokenJaccardScore("", "")` and asserts:
       - `tokenSet == 0.0` (DEVIATION — RapidFuzz issue #110 / fuzzywuzzy compat)
       - `tokenJaccard == 1.0` (STANDARD catalogue convention — both-empty identity)
       - The test docstring cites plan 06-02's "DEVIATION LOCKED" record and RESEARCH.md Pitfall 2

    Optional third test (recommended): **`TestCrossAlgo_MongeElkanSymmetric_VsAsymmetric_Identity`**: Pin that for non-empty identical inputs, both variants return 1.0 (the symmetric variant doesn't degrade identity); for asymmetric pairs the symmetric variant returns the average of both directions per CONTEXT §4. Cites plan 06-05's "Asymmetric reference vector LOCKED" record.

    Optional fourth test: **`TestCrossAlgo_PartialRatio_VsRatcliffObershelp_DistinctSemantic`**: Pin that PartialRatio (Indel formula) and RatcliffObershelp (difflib semantic) produce DIFFERENT scores on at least one input where both are well-defined. Cites the Phase 4 RatcliffObershelp godoc (which already notes the difflib-vs-Indel distinction) and the Phase 6 PartialRatio godoc (which references RatcliffObershelp as the difflib-equivalent for consumers wanting that semantic).

    All cross-algorithm tests use `package fuzzymatch_test`, stdlib `testing` only (no testify per .claude/skills/go-coding-standards), `t.Errorf` / `t.Fatalf` for failure messages, float comparisons via `math.Abs(got-want) > 1e-9` per .claude/skills/go-testing-standards.
  </action>
  <verify>
    <automated>go test -run TestGolden_Algorithms_Merge -race -shuffle=on -count=1 ./... && go test -run TestCrossAlgo -race -shuffle=on -count=1 ./... && go test ./examples/identifier-similarity/... && go run ./examples/identifier-similarity > /dev/null && make verify-determinism</automated>
  </verify>
  <done>
    `algorithms.json` extended with all 5 Phase 6 algorithm entries (verified by `TestGolden_Algorithms_Merge` passing without `-update`); identifier-similarity example renders the 19-column table; `examples/identifier-similarity/main_test.go` (if any) updated for the column-count change; `cross_algorithm_consistency_test.go` has ≥ 2 new Phase 6 cross-algorithm tests pinning the LOCKED divergences (set-vs-multiset; empty-set deviation); `make verify-determinism` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full-replace bench.txt with regenerated benchmarks including all Phase 6 fixtures + verify all 5 TOKEN-01..05 commit-status check</name>
  <files>bench.txt</files>
  <read_first>
    - bench.txt (current Phase 5 baseline — note the file format is `go test -bench=. -benchmem -count=10` output; see Makefile `bench` target for the canonical generation command)
    - Makefile `bench` target (the canonical command for generating `bench.txt`)
    - .claude/skills/performance-standards/SKILL.md "Updating bench.txt" section (full-replace policy on tagged release boundaries; intentional performance improvements with rationale; never silently to mask a regression)
    - .planning/phases/06-token-based-algorithms/06-CONTEXT.md §6 (bench.txt full-replace finalisation pattern; includes the 3 LOCKED pathological fixtures)
    - The 3 pathological bench fixtures in plans 06-02 / 06-03 / 06-05:
      - `BenchmarkTokenSetRatio_Pathological_AsymmetricSetCardinalities` (plan 06-02)
      - `BenchmarkPartialRatio_Pathological_LongShortMismatch_Bytes` AND `BenchmarkPartialRatio_Pathological_LongShortMismatch_Runes` (plan 06-03 — both byte and rune surfaces)
      - `BenchmarkMongeElkan_Pathological_1000Tokens` (plan 06-05)
  </read_first>
  <behavior>
    - `bench.txt` is regenerated via `make bench` (or equivalent `go test -bench=. -benchmem -count=10 ./... > bench.txt`) and committed
    - The regenerated file contains entries for all Phase 6 algorithm benchmarks: `BenchmarkTokenSortRatioScore_*`, `BenchmarkTokenSetRatioScore_*`, `BenchmarkPartialRatioScore_*`, `BenchmarkPartialRatioScoreRunes_*`, `BenchmarkTokenJaccardScore_*`, `BenchmarkMongeElkanScore_*`, `BenchmarkMongeElkanScoreSymmetric_*` AT MINIMUM (ASCII Short / Medium / Long / Unicode Short for each)
    - The regenerated file contains entries for all 3 LOCKED pathological fixtures: `BenchmarkTokenSetRatio_Pathological_AsymmetricSetCardinalities`, `BenchmarkPartialRatio_Pathological_LongShortMismatch_Bytes`, `BenchmarkPartialRatio_Pathological_LongShortMismatch_Runes`, `BenchmarkMongeElkan_Pathological_1000Tokens`
    - Carry-forward Phase 1-5 benchmarks remain present (`BenchmarkLevenshteinScore_*`, `BenchmarkQGramJaccardScore_*`, etc.) — full-replace, NOT additive append
    - benchstat regression detection (run via `make bench-compare` if implemented, or skipped if the runner is not the self-hosted shared one) shows no > 10% regression on carry-forward benchmarks vs the Phase 5 baseline
    - Plan SUMMARY records "TOKEN-01..05 COMMITTED" with the specific commit hashes for plans 06-01..06-05 (or just notes that all 5 algorithms appear in the dispatch table, in algorithms.json, in the identifier-similarity example, in the staging-golden files, and in the bench.txt baseline)
  </behavior>
  <action>
    Run `make bench` (or the equivalent `go test -bench=. -benchmem -count=10 ./... > bench.txt` per .claude/skills/performance-standards) to regenerate `bench.txt` from a clean run. Verify the output file:
    - Contains entries for ALL Phase 6 benchmarks (search for `^BenchmarkTokenSortRatio`, `^BenchmarkTokenSetRatio`, `^BenchmarkPartialRatio`, `^BenchmarkTokenJaccard`, `^BenchmarkMongeElkan`)
    - Contains entries for ALL 3 LOCKED pathological fixtures (`grep -c '_Pathological_' bench.txt` returns ≥ 3 — exact count depends on the rune-path bench naming: BenchmarkPartialRatio_Pathological_LongShortMismatch_Bytes + _Runes count as 2, plus TokenSet + ME = 4 total)
    - Carry-forward Phase 1-5 entries are preserved (search for `^BenchmarkLevenshteinScore` and `^BenchmarkQGramJaccardScore` — both must appear)

    Note: if the local developer hardware is not the self-hosted shared bench runner, the absolute timing numbers will differ from CI. The full-replace policy (per .claude/skills/performance-standards "Updating bench.txt" section) is acceptable here because Phase 6 introduces 5 new algorithms that didn't exist in the Phase 5 baseline — the additive-append alternative would silently keep stale numbers for any benchmarks that were renamed or retired. Document this in plan SUMMARY: "bench.txt full-replaced on developer hardware; CI on the self-hosted runner will rebaseline on the next merge to main."

    Write a one-line check verifying all 5 algorithms are committed: search for each of `TokenSortRatioScore`, `TokenSetRatioScore`, `PartialRatioScore`, `TokenJaccardScore`, `MongeElkanScoreSymmetric` in dispatch (verified via `go test -run TestDispatch -race ./...` or by grepping `dispatch_*.go` files for the slot assignments). Confirm AlgoIDs() returns the 23-element list including all 5 Phase 6 entries (no enum reordering — verified by `algoid_test.go::TestAlgoIDs`).

    NO modifications to algorithm sources, no llms.txt / llms-full.txt changes (already done per-plan in 06-01..06-05).
  </action>
  <verify>
    <automated>test -f bench.txt && grep -q 'BenchmarkTokenSortRatioScore_' bench.txt && grep -q 'BenchmarkTokenSetRatio' bench.txt && grep -q 'BenchmarkPartialRatio' bench.txt && grep -q 'BenchmarkTokenJaccardScore_' bench.txt && grep -q 'BenchmarkMongeElkan' bench.txt && grep -q '_Pathological_' bench.txt && grep -q 'BenchmarkLevenshteinScore' bench.txt && grep -q 'BenchmarkQGramJaccardScore' bench.txt && go test -run TestAlgoIDs -race -count=1 ./...</automated>
  </verify>
  <done>
    `bench.txt` is regenerated and committed; contains all Phase 6 + 3 pathological fixtures + carry-forward Phase 1-5 benchmarks; benchstat regression check (if the runner supports it) shows no > 10% regression; plan SUMMARY records the 5 algorithms as committed and references their respective plan SUMMARYs (06-01 / 06-02 / 06-03 / 06-04 / 06-05); benchmark-runner-hardware caveat documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 6 finalisation has no security-relevant attack surface — pure-function library, no I/O, no goroutines, no untrusted-input crossing |

## STRIDE Threat Register

This plan is finalisation/wiring only — no new algorithm code is introduced. The DoS-T1 / DoS-T2 / DoS-T3 threats from plans 06-02 / 06-03 / 06-05 are mitigated by the bench-fixture baseline committed to `bench.txt` here, plus the godoc DoS notices already landed in the algorithm files.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | T (tampering — silent algorithms.json drift) | testdata/golden/algorithms.json out-of-sync with staging files | mitigate | `TestGolden_Algorithms_Merge` re-runs on every PR; the `-update` flag is required to regenerate, and the duplicate-Name sanity check at lines 195-198 fails on conflict. Cross-platform CI matrix verifies byte-identical diff across linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64. |
| T-06-06-02 | T (tampering — bench.txt regression masking) | benchstat regression > 10% silently merged | mitigate | benchstat regression detection runs in CI (per .claude/skills/performance-standards "Benchstat Regression Detection"); > 10% regression at p < 0.05 fails the build with the benchstat output. The full-replace policy here is acceptable because Phase 6 introduces 5 new algorithms; the baseline is freshly grounded in their actual numbers, not stale Phase 5 numbers. |
</threat_model>

<verification>
- `go test -run 'TestGolden_Algorithms_Merge|TestCrossAlgo|TestAlgoIDs' -race -shuffle=on -count=1 ./...` exits 0
- `go test ./examples/identifier-similarity/...` exits 0
- `go run ./examples/identifier-similarity > /dev/null` exits 0 (smoke test the rendered output)
- `make verify-determinism` exits 0 (golden file diff)
- `make fmt-check && make vet && make lint && make verify-license-headers && make verify-deps-allowlist` exit 0
- `make check` exits 0 (full quality gate including coverage floors)
- `bench.txt` exists and contains entries for all Phase 6 benchmarks + 3 pathological fixtures
</verification>

<success_criteria>
- `testdata/golden/algorithms.json` extended with all 5 Phase 6 algorithm entries; cross-platform diff green
- `examples/identifier-similarity/` rendered table extended from 14 to 19 columns
- `bench.txt` regenerated full-replace including all Phase 6 algorithm benchmarks + 3 LOCKED pathological fixtures (TokenSet asymmetric / PartialRatio long-short / MongeElkan 1000-tokens)
- `cross_algorithm_consistency_test.go` extended with ≥ 2 new Phase 6 cross-algorithm tests (set-vs-multiset divergence; empty-set deviation pin)
- All 5 TOKEN-01..05 requirement IDs verified as committed: each algorithm appears in dispatch[], in algorithms.json, in the identifier-similarity example, in the staging-golden files (per-plan), and in the bench.txt baseline
- NO modifications to llms.txt / llms-full.txt (per-plan sync was done in 06-01..06-05; this plan is finalisation-only)
- `make check` exits 0 (full quality gate green)
- Coverage floors maintained: ≥ 95% overall, ≥ 90% per file, 100% on public API surface
</success_criteria>

<output>
After completion, create `.planning/phases/06-token-based-algorithms/06-06-SUMMARY.md` documenting:
- File-by-file changes (5 staging-file paths appended; algorithms.json regenerated; identifier-similarity 14-to-19 column extension; bench.txt full-replace; ≥ 2 new cross-algorithm consistency tests)
- TOKEN-01..05 commit-status verification (each requirement ID confirmed as having all required artifacts in place)
- bench.txt full-replace caveat (developer hardware vs CI self-hosted runner)
- Phase 6 close-out: ROADMAP.md Phase 6 entry should be flipped to `[x]` (or whatever the project's phase-completion marker is — gsd-verify-work handles this); REQUIREMENTS.md TOKEN-01..05 entries should be flipped to `[x]` (or "Complete" in the traceability table)
- Phase 7 forward-compatibility note: when Phase 7 lands, planners ADD AlgoSoundex / AlgoDoubleMetaphone / AlgoNYSIIS / AlgoMRA to `permittedMongeElkanInner` (per plan 06-05 SUMMARY's Phase 7 forward-compatibility note) AND add 4 new staging-golden files + 4 new identifier-similarity columns + 4 new bench entries
</output>