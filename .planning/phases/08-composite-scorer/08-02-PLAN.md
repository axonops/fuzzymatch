---
type: execute
plan: 08-02
phase: 08
wave: 2
depends_on: [08-01]
files_created:
  - scorer.go
  - scorer_test.go
  - scorer_internal_test.go
files_modified:
  - llms.txt
  - llms-full.txt
autonomous: true
requirements: [SCORER-01, SCORER-03, SCORER-04, SCORER-06]
must_haves:
  truths:
    - "`NewScorer(opts ...ScorerOption) (*Scorer, error)` returns a fully-validated, immutable `*Scorer` or one of the documented sentinel errors"
    - "Validation pipeline fires in the LOCKED order: missing-threshold FIRST → empty-algorithms → invalid weight → invalid threshold → invalid AlgoID → q-gram-size/Tversky params (Pitfall 3)"
    - "Duplicate `WithAlgorithm(SameAlgoID, _)` calls follow last-write-wins; `WithoutAlgorithm(absent)` is a silent no-op (Pitfall 4)"
    - "Weights auto-normalise to sum-to-1 when `WithNormaliseWeights(true)` (default); raw weights preserved when explicitly `WithNormaliseWeights(false)`"
    - "`Scorer.Score(a, b string) float64` applies the float-determinism reduction `acc = acc + (entry.weight * score)` over AlgoID-sorted entries with optional pre-normalisation"
    - "`Scorer.Match(a, b string) bool` returns `Score(a, b) >= s.threshold`"
    - "Scorer struct fields are set once in NewScorer and never mutated; safe for concurrent use without locks (no mutex)"
    - "Float-determinism: no `math.Pow`/`math.Log`/`math.Exp`/`math.FMA`/parallel-reductions; only `+` and `*` on the composite path; explicit `(weight*score)+acc` parenthesisation"
    - "Map iteration order in any `ScoreAll`-like surface is documented non-deterministic; this plan does NOT introduce ScoreAll (lands in plan 08-03), but internal AlgoID-sorted iteration is established here"
    - "Zero runtime deps in root go.mod; no testify in root tests; no goroutines in library code; no init()"
  facts:
    - "scorer.go declares `type Scorer struct {` and `func NewScorer(opts ...ScorerOption) (*Scorer, error)` and methods `Score`, `Match` (Threshold + Algorithms accessors land in plan 08-03)"
    - "scorer_internal_test.go uses `package fuzzymatch` to verify last-write-wins and weight normalisation invariants against the unexported scorerConfig and Scorer internals"
    - "scorer_test.go uses `package fuzzymatch_test` for external happy-path + error-path + sentinel-discrimination coverage"
    - "go test -race -run TestNewScorer ./... exits 0 — covers SCORER-01, SCORER-04, SCORER-06"
estimated_effort: medium
---

<objective>
Land the `Scorer` type, the `NewScorer` validation pipeline, weight auto-normalisation, the
last-write-wins dedup semantics, the float-determinism composite reduction loop, and the
`Score` + `Match` methods.

This is the first executable Scorer surface. The validation pipeline order is locked from
CONTEXT.md §2 + Pitfall 3 (missing-threshold check fires FIRST). The reduction loop pattern
is locked from CONTEXT.md §5 (explicit `(weight * score) + acc` parens, AlgoID-sorted
iteration). This plan does NOT introduce `ScoreAll`, `Threshold()`, `Algorithms()`,
`DefaultScorer()`, or `DefaultScorerOptions()` — those land in plan 08-03.

Purpose: establish the validated immutable Scorer + the reduction loop. Once green, plans
08-03 (accessors + defaults + property/concurrent) and 08-04 (finalisation) build on top.

Output:
- `scorer.go` (new) — Scorer struct + NewScorer + Score + Match
- `scorer_test.go` (new) — external coverage of NewScorer error paths and Score/Match happy paths
- `scorer_internal_test.go` (new) — package-internal coverage of last-write-wins + weight normalisation
- `llms.txt`, `llms-full.txt` (modified) — sync new exported symbols
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-composite-scorer/08-CONTEXT.md
@.planning/phases/08-composite-scorer/08-RESEARCH.md
@.planning/phases/08-composite-scorer/08-PATTERNS.md
@.planning/phases/08-composite-scorer/08-VALIDATION.md
@.planning/phases/08-composite-scorer/08-01-SUMMARY.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/go-testing-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From plan 08-01 (now committed):
```
type ScorerOption func(*scorerConfig) error
type scorerEntry struct { id AlgoID; weight float64; scoreFn func(a, b string) float64 }
type scorerConfig struct {
    entries          []scorerEntry
    threshold        float64
    thresholdSet     bool
    normaliseWeights bool
    applyNorm        bool
    normOpts         NormalisationOptions
}
var ErrEmptyScorer, ErrInvalidWeight, ErrInvalidThreshold, ErrMissingThreshold error
```

From `algoid.go`:
```
type AlgoID int
const numAlgorithms = 23
var dispatch [numAlgorithms]func(a, b string) float64
func AlgoIDs() []AlgoID  // returns canonical iota order
```

From `normalise.go`:
```
func Normalise(s string, opts NormalisationOptions) string
type NormalisationOptions struct { ... }
```

From `cosine.go:341-344` (the LOAD-BEARING float-determinism precedent):
```
var dot float64
for _, k := range intersectionKeys {
    dot = (float64(qa[k]) * float64(qb[k])) + dot
}
```

This is the pattern `Scorer.Score`'s reduction loop mirrors verbatim (see CONTEXT.md §5 LOCKED).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare Scorer struct + NewScorer validation pipeline + weight normalisation + AlgoID-sorted entry slice</name>
  <files>scorer.go, scorer_internal_test.go, llms.txt, llms-full.txt</files>
  <read_first>
    - scorer_options.go (full file from plan 08-01) — must match scorerConfig field set exactly
    - errors.go (full file, ~120 lines after plan 08-01) — confirms the 10 sentinels available for NewScorer to return
    - algoid.go full file — AlgoID enum constants, dispatch array, numAlgorithms, AlgoIDs() canonical order
    - .planning/phases/08-composite-scorer/08-CONTEXT.md §2 (validation pipeline order LOCKED), §5 (float-determinism reduction LOCKED)
    - .planning/phases/08-composite-scorer/08-PATTERNS.md "scorer.go (service/type, composite reduction)" section (lines 36-117 of PATTERNS.md) — AlgoIDs() canonical-sort dedup pattern (lines 92-113 of PATTERNS.md)
    - .planning/phases/08-composite-scorer/08-RESEARCH.md "Pattern 1: `scorerEntry` Slice Design" (lines 196-234) — `seen := map[AlgoID]scorerEntry{}` dedup + `for _, id := range AlgoIDs()` sorted-merge
    - .planning/phases/08-composite-scorer/08-RESEARCH.md Pitfall 3, 4, 5 (validation order, no-op WithoutAlgorithm, double-normalisation)
  </read_first>
  <behavior>
    - `NewScorer()` (no options at all) returns `(nil, ErrMissingThreshold)` — missing-threshold check fires FIRST per Pitfall 3
    - `NewScorer(WithThreshold(0.5))` (threshold but no algorithms) returns `(nil, ErrEmptyScorer)`
    - `NewScorer(WithAlgorithm(AlgoLevenshtein, 1.0), WithThreshold(0.5))` returns `(*Scorer, nil)` with one entry
    - `NewScorer(WithAlgorithm(AlgoLevenshtein, 0.3), WithAlgorithm(AlgoLevenshtein, 0.7), WithThreshold(0.5))` returns a Scorer with ONE entry (last-write-wins; weight 0.7 raw, normalised to 1.0 since it's the only entry)
    - `NewScorer(WithAlgorithm(AlgoLevenshtein, 1.0), WithAlgorithm(AlgoJaroWinkler, 3.0), WithThreshold(0.5))` produces a Scorer whose entries have normalised weights summing to exactly 1.0 (Levenshtein 0.25, JaroWinkler 0.75) when `WithNormaliseWeights` defaults to true
    - `NewScorer(WithAlgorithm(AlgoLevenshtein, 1.0), WithAlgorithm(AlgoJaroWinkler, 3.0), WithThreshold(0.5), WithNormaliseWeights(false))` produces a Scorer whose entries have raw weights (1.0, 3.0) — no normalisation
    - Returned Scorer's internal entry slice is AlgoID-ascending (canonical iota order from AlgoIDs())
    - Default scorerConfig zero value MUST be initialised with `normaliseWeights = true` and `applyNorm = true` and `normOpts = DefaultNormalisationOptions()` so that options that don't touch normalisation produce the documented default behaviour (per spec §9.4)
    - Validation order: pre-process options into a `scorerConfig` first; if any option returns an error, NewScorer returns (nil, that error) immediately; THEN check thresholdSet (ErrMissingThreshold), THEN check len(entries)==0 (ErrEmptyScorer); then per-entry AlgoID bounds + dispatch nil-check (defensive — should already be caught at option-time but re-check); then normalise weights
  </behavior>
  <action>
    Create `scorer.go` with the Apache 2.0 file header. Add imports: `"sort"` only (no math, no third-party).

    Declare `type Scorer struct {` with unexported fields:
    - `algorithmsAlgoIDSorted []scorerEntry` — final dedup'd + AlgoID-sorted slice
    - `threshold float64`
    - `applyNormalisation bool`
    - `normaliseOpts NormalisationOptions`

    Implement `func NewScorer(opts ...ScorerOption) (*Scorer, error)` with the LOCKED pipeline:

    1. Initialise `cfg := scorerConfig{normaliseWeights: true, applyNorm: true, normOpts: DefaultNormalisationOptions()}` (defaults BEFORE applying options so options that don't touch normalisation produce the documented default)
    2. Loop `for _, opt := range opts { if err := opt(&cfg); err != nil { return nil, err } }`
    3. If `!cfg.thresholdSet` → `return nil, ErrMissingThreshold` (FIRST gate per CONTEXT.md §2 + Pitfall 3)
    4. If `len(cfg.entries) == 0` → `return nil, ErrEmptyScorer`
    5. Defensive per-entry bounds re-check: for each `e := range cfg.entries`, verify `int(e.id) >= 0 && int(e.id) < numAlgorithms && dispatch[e.id] != nil` → else `ErrInvalidAlgorithm` (defense in depth — option-level check should catch this, but the Scorer guarantees the invariant)
    6. Dedup via `seen := make(map[AlgoID]scorerEntry, len(cfg.entries))`; iterate `cfg.entries` in order, assigning `seen[e.id] = e` (last-write-wins per Pitfall 4)
    7. Build AlgoID-sorted slice: `sorted := make([]scorerEntry, 0, len(seen))`; iterate `for _, id := range AlgoIDs() { if e, ok := seen[id]; ok { sorted = append(sorted, e) } }` — this guarantees AlgoID-ascending order matching canonical iota
    8. If `cfg.normaliseWeights == true`: compute `sum := 0.0` over `sorted` weights using `sum = sum + sorted[i].weight` (explicit left-to-right per DET-06); if `sum == 0` → `return nil, ErrInvalidWeight` (defence; should be unreachable after option validation); divide each entry's weight by sum: `sorted[i].weight = sorted[i].weight / sum`
    9. Construct and return `&Scorer{algorithmsAlgoIDSorted: sorted, threshold: cfg.threshold, applyNormalisation: cfg.applyNorm, normaliseOpts: cfg.normOpts}, nil`

    Every step's intent is documented inline with a brief comment citing CONTEXT.md §X or Pitfall N.

    Create `scorer_internal_test.go` with `package fuzzymatch` (internal — accesses unexported fields). Add:
    - `TestScorer_LastWriteWins` — calls `NewScorer(WithAlgorithm(AlgoLevenshtein, 0.3), WithAlgorithm(AlgoLevenshtein, 0.7), WithThreshold(0.5))`; asserts `len(s.algorithmsAlgoIDSorted) == 1`, `s.algorithmsAlgoIDSorted[0].id == AlgoLevenshtein`, `s.algorithmsAlgoIDSorted[0].weight == 1.0` (after normalisation of a single entry)
    - `TestScorer_WeightNormalisation_SumsToOne` — 2-algo scorer with raw weights 1.0 and 3.0; assert `s.algorithmsAlgoIDSorted[0].weight + s.algorithmsAlgoIDSorted[1].weight == 1.0` (use `math.Abs(sum - 1.0) < 1e-12` since reduction order may shift; OR assert exact 1.0 if computed left-to-right — pick exact and explain in comment)
    - `TestScorer_WeightNormalisation_DisabledRawPreserved` — same 2-algo, plus `WithNormaliseWeights(false)`; assert raw weights 1.0 and 3.0 preserved
    - `TestScorer_EntriesSorted_AlgoIDAscending` — 3-algo scorer with options in REVERSE AlgoID order; assert `s.algorithmsAlgoIDSorted` is in AlgoID-ascending order

    Update llms.txt + llms-full.txt: add `Scorer` (type), `NewScorer` (function). `Score` and `Match` land in Task 2 of this plan; sync them in that task.
  </action>
  <acceptance_criteria>
    - scorer.go contains `type Scorer struct {` (exported)
    - scorer.go contains `func NewScorer(opts ...ScorerOption) (*Scorer, error)`
    - scorer.go imports ONLY `"sort"` from stdlib (`grep -A5 "^import" scorer.go | grep -v "^import\|^)\|^(\|\".*\"" | wc -l` returns 0, or simpler: verify the import block contains only `"sort"`)
    - scorer.go has the Apache 2.0 license header (1 occurrence of "Licensed under the Apache License")
    - scorer.go has no `init()` function (`! grep -q "^func init" scorer.go`)
    - scorer.go has no `go ` keyword (no goroutines) — `! grep -q "^[[:space:]]*go [a-zA-Z]" scorer.go`
    - scorer.go has no usage of `math.Pow`, `math.Log`, `math.Exp`, `math.FMA` (`! grep -E "math\.(Pow|Log|Exp|FMA)" scorer.go`)
    - First check in NewScorer body is `!cfg.thresholdSet` (`grep -A2 "ErrMissingThreshold\|thresholdSet" scorer.go | head -20` shows the gate fires BEFORE any len(entries) check — verify by reading)
    - `go vet ./...` exits 0
    - `gofmt -l scorer.go scorer_internal_test.go` produces no diff
    - `go test -race -run "TestScorer_LastWriteWins|TestScorer_WeightNormalisation|TestScorer_EntriesSorted" ./...` exits 0
    - `go test -race -count=1 ./...` exits 0 (no regression in plan 08-01 tests)
    - llms.txt grows by 2 lines (Scorer + NewScorer)
  </acceptance_criteria>
  <verify>
    <automated>go test -race -run "TestScorer_LastWriteWins|TestScorer_WeightNormalisation|TestScorer_EntriesSorted|TestLLMSync" ./...</automated>
  </verify>
  <done>Scorer struct + NewScorer pipeline lands with locked validation order; dedup + AlgoID-sorted slice + weight normalisation invariants verified by internal tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Scorer.Score with float-determinism reduction loop + Scorer.Match threshold comparison + comprehensive external tests</name>
  <files>scorer.go, scorer_test.go, llms.txt, llms-full.txt</files>
  <read_first>
    - scorer.go (current state from Task 1) — append Score + Match methods at the bottom
    - cosine.go lines 280-360 — the load-bearing float-determinism precedent (lines 341-344 in particular). Note the documented FMA-on-arm64 caveat at lines 288-297.
    - normalise.go header + the Normalise function signature (single read)
    - .planning/phases/08-composite-scorer/08-CONTEXT.md §3 (normalisation flow LOCKED — pre-normalise once, pass na/nb to all algorithms) and §5 (reduction loop LOCKED)
    - .planning/phases/08-composite-scorer/08-PATTERNS.md "Core reduction loop pattern" + "Float-determinism reduction" shared pattern (lines 62-77 and 881-890 of PATTERNS.md)
    - .planning/phases/08-composite-scorer/08-RESEARCH.md Pitfall 1 (FMA fusion risk) — empirical observation that small-magnitude operations stay below byte-diff; mitigation only if golden fails
    - cosine_test.go (lines 1-60) — analog for stdlib testing patterns (no testify, t.Errorf/t.Fatalf with %g for floats)
  </read_first>
  <behavior>
    - `s.Score(a, b)` with `applyNormalisation=true` first computes `na := Normalise(a, s.normaliseOpts)` and `nb := Normalise(b, s.normaliseOpts)` ONCE before the reduction loop (pre-normalise at boundary per CONTEXT.md §3)
    - `s.Score(a, b)` with `applyNormalisation=false` uses raw `na := a, nb := b` (no normalisation; supports WithoutNormalisation semantic per spec §9.4)
    - Reduction loop iterates `s.algorithmsAlgoIDSorted` in slice order (already AlgoID-ascending from Task 1); accumulator is computed as `acc = acc + (entry.weight * score)` with EXPLICIT parens (verifies textually in the source code — grep can confirm)
    - `s.Score(x, x)` for non-empty x with default-normalisation Scorer composed of identity-preserving algorithms returns 1.0 exactly when each per-algorithm Score(x, x) returns 1.0 and weights sum to 1.0
    - `s.Score("", "")` behaviour is documented but not asserted exactly — defer the exact pin to plan 08-04's golden file
    - `s.Match(a, b)` returns `s.Score(a, b) >= s.threshold`
    - Concurrent calls to Score from multiple goroutines on the same `*Scorer` produce identical results (proven definitively by plan 08-03's concurrent test; this plan asserts byte-equal results across repeated single-thread calls as a deterministic-across-runs check)
  </behavior>
  <action>
    Append to `scorer.go`:

    `func (s *Scorer) Score(a, b string) float64` body:
    1. Declare `na, nb := a, b`
    2. `if s.applyNormalisation { na = Normalise(a, s.normaliseOpts); nb = Normalise(b, s.normaliseOpts) }` (single conditional, two Normalise calls)
    3. `var acc float64` (zero-value)
    4. Reduction loop: `for _, entry := range s.algorithmsAlgoIDSorted { score := entry.scoreFn(na, nb); acc = acc + (entry.weight * score) }` — the parens MUST be textually present in the source (`(entry.weight * score)`). Add an inline comment citing `cosine.go:343` as the canonical precedent and `// DET-06 explicit parens — see CONTEXT.md §5 LOCKED`.
    5. `return acc`

    `func (s *Scorer) Match(a, b string) bool` body:
    1. `return s.Score(a, b) >= s.threshold`

    Add godoc on both methods opening with the method name. `Score`'s godoc explicitly states (a) pre-normalisation policy (per CONTEXT.md §3), (b) determinism guarantee ("byte-identical across runs, processes, platforms"), (c) AlgoID-sorted internal iteration ("iteration order is fixed by AlgoID at construction time; does not depend on option order"), (d) NaN/Inf invariant ("guaranteed to return a value in `[0.0, 1.0]` when weights are normalised; raw-weights mode waives the upper bound"). `Match`'s godoc states that it uses `>= threshold` (boundary inclusive).

    Create `scorer_test.go` with `package fuzzymatch_test`. Stdlib testing only. Tests:
    - `TestNewScorer_MissingThreshold` — `NewScorer()` and `NewScorer(WithAlgorithm(AlgoLevenshtein, 1.0))` both return `(nil, ErrMissingThreshold)` discriminated via errors.Is
    - `TestNewScorer_EmptyScorer` — `NewScorer(WithThreshold(0.5))` returns `(nil, ErrEmptyScorer)`
    - `TestNewScorer_InvalidWeightPropagates` — `NewScorer(WithAlgorithm(AlgoLevenshtein, -0.5), WithThreshold(0.5))` returns `ErrInvalidWeight` (already covered at option-time by 08-01; this confirms the error propagates through NewScorer)
    - `TestScorer_Score_Identity` — single-Levenshtein Scorer with threshold 0.5; `s.Score("kitten", "kitten") == 1.0` exactly
    - `TestScorer_Score_AsciiPair` — single-Levenshtein Scorer; `s.Score("kitten", "sitting")` produces a known float matching `LevenshteinScore("kitten", "sitting")` exactly (composite of 1 algo with normalised weight 1.0)
    - `TestScorer_Score_DeterministicAcrossCalls` — same Scorer, 1000 calls with same inputs all return byte-identical float64 (`got == want` exactly; no tolerance)
    - `TestScorer_Match_ThresholdInclusive` — Score(a,b) == 0.5, threshold 0.5 → Match returns true (boundary inclusive). Construct a custom Scorer whose normalised single-algo score equals threshold for a specific input.
    - `TestScorer_Match_BelowThreshold` — Score < threshold → Match false
    - `TestScorer_WithoutNormalisation` — Scorer with `WithoutNormalisation()`; verify that `s.Score("XMLParser", "xml_parser")` differs from a default-normalisation Scorer's score on the same input (since raw bytes differ); also verify `s.Score("hello", "hello") == 1.0` (identity holds regardless of normalisation)

    No testify. Float compares use exact `==` for byte-determinism assertions; for cross-algorithm "is this in range" use `if got < 0.0 || got > 1.0 { t.Errorf(...) }`.

    Update llms.txt + llms-full.txt: add `Score` and `Match` as methods of Scorer.
  </action>
  <acceptance_criteria>
    - scorer.go contains `func (s *Scorer) Score(a, b string) float64`
    - scorer.go contains `func (s *Scorer) Match(a, b string) bool`
    - scorer.go reduction loop contains the EXACT substring `(entry.weight * score)` (parens textually present — `grep -F "(entry.weight * score)" scorer.go` returns 1+ matches)
    - scorer.go reduction loop contains the substring `acc = acc + ` (left-to-right additive accumulation)
    - scorer.go imports remain ONLY stdlib (`"sort"` from Task 1)
    - `go vet ./...` exits 0
    - `gofmt -l scorer.go scorer_test.go` produces no diff
    - `golangci-lint run ./...` exits 0 with no new findings on scorer.go or scorer_test.go
    - `go test -race -run "TestNewScorer|TestScorer_Score|TestScorer_Match" ./...` exits 0 with all subtests green
    - `go test -race -count=1 ./...` exits 0 — no regression in any prior phase test
    - `go test -race -count=10 -run TestScorer_Score_DeterministicAcrossCalls ./...` exits 0 — running 10 iterations confirms determinism
    - `! grep -E "math\.(Pow|Log|Exp|FMA)" scorer.go` (no forbidden transcendentals)
    - `! grep -E "^[[:space:]]*go [a-zA-Z]" scorer.go` (no goroutines)
    - llms.txt grows by 2 additional lines (Score, Match); `go test -run TestLLMSync ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test -race -count=1 ./...</automated>
  </verify>
  <done>Score + Match methods live with the locked reduction pattern; external test suite covers happy paths, threshold edge, WithoutNormalisation, and determinism across calls; full root suite green; ready for plan 08-03 accessors + property/concurrent tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer→NewScorer | Variadic ScorerOption slice enters the validation pipeline |
| consumer→Score/Match | Arbitrary string inputs (potentially adversarial UTF-8, very long, embedded nulls) enter the composite path through Normalise + dispatch[algo] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `NewScorer` weight normalisation | mitigate | Weights validated `> 0` at option-time (plan 08-01); defensive `sum == 0` guard inside NewScorer (Step 8) returns ErrInvalidWeight; `TestScorer_WeightNormalisation_SumsToOne` asserts exact 1.0 sum; `PropScorer_ScoreInRange` in plan 08-03 asserts [0,1] bound across arbitrary inputs |
| T-08-02 | DoS / Information Disclosure | `NewScorer` AlgoID bounds | mitigate | Defensive `int(e.id) >= 0 && int(e.id) < numAlgorithms && dispatch[e.id] != nil` check in NewScorer Step 5 returns `ErrInvalidAlgorithm` rather than reaching the `dispatch[e.id](na, nb)` call site where it would panic. Verified by `TestNewScorer_InvalidAlgoID` (covered by 08-01 already; reconfirmed here) |
| T-08-03 | DoS | `Scorer.Score`, `Scorer.Match` (adversarial string input) | mitigate | All 23 underlying algorithms have guaranteed linear/polynomial worst-case complexity (RE2-safe by design); `Normalise` handles invalid UTF-8 via `U+FFFD` replacement; existing fuzz harnesses (`*_fuzz_test.go`) assert no panics on arbitrary input; this plan adds no new fuzz target, but the reduction loop iterates only 1-6 entries so the Scorer cannot amplify per-algorithm worst-case more than 6×. `PropScorer_NoNaN` / `PropScorer_NoInf` land in plan 08-03 |
</threat_model>

<verification>
- `go test -race -count=1 ./...` — full root module suite green
- `go vet ./...` exits 0
- `gofmt -l scorer.go scorer_test.go scorer_internal_test.go` produces no diff
- `golangci-lint run ./...` exits 0
- `grep -F "(entry.weight * score)" scorer.go` returns 1+ matches (explicit parens textually present per CONTEXT.md §5)
- `grep -F "acc = acc +" scorer.go` returns 1+ matches (left-to-right accumulation)
- `! grep -E "math\.(Pow|Log|Exp|FMA)" scorer.go`
- `! grep -q "^[[:space:]]*go [a-zA-Z]" scorer.go scorer_test.go scorer_internal_test.go`
- `! grep -q "testify" scorer.go scorer_test.go scorer_internal_test.go`
- `! grep -q "^require " go.mod` (root go.mod still has zero non-stdlib requires)
- `go test -race -count=10 -run TestScorer_Score_DeterministicAcrossCalls ./...` confirms determinism over repeated runs
- llms.txt sync verified by `go test -run TestLLMSync ./...`
</verification>

<success_criteria>
- Scorer type exported with NewScorer / Score / Match (Threshold + Algorithms + ScoreAll + DefaultScorer + DefaultScorerOptions land in plan 08-03)
- Validation pipeline order matches CONTEXT.md §2 + Pitfall 3 exactly: missing-threshold FIRST → empty → invalid weight → invalid threshold → invalid AlgoID → params
- Last-write-wins for duplicate `WithAlgorithm(SameID, _)` proven by TestScorer_LastWriteWins
- `WithoutAlgorithm` no-op on absent ID covered indirectly (the option itself is unit-tested in plan 08-01)
- Weight auto-normalisation sums to 1.0 (when enabled); raw weights preserved when disabled
- Float-determinism reduction loop uses explicit `(entry.weight * score) + acc` parenthesisation
- No goroutines, no init(), no testify in root tests, no math.Pow/Log/Exp/FMA
- Plan 08-03 can build ScoreAll + accessors + property tests on top of a working Score
</success_criteria>

<output>
After completion, create `.planning/phases/08-composite-scorer/08-02-SUMMARY.md` documenting:
- Final Scorer struct field set + final NewScorer step ordering
- The reduction loop's textual form (copy-paste from scorer.go)
- Weight normalisation correctness proof (exact 1.0 sum vs tolerance) — record which the implementation chose
- Any deviation from CONTEXT.md / RESEARCH.md and rationale
- Note for plan 08-03: ScoreAll, Threshold(), Algorithms(), DefaultScorer(), DefaultScorerOptions() are NOT yet implemented; the property + concurrent test scaffolding will validate the Score/Match surface end-to-end
</output>
