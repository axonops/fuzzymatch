---
type: execute
plan: 08-01
phase: 08
wave: 1
depends_on: []
files_created:
  - scorer_options.go
  - scorer_options_test.go
files_modified:
  - errors.go
  - llms.txt
  - llms-full.txt
autonomous: true
requirements: [SCORER-01, SCORER-03, SCORER-08]
must_haves:
  truths:
    - "Four new sentinel errors exist as package-level vars in errors.go and follow the existing godoc + `Discriminate via errors.Is` pattern"
    - "`ScorerOption func(*scorerConfig) error` is exported; the unexported `scorerConfig` struct accumulates state; no `Scorer` type or methods exist yet (those land in plan 08-02)"
    - "Every option function validates inputs at option-application time and returns a sentinel error on failure (ErrInvalidWeight, ErrInvalidQGramSize, ErrInvalidTverskyParam, ErrInvalidThreshold, ErrInvalidAlgorithm)"
    - "Zero runtime deps added: errors.go and scorer_options.go use stdlib only; root go.mod still has only `golang.org/x/text`"
    - "No goroutines, no init(), no testify in scorer_options_test.go (stdlib `testing` only)"
    - "llms.txt + llms-full.txt extended with every new exported symbol introduced in this plan, in lockstep"
  facts:
    - "errors.go grows by 4 var declarations: ErrEmptyScorer, ErrInvalidWeight, ErrInvalidThreshold, ErrMissingThreshold (each with the standard godoc block ending in `Discriminate via errors.Is(err, fuzzymatch.ErrX); never match the error message string.`)"
    - "scorer_options.go declares the public types `ScorerOption`, the unexported `scorerConfig`, and 12 public option functions per CONTEXT.md §4 (WithAlgorithm, WithoutAlgorithm, WithQGramJaccardAlgorithm, WithSorensenDiceAlgorithm, WithCosineAlgorithm, WithTverskyAlgorithm, WithMongeElkanAlgorithm, WithSmithWatermanGotohAlgorithm, WithNormalisation, WithoutNormalisation, WithThreshold, WithNormaliseWeights)"
    - "scorer_options_test.go runs `go test -run TestWith ./...` green covering every option's happy path + every error path"
estimated_effort: medium
---

<objective>
Land the Phase 8 foundation: the four new sentinel errors and the complete functional-options
surface (`ScorerOption` + `scorerConfig` + all 12 `With*` functions). NO `Scorer` type or
methods are introduced here — options merely accumulate state into `scorerConfig`. This plan
is the substrate that plan 08-02's `NewScorer` validation pipeline consumes.

Purpose: separate the option/error surface (mechanical, table-driven, fully unit-testable in
isolation) from the constructor/Score reduction (algorithmic, dependent on dispatch[] and
Normalise wiring). Keeping these in separate plans means plan 08-02 can start with the
options + errors as a frozen contract.

Output:
- `scorer_options.go` (new) — type + struct + 12 option functions
- `scorer_options_test.go` (new) — happy + error path coverage per option
- `errors.go` (modified) — append 4 new sentinel errors
- `llms.txt`, `llms-full.txt` (modified) — sync new exported symbols
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-composite-scorer/08-CONTEXT.md
@.planning/phases/08-composite-scorer/08-RESEARCH.md
@.planning/phases/08-composite-scorer/08-PATTERNS.md
@.planning/phases/08-composite-scorer/08-VALIDATION.md
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/go-testing-standards/SKILL.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From `errors.go` (existing sentinel godoc pattern, lines 48-107):
```
var ErrInvalidInput = errors.New("fuzzymatch: invalid input")
// preceded by 9-line godoc block ending: "Discriminate via errors.Is(err, fuzzymatch.ErrInvalidInput); never match the error message string."
var ErrInvalidQGramSize = errors.New("fuzzymatch: invalid q-gram size")
var ErrInvalidTverskyParam = errors.New("fuzzymatch: invalid tversky parameter")
var ErrInvalidAlgorithm = errors.New("fuzzymatch: invalid algorithm")
```

From `algoid.go`:
```
type AlgoID int
const numAlgorithms = 23
var dispatch [numAlgorithms]func(a, b string) float64
func AlgoIDs() []AlgoID { /* canonical iota order */ }
// Public constants AlgoLevenshtein, AlgoDamerauLevenshteinOSA, AlgoCosine, AlgoTversky,
// AlgoMongeElkan, AlgoSmithWatermanGotoh, AlgoSoundex, ... AlgoMRA (lines 47-176).
```

From `normalise.go`:
```
type NormalisationOptions struct { Lowercase bool; StripSeparators bool; SeparatorChars string; SplitCamelCase bool }
func DefaultNormalisationOptions() NormalisationOptions
func Normalise(s string, opts NormalisationOptions) string
```

From `swg.go`:
```
type SWGParams struct { MatchScore, MismatchScore, GapOpen, GapExtend float64 }
func NewSWGParams() SWGParams  // returns defaults
```

From `dispatch_cosine.go:41-46` (closure-capture analog pattern):
```
var _ = func() bool {
    dispatch[AlgoCosine] = func(a, b string) float64 { return CosineScore(a, b, 3) }
    return true
}()
```

The 12 option functions to declare in `scorer_options.go` (per CONTEXT.md §4 + §3):
- WithAlgorithm(algo AlgoID, weight float64) ScorerOption
- WithoutAlgorithm(id AlgoID) ScorerOption                          // silent no-op if absent (Pitfall 4)
- WithQGramJaccardAlgorithm(weight float64, n int) ScorerOption
- WithSorensenDiceAlgorithm(weight float64, n int) ScorerOption
- WithCosineAlgorithm(weight float64, n int) ScorerOption
- WithTverskyAlgorithm(weight, alpha, beta float64, n int) ScorerOption
- WithMongeElkanAlgorithm(weight float64, inner AlgoID) ScorerOption
- WithSmithWatermanGotohAlgorithm(weight float64, params SWGParams) ScorerOption
- WithNormalisation(opts NormalisationOptions) ScorerOption          // applyNorm=true; stores opts
- WithoutNormalisation() ScorerOption                                // applyNorm=false
- WithThreshold(t float64) ScorerOption                              // validates t ∈ [0.0, 1.0], sets thresholdSet=true
- WithNormaliseWeights(normalise bool) ScorerOption                  // default true at NewScorer time
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append four sentinel errors to errors.go and sync llms.txt/llms-full.txt</name>
  <files>errors.go, errors_test.go, llms.txt, llms-full.txt</files>
  <read_first>
    - errors.go (full file, lines 1-107) — see the existing godoc block + var-decl pattern; new sentinels MUST mirror it exactly
    - .planning/phases/08-composite-scorer/08-CONTEXT.md §2 (ErrMissingThreshold rationale and godoc paragraph)
    - .planning/phases/08-composite-scorer/08-PATTERNS.md "errors.go (append 4 new sentinels)" section (lines 206-262 of PATTERNS.md) — pattern to copy verbatim
    - llms.txt (full file, < 200 lines) — see existing entries and add 4 new lines under Errors
    - llms-full.txt (header + Errors section) — append full godoc blocks
  </read_first>
  <behavior>
    - errors.Is(ErrEmptyScorer, ErrEmptyScorer) == true (canonical sentinel identity)
    - errors.Is(ErrInvalidWeight, ErrInvalidWeight) == true
    - errors.Is(ErrInvalidThreshold, ErrInvalidThreshold) == true
    - errors.Is(ErrMissingThreshold, ErrMissingThreshold) == true
    - All four sentinels have non-empty message strings starting with the lowercase prefix `fuzzymatch:` and no trailing punctuation
    - ErrEmptyScorer.Error() == "fuzzymatch: scorer has no algorithms (pass at least one WithAlgorithm option or use DefaultScorer)"
    - ErrInvalidWeight.Error() == "fuzzymatch: invalid algorithm weight (must be > 0)"
    - ErrInvalidThreshold.Error() == "fuzzymatch: invalid threshold (must be in [0.0, 1.0])"
    - ErrMissingThreshold.Error() == "fuzzymatch: scorer threshold required (pass WithThreshold or use DefaultScorer)"
  </behavior>
  <action>
    Append four `var ErrXxx = errors.New("fuzzymatch: ...")` declarations to errors.go after the existing `ErrEmptyInput` block (current line 107). Each declaration MUST be preceded by a godoc block in the exact form of the existing sentinels (`errors.go:39-47` for ErrInvalidInput is the canonical model): description sentence(s), blank doc line, blank doc line, the closing `Discriminate via errors.Is(err, fuzzymatch.ErrXxx); never match the error message string.` line. For ErrMissingThreshold specifically, use the godoc paragraph from CONTEXT.md §2 verbatim (mentions the calibration-parameter rationale and points users at DefaultScorer / WithThreshold). Do NOT add a new import — `errors` is already imported.

    Create `errors_test.go` (if not already present in the codebase) OR add to the existing errors_test.go (use Bash `ls errors_test.go` to check). Add a single table-driven test `TestSentinels_Identity` that iterates the four new sentinels and asserts `errors.Is(sentinel, sentinel)` and that `sentinel.Error()` starts with the prefix `fuzzymatch:`.

    Update `llms.txt`: add four entries in the existing Errors section, formatted to match existing rows (e.g. `var ErrEmptyScorer error`). Update `llms-full.txt`: append the full godoc blocks for the four new sentinels in their own subsection under Errors.

    No fenced code blocks in this task action — the godoc prose lives in errors.go itself per the analog pattern.
  </action>
  <acceptance_criteria>
    - errors.go contains `var ErrEmptyScorer = errors.New(` on an appended line
    - errors.go contains `var ErrInvalidWeight = errors.New(` on an appended line
    - errors.go contains `var ErrInvalidThreshold = errors.New(` on an appended line
    - errors.go contains `var ErrMissingThreshold = errors.New(` on an appended line
    - Each new var declaration is preceded by a `// Discriminate via errors.Is(err, fuzzymatch.Err` line (verify with: `grep -B1 "var ErrEmptyScorer\|var ErrInvalidWeight\|var ErrInvalidThreshold\|var ErrMissingThreshold" errors.go | grep -c "Discriminate"` returns 4)
    - `go vet ./...` exits 0
    - `go test -run TestSentinels ./...` exits 0
    - `grep -c "^var Err" errors.go` returns 10 (6 existing + 4 new)
    - llms.txt contains the four new error symbol names (`grep -c "ErrEmptyScorer\|ErrInvalidWeight\|ErrInvalidThreshold\|ErrMissingThreshold" llms.txt` returns ≥ 4)
    - llms-full.txt contains the same four symbols with godoc paragraphs (`grep -c "ErrEmptyScorer\|ErrInvalidWeight\|ErrInvalidThreshold\|ErrMissingThreshold" llms-full.txt` returns ≥ 4)
    - `go test -run TestLLMSync ./...` exits 0 (existing `ai_friendly_test.go` meta-test passes — confirms ast scan of all exported symbols matches llms.txt entries)
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestSentinels|TestLLMSync" -race</automated>
  </verify>
  <done>Four new sentinels appended to errors.go with godoc parity to existing sentinels; TestSentinels_Identity green; llms.txt + llms-full.txt synced; existing meta-test confirms ast/llms consistency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Declare ScorerOption + scorerConfig + scorerEntry in scorer_options.go and the 8 non-parameterised/normalisation/threshold/weight-control options</name>
  <files>scorer_options.go, scorer_options_test.go, llms.txt, llms-full.txt</files>
  <read_first>
    - .planning/phases/08-composite-scorer/08-CONTEXT.md §1, §3, §4 (option list)
    - .planning/phases/08-composite-scorer/08-PATTERNS.md "scorer_options.go (config/builder, option accumulation)" section (lines 119-202 of PATTERNS.md) — closure capture pattern from dispatch_cosine.go:41-46
    - .planning/phases/08-composite-scorer/08-RESEARCH.md "Pattern 4: dispatch[] Access for Non-Parameterised `WithAlgorithm`" (lines 298-321 of RESEARCH.md)
    - algoid.go lines 1-330 (full file) — read once to absorb: AlgoID enum, dispatch array, numAlgorithms constant, AlgoIDs() canonical order
    - normalise.go header + DefaultNormalisationOptions definition (single read; extract the NormalisationOptions struct shape)
    - dispatch_cosine.go (full file, <50 lines) — canonical closure-capture analog
    - errors.go (just-extended; confirms the 4 new sentinels are available for return)
  </read_first>
  <behavior>
    - `WithAlgorithm(AlgoLevenshtein, 0.5)` produces an option that, when applied to a fresh `scorerConfig`, appends an entry with `id=AlgoLevenshtein, weight=0.5, scoreFn=dispatch[AlgoLevenshtein]`
    - `WithAlgorithm(AlgoLevenshtein, 0)` returns ErrInvalidWeight (weight must be strictly > 0)
    - `WithAlgorithm(AlgoLevenshtein, -0.1)` returns ErrInvalidWeight
    - `WithAlgorithm(AlgoID(999), 1.0)` returns ErrInvalidAlgorithm (out-of-range AlgoID)
    - `WithoutAlgorithm(AlgoLevenshtein)` applied to a fresh `scorerConfig` (no matching entry) returns nil silently — no-op (Pitfall 4 from RESEARCH.md)
    - `WithoutAlgorithm(AlgoLevenshtein)` applied after `WithAlgorithm(AlgoLevenshtein, 0.5)` removes the entry (cfg.entries length goes from 1 → 0)
    - `WithThreshold(0.5)` sets `cfg.threshold = 0.5` AND `cfg.thresholdSet = true`
    - `WithThreshold(-0.1)` returns ErrInvalidThreshold
    - `WithThreshold(1.5)` returns ErrInvalidThreshold
    - `WithThreshold(0.0)` is accepted (boundary inclusive)
    - `WithThreshold(1.0)` is accepted (boundary inclusive)
    - `WithNormalisation(opts)` sets `cfg.applyNorm = true` AND stores `cfg.normOpts = opts`
    - `WithoutNormalisation()` sets `cfg.applyNorm = false` (does not touch normOpts)
    - `WithNormaliseWeights(true)` sets `cfg.normaliseWeights = true`; `WithNormaliseWeights(false)` sets it false
    - Default value of `cfg.normaliseWeights` is `true` when zero-value `scorerConfig{}` is initialised by NewScorer (plan 08-02) — but in this plan, document the semantic via the option's godoc
  </behavior>
  <action>
    Create `scorer_options.go` with the Apache 2.0 file header (copy from any existing .go file; see PATTERNS.md "Shared Patterns / File header" section). Declare:

    1. Public type `ScorerOption func(*scorerConfig) error`
    2. Unexported struct `scorerEntry` with fields `id AlgoID`, `weight float64`, `scoreFn func(a, b string) float64`
    3. Unexported struct `scorerConfig` with fields: `entries []scorerEntry`, `threshold float64`, `thresholdSet bool`, `normaliseWeights bool`, `applyNorm bool`, `normOpts NormalisationOptions`

    Implement 6 option functions in this task (the parameterised algorithm options land in Task 3):
    - `WithAlgorithm(algo AlgoID, weight float64) ScorerOption` — validates `weight > 0` (else ErrInvalidWeight); validates `int(algo) >= 0 && int(algo) < numAlgorithms && dispatch[algo] != nil` (else ErrInvalidAlgorithm); appends `scorerEntry{id: algo, weight: weight, scoreFn: dispatch[algo]}` to `cfg.entries`
    - `WithoutAlgorithm(id AlgoID) ScorerOption` — linear scan of `cfg.entries`; if found at index `i`, remove via `cfg.entries = append(cfg.entries[:i], cfg.entries[i+1:]...)`; if not found, return nil (silent no-op per Pitfall 4)
    - `WithNormalisation(opts NormalisationOptions) ScorerOption` — sets `cfg.applyNorm = true`, `cfg.normOpts = opts`
    - `WithoutNormalisation() ScorerOption` — sets `cfg.applyNorm = false`
    - `WithThreshold(t float64) ScorerOption` — validates `t >= 0.0 && t <= 1.0` (inclusive boundaries — else ErrInvalidThreshold); sets `cfg.threshold = t` AND `cfg.thresholdSet = true`
    - `WithNormaliseWeights(normalise bool) ScorerOption` — sets `cfg.normaliseWeights = normalise`

    Every option function has a godoc block opening with the function name. `WithoutAlgorithm`'s godoc MUST explicitly document the silent-no-op-on-absent behaviour ("If id is not currently in the option slice, returns nil without error — enabling `append(DefaultScorerOptions(), WithoutAlgorithm(X))...` composition") per CONTEXT.md §1. `WithThreshold`'s godoc MUST reference ErrMissingThreshold ("If you do not pass this option, NewScorer returns ErrMissingThreshold.")

    Use the standard import order: stdlib first (here: no imports needed if all types are package-local — verify). If `NormalisationOptions` requires the package-local reference, no import is needed (same package). No `math`, no `sort`, no third-party imports in this file.

    Create `scorer_options_test.go` with `package fuzzymatch_test`. Add table-driven tests:
    - `TestWithAlgorithm_HappyPath` — applies WithAlgorithm with valid (algo, weight) and asserts cfg.entries length 1, entry id/weight match (requires the test to construct a fresh scorerConfig via the option's apply — since scorerConfig is unexported, the test asserts indirectly by calling `NewScorer(opt, WithThreshold(0.5))` in plan 08-02. For this plan, the unit test invokes the option function directly via a per-package test helper OR the test uses `_internal_test.go` package access. Simpler approach: add a thin exported helper `applyOptionsForTest(opts ...ScorerOption) ([]scorerEntry, error)` in `scorer_internal_test.go` for plan 08-02, and for plan 08-01 the tests assert error-return values only — happy path is exercised end-to-end in plan 08-02. Use this latter approach.)
    - For this plan's `scorer_options_test.go`: tests assert error-return values from option application by constructing a `scorerConfig` via a package-internal helper. To stay within `package fuzzymatch_test`, the test calls each option `o := WithAlgorithm(...)` and inspects the closure's return via running it against an unexported-but-test-accessible probe. SIMPLEST PATH: also add a tiny internal-test file `scorer_options_internal_test.go` with `package fuzzymatch` that exposes a probe function `applyOptionForProbe(opt ScorerOption) (scorerConfig, error)`; the `package fuzzymatch_test` tests can then run external probes via this. (Document this choice in the file header so the reviewer sees the rationale.)

    Add `TestWithAlgorithm_InvalidWeight` (weight ≤ 0), `TestWithAlgorithm_InvalidAlgoID` (AlgoID(999)), `TestWithoutAlgorithm_NoOpOnAbsent`, `TestWithoutAlgorithm_RemovesPresent`, `TestWithThreshold_Inclusive` (0.0 and 1.0 both pass), `TestWithThreshold_OutOfRange` (-0.1 and 1.5 both fail with ErrInvalidThreshold), `TestWithNormalisation_StoresOpts`, `TestWithoutNormalisation_SetsFlag`, `TestWithNormaliseWeights_StoresValue`. Every test uses `errors.Is(err, fuzzymatch.ErrXxx)` for error discrimination — never string match.

    Update llms.txt: add lines for ScorerOption, scorerConfig is unexported (skip), and the 6 option functions covered in this task. Update llms-full.txt with full godoc.
  </action>
  <acceptance_criteria>
    - scorer_options.go contains `type ScorerOption func(*scorerConfig) error`
    - scorer_options.go contains `type scorerConfig struct {` (unexported)
    - scorer_options.go contains `type scorerEntry struct {` (unexported)
    - scorer_options.go contains exactly these public function signatures (verify with grep): `func WithAlgorithm(algo AlgoID, weight float64) ScorerOption`, `func WithoutAlgorithm(id AlgoID) ScorerOption`, `func WithNormalisation(opts NormalisationOptions) ScorerOption`, `func WithoutNormalisation() ScorerOption`, `func WithThreshold(t float64) ScorerOption`, `func WithNormaliseWeights(normalise bool) ScorerOption`
    - scorer_options.go has zero `import` of any non-stdlib path
    - scorer_options.go has no `init()` function
    - scorer_options.go has the Apache 2.0 license header (`grep -c "Licensed under the Apache License" scorer_options.go` returns 1)
    - `go vet ./...` exits 0
    - `go test -race -run "TestWithAlgorithm|TestWithoutAlgorithm|TestWithThreshold|TestWithNormalisation|TestWithoutNormalisation|TestWithNormaliseWeights" ./...` exits 0 with all tests green
    - Probe helper file (if used) is named `scorer_options_internal_test.go` with `package fuzzymatch` and contains `func applyOptionForProbe(`
    - llms.txt grows by exactly 7 new symbol lines (ScorerOption + 6 option functions); llms-full.txt grows with full godoc blocks
    - `go test -run TestLLMSync ./...` exits 0 (existing meta-test passes after the new symbols are added)
    - No `_test.go` file imports `github.com/stretchr/testify` (`grep -rn "testify" *_test.go scorer_options_internal_test.go 2>/dev/null` returns nothing)
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestWithAlgorithm|TestWithoutAlgorithm|TestWithThreshold|TestWithNormalisation|TestWithoutNormalisation|TestWithNormaliseWeights|TestLLMSync" -race</automated>
  </verify>
  <done>scorer_options.go declares ScorerOption + scorerConfig + scorerEntry + 6 non-parameterised option functions; scorer_options_test.go covers happy + error paths for each; probe helper enables `package fuzzymatch_test` to assert error behaviour; llms.txt and llms-full.txt synced; all gates green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the 6 parameterised algorithm option functions</name>
  <files>scorer_options.go, scorer_options_test.go, llms.txt, llms-full.txt</files>
  <read_first>
    - scorer_options.go (current state from Task 2) — read once; new functions appended at the bottom following the same style
    - .planning/phases/08-composite-scorer/08-PATTERNS.md "scorer_options.go (config/builder, option accumulation)" section — parameterised closure pattern from dispatch_cosine.go:41-46 and dispatch_monge_elkan.go:68-73
    - .planning/phases/08-composite-scorer/08-RESEARCH.md "Pattern 3: Parameterised Closure Capture" (lines 254-294 of RESEARCH.md)
    - dispatch_monge_elkan.go (full file, <80 lines) — confirms MongeElkanScoreSymmetric signature and JaroWinkler-default precedent
    - swg.go lines 90-140 — confirms `type SWGParams struct {` exact name and `NewSWGParams()` returning a value (use `SWGParams` as the param type, NOT a pointer)
    - cosine.go lines 1-50 + monge_elkan.go lines 370-410 — confirm the public `CosineScore(a, b, n)`, `QGramJaccardScore(a, b, n)`, `SorensenDiceScore(a, b, n)`, `TverskyScore(a, b, alpha, beta, n)`, `MongeElkanScoreSymmetric(a, b, inner, opts)`, `SmithWatermanGotohScore(a, b, params)` signatures (the underlying functions that the option closures call)
  </read_first>
  <behavior>
    - `WithQGramJaccardAlgorithm(0.5, 3)` produces an option storing `scoreFn: func(a, b string) float64 { return QGramJaccardScore(a, b, 3) }` with `id: AlgoQGramJaccard, weight: 0.5`
    - `WithQGramJaccardAlgorithm(_, 0)` returns ErrInvalidQGramSize (n must be ≥ 1)
    - `WithQGramJaccardAlgorithm(_, -1)` returns ErrInvalidQGramSize
    - `WithQGramJaccardAlgorithm(0, _)` returns ErrInvalidWeight (weight ≤ 0)
    - Same shape for WithSorensenDiceAlgorithm and WithCosineAlgorithm (n validation + weight validation)
    - `WithTverskyAlgorithm(0.5, 1.0, 1.0, 3)` produces an option with scoreFn closure over (alpha=1, beta=1, n=3)
    - `WithTverskyAlgorithm(_, -0.1, 1.0, 3)` returns ErrInvalidTverskyParam (alpha must be ≥ 0)
    - `WithTverskyAlgorithm(_, 1.0, -0.1, 3)` returns ErrInvalidTverskyParam (beta must be ≥ 0)
    - `WithTverskyAlgorithm(_, _, _, 0)` returns ErrInvalidQGramSize
    - `WithMongeElkanAlgorithm(0.5, AlgoJaroWinkler)` produces an option storing scoreFn closure that calls MongeElkanScoreSymmetric(a, b, AlgoJaroWinkler, DefaultNormalisationOptions())
    - `WithMongeElkanAlgorithm(_, AlgoMongeElkan)` returns ErrInvalidAlgorithm (Monge-Elkan cannot be its own inner; the closure does not enforce the full Phase 6 permitted-inner allow-list — the consumer-facing constraint is documented in godoc and the runtime call inside MongeElkanScoreSymmetric will panic on a forbidden inner per Phase 6's locked behaviour; the option function only catches the trivial recursive case + bounds check)
    - `WithSmithWatermanGotohAlgorithm(0.5, NewSWGParams())` produces an option with scoreFn calling SmithWatermanGotohScore(a, b, params)
    - For each parameterised option: weight ≤ 0 returns ErrInvalidWeight; out-of-range numeric params return the appropriate sentinel (ErrInvalidQGramSize / ErrInvalidTverskyParam / ErrInvalidAlgorithm)
  </behavior>
  <action>
    Append 6 new exported option functions to `scorer_options.go`:

    - `WithQGramJaccardAlgorithm(weight float64, n int) ScorerOption` — validates weight > 0 (ErrInvalidWeight) and n ≥ 1 (ErrInvalidQGramSize); on success appends `scorerEntry{id: AlgoQGramJaccard, weight: weight, scoreFn: func(a, b string) float64 { return QGramJaccardScore(a, b, n) }}`
    - `WithSorensenDiceAlgorithm(weight float64, n int) ScorerOption` — same shape; AlgoSorensenDice; closure calls SorensenDiceScore(a, b, n)
    - `WithCosineAlgorithm(weight float64, n int) ScorerOption` — same shape; AlgoCosine; closure calls CosineScore(a, b, n)
    - `WithTverskyAlgorithm(weight, alpha, beta float64, n int) ScorerOption` — validates weight > 0; n ≥ 1 (ErrInvalidQGramSize); alpha ≥ 0 AND beta ≥ 0 (ErrInvalidTverskyParam — both); closure calls TverskyScore(a, b, alpha, beta, n)
    - `WithMongeElkanAlgorithm(weight float64, inner AlgoID) ScorerOption` — validates weight > 0; bounds-check inner (`int(inner) >= 0 && int(inner) < numAlgorithms && dispatch[inner] != nil` else ErrInvalidAlgorithm); reject `inner == AlgoMongeElkan` with ErrInvalidAlgorithm (trivial recursion); closure calls MongeElkanScoreSymmetric(a, b, inner, DefaultNormalisationOptions())
    - `WithSmithWatermanGotohAlgorithm(weight float64, params SWGParams) ScorerOption` — validates weight > 0; closure calls SmithWatermanGotohScore(a, b, params). (SWGParams validation is the responsibility of SmithWatermanGotohScore per Phase 3.)

    Every closure stores its captured params by value (n, alpha, beta, inner, params SWGParams) so the option can be applied any number of times without state aliasing.

    Each function has a godoc block opening with the function name and a one-line summary citing the algorithm's spec section in `docs/requirements.md` and CONTEXT.md §3 (ME-opts-vestigial note for WithMongeElkanAlgorithm — explain that the ME function's `opts NormalisationOptions` parameter is currently a no-op, accepted for forward-compat).

    Extend `scorer_options_test.go` with happy-path + error-path tests for each new option function. Reuse the `applyOptionForProbe` helper from Task 2 (or extend it if needed). Tests use `errors.Is` for error discrimination.

    Update llms.txt + llms-full.txt: add lines for the 6 new functions. Verify the running ai_friendly_test.go meta-test still passes.
  </action>
  <acceptance_criteria>
    - scorer_options.go contains exactly these public function signatures (verify with grep): `func WithQGramJaccardAlgorithm(weight float64, n int) ScorerOption`, `func WithSorensenDiceAlgorithm(weight float64, n int) ScorerOption`, `func WithCosineAlgorithm(weight float64, n int) ScorerOption`, `func WithTverskyAlgorithm(weight, alpha, beta float64, n int) ScorerOption`, `func WithMongeElkanAlgorithm(weight float64, inner AlgoID) ScorerOption`, `func WithSmithWatermanGotohAlgorithm(weight float64, params SWGParams) ScorerOption`
    - scorer_options.go now exports exactly 12 `With*` functions (`grep -c "^func With" scorer_options.go` returns 12)
    - `go vet ./...` exits 0
    - `go test -race -run "TestWithQGramJaccard|TestWithSorensenDice|TestWithCosine|TestWithTversky|TestWithMongeElkan|TestWithSmithWatermanGotoh" ./...` exits 0
    - llms.txt grows by exactly 6 additional lines; llms-full.txt grows accordingly
    - `go test -run TestLLMSync ./...` exits 0
    - `go test -race ./... -count=1` exits 0 (full suite; no regressions in prior phases)
    - No new imports added to scorer_options.go beyond what Task 2 introduced (parameterised closures are package-local references to existing functions)
    - `git diff scorer_options.go | grep -c "^-"` shows no removals from Task 2 — this task only appends
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -race -count=1</automated>
  </verify>
  <done>All 12 option functions implemented and unit-tested; llms.txt + llms-full.txt synced; full root suite green; ready for plan 08-02 NewScorer + Score implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer→option function | Consumer-supplied AlgoID values, weights, threshold values, q-gram n, Tversky alpha/beta, SWGParams enter the library here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `WithAlgorithm`, parameterised options (weight + numeric params) | mitigate | Every option validates weight strictly `> 0` returning `ErrInvalidWeight`; q-gram n validated `≥ 1` returning `ErrInvalidQGramSize`; Tversky alpha/beta validated `≥ 0` returning `ErrInvalidTverskyParam`; threshold validated `t ∈ [0.0, 1.0]` inclusive returning `ErrInvalidThreshold`. Verified by per-option error-path unit tests in `scorer_options_test.go` and by the entire 4-plan property test suite which asserts no NaN/Inf escape any Scorer surface. |
| T-08-02 | DoS / Information Disclosure | `WithAlgorithm`, `WithMongeElkanAlgorithm` (AlgoID input) | mitigate | Out-of-range AlgoID values (e.g. `AlgoID(999)`) caught by `int(algo) >= numAlgorithms \|\| dispatch[algo] == nil` returning `ErrInvalidAlgorithm` instead of panicking inside `dispatch[algo](a,b)`. Trivial recursion `WithMongeElkanAlgorithm(_, AlgoMongeElkan)` rejected explicitly. Verified by `TestWithAlgorithm_InvalidAlgoID` and `TestWithMongeElkanAlgorithm_RejectsSelf`. |
</threat_model>

<verification>
- `go test -race -count=1 ./...` — full root module suite green
- `go vet ./...` exits 0
- `gofmt -l scorer_options.go scorer_options_test.go errors.go` produces no diff
- `golangci-lint run ./...` exits 0
- `grep -c "^var Err" errors.go` returns 10
- `grep -c "^func With" scorer_options.go` returns 12
- `! grep -q "testify\|errgroup\|github.com/" scorer_options.go scorer_options_test.go` (no third-party imports in scorer_options.go or scorer_options_test.go)
- `! grep -q "^require" go.mod` (root go.mod still has zero non-stdlib require lines)
- llms.txt sync verified by `go test -run TestLLMSync ./...`
</verification>

<success_criteria>
- 4 new sentinel errors live in errors.go with godoc parity to existing ones
- 12 option functions live in scorer_options.go covering CONTEXT.md §4's complete list
- scorer_options_test.go covers happy + error paths for every option (one test per option × one happy + one-or-more error scenarios)
- Plan 08-02 can call `NewScorer(opts...)` with any combination of these options and have the option-level validations already enforced
- Zero new runtime deps; zero new imports beyond stdlib; no init(); no goroutines; stdlib `testing` only in root tests
- llms.txt + llms-full.txt synced in lockstep with new exported symbols
</success_criteria>

<output>
After completion, create `.planning/phases/08-composite-scorer/08-01-SUMMARY.md` documenting:
- The 4 new sentinel error names and their canonical message strings
- The 12 option function signatures (final exact form)
- The internal `scorerConfig` field set and rationale for slice-of-entries (vs map-keyed) per RESEARCH.md primary recommendation
- The `applyOptionForProbe` helper mechanism if introduced
- Any deviations from CONTEXT.md / PATTERNS.md / RESEARCH.md and their rationale
</output>
