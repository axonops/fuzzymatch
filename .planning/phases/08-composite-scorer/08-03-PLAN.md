---
type: execute
plan: 08-03
phase: 08
wave: 3
depends_on: [08-02]
files_created: []
files_modified:
  - scorer.go
  - scorer_test.go
  - llms.txt
  - llms-full.txt
autonomous: true
requirements: [SCORER-02, SCORER-05, SCORER-06, SCORER-07]
must_haves:
  truths:
    - "`Scorer.ScoreAll(a, b) map[AlgoID]float64` ships with typed AlgoID keys (SPEC OVERRIDE per CONTEXT.md §1) — godoc carries the SPEC-OVERRIDE notice; api-ergonomics-reviewer sign-off is recorded in this plan's PR description"
    - "`Scorer.Threshold() float64` returns the stored threshold (plain accessor)"
    - "`Scorer.Algorithms() []ScorerAlgorithm` returns a FRESH slice on every call, in AlgoID-ascending order (matching internal iteration); `ScorerAlgorithm{ID AlgoID, Weight float64}` exports the post-normalisation weight"
    - "`DefaultScorer() *Scorer` cannot fail (panics on internal inconsistency only); bakes threshold 0.85 and the spec-§8.5 6-algorithm composition; safe for concurrent use"
    - "`DefaultScorerOptions() []ScorerOption` returns a FRESH slice (consumers can append/mutate without affecting subsequent DefaultScorer calls) producing the same Scorer as `DefaultScorer()` when passed to `NewScorer`"
    - "Property tests prove: deterministic across runs, weight sum-to-one (when normalised), score in [0,1] (when normalised) — using stdlib `testing/quick`"
    - "Concurrent test (`sync.WaitGroup`, stdlib only) invokes Score/ScoreAll/Match on the same `*Scorer` from N=100 goroutines — `go test -race` clean, all goroutines return byte-identical results"
    - "Map iteration order in ScoreAll is documented non-deterministic; internal computation iterates AlgoID-sorted slice (slice, not map) so the score values are deterministic"
    - "ScoreAll never iterates a map on the hot path (no-map-iteration rule per DET-03); iterates s.algorithmsAlgoIDSorted (slice) and writes into a fresh map"
  facts:
    - "scorer.go gains 6 new exported symbols: ScoreAll, Threshold, Algorithms (methods); ScorerAlgorithm (struct); DefaultScorer, DefaultScorerOptions (functions)"
    - "scorer_test.go gains 3 property tests + 1 concurrent test + happy-path coverage for the 6 new symbols"
    - "go test -race -run TestProp_Scorer ./... covers PropScorer_DeterministicAcrossRuns / PropScorer_WeightSumOne / PropScorer_ScoreInRange"
    - "go test -race -run TestScorer_ConcurrentSafety ./... covers SCORER-01's concurrent-safety contract end-to-end"
    - "Plan 08-03's PR description explicitly records api-ergonomics-reviewer sign-off on the ScoreAll SPEC OVERRIDE (typed AlgoID keys), following Phase 7 plan 07-02's algorithm-licensing-reviewer sign-off pattern"
estimated_effort: medium
---

<objective>
Land the remaining Scorer public surface — `ScoreAll`, `Threshold()`, `Algorithms()`, the
`ScorerAlgorithm` struct, `DefaultScorer()`, and `DefaultScorerOptions()` — plus the property
test suite and the concurrent-safety test.

This plan locks in the SPEC OVERRIDE on `ScoreAll`'s key type (`map[AlgoID]float64` instead
of the spec's `map[string]float64`); the api-ergonomics-reviewer sign-off MUST appear in the
PR description following the Phase 7 plan 07-02 precedent.

Purpose: complete the Scorer's public API + verify concurrent safety + verify mathematical
invariants via property tests + verify deterministic-across-runs behaviour. After this plan,
plan 08-04 only needs to land the golden file, BDD scenarios, examples, docs, and benchmark.

Output:
- `scorer.go` (modified) — append ScoreAll + Threshold + Algorithms + ScorerAlgorithm + DefaultScorer + DefaultScorerOptions
- `scorer_test.go` (modified) — append property tests + concurrent test + accessor unit tests
- `llms.txt`, `llms-full.txt` (modified) — sync new exported symbols
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-composite-scorer/08-CONTEXT.md
@.planning/phases/08-composite-scorer/08-RESEARCH.md
@.planning/phases/08-composite-scorer/08-PATTERNS.md
@.planning/phases/08-composite-scorer/08-VALIDATION.md
@.planning/phases/08-composite-scorer/08-01-SUMMARY.md
@.planning/phases/08-composite-scorer/08-02-SUMMARY.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/go-testing-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
<!-- From plans 08-01 + 08-02 (committed): -->
```
type Scorer struct {
    algorithmsAlgoIDSorted []scorerEntry  // AlgoID-ascending, post-normalisation weights
    threshold              float64
    applyNormalisation     bool
    normaliseOpts          NormalisationOptions
}
func NewScorer(opts ...ScorerOption) (*Scorer, error)
func (s *Scorer) Score(a, b string) float64
func (s *Scorer) Match(a, b string) bool
```

<!-- DefaultScorer composition per spec §8.5 / CONTEXT.md §1 SPEC OVERRIDE golden corpus example: -->
```
DefaultScorer = NewScorer(
    WithAlgorithm(AlgoDamerauLevenshteinOSA, 1.0),
    WithAlgorithm(AlgoJaroWinkler, 1.0),
    WithAlgorithm(AlgoTokenJaccard, 1.0),
    WithAlgorithm(AlgoQGramJaccard, 1.0),    // uses default n=3 via dispatch
    WithAlgorithm(AlgoSorensenDice, 1.0),    // uses default n=3 via dispatch
    WithAlgorithm(AlgoDoubleMetaphone, 1.0),
    WithThreshold(0.85),
)
```

Six equal raw weights → post-normalisation each entry has weight 1/6 ≈ 0.16666... Confirmation pin: each algo contributes equally.

<!-- testing/quick property test scaffold (from normalise_test.go:283-293): -->
```
func TestProp_X(t *testing.T) {
    f := func(s string) bool { ... }
    if err := quick.Check(f, nil); err != nil { t.Errorf(...) }
}
```

<!-- sync.WaitGroup concurrent pattern (from PATTERNS.md lines 322-342): -->
```
var wg sync.WaitGroup
const n = 100
wg.Add(n)
results := make([]float64, n)
for i := 0; i < n; i++ {
    go func(idx int) {
        defer wg.Done()
        results[idx] = s.Score("user_id", "userId")
    }(i)
}
wg.Wait()
```
The `go` statement is **permitted in tests** only — never in library code.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Threshold, Algorithms, ScorerAlgorithm struct + ScoreAll with SPEC-OVERRIDE godoc</name>
  <files>scorer.go, scorer_test.go, llms.txt, llms-full.txt</files>
  <read_first>
    - scorer.go (current state from plan 08-02; Score + Match already implemented)
    - .planning/phases/08-composite-scorer/08-CONTEXT.md §1 (SPEC OVERRIDE rationale + the godoc paragraph for ScoreAll)
    - .planning/phases/08-composite-scorer/08-PATTERNS.md — no analog file for ScorerAlgorithm struct (first introduction); use existing struct-with-godoc pattern from any algorithm result type
    - .planning/phases/08-composite-scorer/08-RESEARCH.md Pitfall 2 (map iteration discipline — ScoreAll iterates the SLICE to populate the map, never iterates the map)
    - .claude/skills/determinism-standards/SKILL.md "What's NOT Deterministic" section (ScoreAll iteration order documented non-deterministic)
  </read_first>
  <behavior>
    - `s.Threshold()` returns `s.threshold` exactly (no mutation, plain accessor)
    - `s.Algorithms()` returns a fresh `[]ScorerAlgorithm` slice on every call (modifying the returned slice MUST NOT affect a subsequent call)
    - `s.Algorithms()` length equals `len(s.algorithmsAlgoIDSorted)`
    - `s.Algorithms()` is sorted in AlgoID-ascending order
    - Each returned `ScorerAlgorithm{ID, Weight}` has the POST-normalisation weight (consumers see what the Score computation actually uses)
    - `s.ScoreAll(a, b)` returns `map[AlgoID]float64` with one entry per configured algorithm
    - `s.ScoreAll(a, b)` map values are byte-identical to per-algorithm calls — `result[AlgoLevenshtein] == entry.scoreFn(na, nb)` for the Levenshtein entry (where na/nb are pre-normalised same as Score)
    - `s.ScoreAll(a, b)` map iteration order is non-deterministic (Go map semantics — documented in godoc; this is not a defect)
    - `s.ScoreAll(a, b)` pre-normalises ONCE (just like Score) — same `applyNormalisation` gate; this ensures `ScoreAll[X]` matches the per-algorithm contribution to `Score`
    - Each call to `ScoreAll` allocates a fresh map (per spec §8.6)
  </behavior>
  <action>
    Append to `scorer.go`:

    1. Public struct `type ScorerAlgorithm struct {` with exported fields `ID AlgoID` and `Weight float64`. Godoc explains: post-normalisation weight, returned by Scorer.Algorithms in AlgoID-ascending order.

    2. `func (s *Scorer) Threshold() float64` — single-line `return s.threshold`. Godoc opens with the method name and states "returns the configured match threshold; values in [0.0, 1.0]".

    3. `func (s *Scorer) Algorithms() []ScorerAlgorithm`:
       - `out := make([]ScorerAlgorithm, 0, len(s.algorithmsAlgoIDSorted))`
       - For each `e := range s.algorithmsAlgoIDSorted` append `ScorerAlgorithm{ID: e.id, Weight: e.weight}`
       - `return out`
       Godoc explains: fresh slice per call, AlgoID-ascending, post-normalisation weights.

    4. `func (s *Scorer) ScoreAll(a, b string) map[AlgoID]float64`:
       - Pre-normalise (same as Score): `na, nb := a, b; if s.applyNormalisation { na = Normalise(a, s.normaliseOpts); nb = Normalise(b, s.normaliseOpts) }`
       - `out := make(map[AlgoID]float64, len(s.algorithmsAlgoIDSorted))`
       - For each `entry := range s.algorithmsAlgoIDSorted` (slice — DETERMINISTIC iteration order; never iterate a map here): `out[entry.id] = entry.scoreFn(na, nb)`
       - `return out`

    GODoc for ScoreAll MUST OPEN with: `// ScoreAll returns per-algorithm raw scores for the configured algorithm set as a map[AlgoID]float64.` Following paragraphs MUST contain:
    - `// SPEC OVERRIDE: docs/requirements.md §8.3 specifies map[string]float64; this implementation returns map[AlgoID]float64 because AlgoID is a typed enum that the rest of the library exposes, giving consumers compile-time key safety. Use AlgoID.String() for snake_case display. The spec deviation is documented in CONTEXT.md §1 (Phase 8) and api-ergonomics-reviewer signed off on this override in plan 08-03's PR.`
    - `// Map iteration order is non-deterministic per Go map semantics. Map CONTENTS are deterministic byte-for-byte (per-algorithm scores are deterministic; see PropScorer_DeterministicAcrossRuns). Consumers requiring stable iteration order MUST sort the keys themselves — typically via fuzzymatch.AlgoIDs() then key-lookup.`
    - `// A fresh map is allocated on every call (spec §8.6). Hot-path callers wanting to avoid the allocation should use Score(a, b) which returns the composite float without per-algorithm breakdown.`

    Append to `scorer_test.go` (`package fuzzymatch_test`):
    - `TestScorer_Threshold_ReturnsStoredValue` — construct a Scorer with `WithThreshold(0.73)`; assert `s.Threshold() == 0.73`
    - `TestScorer_Algorithms_FreshSlice` — call `s.Algorithms()` twice; modify the first slice (`first[0].Weight = 999`); assert `s.Algorithms()` returns unmodified data on the second call
    - `TestScorer_Algorithms_SortedAscending` — construct a 3-algo Scorer; assert returned slice IDs are AlgoID-ascending (e.g. AlgoLevenshtein=0 < AlgoDamerauLevenshteinOSA=1 < AlgoJaroWinkler=4 in canonical iota order)
    - `TestScorer_Algorithms_PostNormalisationWeights` — construct 2-algo Scorer with raw weights 1.0 and 3.0; assert `Algorithms()` returns weights 0.25 and 0.75 (post-normalised)
    - `TestScorer_ScoreAll_Keys` — construct a 3-algo Scorer with known AlgoIDs; assert `len(result) == 3` and exactly those AlgoIDs are present as map keys
    - `TestScorer_ScoreAll_ValuesMatchPerAlgoCalls` — single-Levenshtein Scorer; assert `s.ScoreAll("kitten", "sitting")[AlgoLevenshtein] == fuzzymatch.LevenshteinScore("kitten", "sitting")` (after considering normalisation — use `WithoutNormalisation()` for a clean direct comparison OR build the Scorer with `WithNormalisation(opts)` matching the per-algo call's expectations)
    - `TestScorer_ScoreAll_FreshMap` — `m1 := s.ScoreAll(a, b)`; `m1[AlgoLevenshtein] = 999`; `m2 := s.ScoreAll(a, b)`; assert `m2[AlgoLevenshtein] != 999`

    Update llms.txt + llms-full.txt with the 5 new exported symbols (ScorerAlgorithm, Threshold, Algorithms, ScoreAll, ScoreAll's godoc paragraph). For llms-full.txt, the ScoreAll entry MUST include the SPEC OVERRIDE paragraph verbatim.
  </action>
  <acceptance_criteria>
    - scorer.go contains `type ScorerAlgorithm struct {`
    - scorer.go contains `func (s *Scorer) Threshold() float64`
    - scorer.go contains `func (s *Scorer) Algorithms() []ScorerAlgorithm`
    - scorer.go contains `func (s *Scorer) ScoreAll(a, b string) map[AlgoID]float64`
    - ScoreAll's godoc contains the literal phrase `SPEC OVERRIDE` (`grep -F "SPEC OVERRIDE" scorer.go` returns at least 1)
    - ScoreAll's godoc contains `Map iteration order is non-deterministic` (`grep -F "iteration order is non-deterministic" scorer.go` returns at least 1)
    - ScoreAll's body iterates `s.algorithmsAlgoIDSorted` (slice) — verify with: read the file and confirm there is no `for k, v := range out` or `for k := range out` inside ScoreAll
    - `go vet ./...` exits 0
    - `gofmt -l scorer.go scorer_test.go` produces no diff
    - `go test -race -run "TestScorer_Threshold|TestScorer_Algorithms|TestScorer_ScoreAll" ./...` exits 0 with every subtest green
    - `go test -race -count=1 ./...` exits 0
    - llms.txt grows by 4 new lines (Threshold, Algorithms, ScoreAll, ScorerAlgorithm); llms-full.txt extended with full godoc blocks including the ScoreAll SPEC OVERRIDE paragraph
    - `go test -run TestLLMSync ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test -race -count=1 ./...</automated>
  </verify>
  <done>Accessor methods + ScoreAll live with the SPEC OVERRIDE godoc; tests cover fresh-slice / fresh-map invariants + AlgoID-sorted order + post-normalisation weights + key coverage; llms.txt + llms-full.txt synced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DefaultScorer + DefaultScorerOptions (cannot fail; spec §8.5 composition)</name>
  <files>scorer.go, scorer_test.go, llms.txt, llms-full.txt</files>
  <read_first>
    - scorer.go (current state — has NewScorer, Score, Match, Threshold, Algorithms, ScoreAll)
    - docs/requirements.md §8.5 — DefaultScorer composition (re-read the relevant section; ~30 lines)
    - .planning/phases/08-composite-scorer/08-CONTEXT.md §2 (DefaultScorer bakes 0.85 + 6-algorithm mix per spec §8.5)
    - examples/identifier-similarity/main.go — shows the AlgoIDs the existing example exercises, useful sanity check for what the 6-algorithm mix is supposed to cover
  </read_first>
  <behavior>
    - `DefaultScorer()` returns a `*Scorer` (never an error; never panics under any reasonable load-time scenario — panic only if dispatch[] is somehow nil for an AlgoID, which is a programmer error that should be unreachable post-Phase-7)
    - `DefaultScorer()`'s configured algorithms are exactly: AlgoDamerauLevenshteinOSA, AlgoJaroWinkler, AlgoTokenJaccard, AlgoQGramJaccard, AlgoSorensenDice, AlgoDoubleMetaphone (per spec §8.5 and CONTEXT.md §6 golden corpus example)
    - `DefaultScorer().Threshold() == 0.85`
    - `DefaultScorer().Algorithms()` has length 6
    - Each post-normalisation weight equals 1.0/6.0 (within byte equality — pin via test)
    - `DefaultScorerOptions()` returns a `[]ScorerOption` slice of length 7 (6 WithAlgorithm + 1 WithThreshold)
    - `DefaultScorerOptions()` returns a FRESH slice on every call (modifying the returned slice MUST NOT affect a subsequent call)
    - `NewScorer(DefaultScorerOptions()...)` produces a Scorer behaviourally identical to `DefaultScorer()` — same configured AlgoIDs, same weights post-normalisation, same threshold
    - `NewScorer(append(DefaultScorerOptions(), WithoutAlgorithm(AlgoDoubleMetaphone))...)` produces a Scorer with 5 algorithms (DM removed), still threshold 0.85
  </behavior>
  <action>
    Append to `scorer.go`:

    `func DefaultScorerOptions() []ScorerOption` body:
    1. `return []ScorerOption{`
    2. `    WithAlgorithm(AlgoDamerauLevenshteinOSA, 1.0),`
    3. `    WithAlgorithm(AlgoJaroWinkler, 1.0),`
    4. `    WithAlgorithm(AlgoTokenJaccard, 1.0),`
    5. `    WithAlgorithm(AlgoQGramJaccard, 1.0),       // uses default n=3 from dispatch_qgram_jaccard.go`
    6. `    WithAlgorithm(AlgoSorensenDice, 1.0),       // uses default n=3 from dispatch_sorensen_dice.go`
    7. `    WithAlgorithm(AlgoDoubleMetaphone, 1.0),`
    8. `    WithThreshold(0.85),`
    9. `}`

    Godoc opens with the function name; explains: "returns a fresh, mutable slice of ScorerOption matching DefaultScorer's composition (6 algorithms at equal raw weight + threshold 0.85). Consumers can append additional options or splice in `WithoutAlgorithm(X)` to derive customised Scorers from the default. The slice is constructed fresh on every call, so consumers can mutate it without affecting subsequent DefaultScorer() / DefaultScorerOptions() calls."

    `func DefaultScorer() *Scorer` body:
    1. `s, err := NewScorer(DefaultScorerOptions()...)`
    2. `if err != nil { panic("fuzzymatch: DefaultScorer construction failed (this is a bug): " + err.Error()) }`
    3. `return s`

    Godoc opens with the function name; explains: "returns the opinionated default Scorer (spec §8.5 composition: DamerauLevenshteinOSA + JaroWinkler + TokenJaccard + QGramJaccard + SorensenDice + DoubleMetaphone, equal weights, threshold 0.85). Cannot fail under normal operation; panics only on internal inconsistency (programmer error). Safe for concurrent use without locks. Consumers wanting `default minus algorithm X` use `NewScorer(append(DefaultScorerOptions(), WithoutAlgorithm(X))...)`."

    Append to `scorer_test.go`:
    - `TestDefaultScorer_Composition` — `s := DefaultScorer()`; `algos := s.Algorithms()`; assert `len(algos) == 6` and the exact 6 AlgoIDs are present; assert `s.Threshold() == 0.85`
    - `TestDefaultScorer_WeightsEqual` — each `algos[i].Weight` equals `1.0/6.0` exactly (within byte equality)
    - `TestDefaultScorer_NeverFails` — invoke `DefaultScorer()` 100 times in a tight loop; assert each return is non-nil and `recover()` from any panic returns nil
    - `TestDefaultScorerOptions_FreshSlice` — `opts1 := DefaultScorerOptions()`; modify `opts1[0]` (replace with nil); `opts2 := DefaultScorerOptions()`; assert `opts2[0]` is the original WithAlgorithm option (non-nil); use `NewScorer(opts2...)` and verify it still succeeds
    - `TestDefaultScorerOptions_ProducesEquivalentScorer` — `s1 := DefaultScorer()`; `s2, err := NewScorer(DefaultScorerOptions()...)`; assert no err; assert `s1.Score(a, b) == s2.Score(a, b)` for a representative input pair (e.g. "user_id", "userId")
    - `TestDefaultScorer_WithoutAlgorithm_Composition` — `opts := append(DefaultScorerOptions(), WithoutAlgorithm(AlgoDoubleMetaphone))`; `s, err := NewScorer(opts...)`; assert no err; `len(s.Algorithms()) == 5`; assert AlgoDoubleMetaphone not present in `Algorithms()` output

    Update llms.txt + llms-full.txt: add `DefaultScorer` (function) and `DefaultScorerOptions` (function) with full godoc.
  </action>
  <acceptance_criteria>
    - scorer.go contains `func DefaultScorer() *Scorer`
    - scorer.go contains `func DefaultScorerOptions() []ScorerOption`
    - DefaultScorer's body returns `*Scorer` directly (no error in signature)
    - DefaultScorerOptions returns a `make`-allocated or composite-literal slice (NEW slice every call — `grep -F "return []ScorerOption{" scorer.go` returns 1)
    - The 6 AlgoIDs in the slice match CONTEXT.md §6 exactly: AlgoDamerauLevenshteinOSA, AlgoJaroWinkler, AlgoTokenJaccard, AlgoQGramJaccard, AlgoSorensenDice, AlgoDoubleMetaphone (`grep -c "WithAlgorithm(Algo" scorer.go` returns at least 6)
    - `go vet ./...` exits 0
    - `gofmt -l scorer.go scorer_test.go` produces no diff
    - `go test -race -run "TestDefaultScorer|TestDefaultScorerOptions" ./...` exits 0 with every subtest green
    - `go test -race -count=1 ./...` exits 0
    - llms.txt grows by 2 additional lines; `go test -run TestLLMSync ./...` exits 0
    - `! grep -q "panic" scorer.go | grep -v "DefaultScorer construction failed"` — the only panic in scorer.go is the defensive DefaultScorer panic; no other panics introduced
  </acceptance_criteria>
  <verify>
    <automated>go test -race -count=1 ./...</automated>
  </verify>
  <done>DefaultScorer + DefaultScorerOptions live with the spec §8.5 composition; tests verify composition correctness + fresh-slice semantics + the WithoutAlgorithm derivation pattern.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Property tests + concurrent-safety test (PropScorer_DeterministicAcrossRuns / PropScorer_WeightSumOne / PropScorer_ScoreInRange / TestScorer_ConcurrentSafety)</name>
  <files>scorer_test.go</files>
  <read_first>
    - scorer.go (final state from Tasks 1+2)
    - normalise_test.go lines 283-310 — analog `testing/quick` property test pattern (TestProp_Normalise_Idempotent)
    - .planning/phases/08-composite-scorer/08-PATTERNS.md "scorer_test.go" Property test pattern (lines 295-319 of PATTERNS.md) + Concurrent safety test pattern (lines 322-342 of PATTERNS.md)
    - .planning/phases/08-composite-scorer/08-RESEARCH.md "Architecture Patterns / Pattern 6" (concurrent test uses sync.WaitGroup, NOT errgroup)
    - .claude/skills/go-testing-standards/SKILL.md "Property Tests" section + "Scorer-level properties" subsection
  </read_first>
  <behavior>
    - `PropScorer_DeterministicAcrossRuns` (TestProp_Scorer_DeterministicAcrossRuns): for any string pair (a, b) drawn by testing/quick, `DefaultScorer().Score(a, b) == DefaultScorer().Score(a, b)` (byte-equal across two invocations on FRESHLY-constructed DefaultScorer instances — proves determinism across construction events)
    - `PropScorer_WeightSumOne`: for any randomly-composed Scorer with WithNormaliseWeights(true) (default), `sum(s.Algorithms()[i].Weight) == 1.0` within `1e-12` tolerance (allows minor float drift from many-algorithm normalisation; tighter tolerance would over-constrain)
    - `PropScorer_ScoreInRange`: for any input pair (a, b), `DefaultScorer().Score(a, b) ∈ [0.0, 1.0]` — since DefaultScorer has normalised weights AND each per-algorithm score is in [0,1], the composite is bounded
    - `TestScorer_ConcurrentSafety`: 100 goroutines all call `s.Score("user_id", "userId")` on the same `*Scorer = DefaultScorer()`; results collected into a `[]float64`; assert all 100 results are byte-equal to `results[0]`; assert `go test -race` reports no data race
    - Same concurrent test also exercises ScoreAll and Match for full coverage of SCORER-01 — N=100 goroutines per method, three rounds (Score, ScoreAll, Match)
  </behavior>
  <action>
    Append to `scorer_test.go`:

    Add `import "testing/quick"` and `import "sync"` if not already present.

    `func TestProp_Scorer_DeterministicAcrossRuns(t *testing.T)`:
    1. `f := func(a, b string) bool { return DefaultScorer().Score(a, b) == DefaultScorer().Score(a, b) }` — pure equality, no tolerance (we want byte-identical floats)
    2. `if err := quick.Check(f, nil); err != nil { t.Errorf("PropScorer_DeterministicAcrossRuns: %v", err) }`

    `func TestProp_Scorer_WeightSumOne(t *testing.T)`:
    1. Use a fixed-construction (not quick.Check-generated) test: construct several Scorers with WithNormaliseWeights(true) (default) and varied raw weights (e.g. 1.0/2.0/3.0/0.7/0.001/1000.0 in different combinations across subtests)
    2. For each Scorer, compute `var sum float64; for _, a := range s.Algorithms() { sum = sum + a.Weight }` and assert `math.Abs(sum - 1.0) < 1e-12` (tolerance accounts for many-algorithm float drift)
    3. Use `t.Run` for each subtest with named scenarios
    4. ALSO add a quick.Check version that generates random raw weights (positive float64 ≤ 100) and constructs a 1-3 algo Scorer, asserting the same sum-to-one invariant

    `func TestProp_Scorer_ScoreInRange(t *testing.T)`:
    1. `s := DefaultScorer()`
    2. `f := func(a, b string) bool { score := s.Score(a, b); return score >= 0.0 && score <= 1.0 }`
    3. `if err := quick.Check(f, nil); err != nil { t.Errorf("PropScorer_ScoreInRange: %v", err) }`

    `func TestProp_Scorer_NoNaN_NoInf(t *testing.T)`:
    1. `s := DefaultScorer()`
    2. `f := func(a, b string) bool { score := s.Score(a, b); return !math.IsNaN(score) && !math.IsInf(score, 0) }`
    3. `if err := quick.Check(f, nil); err != nil { t.Errorf(...) }`

    `func TestScorer_ConcurrentSafety(t *testing.T)`:
    1. `s := DefaultScorer()`
    2. `const n = 100`
    3. Subtest "Score": construct `var wg sync.WaitGroup; wg.Add(n); results := make([]float64, n); for i := 0; i < n; i++ { go func(idx int) { defer wg.Done(); results[idx] = s.Score("user_id", "userId") }(i) }; wg.Wait()`; then `for i, got := range results { if got != results[0] { t.Errorf("goroutine %d: got %g; want %g", i, got, results[0]) } }`
    4. Subtest "ScoreAll": same pattern; collect `results := make([]map[AlgoID]float64, n)`; after Wait, verify each map has identical keys (`len == 6`) and identical values per key compared to `results[0]`
    5. Subtest "Match": same pattern; collect `results := make([]bool, n)`; assert all equal

    NO testify. NO errgroup. NO `t.Parallel()` inside the goroutine bodies (we want them all to run concurrently against the same Scorer pointer).

    Note: the property tests count toward the `testing/quick` standard suite. `quick.Check` will run 100 random inputs by default; for `PropScorer_DeterministicAcrossRuns` consider passing `&quick.Config{MaxCount: 200}` to increase coverage of pathological inputs, but the default 100 is acceptable.
  </action>
  <acceptance_criteria>
    - scorer_test.go contains `func TestProp_Scorer_DeterministicAcrossRuns(t *testing.T)`
    - scorer_test.go contains `func TestProp_Scorer_WeightSumOne(t *testing.T)`
    - scorer_test.go contains `func TestProp_Scorer_ScoreInRange(t *testing.T)`
    - scorer_test.go contains `func TestProp_Scorer_NoNaN_NoInf(t *testing.T)`
    - scorer_test.go contains `func TestScorer_ConcurrentSafety(t *testing.T)`
    - scorer_test.go imports `"testing/quick"` and `"sync"` (verify with grep)
    - scorer_test.go does NOT import `"github.com/stretchr/testify"` or `"golang.org/x/sync/errgroup"` (`! grep -E "testify|errgroup" scorer_test.go`)
    - `go test -race -run "TestProp_Scorer|TestScorer_ConcurrentSafety" ./...` exits 0 with every subtest green
    - `go test -race -count=5 -run TestScorer_ConcurrentSafety ./...` exits 0 (5 iterations of the concurrent test — `-race` confirms no data race across multiple runs)
    - `go test -race -count=1 ./...` exits 0 — full root suite green
    - `golangci-lint run ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test -race -count=5 -run "TestProp_Scorer|TestScorer_ConcurrentSafety" ./...</automated>
  </verify>
  <done>Four property tests + one concurrent-safety test green; race detector clean across 5 iterations of the concurrent test; SCORER-01 (concurrent-safe), SCORER-03 (weight sum-to-one), SCORER-04 (score in range) all proven by automated tests.</done>
</task>

</tasks>

<api_review_gate>
## api-ergonomics-reviewer Sign-Off — MANDATORY for this plan

Before merging this plan, the api-ergonomics-reviewer agent MUST sign off on the SPEC OVERRIDE
on `ScoreAll`'s return type. Process (following Phase 7 plan 07-02's algorithm-licensing-reviewer
precedent):

1. After plan 08-03 implementation is complete and all tests are green, spawn:
   `Task(subagent_type="api-ergonomics-reviewer", description="Sign off on ScoreAll SPEC OVERRIDE", prompt="Review scorer.go's ScoreAll signature (map[AlgoID]float64 vs spec §8.3's map[string]float64). Confirm sign-off per CONTEXT.md §1. Reference the godoc paragraph in ScoreAll for the rationale.")`
2. The agent's APPROVED response — including its summary of why typed AlgoID keys are
   the right call here — is copied verbatim into the plan 08-03 PR description.
3. If the agent issues CHANGES_REQUESTED, the plan does not merge until the concern is
   addressed (either by adjusting the signature, the godoc, or escalating to the user).

This sign-off is recorded in `.planning/phases/08-composite-scorer/08-03-SUMMARY.md` under
a heading `## api-ergonomics-reviewer Sign-Off`.
</api_review_gate>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer→Scorer methods (Score, ScoreAll, Match) | Already crossed in plan 08-02; this plan adds Threshold (no-input), Algorithms (no-input), ScoreAll (string inputs) — same boundary as Score |
| consumer→DefaultScorer / DefaultScorerOptions | No consumer-supplied data — these are library-internal constructors |
| concurrent goroutines→shared *Scorer pointer | First plan that proves the concurrent-safety contract end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering / DoS | Concurrent goroutines reading the same `*Scorer` | mitigate | Scorer struct fields are set once in NewScorer (plan 08-02) and never mutated — no `sync.Mutex` needed. `Algorithms()` allocates a fresh slice to prevent consumers from mutating internal state via the returned slice. `ScoreAll` allocates a fresh map per call. `TestScorer_ConcurrentSafety` exercises 100 goroutines × {Score, ScoreAll, Match} under `go test -race` for 5 iterations; race detector clean is the acceptance gate |
| T-08-03 | DoS | `ScoreAll` adversarial input (same as Score's threat from plan 08-02) | mitigate | Inherits Score's mitigation (per-algorithm linear/polynomial worst-case); `PropScorer_NoNaN_NoInf` asserts no non-finite values escape; `PropScorer_ScoreInRange` asserts the [0,1] bound holds |
</threat_model>

<verification>
- `go test -race -count=1 ./...` — full root suite green
- `go test -race -count=5 -run TestScorer_ConcurrentSafety ./...` — race detector clean across 5 iterations
- `go vet ./...` exits 0
- `gofmt -l scorer.go scorer_test.go` produces no diff
- `golangci-lint run ./...` exits 0
- `grep -F "SPEC OVERRIDE" scorer.go` returns at least 1 (ScoreAll godoc has the prominent notice)
- `grep -F "(entry.weight * score)" scorer.go` still returns 1+ matches (plan 08-02 reduction loop preserved)
- `! grep -E "math\.(Pow|Log|Exp|FMA)" scorer.go` (no forbidden transcendentals)
- `! grep -q "^[[:space:]]*go [a-zA-Z]" scorer.go` (no goroutines in library code; goroutines only in scorer_test.go inside TestScorer_ConcurrentSafety)
- `! grep -q "testify" scorer.go scorer_test.go scorer_internal_test.go`
- `! grep -q "^require " go.mod` (root go.mod still has zero non-stdlib requires)
- `grep -c "^func Default\|^func (s \*Scorer)" scorer.go` returns ≥ 8 (NewScorer + Score + Match + Threshold + Algorithms + ScoreAll + DefaultScorer + DefaultScorerOptions)
- api-ergonomics-reviewer APPROVED response captured in PR description and 08-03-SUMMARY.md
</verification>

<success_criteria>
- ScorerAlgorithm struct + Threshold + Algorithms + ScoreAll + DefaultScorer + DefaultScorerOptions all exported and unit-tested
- ScoreAll's godoc carries the SPEC OVERRIDE notice + api-ergonomics-reviewer sign-off recorded in PR description
- Property tests prove deterministic-across-runs, weight-sum-one (normalised), score-in-range, no-NaN/no-Inf
- TestScorer_ConcurrentSafety proves SCORER-01 (concurrent-safe) end-to-end with race detector clean
- Plan 08-04 can build the golden file, BDD scenarios, examples, docs, and benchmark on a complete Scorer surface
- All 8 SCORER-* requirements are now provably satisfied at the unit-test layer (golden + BDD seal them in plan 08-04)
</success_criteria>

<output>
After completion, create `.planning/phases/08-composite-scorer/08-03-SUMMARY.md` documenting:
- Final ScorerAlgorithm struct shape
- ScoreAll's full godoc paragraph (the SPEC OVERRIDE notice) copy-pasted
- DefaultScorer's exact composition (6 AlgoIDs in order)
- `## api-ergonomics-reviewer Sign-Off` heading with the agent's APPROVED response verbatim
- Property test results (initial run output) and the concurrent test's race-detector confirmation
- Note for plan 08-04: golden file, BDD scenarios, examples, docs, benchmark are the remaining work
</output>
