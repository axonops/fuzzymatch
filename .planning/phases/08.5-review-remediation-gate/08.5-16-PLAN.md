---
phase: 08.5
plan: 16
type: execute
wave: 6
depends_on: [06, 07, 08]
files_modified:
  - .github/workflows/release.yml
  - .github/workflows/nightly.yml
  - .github/workflows/ci.yml
  - .github/workflows/codeql.yml
  - .github/workflows/security.yml
  - .github/workflows/cla.yml
  - .github/workflows/commitlint.yml
  - .github/workflows/license-headers.yml
  - Makefile
  - bench.txt
  - .planning/phases/08.5-review-remediation-gate/08.5-LICENCE-AUDIT.md
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
  - CI-04
  - CI-05
  - CI-09
  - REL-01
  - REL-02
  - Q13
threat_model_refs: [T-08.5-30, T-08.5-31, T-08.5-32]

must_haves:
  truths:
    - "release.yml has needs:[ci] on the release job"
    - "nightly.yml exists with long-form fuzz + benchstat regression + auto-PR for corpus entries"
    - "Every actions reference in all 8 workflow files is pinned to a 40-char commit SHA with a # comment indicating the major version"
    - "make verify-llms-sync target exists; CI invokes it; non-zero exit when llms.txt or llms-full.txt are out of sync"
    - "bench.txt regenerated AFTER Plan 06 + Plan 07 + Plan 08 landed; includes BenchmarkDefaultScorer_ASCII_Short/Medium baselines"
    - "HashiCorp utility-library licence audit completed and documented in 08.5-LICENCE-AUDIT.md"
    - "docs/algorithms.md H2 anchor casing standardised to hyphenated; README cross-refs match"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "needs:[ci] gate on release job"
      contains: "ci"
    - path: ".github/workflows/nightly.yml"
      provides: "Long-form fuzz + benchstat + auto-PR workflow"
      contains: "long-form fuzz"
    - path: "Makefile"
      provides: "verify-llms-sync target"
      contains: "verify-llms-sync"
    - path: "bench.txt"
      provides: "Regenerated benchmark baseline post-Q7a/Q8b/Q11b"
      contains: "BenchmarkDefaultScorer_ASCII_"
    - path: ".planning/phases/08.5-review-remediation-gate/08.5-LICENCE-AUDIT.md"
      provides: "HashiCorp utility-library licence audit result"
      contains: "HashiCorp"
  key_links:
    - from: "release.yml jobs.release"
      to: "ci.yml workflow"
      via: "needs gate"
      pattern: "needs"
    - from: "make verify-llms-sync"
      to: "scripts/cmd/verify-exported-coverage (Plan 11 AST helper)"
      via: "shared AST-walk logic"
      pattern: "verify-llms-sync"
---

<objective>
Q13 — full devops/CI/build hygiene cluster. Touches 8 workflow files + Makefile + 3 new artefacts (nightly.yml, the licence audit, the regenerated bench.txt).

Hard dependencies (sequencing-critical): Plan 06 (Q11b FMA), Plan 07 (Q7a DM [4]byte), Plan 08 (Q8b Tokenise) MUST all land BEFORE bench.txt regeneration. The committed bench numbers reflect post-change code.

Output: comprehensive CI hardening + fresh bench.txt baseline + documented licence audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@docs/requirements.md
@CLAUDE.md
@.claude/skills/algorithm-licensing-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
Q13 LOCKED — full devops cluster per CONTEXT.md and RESEARCH.md §3.7.

Current workflows (8 files in .github/workflows/):
- ci.yml (5-platform matrix; needs SHA pinning + verify-llms-sync invocation)
- release.yml (release pipeline; needs gate on ci)
- codeql.yml (push to main + weekly; SHA pinning)
- security.yml (govulncheck + gosec weekly; SHA pinning)
- cla.yml (CLA Assistant Lite; SHA pinning)
- commitlint.yml (conventional-commit lint on PR; SHA pinning)
- license-headers.yml (Apache 2.0 header verification; SHA pinning)
- nightly.yml — DOES NOT EXIST; this plan creates it

Plan 11 dependency: verify-exported-coverage Go helper. The llms-sync helper uses the same AST walk.

bench.txt sequencing: Plan 06 + Plan 07 + Plan 08 land first; then regenerate.

HashiCorp audit: project LICENSE is Apache-2.0. Audit verifies no MPL-2.0-derived utility shims slipped into algorithm code.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: release.yml needs:[ci] gate + nightly.yml implementation + SHA pinning</name>
  <files>.github/workflows/release.yml, .github/workflows/nightly.yml, .github/workflows/ci.yml, .github/workflows/codeql.yml, .github/workflows/security.yml, .github/workflows/cla.yml, .github/workflows/commitlint.yml, .github/workflows/license-headers.yml</files>
  <read_first>
    - .github/workflows/release.yml in full
    - .github/workflows/ci.yml in full (5-platform matrix + existing action references)
    - .github/workflows/codeql.yml, security.yml, cla.yml, commitlint.yml, license-headers.yml (action references to SHA-pin)
    - .planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md §2.P
    - docs/requirements.md §17
  </read_first>
  <action>
1. release.yml: add `needs: [ci]` immediately under the release job declaration (next to `runs-on:`). Verify the ci.yml workflow job name matches.

2. nightly.yml: create new file. Triggers `on: schedule: - cron: 0 2 * * *` daily at 02:00 UTC plus `on: workflow_dispatch` for manual trigger. Three jobs:
   - long-form fuzz: matrix over 26 root-package fuzzers; 5 min per fuzzer (-fuzztime=5m); uploads any new corpus entries.
   - benchstat regression: runs on self-hosted runner if available (runs-on [self-hosted, fuzzymatch-bench]) with ubuntu-latest informational fallback. Compares current run against committed bench.txt; if any benchmark regresses more than 10%, fail.
   - auto-PR for fuzz corpus: if long-form fuzz discovered new entries, open a PR via peter-evans/create-pull-request action with branch auto/fuzz-corpus-YYYY-MM-DD.
   - All actions SHA-pinned (see Task 1 SHA-pinning step).

3. SHA pinning across all 8 workflow files: for each action reference (actions/checkout, actions/setup-go, goreleaser/goreleaser-action, github/codeql-action, wagoid/commitlint-github-action, contributor-assistant/github-action, peter-evans/create-pull-request, sigstore/cosign-installer, securego/gosec, etc.):
   - Replace `@vN` with `@<40-char SHA>` corresponding to the current release of that major version.
   - Add a `# v6.x.x` comment after the SHA for human readability.
   - Consult upstream GitHub release pages for the current SHAs.
   - Apply to all 8 workflow files: ci.yml, release.yml, codeql.yml, security.yml, cla.yml, commitlint.yml, license-headers.yml, nightly.yml.

4. Run actionlint locally if available, or commit and rely on CI verification.
  </action>
  <verify>
    <automated>find .github/workflows -name '*.yml' -type f | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "needs:" .github/workflows/release.yml | grep -c ci` returns at least 1.
    - `ls .github/workflows/nightly.yml` returns the file.
    - `grep -cE "uses:.*@[0-9a-f]{40}" .github/workflows/*.yml` returns the total count of action references; `grep -cE "uses:.*@v[0-9]+$" .github/workflows/*.yml` returns 0 (no remaining unpinned @vN).
    - YAML syntax is valid; CI run passes lint stage.
    - **nightly.yml self-hosted-runner fallback is explicit**: `grep -nE "runs-on:.*self-hosted|fallback_runner|if: \$\{\{" .github/workflows/nightly.yml | wc -l` returns at least 1, AND the benchstat job's `runs-on` resolves to either a self-hosted matrix entry, a `${{ inputs.fallback_runner || '"'"'self-hosted'"'"' }}` expression, or an explicit `if: ${{ ... }}` clause encoding the "self-hosted preferred, ubuntu-latest informational fallback" semantic per Q13 lock. The executor MUST NOT ship a nightly.yml that hard-codes only `runs-on: ubuntu-latest` for the benchstat job.
  </acceptance_criteria>
  <done>
    release.yml gates on ci; nightly.yml exists with long-form fuzz + benchstat + auto-PR; every action reference is SHA-pinned.
  </done>
</task>

<task type="auto">
  <name>Task 2: make verify-llms-sync + HashiCorp licence audit + docs/algorithms.md anchor casing</name>
  <files>Makefile, .planning/phases/08.5-review-remediation-gate/08.5-LICENCE-AUDIT.md, .github/workflows/ci.yml</files>
  <read_first>
    - Makefile in full (existing target patterns)
    - scripts/cmd/verify-exported-coverage/main.go (Plan 11 AST helper)
    - llms.txt + llms-full.txt
    - LICENSE
    - .github/workflows/ci.yml
  </read_first>
  <action>
1. make verify-llms-sync Makefile target:
   - Add target `verify-llms-sync: ; go run ./scripts/cmd/verify-llms-sync`.
   - The helper at scripts/cmd/verify-llms-sync/main.go (NEW) uses go/ast to enumerate exported symbols, parses llms.txt and llms-full.txt for symbol declarations, and asserts every exported symbol appears in both files.
   - Share the AST-walk function with Plan 11's helper by extracting `collectExportedSymbols()` into `scripts/internal/astwalk/astwalk.go` and importing from both helpers.
   - Add a verify-llms-sync invocation to .github/workflows/ci.yml as part of the build matrix.

2. HashiCorp utility-library licence audit:
   - Run `find . -name '*.go' -not -path './tests/*' | xargs grep -l "HashiCorp\\|hashicorp\\|MPL"` and capture output.
   - Expected: zero matches (pure-Go stdlib + golang.org/x/text only).
   - Document audit in .planning/phases/08.5-review-remediation-gate/08.5-LICENCE-AUDIT.md:
     - Header: date, auditor (Claude executor with algorithm-licensing-reviewer agent), exact command run, raw output.
     - Conclusion: no MPL-2.0 derivation; Apache-2.0 LICENSE is correct.
     - One paragraph noting the audit will repeat at v1.0 release per algorithm-licensing-standards SKILL.
   - If matches ARE found (unexpected), HALT and surface to algorithm-licensing-reviewer for review.

3. docs/algorithms.md H2 anchor casing:
   - Open docs/algorithms.md (currently 100% TBD scaffold).
   - Standardise H2 anchor casing to hyphenated form (e.g. `## Damerau-Levenshtein OSA` rather than `## DamerauLevenshteinOSA`).
   - Verify existing README cross-references match the new anchor casing.
   - Full algorithm-content fill-in is Plan 17's responsibility; this task only fixes anchor casing structure.
  </action>
  <verify>
    <automated>make verify-llms-sync</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "verify-llms-sync:" Makefile | wc -l` returns at least 1.
    - `ls scripts/cmd/verify-llms-sync/main.go` returns the file.
    - `make verify-llms-sync` exits 0 (llms files in sync with exported symbols).
    - `ls .planning/phases/08.5-review-remediation-gate/08.5-LICENCE-AUDIT.md` returns the file.
    - `grep -nE "HashiCorp|hashicorp|MPL" .planning/phases/08.5-review-remediation-gate/08.5-LICENCE-AUDIT.md | wc -l` returns at least 1 (audit mentions the term in its narrative).
    - docs/algorithms.md H2 anchors use hyphenated casing where multi-word algorithm names appear.
  </acceptance_criteria>
  <done>
    Makefile has verify-llms-sync target; CI invokes it; the helper at scripts/cmd/verify-llms-sync exists; the licence audit document records the HashiCorp/MPL audit result; docs/algorithms.md has consistent hyphenated H2 anchors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regenerate bench.txt baseline (Plan 06 + Plan 07 + Plan 08 must be green first)</name>
  <files>bench.txt</files>
  <read_first>
    - bench.txt (current state — 166 KB per RESEARCH.md)
    - .planning/phases/08.5-review-remediation-gate/08.5-06-SUMMARY.md (Plan 06 outcome — Q11b FMA landed)
    - .planning/phases/08.5-review-remediation-gate/08.5-07-SUMMARY.md (Plan 07 outcome — Q7a DM optimisation landed)
    - .planning/phases/08.5-review-remediation-gate/08.5-08-SUMMARY.md (Plan 08 outcome — Q8b Tokenise landed)
    - Makefile (find existing bench target)
  </read_first>
  <action>
1. Pre-execution verification:
   - Confirm Plan 06, 07, 08 SUMMARY files all exist (their respective work landed).
   - Run `go test -race ./...` and confirm exit 0.
2. Regenerate bench.txt:
   - Run `go test -bench=. -benchmem -count=10 -run=^$ ./... | tee bench.txt`.
   - The new bench.txt MUST include BenchmarkDefaultScorer_ASCII_Short and BenchmarkDefaultScorer_ASCII_Medium entries (Q13 + Q8c) — verify via grep.
   - If running on the self-hosted benchmark runner (mask/audit shared), prefer that hardware for stable numbers. Otherwise document the runner in the bench.txt header.
3. Inspect the diff vs old bench.txt:
   - Use `benchstat old-bench.txt bench.txt` (committed file vs new file) to see deltas.
   - Q7a is expected to show DoubleMetaphone alloc count drop (~6 → ~2).
   - Q8b is expected to show token-tier alloc improvements (TokenSort, TokenSet, PartialRatio, TokenJaccard, MongeElkan).
   - Q11b is expected to be performance-neutral.
   - Any unexpected regression >10% on a benchmark blocks the commit; investigate before committing.
4. Commit the regenerated bench.txt with message `chore(bench): regenerate baseline post-Q7a/Q8b/Q11b (Q13)`.
  </action>
  <verify>
    <automated>grep -cE "BenchmarkDefaultScorer_ASCII_(Short|Medium)" bench.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "BenchmarkDefaultScorer_ASCII_(Short|Medium)" bench.txt` returns at least 2.
    - `wc -l bench.txt` returns a value greater than the pre-regeneration line count (more benchmarks now present).
    - `grep -cE "^Benchmark" bench.txt` returns the full set of root-package benchmarks (≥ the count present in *_bench_test.go files).
    - No benchmark regresses by more than 10% vs the pre-Phase-8.5 bench.txt (verified by benchstat diff in the SUMMARY).
  </acceptance_criteria>
  <done>
    bench.txt is fully regenerated; includes the new BenchmarkDefaultScorer_* baselines; deltas are within expectations; the file is committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → workflow execution | Runners are remote infrastructure; SHA-pinned actions reduce supply-chain risk. |
| External action authors → repo code | Pinned SHA freezes the action version; any upstream tampering is detectable via SHA mismatch. |
| Benchmark hardware variance → benchstat regression detection | Self-hosted shared runner provides stable hardware; ubuntu-latest fallback is noisier. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-30 | S — Spoofing via action tag movement | actions references in workflow files | mitigate | SHA-pinning every action reference. Any upstream tag movement (e.g. v6 pointing at a different commit) cannot affect the pinned SHA. CI fails on SHA mismatch. |
| T-08.5-31 | T — Tampering via release path bypass | release.yml without needs:[ci] | mitigate | Adding `needs: [ci]` ensures a red CI cannot publish a release. Belt-and-braces alongside the tag-trigger-only release pipeline. |
| T-08.5-32 | I — Information disclosure via auto-PR | nightly.yml auto-PR for fuzz corpus | mitigate | Auto-PRs are opened by a controlled `peter-evans/create-pull-request` action; PRs require human approval before merge; fuzz corpus contains only synthetic test inputs (no PII). |
</threat_model>

<verification>
- release.yml gates on ci.
- nightly.yml exists with three jobs.
- All workflow action references SHA-pinned (grep test).
- make verify-llms-sync exits 0.
- 08.5-LICENCE-AUDIT.md documents the audit.
- bench.txt regenerated with BenchmarkDefaultScorer_* entries; no regression > 10%.
</verification>

<success_criteria>
- Q13 lock satisfied: release.yml needs:[ci] + nightly.yml + SHA pinning + verify-llms-sync + HashiCorp audit + bench.txt regen + docs/algorithms.md anchor casing.
- Phase 8.5 success criterion 6 (bench.txt includes BenchmarkDefaultScorer_* baselines + post-Q7a DM numbers; benchstat regression CI gate unblocked) satisfied.
- Phase 8.5 success criterion 8 (CI workflow split per CLAUDE.md, release.yml has needs:[ci], nightly.yml implemented, action SHA pinning, make verify-llms-sync plus CI check, bench-diff CI check) satisfied.
- The devops Important (×6), security Important (×2), test-writer Critical, algorithm-performance Critical findings on devops/bench close.
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-16-SUMMARY.md` documenting:
- the SHA values chosen for each action;
- the benchstat diff before/after;
- the licence audit raw output;
- any deviations from the expected bench numbers;
- agent reviews invoked (devops mandatory; security-reviewer for SHA pinning; algorithm-licensing-reviewer for the HashiCorp audit; algorithm-performance-reviewer for the bench.txt regen).
</output>
