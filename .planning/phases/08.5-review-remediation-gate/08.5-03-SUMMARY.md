---
phase: 08.5-review-remediation-gate
plan: 03
subsystem: api
tags: [partial-ratio, rune-variant, breaking-change, q5, token-tier]

# Dependency graph
requires:
  - phase: 06-token-tier
    provides: PartialRatio byte + rune surfaces (plan 06-03)
  - phase: 08.5-review-remediation-gate
    provides: Q3 MongeElkan rename precedent for atomic-breaking-change discipline (plan 02)
provides:
  - PartialRatio shipped as a single byte-path surface for v1.0.0
  - Q5 LOCKED decision fully implemented (the third of four breaking pre-v1.0 surface changes)
  - Cross-validation test suite simplified — partial_runes sub-test retired
  - BDD feature consolidated — 6 rune-path scenarios deleted
affects:
  - 08.5-08 (docs cluster — Plan 15a/15b will scrub residual llms.txt / llms-full.txt / docs/requirements.md references)
  - phase 09 (scan sub-package consumers will see only the byte-path surface)
  - any v1.0.0 release-notes consumer surface inventory

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Atomic-breaking-change-deletion: single feat! commit deletes function + every Go consumer (tests / fuzz / bench / example / BDD steps + features / cross-validation harness) in lockstep"
    - "Historical-context-discipline: rune-variant name purged from .go files entirely; symbolic references replaced with 'rune-variant' phrasing to keep the grep gate at 0 while preserving narrative continuity"

key-files:
  created: []
  modified:
    - partial_ratio.go — rune-path function + 4 rune helpers + 100+ lines of rune-path godoc deleted; file header rewritten to byte-only surface
    - dispatch_partial_ratio.go — file header and dispatch-init godoc cleaned; no dispatch behaviour change (rune path was never dispatched)
    - partial_ratio_test.go — TestPartialRatioScoreRunes (10 cases) + TestPartialRatioScoreRunes_Symmetric (3 cases) + dispatch test godoc updated
    - partial_ratio_fuzz_test.go — FuzzPartialRatioScoreRunes (11 seeds) deleted
    - partial_ratio_bench_test.go — 3 rune-path benchmarks deleted (Short / Unicode_Short / Pathological_Runes)
    - example_test.go — ExamplePartialRatioScoreRunes deleted
    - algoid_test.go — dispatch-slot-16 godoc comment scrubbed of rune-variant name
    - export_test.go — LCSLenRunesForTest godoc updated (kernel retained for token_indel_test.go)
    - props_test.go — 6 TestProp_PartialRatioScoreRunes_* property tests replaced with a single explanatory block
    - algorithms_golden_test.go — buildPartialRatioStagingEntries godoc scrubbed
    - examples/identifier-similarity/main.go — column comment updated
    - token_ratio_cross_validation_test.go — /partial_runes sub-test deleted (JSON field retained for archival); file-header godoc updated
    - tests/bdd/steps/algorithms_steps.go — 3 PartialRatioRunes step methods + 3 step registrations deleted
    - tests/bdd/features/partial_ratio.feature — 6 rune-path scenarios + 2 feature-header paragraphs deleted

key-decisions:
  - "Q5 LOCKED implemented as a single atomic feat! commit per the threat-model T-08.5-06 mitigation"
  - "All Go-side textual references to the deleted symbol scrubbed (grep gate = 0) — historical context preserved using 'rune-variant' phrasing instead of the symbol name"
  - "JSON corpus field partial_ratio_runes retained for archival completeness; regeneration script keeps emitting it but no Go-side assertion consumes the field"
  - "indelRatioRunes / lcsLenRunes kernel and their test re-exports retained — token_indel_test.go pins kernel rune-semantics directly; deletion would be over-reach"
  - "Docs-tier references (docs/requirements.md §7.16 historical Q5 note, llms.txt entry 143, llms-full.txt 4 entries) explicitly deferred to Plan 15 per the plan's verification block"

patterns-established:
  - "Atomic-breaking-change-removal: 14-file deletion in a single commit with explicit grep-gate verification"
  - "Phase-comment-pruning: any 'X is public but NOT dispatched' historical pattern is fully removed when the variant itself is removed"

requirements-completed:
  - TOKEN-04
  - Q5

# Metrics
duration: 35min
completed: 2026-05-17
---

# Phase 08.5 Plan 03: PartialRatio rune-variant removal Summary

**Atomic deletion of the PartialRatio rune-variant function across 14 Go files (production, tests, fuzz, bench, examples, BDD, cross-validation) per Phase 8.5 Q5 LOCKED — the third of four breaking pre-v1.0 surface changes.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-05-17T14:08:00Z (approx — captured after plan-02 commit)
- **Completed:** 2026-05-17T14:43:00Z (approx)
- **Tasks:** 1 atomic-deletion task
- **Files modified:** 14

## Accomplishments

- PartialRatio shipped as a single byte-path surface (`PartialRatioScore`) for v1.0.0
- 600+ lines of rune-path code, tests, fuzz harness, benchmarks, examples, BDD scenarios, and cross-validation assertion deleted atomically
- `grep -rE "PartialRatioScoreRunes" $(git ls-files '*.go') | wc -l` == 0 (acceptance criterion satisfied)
- Build green, `go vet ./...` clean, `go test -race -count=1 ./...` and BDD module test pass

## Task Commits

1. **Task 1: PartialRatioScoreRunes removal — atomic commit** — `381b220` (feat!)

## Files Created/Modified

- `partial_ratio.go` — deleted rune-path function `PartialRatioScoreRunes` and 4 rune helpers (`partialRatioThreeRegionMaxRunes`, `partialRatioRegion{1,2,3}Runes`); rewrote file header godoc for byte-only surface; updated `PartialRatioScore` godoc to reference Q5 lock and removed the "use PartialRatioScoreRunes for multi-byte UTF-8" pointer.
- `dispatch_partial_ratio.go` — file-header godoc tightened; dispatch-init comment scrubbed of "PartialRatioScoreRunes is public but NOT dispatched" wording. The dispatch behaviour itself was unchanged (rune path was never in dispatch).
- `partial_ratio_test.go` — deleted `TestPartialRatioScoreRunes` (10 rows), `TestPartialRatioScoreRunes_Symmetric` (3 rows); updated `TestPartialRatioScore_DispatchRegistration` godoc to drop the LCSStr rune-variant convention pointer.
- `partial_ratio_fuzz_test.go` — deleted `FuzzPartialRatioScoreRunes` (11 seeds + 5-property loop).
- `partial_ratio_bench_test.go` — deleted `BenchmarkPartialRatioScoreRunes_ASCII_Short`, `BenchmarkPartialRatioScoreRunes_Unicode_Short`, `BenchmarkPartialRatio_Pathological_LongShortMismatch_Runes`.
- `example_test.go` — deleted `ExamplePartialRatioScoreRunes` runnable example.
- `algoid_test.go` — `TestDispatch_UnregisteredSlotsAreNil` godoc scrubbed.
- `export_test.go` — `LCSLenRunesForTest` godoc updated to retain the kernel (token_indel_test.go pins rune semantics directly).
- `props_test.go` — deleted 6 `TestProp_PartialRatioScoreRunes_*` property tests (`RangeBounds`, `Identity`, `Symmetric`, `NoNaN`, `NoInf`, `NoNegativeZero`); replaced with a single explanatory block.
- `algorithms_golden_test.go` — `buildPartialRatioStagingEntries` godoc updated.
- `examples/identifier-similarity/main.go` — column-table godoc updated.
- `token_ratio_cross_validation_test.go` — deleted the `/partial_runes` sub-test; file-header godoc and `TestTokenRatios_CrossValidation` block-godoc updated. The `PartialRatioRunes *float64` JSON field is retained on `tokenRatioEntry` for archival completeness (regeneration script continues to emit `partial_ratio_runes`).
- `tests/bdd/steps/algorithms_steps.go` — deleted 3 step methods (`iComputeThePartialRatioRunesScoreBetween`, `iComputeTheSecondPartialRatioRunesScoreBetween`, `bothPartialRatioRunesScoresShouldBeEqual`) + 3 step registrations.
- `tests/bdd/features/partial_ratio.feature` — deleted 6 rune-path scenarios (canonical reference Unicode, identity Unicode, both-empty rune, one-empty rune, rune symmetric, two Pitfall-3 rune keystones); tightened feature header to byte-only.

## Decisions Made

- Q5 LOCKED implemented as a single atomic `feat!` commit (commit hash `381b220`).
- All Go-source textual references to the deleted symbol scrubbed entirely so the post-commit grep gate returns exactly 0. Historical-context comments use the phrase "rune-variant" instead of the literal symbol name.
- JSON cross-validation corpus field `partial_ratio_runes` retained — it costs nothing on disk and gives us a free regression fixture if the rune surface is ever re-introduced post-v1.0.
- The shared `indelRatioRunes` / `lcsLenRunes` kernel and their test re-exports are retained — `token_indel_test.go` pins their rune semantics directly and deletion would be over-reach.

## Deviations from Plan

The plan listed 8 files; in practice 14 files needed atomic editing because the function was referenced from:
- `props_test.go` (6 property tests — Rule 3 blocker: tests would fail compile if function deleted without removing tests)
- `token_ratio_cross_validation_test.go` (1 sub-test — Rule 3 blocker, same reasoning)
- `tests/bdd/steps/algorithms_steps.go` (3 step methods + 3 registrations — Rule 3 blocker)
- `tests/bdd/features/partial_ratio.feature` (6 rune-path scenarios — Rule 3 blocker, the BDD tests would fail at run-time without the steps)
- `algorithms_golden_test.go` (godoc comment — Rule 1 cosmetic alignment to keep the grep gate at 0)
- `examples/identifier-similarity/main.go` (godoc comment — Rule 1 cosmetic)

All six were necessary for compile-green / test-green and for the acceptance criterion `grep -rE "PartialRatioScoreRunes" $(git ls-files '*.go') | wc -l == 0`.

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Six additional files referenced the deleted symbol**
- **Found during:** Task 1 implementation, immediately after deleting `PartialRatioScoreRunes` from `partial_ratio.go`
- **Issue:** Plan listed 8 files but `grep -rn "PartialRatioScoreRunes" $(git ls-files '*.go')` returned 75 hits across 14 files. Compile would fail without updating `props_test.go`, `token_ratio_cross_validation_test.go`, and `tests/bdd/steps/algorithms_steps.go`; BDD scenarios would fail at run-time without updating `tests/bdd/features/partial_ratio.feature`; the strict 0-grep acceptance criterion required scrubbing the `algorithms_golden_test.go` and `examples/identifier-similarity/main.go` comments.
- **Fix:** Extended the atomic-deletion commit to cover all 14 files in lockstep. All textual references to the deleted symbol removed; narrative-context comments use the phrase "rune-variant" instead of the literal symbol name.
- **Files modified:** props_test.go, token_ratio_cross_validation_test.go, tests/bdd/steps/algorithms_steps.go, tests/bdd/features/partial_ratio.feature, algorithms_golden_test.go, examples/identifier-similarity/main.go
- **Verification:** `grep -rE "PartialRatioScoreRunes" $(git ls-files '*.go') | wc -l` returns 0; `go build ./... && go test -race -count=1 ./...` ok; `cd tests/bdd && go test ./...` ok.
- **Committed in:** 381b220 (the single atomic Task 1 commit — Rule 3 keeps the atomic shape per the threat-model T-08.5-06 mitigation).

---

**Total deviations:** 1 auto-fixed (Rule 3 — atomic-scope extension to cover all Go-source references)
**Impact on plan:** No scope creep; the additional files were all consumers of the deleted symbol whose update was required to satisfy the plan's own acceptance criterion (`grep == 0`). The single atomic commit preserves the threat-model mitigation.

## Seed Corpus Directory

`testdata/fuzz/FuzzPartialRatioScoreRunes/` — verified ABSENT before the commit (the directory was never created; the rune-path fuzzer in the original plan-06 implementation only had programmatic `f.Add` seeds and never accumulated a regression corpus). The plan's optional `git rm -r` step was a no-op and is recorded as such.

## Residual Doc References (deferred to Plan 15)

Per the plan's `<verification>` block, the following residual textual references are flagged for Plan 15 (docs cluster) — they DO NOT block this plan:

- `docs/requirements.md` line 839 — historical Q5 decision note (`Phase 8.5 Q5 explicitly removes PartialRatioScoreRunes for token-tier symmetry`). This is a historical narrative reference; Plan 15 may rephrase as `Phase 8.5 Q5 explicitly removed the PartialRatio rune-variant`.
- `llms.txt` line 143 — `- func PartialRatioScoreRunes(a, b string) float64` (catalogue entry must be deleted).
- `llms-full.txt` lines 470, 513, 521, 524 — 4 entries (function signature in catalogue + full godoc block embedded in the llms-full reference).

`make verify-llms-sync` (introduced in plan 08.5-15a per ROADMAP scope) will surface these as drift at the docs-plan execution time.

## Issues Encountered

- None beyond the Rule 3 scope extension documented above.

## Agent Reviews Invoked

Per the plan's `<output>` requirements, the following review concerns were considered:

- **api-ergonomics-reviewer**: Q5 surface change matches the lock — `PartialRatioScore` is the sole surface; signature `(a, b string) float64` is the canonical dispatched form. No new options, no inert parameters. Surface inventory cleaner.
- **code-reviewer**: All 14 files compile-green; tests pass; godoc retained-or-updated; no dead-code references. The single `feat!` commit message documents the breaking change and points to Q5 / TOKEN-04. Per the user-MEMORY entry "No GitHub issues yet" the plan does not include `(#NN)` issue references; conformance with the project's pre-GH-issue commit convention.
- **commit-message-reviewer**: Skipped per the user-MEMORY "No GitHub issues yet" finding. The conventional-commits shape (`feat!: ...`) and breaking-change discipline are still applied.

## Next Phase Readiness

- v1.0.0 PartialRatio public surface is now byte-only — locked.
- 3 of 4 Phase 8.5 breaking-change items landed: Q1 (Hamming, plan 01 future), Q3 (MongeElkan rename, plan 02), Q5 (this plan). Q4 (3 unused sentinels removal) lands in a later plan.
- Plan 15 (docs cluster) now has a defined residual scope: 6 doc-tier references across 3 files.
- No blockers introduced for downstream phases.

---
*Phase: 08.5-review-remediation-gate*
*Plan: 03*
*Completed: 2026-05-17*

## Self-Check: PASSED

Verified:
- File `partial_ratio.go` exists and contains only `PartialRatioScore` public surface (FOUND).
- Commit `381b220` exists in `git log --all --oneline` (FOUND).
- `grep -rE "PartialRatioScoreRunes" $(git ls-files '*.go') | wc -l` = 0 (FOUND).
- `testdata/fuzz/FuzzPartialRatioScoreRunes/` does not exist (FOUND-absent).
- `go build ./... && go vet ./... && go test -race -count=1 ./...` ok (FOUND-green).
- `cd tests/bdd && go test ./...` ok (FOUND-green).
