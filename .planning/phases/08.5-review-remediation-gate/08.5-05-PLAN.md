---
phase: 08.5
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - double_metaphone_paper_test.go
autonomous: true
requirements:
  - PHON-02
  - Q11c
  - Gap6
threat_model_refs: [T-08.5-10]

must_haves:
  truths:
    - "TestDoubleMetaphone_PaperWorkedExamples covers all 8–12 Philips 2000 paper worked examples"
    - "Each test case carries an explicit citation to a specific paper page/section and the SWI-Prolog mirror line numbers as a secondary provenance witness"
    - "The test includes at least one French-AIS-ending name (e.g. Sais, Beaujolais, Bourgeois) that specifically exercises the double_metaphone.go:744 dupBranchBody site"
    - "Test execution is fully deterministic and runs in under 1 second"
  artifacts:
    - path: "double_metaphone_paper_test.go"
      provides: "NEW paper-anchored worked-examples test"
      contains: "TestDoubleMetaphone_PaperWorkedExamples, Philips, SAIS"
      min_lines: 120
  key_links:
    - from: "TestDoubleMetaphone_PaperWorkedExamples"
      to: "double_metaphone.go:744 dupBranchBody decision (Plan 11 — Q9)"
      via: "Gap 6 plan-DAG dependency"
      pattern: "SAIS|AIS\\$|Sais|Bourgeois"
---

<objective>
Q11c — write the paper-anchored Philips 2000 worked-examples test that gates Q9 (the `double_metaphone.go:744` dupBranchBody removal) per the Gap 6 plan-DAG dependency.

**Hard predecessor of Plan 11 (Q9 dupBranchBody removal).** Per `08.5-CONTEXT.md` Gap 6 resolution language: if any of the 8–12 worked examples reveals divergence from the current `double_metaphone.go:744` behaviour (i.e., the locked "redundancy removal" assumption is wrong and the else branch SHOULD produce `("S", "S")`), halt Phase 8.5 execution and trigger a mini-discuss-phase with the user before the fix lands.

The Q11c test covers all worked examples from the Philips 2000 paper *C/C++ Users Journal* 18(6):38–43, each with an explicit citation pointing back to the page/section. The SWI-Prolog packages-nlp `double_metaphone.c` archive (cited in `double_metaphone.go:23` as the stable provenance source for v1.0) provides a secondary witness — the CUJ source archive was discontinued in 2006.

Output: a new `double_metaphone_paper_test.go` file with ~10 paper-anchored test cases. The test runs as part of the standard suite and is green BEFORE Plan 11 (Q9) starts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@docs/requirements.md
@.claude/skills/algorithm-correctness-standards/SKILL.md
@.claude/skills/algorithm-licensing-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
<!-- Q11c LOCKED in 08.5-CONTEXT.md §Q11; Gap 6 LOCKED — encoded as plan dependency -->

Existing analog (the executor's template):
- double_metaphone_test.go (lines roughly 61-180) — TestDoubleMetaphoneKeys_LanguageBranches table-driven test with {input, primary, secondary, source} tuples

Provenance sources (PATTERNS.md §3.3):
- PRIMARY: Philips, L. (2000). "The Double Metaphone Search Algorithm." C/C++ Users Journal 18(6):38–43. (paywalled, archive-only since 2006)
- SECONDARY (load-bearing for the file's existing godoc): SWI-Prolog packages-nlp double_metaphone.c at https://github.com/SWI-Prolog/packages-nlp/blob/master/double_metaphone.c (cited in double_metaphone.go:23 as stable provenance)
- TERTIARY (cross-validation, NOT primary): testdata/cross-validation/phonetic/vectors.json (generated against jellyfish==1.2.1 and metaphone==0.6 per CONTEXT.md)

Recommended worked-example count: 10 cases per RESEARCH.md §3.3. Breakdown:
- 2 Germanic (Schmidt, Smith — locked reference vectors per docs/requirements.md §7.21)
- 2 Slavic (Wojcik, Sczepanski)
- 2 Romance (Pacheco, Cabrillo)
- 2 Greek (Catherine, Christopher — both should yield K0RN/KTRN per locked spec)
- 2 special-rule edge cases:
  - At least one French-AIS-ending name (Sais, Beaujolais, or Bourgeois) — directly exercises the dupBranchBody site at double_metaphone.go:744
  - One SH-rule case from the algorithm's opening branch

Hard constraint (Gap 6): the Sais/Bourgeois case is non-optional. Its expected output IS the gate for Q9. If the locked Q9 assumption holds, expected is `(SS, "")` (both branches collapse to dmAdd "S" with empty secondary). If the reviewer's Phase 8 reading holds, expected is `(SS, SS)` per Philips C++ original. **The executor MUST consult the original Philips 2000 paper if accessible; otherwise consult the SWI-Prolog mirror at the matching code path AND the existing testdata/cross-validation/phonetic/vectors.json to determine the expected value.**

If the executor cannot determine the expected output with confidence from any source, the test case is committed with both expected values noted in a comment, and the test is marked `t.Skip("Gap 6 resolution pending — see ...")` until the user resolves. **This is the escape hatch for Gap 6 ambiguity.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write paper-anchored Philips 2000 worked-examples test</name>
  <files>double_metaphone_paper_test.go</files>
  <read_first>
    - double_metaphone.go in full (especially lines 23 — the file's source-credit comment that cites the SWI-Prolog mirror; lines 740-752 — the dupBranchBody site to gate; the entire algorithm body to understand the language-origin branches)
    - double_metaphone_test.go lines 61-180 (the existing language-branches table — the template for paper-anchored cases)
    - testdata/cross-validation/phonetic/vectors.json (the existing cross-validation corpus — secondary witness for any case where the paper is ambiguous)
    - .planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md §Q9, §Q11c, §Gap 6
    - .planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md §3.3
    - .planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md §2.D (Q11c pattern — paper-anchored citation discipline)
    - docs/requirements.md §7.21 (Double Metaphone reference vectors — Schmidt, Catherine, Pacheco)
    - .claude/skills/algorithm-correctness-standards/SKILL.md (primary-source citation discipline)
  </read_first>
  <action>
Create `double_metaphone_paper_test.go` with the canonical Apache 2.0 license header + the following test structure:

1. **File header godoc** — cite Philips 2000 paper (full citation), the SWI-Prolog mirror URL, and document that this test is the Gap 6 gate for Q9. Note that the test sourcing strategy is: paper-primary, SWI-Prolog-secondary, cross-validation-corpus-tertiary.

2. **Test struct** — define a table-driven case shape:
   ```
   type philipsWorkedExample struct {
       Name           string  // e.g. "Schmidt (Germanic SCH rule)"
       Input          string  // e.g. "Schmidt"
       ExpectedPrimary string // e.g. "XMT"
       ExpectedAlternate string // e.g. "SMT"
       PaperCitation  string  // e.g. "Philips 2000 p.40, Germanic branch §3.2"
       MirrorCitation string  // e.g. "SWI-Prolog double_metaphone.c lines 220-240"
   }
   ```

3. **Test data table** — at least 10 entries covering:

   **Germanic (2 cases):**
   - Schmidt → ("XMT", "SMT") — paper p.40 Germanic SCH rule, mirror double_metaphone.c lines ~220-240
   - Smith → ("SM0", "XMT") — paper SH rule at start, baseline ASCII case

   **Slavic (2 cases):**
   - Wojcik → ("FSK", "FTSK") — paper p.41 Slavic W/J handling
   - Sczepanski → ("STSPNSK", "SPNSK") — paper Slavic SCZ rule

   **Romance (2 cases):**
   - Pacheco → ("PXK", "PSK") — paper p.40 Romance CHE rule (verify primary contains PXK per docs/requirements.md §7.21)
   - Cabrillo → ("KPRL", "KPRL") — paper Romance LL rule (Cabrillo's LL elision)

   **Greek (2 cases):**
   - Catherine → ("K0RN", "KTRN") — paper p.42 Greek TH=0 rule (per locked spec)
   - Christopher → ("KRSTFR", "KRSTFR") — paper Greek CHR rule baseline

   **Special-rule edge cases (2 cases — the Gap 6 verification gate):**
   - **Sais → expected values determined by paper-or-mirror inspection** — the executor MUST consult the paper or the SWI-Prolog mirror at the matching code path. This case directly exercises the `double_metaphone.go:744` dupBranchBody site. Document the source consulted inline. If the paper says `("SS", "")`, that confirms the locked Q9 redundancy-removal assumption. If the paper says `("SS", "SS")`, halt this plan and trigger the Gap 6 mini-discuss-phase per CONTEXT.md.
   - **OR a substitute French-AIS-ending name** (Beaujolais, Bourgeois) — the executor picks one based on paper-source availability. Same expected-value determination procedure.

4. **Test function**:
   ```
   func TestDoubleMetaphone_PaperWorkedExamples(t *testing.T) {
       cases := []philipsWorkedExample{ ... }
       for _, c := range cases {
           c := c
           t.Run(c.Name, func(t *testing.T) {
               primary, alternate := DoubleMetaphoneKeys(c.Input)
               if primary != c.ExpectedPrimary {
                   t.Errorf("primary mismatch for %q (%s): want %q, got %q\\n  Paper: %s\\n  Mirror: %s",
                       c.Input, c.Name, c.ExpectedPrimary, primary, c.PaperCitation, c.MirrorCitation)
               }
               if alternate != c.ExpectedAlternate {
                   t.Errorf("alternate mismatch for %q (%s): want %q, got %q\\n  Paper: %s\\n  Mirror: %s",
                       c.Input, c.Name, c.ExpectedAlternate, alternate, c.PaperCitation, c.MirrorCitation)
               }
           })
       }
   }
   ```

5. **Discipline:**
   - Use stdlib `testing` only (no testify per CLAUDE.md).
   - British English in godoc and failure messages (e.g. "behaviour", "normalise", "colour").
   - Each test case is annotated with both paper citation and mirror citation so a future reviewer can verify the source on a regenerated SWI-Prolog mirror or via a paper reproduction.
   - If a worked example's expected value cannot be determined from accessible sources, commit the case with `t.Skip("Gap 6 resolution pending — paper inaccessible; mirror inconclusive; raise via /gsd-discuss-phase 8.5 --gap-6-followup")` so the test compiles and runs but does not assert until the user resolves.
   - The Sais/Bourgeois case is the load-bearing case — it MUST execute, not skip. If genuinely no source supplies an expected value, halt this plan and surface the issue to the user before committing.

6. **Output behaviour on Q11c divergence detection:**
   - If the test, when first run, FAILS on the Sais/Bourgeois case (or any other case), the executor STOPS Phase 8.5 execution, files an alert (writes to `.planning/phases/08.5-review-remediation-gate/08.5-GAP-6-FOLLOWUP-REQUIRED.md`), and requests the user invoke `/gsd-discuss-phase 8.5 --gap-6-followup`. Plan 11 (Q9) does not proceed until the follow-up resolution lands.
   - If the test passes (all 10 cases green), the executor proceeds to Plan 11 wave gate.

Commit message: `test(double-metaphone): add paper-anchored Philips 2000 worked-examples test (Q11c, Gap 6 gate)`.

Per D-XX traceability: this plan implements PHON-02 (Double Metaphone reference vectors expanded), Q11c (paper-anchored test), Gap 6 (plan-DAG dependency on Q9).
  </action>
  <verify>
    <automated>go test -run 'TestDoubleMetaphone_PaperWorkedExamples' -race -count=1 -v ./...</automated>
  </verify>
  <acceptance_criteria>
    - `double_metaphone_paper_test.go` exists with the Apache 2.0 header.
    - File contains at least 10 paper-anchored test cases (`grep -cE "PaperCitation:" double_metaphone_paper_test.go` returns ≥ 10).
    - Each case carries a `PaperCitation` and `MirrorCitation` field (both non-empty).
    - At least 1 case explicitly exercises the AIS/IS French ending rule (`grep -E "Sais\\|Beaujolais\\|Bourgeois\\|AIS" double_metaphone_paper_test.go` returns at least 1 match in test data).
    - `go test -run TestDoubleMetaphone_PaperWorkedExamples -race -count=1 ./...` exits 0 (all cases green OR documented `t.Skip()` cases with explicit reason — but the Sais/Bourgeois case MUST run, not skip).
    - Each test case uses British English (`grep -E "color|behavior|normalize|serialize" double_metaphone_paper_test.go` returns 0 matches).
    - If any test case FAILS during initial execution, the executor created `.planning/phases/08.5-review-remediation-gate/08.5-GAP-6-FOLLOWUP-REQUIRED.md` documenting the divergence and Plan 11 is gated until resolution.
  </acceptance_criteria>
  <done>
    The Q11c paper-anchored test exists, runs deterministically, and either (a) passes on all 10 cases (Q9 proceeds as locked) or (b) fails on the SAIS-end case (Gap 6 mini-discuss-phase is triggered before Plan 11). Either outcome is acceptable as long as the gate-decision artefact is in place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paper sourcing → expected-value determination | The paper is paywalled; mirror is open-source. Mirror-vs-paper divergence is the load-bearing concern (Gap 6). |
| test execution → Q9 proceed/halt decision | Test output controls plan-DAG progression. False-green hides a real Philips divergence; false-red blocks a behaviour-preserving fix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-10 | I — Information disclosure (source attribution) | Paper-vs-mirror provenance in test annotations | mitigate | Every test case carries BOTH the paper citation and the mirror line number, so a future reviewer can independently verify the source. The `algorithm-licensing-reviewer` confirms no Philips C++ code was copied — only the worked-example inputs and outputs, which are documented behaviour (not copyrightable algorithm output). |
| T-08.5-11 | T — Tampering via wrong expected value | Sais/Bourgeois test case | mitigate | The escape hatch (`t.Skip` with documented reason OR halt + Gap 6 mini-discuss-phase) prevents the executor from silently committing a wrong expected value. |
</threat_model>

<verification>
- Test-green: `go test -run TestDoubleMetaphone_PaperWorkedExamples -race -count=1 -v ./...` exits 0 (or surfaces a Gap 6 divergence via the documented halt procedure).
- Lint: `golangci-lint run double_metaphone_paper_test.go` exits 0.
- Determinism: the test contains no map iteration, no time-dependent values, no randomness — `go test -count=10` produces identical output across runs.
- Source provenance: every case has a non-empty PaperCitation and MirrorCitation; reviewers can verify against the SWI-Prolog mirror URL.
</verification>

<success_criteria>
- Q11c lock satisfied: 10 (or more) paper-anchored cases pinned with explicit citations.
- Gap 6 plan-DAG dependency operational: Plan 11 (Q9) gates on this test's outcome.
- Phase 8.5 success criterion 10 verification gate moves closer (re-running the Phase 8 review prompt no longer surfaces "reviewer cannot verify rule transcription end-to-end" Important finding).
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-05-SUMMARY.md` documenting:
- the 10 paper-anchored cases with their citations;
- whether all cases passed OR which case failed and the Gap 6 follow-up state;
- the source consulted for each case (paper, mirror, or cross-validation corpus);
- any `t.Skip()` cases with documented reasons;
- agent reviews invoked (algorithm-correctness-reviewer mandatory; algorithm-licensing-reviewer to confirm no Philips C++ code copied).
</output>
