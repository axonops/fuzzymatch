---
phase: 08.5
plan: 18
type: execute
wave: 7
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12, 13, 14, 15a, 15b, 16, 17a, 17b]
files_modified:
  - .planning/phases/08.5-review-remediation-gate/08.5-VERIFICATION.md
  - REVIEW-FINDINGS.md
autonomous: false
requirements:
  - VerificationGate
threat_model_refs: []

must_haves:
  truths:
    - "Re-running the Phase 8 comprehensive review prompt with the 14 specialist agents returns zero Critical and zero Important findings"
    - "Improvement-tier residue (if any) is itemised in the verification report with reasoned acceptance per Q14b Path A residue handling"
    - "Gap 3 deferred items (scan.feature + suppression.feature) are explicitly acknowledged in the verification report"
    - "Gap 6 conditional follow-up state recorded: either Q9 proceeded as locked OR a mini-discuss-phase resolution is documented"
    - "All 14 Q-decisions + 7 gap resolutions are traceable to specific plans + commits"
    - "The full make check + tests/bdd + cross-platform CI matrix is green"
  artifacts:
    - path: ".planning/phases/08.5-review-remediation-gate/08.5-VERIFICATION.md"
      provides: "Verification report: zero Critical, zero Important, residue list, deferred items, agent panel sign-offs"
      contains: "Critical: 0, Important: 0"
    - path: "REVIEW-FINDINGS.md"
      provides: "Updated review state — 465 → ≤ Improvement-only count post-remediation"
      contains: "Phase 8.5 verification"
  key_links:
    - from: "08.5-VERIFICATION.md"
      to: "Phase 9 unblock"
      via: "verification gate sign-off"
      pattern: "zero Critical"
---

<objective>
**Phase 8.5 verification gate** — the final plan. Re-run the Phase 8 comprehensive review prompt against the post-remediation codebase. Per ROADMAP Phase 8.5 success criterion 10: zero Critical and zero Important findings required to advance to Phase 9.

The verification gate is the load-bearing decision point: if zero Critical + zero Important, Phase 8.5 is complete and Phase 9 can begin. Improvement-tier residue is acceptable but must be acknowledged.

**Hard dependencies:** ALL 19 prior plans MUST land green before this plan starts. The verification gate cannot proceed if any predecessor plan has an open finding.

This plan is **NOT autonomous** — the verification report includes a human-action checkpoint where the user reviews the report before sign-off.

Output: 08.5-VERIFICATION.md with the re-review results, residue acknowledgement, agent panel sign-offs, and the Phase 9 unblock decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@.planning/phases/08.5-review-remediation-gate/08.5-VALIDATION.md
@.planning/phases/08.5-review-remediation-gate/08.5-01-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-02-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-03-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-04-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-05-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-06-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-07-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-08-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-09-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-10-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-11-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-12-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-13-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-14-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-15a-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-15b-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-16-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-17a-SUMMARY.md
@.planning/phases/08.5-review-remediation-gate/08.5-17b-SUMMARY.md
@REVIEW-FINDINGS.md
@.planning/ROADMAP.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
14 specialist review agents per .claude/skills/fuzzymatch-review-protocol/SKILL.md (the agent panel that produced the original 465 Phase 8 findings):
- algorithm-licensing-reviewer
- algorithm-correctness-reviewer
- algorithm-performance-reviewer
- determinism-reviewer
- api-ergonomics-reviewer
- bdd-scenario-reviewer
- test-writer
- test-analyst
- security-reviewer
- code-reviewer
- docs-writer
- user-guide-reviewer
- devops
- go-quality

(Excluding commit-message-reviewer and issue-writer per project memory `no_github_issues.md` — no issue-ref findings to track.)

Phase 8 comprehensive review prompt: located at the path the user invoked originally — locate via `ls REVIEW-*.md` or check `.planning/reviews/` directory.

Gap acknowledgements required in the report:
- Gap 3: scan.feature + suppression.feature deferred to Phase 9 (CONTEXT.md default)
- Gap 6: Q9 outcome (proceeded as locked OR mini-discuss-phase resolution)
- Q14a: cross-algorithm tokenisation cache deferred to v1.x (GitHub issue placeholder pending — no GH issues yet)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification baseline check</name>
  <files></files>
  <read_first>
    - All 19 SUMMARY files for the prior plans (verify each plan completed successfully)
    - .planning/phases/08.5-review-remediation-gate/08.5-GAP-6-FOLLOWUP-REQUIRED.md (verify NOT present)
  </read_first>
  <action>
1. Verify all 19 prior plans have a SUMMARY file. If any is missing, halt and report.
2. Run the full quality gate locally:
   - `make check` — full quality gate (lint + vet + test -race + vulncheck + gosec + markdownlint + tidy-check + coverage Floor 1/2/3 + verify-no-deps + verify-determinism).
   - `make test-bdd` — full BDD suite.
   - `make verify-llms-sync` — Plan 16's sync gate.
3. Confirm the cross-platform CI matrix on the most recent PR is green across all 5 platforms.
4. Confirm Plan 14 (Q9) did not trigger a Gap 6 mini-discuss-phase. If 08.5-GAP-6-FOLLOWUP-REQUIRED.md exists, halt and report.
  </action>
  <verify>
    <automated>make check && make test-bdd && make verify-llms-sync</automated>
  </verify>
  <acceptance_criteria>
    - All 19 SUMMARY files exist (verify with `for n in 01 02 03 04 05 06 07 08 09 10 11 12 13 14 15a 15b 16 17a 17b; do test -f .planning/phases/08.5-review-remediation-gate/08.5-$n-SUMMARY.md || { echo missing 08.5-$n-SUMMARY.md; exit 1; }; done`).
    - `make check` exits 0.
    - `make test-bdd` exits 0.
    - `make verify-llms-sync` exits 0.
    - `.planning/phases/08.5-review-remediation-gate/08.5-GAP-6-FOLLOWUP-REQUIRED.md` does NOT exist.
    - CI matrix is green on the latest PR (verify via gh CLI or by checking the PR status manually).
  </acceptance_criteria>
  <done>
    Baseline is green; all 19 plans landed; no Gap 6 escape-hatch triggered; ready to invoke the agent panel.
  </done>
</task>

<!--
  Task 2 manual-orchestration note (m14 revision 2026-05-17):
  The GSD task taxonomy uses `checkpoint:*` types for tasks that interact with the human.
  This task is different: it orchestrates 14 specialist sub-agents in parallel and
  consolidates their qualitative findings. There is no single shell command that
  captures the panel sweep (which is why `<automated>` is intentionally empty).
  Conceptually this is a `checkpoint:manual-orchestration` step. The execute-plan
  workflow accepts it as a `type="auto"` task with an empty `<automated>` block on
  the explicit understanding that the Task 4 human-verify checkpoint is the
  ultimate gate. Cross-referenced from 08.5-VALIDATION.md "Manual-Only Verifications"
  table row "Verification-gate re-review with 14 specialist agents (Plan 18 Task 2)".
-->
<task type="auto">
  <name>Task 2: Invoke the 14 specialist review agents in parallel</name>
  <files></files>
  <read_first>
    - .claude/skills/fuzzymatch-review-protocol/SKILL.md (agent panel and ordering)
    - REVIEW-FINDINGS.md (the original 465-finding baseline)
    - .claude/agents/*.md (each agent's authority + check criteria — locate via `ls .claude/agents/`)
  </read_first>
  <action>
Invoke each of the 14 specialist agents via the Task tool, passing the relevant file paths and the change description (Phase 8.5 remediation). Where independent, run in parallel.

For each agent, the prompt template is:
"Phase 8.5 (Review Remediation Gate) is complete. Re-review the codebase under your authority area and return: Critical findings count, Important findings count, Improvement findings count. Compare against the original Phase 8 baseline (REVIEW-FINDINGS.md) and indicate which of the original findings in your area are now CLOSED, OPEN, or DEFERRED. Specifically verify the Q-decisions and gap resolutions that fall under your authority per the 08.5-CONTEXT.md scope-cluster-to-agent mapping in 08.5-PATTERNS.md §3 Shared - Agent gate wiring."

Agent authority areas (per fuzzymatch-review-protocol SKILL):

1. algorithm-licensing-reviewer — verify the HashiCorp audit in Plan 16; verify no GPL/LGPL derivation introduced; verify the jellyfish + py_stringmatching licences acceptable for Plan 10 cross-validation corpora.

2. algorithm-correctness-reviewer — verify Q9 simplification + Q11c paper-anchored test (Plans 5, 14); verify the 3 new cross-validation corpora correctness (Plan 10); verify the per-algorithm rule table in Validate (Plan 13); verify the cross-algorithm convergence tests (Plan 12 Task 4).

3. algorithm-performance-reviewer — verify Q7a/Q7b/Q7c/Q7d/Q8a/Q8b/Q8c/Q8d budget revisions + benchmark assertions (Plans 7, 8); verify bench.txt regeneration (Plan 16).

4. determinism-reviewer — verify Q11b FMA double-cast at both sites (Plan 6); verify no map iteration on output paths in Validate (Plan 13); verify cross-platform CI matrix golden-byte-identity; verify the Q12b property-test generator distribution (Plan 11).

5. api-ergonomics-reviewer — verify Q3 MongeElkan rename + opts removal (Plan 2); verify Q5 PartialRatioScoreRunes removal (Plan 3); verify Q2 NaN/Inf/α+β guards (Plan 4); verify Q4 Validate + Warning + WarnKind surface (Plan 13); verify Gap 4 ErrInvalidAlgoID rename (Plan 1); verify Gap 5 typed panic (Plan 1); verify Gap 7 outcomes (Plan 15).

6. bdd-scenario-reviewer — verify Gap 2 BDD scenarios for the 3 renamed sentinels (Plan 15); verify Gap 3 normalisation.feature + determinism.feature (Plan 17); verify Q4 validate.feature (Plan 13); verify Q8b BDD scenario (Plan 8).

7. test-writer — verify 9 staging tests added (Plan 9); verify 3 meta-tests + FuzzScorer + 9 rune fuzz + 4 distance fuzz + 1 phonetic-code fuzz (Plan 12); verify 3 cross-validation corpora + loaders (Plan 10).

8. test-analyst — verify Q12a Floor 3 AST helper + Q12b generators (Plan 11); verify the Scorer property tests + ScoreAll determinism (Plan 12 Task 4); verify the cross-validation tests close the gaps.

9. security-reviewer — verify Q2 NaN/Inf guards close the security Critical findings (Plan 4); verify Q13 SHA-pinning closes the security Important findings (Plan 16); verify the threat models in each plan's PLAN.md are reasonable.

10. code-reviewer — verify all code changes across all 19 plans are clean Go.

11. docs-writer — verify Q4 Validate's 6 doc surfaces complete (Plans 13, 17); verify Q12c panic-surface section (Plan 17); verify the README + llms.txt + docs/algorithms.md updates (Plan 17); verify British English sweep.

12. user-guide-reviewer — verify the Validate-then-Score Common Pattern in README (Plan 17); verify docs are consumer-friendly + the runnable example works (Plan 17 Task 3).

13. devops — verify Q13 release.yml needs:[ci] + nightly.yml + SHA pinning + verify-llms-sync + bench.txt (Plan 16); verify the workflow YAML syntax + lint clean.

14. go-quality — verify golangci-lint v2 clean post-Plan-15; verify gocyclo / staticcheck / gocritic / gosec / gofumpt / misspell / unparam all green or have justified //nolint annotations.

Collect each agent's response. If any agent reports a non-zero Critical or non-zero Important count that is NOT already documented as deferred (Gap 3, Q14a), HALT and surface for user decision (additional remediation plan, defer to v1.x, or accept and document).
  </action>
  <verify>
    <automated></automated>
  </verify>
  <acceptance_criteria>
    - All 14 specialist agents invoked.
    - Each agent returns a count: Critical, Important, Improvement.
    - Total Critical across all agents = 0.
    - Total Important across all agents = 0 OR every Important finding falls under a documented deferral (Gap 3 scan/suppression, Q14a cache, etc.).
    - Improvement-tier residue is documented but does not block.
  </acceptance_criteria>
  <done>
    14 agent reports collected; the panel collectively returns zero Critical + zero Important (or documented deferrals only).
  </done>
</task>

<task type="auto">
  <name>Task 3: Write 08.5-VERIFICATION.md report + update REVIEW-FINDINGS.md</name>
  <files>.planning/phases/08.5-review-remediation-gate/08.5-VERIFICATION.md, REVIEW-FINDINGS.md</files>
  <read_first>
    - Task 2's agent responses (collected during prior task)
    - All 19 SUMMARY files for traceability
    - REVIEW-FINDINGS.md current state
  </read_first>
  <action>
1. Create `.planning/phases/08.5-review-remediation-gate/08.5-VERIFICATION.md` with the following structure:

   - **Header**: Date, Phase 8.5 outcome (`Critical: 0, Important: 0, Improvement: <N>`).
   - **Agent panel results**: a table with one row per agent, columns: agent name, Critical (before/after), Important (before/after), Improvement (before/after), sign-off status.
   - **Q-decisions traceability**: a table mapping each of the 14 Q-decisions (Q1-Q14) + 7 gap resolutions (Gap 1-7) to the plan(s) that implemented them + the commit SHA + the agent that signed it off.
   - **Closed findings summary**: a Markdown list of the major Critical / Important finding themes from the original Phase 8 review, marked CLOSED with reference to the plan that closed each.
   - **Deferred items**:
     - Gap 3: scan.feature + suppression.feature deferred to Phase 9.
     - Q14a: cross-algorithm tokenisation cache deferred to v1.x (GitHub issue placeholder pending — project has no GH issues yet).
     - Any other Improvement-tier items the panel surfaced as acceptable residue.
   - **Improvement-tier residue**: enumeration of all remaining Improvement-tier findings with reasoned acceptance per Q14b Path A residue handling.
   - **Phase 9 unblock decision**: "Phase 9 (Collection Scan sub-package) is UNBLOCKED" — assuming the gate passed.

2. Update `REVIEW-FINDINGS.md`:
   - Add a new section header at top: `## Phase 8.5 Verification (YYYY-MM-DD)`.
   - Mark the 74 Critical findings + 195 Important findings as CLOSED with a reference to the Phase 8.5 plan that closed each.
   - Preserve the original Phase 8 finding text for historical traceability.
   - Note any deferred items explicitly.

3. Final discipline: the verification report is the OUTPUT artefact of the entire phase. It MUST be reproducible from the SUMMARY files (no hand-waving). Future readers should be able to trace any closed finding back to its plan + commit.

4. Commit message: `docs(08.5): verification report — Critical 0 / Important 0 / Improvement <N> — Phase 9 unblocked`.

5. **Human-action checkpoint**: pause here. The user reviews 08.5-VERIFICATION.md and approves before STATE.md is advanced to Phase 9.
  </action>
  <verify>
    <automated>ls .planning/phases/08.5-review-remediation-gate/08.5-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/08.5-review-remediation-gate/08.5-VERIFICATION.md` returns the file.
    - The report header explicitly states `Critical: 0, Important: 0`.
    - All 14 agents have a row in the agent-panel table with a sign-off status.
    - All 14 Q-decisions + 7 gap resolutions have a row in the traceability table.
    - Gap 3 + Q14a + (Gap 6 if applicable) deferrals are explicitly documented.
    - REVIEW-FINDINGS.md has a new Phase 8.5 verification section.
    - User approval recorded in the human-action checkpoint section of the report.
  </acceptance_criteria>
  <done>
    08.5-VERIFICATION.md exists with full traceability; Phase 9 is unblocked; STATE.md can advance to Phase 9 after user sign-off.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human review and sign-off on Phase 8.5 verification report</name>
  <what-built>
The full Phase 8.5 remediation pass across 19 prior plans + a verification report (08.5-VERIFICATION.md) summarising the 14-agent panel results. The verification gate (zero Critical, zero Important) has been verified by re-running the Phase 8 comprehensive review prompt.
  </what-built>
  <how-to-verify>
1. Read `.planning/phases/08.5-review-remediation-gate/08.5-VERIFICATION.md` in full.
2. Verify the header states `Critical: 0, Important: 0`.
3. Spot-check the agent-panel table: each of the 14 agents has signed off (no agent in CHANGES_REQUESTED state).
4. Spot-check the Q-decisions traceability table: pick 3 random Q-decisions (e.g. Q3, Q9, Q11b) and verify the cited plan + commit SHA actually implemented the decision.
5. Verify the deferred items list is complete: Gap 3 (scan/suppression.feature), Q14a (tokenisation cache).
6. Run `make check` locally — exits 0.
7. Run `cd tests/bdd && go test ./...` locally — exits 0.
8. Confirm the latest PR's CI matrix is green across all 5 platforms.

If all checks pass, type "approved" to advance STATE.md to Phase 9. If any check fails, describe the issue and the executor will queue additional remediation.
  </how-to-verify>
  <resume-signal>Type "approved" to advance to Phase 9, or describe issues to trigger additional remediation</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent panel collective sign-off → Phase 9 unblock | The panel is the load-bearing decision; user has final approval via checkpoint. |

## STRIDE Threat Register

(No new threats introduced by the verification step itself. The verification report consolidates threat-model statements from the 19 prior plans.)
</threat_model>

<verification>
- All 14 specialist agents return zero Critical + zero Important.
- make check + make test-bdd + verify-llms-sync all green.
- Cross-platform CI matrix green.
- 08.5-VERIFICATION.md exists with full traceability.
- User has explicitly approved.
</verification>

<success_criteria>
- Phase 8.5 success criterion 10 (verification gate: zero Critical + zero Important) SATISFIED.
- All 14 Q-decisions traceable to plans + commits.
- All 7 gap resolutions traceable to plans + commits.
- Phase 9 (Collection Scan Sub-package) is UNBLOCKED.
- STATE.md advances from Phase 8.5 to Phase 9 after user approval.
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-SUMMARY.md` (the phase-level summary, distinct from the per-plan SUMMARY files). The 08.5-VERIFICATION.md itself IS the artefact this plan produces; the phase-level SUMMARY references it.

Update `.planning/STATE.md`:
- Set milestone progress: phases completed +1.
- Advance "Current focus" from Phase 8 → Phase 9.
- Append a "2026-MM-DD: Phase 8.5 complete" entry to the session-continuity log.
- Mark Phase 8.5 row in the progress table as ✓ Complete with completion date.

Update `.planning/ROADMAP.md` Phase 8.5 progress checkbox from `[ ]` to `[x]`.
</output>
