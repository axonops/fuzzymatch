---
phase: 08.5
plan: secure-phase
type: security-audit
date: 2026-05-19
mode: VERIFY-MITIGATIONS
threats_total: 33
threats_closed: 33
threats_open: 0
asvs_level: 2
outcome: SECURED
---

# Phase 8.5 — Security Audit (per-threat verification)

**Mode:** VERIFY-MITIGATIONS — for each of the 33 documented threats (T-08.5-01 .. T-08.5-33), confirm the declared mitigation is actually present in the post-Phase-8.5 codebase. No blind scanning for new vulnerabilities; the register is authoritative.

**Corroborating evidence (not substitute):** 14-agent panel APPROVED with 0 Critical / 0 Important / 2 Improvement on commit `e5ad450`; CI matrix green on all 5 platforms.

## Verification Verdict

**SECURED.** All 33 documented threats have their declared mitigation present in the implementation, in tests, in workflows, or in the accepted-risks/transfer documentation as appropriate.

## Per-Threat Verification Table

| Threat ID | Category | Component | Disposition | Status | Evidence |
|-----------|----------|-----------|-------------|--------|----------|
| T-08.5-01 | I — Info disclosure (sentinel identity) | errors.go sentinel set | mitigate | CLOSED | `errors.go:67,93,124,155,170,183,195,211,240,276` — every sentinel is `var ErrX = errors.New(...)`, consumed via `errors.Is`. Godoc on errors.go lines 28+ warns "never match the error message string." |
| T-08.5-02 | D — DoS via untyped panic value | scorer.go DefaultScorer | mitigate | CLOSED | `scorer.go:621` — `panic(fmt.Errorf("%w: DefaultScorer construction failed: %w", ErrInternalInvariantViolated, err))`. Backed by `scorer_internal_test.go:279` (`errors.Is(_, ErrInternalInvariantViolated)`). |
| T-08.5-03 | T — Tampering via partial atomicity | Gap 4 rename across 5 files | mitigate | CLOSED | `grep -rn ErrInvalidAlgorithm $(git ls-files '*.go' '*.txt')` returns 0 — zero stragglers post-rename. |
| T-08.5-04 | T — Tampering (semantic confusion) | MongeElkanScore name flip | mitigate | CLOSED | `monge_elkan.go:485` — `MongeElkanScore` is now symmetric-by-default; `monge_elkan.go:383` — `MongeElkanScoreAsymmetric` exists as the directional companion. Symmetric/asymmetric invariant pinned at `cross_algorithm_consistency_test.go:707`. |
| T-08.5-05 | D — DoS via permitted-inner bypass | dispatch_monge_elkan.go binding | accept | CLOSED | Phase 6's `permittedMongeElkanInner` allow-list survives at `monge_elkan.go:296` and the guard fires at `monge_elkan.go:387` (`if !permittedMongeElkanInner[inner]`). Accepted disposition is documented and the allow-list is operational. |
| T-08.5-06 | T — Tampering via incomplete deletion | PartialRatioScoreRunes removal | mitigate | CLOSED | `grep -rn PartialRatioScoreRunes --include='*.go' .` returns 0 — single atomic deletion across all sources. |
| T-08.5-07 | D — DoS via NaN propagation | scorer reduction loop | mitigate | CLOSED | `scorer_options.go:171` (WithAlgorithm), `:313` (WithThreshold), `:459,465` (WithTverskyAlgorithm) — every option-construction path rejects NaN/Inf at construction time before reaching the reduction. |
| T-08.5-08 | T — Tampering via invalid Tversky | TverskyScore direct call | mitigate | CLOSED | `tversky.go:278-282,325-329` — direct-call paths panic with `fmt.Errorf("%w: ...", ErrInvalidTverskyParam, ...)`. |
| T-08.5-09 | I — Info disclosure (panic message) | TverskyScore panic format | accept | CLOSED | Panic message at `tversky.go:279,282,326,329` includes `alpha=%v, beta=%v` — these are caller-supplied parameters, not internal state; accepted-risk disposition stands. |
| T-08.5-10 | I — Info disclosure (paper-vs-mirror provenance) | DoubleMetaphone test annotations | mitigate | CLOSED | `double_metaphone_paper_test.go:110-193` — every test case carries `PaperCitation` (e.g. "Philips 2000 p.40, Germanic SCH rule"). Worked-example inputs/outputs are documented behaviour, not copyrightable code. |
| T-08.5-11 | T — Tampering via wrong expected value | Sais/Bourgeois case | mitigate | CLOSED | `double_metaphone_paper_test.go:228-262` — the Sais case carries the documented escape-hatch language ("if this is the Sais/French-AIS-end case, the Gap 6 mini-discuss-phase ...") preventing silent commit of a wrong expected value. |
| T-08.5-12 | T — Tampering via FMA emission | cosine.go + scorer.go | mitigate | CLOSED | `cosine.go:359` — `dot = float64(float64(qa[k])*float64(qb[k])) + dot`; `scorer.go:386` — `acc = float64(entry.weight*score) + acc`. Outer `float64(...)` is the Go-toolchain rounding fence per docs/requirements.md §14.4. Cross-platform CI matrix on 5 platforms gates regressions. |
| T-08.5-13 | D — DoS via golden-file thrash | algorithms.json + scorer-default.json | accept | CLOSED | CI matrix on `.github/workflows/ci.yml` covers ubuntu-latest, ubuntu-24.04-arm, macos-15-intel, macos-latest, windows-latest (5 platforms). Partial-commit hazard mitigated by matrix gating; accepted-risk disposition stands. |
| T-08.5-14 | I — Info disclosure (stack-buffer over-read) | dmAdd | mitigate | CLOSED | `double_metaphone.go:158,172` — bounds checks `if p != "" && *pLen < dmMaxLen` and `if target != "" && *sLen < dmMaxLen` before each byte write. |
| T-08.5-15 | D — DoS via heap-fallback amplification | DL-Full, RO, LCSStr | accept | CLOSED | `docs/algorithms.md:111` heap-fallback scope note + `docs/algorithms.md:1528` 5 MB/call ceiling note. Accepted-risk disposition stands. |
| T-08.5-16 | D — DoS via memory retention | Tokenise ASCII fast path | mitigate | CLOSED | `tokenise.go:154-158` — explicit godoc memory-retention scope note with `strings.Clone (Go 1.18+)` consumer remediation. |
| T-08.5-17 | T — Tampering via property-test failure | PropTokenise_ASCIIFastPathEquivalent | mitigate | CLOSED | `props_test.go:3957` — `TestProp_Tokenise_ASCIIFastPathEquivalent` is the load-bearing correctness gate asserting ASCII path equivalence with rune path. |
| T-08.5-18 | T — Tampering via flaky staging entries | algorithm-staging entries | mitigate | CLOSED | 23 `TestGolden_*_Staging` tests in `algorithms_golden_test.go` (count verified); 23 staging JSON files in `testdata/golden/_staging/`. Entries are hand-written tuples; no randomness, no time, no map iteration. |
| T-08.5-19 | T — Tampering via reference-library drift | jellyfish + py_stringmatching version pins | mitigate | CLOSED | `scripts/gen-character-cross-validation.py:146,151` (`JELLYFISH_VERSION = "1.2.1"`); `scripts/gen-qgram-cross-validation.py:125,152` (`PYSM_VERSION = "0.4.7"` + `assert sm.__version__ == PYSM_VERSION`); `scripts/gen-monge-elkan-cross-validation.py:127,145` (same). |
| T-08.5-20 | I — Info disclosure (committed corpus content) | vectors.json files | accept | CLOSED | `testdata/cross-validation/{character,qgram,monge-elkan}/vectors.json` — synthetic test inputs; no PII. Accepted-risk disposition stands. |
| T-08.5-21 | S — Supply-chain via reference library | jellyfish + py_stringmatching | accept | CLOSED | `.planning/phases/08.5-review-remediation-gate/08.5-LICENCE-AUDIT.md` — `algorithm-licensing-reviewer` confirmed BSD licences; no runtime dependency. Accepted-risk disposition stands. |
| T-08.5-22 | D — DoS via malformed coverage profile | verify-exported-coverage | mitigate | CLOSED | `scripts/cmd/verify-exported-coverage/main.go:127` — `os.Exit(2)` on parse error; line 56 godoc confirms "rejected with a parse error and exit code 2 rather than panicking." |
| T-08.5-23 | T — Tampering via Floor 3 false-green | AST-vs-coverage cross-reference | mitigate | CLOSED | Floor 3a + 3b checks at `scripts/cmd/verify-exported-coverage/main.go:172,184`. CI re-runs on every PR per Plan 16's verify-llms-sync workflow integration. |
| T-08.5-24 | D — DoS via fuzz-discovered panic | Algorithm surfaces under fuzz | mitigate | CLOSED | 41 `*_fuzz_test.go` files at root; sample at `damerau_full_distance_fuzz_test.go` (and others) confirms panic-free + `math.IsNaN`/`math.IsInf` + `< 0.0 \|\| > 1.0` range checks per fuzz harness. |
| T-08.5-25 | D — DoS via pathologically large input | Validate pathologically-large rule | mitigate | CLOSED | `warn_kind.go:96` (`WarnPathologicallyLargeInput`); `validate.go:170-177` — emits the warning in O(len(a)+len(b)). |
| T-08.5-26 | T — Tampering via output ordering | Validate returned slice | mitigate | CLOSED | `validate.go:252` — `sort.SliceStable(warnings, ...)` with complete `(Algorithm, Kind)` sort key. Regression gate at `validate_test.go:309` (`TestValidate_DeterministicOrdering`). |
| T-08.5-27 | T — Tampering via incorrect simplification | double_metaphone.go:744 | mitigate | CLOSED | `double_metaphone.go:767-775` — Q9 simplification carries explicit godoc citing "behaviour preservation is verified by TestDoubleMetaphone_PaperWorkedExamples (Sais case, Plan 05 Gap 6 gate)". |
| T-08.5-28 | T — Tampering via init-order dependency | dispatch_*.go init() | mitigate | CLOSED | 23 dispatch files each carry `func init() { ... }` writing into a unique slot of the dispatch table; `algoid_test.go:38-117` covers count/order/distinct/dense/string-non-empty invariants. |
| T-08.5-29 (Plan 15a) | I — Info disclosure (WriteGoldenFile test-only) | export_test.go | accept | CLOSED | `golden_canonical.go:95` exposes `writeGoldenFile` (unexported); `export_test.go:34-41` re-exports as `WriteGoldenFile` test-only. External consumers cannot import the test-only symbol. |
| T-08.5-29 (Plan 15b) | T — Tampering via lint residue | golangci-lint Important findings | mitigate | CLOSED | `.planning/phases/08.5-review-remediation-gate/golangci_residue.md` — 31 row entries, each with `//nolint` annotation justification. |
| T-08.5-30 | S — Spoofing via action tag movement | workflow files | mitigate | CLOSED | All 36 third-party action references SHA-pinned (40-char hex commit SHA) across `.github/workflows/{ci,release,codeql,security,license-headers,cla,commitlint,nightly}.yml`. Zero `@v<N>$` tag-only references remain. |
| T-08.5-31 | T — Tampering via release path bypass | release.yml without needs:[ci] | mitigate | CLOSED | `release.yml:84` — `needs: [ci]` on the release job; `release.yml:46-83` — `verify-ci-green` pre-flight job that polls GitHub Checks API to assert the tagged SHA carries a green `make check` run before goreleaser runs. |
| T-08.5-32 | I — Info disclosure via auto-PR | nightly.yml auto-PR | mitigate | CLOSED | `nightly.yml:240` — `peter-evans/create-pull-request@22a9089...` SHA-pinned; PRs require human approval before merge per the documented workflow pattern; fuzz corpus is synthetic. |
| T-08.5-33 (Plan 17a) | I — Info disclosure (stale signatures in per-algorithm godoc) | 22-23 algorithm .go files | mitigate | CLOSED | All 23 primary algorithm files carry `[fuzzymatch.Validate]` godoc cross-reference (`grep -c '\[fuzzymatch.Validate\]'` returns ≥ 1 for each of the 23 files). |
| T-08.5-33 (Plan 17b) | I — Info disclosure (stale signatures) | README + llms.txt + per-algorithm godoc | mitigate | CLOSED | `Makefile:216` defines `verify-llms-sync` target; `readme_shop_front_test.go:15` is the README quick-start meta-test. `README.md` carries 7 `Validate` references; `llms.txt` 4; `llms-full.txt` 8. |

## Unregistered Flags

None.

The 19 SUMMARY.md `## Threat Flags` sections were inspected. Where present (Plans 01, 13, 15a, 16, 17a), each explicitly confirms "no new security-relevant surface introduced beyond what the threat-model entries cover." Plans without a `## Threat Flags` section (02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12, 14, 15b, 17b) introduce no new attack surface per their `<threat_model>` blocks and per the 14-agent panel sign-off.

## Disposition Summary

| Disposition | Count | Status |
|-------------|-------|--------|
| `mitigate` | 26 | All CLOSED (grep matches at cited file:line in each case) |
| `accept` | 7 | All CLOSED (accepted-risk disposition documented in CHANGELOG, godoc, or LICENCE-AUDIT.md) |
| `transfer` | 0 | n/a |
| **Total** | **33** | **0 OPEN** |

## ASVS V5/V10/V14 Surface Notes (library scope)

- **V5 (Input Validation):** Validate-then-Score idiom + typed parameter guards (T-08.5-07/08, T-08.5-25/26). All NaN/Inf/sign rejection at option-construction + direct-call paths.
- **V10 (Malicious Code):** Fresh-implementation discipline + 5-platform CI matrix + golden-file determinism gate (T-08.5-13, T-08.5-23). No runtime dependencies beyond `golang.org/x/text`. SHA-pinned actions (T-08.5-30).
- **V14 (Configuration):** No runtime configuration; library is pure-function. Release path: tag-trigger only + `needs:[ci]` + `verify-ci-green` pre-flight (T-08.5-31). Cosign keyless signing + SBOM + OIDC attestation operational.

ASVS V1/V2/V3/V4/V6/V7/V8/V9/V11/V12/V13 are out-of-scope for a pure-Go library with no auth, no network, no I/O, no session, and no persistence.

## Conclusion

**SECURED.** All 33 documented threats are CLOSED with grep-located evidence in the post-Phase-8.5 codebase. No unregistered attack surface flagged. Phase 8.5 is cleared for security sign-off; Phase 9 is unblocked from the security-gate perspective.
