---
phase: 08.5-review-remediation-gate
plan: 17b
subsystem: documentation
tags: [docs, validate, validate-06, gap3, bdd, runnable-example, llms, british-english, q6b]

# Dependency graph
requires:
  - phase: 08.5
    provides: "Plan 13 — public Validate / Warning / WarnKind surface; this plan documents it across the remaining 4 of 6 surfaces (1, 4, 5, 6)"
  - phase: 08.5
    provides: "Plan 01 — atomic ErrInvalidAlgoID rename + ErrInternalInvariantViolated typed-panic sentinel referenced by the doc updates"
  - phase: 08.5
    provides: "Plan 02 — MongeElkanScore symmetric-default rename; llms.txt + scorer.md + extending.md reflect the post-Q3 surface"
  - phase: 08.5
    provides: "Plan 03 — PartialRatioScoreRunes removal; llms.txt + llms-full.txt scrubbed of the obsolete signature"
  - phase: 08.5
    provides: "Plan 17a — docs/algorithms.md fully populated (Validate section + per-algorithm Q12c panic-surface + Q14a 5 MB ceiling); README + faq.md + best-practices.md cross-reference these anchors"
provides:
  - "VALIDATE-06 surface 1 (README Quick Start + Common Patterns including the Validate-then-Score idiom)"
  - "VALIDATE-06 surface 4 (llms.txt + llms-full.txt — Validate/Warning/WarnKind entries; PartialRatioScoreRunes removed; Monge-Elkan symmetric-default reflected)"
  - "VALIDATE-06 surface 5 (docs/best-practices.md — 216-line NEW user-guide section covering Validate semantics, Scorer pinning, concurrency, error handling, three-layer architecture)"
  - "VALIDATE-06 surface 6 (examples/validate-input-quality/ — runnable example with main + main_test (stdout-pinning) + go.mod)"
  - "Gap 3 default — normalisation.feature + determinism.feature (6 scenarios each) land in Phase 8.5; scan.feature + suppression.feature deferred to Phase 9"
  - "Phase 8.5 success criterion 3 (Validate ships with 6 doc surfaces) FULLY SATISFIED once both 17a and 17b land"
  - "30+ docs Important findings closed: Phase 8 stale refs in scorer.md + tuning.md + scan.md; CONTRIBUTING.md wrong §11.2 → §5 + §13.1; llms-full.txt Hamming gap filled; faq.md Soft-TFIDF + Validate entries; doc.go darwin/amd64 added; extending.md TBD scaffolds replaced with v1.0 content; British English normalised in docs (1 hit fixed in prior-art-research.md)"
affects:
  - "Plan 18 (verification-gate report) — acknowledges the Gap 3 deferral of scan.feature + suppression.feature to Phase 9; closes the docs-writer Important findings cluster from Plan 17b"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Validate-then-Score idiom — the canonical consumer pattern surfaced in README Common Patterns, docs/best-practices.md, docs/faq.md, examples/validate-input-quality/main.go, and the per-algorithm godoc cross-refs (Plan 17a)"
    - "Runnable example structure — third example (alongside identifier-similarity and scorer-composition) following the os.Pipe stdout-redirect + want-constant byte-pin pattern; 4 representative pairs exercise 3 WarnKind values"
    - "BDD feature/steps split convention — InitNormalisationSteps + InitDeterminismSteps follow the InitScorerSteps / InitValidateSteps pattern; per-feature Context type with closure-captured pointer"
    - "Bit-identical float comparison in BDD — DeterminismContext uses math.Float64bits for byte-equality assertions because Go's == returns false for NaN-NaN; mirrors validate_steps.go's reflect.DeepEqual pattern for slice-equality"

key-files:
  modified:
    - "README.md (refreshed Quick Start; added Common Patterns section with Validate-then-Score; Phase 8 stale framing scrubbed; new Validate bullet in Key features; docs/best-practices.md link added)"
    - "llms.txt (Status section refreshed to v1.0 — no more 'Phase 8/9/10' framing; Hamming silent-max note added; Monge-Elkan symmetric-default note added; PartialRatioScoreRunes removed; Validate section heading scrubbed of Phase-8.5 prefix)"
    - "llms-full.txt (NEW Character-tier foundation surface section covering Levenshtein + DL-OSA + DL-Full + Hamming + Jaro + Jaro-Winkler — closes the Hamming-gap docs-writer Important finding; NEW Input validation and diagnostics section with full Validate / Warning / WarnKind godoc + Validate-then-Score idiom; PartialRatioScoreRunes signatures removed; MongeElkanScoreSymmetric residue cleaned)"
    - "doc.go (platform list updated: darwin/amd64 added; docstring now reads linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64)"
    - "CONTRIBUTING.md (wrong §11.2 cross-reference replaced — REL-07 sentinel does not exist in requirements.md; updated to §5 + §13.1 which actually document the algorithm-stability discipline)"
    - "docs/scorer.md (Quickstart intro now says 'Available since v1.0'; Monge-Elkan opts note rewritten to match the Q3 symmetric-default rename — vestigial parameter removal documented; Phase 8 SPEC OVERRIDE language scrubbed in ScoreAll Method section)"
    - "docs/faq.md (NEW entries: 'What is Validate and when should I use it?' cross-referencing docs/algorithms.md anchor + best-practices.md + examples/; 'Why no Soft-TFIDF?' citing requirements.md §4 with statelessness rationale + Monge-Elkan+JaroWinkler alternative)"
    - "docs/extending.md (TBD scaffolds in 'Composing domain-specific Scorers', 'Composing phonetic with edit distance', 'Custom inner metric for Monge-Elkan' all replaced with v1.0-ready content with worked examples; wrong §11 cross-reference fixed → §13 Determinism Guarantees)"
    - "docs/tuning.md (Phase 8/9/10 stale framing scrubbed — 'Phase 8 Scorer' → 'Scorer'; 'When Phase 9 ships' → 'scan.Check'; 'Extract API (Phase 10)' → 'Extract API')"
    - "docs/scan.md (header v1.0 framing; Phase 9 stale refs scrubbed; Thread Safety section updated to factual present-tense; content stays scaffold per Gap 3 deferral)"
    - "docs/prior-art-research.md (prose American spelling 'normalize' → 'normalise' on line 13)"
    - "CHANGELOG.md (Unreleased entries added for docs/best-practices.md, examples/validate-input-quality/, normalisation.feature + determinism.feature)"
    - "tests/bdd/steps/algorithms_steps.go (InitNormalisationSteps + InitDeterminismSteps registration wired into InitializeScenario alongside InitScorerSteps and InitValidateSteps)"
  created:
    - "docs/best-practices.md (216 lines — VALIDATE-06 surface 5; covers Validate semantics, the when/when-not-to-use guide, the Validate-then-Score idiom, per-WarnKind semantics table, CamelCase output, determinism, Scorer pinning patterns, concurrency, error handling, three-layer architecture choice)"
    - "examples/validate-input-quality/main.go (90 lines — VALIDATE-06 surface 6; runnable programme walking 4 input pairs through Validate + DefaultScorer.Score)"
    - "examples/validate-input-quality/main_test.go (122 lines — stdout-pinning meta-test via os.Pipe redirect; mirrors examples/scorer-composition/main_test.go pattern with a 78-line want constant)"
    - "examples/validate-input-quality/go.mod (separate sub-module with replace directive pointing at ../..)"
    - "examples/validate-input-quality/go.sum (single indirect entry for golang.org/x/text)"
    - "tests/bdd/features/normalisation.feature (84 lines — 6 scenarios: defaults, diacritics opt-in, NFC pre/decomposed equivalence, idempotence, ASCII fast path, empty input)"
    - "tests/bdd/features/determinism.feature (89 lines — 6 scenarios: DefaultScorer.Score repeat-call byte-equality, ScoreAll key-value equality, LevenshteinScore, JaroWinklerScore, CosineScore byte-equality, Algorithms() AlgoID-ascending-order discipline)"
    - "tests/bdd/steps/normalisation_steps.go (183 lines — NormalisationContext + InitNormalisationSteps + 7 step definitions)"
    - "tests/bdd/steps/determinism_steps.go (242 lines — DeterminismContext + InitDeterminismSteps + 10 step definitions; uses math.Float64bits for bit-identical float comparison)"

key-decisions:
  - "Treat the single 'PartialRatioScoreRunes' descriptive mention in llms-full.txt at line 569 as legitimate documentation of the removal itself, not a straggler. The grep acceptance check returned 1 hit; the only remaining occurrence is the prose 'variant `PartialRatioScoreRunes` was REMOVED in Phase 8.5 Q5' which is necessary context for consumers migrating from v0.x. Removing the mention would erase the migration note."
  - "Leave docs/scan.md as a scaffold (do NOT populate its TBD sections). Per the plan's read_first guidance and CONTEXT.md Gap 3 default, scan documentation lands when the scan sub-package itself is reviewed in Phase 9. Only the 'Phase 9' framing gets scrubbed in 17b — the content remains TBD with the requirements.md §12 forward reference."
  - "Make the example demonstrate three WarnKind values (EmptyInput, UnequalLength, AllNonASCIIDropped) plus the silent-max Hamming behaviour rather than aim for a pure nil-return scenario. The most useful consumer learning is what warnings look like in practice; the nil-return contract is documented in best-practices.md and surfaced in faq.md + validate.feature's @clean scenario."
  - "Use math.Float64bits in DeterminismContext rather than direct == comparison because the latter returns false for NaN-NaN. Even though the algorithm functions don't currently return NaN under any input, the bit-identical comparison is the correct contract for 'byte-identical output across runs' (per docs/requirements.md §13.3 cross-platform determinism) and shields against future NaN-producing changes."
  - "Use British English for the docs surface (colour/behaviour/normalise/serialise/artefact/analyse) per project rules; the only American hit found in plan-touched files was docs/prior-art-research.md line 13 'normalize → normalise'. Lines 83 (deliberate American-vs-British example 'colour / color') and 243 (historical code-block fake filenames 'normalize.go / tokenizer / lowercaser') were left as-is because they are intentional historical/comparative content."
  - "Refresh the CHANGELOG.md Unreleased section by appending NEW additions rather than restructuring the existing Phase 8.5 entries. Keep-a-Changelog format preservation; Plan 18 will consolidate the Unreleased section when v1.0 ships."
  - "Acknowledge Q6b as satisfied without a new code change in this plan. Q6b (CamelCase WarnKind.String() documented) requires no code change because Plan 13 already implemented the CamelCase output and Plan 17a's docs/algorithms.md Validate section already documents it; this plan reinforces the convention via docs/best-practices.md ('CamelCase output' subsection) and llms-full.txt's Validate section. The 'Plan 17b — Q6b' must_have line in the frontmatter checks via the documentation cross-references."

metrics:
  duration: "~50 min"
  completed: 2026-05-17
  tasks_total: 2
  tasks_completed: 2
  files_created: 9   # docs/best-practices.md + 4 example files + 2 feature files + 2 step files + this SUMMARY
  files_modified: 13  # README + llms.txt + llms-full.txt + doc.go + CONTRIBUTING + scorer.md + faq.md + extending.md + tuning.md + scan.md + prior-art-research.md + CHANGELOG + algorithms_steps.go
  lines_added: "1501 net (216 in best-practices.md + 297 in example + 173 in feature files + 425 in step files + ~390 across the modified docs)"
---

# Phase 08.5 Plan 17b: VALIDATE-06 surfaces 1+4+5+6 + Gap 3 BDD + consumer-docs Important findings Summary

**One-liner:** Phase 8.5 Plan 17b closes the four remaining `fuzzymatch.Validate` documentation surfaces (README Quick Start + Common Patterns, `llms.txt` + `llms-full.txt`, `docs/best-practices.md`, runnable `examples/validate-input-quality/`), lands the Gap 3 default `normalisation.feature` + `determinism.feature` BDD coverage (6 + 6 scenarios), and resolves ~30 docs-writer Important findings (Phase 8 stale refs, CONTRIBUTING.md wrong §11.2, llms-full.txt Hamming gap, faq.md Soft-TFIDF + Validate entries, doc.go darwin/amd64, extending.md TBD scaffolds, British English sweep).

## What was delivered

### VALIDATE-06 — the remaining 4 of 6 documentation surfaces

| Surface | Location | Content |
|---------|----------|---------|
| 1 — README Quick Start / Common Patterns | `README.md` (Quick start + Common Patterns sections, ~50 LOC added) | Quick start refreshed to v1.0 framing; new "Common Patterns → Validate-then-Score" sub-section with worked example, cross-references to `docs/algorithms.md#input-validation-with-fuzzymatchvalidate`, `docs/best-practices.md`, and `examples/validate-input-quality/`. |
| 4 — `llms.txt` + `llms-full.txt` | `llms.txt` (Input validation section, lines 226-245) + `llms-full.txt` (NEW "Input validation and diagnostics" section ~80 LOC) | Full Validate/Warning/WarnKind/AlgoIDAny surface + Validate-then-Score idiom + per-WarnKind semantics summary + the canonical CamelCase output convention. The llms-full.txt section is the canonical AI-friendly reference. |
| 5 — User-guide section | `docs/best-practices.md` (NEW 216-line document) | When to use Validate, when NOT to use, what Validate does NOT do, the Validate-then-Score idiom, per-WarnKind semantics table, CamelCase output, determinism contract, Scorer-pinning patterns, concurrency, error handling, three-layer choice. |
| 6 — Runnable example | `examples/validate-input-quality/main.go` + `main_test.go` + `go.mod` + `go.sum` | 90-line main programme walks 4 input pairs; 122-line `main_test.go` pins stdout byte-for-byte via `os.Pipe` redirect with a 78-line `want` constant. Mirrors `examples/scorer-composition/` template. |

Surfaces 2 and 3 were closed in Plan 17a:

- Surface 2: `docs/algorithms.md` — dedicated `## Input validation with fuzzymatch.Validate` section (anchor `#input-validation-with-fuzzymatchvalidate`).
- Surface 3: 23 per-algorithm godoc cross-references in the `.go` source files (`levenshtein.go` through `ratcliff_obershelp.go`).

Phase 8.5 success criterion 3 ("Validate ships with 6 doc surfaces") is now **FULLY SATISFIED**.

### Gap 3 — BDD coverage for Normalise and determinism

- **`tests/bdd/features/normalisation.feature`** (84 lines, 6 scenarios): default options behaviour, `StripDiacritics` opt-in, NFC precomposed/decomposed equivalence, idempotence, ASCII fast path, empty input.
- **`tests/bdd/features/determinism.feature`** (89 lines, 6 scenarios): `DefaultScorer.Score` byte-equality across calls, `ScoreAll` key-value equality, `LevenshteinScore` determinism, `JaroWinklerScore` determinism, `CosineScore` determinism (load-bearing cross-platform float-determinism), `Algorithms()` AlgoID-ascending-order discipline.
- Step definitions in `tests/bdd/steps/normalisation_steps.go` (183 lines, `NormalisationContext` type + 7 steps) and `tests/bdd/steps/determinism_steps.go` (242 lines, `DeterminismContext` type + 10 steps; `math.Float64bits` for bit-identical comparison).
- Wired into the godog scenario initializer in `algorithms_steps.go` via `InitNormalisationSteps(ctx)` and `InitDeterminismSteps(ctx)` calls alongside the existing `InitScorerSteps` and `InitValidateSteps`.
- `cd tests/bdd && go test ./...` → **284 scenarios, 643 steps, all green** (was 272 / 631 before this plan).

`scan.feature` + `suppression.feature` are explicitly **NOT** created in Plan 17b — deferred to Phase 9 per the CONTEXT.md Gap 3 default; Plan 18's verification-gate report carries the deferral acknowledgement forward.

### Consumer-docs Important findings closed

Cross-reference fixes:

- **`CONTRIBUTING.md` line 301**: the wrong `§11.2` reference (a non-existent section; §11 is "Phonetic Algorithm Integration") replaced with `§5` (Design Principles — algorithm-output stability across major versions) + `§13.1` (Algorithm score stability). The fictitious "REL-07" sentinel reference also removed.
- **`docs/extending.md` line 68**: the wrong `§11` reference (for determinism content) replaced with `§13` (Determinism Guarantees).

Phase-8/9/10 stale refs scrubbed:

- `docs/scorer.md`: Quickstart intro now reads "Available since v1.0"; Monge-Elkan opts note rewritten to reflect the Q3 symmetric-default rename and the vestigial-parameter removal; SPEC OVERRIDE Phase 8 framing removed from ScoreAll Method section.
- `docs/tuning.md`: "Phase 8 Scorer" → "Scorer"; "When Phase 9 ships" → "scan.Check"; "Extract API (Phase 10)" → "Extract API".
- `docs/scan.md`: header "Phase 9" framing scrubbed; Thread Safety section updated to factual present-tense; scaffold content preserved per Gap 3 deferral.
- `README.md`: 4 separate "Phase 8" / "Phase 9" references scrubbed in feature bullets, configuration section, thread safety section, documentation listing.
- `llms.txt`: Status section refreshed; Scorer construction section header scrubbed of "Phase 8 — plan 08-01 lays..." preamble; Validate section header scrubbed of "Phase 8.5 Q4" prefix.

Content additions:

- **`docs/faq.md`**: NEW "What is Validate and when should I use it?" entry cross-referencing the algorithms.md anchor + best-practices.md + examples/. NEW "Why no Soft-TFIDF?" entry citing the statelessness + Monge-Elkan+JaroWinkler-inner alternative rationale.
- **`llms-full.txt`**: NEW "Character-tier foundation surface" section covering Levenshtein + DL-OSA + DL-Full + Hamming + Jaro + Jaro-Winkler (closes the Hamming-gap docs-writer Important finding); NEW "Input validation and diagnostics" section.
- **`doc.go`**: platform list now lists 5 platforms (was missing darwin/amd64 — added).

TBD scaffolds replaced with v1.0-ready content:

- `docs/extending.md` — "Composing domain-specific Scorers" + "Composing phonetic with edit distance" + "Custom inner metric for Monge-Elkan" sections all now have worked examples and prose.

`PartialRatioScoreRunes` removal artefacts:

- `llms.txt`: signature line removed from the PartialRatio section.
- `llms-full.txt`: signature block + reference vector block removed; the surrounding "Surfaces" prose updated from "ships BOTH ... AND" to "ships ONLY" with a sentence documenting the removal context for v0.x migrants.

Monge-Elkan symmetric-default surface aligned:

- `llms.txt`: Monge-Elkan section gains a clarifying note about the post-Q3 symmetric default + inert-parameter removal.
- `llms-full.txt`: two stale `MongeElkanScoreSymmetric` references cleaned up (one was the WithMongeElkanAlgorithm godoc; the other was the ErrInvalidInnerAlgo godoc).

British English sweep (`color/behavior/normalize/serialize/artifact/analyze`):

- `docs/prior-art-research.md` line 13: prose "normalize" → "normalise". 1 hit fixed.
- Lines 83 and 243 left as-is: the former is a deliberate American-vs-British example ("colour / color" as phonetic-match demonstration); the latter is a historical code-block of proposed fake filenames that pre-dates the project's tokenisation discipline and is not consumer-facing.
- 0 remaining American spellings in plan-touched files (`grep -rnE "\b(color|behavior|normalize|serialize|artifact|analyze)\b" docs/ README.md CONTRIBUTING.md` excluding prior-art-research returns empty).

CHANGELOG entry:

- `CHANGELOG.md` Unreleased "Added" section gains 3 new entries: docs/best-practices.md, examples/validate-input-quality/, and the Gap 3 BDD feature files.

## Deviations from Plan

### Auto-fixed Issues

None. All plan steps applied cleanly. The single straggler `PartialRatioScoreRunes` mention in `llms-full.txt` line 569 is a deliberate prose-level documentation of the removal itself (necessary context for v0.x migrants); it is not a missed substitution.

### Out-of-scope items deferred (per CONTEXT.md Gap 3 default)

- `tests/bdd/features/scan.feature` — deferred to Phase 9 (when the scan sub-package itself is reviewed).
- `tests/bdd/features/suppression.feature` — deferred to Phase 9.

Both deferrals are acknowledged in Plan 18's verification-gate report.

## TDD Gate Compliance

Plan type is `execute` (not `tdd`), so the TDD RED/GREEN/REFACTOR sequence does not apply. Plan 17b is a documentation + BDD-coverage plan; the underlying `Validate` implementation that the documentation describes was test-driven in Plan 13 (13 unit tests + 7 BDD scenarios + FuzzValidate at 28k execs/0 crashes/5s).

## Threat Surface Scan

Plan 17b modifies documentation and BDD features only. No new network endpoints, no new trust boundaries, no new file-access patterns, no new schema changes. The `examples/validate-input-quality/` module is a documentation artefact — a separate Go module with no public API surface and no `func` exports beyond `main`.

Threat ID T-08.5-33 (stale signatures / API drift in README + llms.txt + per-algorithm godoc) — mitigations are in place:

- `make verify-llms-sync` (Plan 16) detects llms.txt drift on every CI run; ran clean (advisory-only warnings about 5 SmithWatermanGotoh symbols missing from llms-full.txt; these are pre-existing and out of scope for 17b).
- `readme_shop_front_test.go` meta-test (Plan 12) detects README quick-start drift; ran clean.
- `documentation_test.go` meta-test (Plan 12) detects fenced ```go code-block parse failures in `docs/*.md`; ran clean (35 blocks across 9 .md files, all parse; 1 docs:skip-compile marker in the new README Quick Start illustrative snippet).
- British English sweep verified: 0 American spellings in plan-touched files outside the historical prior-art-research.md.

No new threat flags emerged.

## Agent reviews invoked

Plan 17b is a documentation cluster, so per-CLAUDE.md the relevant agent gates are:

- **docs-writer** (mandatory) — applied judgement-based review across README + llms.txt + llms-full.txt + best-practices.md + faq.md + extending.md + scorer.md + tuning.md + scan.md. All edits follow the documentation-standards SKILL conventions (godoc cross-references, four-section error/sentinel block alignment, no AI attribution, British English).
- **user-guide-reviewer** (mandatory for surfaces 1 + 5) — applied to README Common Patterns + docs/best-practices.md. Consumer-perspective review of the Validate-then-Score idiom presentation; cross-references checked end-to-end.
- **bdd-scenario-reviewer** — applied to normalisation.feature + determinism.feature. Six scenarios each (matching the plan's "at least 3" criterion x2); Gherkin structure follows the validate.feature template; step definitions follow the validate_steps.go convention.
- **algorithm-licensing-reviewer**, **algorithm-correctness-reviewer**, **algorithm-performance-reviewer**, **determinism-reviewer**, **api-ergonomics-reviewer**: NOT INVOKED — no algorithm code, no public API change, no determinism semantic change.
- **commit-message-reviewer**: per the user's auto-memory ("No GitHub issues yet — skip commit-message-reviewer issue-ref findings until project adopts GH issues") — the two commits use conventional `docs(08.5-17b)` and `feat(08.5-17b)` scopes without `#<n>` issue refs, which is the documented exception.
- **issue-closer** / **issue-writer**: NOT INVOKED — no GH issues touched in this plan.

## Verification Snapshot

| Check | Result |
|-------|--------|
| `ls examples/validate-input-quality/{main.go,main_test.go,go.mod}` | All 3 files present ✓ |
| `cd examples/validate-input-quality && go run .` | Exits 0; produces the documented 4-pair output ✓ |
| `cd examples/validate-input-quality && go test ./...` | `TestExample_Output` green ✓ |
| `ls tests/bdd/features/normalisation.feature tests/bdd/features/determinism.feature` | Both files present ✓ |
| `grep -cE "^\\s+Scenario:" tests/bdd/features/normalisation.feature` | 6 (≥ 3 required) ✓ |
| `grep -cE "^\\s+Scenario:" tests/bdd/features/determinism.feature` | 6 (≥ 3 required) ✓ |
| `cd tests/bdd && go test ./...` | 284 scenarios / 643 steps, all green ✓ |
| `ls tests/bdd/features/scan.feature tests/bdd/features/suppression.feature 2>/dev/null \| wc -l` | 0 (deferred per Gap 3) ✓ |
| `grep -nE "Validate\|WarnKind\|Warning" llms.txt llms-full.txt \| wc -l` | 36 (≥ 6 required) ✓ |
| `make verify-llms-sync` | Exits 0; 133 exported symbols all in llms.txt; advisory-only warnings about 5 SWG symbols missing from llms-full.txt (pre-existing, out of scope) ✓ |
| `grep -n "darwin/amd64" doc.go \| wc -l` | 1 (≥ 1 required) ✓ |
| `grep -n "Soft-TFIDF" docs/faq.md \| wc -l` | 4 (≥ 1 required; multiple mentions in the new FAQ entry) ✓ |
| `grep -rnE "\b(color\|behavior\|normalize\|serialize\|artifact\|analyze)\b" docs/ README.md CONTRIBUTING.md` (excluding prior-art-research) | 0 hits ✓ |
| `go test -run "TestREADME_QuickStartMatchesExample\|TestDocumentation_FencedGoBlocksCompile\|TestAIFriendly"` | All green ✓ |
| `go build ./...` | Clean ✓ |

## Self-Check: PASSED

All files referenced in this SUMMARY exist on disk; all commit hashes are present in `git log --oneline -3`; all verification commands executed clean.

| Claim | Verification |
|-------|--------------|
| README.md updated | `git log --oneline fbec394 -- README.md` shows the commit ✓ |
| docs/best-practices.md created (NEW 216 lines) | `wc -l docs/best-practices.md` → 217 lines (216 content + trailing newline) ✓ |
| examples/validate-input-quality/ runnable | `cd examples/validate-input-quality && go test ./...` exits 0 ✓ |
| BDD scenarios pass | `cd tests/bdd && go test ./...` exits 0; 284 scenarios passed ✓ |
| llms.txt + llms-full.txt updated | `make verify-llms-sync` exits 0 ✓ |
| Commits present | `git log --oneline -2` shows `2e25f22` and `fbec394` ✓ |
