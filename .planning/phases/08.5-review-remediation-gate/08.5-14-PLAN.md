---
phase: 08.5
plan: 14
type: execute
wave: 5
depends_on: [05]
files_modified:
  - double_metaphone.go
autonomous: true
requirements:
  - PHON-02
  - Q9
  - Gap6
threat_model_refs: [T-08.5-27]

must_haves:
  truths:
    - "double_metaphone.go:744 dupBranchBody is removed — both branches collapse to a single dmAdd call"
    - "An inline comment explains the deliberate behaviour-preserving simplification per Q9 lock"
    - "Plan 05 (Q11c paper-anchored test) was green BEFORE this plan started — Gap 6 dependency satisfied"
    - "If Q11c revealed Philips 2000 divergence, this plan is BLOCKED and a Gap 6 mini-discuss-phase ran first"
  artifacts:
    - path: "double_metaphone.go"
      provides: "Simplified single-branch dmAdd call replacing the dupBranchBody"
      contains: "// Q9 simplification"
  key_links:
    - from: "Plan 05 TestDoubleMetaphone_PaperWorkedExamples green"
      to: "Plan 14 (this plan) start"
      via: "Gap 6 hard predecessor"
      pattern: "TestDoubleMetaphone_PaperWorkedExamples"
---

<objective>
Q9 — remove the `double_metaphone.go:744` dupBranchBody redundancy with an inline comment explaining the deliberate behaviour-preserving simplification.

**HARD DEPENDENCY:** Plan 05 (TestDoubleMetaphone_PaperWorkedExamples) MUST be green before this plan starts. Per Gap 6 lock in CONTEXT.md:
- If Q11c passed all 10 worked examples (including the SAIS-end case), this plan proceeds as the locked redundancy removal.
- If Q11c revealed divergence on ANY example, this plan is BLOCKED until a Gap 6 mini-discuss-phase has produced a written addendum to CONTEXT.md.

This is a minimal-surface plan — single-file edit with no test changes (the test infrastructure is Plan 05's Q11c test + the existing Phase 7 tests). The change is documented as internal cleanup with no behavioural shift (per the locked Q9 reading).

Output: single atomic commit removing the redundant if/else branches and replacing with a single `dmAdd(&p, &alt, "S", "")` call plus an inline comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@.planning/phases/08.5-review-remediation-gate/08.5-05-PLAN.md
@docs/requirements.md
@.claude/skills/algorithm-correctness-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
<!-- Q9 LOCKED in 08.5-CONTEXT.md — Gap 6 lock encoded as dependency on Plan 05 -->

Current code at double_metaphone.go:744 (per CONTEXT §Q9 and 08.5-PATTERNS.md §2.N):

```go
// (illustrative — exact form per file inspection)
if <condition-A> {
    dmAdd(&p, &alt, "S", "")
} else {
    dmAdd(&p, &alt, "S", "")
}
```

Both branches identical. Q9 locks: remove the redundancy. Replace with a single call. Add an inline comment.

Gap 6 escape hatch (already operational from Plan 05): if Q11c divergence appears on Plan 05, the executor wrote `.planning/phases/08.5-review-remediation-gate/08.5-GAP-6-FOLLOWUP-REQUIRED.md` and halted. Before this plan runs, verify that file does NOT exist (or has been resolved into a CONTEXT.md addendum).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Gap 6 gate is open, then remove dupBranchBody at double_metaphone.go:744</name>
  <files>double_metaphone.go</files>
  <read_first>
    - .planning/phases/08.5-review-remediation-gate/08.5-GAP-6-FOLLOWUP-REQUIRED.md (verify NOT present, OR if present, the resolution is recorded in 08.5-CONTEXT.md as a Gap 6 follow-up addendum)
    - .planning/phases/08.5-review-remediation-gate/08.5-05-SUMMARY.md (Plan 05's report — must confirm all 10 paper-anchored cases green)
    - double_metaphone.go lines 740-752 (the dupBranchBody site)
    - double_metaphone_paper_test.go (must exist and be green per Plan 05)
    - .planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md §Q9 + §Gap 6
    - docs/requirements.md §7.21 (Double Metaphone surface — Q9 internal-cleanup nature)
  </read_first>
  <action>
1. **Gap 6 gate check** (executor's first action):
   - Verify `.planning/phases/08.5-review-remediation-gate/08.5-GAP-6-FOLLOWUP-REQUIRED.md` does NOT exist.
   - Verify `.planning/phases/08.5-review-remediation-gate/08.5-05-SUMMARY.md` exists and reports "all 10 paper-anchored cases green" or equivalent.
   - Verify `TestDoubleMetaphone_PaperWorkedExamples` is green by running `go test -run TestDoubleMetaphone_PaperWorkedExamples -race -count=1 ./...` and confirming exit code 0.
   - If any of these checks FAILS, HALT this plan and surface a clear error message: "Gap 6 mini-discuss-phase resolution required before Q9 can land. See 08.5-GAP-6-FOLLOWUP-REQUIRED.md."

2. **Apply the Q9 simplification at double_metaphone.go:744**:
   - Locate the if/else block where both branches call `dmAdd(&p, &alt, "S", "")` (or the exact form in the current file).
   - Replace with a single call:
     ```go
     // Q9 simplification: both branches of the original if/else
     // (legacy from the Philips C++ port) collapsed to the same dmAdd call.
     // The condition discriminated French-end-of-name SAIS-rule cases, but
     // the locked spec records that both paths produce ("S", "") here per
     // docs/requirements.md §7.21. Verified by TestDoubleMetaphone_PaperWorkedExamples.
     dmAdd(&p, &alt, "S", "")
     ```
   - Adjust the surrounding control flow as needed (the condition variable that drove the if/else may still be needed for other rules; verify the broader function context).
   - Do NOT remove the condition test if it gates anything else downstream (read the function fully to verify).
   - British English in the comment.

3. **Verify no regression:**
   - Run `go test -run TestDoubleMetaphone -race -count=1 ./...` and confirm all Double Metaphone tests (including the Phase 7 reference vectors and Plan 05's paper-anchored test) remain green.
   - Run `go test -bench=BenchmarkDoubleMetaphone -benchmem -count=3 -run=^$ ./...` and confirm no significant perf regression.

Commit message: `refactor(double-metaphone): remove dupBranchBody at SAIS-end rule (Q9, Gap 6 verified)`.

Per D-XX traceability: this plan implements PHON-02 (post-Phase 7 surface cleanup), Q9 (locked behaviour-preserving simplification), Gap 6 (plan-DAG dependency satisfied by Plan 05).
  </action>
  <verify>
    <automated>go test -run TestDoubleMetaphone -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/08.5-review-remediation-gate/08.5-GAP-6-FOLLOWUP-REQUIRED.md` does NOT exist OR has been resolved into CONTEXT.md addendum.
    - `grep -nE "// Q9 simplification" double_metaphone.go | wc -l` returns at least 1 (the inline comment present).
    - The dupBranchBody no longer exists: `grep -nE "dmAdd\\(\\&p, \\&alt, \"S\", \"\"\\)" double_metaphone.go | wc -l` returns 1 (down from 2 — the if and else both had this call; now collapsed to 1).
    - `go test -run TestDoubleMetaphone -race -count=1 ./...` exits 0.
    - `go test -run TestDoubleMetaphone_PaperWorkedExamples -race -count=1 ./...` exits 0 (Plan 05's gate still green).
    - `golangci-lint run double_metaphone.go` exits 0 OR confirms the gocritic dupBranchBody warning is gone.
    - `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>
    The dupBranchBody is removed; the inline comment documents the deliberate simplification per Q9 lock; Plan 05's paper-anchored test and all Phase 7 tests remain green; the gocritic warning is cleared.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 05 outcome → Plan 14 start | Gap 6 dependency — Plan 14 must NOT proceed if Plan 05 surfaced Philips 2000 divergence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-27 | T — Tampering via incorrect simplification | double_metaphone.go:744 | mitigate | Gap 6 dependency on Plan 05 paper-anchored test. If Plan 05 reveals divergence, mini-discuss-phase resolves before Plan 14 starts. The post-edit run of `TestDoubleMetaphone_PaperWorkedExamples` confirms no regression. |
</threat_model>

<verification>
- Pre-execution check: GAP-6-FOLLOWUP-REQUIRED.md not present.
- Test-green: TestDoubleMetaphone* all pass.
- Lint: gocritic dupBranchBody warning resolved.
</verification>

<success_criteria>
- Q9 lock satisfied: dupBranchBody removed with documented rationale.
- Gap 6 dependency honoured: Plan 05 was the predecessor; gate check ran.
- The go-quality Critical (gocritic dupBranchBody) finding closes.
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-14-SUMMARY.md` documenting:
- the Gap 6 gate-check outcome (Plan 05 SUMMARY confirmed all 10 cases green);
- the before/after diff at double_metaphone.go:744;
- the test re-run confirmation;
- agent reviews invoked (algorithm-correctness-reviewer mandatory; code-reviewer; go-quality).
</output>
