---
phase: 08.5
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - double_metaphone.go
  - double_metaphone_bench_test.go
  - q_gram.go
  - q_gram_test.go
  - damerau_full_test.go
  - damerau_full_bench_test.go
  - ratcliff_obershelp_bench_test.go
  - scorer_bench_test.go
  - soundex.go
  - normalise.go
autonomous: true
requirements:
  - PHON-02
  - QGRAM-01
  - CHAR-06
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - Q7a
  - Q7b
  - Q7c
  - Q7d
  - Q8a
  - Q8c
  - Q8d
  - Q11e
threat_model_refs: [T-08.5-14]

must_haves:
  truths:
    - "DoubleMetaphoneKeys allocates ≤ 2 allocs per call (down from ~6) post-Q7a [dmMaxLen]byte refactor"
    - "extractQGrams pre-sizes its result map with 25% capacity headroom"
    - "DamerauLevenshteinFullScore_ZeroAllocs_ASCII_Short test is un-skipped under the ≤ 1 alloc Q8a budget"
    - "Per-algorithm long-input heap-fallback scope notes added where the optimisation is structurally absent (Q7c)"
    - "Per-algorithm benchmark assertions match the revised budgets in docs/requirements.md §14.1 / §14.2 / §14.3"
  artifacts:
    - path: "double_metaphone.go"
      provides: "Stack-allocated [dmMaxLen]byte accumulators replacing strings.Builder for primary + secondary key construction"
      contains: "[dmMaxLen]byte"
    - path: "q_gram.go"
      provides: "extractQGrams with 25% capacity hint"
      contains: "len(s)-n+1"
  key_links:
    - from: "BenchmarkDoubleMetaphoneScore_ASCII_Short"
      to: "≤ 2 allocs/op assertion"
      via: "benchmark assertion"
      pattern: "2 allocs/op"
    - from: "BenchmarkDamerauLevenshteinFullScore_ZeroAllocs_ASCII_Short"
      to: "active test (not skipped)"
      via: "benchmark assertion"
      pattern: "1 allocs/op"
---

<objective>
Cluster 5 — performance optimisations bundle. Lands Q7a (DoubleMetaphone `[dmMaxLen]byte` accumulator), Q7d (Q-gram capacity hint), Q11e (DL-Full ZeroAllocs un-skip), Q7c (long-input heap-fallback godoc scope notes), and the benchmark-assertion updates for Q7b/Q8a/Q8c/Q8d. Tokenise ASCII fast path (Q8b) is in a separate dedicated plan (Plan 08) because it requires its own BDD + benchmarks per the CONTEXT lock.

This plan is a soft predecessor of bench.txt regeneration (Plan 16) — the post-Q7a DoubleMetaphone alloc drop cascades into the DefaultScorer composite budget verification.

Output: per-algorithm alloc budgets match implementation reality per the revised `docs/requirements.md §14.1 / §14.2 / §14.3`; the DL-Full ZeroAllocs test is no longer skipped; the existing benchmark numbers reflect the optimisations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@docs/requirements.md
@.claude/skills/performance-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
<!-- Q7a/Q7b/Q7c/Q7d/Q8a/Q8c/Q8d/Q11e LOCKED in 08.5-CONTEXT.md §Q7+Q8+Q11e -->

Q7a — DoubleMetaphone strings.Builder → [dmMaxLen]byte:
- Current: double_metaphone.go uses strings.Builder for primary + secondary key accumulation (~6 allocs per DoubleMetaphoneKeys call)
- Locked target: [dmMaxLen]byte * 2 + length counters (≤ 2 allocs per call)
- Pattern source: double_metaphone.go:169-211 dmPrep already uses [64]byte — extend the in-file pattern

Q7d — Q-gram extractQGrams capacity hint:
- Current: m := make(map[string]int) (no capacity hint)
- Locked target: m := make(map[string]int, (len(s)-n+1) + (len(s)-n+1)/4) (25% capacity headroom)

Q11e — DL-Full ZeroAllocs un-skip:
- Current: damerau_full_test.go has t.Skip(...) on TestDamerauLevenshteinFullScore_ZeroAllocs_ASCII_Short
- Locked target: un-skip; assert allocs/op ≤ 1 (Q8a budget — 34 KB stack frame deemed too fragile for 0 allocs)

Q7c — Long-input heap-fallback godoc scope notes:
- Per-algorithm godoc where the optimisation is structurally absent (DL-Full, RO recursion, etc.)

Q7b/Q8a/Q8c/Q8d — Benchmark assertion updates (no algorithm code changes; assertion updates):
- DL-Full Medium: 8.2 µs (was < 3 µs); DL-Full Short: ≤ 1 alloc
- SWG Medium: < 10 µs
- Normalise ASCII Short: ≤ 1 alloc; Soundex Code: ≤ 1 alloc
- Scorer ASCII Short: ≤ 8; Scorer ASCII Medium: ≤ 20
- Ratcliff-Obershelp Short: ≤ 4 allocs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Q7a DoubleMetaphone [dmMaxLen]byte accumulators + alloc-budget benchmark assertion</name>
  <files>double_metaphone.go, double_metaphone_bench_test.go</files>
  <read_first>
    - double_metaphone.go in full (especially lines 169-211 dmPrep — the existing [64]byte stack-buffer pattern that this task extends)
    - double_metaphone_bench_test.go (the existing benchmark structure + assertion shape)
    - .planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md §Q7a
    - .planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md §2.F (Q7a stack-buffer pattern)
    - .claude/skills/performance-standards/SKILL.md (ASCII fast path + stack-buffer idioms)
    - docs/requirements.md §14.1 (revised DoubleMetaphone alloc budget)
  </read_first>
  <action>
1. In double_metaphone.go:
   - Locate the strings.Builder declarations for the primary and secondary key accumulators (find via grep `strings.Builder`).
   - Declare a package-level constant `dmMaxLen` if not present (Philips 2000 key length is 4 — verify via existing MaxLen constants in the file).
   - Replace each strings.Builder with a `[dmMaxLen]byte` array + an `int` length counter. Substitution pattern (PATTERNS.md §2.F):
     - `var p strings.Builder` becomes `var pBuf [dmMaxLen]byte; var pLen int`
     - `p.WriteByte(c)` becomes `if pLen < dmMaxLen { pBuf[pLen] = c; pLen++ }`
     - `p.String()` becomes `string(pBuf[:pLen])`
     - `p.Len()` becomes `pLen`
   - Update the dmAdd helper (or whichever helper appends to the accumulators) to use the new buffer + length signature (pass pointers if needed).
   - The public API `DoubleMetaphoneKeys` returning (primary string, alternate string) does NOT change.
   - Add a brief inline comment at the buffer declarations citing Q7a / docs/requirements.md §14.1.
2. In double_metaphone_bench_test.go:
   - Update the BenchmarkDoubleMetaphoneScore_ASCII_Short assertion (or the corresponding alloc-budget test) to expect `allocs/op` ≤ 2.
   - Run `go test -bench=BenchmarkDoubleMetaphone -benchmem -count=10 -run=^$ ./...` and confirm the assertion holds locally.
3. Verify no regression: the existing TestDoubleMetaphone language-branches table from Phase 7 must remain green.
  </action>
  <verify>
    <automated>go build ./... && go test -run TestDoubleMetaphone -race -count=1 ./... && go test -bench=BenchmarkDoubleMetaphone -benchmem -count=10 -run=^$ ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "strings\\.Builder" double_metaphone.go | wc -l` returns 0 (no remaining Builder declarations in the key-accumulation path; an unrelated use elsewhere in the file is acceptable but verify).
    - `grep -E "\\[dmMaxLen\\]byte|\\[4\\]byte" double_metaphone.go | wc -l` returns at least 2 (primary + secondary buffers).
    - Benchmark output: `go test -bench=BenchmarkDoubleMetaphoneScore_ASCII_Short -benchmem -count=10 -run=^$ ./... | grep -E "[12]\\s+allocs/op"` returns at least 1 line (verifies ≤ 2 allocs/op).
    - `go test -run TestDoubleMetaphone -race -count=1 ./...` exits 0 (correctness preserved).
    - `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>
    DoubleMetaphone key accumulation uses stack-allocated buffers; alloc count is ≤ 2 per call; existing correctness tests pass; benchmark assertion reflects the new budget.
  </done>
</task>

<task type="auto">
  <name>Task 2: Q7d Q-gram capacity hint + Q11e DL-Full un-skip + Q7b/Q8a/Q8c/Q8d benchmark assertions + Q7c godoc scope notes</name>
  <files>q_gram.go, q_gram_test.go, damerau_full_test.go, damerau_full_bench_test.go, ratcliff_obershelp_bench_test.go, scorer_bench_test.go, soundex.go, normalise.go</files>
  <read_first>
    - q_gram.go (extractQGrams current map-construction line)
    - q_gram_test.go (q-gram correctness tests — must remain green)
    - damerau_full_test.go (the t.Skip line on TestDamerauLevenshteinFullScore_ZeroAllocs_ASCII_Short — confirm exact form)
    - damerau_full_bench_test.go (alloc-budget assertion shape)
    - ratcliff_obershelp_bench_test.go (the Short budget — Q8d)
    - scorer_bench_test.go (Short ≤ 8, Medium ≤ 20 — Q8c)
    - docs/requirements.md §14.1, §14.2, §14.3 (revised per-algorithm budgets)
    - soundex.go + normalise.go (Q7b godoc scope notes target)
  </read_first>
  <action>
1. **Q7d — Q-gram capacity hint** in q_gram.go:
   - Locate the extractQGrams function and find the `make(map[string]int)` call.
   - Replace with: `m := make(map[string]int, qgramCap(len(s), n))` where `qgramCap` is a small helper that returns `0` when `len(s) < n`, else returns `(len(s)-n+1) + (len(s)-n+1)/4` (25% headroom). Inline the formula directly if no helper is needed.
   - Verify guard: `if len(s) < n { return ... }` may already exist — preserve existing short-input semantics.
   - Add a one-line inline comment citing Q7d.

2. **Q11e — DL-Full ZeroAllocs un-skip** in damerau_full_test.go:
   - Find `t.Skip(` in TestDamerauLevenshteinFullScore_ZeroAllocs_ASCII_Short (or its closest equivalent).
   - Remove the skip directive.
   - Update the assertion to expect `allocs/op` ≤ 1 (Q8a budget).
   - Rename the test if its name still says "ZeroAllocs" but the new budget is 1 alloc — recommend `TestDamerauLevenshteinFullScore_ShortAllocBudget_ASCII` or similar (executor's judgement; document the rename in the commit message).

3. **Q8a/Q8c/Q8d/Q7b — Benchmark assertion updates** (assertion-only edits):
   - In damerau_full_bench_test.go: Short alloc assertion is `≤ 1` (Q8a); Medium time-budget assertion is `< 8.2 µs` (Q7b).
   - In ratcliff_obershelp_bench_test.go: Short alloc assertion is `≤ 4` (Q8d).
   - In scorer_bench_test.go: ensure BenchmarkDefaultScorer_ASCII_Short alloc assertion is `≤ 8`; BenchmarkDefaultScorer_ASCII_Medium is `≤ 20` (Q8c). If BenchmarkDefaultScorer_* benchmarks do not exist, this task creates skeleton functions with the assertion targets — full Scorer bench coverage lands in Plan 16, but the assertion shape is set here.
   - In soundex_bench_test.go (if exists; check via `ls`): Code alloc assertion is `≤ 1` (Q7b — was `0`).
   - In normalise_bench_test.go (if exists): Normalise ASCII Short alloc assertion is `≤ 1` (Q7b — was `0`).

4. **Q7c — Long-input heap-fallback godoc scope notes**:
   - For algorithms where the long-input heap-fallback optimisation is structurally absent (DL-Full, Ratcliff-Obershelp recursion, LCSStr full-table path), add a one-paragraph godoc scope note. Suggested wording:
     "Performance scope: This algorithm's allocation budget assumes inputs fit within the ASCII fast path (≤ 50 characters) and the two-row DP optimisation engages. For long inputs (≥ 500 characters), the algorithm falls back to a heap-allocated DP table; the per-call allocation count scales linearly with |a|+|b|. See docs/requirements.md §14.1 for the full per-input-size budget table."
   - Apply at the public function godoc of: DamerauLevenshteinFullScore, RatcliffObershelpScore, LongestCommonSubstring* (LCSStr surface).
   - Also add to Soundex and Normalise godoc the note that their ≤ 1 alloc budget is the new floor per Q7b (the `string(stackBuf[:n])` return is structurally unavoidable without `unsafe.String`).
  </action>
  <verify>
    <automated>go build ./... && go test -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "len\\(s\\)-n\\+1" q_gram.go | wc -l` returns at least 1 (capacity hint applied).
    - `grep -nE "t\\.Skip" damerau_full_test.go | grep -i ZeroAllocs` returns 0 (Q11e un-skipped).
    - The DL-Full Short alloc-budget test runs and passes: `go test -run 'TestDamerauLevenshteinFull.*Alloc' -race -count=1 ./...` exits 0.
    - `go test -race -count=1 ./...` exits 0 (no Phase 5 q-gram correctness regression; no Phase 2 DL-Full regression).
    - Godoc scope notes: `grep -n "Performance scope" damerau_full.go ratcliff_obershelp.go lcs_str.go | wc -l` returns at least 3 (one per file).
    - `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>
    Q7d capacity hint landed; Q11e un-skipped and passes the ≤ 1 alloc assertion; Q7b/Q8a/Q8c/Q8d budget assertions updated to match revised §14.1/§14.2/§14.3; Q7c long-input scope notes added to DL-Full, RO, LCSStr, Soundex, Normalise godoc.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → DoubleMetaphoneKeys / extractQGrams / DL-Full | Untrusted-length input may exceed the stack-buffer bounds (S — Spoofing of buffer size); on long inputs heap fallback engages. The optimisations preserve the public API; no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-14 | I — Information disclosure (stack-buffer over-read) | dmAdd append into [dmMaxLen]byte | mitigate | The Q7a refactor explicitly guards `if pLen < dmMaxLen` before each byte write, preventing out-of-bounds writes. Existing tests (Phase 7 language-branches table) verify correctness post-refactor. |
| T-08.5-15 | D — DoS via heap-fallback amplification on long inputs | DL-Full, RO, LCSStr | accept | The Q7c godoc scope notes document the long-input heap-fallback as expected behaviour. The Q14a 5 MB/call ceiling (documented in Plan 17) bounds the worst case. |
</threat_model>

<verification>
- Compile-green: `go build ./...` exits 0.
- Test-green: `go test -race -count=1 ./...` exits 0.
- Bench-green: alloc-budget assertions pass on local hardware (`go test -bench=. -benchmem -count=3 -run=^$ ./...`).
- Godoc: `grep -n "Performance scope" damerau_full.go ratcliff_obershelp.go lcs_str.go` returns at least 3 matches.
- Q7d static: `grep -nE "len\\(s\\)-n\\+1" q_gram.go` returns at least 1.
- Q11e static: `grep -ni "t\\.Skip" damerau_full_test.go | grep -i ZeroAllocs` returns 0.
</verification>

<success_criteria>
- Q7a alloc reduction observed locally (≤ 2 allocs/op on BenchmarkDoubleMetaphoneScore_ASCII_Short).
- Q7d capacity hint landed.
- Q11e un-skipped and passing.
- Q7c godoc scope notes added.
- Q7b/Q8a/Q8c/Q8d benchmark assertions match the revised budgets.
- Soft predecessor of Plan 16 (bench.txt regeneration) — bench.txt regen sequences AFTER this plan + Plan 06 (Q11b) + Plan 08 (Q8b).
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-07-SUMMARY.md` documenting:
- the alloc-count before/after for BenchmarkDoubleMetaphoneScore_ASCII_Short (recorded as `before: N → after: M`);
- the Q11e un-skipped test's final assertion;
- the godoc scope-note additions per file;
- agent reviews invoked (algorithm-performance-reviewer mandatory; code-reviewer; commit-message-reviewer).
</output>
