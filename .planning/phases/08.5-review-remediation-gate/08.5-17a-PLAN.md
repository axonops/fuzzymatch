---
phase: 08.5
plan: 17a
type: execute
wave: 6
depends_on: [01, 02, 03, 13]
files_modified:
  - docs/algorithms.md
  - levenshtein.go
  - damerau_full.go
  - damerau_osa.go
  - hamming.go
  - jaro.go
  - jaro_winkler.go
  - strcmp95.go
  - swg.go
  - lcs_str.go
  - q_gram.go
  - cosine.go
  - tversky.go
  - monge_elkan.go
  - token_sort_ratio.go
  - token_set_ratio.go
  - partial_ratio.go
  - token_jaccard.go
  - soundex.go
  - double_metaphone.go
  - nysiis.go
  - mra.go
  - ratcliff_obershelp.go
  - dice.go
autonomous: true
requirements:
  - VALIDATE-06
  - Q4
  - Q12c
  - Q14a
  - Q6a
threat_model_refs: [T-08.5-33]

must_haves:
  truths:
    - "docs/algorithms.md fully populated (not a TBD scaffold) with per-algorithm sections + Q12c panic-surface section + Q14a 5 MB/call expected-behaviour note"
    - "23 algorithm files have godoc cross-references to fuzzymatch.Validate"
    - "docs/algorithms.md anchor casing standardised to hyphenated"
    - "Ratcliff-Obershelp asymmetric-by-design exception documented in .claude/skills/algorithm-correctness-standards/SKILL.md"
  artifacts:
    - path: "docs/algorithms.md"
      provides: "Full per-algorithm content + panic-surface section + 5 MB ceiling note"
      contains: "Validate"
      min_lines: 600
  key_links:
    - from: "Validate godoc cross-references"
      to: "23 algorithm files"
      via: "fuzzymatch.Validate"
      pattern: "fuzzymatch.Validate"
---

<objective>
**Plan 17a (the codebase-heavy half of the split former-Plan-17, M6 revision 2026-05-17)** — the docs/algorithms.md full fill-in plus the 22 per-algorithm godoc cross-references to `fuzzymatch.Validate`. Touches ~14 files (1 doc + 22 .go files; some .go files have the cross-ref already pre-Plan-17a, reducing the actual modification count).

Scope:
1. **docs/algorithms.md full fill-in** (currently 100% TBD scaffold per Phase 8 docs-writer Critical) — per-algorithm sections + Validate dedicated section.
2. **Q12c — Panic-surface section** in docs/algorithms.md + README cross-reference.
3. **Q14a — 5 MB/call expected-behaviour note** in docs/algorithms.md#performance-characteristics.
4. **23 per-algorithm godoc cross-references** to `fuzzymatch.Validate` in each algorithm .go file.
5. **Q6a — Ratcliff-Obershelp asymmetric-by-design exception** documented in `.claude/skills/algorithm-correctness-standards/SKILL.md` (deferred to a SKILL update, treated as a code-adjacent edit since it gates property tests).

The docs-heavy consumer-facing work (README + Gap 3 BDD features + examples + llms.txt + llms-full.txt + docs/scorer.md/tuning.md/etc. + British English sweep across docs) lives in Plan 17b.

**Hard dependencies:** Plans 01 (sentinel renames), 02 (MongeElkan rename), 03 (PartialRatioScoreRunes removal), 13 (Validate core exists). Plan 17b depends on Plan 17a + 13.

Output: a per-algorithm-godoc-rich pass that closes the docs/algorithms.md TBD-scaffold Critical + Q12c panic-surface + Q14a 5 MB ceiling + Q6a SKILL exception.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@docs/requirements.md
@CHANGELOG.md
@REVIEW-FINDINGS.md
@.claude/skills/documentation-standards/SKILL.md
@.claude/skills/algorithm-correctness-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
Plan 13 outputs (consumed here):
- Public Validate, Warning, WarnKind surfaces exist in validate.go + warn_kind.go
- Per-algorithm rule table from Plan 13 SUMMARY (the executor lifts this into docs/algorithms.md)

Documentation-standards SKILL six-surface mandate — Plan 17a covers surfaces 2 and 3:
- Surface 2: docs/algorithms.md dedicated section
- Surface 3: per-algorithm godoc cross-references (22-23 files)

Plan 17b covers surfaces 1, 4, 5, 6:
- Surface 1: README Quick Start / Common Patterns
- Surface 4: llms.txt + llms-full.txt
- Surface 5: user-guide section (docs/best-practices.md)
- Surface 6: runnable examples/validate-input-quality/

Q12c panic-surface section (Plan 17a):
- Enumerate every public function that panics and the sentinel each panic carries
- Cross-reference from README — the actual README edit lives in Plan 17b; this plan adds the docs/algorithms.md section that README will link to

Q14a 5 MB/call expected-behaviour note (Plan 17a):
- "Cross-algorithm allocation budget ceiling: each algorithm call allocates at most 5 MB / call for inputs up to 1 MB. Cross-algorithm caching of tokenisation results is deferred to v1.x post-1.0 (GitHub issue #2). See docs/requirements.md §14 for the per-input-size budget table."

Q6a SKILL update (Plan 17a, code-adjacent):
- Extend `.claude/skills/algorithm-correctness-standards/SKILL.md` § "Symmetric algorithms" exception list to include "Ratcliff-Obershelp (asymmetric by design for difflib parity per OQ-1, locked 2026-05-14, re-confirmed Q6a 2026-05-17)"

British English: every doc edit uses British spelling.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/algorithms.md full fill-in + Validate section + Q12c panic-surface + Q14a 5 MB note + anchor casing</name>
  <files>docs/algorithms.md</files>
  <read_first>
    - docs/algorithms.md (current 100% TBD scaffold; verify anchor casing from any prior Plan 16 work)
    - docs/scorer.md + docs/tuning.md (sister doc templates)
    - Each of the 23 algorithm source files for their godoc + reference vectors (used to fill in per-algorithm sections)
    - Plan 13 SUMMARY (the per-algorithm WarnKind rule table)
    - docs/requirements.md §7, §11.5, §14
    - .planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
    - .claude/skills/documentation-standards/SKILL.md
  </read_first>
  <action>
1. Rewrite docs/algorithms.md from the TBD scaffold to a full document. Structure:

   - **Table of contents** with hyphenated H2 anchors (matching the anchor-casing standardisation from Plan 16).
   - **Per-algorithm sections** (23 sections — one per algorithm): for each algorithm:
     - One-paragraph overview citing the primary academic source.
     - Function signature(s) (post-Q1/Q3/Q5 surface — pull directly from the .go files).
     - Reference vectors from the literature.
     - WarnKind values it can trigger (cross-reference Plan 13's rule table).
     - Performance characteristics (alloc budget; ASCII fast path; long-input scope per Q7c).
     - Limitations + when to use vs alternatives.
   - **Validate section** (dedicated, ~150 LOC per RESEARCH.md §3.8): when to use, why to use, realistic example from audit-tool / dedup / search use cases, per-WarnKind semantics. This is the documentation-standards SKILL surface 2 for VALIDATE-06.
   - **Q12c panic-surface section**: enumerate every public function that panics and the sentinel each panic carries. Pull from the Phase 8 review + Plan 01 sentinel additions + Plan 04 Tversky direct-call panic + monge_elkan direct-call panic. README cross-reference is added in Plan 17b.
   - **Q14a 5 MB/call expected-behaviour note** at the performance-characteristics section: "Cross-algorithm allocation budget ceiling: each algorithm call allocates at most 5 MB / call for inputs up to 1 MB. Cross-algorithm caching of tokenisation results is deferred to v1.x post-1.0 (GitHub issue #2). See docs/requirements.md §14 for the per-input-size budget table."

2. British English throughout.

3. Markdown anchors use hyphenated casing (set by Plan 16).
  </action>
  <verify>
    <automated>markdownlint docs/algorithms.md 2>&1 || true && wc -l docs/algorithms.md</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l docs/algorithms.md` returns at least 600 (full content).
    - `grep -cE "^## " docs/algorithms.md` returns at least 23 (one H2 per algorithm) + extra sections for Validate + panic surface + performance.
    - `grep -n "Validate" docs/algorithms.md | wc -l` returns at least 5 (Validate section + cross-refs).
    - `grep -nE "5 MB" docs/algorithms.md | wc -l` returns at least 1 (Q14a note).
    - `grep -nE "Panic-surface|panic surface|Panic surface" docs/algorithms.md | wc -l` returns at least 1 (Q12c section).
    - `grep -nE "color|behavior|normalize|serialize" docs/algorithms.md | wc -l` returns 0 (British English).
    - `markdownlint docs/algorithms.md` exits 0.
  </acceptance_criteria>
  <done>
    docs/algorithms.md is fully populated; Validate has a dedicated section; Q12c panic-surface enumerated; Q14a 5 MB ceiling documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: 22-23 per-algorithm godoc cross-references to fuzzymatch.Validate</name>
  <files>levenshtein.go, damerau_full.go, damerau_osa.go, hamming.go, jaro.go, jaro_winkler.go, strcmp95.go, swg.go, lcs_str.go, q_gram.go, dice.go, cosine.go, tversky.go, monge_elkan.go, token_sort_ratio.go, token_set_ratio.go, partial_ratio.go, token_jaccard.go, soundex.go, double_metaphone.go, nysiis.go, mra.go, ratcliff_obershelp.go</files>
  <read_first>
    - Each of the 23 algorithm .go files (find the public function godoc block to add the Validate cross-reference)
    - Plan 13 SUMMARY (Validate surface + WarnKind rule table to embed)
  </read_first>
  <action>
For each of the 23 algorithm files, add a one-line godoc cross-reference to fuzzymatch.Validate near the top of the public function's godoc:
```
// For programmatic input-quality checks before scoring,
// see [fuzzymatch.Validate].
```

Files: levenshtein.go, damerau_full.go, damerau_osa.go, hamming.go, jaro.go, jaro_winkler.go, strcmp95.go, swg.go, lcs_str.go, q_gram.go (utility — cross-ref optional), dice.go, cosine.go, tversky.go, monge_elkan.go, token_sort_ratio.go, token_set_ratio.go, partial_ratio.go, token_jaccard.go, soundex.go, double_metaphone.go, nysiis.go, mra.go, ratcliff_obershelp.go.

British English. Apache 2.0 license header already present.
  </action>
  <verify>
    <automated>go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "fuzzymatch\\.Validate" levenshtein.go damerau_full.go damerau_osa.go hamming.go jaro.go jaro_winkler.go strcmp95.go swg.go lcs_str.go cosine.go tversky.go monge_elkan.go token_sort_ratio.go token_set_ratio.go partial_ratio.go token_jaccard.go soundex.go double_metaphone.go nysiis.go mra.go ratcliff_obershelp.go | grep -vE ':0$' | wc -l` returns at least 21 (all primary algorithm files have the cross-ref; q_gram.go and dice.go are utility/shared files and may be exempt).
    - `go build ./...` exits 0.
    - `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>
    All primary algorithm files carry the `[fuzzymatch.Validate]` godoc cross-reference.
  </done>
</task>

<task type="auto">
  <name>Task 3: Q6a Ratcliff-Obershelp asymmetric-by-design exception in algorithm-correctness-standards/SKILL.md</name>
  <files>.claude/skills/algorithm-correctness-standards/SKILL.md</files>
  <read_first>
    - .claude/skills/algorithm-correctness-standards/SKILL.md (find the "Symmetric algorithms" section)
    - docs/requirements.md §7.24 (Ratcliff-Obershelp specification)
  </read_first>
  <action>
1. Edit `.claude/skills/algorithm-correctness-standards/SKILL.md` § "Symmetric algorithms" (around line 117-119): extend the exception list from "Monge-Elkan + Tversky" to include "Ratcliff-Obershelp (asymmetric by design for difflib parity per OQ-1, locked 2026-05-14, re-confirmed Q6a 2026-05-17)".
2. Add a one-paragraph rationale: Ratcliff-Obershelp's recursive LCSubstr decomposition makes (a, b) and (b, a) produce different sequences of recursive matches; this is the Dr. Dobb's 1988 specification and matches Python difflib.SequenceMatcher.ratio() behaviour.
3. Cross-reference `docs/algorithms.md` Ratcliff-Obershelp section.
4. `PropAlgorithmScore_Symmetric` already excludes AlgoRatcliffObershelp per Phase 4 work; verify (no code change expected, just the SKILL update).

British English throughout.
  </action>
  <verify>
    <automated>grep -n "Ratcliff-Obershelp" .claude/skills/algorithm-correctness-standards/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "Ratcliff-Obershelp.*asymmetric|asymmetric.*Ratcliff-Obershelp" .claude/skills/algorithm-correctness-standards/SKILL.md | wc -l` returns at least 1.
    - `grep -nE "Q6a|OQ-1" .claude/skills/algorithm-correctness-standards/SKILL.md | wc -l` returns at least 1.
  </acceptance_criteria>
  <done>
    Q6a Ratcliff-Obershelp asymmetric-by-design exception documented in the SKILL; cross-reference to docs/algorithms.md present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validate documentation → consumer adoption | If Validate is not discoverable across the 6 surfaces, the docs-writer / user-guide-reviewer Critical findings re-fire on the verification gate. Plan 17a covers surfaces 2 and 3; Plan 17b covers 1, 4, 5, 6. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-33 | I — Information disclosure (stale signatures in per-algorithm godoc) | 22-23 algorithm .go files | mitigate | The Validate cross-reference is the only new godoc element this plan adds to each file; the existing signatures are post-Plan 01/02/03 and are not modified here. Plan 17b's `make verify-llms-sync` + README meta-test catch any signature drift in the docs-facing surfaces. |
</threat_model>

<verification>
- markdownlint exits 0 on docs/algorithms.md.
- All 21+ algorithm files have Validate cross-references.
- Q6a SKILL update present.
- British English in the new docs/algorithms.md.
</verification>

<success_criteria>
- docs/algorithms.md fully populated (closes the docs-writer Critical 100% TBD scaffold finding).
- Q12c panic-surface section present.
- Q14a 5 MB ceiling documented.
- 22-23 per-algorithm godoc cross-refs (VALIDATE-06 surface 3 complete).
- Q6a SKILL exception recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-17a-SUMMARY.md` documenting:
- docs/algorithms.md final LOC count;
- per-algorithm files touched + cross-ref count;
- Q12c panic-surface section anchor;
- Q14a 5 MB note anchor;
- Q6a SKILL update line ref;
- agent reviews invoked (docs-writer mandatory; algorithm-correctness-reviewer for the per-algorithm content; api-ergonomics-reviewer for the panic-surface section).
</output>
