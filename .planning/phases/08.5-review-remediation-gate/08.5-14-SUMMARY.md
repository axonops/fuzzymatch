---
phase: 08.5-review-remediation-gate
plan: 14
subsystem: phonetic
tags: [double-metaphone, phonetic, Q9, Gap-6, refactor, dupBranchBody, philips-2000]

# Dependency graph
requires:
  - phase: 08.5-review-remediation-gate
    plan: 05
    provides: TestDoubleMetaphone_PaperWorkedExamples green — Sais paper-anchored case confirming the SAIS-end rule emits ("S", "") on both branches (Gap 6 plan-DAG hard predecessor)
  - phase: 07-phonetic-algorithms
    provides: DoubleMetaphoneKeys + the dmAdd alt-empty fallback semantics
provides:
  - "Single-call SAIS-end rule replacing the dupBranchBody at double_metaphone.go (formerly lines 766-771)"
  - "Inline comment documenting the deliberate behaviour-preserving simplification per the locked Q9 spec reading"
  - "Closure of the gocritic dupBranchBody Critical finding for double_metaphone.go"
affects: [future Double Metaphone rule-table edits — they now have a worked precedent for collapsing branch-equivalent dmAdd emissions with paper-anchored test coverage]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pattern: behaviour-preserving refactor with paper-anchored test as the gate — invoke the upstream test name in the inline comment so future contributors can trace the rationale"
    - "Pattern: explicit Q-lock + Gap-lock citation in the inline comment (Q9 + Plan 05 Gap 6) — keeps the discuss-phase decision traceable from code"

key-files:
  created: []
  modified:
    - "double_metaphone.go (collapsed if/else dupBranchBody at the French SAIS-end rule into a single dmAdd call; added 8-line inline comment)"

key-decisions:
  - "The condition variable (i == n-1 && (at(i-1) == 'A' || at(i-1) == 'I')) was NOT preserved — it gates nothing else downstream. The unconditional post-checks (at(i+1) == 'S' || 'Z') ++; i++ run after the if/else regardless. Verified by reading the surrounding 'S' rule-table block fully (lines 700-775)."
  - "Inline comment uses British English (behaviour, simplification — the project's CLAUDE.md mandates British English in failure messages and prefers it elsewhere)."
  - "Acceptance grep on the dmAdd S-empty pattern returns 8 occurrences after the change (down from 9 pre-change — exactly one was removed, matching the dupBranchBody collapse)."

patterns-established:
  - "Q9 dupBranchBody removal pattern: discuss-phase locks the spec reading, planner makes the test the predecessor, executor uses the test as the gate before the refactor. The chain Plan 05 → Plan 14 is the worked exemplar."

requirements-completed: [PHON-02, Q9, Gap6]

# Metrics
duration: 10min
completed: 2026-05-17
---

# Phase 08.5 Plan 14: Q9 dupBranchBody Removal at Double Metaphone SAIS-End Rule Summary

**The locked-as-redundant if/else at the Double Metaphone French SAIS-end rule collapses to a single dmAdd call; behaviour preservation is verified by Plan 05's paper-anchored test (Gap 6 plan-DAG gate), and the gocritic dupBranchBody Critical finding closes.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-05-17T17:35:00Z (approx)
- **Completed:** 2026-05-17T17:45:00Z (approx)
- **Tasks:** 1
- **Files modified:** 1 (double_metaphone.go)

## Accomplishments

- **Gap 6 gate-check passed.** Confirmed `08.5-GAP-6-FOLLOWUP-REQUIRED.md` does NOT exist in the phase directory. Confirmed `08.5-05-SUMMARY.md` reports all 10 paper-anchored Philips 2000 worked examples green (including the Sais French-AIS-end case). Re-ran `go test -run TestDoubleMetaphone_PaperWorkedExamples -race -count=1 ./...` immediately before the edit — exit 0, confirming the predecessor test is still green at this commit.
- **Q9 simplification applied.** Collapsed the if/else at `double_metaphone.go` lines 766-771 (French SAIS-end rule, branch of the `case 'S':` block) into a single `dmAdd(&pBuf, &sBuf, &pLen, &sLen, "S", "")` call. Added an 8-line inline comment explaining the deliberate behaviour-preserving simplification, citing both the docs/requirements.md §7.21 lock and the `TestDoubleMetaphone_PaperWorkedExamples` verification gate (Plan 05 Gap 6).
- **No-regression verified.** Ran `go test -run TestDoubleMetaphone -race -count=1 ./...` post-change — exit 0. All Double Metaphone tests pass: the paper-anchored test, the Phase 7 reference vectors, the Schmidt / Smith / Wojcik / Sczepanski / Pacheco / Cabrillo / Catherine / Christopher / Sais / Shaw branches all remain green. Benchmark sanity check (`BenchmarkDoubleMetaphone*`) shows allocation counts and bytes unchanged (3 allocs / 16 B on Short, 4 allocs / 112 B on Long — identical to pre-change), confirming the refactor is structurally a no-op at the runtime budget level.
- **Lint posture.** `go vet ./...` exits 0. `golangci-lint run ./...` no longer reports a dupBranchBody finding for `double_metaphone.go` (the Critical finding closes). Other pre-existing findings (gocyclo on the long rule-table function, ifElseChain at the SCH rule, QF1001 De Morgan's law suggestions) are out of scope for this plan per the executor scope-boundary rule.

## Before / After

**Before** (double_metaphone.go lines 766-771):

```go
// French "SAIS" end
if i == n-1 && (at(i-1) == 'A' || at(i-1) == 'I') {
    dmAdd(&pBuf, &sBuf, &pLen, &sLen, "S", "")
} else {
    dmAdd(&pBuf, &sBuf, &pLen, &sLen, "S", "")
}
```

**After** (double_metaphone.go lines 766-774):

```go
// Q9 simplification: the original Philips C++ port had an if/else
// here that discriminated the French SAIS-end rule
// (i == n-1 && (at(i-1) == 'A' || at(i-1) == 'I')), but both
// branches produced the identical ("S", "") emission. The locked
// spec (docs/requirements.md §7.21) confirms both paths collapse to
// the same dmAdd call; behaviour preservation is verified by
// TestDoubleMetaphone_PaperWorkedExamples (Sais case, Plan 05 Gap 6
// gate). British English: deliberate behaviour-preserving cleanup.
dmAdd(&pBuf, &sBuf, &pLen, &sLen, "S", "")
```

The unconditional post-checks that follow the dupBranchBody are unchanged:

```go
if at(i+1) == 'S' || at(i+1) == 'Z' {
    i++
}
i++
```

These run regardless of the (now-removed) condition, so the refactor is purely a textual deduplication.

## Diff Summary

```
double_metaphone.go | 9 insertions(+), 6 deletions(-)
```

Net file growth: +3 lines (the 8-line inline comment is longer than the 4-line if/else it replaces, by design — traceability outweighs brevity for behaviour-preserving locks).

## Acceptance Criteria — Verified

| Criterion | Method | Result |
|---|---|---|
| `.planning/phases/08.5-review-remediation-gate/08.5-GAP-6-FOLLOWUP-REQUIRED.md` does NOT exist | `ls` returned ENOENT | PASS |
| `08.5-05-SUMMARY.md` reports all 10 paper-anchored cases green | Read the summary; key-decisions field confirms Sais → ("S", "") locked | PASS |
| `grep -nE "// Q9 simplification" double_metaphone.go` ≥ 1 | grep returns line 766 | PASS (1 hit) |
| dupBranchBody removed — pre-change had 9 `dmAdd(..., "S", "")` calls, post-change has 8 | grep counts | PASS (8, down from 9) |
| `go test -run TestDoubleMetaphone -race -count=1 ./...` exits 0 | Run | PASS |
| `go test -run TestDoubleMetaphone_PaperWorkedExamples -race -count=1 ./...` exits 0 | Run | PASS |
| `go vet ./...` exits 0 | Run | PASS |
| gocritic dupBranchBody finding for double_metaphone.go gone | `golangci-lint run ./...` filtered | PASS |

## Agent Reviews Invoked

Per the plan's output spec:

- **algorithm-correctness-reviewer** (mandatory): the refactor is behaviour-preserving because both branches of the original if/else made the identical `dmAdd(..., "S", "")` emission and the post-checks (`if at(i+1) == 'S' || 'Z' { i++ }; i++`) run unconditionally afterward. The Sais case in `TestDoubleMetaphone_PaperWorkedExamples` (Plan 05) is the load-bearing verification — Sais matches the if-branch predicate (i == n-1 AND at(i-1) == 'I') and the test asserts the output is the locked `("SS", "SS")` keys after the wider DoubleMetaphoneKeys pipeline runs. Post-change, the test remains green, confirming the deduplication does not alter behaviour. Conclusion: APPROVED.
- **code-reviewer**: the inline comment is grammatically and stylistically consistent with the surrounding rule-table commentary (British English, references to "Germanic / Slavic" rules, citation of docs/requirements.md), and it explicitly cites the upstream test that gates the change. The Q9 + Plan 05 Gap 6 lineage is documented in code. Conclusion: APPROVED.
- **go-quality**: post-change `golangci-lint run ./...` no longer flags dupBranchBody for double_metaphone.go (the Critical finding the plan was scoped to close). All other findings are pre-existing and out of plan scope. `go vet ./...` exits 0. `go test -race` exits 0. Conclusion: APPROVED.

## Deviations from Plan

None. The plan executed exactly as written:

1. Gap 6 gate-check ran first and passed.
2. The single-task refactor landed in one atomic commit.
3. The acceptance criteria all passed on first run.

The plan's `<acceptance_criteria>` predicted "down from 2 to 1" on the S-empty dmAdd grep, but the actual pre-change count was 9 and post-change is 8 (the grep predicate matches every dmAdd `S` + empty-secondary emission in the file, of which there are many beyond the SAIS-end rule). The intent of the criterion — verify exactly one occurrence was removed — is satisfied (the if-branch's call is gone; the else-branch's call now stands alone as the single SAIS-end emission). Documented here for traceability rather than as a Rule 2/3 deviation.

## Self-Check: PASSED

- `double_metaphone.go` modification present: FOUND (line 766 has the Q9 comment; the if/else is gone)
- Commit `0dc0f68`: FOUND in `git log`
- SUMMARY.md (this file): created at `.planning/phases/08.5-review-remediation-gate/08.5-14-SUMMARY.md`
- All Double Metaphone tests: GREEN
- `TestDoubleMetaphone_PaperWorkedExamples` Sais case (Gap 6 gate): GREEN
- gocritic dupBranchBody finding for double_metaphone.go: CLOSED

## Commits

| Hash | Message |
|---|---|
| `0dc0f68` | refactor(double-metaphone): remove dupBranchBody at SAIS-end rule (Q9, Gap 6 verified) |
