# Phase 8.5 Discussion Log

**Phase:** 8.5 — Review Remediation Gate
**Discussion date:** 2026-05-17 (external session) + 2026-05-17 (verification session that wrote this artefact)
**Mode:** Verification-only — the substantive 14-question session ran externally; this session confirmed alignment and recorded the canonical CONTEXT.

---

## Context

The `/gsd-discuss-phase 8.5` invocation in this session was framed explicitly as **verification, not re-discussion**. The user's command preamble named the four artefacts that already carry the locked decisions:

- `docs/requirements.md` — spec amendments, new `Validate` feature, panic policy, Coverage Floor 3, property-test generator distribution
- `.planning/ROADMAP.md` Phase 8.5 section — 9 scope clusters, all 465 findings in-scope, optional 8.5a/8.5b split
- `CHANGELOG.md` `[Unreleased]` — breaking changes (Q1, Q3, Q4, Q5) + new features (Q2, Q4)
- `.planning/REQUIREMENTS.md` — VALIDATE-01..VALIDATE-06 + `ErrInvalidInnerAlgo` in FOUND-05 + traceability matrix

The job in this session was:

1. Read `REVIEW-FINDINGS.md` and confirm every Critical and Important finding has a corresponding resolution recorded in the aligned docs.
2. Confirm internal consistency across the four aligned docs.
3. Flag any finding lacking a clear resolution path.
4. Flag any decision in the aligned docs that doesn't match current repo state.
5. Produce the canonical Phase 8.5 discuss-phase artefact.

The user explicitly directed: **"Do NOT re-litigate the 14 decisions. If something looks wrong, surface it for discussion — don't propose alternative paths unprompted."**

---

## The 14 Locked Decisions (External Session)

Recorded here as a single block; full per-decision detail with sources lives in `08.5-CONTEXT.md`.

| # | Lock |
|---|---|
| Q1 | Hamming silent-max policy. Code wins; spec catches up. |
| Q2 | Data-vs-parameter framework + `WithThreshold` NaN guard + `WithTverskyAlgorithm α + β > 0` guard + `WithAlgorithm` NaN/Inf/≤0 rejection. |
| Q3 | MongeElkan symmetric-by-default rename. Inert `opts NormalisationOptions` removed from both directional and symmetric variants. |
| Q4 | New `Validate` / `Warning` / `WarnKind` consumer-facing surface. Three unused sentinels removed. Four-section godoc template. `ErrInvalidInnerAlgo` added (follow-up commit `c5b377d`). Monge-Elkan direct-call panic policy. |
| Q5 | ASCII-only algorithms (Strcmp95, phonetic, token-tier) have no `*Runes` variants. `PartialRatioScoreRunes` removed. |
| Q6a | Ratcliff-Obershelp asymmetric-by-design (re-confirms OQ-1). |
| Q6b | `AlgoID.String()` CamelCase locked. `WarnKind.String()` follows same convention. |
| Q7a | DoubleMetaphone `strings.Builder` → `[4]byte` optimisation. |
| Q7b | 5 performance-budget amendments in §14.1 / §14.2 / §14.3. |
| Q7c | Long-input heap-fallback scope notes. |
| Q7d | Q-gram `extractQGrams` 25 % capacity hint. |
| Q8a | DL-Full Short ≤ 1 alloc (full 0-alloc rejected as too fragile for the 34 KB stack frame). |
| Q8b | `Tokenise` ASCII fast path (substring zero-copy + single scratch buffer). |
| Q8c | Scorer §14.2 budget revised (Short ≤ 8, Medium ≤ 20). |
| Q8d | Ratcliff-Obershelp Short ≤ 4 allocs. |
| Q9 | DoubleMetaphone `dupBranchBody` redundancy removal at `double_metaphone.go:744` (subject to Q11c verification — see Gap 6 in CONTEXT). |
| Q10 | Cross-validation corpora added for character tier (`jellyfish`), q-gram tier (`py_stringmatching`), Monge-Elkan (`py_stringmatching`). |
| Q11a | Delete `bench.txt.new` stale property-test failure capture. |
| Q11b | FMA-defeating double-cast at `cosine.go:343` and `scorer.go:380` with inline rationale. |
| Q11c | Paper-anchored Philips 2000 worked-examples test (Double Metaphone). |
| Q11d | `partial_ratio.go` variable rename + TODO removal. |
| Q11e | DL-Full ZeroAllocs test un-skipped after Q8a budget revision. |
| Q12a | Coverage Floor 3 tightened to ≥ 90 % statement coverage (AST-based exported-symbol detection). |
| Q12b | `testing/quick` Generator mixed-shape distribution. Fixes `TestProp_Scorer_WeightSumOne` uint16 overflow. |
| Q12c | Panic-surface doc section in `docs/algorithms.md` + README cross-reference. |
| Q13 | Devops cluster: `release.yml needs: [ci]`, `nightly.yml`, action SHA pinning, `make verify-llms-sync`, bench-diff CI check, `docs/algorithms.md` H2 anchor casing, AST-based coverage Floor 3 script, HashiCorp utility licence audit, 9 missing `TestGolden_*_Staging` tests. |
| Q14a | Cross-algorithm allocation budget / tokenisation cache deferred to v1.x (GitHub issue #2). |
| Q14b | Path A — 196 Improvement-tier findings in-scope for Phase 8.5. `WriteGoldenFile` unexport, `var _ = func()bool{}()` dispatch-init refactor, 30+ Code-fix items. |

---

## Verification Activity (This Session)

| Step | Result |
|---|---|
| Read `REVIEW-FINDINGS.md` summary + cross-cutting themes table | 74 Critical, 195 Important, 196 Improvement = 465 total. 13 cross-cutting themes flagged by ≥ 2 reviewers. |
| Read `docs/requirements.md` Phase 8.5 amendments (§6, §6.A, §7.x, §11.5, §14.x, §15.x) | All 14 Q-decisions reflected in spec. |
| Read `.planning/ROADMAP.md` Phase 8.5 section | 9 scope clusters cover all 14 Q-decisions; verification gate language ("zero Critical, zero Important") aligns with success criterion 10. |
| Read `CHANGELOG.md` `[Unreleased]` | Breaking changes (Q1, Q3, Q4, Q5) + Added (Validate, ErrInvalidInnerAlgo) + Changed (NaN guards, α+β>0 guard, CamelCase lock, sentinel godoc, Monge-Elkan panic policy) all present. |
| Read `.planning/REQUIREMENTS.md` Phase 8.5 entries | VALIDATE-01..VALIDATE-06 mapped to Phase 8.5; `ErrInvalidInnerAlgo` added to FOUND-05; traceability matrix updated (99 v1 requirements across 12 phases). |
| Enumerate all `### [Critical]` headings in REVIEW-FINDINGS.md | 74 Critical findings located across the 14 reviewer sections. |
| Spot-check Critical findings against the 14 Q-decisions | 71 / 74 mapped cleanly; 3 implicit (Gaps 1, 2, 3); 2 explicit gaps (Gaps 4, 5); 1 conflict to verify (Gap 6). |
| Sample Important findings across themes | All 13 cross-cutting Important themes mapped to a Q-decision or scope cluster. 4 individual Important api-ergonomics findings without an explicit Q-decision flagged as Gap 7. |
| Spot-check spec amendments against current repo state | Confirmed: `MongeElkanScore` is asymmetric (line 377), `MongeElkanScoreSymmetric` exists (line 467), `PartialRatioScoreRunes` exists, `HammingDistance` returns `int`, `cosine.go:343` lacks the outer `float64(...)` wrap, `scorer.go:380` lacks the double-cast, `bench.txt.new` exists, `Validate`/`Warning`/`WarnKind` do not exist, `ErrInvalidInnerAlgo` does not exist, `WriteGoldenFile` is exported, `double_metaphone.go:744` has the dupBranchBody. All consistent with "Phase 8.5 has not yet been executed; locked decisions describe the target state". |
| Inspect spec-vs-code naming for invalid-AlgoID sentinel | Spec §6 (line 221) and §6.A (line 394) say `ErrInvalidAlgoID`; code (`errors.go:98`, `scorer_options.go:156`, `scorer.go:231`, `scorer_options_test.go:86-499`) says `ErrInvalidAlgorithm`. `monge_elkan.go:162` and `:342` reference the non-existent `ErrInvalidAlgoID`. Flagged as Gap 4 — Q4 follow-up addresses the Monge-Elkan path via `ErrInvalidInnerAlgo` but does not resolve the broader name choice. |
| Inspect Q9 vs reviewer's Critical finding on `double_metaphone.go:744` | Q9 locks redundancy removal as behaviour-preserving. Reviewer says Philips C++ original + Python `metaphone==0.6` produce `("S", "S")` in the else branch (i.e., a real correctness defect, not a redundancy). Flagged as Gap 6 — Q11c paper-anchored test must run before Q9 implementation so Q9 can be re-opened if Philips 2000 itself diverges. |
| Compose canonical CONTEXT.md | Wrote `08.5-CONTEXT.md` with domain, spec-lock, all 14 Q-decisions verbatim, verification results, 7 gaps for planner triage, phase-split recommendation, verification gate, deferral, canonical refs, code-context snapshot, ready-for-planning summary. |

---

## Gaps Flagged for `/gsd-plan-phase 8.5`

See `08.5-CONTEXT.md` § "Gaps for /gsd-plan-phase to Triage" for full text. Numbered summary:

1. `scorer_options_test.go` filename rename (Critical, low-risk, mechanical).
2. BDD scenarios + step infrastructure for 3 sentinels (Critical, scope-cluster-6 implicit).
3. Which of the 4 missing BDD feature files belong to Phase 8.5 vs Phase 9? (`normalisation.feature` + `determinism.feature` → 8.5; `scan.feature` + `suppression.feature` → 9; planner must confirm with user.)
4. ~~`ErrInvalidAlgoID` vs `ErrInvalidAlgorithm` naming inconsistency.~~ **RESOLVED 2026-05-17 (follow-up session):** rename code `ErrInvalidAlgorithm` → `ErrInvalidAlgoID` (atomic, all call sites in lockstep). Recorded in `docs/requirements.md §6 + §6.A`, `CHANGELOG.md` "Changed", `.planning/REQUIREMENTS.md FOUND-05`, `.planning/ROADMAP.md` scope cluster 2.
5. ~~`DefaultScorer` panics on internal inconsistency.~~ **RESOLVED 2026-05-17 (follow-up session):** add new sentinel `ErrInternalInvariantViolated` and panic with a typed-error wrapping the internal cause. Semantic contract: "library bug — never fires in correct usage — file an issue." MUST NOT wrap caller-supplied parameter errors. Four-section godoc template recorded in `docs/requirements.md §6.A`. Recorded in `CHANGELOG.md` "Added", `.planning/REQUIREMENTS.md FOUND-05`, `.planning/ROADMAP.md` scope cluster 2.
6. ~~Q9 vs Philips 2000 reference.~~ **PLAN DEPENDENCY ENCODED 2026-05-17 (follow-up session):** Q11c (`TestDoubleMetaphone_PaperWorkedExamples`) is a hard predecessor of Q9 in the plan task DAG. If Q11c reveals any of the 8–12 Philips 2000 worked examples diverge, Q9 re-opens via a mini-discuss-phase before the fix lands; the mini-discuss-phase produces a "Gap 6 follow-up" section in this CONTEXT.md.
7. Four Important-tier api-ergonomics findings without an explicit Q-decision (MRACompare tuple, NewSWGParams mutability, WithoutAlgorithm silent no-op, AlgoIDs() per-call alloc). Default recommendations supplied; planner should batch-ask the user.

---

## Follow-Up Session (2026-05-17)

Immediately after the initial verification artefact was written, the user issued an explicit second-round instruction resolving Gaps 4 and 5 and encoding the Q11c → Q9 dependency for Gap 6. The follow-up did NOT re-litigate any of the original 14 Q-decisions; it filled in three specific resolution paths that the original session had flagged for user input.

| Gap | Resolution applied |
|---|---|
| 4 | Rename `ErrInvalidAlgorithm` → `ErrInvalidAlgoID` (breaking, pre-v1.0). All 11+ call sites and doc references updated atomically. |
| 5 | New sentinel `ErrInternalInvariantViolated`. Replaces bare panic at `scorer.go:586` with typed `panic(fmt.Errorf("%w: ...: %w", ErrInternalInvariantViolated, err))`. Reserved for library-internal invariants only — never for caller-supplied parameter errors. Companion `TestDefaultScorer_NeverPanics_PropertyTest` to be added during Phase 8.5 implementation. |
| 6 | Plan task-DAG dependency: Q11c (`TestDoubleMetaphone_PaperWorkedExamples`, covering all 8–12 Philips 2000 worked examples with paper-page citations) executes BEFORE Q9 (`double_metaphone.go:744` dupBranchBody fix). If Q11c reveals divergence, Q9 re-opens via mini-discuss-phase. |

**Files updated in the follow-up session:**

- `docs/requirements.md` — §6 canonical var block (added `ErrInternalInvariantViolated`, added Gap 4/5 history notes); §6.A enumeration (added `ErrInternalInvariantViolated`, noted Gap 4 rename on `ErrInvalidAlgoID`); §6.A 4-section template (added full exemplar for `ErrInternalInvariantViolated`)
- `CHANGELOG.md` — "Added" entry for `ErrInternalInvariantViolated`; "Changed" entry for the `ErrInvalidAlgorithm` → `ErrInvalidAlgoID` rename
- `.planning/REQUIREMENTS.md` — FOUND-05 rewritten to list the full canonical v1.0 sentinel set with phase-of-introduction notes
- `.planning/ROADMAP.md` — Phase 8.5 scope cluster 2 extended; Phase 8.5 "Discuss-phase decisions (LOCKED)" paragraph extended with Gap 4/5/6 resolutions and the Q11c → Q9 plan-dependency note
- `.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md` — Gap 4, Gap 5, Gap 6 sections rewritten with explicit RESOLVED / PLAN DEPENDENCY ENCODED status; code-context snapshot table updated; ready-for-planning summary updated
- `.planning/phases/08.5-review-remediation-gate/08.5-DISCUSSION-LOG.md` — this file (the follow-up section you are reading)

---

## Scope Creep Captured

None during this session. The user's preamble explicitly forbade re-litigation. No new ideas were introduced; no decisions were re-opened; no proposals were made outside the verification mandate.

---

## Deferred Ideas (For Later Phases)

- **Q14a / v1.x:** cross-algorithm allocation budget / tokenisation cache (GitHub issue #2).
- **v2 backlog** (existing): all V2-API-* and V2-ALGO-* requirements in `.planning/REQUIREMENTS.md`. None touched by Phase 8.5.

---

*Discussion log produced 2026-05-17.*
