---
phase: 08.5
plan: 11
type: execute
wave: 4
depends_on: [03, 08]
files_modified:
  - bench.txt.new
  - partial_ratio.go
  - props_test.go
  - scripts/cmd/verify-exported-coverage/main.go
  - scripts/verify-coverage-floors.sh
  - go.mod
  - go.sum
autonomous: true
requirements:
  - DET-05
  - TEST-03
  - TEST-06
  - PERF-04
  - Q11a
  - Q11d
  - Q12a
  - Q12b
threat_model_refs: [T-08.5-22]

must_haves:
  truths:
    - "bench.txt.new is deleted from the repo (Q11a cleanup)"
    - "partial_ratio.go:148 misleading variable name is fixed; the TODO(#TBD) comment is removed (Q11d)"
    - "testing/quick Generator implementations in props_test.go use a balanced mix of empty / short-ASCII / long-non-ASCII / all-same-char-runs / Unicode-rich strings (Q12b)"
    - "TestProp_Scorer_WeightSumOne uint16 overflow flake is fixed (u+1 overflow at u==65535)"
    - "scripts/verify-coverage-floors.sh uses AST-based exported-symbol detection via a Go helper (Q12a)"
    - "Coverage Floor 3 enforces >= 90% statement coverage per exported function (tightened from 'exists-at-least-one-test')"
  artifacts:
    - path: "scripts/cmd/verify-exported-coverage/main.go"
      provides: "NEW AST-walking Go helper that enumerates exported symbols and cross-references against go tool cover -func"
      contains: "go/parser, go/ast"
    - path: "scripts/verify-coverage-floors.sh"
      provides: "REWRITE to invoke the Go helper for Floor 3"
      contains: "verify-exported-coverage"
    - path: "partial_ratio.go"
      provides: "Renamed local variable at former line 148; TODO removed"
      contains: "partial_ratio.go"
    - path: "props_test.go"
      provides: "Mixed-shape testing/quick generators + uint16 overflow fix"
      contains: "mixedShapeStringGenerator, u + 1"
  key_links:
    - from: "scripts/verify-coverage-floors.sh"
      to: "scripts/cmd/verify-exported-coverage/main.go"
      via: "shell invocation of Go helper"
      pattern: "go run \\./scripts/cmd/verify-exported-coverage"
---

<objective>
Cluster 6 test-infrastructure bundle (small-grain items). Bundles:

- **Q11a** — delete `bench.txt.new` (857-byte stale captured property-test failure at root); investigate the underlying `TestProp_MongeElkanScore_AsymmetricWhenTokenCountAsymmetric` test if it re-fires.
- **Q11d** — `partial_ratio.go:148` misleading variable rename + remove the `TODO(#TBD)` comment (no GH issues yet per project memory).
- **Q12b** — mixed-shape `testing/quick` generators (empty, short ASCII, long non-ASCII, all-same-char runs, Unicode-rich) + fix the `TestProp_Scorer_WeightSumOne` uint16 overflow flake (`u+1` overflows to 0 when `u == 65535`).
- **Q12a** — AST-based exported-symbol detection for Coverage Floor 3; tighten Floor 3 from "exists-at-least-one-test" to ≥ 90% statement coverage per exported function.

The AST helper (Q12a) is also consumed by the `verify-llms-sync` target (Plan 14 / Q13). This plan ships the helper; Plan 14 invokes it for the llms-sync check too.

Output: one repo state with the four cleanups + the AST helper + the tightened Floor 3 script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@docs/requirements.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/go-testing-standards/SKILL.md

<interfaces>
<!-- Q11a/Q11d/Q12a/Q12b LOCKED in 08.5-CONTEXT.md -->

Q11a — `bench.txt.new` at repo root (857 bytes per RESEARCH.md §3.6 6.E). Stale.

Q11d — partial_ratio.go:148 — current "TODO without GitHub issue ref" misleading variable name. Per project memory `no_github_issues.md`: remove the (#TBD) placeholder; don't replace with a real issue ref.

Q12b — current `testing/quick` generators in props_test.go use default `rand.String`. Mixed-shape spec (CONTEXT.md):
- balanced mix of: empty strings, short ASCII (≤ 8 chars), long non-ASCII (≥ 64 chars), all-same-character runs, Unicode-rich strings
- TestProp_Scorer_WeightSumOne uint16 overflow: `u+1` where `u uint16 = 65535` produces 0; widen to `int(u) + 1` or use `(u % 100) + 1`.

Q12a — Current state: `scripts/verify-coverage-floors.sh` parses `go tool cover -func` and uses `go doc -short` for exported-symbol enumeration. Locked target: AST-based detection via a new Go helper at `scripts/cmd/verify-exported-coverage/main.go`. Helper reads:
- All `*.go` files except `*_test.go` and `tests/bdd/...`
- Top-level decls: `*ast.FuncDecl` (exported if `decl.Name.IsExported() && decl.Recv == nil`); `*ast.GenDecl` for `var`/`const`/`type` (each spec's name)
- Cross-reference against `go tool cover -func` output
- Reject when (a) any exported symbol has zero coverage rows OR (b) any exported func's percent < 90.0

Helper analog: `ai_friendly_test.go` already uses `go/ast` to enumerate exported symbols — extract `collectExportedSymbols` into the new standalone main.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Q11a + Q11d cleanups (delete bench.txt.new; rename partial_ratio.go variable)</name>
  <files>partial_ratio.go</files>
  <read_first>
    - `ls -la bench.txt.new` (verify file exists at root; size ~857 bytes)
    - partial_ratio.go lines 140-155 (locate the TODO + misleading variable)
    - .planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md §Q11a + §Q11d
  </read_first>
  <action>
1. **Q11a — delete bench.txt.new**:
   - `git rm bench.txt.new` (or `rm` then `git add -u`).
   - Audit `TestProp_MongeElkanScore_AsymmetricWhenTokenCountAsymmetric` (find via grep — typically in props_test.go or monge_elkan_test.go). If it currently fails when run as `go test -run TestProp_MongeElkan -race -count=1 ./...`, file the issue with a note in the SUMMARY (the project has no GH issues yet; track in PROJECT.md or KNOWN_ISSUES.md). If it passes, no further action.

2. **Q11d — partial_ratio.go:148 rename + TODO removal**:
   - Locate the variable at line 148 (or its current location — line numbers may have shifted; find by reading the file). The variable name is misleading (e.g. `last`, `prev`, `idx` — examine in context).
   - Rename to a clearer name based on semantics: e.g. `windowStart`, `bestMatchOffset`, `iterIdx` — pick the most descriptive.
   - Remove the `TODO(#TBD)` (or similar placeholder) comment entirely.
   - Update all in-file references to the renamed variable.
  </action>
  <verify>
    <automated>go build ./... && go test -run 'TestPartialRatio' -race -count=1 ./... && [ ! -f bench.txt.new ]</automated>
  </verify>
  <acceptance_criteria>
    - `ls bench.txt.new` returns "No such file or directory".
    - `grep -nE "TODO\\(#TBD\\)" partial_ratio.go | wc -l` returns 0 (TODO removed).
    - `go build ./...` exits 0.
    - `go test -run TestPartialRatio -race -count=1 ./...` exits 0 (variable rename did not break tests).
  </acceptance_criteria>
  <done>
    bench.txt.new gone; partial_ratio.go variable renamed clearly with TODO removed; tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Q12b mixed-shape testing/quick generators + uint16 overflow fix</name>
  <files>props_test.go</files>
  <read_first>
    - props_test.go in full (~220 functions per RESEARCH.md — large file; read in chunks if needed)
    - .claude/skills/go-testing-standards/SKILL.md (testing/quick generator discipline)
    - docs/requirements.md §15.3 (property-test generator spec)
  </read_first>
  <action>
1. **Add mixed-shape generators** at the top of props_test.go (or in a `prop_generators_test.go` if size warrants splitting):
   ```
   // mixedShapeString returns a quick.Generator that produces a balanced mix
   // of input shapes to exercise edge cases the default rand.String misses:
   // empty strings, short ASCII (<=8 chars), long non-ASCII (>=64 chars),
   // all-same-character runs, and Unicode-rich strings. Q12b lock.
   func mixedShapeStringGenerator() ... { ... }
   ```
   - Implement using a rand.Intn dispatch across 5 cases (empty / short ASCII / long Unicode / same-char run / Unicode-rich).
   - For Unicode-rich: include a mix of code points from Latin Extended, CJK, emoji, combining marks.

2. **Route existing TestProp_* through the new generator** where appropriate:
   - Identify property tests that currently use `quick.Value(reflect.TypeOf(""), rand)` or default string generation.
   - Update to use `mixedShapeStringGenerator()` via the `quick.Config{Values: ...}` mechanism.
   - Be selective: tests that specifically need ASCII (e.g. PropTokenise_ASCIIFastPathEquivalent from Plan 08) should continue to use ASCII-only generators.

3. **Q12b uint16 overflow fix** — `TestProp_Scorer_WeightSumOne`:
   - Locate `TestProp_Scorer_WeightSumOne`. Find the `u + 1` expression where `u uint16`.
   - Replace with `int(u) + 1` (widening to int prevents the wrap-around at `u == 65535`).
   - Add an inline comment citing Q12b.
  </action>
  <verify>
    <automated>go test -run 'TestProp_' -race -count=1 -timeout 120s ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "mixedShapeStringGenerator\\|mixedShape" props_test.go | wc -l` returns at least 1 (new generator present).
    - `grep -nE "int\\(u\\) ?\\+ ?1\\|int\\(u\\)\\+1" props_test.go | wc -l` returns at least 1 (uint16 fix).
    - `go test -run 'TestProp_Scorer_WeightSumOne' -race -count=10 ./...` exits 0 (10 consecutive runs green — flake fixed).
    - `go test -run 'TestProp_' -race -count=1 -timeout 120s ./...` exits 0 within timeout.
  </acceptance_criteria>
  <done>
    Mixed-shape generators present; uint16 overflow fixed; all property tests green over 10 consecutive runs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Q12a AST-based exported-coverage helper + rewrite Floor 3 script</name>
  <files>scripts/cmd/verify-exported-coverage/main.go, scripts/verify-coverage-floors.sh</files>
  <read_first>
    - ai_friendly_test.go (the AST-walking pattern — extract `collectExportedSymbols` into standalone main)
    - scripts/verify-coverage-floors.sh in full (current Floor 3 shell logic)
    - .planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md §Q12a
    - docs/requirements.md §15.8 (Floor 3 spec)
  </read_first>
  <action>
1. **Create `scripts/cmd/verify-exported-coverage/main.go`**:
   - Apache 2.0 license header.
   - Package `main`.
   - Use `go/parser` and `go/ast` to walk every `.go` file under the root package (excluding `*_test.go` and the `tests/bdd/...` subtree).
   - Enumerate every exported symbol: `*ast.FuncDecl` where `decl.Name.IsExported() && decl.Recv == nil` (top-level functions); `*ast.GenDecl` for `var`/`const`/`type` where each spec's name is exported.
   - Take coverage profile path as CLI arg (`os.Args[1]`).
   - Parse the coverage profile (or invoke `go tool cover -func=<profile>` and parse the output): each row is `<file>:<line>:\t<func>\t<percent>%`.
   - For each exported symbol:
     - If the symbol is a function: find the coverage row matching `<file>:<line>:\t<symbol-name>` and assert `percent >= 90.0`.
     - If the symbol is a var/const/type: no coverage row is expected; verify (via grep) that at least one test file references it (lighter floor — exists-at-least-one-mention).
   - Exit code 0 on success; non-zero on any uncovered exported symbol or any function below 90%.
   - Use stdlib only (no testify, no external deps).
2. **Rewrite `scripts/verify-coverage-floors.sh`**:
   - The current shell logic for Floor 3 (probably `go doc -short` parsing) is replaced with a single invocation: `go run ./scripts/cmd/verify-exported-coverage coverage.out`.
   - Floors 1 and 2 logic preserved if present.
   - Exit code propagation.
3. **Coverage profile prerequisite**: the script may need `go test -coverprofile=coverage.out ./...` to run first. Document or invoke as needed.
4. Run `make coverage && bash scripts/verify-coverage-floors.sh` locally and confirm green (or document which exported symbols currently fail the new tighter floor — those become candidates for Plan 13's improvement sweep).
  </action>
  <verify>
    <automated>go build ./scripts/cmd/verify-exported-coverage && go test -coverprofile=coverage.out ./... && bash scripts/verify-coverage-floors.sh coverage.out</automated>
  </verify>
  <acceptance_criteria>
    - `ls scripts/cmd/verify-exported-coverage/main.go` returns the file.
    - `go build ./scripts/cmd/verify-exported-coverage` exits 0.
    - `grep -n "verify-exported-coverage" scripts/verify-coverage-floors.sh | wc -l` returns at least 1.
    - `bash scripts/verify-coverage-floors.sh coverage.out` exits 0 OR exits non-zero with a clear enumeration of which exported symbols miss the 90% floor (the failure mode is part of the locked Q12a behaviour — failure is acceptable in this plan's verify so long as the helper itself works correctly; the Improvement Sweep plan addresses the residue).
    - `go vet ./scripts/cmd/verify-exported-coverage` exits 0.
  </acceptance_criteria>
  <done>
    AST-based exported-coverage helper exists and runs; the shell script invokes it for Floor 3; the new tighter floor either passes OR surfaces a known residue that Plan 13 addresses.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coverage-profile input → AST helper | The helper trusts the coverage profile format; malformed input could cause panics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-22 | D — DoS via malformed coverage profile | scripts/cmd/verify-exported-coverage | mitigate | Helper validates the profile format and exits non-zero on parse error rather than panicking. CI guarantees a well-formed profile from `go test -coverprofile`. |
| T-08.5-23 | T — Tampering via Floor 3 false-green | AST-vs-coverage cross-reference | mitigate | The AST walk is deterministic; coverage profile is from `go test -coverprofile`. Both inputs are reproducible; CI re-runs on every PR. |
</threat_model>

<verification>
- Compile-green for the new Go helper.
- Property tests green over 10 consecutive runs (uint16 flake fixed).
- Floor 3 script invokes the helper; either passes OR produces a clear residue report.
- `bench.txt.new` deleted.
- `partial_ratio.go` TODO removed.
</verification>

<success_criteria>
- Q11a + Q11d + Q12a + Q12b locks satisfied.
- Phase 8.5 success criterion 8 (CI workflow split + verify-llms-sync + bench-diff) preconditioned by the AST helper.
- The go-quality Critical (×2) and test-analyst Critical (×3) findings for Floor 3 + property-test flake close.
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-11-SUMMARY.md` documenting:
- whether the underlying `TestProp_MongeElkanScore_AsymmetricWhenTokenCountAsymmetric` re-fires after Q11a cleanup;
- the renamed variable at the former partial_ratio.go:148;
- the residue list of exported symbols currently failing the tightened Floor 3 (if any) — this is the input to Plan 13's improvement sweep;
- agent reviews invoked (test-analyst, determinism-reviewer, go-quality).
</output>
