---
phase: 08.5
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - monge_elkan.go
  - dispatch_monge_elkan.go
  - monge_elkan_test.go
  - monge_elkan_bench_test.go
  - monge_elkan_fuzz_test.go
  - cross_algorithm_consistency_test.go
  - example_test.go
  - algoid_test.go
  - algoid.go
autonomous: true
requirements:
  - TOKEN-01
  - Q3
threat_model_refs: [T-08.5-04]

must_haves:
  truths:
    - "MongeElkanScore (the unsuffixed function) is the symmetric default"
    - "MongeElkanScoreAsymmetric is the directional variant"
    - "Neither variant accepts a NormalisationOptions parameter (inert opts removed)"
    - "Every call site across tests/benchmarks/examples/dispatch/cross-algorithm consistency uses the new naming"
    - "AlgoID display name in algoid.go is updated if it referenced the old asymmetric default"
  artifacts:
    - path: "monge_elkan.go"
      provides: "Public symmetric MongeElkanScore + asymmetric MongeElkanScoreAsymmetric (no opts param)"
      contains: "func MongeElkanScore(a, b string, inner AlgoID)"
    - path: "dispatch_monge_elkan.go"
      provides: "Dispatch entry binds the new MongeElkanScore (symmetric default) to AlgoMongeElkan"
      contains: "MongeElkanScore"
  key_links:
    - from: "cross_algorithm_consistency_test.go (Phase 6 consistency tests)"
      to: "MongeElkanScore symmetric semantics"
      via: "test assertion update"
      pattern: "MongeElkanScore\\("
    - from: "example_test.go ExampleMongeElkanScore + ExampleMongeElkanScoreAsymmetric"
      to: "post-rename surface"
      via: "godoc example output pin"
      pattern: "ExampleMongeElkan"
---

<objective>
Q3 — execute the MongeElkan symmetric-by-default rename + remove the inert `NormalisationOptions` parameter from both variants. This is the single largest atomic-rename surface in Phase 8.5: ~22 call sites across 7 files. The rename is breaking pre-v1.0 and is recorded in `CHANGELOG.md "Breaking"`.

Critical semantic discipline: the current `MongeElkanScore` means *asymmetric*; after this plan, the same name means *symmetric*. Reviewers must verify the semantic flip, not just the textual rename. The cross_algorithm_consistency tests and example_test.go are the highest-risk verification points.

Output: a single atomic commit with all call sites updated in lockstep; tests, benchmarks, examples, dispatch, and consistency tests all reference the post-rename surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@docs/requirements.md
@CHANGELOG.md
@.claude/skills/algorithm-correctness-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
<!-- Q3 LOCKED in 08.5-CONTEXT.md — semantic flip + opts removal -->

BEFORE (current — Phase 8 surface):
- `func MongeElkanScore(a, b string, inner AlgoID, opts NormalisationOptions) float64`  // ASYMMETRIC, inert opts
- `func MongeElkanScoreSymmetric(a, b string, inner AlgoID, opts NormalisationOptions) float64`  // symmetric, inert opts

AFTER (Phase 8.5 target):
- `func MongeElkanScore(a, b string, inner AlgoID) float64`  // SYMMETRIC (default), no opts
- `func MongeElkanScoreAsymmetric(a, b string, inner AlgoID) float64`  // ASYMMETRIC, no opts

Touched files & call counts (per RESEARCH.md §3.2):
- monge_elkan.go:377 (asymmetric def), 467 (symmetric def)
- monge_elkan_test.go (numerous call sites — discover via grep)
- monge_elkan_bench_test.go (6 call sites)
- monge_elkan_fuzz_test.go (fuzz harness call sites)
- cross_algorithm_consistency_test.go:737-766 (4 calls — semantic flip review point)
- example_test.go:434-495 (ExampleMongeElkanScore + ExampleMongeElkanScoreSymmetric — both need updating to new names + Output line refresh)
- algoid_test.go:444 (AlgoID display name assertion)
- dispatch_monge_elkan.go:24-37, 70 (godoc + binding)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MongeElkan rename + opts removal — atomic commit</name>
  <files>monge_elkan.go, dispatch_monge_elkan.go, monge_elkan_test.go, monge_elkan_bench_test.go, monge_elkan_fuzz_test.go, cross_algorithm_consistency_test.go, example_test.go, algoid_test.go, algoid.go</files>
  <read_first>
    - monge_elkan.go in full (establishes current signatures + the symmetric/asymmetric algorithm bodies)
    - dispatch_monge_elkan.go in full (the dispatch binding tells the executor which variant Monge-Elkan dispatches against — this is what changes from asymmetric to symmetric default)
    - cross_algorithm_consistency_test.go:730-770 (the semantic-flip-sensitive consistency tests)
    - example_test.go:430-500 (the runnable examples whose Output line must match the post-rename behaviour)
    - algoid.go and algoid_test.go (to verify AlgoMongeElkan's display name and dispatch binding are consistent post-rename)
    - .planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md Q3 lock
    - docs/requirements.md §7.15 (post-rename canonical surface)
    - CHANGELOG.md `[Unreleased] > Breaking` (current entry that records Q3)
  </read_first>
  <action>
Execute the rename + opts removal as a single atomic commit. Order operations to maintain compile-green throughout the diff if possible (Go's package-scope means you must change everything at once):

1. **In monge_elkan.go** —
   - Rename the EXISTING `MongeElkanScoreSymmetric` (currently at line 467) to `MongeElkanScore` and REMOVE its `opts NormalisationOptions` parameter from the signature. The body keeps the symmetric-by-default algorithm (the existing symmetric implementation).
   - Rename the EXISTING `MongeElkanScore` (currently asymmetric at line 377) to `MongeElkanScoreAsymmetric` and REMOVE its `opts NormalisationOptions` parameter. The body keeps the asymmetric algorithm (the existing asymmetric implementation).
   - Update godoc on BOTH functions to reflect the new naming. The post-rename `MongeElkanScore` godoc must clearly state "symmetric by default — the canonical Monge-Elkan composite via max(forward, backward) for invariant under argument-swap"; the `MongeElkanScoreAsymmetric` godoc must clearly state "directional variant; not symmetric; useful when (a, b) and (b, a) should differ".
   - Remove all references to `opts NormalisationOptions` from internal helper signatures that propagated it (verify by grep across the file).
   - Add British-English prose throughout new godoc.

2. **In dispatch_monge_elkan.go** —
   - At lines 24-37 (godoc) and line 70 (binding): update the binding to reference the post-rename `MongeElkanScore` (now symmetric). Update the godoc to describe the symmetric-by-default behaviour.
   - The `permittedMongeElkanInner` allow-list (introduced in Phase 6) remains unchanged.

3. **In monge_elkan_test.go** —
   - Update every `MongeElkanScore(` call to drop the `opts` argument.
   - Update every `MongeElkanScoreSymmetric(` call to `MongeElkanScore(` and drop the `opts` argument.
   - Carefully audit tests that previously asserted asymmetric behaviour against `MongeElkanScore` — those tests now need to call `MongeElkanScoreAsymmetric`. The Phase 8 review's Phase 6 test-writer Critical findings on Monge-Elkan asymmetry should make this audit explicit.

4. **In monge_elkan_bench_test.go** — same call-site updates × 6 sites.

5. **In monge_elkan_fuzz_test.go** — same call-site updates.

6. **In cross_algorithm_consistency_test.go** —
   - Lines 737-766: this is the highest-risk semantic-flip surface. The existing test calls `MongeElkanScore` and likely expects asymmetric output. Audit each call and change to `MongeElkanScoreAsymmetric` where asymmetric semantics are required, OR update expected values where the rename now exposes symmetric semantics by design.
   - Verify the post-rename test still asserts a meaningful invariant.

7. **In example_test.go** —
   - Lines 434-495: the two `ExampleMongeElkan*` functions. Rename `ExampleMongeElkanScore` to `ExampleMongeElkanScoreAsymmetric` (since the OLD name produced asymmetric output). Rename `ExampleMongeElkanScoreSymmetric` to `ExampleMongeElkanScore` (the new symmetric default).
   - Update each example's `// Output:` line to match the renamed function's actual output (the symmetric and asymmetric scores ARE different numerically — the Output lines for the renamed examples MUST be regenerated by running `go test -run ExampleMongeElkan` and capturing the actual output, NOT copied from the old example).

8. **In algoid_test.go** — verify the assertion at line 444 (`AlgoMongeElkan` display name = `"MongeElkan"` per Q6b CamelCase). No change to the display name unless it was previously `MongeElkanScore` or similar.

9. **In algoid.go** — verify the `AlgoMongeElkan.String()` returns `"MongeElkan"` per Q6b CamelCase (Phase 8 surface — should already be correct). No code change unless inconsistency found.

Commit message follows the breaking-change marker: `feat!: MongeElkan symmetric-by-default + opts removal (Q3, breaking pre-v1.0)`.

**Discipline:** the executor MUST run `go test ./...` at the end of the diff — if any test fails because the asymmetric-vs-symmetric distinction was missed in audit, fix BEFORE committing. The CHANGELOG entry must accurately reflect the breaking nature of the rename.

Per D-XX traceability: this plan implements TOKEN-01 (post-Phase 6 surface evolution) and Q3 (locked breaking-change).
  </action>
  <verify>
    <automated>go build ./... && go test -run 'TestMongeElkan|ExampleMongeElkan|TestCrossAlgorithmConsistency' -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "MongeElkanScoreSymmetric" $(git ls-files '*.go') | wc -l` returns 0 (the old symmetric name is gone).
    - `grep -E "func MongeElkanScore\\(" monge_elkan.go | wc -l` returns 1 (the new symmetric default).
    - `grep -E "func MongeElkanScoreAsymmetric\\(" monge_elkan.go | wc -l` returns 1 (the directional variant).
    - `grep -E "MongeElkanScore.*opts NormalisationOptions" $(git ls-files '*.go') | wc -l` returns 0 (opts removed everywhere).
    - `go build ./...` exits 0.
    - `go test -run 'ExampleMongeElkan' -race -count=1 ./...` exits 0 (examples' Output lines match runtime output).
    - `go test -run 'TestCrossAlgorithmConsistency' -race -count=1 ./...` exits 0 (semantic flip audited; consistency assertions hold post-rename).
    - `go vet ./...` exits 0.
    - CHANGELOG.md `[Unreleased]` Breaking section references Q3 with the post-rename surface.
    - **British English on Monge-Elkan godoc and examples** (M7 revision 2026-05-17): `grep -nE "normalize|behavior|color|serialize|tokenize|analyze|organize|center|favorite" monge_elkan.go example_test.go` returns 0. The Q3 rename regenerates Output lines and rewrites the godoc on both `MongeElkanScore` (post-rename symmetric) and `MongeElkanScoreAsymmetric`; this acceptance criterion catches American spellings that may have persisted from the pre-rename prose.
  </acceptance_criteria>
  <done>
    The entire root package compiles; every Monge-Elkan call site uses the post-rename API without `opts`; both examples (`ExampleMongeElkanScore` symmetric default + `ExampleMongeElkanScoreAsymmetric` directional) have `// Output:` lines that match the actual runtime output; cross-algorithm consistency tests still hold the documented semantic invariants under the new naming.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → MongeElkan public functions | Post-rename surface — consumers may pass arbitrary `inner AlgoID` (D — DoS via inner-loop pathological inputs); permittedMongeElkanInner allow-list (Phase 6) prevents this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-04 | T — Tampering (semantic confusion) | MongeElkanScore name flip | mitigate | The rename atomic-commits the directional flip across 22+ call sites. Cross-algorithm consistency tests at `cross_algorithm_consistency_test.go:737-766` lock the semantic invariants (symmetry, range bounds). Reviewers (`api-ergonomics-reviewer`, `algorithm-correctness-reviewer`) verify the semantic implication beyond the textual rename. |
| T-08.5-05 | D — DoS via permitted-inner-algorithm bypass | dispatch_monge_elkan.go binding | accept | Phase 6 already shipped `permittedMongeElkanInner` allow-list; the rename does not affect that surface. |
</threat_model>

<verification>
- Compile-green: `go build ./...` exits 0.
- Test-green: `go test -run 'TestMongeElkan|ExampleMongeElkan|TestCrossAlgorithmConsistency' -race -count=1 ./...` exits 0.
- Examples runtime-match: `go test -run ExampleMongeElkan -race ./...` validates the `// Output:` lines.
- Static check: `grep -E "MongeElkanScoreSymmetric" $(git ls-files '*.go')` returns no matches.
- Static check: `grep -E "opts NormalisationOptions" monge_elkan.go monge_elkan_test.go` returns no matches (opts gone).
</verification>

<success_criteria>
- Q3 lock (CONTEXT.md): MongeElkan symmetric-by-default rename + inert opts removal complete.
- Phase 8.5 success criterion 2 (breaking-change items land cleanly with passing tests): MongeElkan portion done.
- CHANGELOG.md Breaking entry remains accurate post-rename.
- No regression to Phase 6 TOKEN-01 requirement (permittedMongeElkanInner allow-list still enforced).
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-02-SUMMARY.md` documenting:
- which files touched, with grep counts before/after the rename;
- the semantic-flip audit notes for cross_algorithm_consistency_test.go (which test calls became asymmetric explicit);
- the regenerated Output lines for the two `ExampleMongeElkan*` functions;
- agent reviews invoked (api-ergonomics-reviewer mandatory; algorithm-correctness-reviewer; code-reviewer; commit-message-reviewer).
</output>
