---
phase: 08.5
plan: 15a
type: execute
wave: 5
depends_on: [01, 02, 03]
files_modified:
  - golden_canonical.go
  - export_test.go
  - dispatch_cosine.go
  - dispatch_levenshtein.go
  - dispatch_jaro.go
  - dispatch_jarowinkler.go
  - dispatch_hamming.go
  - dispatch_damerau_full.go
  - dispatch_damerau_osa.go
  - dispatch_double_metaphone.go
  - dispatch_lcsstr.go
  - dispatch_monge_elkan.go
  - dispatch_mra.go
  - dispatch_nysiis.go
  - dispatch_partial_ratio.go
  - dispatch_q_gram_jaccard.go
  - dispatch_q_gram_dice.go
  - dispatch_q_gram_cosine.go
  - dispatch_q_gram_tversky.go
  - dispatch_ratcliff_obershelp.go
  - dispatch_smith_waterman_gotoh.go
  - dispatch_soundex.go
  - dispatch_strcmp95.go
  - dispatch_token_jaccard.go
  - dispatch_token_set_ratio.go
  - dispatch_token_sort_ratio.go
  - scorer_options_test.go
  - scorer_options_internal_test.go
autonomous: true
requirements:
  - Gap1
  - Q14b
threat_model_refs: [T-08.5-28, T-08.5-29]

must_haves:
  truths:
    - "WriteGoldenFile is unexported (renamed to writeGoldenFile in golden_canonical.go); test-only re-export in export_test.go preserves test visibility"
    - "Every dispatch_*.go file replaces 'var _ = func() bool {...}()' with explicit init() (option A — least disruptive)"
    - "scorer_options_test.go is renamed to scorer_options_internal_test.go (Gap 1 mechanical rename)"
  artifacts:
    - path: "golden_canonical.go"
      provides: "Unexported writeGoldenFile + test-only re-export pattern"
      contains: "func writeGoldenFile"
    - path: "export_test.go"
      provides: "Test-only re-export var WriteGoldenFile = writeGoldenFile"
      contains: "WriteGoldenFile = writeGoldenFile"
    - path: "scorer_options_internal_test.go"
      provides: "Renamed file (was scorer_options_test.go)"
      contains: "package fuzzymatch"
  key_links:
    - from: "dispatch_*.go × 23"
      to: "explicit init() per file"
      via: "package-init dispatch table writes"
      pattern: "^func init\\("
---

<objective>
**Plan 15a (the mechanical half of the split former-Plan-15, M5 revision 2026-05-17)** — mechanical refactors that move the per-file count under the BLOCKER threshold:

- **Q14b**: `WriteGoldenFile` unexport + test-only re-export pattern
- **Q14b**: `var _ = func() bool {...}()` refactor across 23 dispatch files (option A — explicit `func init()`)
- **Gap 1**: `scorer_options_test.go` → `scorer_options_internal_test.go` mechanical file rename

Total: ~25 files, all mechanical edits with identical patterns. No behavioural changes. The non-mechanical work (Gap 2 BDD scenarios, Gap 5 companion property test, Gap 7 outcomes, 30+ Code-fix lint sweep) lives in Plan 15b.

**Hard dependencies:**
- Plan 01 (Gap 4 rename) — code-fix items downstream reference the renamed `ErrInvalidAlgoID`. This plan does not touch those references directly, but compiles against Plan 01 baseline.
- Plan 02 (Q3 MongeElkan rename) — `dispatch_monge_elkan.go` references the post-rename binding.
- Plan 03 (Q5 PartialRatioScoreRunes removal) — `dispatch_partial_ratio.go` no longer carries the rune-variant entry; its init() refactor reflects the post-removal state.

Output: a large but uniformly mechanical commit set; lint surface preserved (Plan 15b cleans residue).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@REVIEW-FINDINGS.md
@docs/requirements.md
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
<!-- Q14b mechanical refactors + Gap 1 file rename — see CONTEXT.md §Q14b and §Gap 1 -->

WriteGoldenFile rename:
- Current: golden_canonical.go:88 declares WriteGoldenFile (exported); 5 call sites (algorithms_golden_test.go:92, golden_canonical_test.go:240/263/284, golden_test.go:74) — ALL in _test.go files; 0 production call sites.
- Target: rename to writeGoldenFile (unexported); add export_test.go entry `var WriteGoldenFile = writeGoldenFile` for test-only visibility.

var _ = func() bool {...}() refactor:
- Pattern site: dispatch_*.go × 23 files, each carries a package-level `var _ = func() bool { ... return true }()` that registers the algorithm's score function into the dispatch[] array.
- Q14b option A (recommended in PATTERNS.md §2.O): replace with explicit `func init() { ... }` per file.
- Caveat: the determinism-reviewer's "no init-time side effects" rule applies to ANY init-time work that touches IO, time, goroutines, or non-deterministic order. A pure-write-into-a-table is acceptable as long as init() functions across files write into NON-OVERLAPPING slots (Go's init() ordering across files within a package is alphabetical by filename — so dispatch[AlgoCosine] is set by dispatch_cosine.go's init() while dispatch[AlgoLevenshtein] is set by dispatch_levenshtein.go's init() — independent slots, no ordering dependency).

Gap 1 rename: scorer_options_test.go → scorer_options_internal_test.go (mechanical git mv + commit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WriteGoldenFile unexport + 23 dispatch init() refactor</name>
  <files>golden_canonical.go, export_test.go, dispatch_cosine.go, dispatch_levenshtein.go, dispatch_jaro.go, dispatch_jarowinkler.go, dispatch_hamming.go, dispatch_damerau_full.go, dispatch_damerau_osa.go, dispatch_double_metaphone.go, dispatch_lcsstr.go, dispatch_monge_elkan.go, dispatch_mra.go, dispatch_nysiis.go, dispatch_partial_ratio.go, dispatch_q_gram_jaccard.go, dispatch_q_gram_dice.go, dispatch_q_gram_cosine.go, dispatch_q_gram_tversky.go, dispatch_ratcliff_obershelp.go, dispatch_smith_waterman_gotoh.go, dispatch_soundex.go, dispatch_strcmp95.go, dispatch_token_jaccard.go, dispatch_token_set_ratio.go, dispatch_token_sort_ratio.go</files>
  <read_first>
    - golden_canonical.go:88 (WriteGoldenFile declaration)
    - export_test.go in full (test-only export pattern)
    - One representative dispatch_*.go file (e.g. dispatch_cosine.go) to confirm the `var _ = func() bool {...}()` shape
    - .planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md §2.O
    - .claude/skills/determinism-standards/SKILL.md (init-time side effects rule)
  </read_first>
  <action>
1. **WriteGoldenFile unexport**:
   - Rename `WriteGoldenFile` → `writeGoldenFile` in `golden_canonical.go`.
   - In `export_test.go`, add: `var WriteGoldenFile = writeGoldenFile` (test-only re-export at package scope so the 5 `_test.go` call sites continue to work without modification).
   - Verify all 5 call sites still compile.

2. **var _ = func() bool {...}() refactor across 23 dispatch files**:
   - In each `dispatch_*.go`, replace the `var _ = func() bool { ... return true }()` block with an explicit `func init() { ... }` containing the same body (minus the `return true` line).
   - The function body that writes into the dispatch table is unchanged — only the wrapper switches from `var _ = func() bool` to `init`.
   - Verify the dispatch table is correctly populated by running `go test -run 'TestAlgoID|TestDispatch' -race -count=1 ./...` and confirming all algorithms still dispatch correctly.
   - This is a mechanical refactor: 23 files × ~10 lines each = ~230 lines of identical mechanical edits.
   - Add a one-line comment at each init explaining the choice: `// init registers the <algo> dispatch entry. Q14b option A — explicit init replaces the var _ = func() bool {...}() pattern per the determinism-standards SKILL (pure-write into a pre-allocated slot; no IO, no time, no goroutines, no ordering dependency on other init functions).`
   - British English in the comment.
  </action>
  <verify>
    <automated>go build ./... && go test -run 'TestAlgoID|TestDispatch' -race -count=1 ./... && grep -cE "func init\\(\\)" dispatch_*.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^func WriteGoldenFile" golden_canonical.go | wc -l` returns 0 (unexported).
    - `grep -nE "^func writeGoldenFile" golden_canonical.go | wc -l` returns 1.
    - `grep -nE "var WriteGoldenFile = writeGoldenFile" export_test.go | wc -l` returns 1.
    - `grep -cE "^var _ = func\\(\\) bool" dispatch_*.go | grep -v ':0$' | wc -l` returns 0 (no remaining instances).
    - `grep -cE "^func init\\(\\)" dispatch_*.go | grep -vE ':0$' | wc -l` returns 23 (one init per dispatch file).
    - `go build ./...` exits 0.
    - `go test -run 'TestAlgoID|TestDispatch' -race -count=1 ./...` exits 0.
    - `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>
    WriteGoldenFile is unexported with test-only re-export; 23 dispatch files use explicit init() per Q14b option A.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gap 1 — scorer_options_test.go → scorer_options_internal_test.go file rename</name>
  <files>scorer_options_test.go, scorer_options_internal_test.go</files>
  <read_first>
    - scorer_options_test.go (the file being renamed — verify it is `package fuzzymatch` and not `package fuzzymatch_test`, so the `_internal_` convention applies)
  </read_first>
  <action>
1. `git mv scorer_options_test.go scorer_options_internal_test.go`
2. Verify the file compiles: `go test -run TestScorerOptions -race -count=1 ./...` exits 0.
3. No content changes.
  </action>
  <verify>
    <automated>go test -run TestScorerOptions -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - `ls scorer_options_internal_test.go` returns the file.
    - `ls scorer_options_test.go` returns "No such file or directory" (the old name is gone).
    - `go test -run TestScorerOptions -race -count=1 ./...` exits 0 (no semantic change).
  </acceptance_criteria>
  <done>
    Gap 1 mechanical rename complete; the file now follows the `_internal_test.go` naming convention for internal-package tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| init() across 23 dispatch files | Init-time work — must remain pure (no IO, time, goroutines). |
| WriteGoldenFile test-only export | Test files in the same package can still write golden files; production consumers cannot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-28 | T — Tampering via init-order dependency | dispatch_*.go init() | mitigate | Each init() writes into a unique slot of the pre-allocated dispatch table — independent of init order. Verified by `go test -run TestAlgoID -race -count=10` (no flake under shuffle). |
| T-08.5-29 | I — Information disclosure (test-only writeGoldenFile surface) | export_test.go | accept | The test-only re-export pattern is the standard Go idiom; only test files in the same package see it. External consumers cannot import the test-only symbol. |
</threat_model>

<verification>
- Compile-green: `go build ./...` exits 0.
- Dispatch tests green: `go test -run 'TestAlgoID|TestDispatch' -race -count=1 ./...` exits 0.
- Filename rename: `ls scorer_options_internal_test.go` exists; old file gone.
- Mechanical count: 23 `func init()` declarations across `dispatch_*.go`.
</verification>

<success_criteria>
- Q14b mechanical refactors landed (WriteGoldenFile unexport + 23 dispatch init refactor).
- Gap 1 mechanical rename complete.
- Phase 8.5 Improvement-sweep mechanical portion advanced (residue handled by Plan 15b).
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-15a-SUMMARY.md` documenting:
- the WriteGoldenFile unexport diff;
- the 23 dispatch_*.go init() refactor count + any deviations from the option A pattern;
- the Gap 1 rename confirmation;
- agent reviews invoked (code-reviewer, determinism-reviewer, go-quality).
</output>
