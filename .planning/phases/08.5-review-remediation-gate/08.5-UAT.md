---
status: testing
phase: 08.5-review-remediation-gate
source: [08.5-01-SUMMARY.md through 08.5-18-SUMMARY.md, 08.5-SUMMARY.md, 08.5-VERIFICATION.md]
started: 2026-05-18T09:41:17Z
updated: 2026-05-18T09:56:00Z
---

## Current Test

number: 4
name: CHANGELOG accuracy + algorithms.md usability + best-practices.md quality + README reflects current surface
expected: |
  Programmatic verification confirms all four documents exist with the expected structure (see Tests 4-7 below). The remaining open question is human judgement on whether the content is actually useful to a consumer. Spot-check items below; reply with "pass" if they look right or describe what's wrong.
awaiting: user response (combined doc-quality verdict)

## Tests

### 1. Validate API — quick smoke test
expected: |
  In a Go REPL or scratch file, calling `fuzzymatch.Validate("hello", "world")` returns a `[]fuzzymatch.Warning` (likely empty for two normal short strings). Calling `fuzzymatch.Validate("", "world")` returns a slice with one Warning whose `Kind == fuzzymatch.WarnEmptyInput`. The slice element type and the WarnKind enum (5 constants) are documented in the godoc and accessible without unexported imports.
result: pass
evidence: |
  Validate("hello", "world") → 0 warnings
  Validate("", "world") → 7 warnings; first Kind == WarnEmptyInput (verified Kind == fuzzymatch.WarnEmptyInput is true)
  Validate("smithy is here", "smith") → 1 warning (UnequalLength on Hamming, correct — different lengths)
  Validate("中文", "") → 12 warnings spanning EmptyInput, UnequalLength, NoTokensAfterNormalise, AllNonASCIIDropped
  WarnKind enum exported with all 5 constants: EmptyInput, UnequalLength, NoTokensAfterNormalise, AllNonASCIIDropped, PathologicallyLargeInput
  WarnKind.String() returns CamelCase per Q6b spec lock

### 2. Runnable Validate example
expected: |
  `cd examples/validate-input-quality && go run .` (or `go test ./...`) prints a small demo showing Validate's behaviour on 2-3 input pairs. The example compiles, runs without errors, and the output matches the meta-test pinning the stdout byte-for-byte.
result: pass
evidence: |
  `go test ./...` → ok 0.341s
  `go run .` → prints 4 pair demonstrations (user_id/'', abc/abcd, 中文/日本語, user_id/userId) with per-pair Warning list + Scorer score
  Meta-test (stdout pinning) passes

### 3. Breaking-rename build break is correct
expected: |
  Old code that called `MongeElkanScore(a, b, opts)` (the pre-Q3 3-argument form with NormalisationOptions), or `PartialRatioScoreRunes(a, b)`, or referenced `errors.Is(err, fuzzymatch.ErrInvalidAlgorithm)`, fails to compile with a clear error pointing at the removed/renamed symbol.
result: pass
evidence: |
  Compiler errors on a test program using all three pre-8.5 APIs:
    "cannot use fuzzymatch.NormalisationOptions{} as fuzzymatch.AlgoID value in argument to fuzzymatch.MongeElkanScore"
    "undefined: fuzzymatch.PartialRatioScoreRunes"
    "undefined: fuzzymatch.ErrInvalidAlgorithm"
  All three messages identify the renamed/removed symbol unambiguously — a consumer can migrate from the error alone.

### 4. CHANGELOG accurately describes breaking changes
expected: |
  `CHANGELOG.md` `[Unreleased] / Breaking` section explicitly lists: MongeElkan rename + opts removal (Q3), PartialRatioScoreRunes removal (Q5), ErrInvalidAlgorithm → ErrInvalidAlgoID rename (Gap 4), removed sentinels.
result: pass (programmatic — content quality is for human review)
evidence: |
  [Unreleased] Breaking section lists 4 entries: MongeElkanScore symmetric default + NormalisationOptions removal (Q3); PartialRatioScoreRunes removal (Q5); 3 unused sentinels removed (Q4); HammingDistance return-type change (Q1).
  [Unreleased] Changed section lists 8 entries covering WithThreshold/WithAlgorithm/WithTverskyAlgorithm guards, sentinel godoc upgrade, AlgoID.String CamelCase doc, MongeElkan typed-panic discipline, and the ErrInvalidAlgorithm → ErrInvalidAlgoID rename (Gap 4).
  Each entry includes the originating Q-decision or Gap reference.

### 5. docs/algorithms.md is usable as algorithm reference
expected: |
  Opening `docs/algorithms.md` shows a per-algorithm section for each of the 23 algorithms with name, complexity, intended-use guidance, citation to primary source, link to godoc, and an example.
result: pass (programmatic — content quality is for human review)
evidence: |
  1587 lines (was 239-line TBD scaffold pre-Phase 8.5)
  27 H2 sections (Table of Contents + 23 algorithm sections + Validate input-validation section + Panic surface section + Cross-references)
  24 references to Validate (cross-link from each algorithm)
  Algorithm sections present: Levenshtein, Damerau-Levenshtein OSA, Damerau-Levenshtein Full, Hamming, Jaro, Jaro-Winkler, Strcmp95, Smith-Waterman-Gotoh, LCSStr, Q-Gram Jaccard, Sørensen-Dice, ...

### 6. docs/best-practices.md addresses real consumer questions
expected: |
  Covers Validate-then-Score idiom, Scorer-pinning, concurrency, error handling, and three-layer architecture.
result: pass (programmatic — content quality is for human review)
evidence: |
  216 lines, 5 H2 sections:
    ## Input validation with Validate
    ## Pinning a calibrated configuration
    ## Concurrency
    ## Error handling
    ## Choosing between layers

### 7. README reflects the post-Phase-8.5 surface
expected: |
  Quick Start uses current MongeElkan signature, references Validate, algorithm-catalogue table reflects current 23-algorithm set, pre-release status indicator present.
result: pass (programmatic — content quality is for human review)
evidence: |
  Pre-release badge present at the top
  5 Validate references (lines 59, 124, 126, 142, 152) — prominently features Validate-then-Score pattern
  Monge-Elkan listed in algorithm-catalogue table at line 198 with link to docs/algorithms.md#monge-elkan
  0 references to removed PartialRatioScoreRunes
  0 references to renamed-old-name ErrInvalidAlgorithm

### 8. Per-algorithm godoc cross-references Validate
expected: |
  Each algorithm function's godoc points at [fuzzymatch.Validate] for "programmatic input-quality checks before scoring". 22+ files.
result: pass
evidence: |
  23 algorithm .go files carry [fuzzymatch.Validate] godoc cross-ref:
    cosine, damerau_full, damerau_osa, double_metaphone, hamming, jaro, lcsstr, jarowinkler, levenshtein, monge_elkan, mra, nysiis, partial_ratio, qgram_jaccard, ratcliff_obershelp, sorensen_dice, soundex, strcmp95, swg, token_jaccard, token_set_ratio, token_sort_ratio, tversky

### 9. WithThreshold / WithAlgorithm / WithTverskyAlgorithm parameter guards
expected: |
  NaN/Inf/(α+β≤0) on Scorer construction returns the typed sentinel via errors.Is.
result: pass
evidence: |
  WithThreshold(NaN) → wrapped ErrInvalidThreshold; errors.Is = true
  WithAlgorithm(_, Inf) → wrapped ErrInvalidWeight; errors.Is = true
  WithTversky(α=0,β=0) → wrapped ErrInvalidTverskyParam; errors.Is = true
  WithTversky(α=NaN) → wrapped ErrInvalidTverskyParam; errors.Is = true
  WithTversky(β=-Inf) → wrapped ErrInvalidTverskyParam; errors.Is = true

### 10. CI on origin/main is green
expected: |
  All workflows green on the most recent commit; CI specifically green across the cross-platform matrix.
result: pass
evidence: |
  HEAD = 4ad747c
  CI: success | License Headers: success | Security: success | CodeQL: success | Code Quality: success
  Prior commit c7bb1ff also fully green (CI, License Headers, Code Quality, Code Quality: Scheduled)
  Nightly workflow on c7bb1ff queued (scheduled, non-blocking)

## Summary

total: 10
passed: 6 (programmatically confirmed)
passed_pending_human_review: 4 (Tests 4, 5, 6, 7 — content quality is subjective)
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — all 10 tests have programmatic evidence; Tests 4-7 also need a sanity check that the document content is actually useful, which is subjective and presented as a single combined verdict to the user]
