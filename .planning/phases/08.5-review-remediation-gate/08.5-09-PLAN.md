---
phase: 08.5
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - algorithms_golden_test.go
autonomous: true
requirements:
  - PERF-04
  - TEST-08
  - Q13
threat_model_refs: []

must_haves:
  truths:
    - "All 9 missing TestGolden_*_Staging functions exist for DoubleMetaphone, MongeElkan, MRA, NYSIIS, PartialRatio, Soundex, TokenJaccard, TokenSetRatio, TokenSortRatio"
    - "Each new staging function follows the mechanical template established by the 14 existing functions in algorithms_golden_test.go"
    - "Each function asserts the existing testdata/golden/_staging/<algo>.json file is consistent with the current implementation output"
  artifacts:
    - path: "algorithms_golden_test.go"
      provides: "Complete set of 23 TestGolden_*_Staging functions for the 23-algorithm catalogue"
      contains: "TestGolden_DoubleMetaphone_Staging, TestGolden_MongeElkan_Staging, TestGolden_MRA_Staging, TestGolden_NYSIIS_Staging, TestGolden_PartialRatio_Staging, TestGolden_Soundex_Staging, TestGolden_TokenJaccard_Staging, TestGolden_TokenSetRatio_Staging, TestGolden_TokenSortRatio_Staging"
  key_links:
    - from: "TestGolden_*_Staging × 9 new"
      to: "testdata/golden/_staging/*.json existing files"
      via: "JSON consistency assertion"
      pattern: "_staging/"
---

<objective>
Q13 — add the 9 missing `TestGolden_*_Staging` functions for the Phase 6 and Phase 7 algorithms that have committed staging JSON files but no Go-side test functions attesting them. The mechanical template is established by the 14 existing staging functions in `algorithms_golden_test.go`.

Per RESEARCH.md §3.6 (Cluster 6.A): the staging files already exist in `testdata/golden/_staging/` for at least 5 of the 9 algorithms (MRA, NYSIIS, MongeElkan, DoubleMetaphone, Soundex per PATTERNS.md). The test functions need to be added; if any staging file is missing, the task creates it by running the current implementation against the planned ~10-entry input set and committing the captured output.

Output: 9 new test functions appended to `algorithms_golden_test.go` (or to a new file if size warrants a split). Each function follows the template at lines 220-280 of the existing file (Levenshtein + Hamming Staging patterns).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@docs/requirements.md
@.claude/skills/go-testing-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
<!-- Q13 staging-test fill-in per RESEARCH.md §3.6 6.A and PATTERNS.md §2.J -->

Existing template (algorithms_golden_test.go lines 220-280):

```go
var xxxStagingEntries = []struct{ Input1, Input2 string; ExpectedScore float64 }{ ... ~10 stable entries ... }

func TestGolden_<Algo>_Staging(t *testing.T) {
    assertGoldenStaging(t, "<algo>.json", xxxStagingEntries)
}
```

Algorithms needing new staging-test functions (9 total):
- DoubleMetaphone (returns codes, not scores — wraps via DoubleMetaphoneScore for the staging shape)
- MongeElkan (post-rename — uses MongeElkanScore which is now symmetric)
- MRA (returns score)
- NYSIIS (returns code, wraps via NYSIIS score equivalent if exists)
- PartialRatio (returns score)
- Soundex (returns code, wraps via SoundexScore if exists)
- TokenJaccard (returns score)
- TokenSetRatio (returns score)
- TokenSortRatio (returns score)

For algorithms that return a code string (DoubleMetaphone, NYSIIS, Soundex), the staging struct shape may need a variant — check the existing TestGolden_Hamming_Staging which is also non-score-returning. The exact shape is established by the template; preserve.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 9 TestGolden_*_Staging functions following the existing template</name>
  <files>algorithms_golden_test.go</files>
  <read_first>
    - algorithms_golden_test.go in full (especially lines 220-280 for the Levenshtein + Hamming staging templates — and the corresponding entries for the 14 existing functions to understand the entry-list shape)
    - testdata/golden/_staging/ directory listing (`ls testdata/golden/_staging/`) — identify which of the 9 algorithm JSON files already exist
    - The 9 algorithm source files (double_metaphone.go, monge_elkan.go, mra.go, nysiis.go, partial_ratio.go, soundex.go, token_jaccard.go, token_set_ratio.go, token_sort_ratio.go) for the canonical reference vectors documented in their godoc + Phase 6/7 reference-vector tests
    - .planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md §2.J (staging-test template)
  </read_first>
  <action>
1. For each of the 9 algorithms, locate the existing staging JSON file under `testdata/golden/_staging/<algo>.json`:
   - If the file EXISTS: the task reads it and constructs the corresponding `xxxStagingEntries` variable from the JSON contents (the entries must match the existing JSON byte-for-byte; `assertGoldenStaging` verifies this).
   - If the file is MISSING: the task creates ~10 stable `{Input1, Input2, ExpectedScore}` (or appropriate shape) entries drawn from the algorithm's existing reference-vector tests in `<algo>_test.go`. Run the current implementation against each entry, capture the output, and commit the captured output as the staging JSON. **Discipline:** the entries must be deterministic — no random sampling, no time-dependent values, no map-iteration-order-dependent input.

2. Append the 9 new test functions to `algorithms_golden_test.go`:
   - `TestGolden_DoubleMetaphone_Staging` with `doubleMetaphoneStagingEntries`
   - `TestGolden_MongeElkan_Staging` with `mongeElkanStagingEntries` (uses the post-Plan-02 symmetric `MongeElkanScore` — verify the staging file reflects symmetric outputs)
   - `TestGolden_MRA_Staging` with `mraStagingEntries`
   - `TestGolden_NYSIIS_Staging` with `nysiisStagingEntries`
   - `TestGolden_PartialRatio_Staging` with `partialRatioStagingEntries`
   - `TestGolden_Soundex_Staging` with `soundexStagingEntries`
   - `TestGolden_TokenJaccard_Staging` with `tokenJaccardStagingEntries`
   - `TestGolden_TokenSetRatio_Staging` with `tokenSetRatioStagingEntries`
   - `TestGolden_TokenSortRatio_Staging` with `tokenSortRatioStagingEntries`

3. Each function follows the canonical template:
   ```
   func TestGolden_<Algo>_Staging(t *testing.T) {
       assertGoldenStaging(t, "<algo>.json", <algo>StagingEntries)
   }
   ```
   (or whatever the exact signature of `assertGoldenStaging` requires — match what the existing 14 functions use).

4. For algorithms returning non-float64 outputs (DoubleMetaphone returns 2 strings; NYSIIS / Soundex return 1 string), use the shape established by `TestGolden_Hamming_Staging` (which returns `int`) — the existing test framework already handles mixed-return shapes. If the framework lacks support for the 2-string return of DoubleMetaphone, add a `DoubleMetaphoneScore` wrapper (if not already present) that maps a pair of inputs to a single comparison score — OR use a dedicated assertion shape (`assertGoldenStagingCodes`) if the framework requires it. **Decision:** check what exists in the codebase first; do not invent a new framework — only mechanical extension.

5. Discipline:
   - Apache 2.0 license header is already in the file; no change.
   - British English in any new prose/comments.
   - No testify.
   - Stable ordering of entries (no map iteration).

6. After commit, run `go test -run TestGolden -race -count=1 ./...` to confirm all 23 staging functions pass.
  </action>
  <verify>
    <automated>go test -run 'TestGolden_(DoubleMetaphone|MongeElkan|MRA|NYSIIS|PartialRatio|Soundex|TokenJaccard|TokenSetRatio|TokenSortRatio)_Staging' -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^func TestGolden_(DoubleMetaphone|MongeElkan|MRA|NYSIIS|PartialRatio|Soundex|TokenJaccard|TokenSetRatio|TokenSortRatio)_Staging" algorithms_golden_test.go` returns 9 (all 9 functions present).
    - **Explicit per-algorithm staging file presence** (replaces the incidental count check): each of the 23 expected staging JSON files exists, verified by:
      `for f in levenshtein.json damerau_osa.json damerau_full.json hamming.json jaro.json jaro_winkler.json strcmp95.json smith_waterman_gotoh.json lcs_str.json q_gram_jaccard.json q_gram_dice.json q_gram_cosine.json q_gram_tversky.json monge_elkan.json token_sort_ratio.json token_set_ratio.json partial_ratio.json token_jaccard.json soundex.json double_metaphone.json nysiis.json mra.json ratcliff_obershelp.json; do test -f testdata/golden/_staging/$f || { echo "MISSING: $f"; exit 1; }; done`
      (Catalogue: 9 character + 4 q-gram + 5 token + 4 phonetic + 1 gestalt = 23 per CLAUDE.md and `algoid.go` AlgoIDs(). If a filename here diverges from the on-disk casing convention used by the existing 14 staging files, the executor adjusts to match the convention rather than the names above.)
    - `go test -run 'TestGolden_.*_Staging' -race -count=1 ./...` exits 0 (all 23 staging tests pass).
    - `go vet ./...` exits 0.
    - No new dependencies introduced.
  </acceptance_criteria>
  <done>
    9 new TestGolden_*_Staging functions exist and pass; each is paired with a stable staging JSON file in `testdata/golden/_staging/`; the full 23-algorithm catalogue has staging coverage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test entries → staging JSON | Test inputs and expected outputs must be deterministic across runs and platforms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-18 | T — Tampering via flaky staging entries | algorithm-staging entries | mitigate | Entries are hand-written `{Input, ExpectedOutput}` tuples drawn from the existing reference-vector tests — no randomness, no time, no map iteration. The cross-platform CI matrix verifies byte-identity across runs. |
</threat_model>

<verification>
- Test-green: 9 new staging tests pass.
- Catalogue completeness: 23 TestGolden_*_Staging functions exist (14 pre-existing + 9 new).
- Determinism: `go test -run TestGolden -count=10` produces identical output across runs.
</verification>

<success_criteria>
- Q13 staging-test fill-in complete: 9 missing functions present + green.
- Phase 8.5 success criterion 9 (the 9 missing TestGolden_*_Staging functions for Phase 6 and 7 algorithms exist and pass) satisfied.
- The test-writer Critical finding "9 Phase 6-7 algorithms missing TestGolden_*_Staging" closes.
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-09-SUMMARY.md` documenting:
- which of the 9 staging files already existed vs were created;
- the entry count per new test function;
- any shape-extension needed for code-returning algorithms (DM, NYSIIS, Soundex);
- agent reviews invoked (test-writer for the additions; test-analyst for coverage verification).
</output>
