---
phase: 08.5-review-remediation-gate
plan: 11
subsystem: testing
tags: [test-infrastructure, coverage-floor, property-tests, ast-walk, q11a, q11d, q12a, q12b]

# Dependency graph
requires:
  - phase: 08.5
    provides: "Plan 03 (PartialRatio rune-variant removal); Plan 08 (Tokenise ASCII fast path) — both unchanged here but Plan 11 sits in their wake"
provides:
  - "Q11a: stale bench.txt.new artefact removed from the working tree"
  - "Q11d: partial_ratio.go TODO(#TBD) comment replaced with a plain spec-deferred note (no fake issue marker)"
  - "Q12b: mixedShapeStringGenerator() — five-shape testing/quick generator (empty / short-ASCII / long-non-ASCII / all-same-run / Unicode-rich)"
  - "Q12b: uint16 overflow flake fix in TestProp_Scorer_WeightSumOne (toPositive widens via uint32 before +1)"
  - "Q12a: AST-based exported-coverage helper at scripts/cmd/verify-exported-coverage/main.go"
  - "Q12a: Floor 3 tightened from 'exists-at-least-one-test' to '>= 90.0% statement coverage per exported function'"
affects: ["Plan 13 (improvement sweep — consumes residue list)", "Plan 14 (Q13 verify-llms-sync — consumes AST helper)"]

# Tech tracking
tech-stack:
  added: ["no new runtime deps (stdlib go/ast + go/parser only); zero changes to go.mod"]
  patterns: ["AST-precise gate as a standalone go/cmd helper invoked from a shell wrapper; per-shape mixed quick.Config.Values generator"]

key-files:
  created:
    - "scripts/cmd/verify-exported-coverage/main.go — AST-based exported-coverage gate (Floor 3a + Floor 3b)"
  modified:
    - "partial_ratio.go — TODO(#TBD) removed; spec-deferred note rewritten"
    - "monge_elkan.go — Rule 1 fix: stale test-name reference updated to post-Q3 rename"
    - "props_test.go — added mixedShapeStringGenerator() + 5 shape helpers + TestProp_LevenshteinScore_RangeBounds_MixedShape usage gate"
    - "scorer_test.go — uint16 overflow fix in TestProp_Scorer_WeightSumOne toPositive"
    - "scripts/verify-coverage-floors.sh — Floor 3 rewritten to invoke the Go helper; Floors 1/2 unchanged"

key-decisions:
  - "Q11a: bench.txt.new was never tracked (already in .gitignore at line 10); deletion is filesystem-only — no git rm needed."
  - "Q11d: partial_ratio.go has no 'misleading local variable' in the strict programming sense — the misleading reference is the TODO(#TBD) placeholder itself (a fake issue-tracker reference per the no_github_issues.md memory entry). Fix is comment rewrite, not variable rename."
  - "Q12b mixed-shape generator wired in ONLY at TestProp_LevenshteinScore_RangeBounds_MixedShape as the load-bearing 'must be active' gate. Broader adoption is deferred to Phase 9+ — re-routing existing tests through the generator would require re-validating seed sufficiency (their seeds were calibrated against rand.String). The Q12b lock requires availability + active wiring, not blanket adoption."
  - "Q12a helper exports a `floorViolationErr` error type so main() can map Floor-3 failures (exit 1) vs invocation errors (exit 2) — required for CI exit-code semantics."
  - "Q12a helper reduces duplicate coverage rows (same function name across build-tag variants) to MIN — Floor 3a is the worst-case guarantee, not the best-case."
  - "Q12a Floor 3b (non-func symbols) uses AST identifier scanning, not grep — a string-literal mention does NOT count. ScorerAlgorithm is flagged because it appears only in a comment, not as a typed value."
  - "The 6-item residue list (5 funcs + 1 type below floor) is intentionally surfaced — Plan 13's improvement sweep is the address point. Plan 11 ships the GATE, not the FIX."

patterns-established:
  - "AST-precise gate-as-a-helper: pattern is to ship a single-file go/cmd at scripts/cmd/<name>/main.go invoked from a thin shell wrapper. The shell wrapper handles invocation context (CWD, profile path); the Go helper handles all parsing, walking, and decision-making."
  - "Mixed-shape generator: testing/quick.Config{Values: fn} with a per-shape switch is the idiom for replacing rand.String when default seed coverage misses edge cases. Five shapes (empty / short-ASCII / long-non-ASCII / all-same-run / Unicode-rich) is the documented lock per docs/requirements.md §15.3."
  - "uint16 + 1 overflow protection: widen to uint32 BEFORE the addition, not the multiplication. `float64(uint32(u)+1)` not `float64(uint32(u+1))`."

requirements-completed: [DET-05, TEST-03, TEST-06, PERF-04, Q11a, Q11d, Q12a, Q12b]

# Metrics
duration: 40min
completed: 2026-05-17
---

# Phase 08.5 Plan 11: Cluster 6 Test-Infrastructure Bundle Summary

**Q11a/Q11d/Q12a/Q12b small-grain test-infrastructure cleanups land in three atomic commits; the AST-based exported-coverage helper introduces Floor 3 tightening (>= 90% per exported func) and surfaces a 6-item residue for Plan 13's improvement sweep.**

## Performance

- **Duration:** 40 min
- **Started:** 2026-05-17T16:40:00Z
- **Completed:** 2026-05-17T17:20:00Z
- **Tasks:** 3 completed
- **Files modified:** 5 (+ 1 created)

## Accomplishments

- **Q11a closed**: stale `bench.txt.new` (857 bytes; never tracked, already in `.gitignore`) removed from the working tree. The underlying `TestProp_MongeElkanScoreAsymmetric_DirectionSensitiveWhenTokenCountAsymmetric` property test is green over 5 consecutive `-race -count=1` runs at HEAD — no re-fire observed.
- **Q11d closed**: `partial_ratio.go` `TODO(#TBD)` comment rewritten as a plain spec-deferred note. Project memory `no_github_issues.md` records the absence of an issue tracker; the new comment honours that without the misleading placeholder. Rule 1 bonus fix: stale `monge_elkan.go:190` test-name reference updated to the post-Q3 rename.
- **Q12b closed**: 5-shape `mixedShapeStringGenerator()` lands in `props_test.go` per the docs/requirements.md §15.3 lock; wired into `TestProp_LevenshteinScore_RangeBounds_MixedShape` as the load-bearing "active wiring" gate; uint16 overflow flake in `TestProp_Scorer_WeightSumOne.toPositive` fixed (verified 10 consecutive race runs).
- **Q12a closed**: new `scripts/cmd/verify-exported-coverage/main.go` AST helper enumerates every exported symbol in the root package (via `go/parser.ParseDir` + `go/ast`) and cross-references against `go tool cover -func`. Floor 3a (>= 90% per func) + Floor 3b (every non-func referenced in a `*_test.go` AST) implemented; `scripts/verify-coverage-floors.sh` rewritten to invoke the helper.
- **Residue surfaced for Plan 13**: 6 Floor-3 violations now visible (5 funcs below 90%, 1 type unreferenced in tests) — this is the input to Plan 13's improvement sweep, not Plan 11's scope.

## Task Commits

Each task was committed atomically:

1. **Task 1: Q11a + Q11d cleanups** — `2d93ed6` (docs)
2. **Task 2: Q12b mixed-shape generators + uint16 overflow fix** — `ea8b307` (test)
3. **Task 3: Q12a AST helper + Floor 3 tightening** — `fba3e32` (feat)

## Files Created/Modified

### Created
- `scripts/cmd/verify-exported-coverage/main.go` — AST-based Floor 3 helper (~370 LOC; stdlib-only; Apache-2.0 header)

### Modified
- `partial_ratio.go` — Q11d TODO(#TBD) replaced with spec-deferred note
- `monge_elkan.go` — Rule 1 stale test-name reference fix (post-Q3 rename)
- `props_test.go` — Q12b 5-shape generator + 5 shape helpers + 1 usage test (~200 insertions)
- `scorer_test.go` — Q12b uint16 overflow fix (`uint32(u)+1` widening)
- `scripts/verify-coverage-floors.sh` — Floor 3 section rewritten to invoke the Go helper

## Decisions Made

| # | Decision | Rationale |
|---|----------|-----------|
| 1 | Delete `bench.txt.new` via `rm`, not `git rm` | File was never tracked (already in `.gitignore` line 10); `git rm` would error. The cleanup is filesystem-only. |
| 2 | Q11d interpreted as "TODO is the misleading reference", not "rename a code variable" | The actual `partial_ratio.go` source has no misleading variable in the strict sense at the cited line. The `TODO(#TBD)` placeholder — a fake issue-tracker reference — is the only misleading identifier in the region. Rewriting the comment closes the Q11d lock. |
| 3 | Mixed-shape generator wired into ONE test, not all property tests | The Q12b lock requires the generator to be available AND demonstrably wired in. Broader adoption is Phase 9+ retrofit — re-routing existing tests would require re-validating their seed sufficiency (they were calibrated against `rand.String`). |
| 4 | uint16 fix uses `uint32(u)+1` not `int(u)+1` | `uint32` is the minimal-width safe type that prevents wrap. Using `int` would work but obscures the intent (the source value IS a uint16). |
| 5 | Helper exports `floorViolationErr` for exit-code semantics | CI shell scripts need to distinguish "code has uncovered exports" (exit 1) from "couldn't even run the check" (exit 2). |
| 6 | Helper enumerates methods via the dot-split in `go tool cover` output but does NOT impose Floor 3a per-method | Per-method floors would over-constrain methods with rare paths; Floor 3a applies only to top-level FuncDecls (Recv == nil). |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bug] Stale test-name reference in monge_elkan.go:190**
- **Found during:** Task 1 (Q11a `bench.txt.new` audit step — searching for the underlying test name surfaced the comment)
- **Issue:** `monge_elkan.go:190` documentation comment referenced `TestProp_MongeElkanScore_AsymmetricWhenTokenCountAsymmetric` — a test name that no longer exists. Q3 (Plan 08.5-02, commit 8ffce1b) renamed the asymmetric direct surface to `MongeElkanScoreAsymmetric` and the property test name to `TestProp_MongeElkanScoreAsymmetric_DirectionSensitiveWhenTokenCountAsymmetric`. The doc comment was not updated.
- **Fix:** Replaced the stale name with the current name in the same commit as the Q11d TODO cleanup (logical co-location: both are doc-tier hygiene in adjacent files).
- **Files modified:** `monge_elkan.go`
- **Verification:** `grep -rn "TestProp_MongeElkanScore_AsymmetricWhenTokenCountAsymmetric" --include="*.go"` now returns zero hits.
- **Committed in:** `2d93ed6` (Task 1 commit)

**2. [Q11a investigation outcome] Stale test does NOT re-fire**
- **Found during:** Task 1 (Q11a underlying test audit step per plan instructions)
- **Outcome:** `go test -run TestProp_MongeElkanScoreAsymmetric_DirectionSensitiveWhenTokenCountAsymmetric -race -count=5 -timeout 120s .` exits 0 cleanly. The `bench.txt.new` failure is a stale capture from pre-Q3 generator state; the post-Q3 test premise (with the `fwd != 1.0` and `fwd > 0` guards added in commit `2cc6f4f` and the test-name rename) is no longer susceptible to the original flake.
- **Action:** No further action required — Q11a is closed by file deletion alone. No `KNOWN_ISSUES.md` entry needed.

**3. [Scope clarification] Q12a residue surfaced, not fixed**
- **Found during:** Task 3 (Floor 3 verification step)
- **Outcome:** Helper correctly identifies 6 Floor-3 violations:

  | Symbol | Type | Current Coverage | Floor | Gap |
  |---|---|---|---|---|
  | `DamerauLevenshteinOSADistanceRunes` | func | 80.0% | 90.0% | -10.0pp |
  | `DamerauLevenshteinOSAScoreRunes` | func | 81.8% | 90.0% | -8.2pp |
  | `DefaultScorer` | func | 75.0% | 90.0% | -15.0pp |
  | `DoubleMetaphoneKeys` | func | 63.5% | 90.0% | -26.5pp |
  | `NYSIISCode` | func | 71.7% | 90.0% | -18.3pp |
  | `ScorerAlgorithm` | type | — | (referenced) | not referenced in any `*_test.go` AST (only in a comment) |

- **Disposition:** This residue is exactly what the plan acceptance criteria anticipated: *"failure is acceptable in this plan's verify so long as the helper itself works correctly; the Improvement Sweep plan addresses the residue."* The 6 items are the input to **Plan 13's improvement sweep** — they should appear in Plan 13's `must_haves.truths` as targets to bring above floor.
- **Floor 1 separately failing**: the existing 95% overall floor exceeds the current 90.7% actual. Pre-existing, not caused by this plan. Also residue for Plan 13.

### Authentication Gates

None — this plan is purely test-infrastructure / dev-tooling work.

## Threat Surface Notes

T-08.5-22 mitigation (DoS via malformed coverage profile): the helper's coverage-row parser skips unparseable rows with `continue` (rather than panicking on `strconv.ParseFloat` failure). CI guarantees a well-formed profile from `go test -coverprofile`. Verified by reading helper code path at `loadFuncCoverage`.

## Known Stubs

None — every artefact landed has its own real implementation. The 6-item residue is not a stub but an enforcement target for Plan 13.

## TDD Gate Compliance

N/A — plan type is `execute` (not `tdd`); no per-task RED/GREEN/REFACTOR cycle required. The Q12b property test (`TestProp_LevenshteinScore_RangeBounds_MixedShape`) was added in the SAME commit as the generator helper (`ea8b307`), which is conventional for "new test for new helper" patterns where the test IS the demonstration of correctness.

## Verification Results

### Task 1 acceptance criteria (Q11a + Q11d)

| Criterion | Result |
|---|---|
| `ls bench.txt.new` returns "No such file or directory" | ✓ |
| `grep -nE "TODO\(#TBD\)" partial_ratio.go \| wc -l` returns 0 | ✓ (returns 0) |
| `go build ./...` exits 0 | ✓ |
| `go test -run TestPartialRatio -race -count=1 ./...` exits 0 | ✓ (`ok` 1.281s) |

### Task 2 acceptance criteria (Q12b)

| Criterion | Result |
|---|---|
| `grep -nE "mixedShapeStringGenerator\|mixedShape" props_test.go \| wc -l` returns at least 1 | ✓ (returns 20) |
| `grep -nE "uint32\(u\)\+1\|int\(u\)\+1" scorer_test.go \| wc -l` returns at least 1 | ✓ (returns 1) |
| `go test -run TestProp_Scorer_WeightSumOne -race -count=10` exits 0 | ✓ (10 consecutive races green) |
| `go test -run TestProp_ -race -count=1 -timeout 120s` exits 0 within timeout | ✓ (13.367s) |

### Task 3 acceptance criteria (Q12a)

| Criterion | Result |
|---|---|
| `ls scripts/cmd/verify-exported-coverage/main.go` returns the file | ✓ |
| `go build ./scripts/cmd/verify-exported-coverage` exits 0 | ✓ |
| `grep -n verify-exported-coverage scripts/verify-coverage-floors.sh \| wc -l` >= 1 | ✓ (returns 5) |
| `bash scripts/verify-coverage-floors.sh coverage.out` exits 0 OR exits non-zero with clear enumeration | ✓ (exits non-zero with the 6-item residue + Floor 1 — both expected per acceptance language) |
| `go vet ./scripts/cmd/verify-exported-coverage` exits 0 | ✓ |

## Agent Reviews Invoked

Per plan output spec, the following agent perspectives were applied during execution:

- **test-analyst**: confirmed the mixed-shape generator's 5-shape distribution matches the docs/requirements.md §15.3 lock; confirmed `TestProp_LevenshteinScore_RangeBounds_MixedShape` is the minimal but sufficient usage gate (broader rerouting deferred to Phase 9+ to avoid re-validating other tests' seed calibration).
- **determinism-reviewer**: confirmed `sortedKeys` in the helper produces stable output ordering for offender lists; confirmed the helper's "MIN of duplicate coverage rows" rule produces deterministic worst-case Floor 3a evaluation.
- **go-quality**: confirmed the helper compiles + vets clean; confirmed `strings.Fields` (not `strings.Split` on `\t`) is the correct parser for `go tool cover -func` output (multi-tab padding); confirmed the `floorViolationErr` exit-code mapping at `main()`.

## References

- `.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md` §Q11a, §Q11d, §Q12a, §Q12b
- `.planning/phases/08.5-review-remediation-gate/08.5-11-PLAN.md` (this plan)
- `docs/requirements.md` §15.3 (mixed-shape generator distribution), §15.8 (Floor 3 spec)
- Project memory `no_github_issues.md` (the "no GH issues yet" rule applied to Q11d)
- Prior art: `ai_friendly_test.go` (extracted the `go/parser.ParseDir` + `collectExportedSymbols` pattern into the standalone helper)

## Self-Check: PASSED

- `scripts/cmd/verify-exported-coverage/main.go` exists: FOUND
- `scripts/verify-coverage-floors.sh` mentions `verify-exported-coverage` (5 hits): FOUND
- `bench.txt.new` deleted: CONFIRMED ("No such file or directory")
- `TODO(#TBD)` in partial_ratio.go: CONFIRMED 0 occurrences
- Commit `2d93ed6` exists in git log: FOUND
- Commit `ea8b307` exists in git log: FOUND
- Commit `fba3e32` exists in git log: FOUND
- All three tasks committed atomically in order
