---
phase: 08.5
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
  - cosine.go
  - scorer.go
autonomous: true
requirements:
  - QGRAM-03
  - SCORER-04
  - DET-03
  - Q11b
threat_model_refs: [T-08.5-12]

must_haves:
  truths:
    - "cosine.go:343 wraps the qa[k]*qb[k] product in an outer float64(...) cast"
    - "scorer.go:380 wraps the entry.weight*score product in an outer float64(...) cast"
    - "The inline comment at both sites documents the FMA-defence rationale per docs/requirements.md §14.4"
    - "Cross-platform CI matrix (linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64) produces byte-identical algorithms.json and scorer-default.json after the change"
  artifacts:
    - path: "cosine.go"
      provides: "FMA-defeating double-cast on the dot-product reduction"
      contains: "float64(float64(qa[k])"
    - path: "scorer.go"
      provides: "FMA-defeating double-cast on the composite reduction"
      contains: "float64(entry.weight"
  key_links:
    - from: "cosine.go:343 dot-product loop"
      to: "cross-platform golden file algorithms.json"
      via: "byte-identical determinism gate"
      pattern: "make verify-determinism"
    - from: "scorer.go:380 composite reduction"
      to: "cross-platform golden file scorer-default.json"
      via: "byte-identical determinism gate"
      pattern: "make verify-determinism"
---

<objective>
Q11b — apply the FMA-defeating double-cast at the two known FMA-emission sites in the floating-point reduction loops. Per `docs/requirements.md §14.4`: explicit parenthesisation alone does not prevent compiler-emitted FMA on arm64 (per `golang/go#17895`); wrapping the multiplication product in an outer `float64(...)` cast forces an intermediate rounding point that the compiler treats as a fence and therefore cannot fuse.

This is COSMETICALLY SMALL (~4 line changes across 2 files, plus comment rewrites) but DETERMINISM-CRITICAL. The cross-platform CI matrix (5 platforms) is the load-bearing verification gate.

Output: single atomic commit at both sites plus comment rewrites that remove the "FMA may fuse" hedge and document the explicit defence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@docs/requirements.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
<!-- Q11b LOCKED in 08.5-CONTEXT.md — exact line changes -->

BEFORE (cosine.go:343 — Cosine dot-product reduction):
```
dot = (float64(qa[k]) * float64(qb[k])) + dot
```
Comment at lines 332-340 hedges: "parens do NOT prevent FMA fusion on arm64 (see golang/go#17895)".

AFTER:
```
dot = float64(float64(qa[k]) * float64(qb[k])) + dot
```
Comment rewritten to remove the hedge and document the explicit FMA defence.

BEFORE (scorer.go:380 — Scorer composite reduction):
```
acc = acc + (entry.weight * score)
```
(Carries `//nolint:gocritic` per Phase 8.)

AFTER:
```
acc = float64(entry.weight*score) + acc
```
(matches the Q11b lock exactly per CONTEXT.md). Comment rewritten to document the FMA defence inline.

Reference: `golang/go#17895` (cited by `cosine.go:335` currently) — the canonical Go-team-acknowledged context for FMA-defeat patterns. The double-cast technique is the established workaround per the issue's discussion.

Cross-platform verification: `make verify-determinism` locally + the CI matrix on PR.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply FMA double-cast at cosine.go:343 + scorer.go:380, rewrite rationale comments</name>
  <files>cosine.go, scorer.go</files>
  <read_first>
    - cosine.go in full (especially lines 325-365 — the dot-product reduction loop and the existing FMA-hedge comment block)
    - scorer.go lines 370-395 (the composite reduction loop and the existing `//nolint:gocritic` comment)
    - .planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md §Q11b
    - .planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md §2.E (FMA double-cast pattern)
    - docs/requirements.md §14.4 (authoritative FMA defence rationale)
    - .claude/skills/determinism-standards/SKILL.md (no FMA, no transcendentals, left-to-right reductions only)
  </read_first>
  <action>
Apply both edits in a single atomic commit:

1. **In cosine.go (around line 343)** — locate the dot-product reduction line. The current form (from RESEARCH.md and PATTERNS.md citations):
   ```
   dot = (float64(qa[k]) * float64(qb[k])) + dot
   ```
   Replace with:
   ```
   dot = float64(float64(qa[k]) * float64(qb[k])) + dot
   ```
   The added outer `float64(...)` cast around the multiplication product forces intermediate rounding (an IEEE-754 round-to-nearest-even at `float64` precision) and acts as a fence the Go compiler will not fuse across into FMA.

2. **In cosine.go (the comment block around lines 332-340)** — rewrite to remove the "FMA may fuse" hedge and document the explicit defence. The new comment should:
   - Cite `docs/requirements.md §14.4` as authoritative.
   - Cite `golang/go#17895` for the underlying Go-toolchain context.
   - Explicitly say: "The outer float64(...) cast on the multiplication product forces an IEEE-754 round-to-nearest-even at float64 precision, which the Go compiler treats as a rounding fence and therefore cannot fuse into FMA (fused multiply-add). On arm64, where the FPU emits FMA by default for `(a*b)+c` patterns, this defence is load-bearing for cross-platform byte-identical output on `testdata/golden/algorithms.json`."
   - Be in British English (no `behavior`, use `behaviour`).

3. **In scorer.go (around line 380)** — locate the composite reduction line. The current form:
   ```
   acc = acc + (entry.weight * score)  //nolint:gocritic
   ```
   Replace with:
   ```
   acc = float64(entry.weight*score) + acc
   ```
   (The `//nolint:gocritic` line-suffix comment, if it complains about the new form, may need updating or removing — verify by running `golangci-lint run scorer.go`.)

4. **In scorer.go (the existing in-file comment near the reduction)** — add or update the inline rationale to mirror the cosine.go comment. The same FMA-defence rationale applies: cross-platform byte-identical output on `testdata/golden/scorer-default.json` is the load-bearing acceptance test.

Commit message: `fix(determinism): apply FMA-defeating double-cast at cosine + scorer reductions (Q11b)`.

**Verification discipline (mandatory):**
- After the edit, run `make verify-determinism` locally. If the local platform's golden files diverge from the committed bytes, halt and investigate before pushing.
- The PR must trigger the cross-platform CI matrix (linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64). All 5 platforms must produce byte-identical `algorithms.json` and `scorer-default.json` post-edit. If any platform diverges, the Q11b plan halts and rolls back on the affected platform; document the deviation; escalate to the user.
- The plan is considered green ONLY when the CI matrix is green across all 5 platforms.

Per D-XX traceability: this plan implements QGRAM-03 (Cosine determinism), SCORER-04 (Scorer determinism), DET-03 (no FMA emission on output paths), Q11b (locked double-cast).
  </action>
  <verify>
    <automated>go build ./... && go test -run 'TestCosine|TestScorer|TestGolden' -race -count=1 ./... && make verify-determinism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "float64(float64(qa\\[k\\])" cosine.go | wc -l` returns at least 1 (or equivalent pattern — outer float64 wrap on the product).
    - `grep -n "float64(entry.weight" scorer.go | wc -l` returns at least 1 (outer float64 wrap on the weight×score product).
    - `grep -n "FMA may fuse\\|may fuse FMA" cosine.go | wc -l` returns 0 (old hedge removed).
    - `grep -n "FMA\\|fused multiply-add" cosine.go scorer.go | wc -l` returns at least 2 (new defence rationale documented at both sites).
    - `go build ./...` exits 0.
    - `go test -race -count=1 ./...` exits 0 (no test asserts a different cosine/scorer output value).
    - `make verify-determinism` exits 0 locally.
    - CI matrix on the PR is green across all 5 platforms (linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64) with byte-identical `algorithms.json` and `scorer-default.json`.
    - `golangci-lint run` exits 0 (any pre-existing `//nolint:gocritic` resolved appropriately).
  </acceptance_criteria>
  <done>
    Both sites carry the outer `float64(...)` cast on the multiplication product; both sites carry the updated FMA-defence rationale comment; local `make verify-determinism` is green; the cross-platform CI matrix is green on the PR; no test assertion changes (cosine and scorer outputs are mathematically identical post-edit — the cast forces a rounding point that was already in effect on amd64; on arm64 the cast prevents fusion that would have produced a 1-ULP difference).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiler → FPU instruction emission | The Go toolchain may emit FMA on arm64 without source-level opt-in. This is a determinism-vs-performance trade-off the language explicitly permits. |
| CI matrix → golden file byte-identity | If any platform diverges post-edit, the v1.0 cross-platform determinism guarantee is broken. The matrix is the load-bearing gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-12 | T — Tampering via compiler FMA emission | cosine.go:343 + scorer.go:380 | mitigate | Outer `float64(...)` cast forces IEEE-754 round-to-nearest-even at float64 precision. The Go compiler treats this as a rounding fence and cannot fuse into FMA. Verified by cross-platform CI matrix on PR. |
| T-08.5-13 | D — Denial of service via golden-file thrash | algorithms.json / scorer-default.json regeneration | accept | If any platform's golden file changes post-edit, all five must regenerate together (no partial commits). The cross-platform matrix gates the merge. |
</threat_model>

<verification>
- Compile-green: `go build ./...` exits 0.
- Test-green: `go test -race ./...` exits 0.
- Local determinism: `make verify-determinism` exits 0.
- Cross-platform CI matrix: 5/5 platforms green with byte-identical goldens (linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64).
- Static check: `grep -n "FMA may fuse" cosine.go` returns 0 (hedge removed); `grep -nE "float64\\(.*\\*.*\\)" cosine.go scorer.go` returns at least 2 (the outer cast).
</verification>

<success_criteria>
- Q11b lock satisfied: outer `float64(...)` cast at both sites; comment rationale rewritten.
- Phase 8.5 success criterion 7 (Cosine and Scorer reductions carry the FMA-defeating double-cast at cosine.go:343 and scorer.go:380 with the inline rationale comment) satisfied.
- DET-03 (no FMA emission on output paths) re-confirmed via CI matrix.
- The determinism Critical (×1) and Important (Cosine FMA risk) findings from the Phase 8 review close.
- **This plan is a SOFT PREDECESSOR of Plan 16 (bench.txt regeneration)** — bench numbers may shift by 1-2% post-Q11b; bench.txt regen sequences after Q11b.
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-06-SUMMARY.md` documenting:
- exact before/after diff at both sites;
- CI matrix run-IDs and their byte-identity confirmations for algorithms.json and scorer-default.json;
- whether any bench regression (1-2%) was observed locally — recorded for Plan 16's bench.txt regeneration sequencing;
- agent reviews invoked (determinism-reviewer mandatory; algorithm-correctness-reviewer; code-reviewer; commit-message-reviewer).
</output>
