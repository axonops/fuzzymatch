# Phase 8.5: Review Remediation Gate — Pattern Map

**Mapped:** 2026-05-17
**Files analyzed (new + modified):** ~75 distinct files across 11 role-buckets
**Analogs found:** 70 / 75 (5 carry no in-tree analog — flagged explicitly in `## No Analog Found`)

This pattern map is organised by **role-bucket** so the planner can read it linearly while building the Phase 8.5 plan task DAG. Each entry cites a concrete in-tree analog (file + line numbers) the executor copies from, with the actual excerpt inlined so PATTERNS.md is self-contained. Items needing divergence from the analog are flagged.

---

## 1. File Classification — Master List

The table below classifies every file Phase 8.5 will create or modify. Cluster numbers map to RESEARCH.md §3.

| File (new/modified) | Cluster | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|---|
| `validate.go` | 2 (Q4) | algorithm impl / public-API surface | pure-function request-response | `normalise.go` + `tokenise.go` | role-match (no analog for diagnostic returning `[]Warning` slice) |
| `warn_kind.go` | 2 (Q4) | enum + String() | request-response | `algoid.go` lines 213-308 | exact (mirror `AlgoID.String()` + `AlgoIDs()` pattern) |
| `errors.go` (modify) | 2 (Q4, Gap 4, Gap 5) | sentinel declarations | constant data | `errors.go` lines 108-162 (the four "good" sentinels) | exact (in-file pattern) |
| `errors_test.go` (modify) | 2 | unit test | request-response | `errors_test.go` lines 53-60 (sentinel-shape table) | exact (in-file extension) |
| `monge_elkan.go` (modify) | 2 (Q3 + Q4 follow-up) | algorithm impl | pure-function | n/a — atomic rename | role-match (the Q3 directional flip is unique) |
| `hamming.go` (no change) | 2 (Q1) | algorithm impl | n/a — spec catches up | n/a | code wins; no analog needed |
| `partial_ratio.go` (modify — Q5 delete + Q11d rename) | 2 + 6 | algorithm impl | pure-function | `cosine.go` (for clean rune-variant absence) | role-match |
| `tokenise.go` (modify — Q8b ASCII fast path) | 5 (Q8b) | utility | byte stream transform | `normalise.go` lines 144-250 (ASCII fast path), `double_metaphone.go` lines 169-211 (stack-buf pattern) | exact (the two analogs together cover the pattern) |
| `cosine.go:343` (modify — Q11b) | 4 (Q11b) | algorithm impl edit | float reduction | `scorer.go:380` (sister site) | exact (single-line edit, both sites) |
| `scorer.go:380` (modify — Q11b) | 4 (Q11b) | scorer reduction edit | float reduction | `cosine.go:343` (sister site) | exact |
| `scorer.go:586` (modify — Gap 5) | 2 (Gap 5) | panic surface | error wrapping | new pattern; closest is `monge_elkan.go` direct-call panic | role-match |
| `double_metaphone.go` (modify — Q7a + Q9) | 3 + 5 (Q9, Q7a) | algorithm impl | stack-buffer accumulation | `double_metaphone.go:169-211 dmPrep` (already uses `[64]byte`) | exact (extend the in-file pattern to the key accumulators) |
| `double_metaphone_paper_test.go` (NEW — Q11c) | 3 (Q11c) | unit test (paper-anchored) | request-response | `double_metaphone_test.go:61-180 TestDoubleMetaphoneKeys_LanguageBranches` | exact |
| `q_gram.go` (modify — Q7d capacity hint) | 5 (Q7d) | utility | map build | in-file extension | n/a |
| `damerau_full_test.go` (modify — Q11e un-skip) | 5 (Q11e) | test (un-skip) | n/a | in-file edit | n/a |
| `scorer_options.go` (modify — Q2 guards + Q4 sentinel rename) | 2 (Q2, Gap 4) | functional option | request-response | `scorer_options.go:257-266 WithThreshold` (range-check precedent) | exact (extend with NaN/Inf guard) |
| `scorer_options_internal_test.go` (RENAME from `scorer_options_test.go`, Gap 1) | 9 (Gap 1) | unit test (filename rename) | n/a | mechanical | n/a |
| `validate_test.go` (NEW) | 2 (Q4) + 6 | unit test | request-response | `normalise_test.go` + `errors_test.go:53-60` (sentinel-table form) | role-match |
| `validate_bench_test.go` (NEW) | 2 (Q4) + 6 | benchmark | request-response | `cosine_bench_test.go` | exact |
| `validate_fuzz_test.go` (NEW) | 2 (Q4) + 6 | fuzz | request-response | `cosine_fuzz_test.go` | exact |
| `algorithms_golden_test.go` (modify — 9 missing staging fns) | 6 (Q13 6.A) | golden test | request-response | `algorithms_golden_test.go:220-280` (Levenshtein + Hamming Staging templates) | exact (mechanical fill-in) |
| `character_cross_validation_test.go` (NEW — Q10) | 6 (Q10 6.B) | cross-validation loader | JSON-file consume | `token_ratio_cross_validation_test.go` | exact |
| `qgram_cross_validation_test.go` (NEW — Q10) | 6 (Q10 6.B) | cross-validation loader | JSON-file consume | `token_ratio_cross_validation_test.go` | exact |
| `monge_elkan_cross_validation_test.go` (NEW — Q10) | 6 (Q10 6.B) | cross-validation loader | JSON-file consume | `token_ratio_cross_validation_test.go` | exact |
| `scripts/gen-character-cross-validation.py` (NEW — Q10) | 6 (Q10 6.B) | corpus generator | external lib call → JSON output | `scripts/gen-token-ratio-cross-validation.py` | exact |
| `scripts/gen-qgram-cross-validation.py` (NEW — Q10) | 6 (Q10 6.B) | corpus generator | external lib call → JSON output | `scripts/gen-token-ratio-cross-validation.py` | exact |
| `scripts/gen-monge-elkan-cross-validation.py` (NEW — Q10) | 6 (Q10 6.B) | corpus generator | external lib call → JSON output | `scripts/gen-token-ratio-cross-validation.py` | exact |
| `testdata/cross-validation/character/vectors.json` (NEW — Q10) | 6 (Q10 6.B) | committed corpus | data | `testdata/cross-validation/token-ratios/vectors.json` | exact |
| `testdata/cross-validation/qgram/vectors.json` (NEW — Q10) | 6 (Q10 6.B) | committed corpus | data | `testdata/cross-validation/token-ratios/vectors.json` | exact |
| `testdata/cross-validation/monge-elkan/vectors.json` (NEW — Q10) | 6 (Q10 6.B) | committed corpus | data | `testdata/cross-validation/token-ratios/vectors.json` | exact |
| `props_test.go` (modify — Q12b mixed-shape generators + uint16 fix) | 6 (Q12b) | property test | request-response | in-file extension | n/a |
| `scripts/cmd/verify-exported-coverage/main.go` (NEW — Q12a) | 6 (Q12a 6.D) + 7 (Q13) | AST-walking Go helper | source → coverage report | `ai_friendly_test.go` (the AST-walk pattern) | exact (extract `collectExportedSymbols` into a standalone main) |
| `scripts/verify-coverage-floors.sh` (modify — Q12a integration) | 6 (Q12a) + 7 (Q13) | shell wrapper | shell + Go helper invocation | in-file extension | n/a |
| `internal_coverage_test.go` (NEW — meta-test) | 6 (6.E) | meta-test | source enumeration → assertion | `ai_friendly_test.go` | exact |
| `readme_shop_front_test.go` (NEW — meta-test) | 6 (6.E) | meta-test | file content diff | `ai_friendly_test.go` | role-match |
| `documentation_test.go` (NEW — meta-test) | 6 (6.E) | meta-test | source + doc enumeration | `ai_friendly_test.go` | exact |
| `scorer_fuzz_test.go` (NEW — FuzzScorer_* harnesses) | 6 (6.E) | fuzz | request-response | `cosine_fuzz_test.go` | exact |
| `*_runes_fuzz_test.go` (NEW × 9 — rune-variant fuzz) | 6 (6.E) | fuzz | request-response | `cosine_fuzz_test.go` (add `*Runes` call site) | exact |
| `tests/bdd/steps/scorer_steps.go` (modify — Gap 2 + Q4 sentinel coverage) | 9 (Gap 2) | BDD step extension | switch case append | `tests/bdd/steps/scorer_steps.go:326-347 constructingTheScorerShouldReturn` | exact |
| `tests/bdd/features/scorer.feature` (modify — Gap 2 + Validate scenarios) | 9 (Gap 2) | BDD feature scenario | Gherkin scenarios | `tests/bdd/features/scorer.feature:105-127` (3 existing sentinel scenarios) | exact |
| `tests/bdd/features/normalisation.feature` (NEW — Gap 3) | 9 (Gap 3) | BDD feature file | Gherkin | `tests/bdd/features/scorer.feature` (structure) | role-match (no existing analog for `normalisation`-scope scenarios) |
| `tests/bdd/features/determinism.feature` (NEW — Gap 3) | 9 (Gap 3) | BDD feature file | Gherkin | `tests/bdd/features/scorer.feature` (structure) | role-match |
| `tests/bdd/features/validate.feature` (NEW — Q4 VALIDATE-01..VALIDATE-05) | 2 (Q4) + 6 | BDD feature file | Gherkin | `tests/bdd/features/scorer.feature` (structure) | role-match |
| `dispatch_*.go` × 23 (modify — Q14b `var _ = func() bool {...}()` refactor) | 9 (Q14b) | dispatch glue | package init | `dispatch_cosine.go` (current pattern) | exact (refactor target shape per `## Pattern Assignments` §9.A) |
| `golden_canonical.go` (modify — Q14b `WriteGoldenFile` unexport) | 9 (Q14b) | utility (test-only export) | test fixture write | `export_test.go:32` (test-only re-export pattern) | exact |
| `.github/workflows/release.yml` (modify — Q13 `needs: [ci]`) | 7 (Q13) | CI workflow | YAML edit | `.github/workflows/ci.yml` (workflow structure) | role-match |
| `.github/workflows/nightly.yml` (NEW — Q13) | 7 (Q13) | CI workflow | scheduled cron | `.github/workflows/ci.yml` (matrix + SHA pin patterns) | role-match (no nightly analog yet) |
| `.github/workflows/*.yml` (modify — Q13 SHA-pin all action refs) | 7 (Q13) | CI workflow | YAML edit | in-place text replacement across 7 files | n/a |
| `Makefile` (modify — Q13 `verify-llms-sync` + Q10 regen targets) | 6 + 7 | build glue | recipe additions | `Makefile` existing targets (verify-license-headers, etc.) | exact (in-file extension) |
| README.md (modify — Q4 Validate Quick Start, post-Q3 / Q5 signatures) | 2 + 8 | docs surface | Markdown | mask README structure (per STACK.md) | exact |
| `docs/algorithms.md` (modify — 100 % TBD scaffold → full fill-in) | 8 | docs surface | Markdown | `docs/scorer.md`, `docs/tuning.md` (sister docs in same dir) | exact |
| `docs/algorithms.md` (Q12c panic-surface section + Q14a 5 MB ceiling) | 8 (Q12c, Q14a) | docs surface | Markdown | in-file new section | n/a |
| `llms.txt` (modify — Validate + signature refresh post-Q3/Q5) | 8 | AI-friendly index | line-based listing | `llms.txt` existing entries | exact |
| `llms-full.txt` (modify — Validate + signature refresh) | 8 | AI-friendly full doc | long-form Markdown | `llms-full.txt` existing entries | exact |
| 23 algorithm `*.go` files (modify — godoc cross-ref to `Validate`) | 8 (Q4) | per-algorithm godoc | text addition | one consistent template (`## Pattern Assignments` §8.C) | exact |
| `examples/validate-input-quality/main.go` (NEW — Q4 surface 6) | 2 + 8 | runnable example | stdout I/O | `examples/scorer-composition/main.go` | exact |
| `examples/validate-input-quality/main_test.go` (NEW) | 2 + 8 | example stdout pin | os.Pipe redirect | `examples/scorer-composition/main_test.go` | exact |
| `examples/validate-input-quality/go.mod` (NEW) | 2 + 8 | module file | data | `examples/scorer-composition/go.mod` | exact |
| `CHANGELOG.md` (modify — already populated, may need touch-ups) | 8 | docs surface | Markdown | `CHANGELOG.md` `[Unreleased]` | exact |
| `bench.txt` (regenerate — after Q7a + Q8b + Q11b land) | 7 (Q13) | benchmark baseline | data | existing `bench.txt` | exact (regen, not edit) |
| `.planning/phases/08.5-review-remediation-gate/08.5-LICENCE-AUDIT.md` (NEW — Q13) | 7 (Q13) | project meta | Markdown | n/a (new file) | no analog |
| `bench.txt.new` (DELETE — Q11a) | 6 (Q11a) | stale artefact | n/a | n/a | n/a |

---

## 2. Pattern Assignments — By Role-Bucket

The role-buckets below are ordered to match the planner's task DAG sequencing (Cluster 2 first, then 3, 4, 5, 6, 7, 8, 9).

### 2.A — Sentinel declarations (`errors.go` modifications) — Cluster 2 (Q4 + Gap 4 + Gap 5)

**Analog:** `errors.go` lines 108-162 — the four "good" four-section sentinels (`ErrEmptyScorer`, `ErrInvalidWeight`, `ErrInvalidThreshold`, `ErrMissingThreshold`) already follow the 4-section template (**What** / **Common causes** / **Resolution** / **Example**).

**Four-section template excerpt** (`errors.go:108-121`):

```go
// ErrEmptyScorer indicates NewScorer was called without any algorithm
// option — the option slice contained zero WithAlgorithm /
// With*Algorithm entries by the time validation ran. A Scorer with no
// algorithms has no meaningful composite to compute.
//
// Pass at least one WithAlgorithm option (or use DefaultScorer() for
// the opinionated six-algorithm composition).
//
// Returned by NewScorer (Phase 8) after the missing-threshold check
// passes and the option-validation pipeline finds cfg.entries empty.
//
// Discriminate via errors.Is(err, fuzzymatch.ErrEmptyScorer); never
// match the error message string.
var ErrEmptyScorer = errors.New("fuzzymatch: scorer has no algorithms (pass at least one WithAlgorithm option or use DefaultScorer)")
```

**Apply to:** `ErrInvalidQGramSize`, `ErrInvalidTverskyParam`, `ErrInvalidAlgoID` (post-rename), `ErrInvalidInnerAlgo` (new), `ErrInternalInvariantViolated` (new). The existing godoc on `ErrInvalidInput` (`errors.go:38-48`), `ErrInvalidConfiguration` (`errors.go:50-57`), and `ErrEmptyInput` (`errors.go:100-106`) is **deleted** (Q4 — 3 unused sentinels removed).

**Gap 4 rename mechanics (atomic across 5 files):**

```go
// BEFORE — errors.go:90-98:
var ErrInvalidAlgorithm = errors.New("fuzzymatch: invalid algorithm")

// AFTER:
var ErrInvalidAlgoID = errors.New("fuzzymatch: invalid algorithm identifier")
```

**Call-site renames (15 references across 5 files — atomic single commit):**
- `errors.go:90, 98` (declaration)
- `scorer.go:142, 231` (2 references)
- `scorer_options.go:43, 143, 156, 409, 431, 435` (6 references)
- `scorer_options_test.go:86, 87, 490, 491, 498, 499` (6 references)
- `errors_test.go:57` (table entry)

**Gap 5 new sentinel + panic wrapping pattern:**

```go
// errors.go — add:
// ErrInternalInvariantViolated indicates a library invariant that should
// never fire in correct usage was violated — seeing it is unambiguous
// evidence of a library bug. File a GitHub issue with the panic stack
// trace.
//
// Common causes: DefaultScorer() option-pipeline collision (the locked
// option mix produces a validation error from NewScorer, which the
// constructor catches and wraps in this sentinel via panic).
//
// Resolution: this error is NEVER raised by caller-supplied input;
// always file a bug report with a reproducer. Discriminate via
// recover()+errors.Is(panicValue.(error), ErrInternalInvariantViolated).
//
// Example:
//   defer func() {
//       if r := recover(); r != nil {
//           if err, ok := r.(error); ok && errors.Is(err, fuzzymatch.ErrInternalInvariantViolated) {
//               // library bug — collect stack + file issue
//           }
//       }
//   }()
//   s := fuzzymatch.DefaultScorer()
var ErrInternalInvariantViolated = errors.New("fuzzymatch: internal invariant violated (library bug — please file an issue)")
```

```go
// scorer.go:586-592 — BEFORE:
func DefaultScorer() *Scorer {
    s, err := NewScorer(DefaultScorerOptions()...)
    if err != nil {
        panic("fuzzymatch: DefaultScorer construction failed (this is a bug): " + err.Error())
    }
    return s
}

// AFTER:
func DefaultScorer() *Scorer {
    s, err := NewScorer(DefaultScorerOptions()...)
    if err != nil {
        panic(fmt.Errorf("%w: DefaultScorer construction failed: %w", ErrInternalInvariantViolated, err))
    }
    return s
}
```

(Note: `scorer.go` may need `import "fmt"` if not already present in the file.)

---

### 2.B — `ErrInvalidInnerAlgo` panic (Monge-Elkan direct call) — Cluster 2 (Q4 follow-up)

**Apply same `fmt.Errorf("%w: ...", sentinel)` pattern as Gap 5 above** in `monge_elkan.go:162, 342` (current sites already reference the future name in godoc). The panic value MUST be a typed error so `recover()` callers can discriminate via `errors.Is`.

```go
// monge_elkan.go (current direct-call panic sites at lines ~162 and ~342)
// — pattern:
panic(fmt.Errorf("%w: MongeElkanScore inner AlgoID %s not in permitted set", ErrInvalidInnerAlgo, inner))
```

---

### 2.C — Functional-option NaN/Inf/range guards (`scorer_options.go`) — Cluster 2 (Q2)

**Analog:** `scorer_options.go:257-266 WithThreshold` already enforces a range check.

```go
// scorer_options.go:257-266 — current WithThreshold:
func WithThreshold(t float64) ScorerOption {
    return func(cfg *scorerConfig) error {
        if t < 0.0 || t > 1.0 {
            return ErrInvalidThreshold
        }
        cfg.threshold = t
        cfg.thresholdSet = true
        return nil
    }
}
```

**Q2 additions — extend three options with NaN/Inf guards:**

```go
// WithThreshold — extend to reject NaN:
//   if math.IsNaN(t) || t < 0.0 || t > 1.0 {
//       return ErrInvalidThreshold
//   }
// (±Inf already rejected by the range check; NaN compares false to both, so a
// NaN guard MUST come first.)

// WithAlgorithm (scorer_options.go:150-165) — extend to reject NaN, ±Inf,
// w ≤ 0 (currently only the `weight <= 0` half is checked):
//   if math.IsNaN(weight) || math.IsInf(weight, 0) || weight <= 0 {
//       return ErrInvalidWeight
//   }

// WithTverskyAlgorithm (scorer_options.go:381-399) — current check is
// `if alpha < 0 || beta < 0`. Extend with α+β > 0 guard:
//   if math.IsNaN(alpha) || math.IsNaN(beta) ||
//       math.IsInf(alpha, 0) || math.IsInf(beta, 0) ||
//       alpha < 0 || beta < 0 || alpha+beta <= 0 {
//       return ErrInvalidTverskyParam
//   }
// Update the obsolete godoc at scorer_options.go:376-380 that says α+β
// is NOT re-checked in the option.
```

**Note on `import "math"`:** `scorer_options.go` likely needs `import "math"` added (verify before editing).

**Q2 direct-call Tversky panic:** `tversky.go` direct-call must `panic(fmt.Errorf("%w: ...", ErrInvalidTverskyParam))` on the same input — the panic message must reference the sentinel so `errors.Is` works through `recover()`.

---

### 2.D — `Validate` / `Warning` / `WarnKind` surface (NEW — `validate.go`) — Cluster 2 (Q4)

**Analog:** No single existing file matches. The pattern is a hybrid of:
1. **Public function shape:** `normalise.go:144 Normalise(s string, opts NormalisationOptions) string` — public pure function with no error return.
2. **Enum + `String()` + `Constants` + slice constructor:** `algoid.go:213-308` — `AlgoID.String()` (switch per constant), `AlgoIDs()` (returns fresh slice).
3. **Diagnostic-warning return type:** `scorer.go` `ScoreAll` returns `map[string]float64` — closest analog for "function returns a structured result", but `Warning` is a slice not a map.

**Apply `algoid.go` enum pattern to `WarnKind`** (`warn_kind.go`):

```go
// Excerpt of the pattern to copy from algoid.go:213-267:
func (id AlgoID) String() string { //nolint:gocyclo // one switch case per AlgoID is intentional
    switch id {
    case AlgoLevenshtein:
        return "Levenshtein"
    // ...one case per constant...
    default:
        return fmt.Sprintf("AlgoID(%d)", int(id))
    }
}
```

```go
// WarnKind target — exact same pattern (CamelCase per Q6b):
type WarnKind int

const (
    WarnEmptyInput WarnKind = iota
    WarnUnequalLength
    WarnNoTokensAfterNormalise
    WarnAllNonASCIIDropped
    WarnPathologicallyLargeInput
)

func (k WarnKind) String() string {
    switch k {
    case WarnEmptyInput:
        return "EmptyInput"
    case WarnUnequalLength:
        return "UnequalLength"
    case WarnNoTokensAfterNormalise:
        return "NoTokensAfterNormalise"
    case WarnAllNonASCIIDropped:
        return "AllNonASCIIDropped"
    case WarnPathologicallyLargeInput:
        return "PathologicallyLargeInput"
    default:
        return fmt.Sprintf("WarnKind(%d)", int(k))
    }
}
```

**`Warning` struct** — pure data holder, no methods (mirror `ScorerAlgorithm` in `scorer.go:396-409`):

```go
// Warning identifies a single input-quality observation for a single
// algorithm. Per RESEARCH.md VALIDATE-02:
type Warning struct {
    Algorithm AlgoID
    Kind      WarnKind
    Detail    string
}
```

**`Validate` function shape** — mirror `Normalise`'s pure-function signature; never panics:

```go
// Validate returns a slice of input-quality warnings for the pair (a, b)
// across all 23 catalogue algorithms. Returns nil if no warnings fire.
// Never panics, regardless of input shape. See docs/algorithms.md for
// the per-algorithm warning rules.
//
// Returned slice ordering is deterministic — sorted by (Algorithm, Kind)
// per the no-map-iteration rule in determinism-standards.
func Validate(a, b string) []Warning {
    var warnings []Warning
    // Per-algorithm rule application — single pass over the 23 catalogue.
    // ... (the actual rule body — see VALIDATE-05)
    return warnings
}
```

**Deterministic sort key:** `sort.SliceStable` with `(Algorithm, Kind)` as the composite key — mirror `scan.Check` sort discipline cited in STACK.md.

**Four-section godoc template:** mandatory per `documentation-standards/SKILL.md` — every exported symbol in `validate.go` carries the **What / Common causes / Resolution / Example** block (same pattern as `errors.go:108-121`).

---

### 2.E — `Validate` tests (NEW files `validate_test.go`, `validate_bench_test.go`, `validate_fuzz_test.go`) — Cluster 2

#### `validate_test.go`

**Analog:** `errors_test.go:53-60` (sentinel-shape table-driven test) for the constant-shape sub-tests; `normalise_test.go` for the public-function behavioural sub-tests.

**Pattern excerpt — table-driven test (extend `errors_test.go` style):**

```go
// Apply the table-driven shape — Phase 8.5's WarnKind constants are
// exactly the same kind of fixed-set enumeration as the sentinel set:
var warnKindCases = []struct {
    kind WarnKind
    want string
}{
    {WarnEmptyInput, "EmptyInput"},
    {WarnUnequalLength, "UnequalLength"},
    {WarnNoTokensAfterNormalise, "NoTokensAfterNormalise"},
    {WarnAllNonASCIIDropped, "AllNonASCIIDropped"},
    {WarnPathologicallyLargeInput, "PathologicallyLargeInput"},
}

func TestWarnKind_String(t *testing.T) {
    for _, c := range warnKindCases {
        if got := c.kind.String(); got != c.want {
            t.Errorf("WarnKind(%d).String() = %q; want %q", int(c.kind), got, c.want)
        }
    }
}
```

#### `validate_bench_test.go`

**Analog:** `cosine_bench_test.go` lines 1-60 — typical benchmark file shape with `BenchmarkXxx_ASCII_Short/Medium/Long` plus alloc-budget assertion via `b.ReportAllocs()`.

#### `validate_fuzz_test.go`

**Analog:** `cosine_fuzz_test.go`. Key idiom — seed corpus + panic-free + range-bound assertions:

```go
// cosine_fuzz_test.go template — adapt for Validate (no float return; assert
// no panic + warnings slice is well-formed):
func FuzzValidate(f *testing.F) {
    f.Add("", "")
    f.Add("hello", "world")
    // ... more seeds ...
    f.Fuzz(func(t *testing.T, a, b string) {
        defer func() {
            if r := recover(); r != nil {
                t.Fatalf("Validate panicked on (%q, %q): %v", a, b, r)
            }
        }()
        ws := Validate(a, b)
        // assert ws is well-formed: every Warning has Algorithm in range,
        // every Kind in 0..4, Detail is valid UTF-8.
        for _, w := range ws {
            if int(w.Algorithm) < 0 || int(w.Algorithm) >= numAlgorithms {
                t.Errorf("warning has out-of-range AlgoID %d", int(w.Algorithm))
            }
            // ... more shape asserts ...
        }
    })
}
```

---

### 2.F — Q3 MongeElkan rename (atomic across 7 files) — Cluster 2

**No analog applies — Q3 is a directional flip, semantically unique.**

| Site | Current name + signature | Post-Q3 name + signature |
|---|---|---|
| `monge_elkan.go:377` | `func MongeElkanScore(a, b string, inner AlgoID, opts NormalisationOptions) float64` — asymmetric, inert `opts` | `func MongeElkanScoreAsymmetric(a, b string, inner AlgoID) float64` — asymmetric, no `opts` |
| `monge_elkan.go:467` | `func MongeElkanScoreSymmetric(a, b string, inner AlgoID, opts NormalisationOptions) float64` — symmetric, inert `opts` | `func MongeElkanScore(a, b string, inner AlgoID) float64` — symmetric default, no `opts` |

**22+ call-site ripple:** `monge_elkan_test.go`, `monge_elkan_bench_test.go` (6 sites), `monge_elkan_fuzz_test.go`, `cross_algorithm_consistency_test.go:737-766` (4 calls — semantic flip risk!), `example_test.go:434-495` (`ExampleMongeElkanScore` + `ExampleMongeElkanScoreSymmetric` — both need updating to swap meaning), `algoid_test.go:444`, `dispatch_monge_elkan.go:24-37, 70` (godoc + binding).

**Atomicity rule:** single commit, all 7+ files changed together. A partial commit with `MongeElkanScore` meaning asymmetric in some files and symmetric in others is a correctness landmine.

---

### 2.G — Q5 `PartialRatioScoreRunes` removal (atomic across 6 files) — Cluster 2

**No code analog — pure removal.** Sites:
- `partial_ratio.go` — delete function + rune-path implementation
- `dispatch_partial_ratio.go:20, 45` — godoc cleanup (no dispatch entry to remove; the function was public but not dispatch-bound)
- `partial_ratio_test.go` — remove tests
- `partial_ratio_fuzz_test.go:86-127` — remove `FuzzPartialRatioScoreRunes`
- `partial_ratio_bench_test.go:107-135` — remove 3 benchmarks
- `example_test.go:395-408` — remove `ExamplePartialRatioScoreRunes`
- `algoid_test.go:449`, `export_test.go:154` — clean up references

**Atomicity rule:** single commit.

---

### 3.A — Q11c Paper-anchored Philips 2000 test (NEW `double_metaphone_paper_test.go`) — Cluster 3

**Analog:** `double_metaphone_test.go:61-180 TestDoubleMetaphoneKeys_LanguageBranches` — the exact table-driven pattern with sub-tests per language branch.

**Pattern excerpt to copy** (`double_metaphone_test.go:61-86`):

```go
func TestDoubleMetaphoneKeys_LanguageBranches(t *testing.T) {
    t.Run("Germanic/Schmidt", func(t *testing.T) {
        p, s := fuzzymatch.DoubleMetaphoneKeys("Schmidt")
        if p != "XMT" || s != "SMT" {
            t.Errorf("DoubleMetaphoneKeys(\"Schmidt\") = (%q, %q); want (\"XMT\", \"SMT\") — Germanic gate FAILED",
                p, s)
        }
    })
    // ... more sub-tests per branch ...
}
```

**Q11c extension — explicit paper-page citation per case (REQUIRED):**

```go
// double_metaphone_paper_test.go — NEW pattern:
//
// TestDoubleMetaphone_PaperWorkedExamples pins the 10 worked examples from
// Lawrence Philips, "The Double Metaphone Search Algorithm", C/C++ Users
// Journal 18(6):38-43 (June 2000). Each sub-test cites the exact
// page/section where the rule is illustrated; the SWI-Prolog mirror
// (https://github.com/SWI-Prolog/packages-nlp/blob/master/double_metaphone.c)
// is the stable provenance source per double_metaphone.go:23.
//
// REQUIRED COVERAGE per Gap 6: at least one French "SAIS"-end name
// (e.g. "Sais", "Beaujolais", or "Bourgeois") to verify the locked Q9
// behaviour-preserving simplification at double_metaphone.go:744. If
// this sub-test produces ("S", "S") instead of ("S", ""), Q9 re-opens
// via mini-discuss-phase.

func TestDoubleMetaphone_PaperWorkedExamples(t *testing.T) {
    // Each entry: (input, expectedPrimary, expectedSecondary, paperSection)
    cases := []struct {
        input             string
        wantPrimary       string
        wantSecondary     string
        paperSection      string  // e.g. "Philips 2000 §2.1, p.39"
        swiPrologLineRef  string  // SWI-Prolog mirror line for cross-witness
    }{
        // Germanic
        {"Schmidt", "XMT", "SMT", "Philips 2000 §2.2, p.40", "double_metaphone.c:LXXX"},
        {"Smith", "SM0", "XMT", "Philips 2000 §2.2, p.40", "..."},
        // Greek
        {"Catherine", "K0RN", "KTRN", "Philips 2000 §2.3, p.40", "..."},
        // Slavic
        {"Wojcik", "...", "...", "Philips 2000 §2.4, p.41", "..."},
        // Romance
        {"Pacheco", "PXK", "", "Philips 2000 §2.5, p.41", "..."},
        // Special — French SAIS-end rule (Q9 + Gap 6 verification gate)
        {"Beaujolais", "PJL", "PJLS", "Philips 2000 §3.7 SAIS-rule, p.42", "double_metaphone.c:744 area"},
        // ... 4 more for the full 10-example target ...
    }
    for _, c := range cases {
        c := c
        t.Run(c.input, func(t *testing.T) {
            p, s := fuzzymatch.DoubleMetaphoneKeys(c.input)
            if p != c.wantPrimary || s != c.wantSecondary {
                t.Errorf("DoubleMetaphoneKeys(%q) = (%q, %q); want (%q, %q)\n"+
                    "  Paper source: %s\n  SWI-Prolog mirror: %s",
                    c.input, p, s, c.wantPrimary, c.wantSecondary,
                    c.paperSection, c.swiPrologLineRef)
            }
        })
    }
}
```

**Divergence from analog:** add `paperSection` + `swiPrologLineRef` fields to each test case — the existing `TestDoubleMetaphoneKeys_LanguageBranches` cites no per-case paper source. Q11c's gating purpose REQUIRES paper-page citations.

---

### 3.B — Q9 `dupBranchBody` removal — Cluster 3

**No code analog — single 5-line edit with inline comment.** Current site `double_metaphone.go:743-748`:

```go
// BEFORE — double_metaphone.go:743-748:
// French "SAIS" end
if i == n-1 && (at(i-1) == 'A' || at(i-1) == 'I') {
    dmAdd(&p, &alt, "S", "")
} else {
    dmAdd(&p, &alt, "S", "")
}

// AFTER (Q9 locked simplification):
// French "SAIS" end — both branches collapse to ("S", "") per Philips 2000
// §3.7 (the silent-secondary case). The conditional was a Phase 7
// transcription artefact; the rule's actual semantics map to a single
// dmAdd call regardless of the preceding character. Verified by
// TestDoubleMetaphone_PaperWorkedExamples (Q11c) — see Gap 6 for the
// re-open-on-divergence protocol.
dmAdd(&p, &alt, "S", "")
```

**HARD ORDERING:** Q11c (above) MUST pass green BEFORE this edit lands. Gap 6 dependency.

---

### 4.A — Q11b FMA-defeating double-cast (two sites) — Cluster 4

**Both sites are mirror images; the pattern is identical.**

#### Site 1 — `cosine.go:343`

```go
// BEFORE — cosine.go:341-344:
var dot float64
for _, k := range intersectionKeys {
    dot = (float64(qa[k]) * float64(qb[k])) + dot
}

// AFTER (Q11b locked double-cast):
var dot float64
for _, k := range intersectionKeys {
    dot = float64(float64(qa[k])*float64(qb[k])) + dot
}
```

**Comment block at `cosine.go:332-340` rewrite:** delete the FMA-hedging sentence ("parentheses do NOT prevent FMA fusion on arm64 (see golang/go#17895)") and replace with:

```go
// Dot product: explicit (x*y) + z parenthesisation per DET-06, plus an
// outer float64(...) wrap on the multiplication product to defeat
// compiler-emitted FMA on arm64 per the project-canonical FMA-defence
// pattern (RESEARCH.md §3.4, mirrored at scorer.go:380). The outer cast
// forces an intermediate rounding point that the FMA fusion cannot
// elide. golang/go#17895 documents why parentheses alone are not
// sufficient; this is the belt-and-braces remediation per Q11b.
```

#### Site 2 — `scorer.go:380`

```go
// BEFORE — scorer.go:380:
acc = acc + (entry.weight * score) //nolint:gocritic // DET-06 explicit-arithmetic locked pattern ...

// AFTER (Q11b locked double-cast — exact form per CONTEXT.md):
acc = float64(entry.weight*score) + acc //nolint:gocritic // DET-06 + Q11b FMA-defence locked pattern (RESEARCH.md §3.4)
```

**Rationale comment at `scorer.go:371-379` rewrite:** mirror the cosine.go new text — both sites carry inline rationale referencing each other so future readers see the symmetric pattern.

---

### 5.A — Q7a DoubleMetaphone `[4]byte` accumulator — Cluster 5

**Analog:** `double_metaphone.go:169-211 dmPrep` — the file's own `stackBuf [64]byte` pattern.

**Pattern excerpt to extend** (`double_metaphone.go:182-210`):

```go
var stackBuf [64]byte
n := 0
for i := 0; i < len(s); {
    r := s[i]
    if r < 0x80 {
        if (r >= 'a' && r <= 'z') || (r >= 'A' && r <= 'Z') {
            if n < 64 {
                if r >= 'a' {
                    stackBuf[n] = r - 32
                } else {
                    stackBuf[n] = r
                }
                n++
            } else {
                goto heapPath
            }
        }
        // ...
    }
}
return string(stackBuf[:n]) // one allocation: the returned string
```

**Q7a target — replace the `strings.Builder` accumulators for primary + secondary keys** with `[dmMaxLen]byte` + length counters. The exact line numbers need verification during planning — the current `strings.Builder` sites are around the algorithm body (use `grep -n "strings.Builder" double_metaphone.go` to find).

**Pattern excerpt for the replacement:**

```go
// BEFORE (conceptual — exact lines depend on current file):
var primary, secondary strings.Builder
primary.Grow(dmMaxLen)
secondary.Grow(dmMaxLen)
// ... primary.WriteString(target) ... secondary.WriteString(target) ...
return primary.String(), secondary.String()

// AFTER (Q7a [4]byte / [dmMaxLen]byte pattern):
var primaryBuf, secondaryBuf [dmMaxLen]byte
pn, sn := 0, 0
// ... dmAdd helper writes into primaryBuf[pn:]/secondaryBuf[sn:] and
//     bumps the counter ...
return string(primaryBuf[:pn]), string(secondaryBuf[:sn])
```

**Per CONTEXT.md:** saves 4 allocs per `DoubleMetaphoneKeys` call. Returned string values unchanged.

---

### 5.B — Q8b Tokenise ASCII fast path — Cluster 5 (the trickiest item)

**Analog (two-part):**
1. **ASCII gate + branch pattern:** `normalise.go:144-156`:

```go
func Normalise(s string, opts NormalisationOptions) string {
    if s == "" {
        return ""
    }
    if !opts.StripDiacritics && isASCII(s) {
        return normaliseASCII(s, opts)
    }
    return normaliseUnicode(s, opts)
}

// And the isASCII helper at normalise.go:161-168:
func isASCII(s string) bool {
    for i := 0; i < len(s); i++ {
        if s[i] >= 0x80 {
            return false
        }
    }
    return true
}
```

2. **Zero-copy substring + single scratch buffer pattern:** `double_metaphone.go:183-210` shows the `[N]byte` + `string(stackBuf[:n])` finaliser idiom.

**Q8b target shape (apply to `tokenise.go:143-199 Tokenise`):**

```go
func Tokenise(s string, opts TokeniseOptions) []string {
    if s == "" {
        return []string{}
    }
    // ASCII fast path: when the input is pure ASCII, tokens are emitted
    // as substrings of either s (when opts.Lowercase == false) or a
    // once-allocated lowercased scratch buffer (when opts.Lowercase ==
    // true). Both cases avoid the per-token []byte → string allocation
    // that the rune path takes.
    if isASCII(s) {
        return tokeniseASCII(s, opts)
    }
    // Existing rune-path implementation below ...
}

func tokeniseASCII(s string, opts TokeniseOptions) []string {
    var scratch []byte // lazily allocated only if opts.Lowercase
    if opts.Lowercase {
        scratch = make([]byte, len(s))
        for i := 0; i < len(s); i++ {
            b := s[i]
            if b >= 'A' && b <= 'Z' {
                b |= 0x20
            }
            scratch[i] = b
        }
    }
    source := s
    if opts.Lowercase {
        source = string(scratch) // one allocation total for the lowercased view
    }
    // Walk source via byte indices, emit tokens as source[lo:hi] substrings.
    tokens := make([]string, 0, 4)
    // ... boundary detection loop ...
    return tokens
}
```

**Property test REQUIRED (RESEARCH.md §3.5):**

```go
// PropTokenise_ASCIIFastPathEquivalent — assert byte-identical output
// between fast path and rune path on any ASCII input:
func TestProp_Tokenise_ASCIIFastPathEquivalent(t *testing.T) {
    // Use testing/quick with an ASCII-only string generator (Q12b
    // mixed-shape generators give the right input distribution).
    // For every generated ASCII s and every relevant TokeniseOptions,
    // assert tokeniseASCII(s, opts) == tokeniseRunePath(s, opts).
}
```

**Backing-array retention foot-gun:** document in the new `tokeniseASCII` godoc that the returned tokens retain a reference to the input string (or the scratch buffer) until all tokens are released. Same pattern as the q-gram backing-share Important finding flagged in Phase 8 review.

**BDD scenario REQUIRED:** add a scenario in `tests/bdd/features/normalisation.feature` (Gap 3 — new file) asserting the ASCII fast-path output equals the rune-path output.

---

### 5.C — Q7d Q-gram capacity hint — Cluster 5

**Analog:** in-file extension in `q_gram.go`.

```go
// BEFORE — q_gram.go (extractQGrams function, approximate):
m := make(map[string]int)
// ...

// AFTER (Q7d locked):
m := make(map[string]int, (len(s)-n+1)+(len(s)-n+1)/4)
// ^ 25 % capacity headroom — reduces map-rehash probability at Medium input
// size. Per docs/requirements.md §14.1 and Phase 8 algorithm-performance
// review Important findings.
```

---

### 6.A — 9 missing `TestGolden_*_Staging` functions — Cluster 6 (Q13)

**Analog:** `algorithms_golden_test.go:220-280` — the existing 14 staging templates.

**Mechanical template** (`algorithms_golden_test.go:232-280`):

```go
// buildHammingStagingEntries returns the four Hamming entries used by
// TestGolden_Hamming_Staging. ExpectedScore is computed from the current
// implementation so the staging file stays in sync with actual output.
func buildHammingStagingEntries(t *testing.T) []goldenAlgorithmEntry {
    t.Helper()
    return []goldenAlgorithmEntry{
        {
            Name:          "Hamming_empty_empty",
            Algorithm:     "Hamming",
            A:             "",
            B:             "",
            ExpectedScore: fuzzymatch.HammingScore("", ""),
        },
        // ... 3 more entries ...
    }
}

// TestGolden_Hamming_Staging produces testdata/golden/_staging/hamming.json
// for plan 02-07's merge step. Entries are sorted alphabetically by Name.
//
// Run with `-update` to create or refresh the staging file.
// Re-running without `-update` must exit 0 (file is byte-stable).
func TestGolden_Hamming_Staging(t *testing.T) {
    entries := buildHammingStagingEntries(t)
    sort.Slice(entries, func(i, j int) bool {
        return entries[i].Name < entries[j].Name
    })
    file := goldenAlgorithmsFile{Version: 1, Entries: entries}
    assertGoldenStaging(t, "_staging/hamming.json", file)
}
```

**Apply mechanically to the 9 missing algorithms:** DoubleMetaphone, MongeElkan, MRA, NYSIIS, PartialRatio, Soundex, TokenJaccard, TokenSetRatio, TokenSortRatio. The staging JSON files already exist in `testdata/golden/_staging/` — the function bodies must produce output that byte-matches those existing files. Verify with `-update` then diff against the committed JSON.

**Note on PartialRatio:** after Q5 lands (removal of `PartialRatioScoreRunes`), the `TestGolden_PartialRatio_Staging` function MUST NOT reference the removed rune variant. Build entries from `PartialRatioScore` only.

---

### 6.B — Cross-validation generators (3 new Python scripts) — Cluster 6 (Q10)

**Analog:** `scripts/gen-token-ratio-cross-validation.py` (entire file). The script is the canonical template.

**Reusable structural elements (extract from `gen-token-ratio-cross-validation.py`):**

1. **License header** (lines 1-14): Apache 2.0 header.
2. **Module docstring** (lines 16-112): module purpose, reference-impl citation, version-pin gate description, tokeniser-divergence handling, OQ-N resolutions, usage, requirements, determinism.
3. **Version-pin constant + assert** (lines 122-134):

```python
RAPIDFUZZ_VERSION = "3.14.5"
import rapidfuzz  # noqa: E402 — imported after the version constant so the assert message can reference it
assert rapidfuzz.__version__ == RAPIDFUZZ_VERSION, (
    f"rapidfuzz version mismatch: installed {rapidfuzz.__version__}, "
    f"script pinned to {RAPIDFUZZ_VERSION} — "
    f"run: python3 -m pip install --user rapidfuzz=={RAPIDFUZZ_VERSION}"
)
```

4. **`CASES` deterministic ordered list of dicts** (~lines 138-200): one dict per input pair with `name`, `a`, `b`, optional flags.

5. **Per-case score function** (lines 227-287):

```python
def score_case(case: dict) -> dict:
    a, b = case["a"], case["b"]
    # ... compute reference scores ...
    return {
        "name": case["name"],
        "a": a,
        "b": b,
        "<algo>_score": ...,
        # ...
    }
```

6. **`_check_python_version()` helper** (lines 290-309) — guards against pre-3.7 (dict insertion-order pre-guarantee).

7. **`main()` function** (lines 312-336):

```python
def main() -> None:
    _check_python_version()
    for case in CASES:
        _assert_corpus_is_tokenise_safe(case)  # or equivalent
    entries = [score_case(c) for c in CASES]
    out = {
        "version": 1,
        "_metadata": {
            "<lib>_version": LIB_VERSION,
            "python_version": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
            "regenerated_at": datetime.now(timezone.utc).isoformat(),
        },
        "entries": entries,
    }
    path = "testdata/cross-validation/<corpus>/vectors.json"
    os.makedirs(os.path.dirname(path), exist_ok=True)
    with open(path, "w") as f:
        json.dump(out, f, indent=2, sort_keys=False)
        f.write("\n")  # trailing LF matches Phase 2 golden-file canonical convention
```

**Per Q10 — apply this template thrice:**

| Script | Reference lib + pin | Algorithms produced |
|---|---|---|
| `scripts/gen-character-cross-validation.py` | `jellyfish==1.2.1` (already pinned for phonetic) | Levenshtein, Jaro, JaroWinkler, DamerauLevenshteinOSA |
| `scripts/gen-qgram-cross-validation.py` | `py_stringmatching==latest-stable` (verify version) | Q-Gram Jaccard, Sørensen-Dice, Cosine, Tversky |
| `scripts/gen-monge-elkan-cross-validation.py` | `py_stringmatching==latest-stable` | Monge-Elkan with JaroWinkler inner + Levenshtein inner |

**Pitfall:** Python is NOT a runtime dep — the committed `vectors.json` is consumed by the Go loader. Python is only invoked during regeneration.

---

### 6.C — Cross-validation Go loader tests (3 new files) — Cluster 6 (Q10)

**Analog:** `token_ratio_cross_validation_test.go` (entire file).

**Structural elements to copy:**

1. **File header** (lines 1-50): copyright + module docstring naming generator script, version-pin, tolerance, wave activation history.
2. **`Epsilon` constant** (line 68):

```go
const tokenRatioCrossValidationEpsilon = 1e-9
```

3. **Entry struct + metadata struct + corpus struct** (lines 76-104):

```go
type tokenRatioEntry struct {
    Name              string   `json:"name"`
    A                 string   `json:"a"`
    B                 string   `json:"b"`
    TokenSortRatio    *float64 `json:"token_sort_ratio"`
    // ... pointer-to-float64 lets the loader distinguish JSON null from 0.0 ...
}
type tokenRatioMetadata struct {
    RapidFuzzVersion string `json:"rapidfuzz_version"`
    PythonVersion    string `json:"python_version"`
    RegeneratedAt    string `json:"regenerated_at"`
}
type tokenRatioCorpus struct {
    Version  int                `json:"version"`
    Metadata tokenRatioMetadata `json:"_metadata"`
    Entries  []tokenRatioEntry  `json:"entries"`
}
```

4. **Test function** (lines 106-160):

```go
func TestTokenRatios_CrossValidation(t *testing.T) {
    path := filepath.Join("testdata", "cross-validation", "token-ratios", "vectors.json")
    raw, err := os.ReadFile(path)
    if err != nil {
        t.Fatalf("TestTokenRatios_CrossValidation: read %s: %v (regenerate with `make regen-token-ratio-cross-validation`)", path, err)
    }
    var c tokenRatioCorpus
    if err := json.Unmarshal(raw, &c); err != nil {
        t.Fatalf("...: parse %s: %v", path, err)
    }
    if c.Version != 1 {
        t.Fatalf("...: unsupported corpus version %d (want 1)", c.Version)
    }
    if c.Metadata.RapidFuzzVersion == "" {
        t.Fatalf("...: corpus missing _metadata.rapidfuzz_version")
    }
    // ... python_version parse + >= 3.7 assertion ...
    for _, e := range c.Entries {
        e := e
        t.Run(e.Name+"/algo_name", func(t *testing.T) {
            if e.AlgoField == nil {
                t.Skip("entry missing algo_field — corpus regeneration needed")
            }
            got := fuzzymatch.AlgoNameScore(e.A, e.B)
            delta := math.Abs(got - *e.AlgoField)
            if delta > epsilon {
                t.Errorf("AlgoNameScore(%q, %q) = %.17g; reference = %.17g (delta %.2e, tol %g, lib %s, python %s)",
                    e.A, e.B, got, *e.AlgoField, delta, epsilon,
                    c.Metadata.LibVersion, c.Metadata.PythonVersion)
            }
        })
    }
}
```

**Apply thrice** — one loader file per corpus (`character_cross_validation_test.go`, `qgram_cross_validation_test.go`, `monge_elkan_cross_validation_test.go`).

---

### 6.D — Q12a AST-based coverage Floor 3 helper — Cluster 6 + 7

**Analog:** `ai_friendly_test.go:96-173` — the `collectExportedSymbols` + `collectExportedFromFile` + `collectExportedFromSpec` triple.

**Pattern excerpt to extract** (`ai_friendly_test.go:96-132`):

```go
func collectExportedSymbols(t *testing.T) []string {
    t.Helper()
    fset := token.NewFileSet()
    filter := func(info os.FileInfo) bool {
        return !strings.HasSuffix(info.Name(), "_test.go")
    }
    pkgs, err := parser.ParseDir(fset, ".", filter, parser.SkipObjectResolution)
    if err != nil {
        t.Fatalf("parse root package: %v", err)
    }
    seen := make(map[string]struct{})
    for _, pkg := range pkgs {
        if pkg.Name != "fuzzymatch" {
            continue
        }
        for _, f := range pkg.Files {
            collectExportedFromFile(f, seen)
        }
    }
    names := make([]string, 0, len(seen))
    for name := range seen {
        names = append(names, name)
    }
    sort.Strings(names)
    return names
}
```

**Q12a target — extract this into a standalone Go program at `scripts/cmd/verify-exported-coverage/main.go`** that:
1. Walks the root package via `go/parser.ParseDir` (the analog).
2. Collects every exported func/type/var/const name.
3. Parses `coverage.out` via `go tool cover -func=coverage.out` (subprocess) OR by reading the raw profile lines.
4. Asserts every exported func has ≥ 90 % statement coverage.

**Shared with Q13's `verify-llms-sync`:** the same AST-walking surface implements both Q12a and the `make verify-llms-sync` target. Recommend extracting into a shared package (e.g. `scripts/cmd/internal/astwalker/astwalker.go`) and invoking from both `scripts/cmd/verify-exported-coverage/main.go` and `scripts/cmd/verify-llms-sync/main.go`.

---

### 6.E — Meta-tests (3 new files) — Cluster 6 (RESEARCH.md §6.E)

**Analog for all three:** `ai_friendly_test.go` (entire file).

#### `internal_coverage_test.go` (NEW)

Asserts every internal helper has at least one test reference. AST walks the root + builds the call graph; cross-references with `coverage.out`. Pattern: same `parser.ParseDir` + `seen` map as `ai_friendly_test.go`.

#### `readme_shop_front_test.go` (NEW)

Asserts README Quick Start code blocks compile + match an `Example*` runnable test. Pattern: regex-extract code blocks from `README.md`, run `go vet`-equivalent on each, AND for each block assert there's a corresponding entry in `example_test.go`.

```go
// Pattern shape (inspired by ai_friendly_test.go):
func TestReadme_QuickStartMatchesRunnableExample(t *testing.T) {
    readme, err := os.ReadFile("README.md")
    if err != nil {
        t.Fatalf("read README.md: %v", err)
    }
    // Extract fenced `go` code blocks ...
    // For each, ensure example_test.go has a matching Example* function ...
}
```

#### `documentation_test.go` (NEW — VALIDATE-06 meta-test)

Asserts all 6 Validate doc surfaces are populated:
1. README mentions `Validate(`
2. `docs/algorithms.md` has a `## Validate` (or similar) section
3. 23 algorithm files each contain `fuzzymatch.Validate` in their godoc
4. `llms.txt` lists `Validate`
5. `llms-full.txt` documents `Validate` signature + `Warning` + `WarnKind`
6. `examples/validate-input-quality/main.go` exists

Pattern: `os.ReadFile` each surface, `strings.Contains` for each marker.

---

### 6.F — `FuzzScorer_*` harnesses + 9 rune-variant fuzz harnesses — Cluster 6

**Analog:** `cosine_fuzz_test.go` (entire file).

```go
// cosine_fuzz_test.go pattern (typical fuzz harness shape):
func FuzzCosineScore(f *testing.F) {
    seeds := []struct{ a, b string }{
        {"", ""},
        {"hello", "world"},
        // ... reference vectors from cosine_test.go ...
    }
    for _, s := range seeds {
        f.Add(s.a, s.b)
    }
    f.Fuzz(func(t *testing.T, a, b string) {
        defer func() {
            if r := recover(); r != nil {
                t.Fatalf("CosineScore panicked on (%q, %q): %v", a, b, r)
            }
        }()
        got := fuzzymatch.CosineScore(a, b, 3)
        if math.IsNaN(got) || math.IsInf(got, 0) {
            t.Errorf("CosineScore(%q, %q, 3) = %g; want a finite number", a, b, got)
        }
        if got < 0.0 || got > 1.0 {
            t.Errorf("CosineScore(%q, %q, 3) = %g; want in [0, 1]", a, b, got)
        }
    })
}
```

**Apply this template:**
- `scorer_fuzz_test.go` (NEW) — at minimum `FuzzScorer_Score` + `FuzzScorer_Match` + `FuzzScorer_ScoreAll`
- 9 rune-variant fuzz harnesses — adapt to call `*Runes` variants where they exist (`LevenshteinScoreRunes`, `JaroScoreRunes`, etc.). Per Q5, ASCII-only algorithms have NO rune variants — exclude Strcmp95, Soundex, DoubleMetaphone, NYSIIS, MRA, and the token-tier.

---

### 7.A — `.github/workflows/release.yml` — Cluster 7 (Q13 `needs: [ci]`)

**Analog:** `.github/workflows/release.yml` (current file).

```yaml
# BEFORE — jobs.release section:
jobs:
  release:
    name: goreleaser + cosign + attest
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
      attestations: write
      packages: read
    # ...

# AFTER (Q13):
jobs:
  release:
    name: goreleaser + cosign + attest
    runs-on: ubuntu-latest
    needs: [ci]  # Q13: gate release on a green CI run on the same SHA
    permissions:
      contents: write
      # ...
```

**Caveat:** `release.yml` is on push-tag trigger; `ci.yml` is on PR/push-main trigger. Verify `needs: [ci]` resolves to the most-recent CI run on the tagged SHA — if not, alternative is a workflow-level `if:` check using `gh api` to verify the SHA has a green CI run, or rely on branch-protection rules. The planner confirms the exact mechanism during planning.

---

### 7.B — `.github/workflows/nightly.yml` (NEW) — Cluster 7 (Q13)

**Analog:** `.github/workflows/ci.yml` lines 1-90 — matrix structure, SHA pinning conventions, `CGO_ENABLED=0` workflow-level env.

**Pattern excerpt to model** (`ci.yml:1-90`):

```yaml
name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

env:
  CGO_ENABLED: "0"

jobs:
  quality:
    name: "make check (${{ matrix.label }})"
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            arch: amd64
            label: linux-amd64
          # ... 4 more entries for the 5-platform matrix ...
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      # ...
```

**`nightly.yml` extensions** (RESEARCH.md §3.7):
- `on: schedule: - cron: "0 3 * * *"` (3 AM UTC daily)
- Long-form fuzz: per-fuzzer `go test -run=^$ -fuzz=Fuzz<Name> -fuzztime=5m ./...`
- Benchstat regression on self-hosted runner (`runs-on: [self-hosted, fuzzymatch-bench]` with `ubuntu-latest` fallback)
- Auto-PR for new fuzz corpus entries via `peter-evans/create-pull-request@<SHA>`
- All third-party action refs SHA-pinned per Q13

---

### 7.C — SHA-pin all action references — Cluster 7 (Q13)

**Pattern:** mechanical text replacement. Current files (`ci.yml`, `release.yml`, `codeql.yml`, `security.yml`, `commitlint.yml`, `cla.yml`, `license-headers.yml`, and the NEW `nightly.yml`) use `@vN` major-tag pins. Replace each with `@<40-char-SHA>  # vN.x.y` comment.

```yaml
# BEFORE:
- uses: actions/checkout@v6

# AFTER:
- uses: actions/checkout@<commit-sha>  # v6.x.y
```

Use `gh api repos/{owner}/{repo}/git/refs/tags/{tag}` to resolve each tag → SHA before committing.

---

### 8.A — `docs/algorithms.md` full fill-in — Cluster 8

**Analog:** `docs/scorer.md` and `docs/tuning.md` (sister docs in the same directory).

**Read both files to extract structural patterns:**
- Per-algorithm `## <AlgoName>` H2 anchor (hyphenated casing per Q13)
- Sub-sections: What it computes / When to use / When NOT to use / Performance characteristics / Examples / Edge cases / References
- British English throughout
- Runnable code examples cross-link to `example_test.go` entries

**Q12c addition** (in `docs/algorithms.md`): new section "## Panic surface" enumerating every public function that panics + the sentinel each panic carries. Source: `errors.go` + each algorithm file's direct-call panic sites.

**Q14a addition** (in `docs/algorithms.md#performance-characteristics`): 5 MB/call expected-behaviour ceiling note per RESEARCH.md §3.9.

---

### 8.B — `Validate` documentation across 6 surfaces — Cluster 8 (Q4)

**Analog set (Phase 8 Scorer work documented across the same six-surface set):**

| Surface | Analog from Phase 8 (Scorer) | Q4 Validate target |
|---|---|---|
| 1. README Quick Start | README §"Quick Start" (existing — currently shows Scorer pattern) | Add ~30 lines: "Validate-then-Score" sub-pattern |
| 2. `docs/algorithms.md` | `docs/scorer.md` (entire file, ~340 lines) | NEW dedicated section "## Validate", ~150 lines |
| 3. Per-algorithm godoc cross-ref | godoc on `DefaultScorer` in scorer.go (~3 lines pointing to scorer.md) | 23 algorithm files × ~3 lines each |
| 4. `llms.txt` + `llms-full.txt` | existing Scorer entries in both files | Add Validate / Warning / WarnKind entries |
| 5. User-guide section | `docs/scorer.md` "When to use" + "Configuration" sub-sections | `docs/best-practices.md` (likely new file) "Validate" section |
| 6. Runnable example | `examples/scorer-composition/main.go` | NEW `examples/validate-input-quality/main.go` |

**Per-algorithm godoc cross-ref template** (apply to all 23 algorithm files):

```go
// (At the bottom of each algorithm's primary public function godoc:)
//
// For programmatic input-quality checks before scoring (e.g. detecting
// empty input, unequal-length Hamming inputs, all-non-ASCII inputs that
// will produce empty phonetic codes), see [fuzzymatch.Validate].
```

**Example program analog** (use exactly): `examples/scorer-composition/main.go` (full file, ~140 lines) — copy the package-level structure (license header, package doc, var block, helper function, `main()`).

---

### 8.C — `examples/validate-input-quality/main.go` (NEW) — Cluster 8 + 2

**Analog:** `examples/scorer-composition/main.go`. Full file structure to mirror:

```go
// Copyright header + package doc explaining what the example demonstrates.
package main

import (
    "fmt"
    "github.com/axonops/fuzzymatch"
)

// pairs := [][2]string{...}
// var thingS = fuzzymatch.DefaultScorer()
// var minusS = ...
//
// func main() {
//     // Print header.
//     // For each pair, print Validate results + Score + Match.
// }
```

**Companion `main_test.go`** (NEW) — analog `examples/scorer-composition/main_test.go` (the os.Pipe stdout-capture pattern). Critical excerpt:

```go
// main_test.go pattern:
const want = `... committed exact bytes ...`

func TestExample_Output(t *testing.T) {
    origStdout := os.Stdout
    r, w, err := os.Pipe()
    if err != nil { t.Fatalf(...) }
    os.Stdout = w
    defer func() { os.Stdout = origStdout }()
    main()
    if err := w.Close(); err != nil { ... }
    var buf bytes.Buffer
    if _, err := io.Copy(&buf, r); err != nil { ... }
    // ... diff against want ...
}
```

**Companion `go.mod`** (NEW) — mirror `examples/scorer-composition/go.mod`. Separate module so the example is a true standalone consumer of the root module.

---

### 9.A — `var _ = func() bool {...}()` dispatch-init refactor (23 files) — Cluster 9 (Q14b)

**Current state** (`dispatch_cosine.go:41-46`):

```go
// _ ensures dispatch[AlgoCosine] is populated before any call to the
// Scorer (Phase 8) or Extract (Phase 10) that reads the dispatch
// table. Default n = 3 trigram per CONTEXT.md Deferred §4.
var _ = func() bool {
    dispatch[AlgoCosine] = func(a, b string) float64 {
        return CosineScore(a, b, 3)
    }
    return true
}()
```

**RESEARCH.md §3.9 recommendation: option (A) explicit `init()` with nolint annotation.** This is the planner's recommended default — least disruptive, idiomatic Go.

**Refactor target shape (canonical for all 23 files):**

```go
// init registers dispatch[AlgoCosine] at package load time. This is the
// canonical Go idiom for package-level dispatch registration. The
// function performs zero I/O, no goroutines, and no time-dependent
// logic — it writes a single function-pointer entry into a
// pre-allocated array. Determinism-reviewer's "no init-time side
// effects" rule allows this pattern (per RESEARCH.md Assumption A2);
// the prior `var _ = func() bool {...}()` form was functionally
// identical but obscured intent.
//
// Default n = 3 trigram per CONTEXT.md Deferred §4.
func init() { //nolint:gochecknoinits // canonical dispatch-table registration; see godoc above
    dispatch[AlgoCosine] = func(a, b string) float64 {
        return CosineScore(a, b, 3)
    }
}
```

**Apply mechanically to all 23 `dispatch_*.go` files.** Comment block at top of each file updates to reflect the new pattern (drop the "var _ = func() bool { ... }() idiom is the canonical Phase-2-onward form" sentences in `dispatch_cosine.go:32-34` and equivalents).

**Pitfall (Assumption A2 in RESEARCH.md):** if determinism-reviewer rejects even `init()` for "no init-time side effects", escalate to user during planning — option (B) compile-time dispatch table via `go generate` is the fallback. The planner should consult `determinism-reviewer` agent before committing the refactor.

---

### 9.B — `WriteGoldenFile` unexport — Cluster 9 (Q14b)

**Analog:** `export_test.go:32` (existing test-only re-export pattern).

**Current pattern in `export_test.go:24-32`:**

```go
// CanonicalMarshalForTest exposes the unexported canonicalMarshal helper
// to the external (black-box) fuzzymatch_test package so that
// golden_canonical_test.go can assert the locked v1.x byte contract
// without dragging canonicalMarshal into the public API.
//
// Do not use this in production code — it does not exist outside of
// _test.go compilation. Use WriteGoldenFile (the public test-maintenance
// wrapper) instead.
var CanonicalMarshalForTest = canonicalMarshal
```

**Q14b target:**

1. In `golden_canonical.go:75-100`, rename `func WriteGoldenFile` → `func writeGoldenFile` (lowercase). The function body is unchanged.
2. In `export_test.go`, add the test-only re-export:

```go
// WriteGoldenFile re-exports the unexported writeGoldenFile helper to the
// external (black-box) fuzzymatch_test package so that
// algorithms_golden_test.go's -update workflow can write golden files
// without dragging writeGoldenFile into the public API.
//
// Do not use this in production code — it does not exist outside of
// _test.go compilation.
var WriteGoldenFile = writeGoldenFile
```

3. The `internalAllowlist` entry in `ai_friendly_test.go:50-52` becomes obsolete (WriteGoldenFile is no longer a root-package exported symbol). Remove the entry.

4. Call sites in `_test.go` files (5 sites per RESEARCH.md §3.9) keep using `fuzzymatch.WriteGoldenFile(...)` — the test-only re-export keeps the same package-qualified name, so no caller-side edits.

---

### 9.C — BDD step extension for 3 sentinels — Cluster 9 (Gap 2)

**Analog:** `tests/bdd/steps/scorer_steps.go:326-347 constructingTheScorerShouldReturn` (the existing switch).

**Pattern excerpt to extend** (`tests/bdd/steps/scorer_steps.go:326-347`):

```go
func (sc *ScorerContext) constructingTheScorerShouldReturn(errName string) error {
    if sc.lastErr == nil {
        return fmt.Errorf("expected error %s, got nil", errName)
    }
    var target error
    switch errName {
    case "ErrMissingThreshold":
        target = fuzzymatch.ErrMissingThreshold
    case "ErrEmptyScorer":
        target = fuzzymatch.ErrEmptyScorer
    case "ErrInvalidWeight":
        target = fuzzymatch.ErrInvalidWeight
    case "ErrInvalidThreshold":
        target = fuzzymatch.ErrInvalidThreshold
    default:
        return fmt.Errorf("unknown sentinel name %q (supported: ErrMissingThreshold, ErrEmptyScorer, ErrInvalidWeight, ErrInvalidThreshold)", errName)
    }
    if !errors.Is(sc.lastErr, target) {
        return fmt.Errorf("expected errors.Is(err, %s), got err = %v", errName, sc.lastErr)
    }
    return nil
}
```

**Gap 2 target — add 3 case branches:**

```go
// Inside the switch (alphabetical order maintained):
case "ErrInvalidAlgoID":  // post-Gap-4 rename
    target = fuzzymatch.ErrInvalidAlgoID
case "ErrInvalidQGramSize":
    target = fuzzymatch.ErrInvalidQGramSize
case "ErrInvalidTverskyParam":
    target = fuzzymatch.ErrInvalidTverskyParam
```

**Update the `default` case error message** to list the new 7 supported sentinels.

**Pre-condition: Gap 4 rename MUST land first** (the case branch references `ErrInvalidAlgoID`, which is the post-rename name).

---

### 9.D — New BDD scenarios for the 3 sentinels (`scorer.feature` extension) — Cluster 9 (Gap 2)

**Analog:** `tests/bdd/features/scorer.feature:105-127` — the 3 existing sentinel scenarios.

**Pattern excerpt** (`scorer.feature:105-127`):

```gherkin
  Scenario: NewScorer without WithThreshold returns ErrMissingThreshold
    ...
    When I attempt to construct a Scorer with Levenshtein weight 1.0 and no threshold
    Then constructing the Scorer should return ErrMissingThreshold

  Scenario: NewScorer without algorithms returns ErrEmptyScorer
    ...
    When I attempt to construct a Scorer with threshold 0.5 and no algorithms
    Then constructing the Scorer should return ErrEmptyScorer

  Scenario: WithAlgorithm with negative weight returns ErrInvalidWeight
    ...
    When I attempt to construct a Scorer with Levenshtein weight -0.5 and threshold 0.5
    Then constructing the Scorer should return ErrInvalidWeight
```

**Add three Gap 2 scenarios** (require companion step regex if not covered by existing `iAttemptToConstructAScorerWith*`):

```gherkin
  Scenario: WithAlgorithm with out-of-range AlgoID returns ErrInvalidAlgoID
    Given a Scorer construction with an out-of-range AlgoID
    When I attempt to construct the Scorer
    Then constructing the Scorer should return ErrInvalidAlgoID

  Scenario: WithQGramJaccardAlgorithm with n=0 returns ErrInvalidQGramSize
    When I attempt to construct a Scorer with QGramJaccard n=0 and threshold 0.5
    Then constructing the Scorer should return ErrInvalidQGramSize

  Scenario: WithTverskyAlgorithm with alpha+beta<=0 returns ErrInvalidTverskyParam
    When I attempt to construct a Scorer with Tversky alpha=0 beta=0 n=3 and threshold 0.5
    Then constructing the Scorer should return ErrInvalidTverskyParam
```

**Companion: new step regexes** in `scorer_steps.go` for the three new `When` clauses (the analog is the existing `iAttemptToConstructAScorerWithLevenshteinWeightAndThreshold` at lines 313-320).

---

### 9.E — New BDD feature files `normalisation.feature` + `determinism.feature` — Cluster 9 (Gap 3)

**Analog:** `tests/bdd/features/scorer.feature` (file structure only — Background, Feature, Scenario blocks). No semantic analog exists for `normalisation`-scope or `determinism`-scope scenarios.

**Per Gap 3 split** (planner confirms with user):
- `normalisation.feature` + `determinism.feature` land in 8.5
- `scan.feature` + `suppression.feature` DEFERRED to Phase 9 (acknowledged in verification report)

---

## 3. Shared Patterns

These cross-cutting patterns apply to multiple new files. Apply consistently.

### Shared — Apache 2.0 license header

**Source:** `errors.go:1-13` (and every other Go file).

```go
// Copyright 2026 AxonOps Limited
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
```

**Apply to every NEW Go/Python file in Phase 8.5.** Enforced by `make verify-license-headers` and `scripts/verify-license-headers.sh`.

### Shared — Four-section sentinel godoc template

**Source:** `errors.go:108-121`.

```go
// Err<Name> indicates <one-line "What" condition>.
//
// <2-3 line "Common causes" paragraph — what user mistake produces it.>
//
// <2-3 line "Resolution" paragraph — what the user does to fix it.>
//
// Returned by <function/option> at <when>. <"Example" — a one-line idiom.>
//
// Discriminate via errors.Is(err, fuzzymatch.Err<Name>); never
// match the error message string.
var Err<Name> = errors.New("fuzzymatch: <lowercase no-trailing-punctuation message>")
```

**Apply to:** `ErrInvalidQGramSize`, `ErrInvalidTverskyParam`, `ErrInvalidAlgoID` (post-rename), `ErrInvalidInnerAlgo` (NEW), `ErrInternalInvariantViolated` (NEW).

### Shared — British English in prose

**Source:** every doc file. Examples: `normalisation` (not `normalization`), `colour` (not `color`), `behaviour` (not `behavior`), `serialise` (not `serialize`).

**Apply to:** every doc edit in Cluster 8. Existing American/British mixed-spelling Important findings (Phase 8 docs-writer) normalise to British in 8.5.

### Shared — Stdlib `testing` only in root tests

**Source:** CLAUDE.md "Architecture" + `go-coding-standards/SKILL.md`. No `testify`, no `assert`, no `require` in any root-package `*_test.go` file.

**Apply to:** every new test file in Phase 8.5: `validate_test.go`, `validate_bench_test.go`, `validate_fuzz_test.go`, `double_metaphone_paper_test.go`, `character_cross_validation_test.go`, `qgram_cross_validation_test.go`, `monge_elkan_cross_validation_test.go`, `scorer_fuzz_test.go`, 9 × rune-variant fuzz files, 3 × meta-test files. `testify` is permitted ONLY in `tests/bdd/steps/*.go` (per `tests/bdd/go.mod`).

### Shared — `t.Helper()` + clear failure messages

**Source:** every existing root test file. Pattern: `t.Helper()` at top of any helper that calls `t.Errorf`/`t.Fatalf`; failure messages include input values + expected vs got + reproduction hint.

### Shared — Determinism rules (no map iteration on output paths)

**Source:** `algoid.go:269-308 AlgoIDs()` returning a hand-written slice (NOT iterating a map). `validate.go` MUST follow: sort warnings by `(Algorithm, Kind)` via `sort.SliceStable` with a complete sort key.

### Shared — Cross-platform CI matrix discipline

**Source:** `.github/workflows/ci.yml:32-53` (5-platform matrix). The bench.txt regeneration step (Cluster 7) MUST be run after Q11b lands and verified against the 5-platform matrix for byte-identity on golden files.

### Shared — Issue references in commits — N/A

**Source:** project user memory (`no_github_issues.md`). Skip `(#N)` issue references in commits per the project's no-GH-issues state. The Q11d `partial_ratio.go:148` TODO removes the `(#TBD)` placeholder; do not replace with a real issue ref.

### Shared — Conventional-commit prefixes

**Source:** CLAUDE.md "Branching & Commits" + `commit-standards/SKILL.md`. Phase 8.5 commits use `feat:`, `fix:`, `test:`, `docs:`, `chore:`, `refactor:`, `perf:`. The Q3 MongeElkan rename and Q5 PartialRatioScoreRunes removal are `feat!:` (breaking-change marker).

### Shared — Agent gate wiring (per CLAUDE.md)

| Cluster | Required agent gates |
|---|---|
| 2 (Q3, Q5, Q4 rename) | `api-ergonomics-reviewer` (atomic rename surface), `commit-message-reviewer` (skip issue-ref findings per memory) |
| 2 (Q4 Validate) | `api-ergonomics-reviewer`, `docs-writer`, `user-guide-reviewer` (all six surfaces) |
| 3 (Q9 + Q11c) | `algorithm-correctness-reviewer` (paper-anchored citations + Gap 6 verification), `algorithm-licensing-reviewer` (no Philips C++ code copied) |
| 4 (Q11b) | `determinism-reviewer` (FMA-defence pattern), cross-platform CI matrix re-run |
| 5 (Q7a, Q8b, Q7d, Q8c) | `algorithm-performance-reviewer` (alloc-budget verification), `algorithm-correctness-reviewer` (Q8b property-test equivalence) |
| 6 (Q10) | `algorithm-licensing-reviewer` (jellyfish + py_stringmatching licence audit), `test-writer`, `algorithm-correctness-reviewer` |
| 6 (Q12a, Q12b, Q11c) | `test-analyst`, `determinism-reviewer` (Q12b NaN/Inf handling) |
| 7 (Q13) | `devops` (release.yml + nightly.yml + SHA pinning), `security-reviewer` (govulncheck + gosec) |
| 8 | `docs-writer`, `user-guide-reviewer` (mandatory for Validate's 6 surfaces) |
| 9 (Q14b) | `code-reviewer` (Improvement sweep), `determinism-reviewer` (dispatch refactor), `go-quality` (final gate) |
| All | `go-quality` (golangci-lint v2 + vet + tidy), `security-reviewer` (govulncheck weekly + per-PR) |

---

## 4. No Analog Found

These items have no close in-tree analog. The planner uses RESEARCH.md and external references instead.

| File | Role | Reason | Substitute |
|---|---|---|---|
| `validate.go` core function body | algorithm impl — diagnostic surface | No existing function returns `[]Warning`; closest are `Tokenise` (returns `[]string`) and `ScoreAll` (returns map) | Build per-algorithm rule table per RESEARCH.md VALIDATE-05; sort output by `(Algorithm, Kind)` per `sort.SliceStable`; no panics |
| `tests/bdd/features/normalisation.feature` | BDD feature file | No `normalisation`-scope BDD scenarios exist in tree; per Gap 3 NEW file | Use `scorer.feature` for Gherkin structure only; semantic content per `normalise.go` godoc + property tests |
| `tests/bdd/features/determinism.feature` | BDD feature file | No `determinism`-scope BDD scenarios; per Gap 3 NEW file | Use `scorer.feature` for Gherkin structure; semantic content per `determinism-standards/SKILL.md` + golden-file tests |
| `tests/bdd/features/validate.feature` | BDD feature file | NEW for Q4 VALIDATE coverage | Use `scorer.feature` for Gherkin structure; one scenario per WarnKind |
| `.planning/phases/08.5-review-remediation-gate/08.5-LICENCE-AUDIT.md` | project meta | NEW for Q13 HashiCorp licence audit result | Free-form Markdown documenting `find . -name '*.go' \| xargs grep -l "HashiCorp\|hashicorp\|MPL"` result + one-paragraph conclusion |

---

## 5. Hard Ordering Dependencies (from RESEARCH.md §4)

These ordering constraints MUST be encoded in the planner's task DAG. Cited here so PATTERNS.md is a complete planning input.

1. **Q11c BEFORE Q9** — Gap 6 lock. `TestDoubleMetaphone_PaperWorkedExamples` must pass green before the `double_metaphone.go:744` dupBranchBody removal lands.
2. **Q4 sentinel rename/add BEFORE Q4 4-section godoc rewrites** — godoc names the post-rename sentinel.
3. **Gap 4 rename BEFORE Gap 2 BDD step extension** — the step case branch references `ErrInvalidAlgoID` (post-rename name).
4. **Q4 `Validate` core BEFORE per-algorithm godoc cross-refs** — godoc references `[fuzzymatch.Validate]` which must compile.
5. **Q7a + Q8b + Q11b BEFORE Q13 bench.txt regeneration** — bench numbers reflect post-change code; Cluster 7's bench.txt regen runs LAST.
6. **Cluster 2 BEFORE Cluster 8 doc writing** — docs reference post-rename signatures.

---

## 6. Metadata

**Analog search scope:**
- Root package `.go` files (~75 files)
- `scripts/*.py`, `scripts/*.sh`
- `testdata/cross-validation/*/vectors.json`
- `testdata/golden/_staging/*.json`
- `tests/bdd/steps/*.go`, `tests/bdd/features/*.feature`
- `.github/workflows/*.yml`
- `docs/*.md`
- `examples/*/main.go`, `examples/*/main_test.go`, `examples/*/go.mod`

**Files scanned (read in full or targeted excerpts):** ~30 source files + 4 generator scripts + 1 BDD feature + 1 BDD step + 2 CI workflows + 1 example program + 2 cross-validation loaders + algoid.go + errors.go + scorer.go FMA site + cosine.go FMA site + tokenise.go + normalise.go + double_metaphone.go critical sections + ai_friendly_test.go + algorithms_golden_test.go templates + golden_canonical.go + export_test.go + scripts/verify-coverage-floors.sh.

**Pattern extraction date:** 2026-05-17

**Confidence:** HIGH for all classifications and analog citations. The codebase already contains every reusable template needed for the 9 scope clusters; Phase 8.5 is execution-discipline-bound, not pattern-discovery-bound. The five "No Analog Found" entries (4 of which are new BDD feature files needing a structural-only template, and 1 of which is a one-off audit report) are the only items where the planner relies on RESEARCH.md guidance instead of in-tree code-copy.

---

## PATTERN MAPPING COMPLETE
