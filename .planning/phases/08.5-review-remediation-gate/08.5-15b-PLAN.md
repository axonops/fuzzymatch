---
phase: 08.5
plan: 15b
type: execute
wave: 5
depends_on: [01, 02, 03]
files_modified:
  - tests/bdd/steps/scorer_steps.go
  - tests/bdd/features/scorer.feature
  - scorer_test.go
  - swg.go
  - mra.go
  - scorer.go
  - algoid.go
  - scorer_options.go
  - errors.go
  - example_test.go
  - props_test.go
  - golangci_residue.md
autonomous: true
requirements:
  - Gap2
  - Gap5
  - Gap7
  - Q14b
threat_model_refs: [T-08.5-29]

must_haves:
  truths:
    - "BDD scenarios for ErrInvalidAlgoID, ErrInvalidQGramSize, ErrInvalidTverskyParam exist in scorer.feature with 3 new case branches in constructingTheScorerShouldReturn"
    - "Gap 5 companion test TestDefaultScorer_NeverPanics_PropertyTest exists and asserts DefaultScorer succeeds under all reasonable build configurations"
    - "Gap 7 outcomes: MRACompare tuple documented (no code change); NewSWGParams validation added (Q2 framework); WithoutAlgorithm silent no-op documented + BDD scenario; AlgoIDs hot-path-caching godoc note added"
    - "30+ Code-fix Improvement-tier items addressed: gosec/staticcheck/gocyclo/gofumpt/misspell/QF1001/SA4004/G602 nolint cleanup"
  artifacts:
    - path: "tests/bdd/features/scorer.feature"
      provides: "3 new sentinel scenarios + 1 WithoutAlgorithm silent no-op scenario"
      contains: "ErrInvalidAlgoID"
    - path: "scorer_test.go"
      provides: "TestDefaultScorer_NeverPanics_PropertyTest"
      contains: "TestDefaultScorer_NeverPanics_PropertyTest"
  key_links:
    - from: "tests/bdd/steps/scorer_steps.go:326 constructingTheScorerShouldReturn switch"
      to: "3 new case branches (ErrInvalidAlgoID, ErrInvalidQGramSize, ErrInvalidTverskyParam)"
      via: "switch case append"
      pattern: "case \"ErrInvalidAlgoID\":"
---

<objective>
**Plan 15b (the non-mechanical half of the split former-Plan-15, M5 revision 2026-05-17)** — Gap 2 BDD scenarios + Gap 5 companion property test + Gap 7 outcomes + 30+ Code-fix lint sweep:

- **Gap 2**: 3 BDD scenarios + step case branches for `ErrInvalidAlgoID`, `ErrInvalidQGramSize`, `ErrInvalidTverskyParam`.
- **Gap 5 companion test**: `TestDefaultScorer_NeverPanics_PropertyTest` (the property test the panic site at scorer.go:586 — landed in Plan 01 — needs to be paired with; this is the test that asserts the panic path never fires under reasonable construction).
- **Gap 7 outcomes** (per CONTEXT.md default recommendations):
  - MRACompare (bool, int) tuple → keep; document in mra.go godoc (no code change).
  - NewSWGParams mutability → add validation (Q2 framework — panic with typed sentinel).
  - WithoutAlgorithm silent no-op → document in scorer_options.go godoc + add BDD scenario.
  - AlgoIDs() per-call alloc → godoc hot-path-caching note (no code change).
- **Q14b lint sweep**: 30+ Code-fix-tagged items from cross-cutting themes (gosec/staticcheck/gocyclo/gofumpt/misspell/QF1001/SA4004/G602/unparam cleanup) PLUS British English `misspell` sweep across .go files. Residue documented in `golangci_residue.md` with `//nolint` justifications.

**Hard dependencies:**
- Plan 01 (Gap 4 rename `ErrInvalidAlgoID`) — the BDD scenarios in this plan reference the post-rename sentinel.
- Plan 02 (Q3 rename) — code-fix items may touch monge_elkan.go which is post-rename.
- Plan 03 (Q5 PartialRatioScoreRunes removal) — code-fix items must not re-reference the removed function.
- **Plan 15a** (the mechanical half) — landed independently; this plan does not literally `depends_on: [15a]` because 15a touches a disjoint file set (dispatch_*.go + golden_canonical.go + export_test.go + scorer_options_internal_test.go), but the planner schedules 15b in the same Wave 5 with the same upstream dependencies.

Output: a focused commit set landing Gap 2 + Gap 5 companion + Gap 7 outcomes + the lint sweep; the verification gate's "zero Important" target advances materially.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@REVIEW-FINDINGS.md
@docs/requirements.md
@.claude/skills/go-coding-standards/SKILL.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
Gap 2: 3 BDD scenarios for the renamed sentinels:
- tests/bdd/features/scorer.feature: add 3 scenarios asserting NewScorer returns the named sentinel
- tests/bdd/steps/scorer_steps.go:326 constructingTheScorerShouldReturn: add 3 case branches

Gap 5 companion test:
- TestDefaultScorer_NeverPanics_PropertyTest: asserts DefaultScorer() succeeds under all reasonable build configurations (the panic path is dead-code by construction). Place in scorer_test.go.

Gap 7 outcomes:
- mra.go: add godoc paragraph documenting MRACompare's (bool, int) tuple convention
- swg.go: add validation to NewSWGParams (extend with Q2 framework)
- scorer_options.go: document WithoutAlgorithm silent no-op behaviour in its godoc
- algoid.go: add godoc note recommending `var allAlgos = fuzzymatch.AlgoIDs()` hot-path cache
- tests/bdd/features/scorer.feature: add scenario asserting WithoutAlgorithm(<unknown>) silently no-ops

30+ Code-fix items (sampling from REVIEW-FINDINGS.md cross-cutting themes table — executor enumerates by reading the file):
- gocritic ifElseChain (multiple sites)
- staticcheck SA4004 (first-rune-only — multiple sites)
- staticcheck QF1001 (De Morgan ×6)
- gosec G602 mra.go nolint cleanup
- gofumpt var grouping
- misspell artifact/artefact (British English)
- unparam runeAt unused return
- unparam probeScoreFnInvoke i=0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gap 2 BDD scenarios + Gap 5 companion property test</name>
  <files>tests/bdd/steps/scorer_steps.go, tests/bdd/features/scorer.feature, scorer_test.go</files>
  <read_first>
    - tests/bdd/steps/scorer_steps.go:326 (the constructingTheScorerShouldReturn switch)
    - tests/bdd/features/scorer.feature lines 105-127 (existing sentinel-scenario shape — extend pattern)
    - scorer.go:586-592 (the Gap 5 panic site landed by Plan 01)
  </read_first>
  <action>
1. **Gap 2 — BDD scenarios + step case branches**:
   - In `tests/bdd/steps/scorer_steps.go:326`, locate the `constructingTheScorerShouldReturn` switch. Add 3 case branches:
     ```go
     case "ErrInvalidAlgoID":
         return errors.Is(constructionErr, fuzzymatch.ErrInvalidAlgoID)
     case "ErrInvalidQGramSize":
         return errors.Is(constructionErr, fuzzymatch.ErrInvalidQGramSize)
     case "ErrInvalidTverskyParam":
         return errors.Is(constructionErr, fuzzymatch.ErrInvalidTverskyParam)
     ```
     (Match the exact shape of the existing case branches.)
   - In `tests/bdd/features/scorer.feature`, add 3 scenarios mirroring the existing sentinel-scenario shape at lines 105-127. Each scenario:
     ```
     Scenario: NewScorer rejects an invalid AlgoID
       Given a Scorer construction with WithAlgorithm(AlgoID(999), 1.0)
       When I construct the Scorer
       Then the result is ErrInvalidAlgoID
     ```
     (Adapt for ErrInvalidQGramSize and ErrInvalidTverskyParam.)

2. **Gap 5 companion test**:
   - In `scorer_test.go`, add `TestDefaultScorer_NeverPanics_PropertyTest`:
     - Use `testing/quick.Config{MaxCount: 100}` with a generator that constructs reasonable Scorer option permutations (subsets of DefaultScorerOptions).
     - Property: `DefaultScorer()` succeeds (returns a non-nil *Scorer) for the default option set; alternative reasonable construction patterns succeed without panic.
     - The locked test name in CONTEXT.md is `TestDefaultScorer_NeverPanics_PropertyTest` — use that.
  </action>
  <verify>
    <automated>go build ./... && go test -run 'TestDefaultScorer_NeverPanics' -race -count=1 ./... && cd tests/bdd && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "case \"ErrInvalidAlgoID\"|case \"ErrInvalidQGramSize\"|case \"ErrInvalidTverskyParam\"" tests/bdd/steps/scorer_steps.go` returns 3 (Gap 2 step case branches added).
    - `grep -cE "Scenario:.*ErrInvalidAlgoID|Scenario:.*ErrInvalidQGramSize|Scenario:.*ErrInvalidTverskyParam" tests/bdd/features/scorer.feature` returns 3 (Gap 2 scenarios added).
    - `grep -nE "TestDefaultScorer_NeverPanics_PropertyTest" scorer_test.go | wc -l` returns 1 (Gap 5 companion test).
    - `cd tests/bdd && go test ./...` exits 0 (3 new scenarios green).
    - `go test -run 'TestDefaultScorer_NeverPanics' -race -count=1 ./...` exits 0.
  </acceptance_criteria>
  <done>
    Gap 2 BDD scenarios + step case branches landed; Gap 5 companion property test exists and passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gap 7 outcomes + WithoutAlgorithm BDD scenario</name>
  <files>mra.go, swg.go, scorer_options.go, algoid.go, errors.go, tests/bdd/features/scorer.feature</files>
  <read_first>
    - mra.go (the MRACompare surface for Gap 7 documentation)
    - swg.go (NewSWGParams for Gap 7 validation addition)
    - scorer_options.go (WithoutAlgorithm for Gap 7 documentation)
    - algoid.go (AlgoIDs for Gap 7 hot-path-caching godoc note)
    - errors.go (verify post-Plan-01 sentinel set; if `ErrInvalidSWGParam` is missing, add it here OR re-use an existing sentinel per Q2 framework)
  </read_first>
  <action>
1. **MRACompare in mra.go**: add a godoc paragraph on `MRACompare` explaining the `(bool, int)` tuple convention is intentional (the NBS Tech Note 943 spec returns both the match decision AND the similarity score; the tuple is the canonical surface). Cross-reference docs/scorer.md and docs/algorithms.md (the doc updates land in Plan 17a/17b).

2. **NewSWGParams in swg.go**: add validation per the Q2 framework. The locked decision in CONTEXT.md Gap 7 says:
   > **Recommendation: Option B (panic with typed sentinel)** — minimal breaking-change surface, matches Tversky direct-call panic policy. Add a sentinel `ErrInvalidSWGParam` if not already declared (Plan 01 added several sentinels; cross-reference).

   Execute Option B: panic-on-invalid-construction with a typed sentinel. If `ErrInvalidSWGParam` does not exist in errors.go (post-Plan-01), add it in this task with the 4-section godoc template per documentation-standards SKILL. The signature `func NewSWGParams(...) SWGParams` is preserved (no error return; the panic carries the typed sentinel for `recover()` discrimination per Q2).

3. **WithoutAlgorithm in scorer_options.go**: add a godoc paragraph documenting the silent no-op behaviour:
   "`WithoutAlgorithm(id)` removes a previously-added algorithm from the option chain. If the named AlgoID is not present in the option chain (e.g. removing an algorithm that was never added), the option is a silent no-op — there is no error or warning. To verify removal succeeded, inspect `s.Algorithms()` post-construction."

4. **AlgoIDs in algoid.go**: add a godoc note recommending hot-path caching:
   "`AlgoIDs()` returns a freshly-allocated slice on every call. For hot paths that iterate over all algorithms, cache the slice once: `var allAlgos = fuzzymatch.AlgoIDs()`. The returned slice is safe to read concurrently but MUST NOT be mutated by the caller."

5. **WithoutAlgorithm BDD scenario** in tests/bdd/features/scorer.feature:
   ```
   Scenario: WithoutAlgorithm silently no-ops on absent AlgoID
     Given a Scorer with default options
     When I add WithoutAlgorithm(AlgoNonExistent) to the option chain
     And I construct the Scorer
     Then the Scorer construction succeeds
     And the Scorer's Algorithms list is unchanged
   ```
   Wire the step into tests/bdd/steps/scorer_steps.go via the existing pattern.

British English throughout; no testify in root tests.
  </action>
  <verify>
    <automated>go build ./... && go test -race -count=1 ./... && cd tests/bdd && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "Hot-path caching|hot path caching|hot-path caching" algoid.go | wc -l` returns at least 1 (Gap 7 AlgoIDs godoc note).
    - `grep -nE "silent no-op|silently no-ops" scorer_options.go | wc -l` returns at least 1 (Gap 7 WithoutAlgorithm godoc).
    - `grep -nE "Scenario:.*WithoutAlgorithm silently" tests/bdd/features/scorer.feature | wc -l` returns 1 (Gap 7 BDD scenario).
    - `grep -nE "panic.*fmt\\.Errorf.*ErrInvalidSWGParam|ErrInvalidSWGParam" swg.go | wc -l` returns at least 1 (Gap 7 NewSWGParams validation).
    - `grep -nE "// MRACompare" mra.go | wc -l` returns at least 1 (Gap 7 documentation paragraph anchor).
    - `go test -race -count=1 ./...` exits 0.
    - `cd tests/bdd && go test ./...` exits 0.
  </acceptance_criteria>
  <done>
    All four Gap 7 outcomes landed per CONTEXT.md default recommendations; one new BDD scenario covers WithoutAlgorithm silent no-op.
  </done>
</task>

<task type="auto">
  <name>Task 3: 30+ Code-fix lint sweep + British English misspell sweep</name>
  <files>golangci_residue.md, example_test.go, props_test.go</files>
  <read_first>
    - REVIEW-FINDINGS.md cross-cutting themes table (the explicit list of 30+ Code-fix items)
    - golangci-lint output: `golangci-lint run ./... 2>&1 | tee /tmp/lint-before.txt`
  </read_first>
  <action>
1. **Lint sweep**: address every Code-fix-tagged item from the REVIEW-FINDINGS.md cross-cutting themes table:
   - gocritic ifElseChain (multiple sites) — refactor if/else chains to switch where idiomatic.
   - staticcheck SA4004 (first-rune-only) — fix per-site.
   - staticcheck QF1001 (De Morgan ×6) — apply De Morgan transformations.
   - gosec G602 mra.go nolint cleanup — confirm bounds-check is correct then remove the nolint directive, OR keep with a tightened justification comment.
   - gofumpt var grouping — apply `gofumpt -w`.
   - unparam runeAt unused return — drop the unused return value or justify.
   - unparam probeScoreFnInvoke i=0 — refactor or justify.

2. **British English `misspell` sweep**: run `golangci-lint run --enable misspell ./...` across all .go files; convert American spellings to British equivalents (`color → colour`, `behavior → behaviour`, `serialize → serialise`, `normalize → normalise`, `artifact → artefact`, `analyze → analyse`).

3. **Residue document**: any Code-fix item that CANNOT be cleanly fixed gets a `//nolint:<linter>` annotation with a one-line justification comment, AND an entry in `.planning/phases/08.5-review-remediation-gate/golangci_residue.md` listing:
   - file:line
   - linter + rule
   - reason for keeping the nolint
   - whether the residue should be revisited at v1.x

   This file feeds Plan 18's verification report (the "Improvement-tier residue" section).
  </action>
  <verify>
    <automated>golangci-lint run ./...</automated>
  </verify>
  <acceptance_criteria>
    - `golangci-lint run ./...` exits 0 (or every remaining issue is a documented `//nolint` annotation with a corresponding entry in `golangci_residue.md`).
    - `grep -rE "color\\b|behavior\\b|normalize\\b|serialize\\b|artifact\\b|analyze\\b" $(git ls-files '*.go') | wc -l` returns 0 (British English in .go files).
    - `ls .planning/phases/08.5-review-remediation-gate/golangci_residue.md` returns the file (even if it lists zero residue — the file documents the sweep result).
    - `go test -race -count=1 ./...` exits 0 (no regression).
    - `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>
    Lint surface is clean (or residue is justified + documented); British English normalised across .go files; the golangci_residue.md feeds Plan 18's verification report.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → NewSWGParams direct call | Gap 7 lock: invalid construction parameters panic with typed sentinel; consumers can `recover()` and discriminate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-29 | T — Tampering via lint residue | golangci-lint Important findings | mitigate | Every residue carries a `//nolint` annotation with justification AND a `golangci_residue.md` row feeding the verification report. |
</threat_model>

<verification>
- Compile-green: `go build ./...` exits 0.
- Full test suite: `go test -race -count=1 ./...` exits 0.
- BDD: `cd tests/bdd && go test ./...` exits 0 with 4 new scenarios green (3 Gap 2 + 1 Gap 7).
- Lint: `golangci-lint run ./...` exits 0 (or documented residue).
</verification>

<success_criteria>
- Gap 2 BDD scenarios + step case branches landed.
- Gap 5 companion property test in place.
- Gap 7 outcomes (4 items) landed per CONTEXT.md defaults.
- Q14b lint sweep substantially advanced; residue documented.
- Phase 8.5 verification gate (zero Important) materially advances.
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-15b-SUMMARY.md` documenting:
- the Gap 2 + Gap 5 companion + Gap 7 outcomes;
- the lint-sweep count (before/after);
- any remaining Improvement-tier items deferred (documented as `//nolint` with rationale OR carried into Plan 17a/17b's docs);
- agent reviews invoked (api-ergonomics-reviewer, code-reviewer, go-quality, security-reviewer for the lint surface).
</output>
