---
phase: 08.5
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scorer_options.go
  - scorer_options_test.go
  - tversky.go
  - tversky_test.go
autonomous: true
requirements:
  - SCORER-02
  - SCORER-05
  - Q2
threat_model_refs: [T-08.5-07, T-08.5-08]

must_haves:
  truths:
    - "WithThreshold(NaN) returns ErrInvalidThreshold without panic"
    - "WithThreshold(+Inf) and WithThreshold(-Inf) return ErrInvalidThreshold"
    - "WithAlgorithm(_, NaN) returns ErrInvalidWeight"
    - "WithAlgorithm(_, +Inf) and WithAlgorithm(_, w<=0) return ErrInvalidWeight"
    - "WithTverskyAlgorithm(_, α, β, _) returns ErrInvalidTverskyParam when α + β <= 0"
    - "Direct TverskyScore call panics wrapping ErrInvalidTverskyParam on the same invalid (α, β) input"
    - "scorer_options.go godoc lines 376-380 are updated to reflect that α+β>0 IS re-checked at option time"
  artifacts:
    - path: "scorer_options.go"
      provides: "NaN/Inf/≤0 guards on WithThreshold, WithAlgorithm, WithTverskyAlgorithm"
      contains: "math.IsNaN, math.IsInf"
    - path: "scorer_options_test.go"
      provides: "Table-driven cases asserting each guard rejects NaN/Inf and returns the correct sentinel"
      contains: "TestWithThreshold_RejectsNaN, TestWithAlgorithm_RejectsNaN, TestWithTverskyAlgorithm_RejectsAlphaPlusBetaNonPositive"
  key_links:
    - from: "scorer_options.go WithTverskyAlgorithm"
      to: "ErrInvalidTverskyParam"
      via: "α+β>0 guard"
      pattern: "alpha\\+beta|α\\+β"
    - from: "tversky.go TverskyScore direct-call path"
      to: "ErrInvalidTverskyParam"
      via: "panic with typed sentinel wrap"
      pattern: "panic.*ErrInvalidTverskyParam"
---

<objective>
Q2 — execute the data-vs-parameter validation framework guards. Per `docs/requirements.md §6.A`: comparison-data inputs are lenient; parameter inputs are strict. This plan lands the strict-parameter guards on the three functional options that today admit pathological values, plus the direct-call panic discipline on `TverskyScore` that closes the panic-at-Score-time escape.

The locked spec entries:
- `WithThreshold(t)`: reject `math.IsNaN(t) || t < 0.0 || t > 1.0` → `ErrInvalidThreshold` (±Inf already rejected by range check).
- `WithAlgorithm(_, w)`: reject `math.IsNaN(w) || math.IsInf(w, 0) || w <= 0` → `ErrInvalidWeight` (replaces current `w <= 0`-only check).
- `WithTverskyAlgorithm(_, α, β, _)`: reject `α + β <= 0` → `ErrInvalidTverskyParam` (closes panic-at-Score-time escape; currently option only checks `α < 0 || β < 0`).
- Direct `TverskyScore(a, b, n, α, β)` call path: panic with typed `fmt.Errorf("%w: ...", ErrInvalidTverskyParam, ...)` on the same invalid input.

Output: a single atomic commit landing the four guards and their table-driven test entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@docs/requirements.md
@CHANGELOG.md
@.claude/skills/determinism-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
<!-- Q2 LOCKED in 08.5-CONTEXT.md §Q2; spec §6.A is authoritative -->

Functional option current state (scorer_options.go):
- WithThreshold at lines 257-266: range check `t < 0.0 || t > 1.0` exists; NaN check MISSING.
- WithAlgorithm at lines 150-165: range check `weight <= 0`; NaN/Inf checks MISSING.
- WithTverskyAlgorithm at lines 381-399: `alpha < 0 || beta < 0` check; `α+β <= 0` check MISSING; godoc at lines 376-380 hedges that α+β>0 is NOT re-checked (that comment becomes obsolete).

Direct-call panic discipline:
- tversky.go: existing direct-call panic; spec §6.A locks `panic(fmt.Errorf("%w: ...", ErrInvalidTverskyParam, ...))`. Verify current panic wraps the sentinel (may already be in place from Phase 5).

Sentinel references (these MUST be the post-Plan-01 sentinels):
- ErrInvalidThreshold (already canonical pre-8.5)
- ErrInvalidWeight (already canonical pre-8.5)
- ErrInvalidTverskyParam (already canonical pre-8.5)

Dependency on Plan 01: this plan DOES soft-depend on Plan 01's 4-section godoc rewrites for `ErrInvalidThreshold`, `ErrInvalidWeight`, and `ErrInvalidTverskyParam` (Plan 04's table-driven tests reference these sentinels by identity). However, this plan does NOT depend on Plan 01's Gap 4 rename (`ErrInvalidAlgorithm` → `ErrInvalidAlgoID`) because the three sentinels touched here are unaffected by that rename. Wave assignment (Plan 04 = Wave 2, Plan 01 = Wave 1) reflects the soft dependency: the godoc rewrites must already exist when Plan 04 edits the file containing them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NaN/Inf/α+β guards to 3 functional options + direct-call TverskyScore panic discipline</name>
  <files>scorer_options.go, scorer_options_test.go, tversky.go, tversky_test.go</files>
  <read_first>
    - scorer_options.go in full (especially lines 130-170 for WithAlgorithm, 257-280 for WithThreshold, 376-410 for WithTverskyAlgorithm)
    - scorer_options_test.go (the table-driven option-test pattern — find the existing tables and extend rather than create new tests)
    - tversky.go in full (the direct-call panic shape — verify it currently uses a string panic OR an `fmt.Errorf` wrap; Q2 locks the typed wrap)
    - tversky_test.go (the direct-call panic test — extend or add)
    - errors.go in full (post-Plan-01 state — confirm the 4-section godoc blocks for `ErrInvalidThreshold`, `ErrInvalidWeight`, `ErrInvalidTverskyParam` are present and intact; this plan must NOT damage them)
    - docs/requirements.md §6.A (the data-vs-parameter framework — authoritative source for the guards)
    - .planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md §2.C (the `WithThreshold` range-check analog + NaN extension pattern)
    - CHANGELOG.md `[Unreleased] > Changed` (existing Q2 entries — verify the post-edit code matches the documented behaviour)
  </read_first>
  <action>
Apply the four guards in a single atomic commit:

1. **In scorer_options.go — `WithThreshold`** (lines 257-266 or equivalent post-edit):
   - Add `math.IsNaN(t)` check BEFORE the existing range check.
   - Add `math.IsInf(t, 0)` check explicitly (even though `t > 1.0` already rejects `+Inf`, an explicit IsInf check is clearer and defends against any future relaxation of the range check).
   - Return `ErrInvalidThreshold` from both new branches.
   - Update the godoc to enumerate the rejection cases: NaN, ±Inf, t<0, t>1.
   - Add `import "math"` if not already present.

2. **In scorer_options.go — `WithAlgorithm`** (lines 150-165 or equivalent):
   - Replace the existing `weight <= 0` check with a comprehensive guard: `if math.IsNaN(weight) || math.IsInf(weight, 0) || weight <= 0 { return ErrInvalidWeight }`.
   - Update the godoc to enumerate the rejection cases: NaN, ±Inf, weight≤0.
   - The error sentinel `ErrInvalidWeight` is unchanged.

3. **In scorer_options.go — `WithTverskyAlgorithm`** (lines 381-399 or equivalent):
   - Add the `α + β <= 0` guard AFTER the existing `α < 0 || β < 0` check. The full guard becomes:
     - `if math.IsNaN(alpha) || math.IsNaN(beta) || math.IsInf(alpha, 0) || math.IsInf(beta, 0) { return ErrInvalidTverskyParam }`
     - `if alpha < 0 || beta < 0 { return ErrInvalidTverskyParam }`  (existing check preserved)
     - `if alpha + beta <= 0 { return ErrInvalidTverskyParam }`  (new check — closes the panic-at-Score-time escape)
   - The arithmetic `α + β` on two non-negative finite floats cannot itself overflow to a problematic value; the locked spec accepts this analysis.
   - Update the godoc at lines 376-380: REMOVE the hedge that says α+β>0 is NOT re-checked here. Replace with a clear enumeration of all rejection cases.

4. **In scorer_options_test.go** — extend the table-driven test cases:
   - Add to the WithThreshold test table: `{NaN, ErrInvalidThreshold}`, `{+Inf, ErrInvalidThreshold}`, `{-Inf, ErrInvalidThreshold}`.
   - Add to the WithAlgorithm test table: `{AlgoID, NaN, ErrInvalidWeight}`, `{AlgoID, +Inf, ErrInvalidWeight}`, `{AlgoID, -Inf, ErrInvalidWeight}`, `{AlgoID, 0, ErrInvalidWeight}`.
   - Add to the WithTverskyAlgorithm test table: `{0.0, 0.0, ErrInvalidTverskyParam}` (α+β=0), `{NaN, 1.0, ErrInvalidTverskyParam}`, `{1.0, +Inf, ErrInvalidTverskyParam}`. Preserve existing α<0/β<0 cases.

5. **In tversky.go — direct-call panic discipline**:
   - Audit the current direct-call entry path of `TverskyScore`. If it does NOT validate `(α, β)` itself (it accepts whatever the caller passes), add an early validation that mirrors the option-time guard. On invalid input, panic with `panic(fmt.Errorf("%w: TverskyScore requires alpha+beta>0 and finite, non-NaN inputs (got alpha=%v, beta=%v)", ErrInvalidTverskyParam, alpha, beta))`.
   - This closes the "data-vs-parameter framework — parameter inputs panic on direct-call" leg of Q2.
   - Add `import "fmt"` if not present.

6. **In tversky_test.go** — add a panic-recovery test asserting:
   - `defer func() { r := recover(); if r == nil { t.Fatal("expected panic") }; err, ok := r.(error); if !ok { t.Fatal("expected error in panic") }; if !errors.Is(err, ErrInvalidTverskyParam) { t.Fatalf("expected ErrInvalidTverskyParam, got %v", err) } }()` and then call `TverskyScore(a, b, n, 0.0, 0.0)` (or similar invalid input).

Commit message: `fix(scorer): reject NaN/Inf and α+β≤0 at option construction + Tversky direct-call panic discipline (Q2)`.

Per D-XX traceability: this plan implements SCORER-02 (validation), SCORER-05 (Tversky surface), Q2 (data-vs-parameter framework).
  </action>
  <verify>
    <automated>go build ./... && go test -run 'TestWithThreshold|TestWithAlgorithm|TestWithTverskyAlgorithm|TestTverskyScore_DirectCall' -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "math.IsNaN" scorer_options.go | wc -l` returns at least 3 (one in each of the three modified options).
    - `grep -n "alpha.*beta.*<=.*0\|alpha+beta.*<=.*0" scorer_options.go | wc -l` returns at least 1 (Tversky α+β guard).
    - `grep -nE "panic\\(fmt\\.Errorf.*ErrInvalidTverskyParam" tversky.go | wc -l` returns at least 1 (typed wrap at direct-call site).
    - `go build ./...` exits 0.
    - `go test -run 'TestWithThreshold|TestWithAlgorithm|TestWithTverskyAlgorithm' -race -count=1 ./...` exits 0.
    - `go test -run 'TestTverskyScore_DirectCall' -race -count=1 ./...` exits 0 (new panic-recovery test asserts `errors.Is(panicValue.(error), ErrInvalidTverskyParam)`).
    - `go vet ./...` exits 0.
    - `WithThreshold(math.NaN())` returns `ErrInvalidThreshold` (verified by table-driven test).
    - `WithAlgorithm(AlgoLevenshtein, math.NaN())` returns `ErrInvalidWeight` (verified by table-driven test).
    - `WithTverskyAlgorithm(AlgoQGramJaccard, 0.0, 0.0, n)` returns `ErrInvalidTverskyParam` (verified by table-driven test).
    - CHANGELOG.md `[Unreleased] > Changed` references Q2 with these guards.
    - **Plan 01 godoc integrity preserved**: `grep -v '^#' errors.go | grep -c '// What:'` returns 9 (the post-Plan-01 baseline: 4-section godoc on each of `ErrInvalidQGramSize`, `ErrInvalidTverskyParam`, `ErrInvalidAlgoID`, `ErrInvalidInnerAlgo`, `ErrInternalInvariantViolated`, `ErrEmptyScorer`, `ErrInvalidWeight`, `ErrInvalidThreshold`, `ErrMissingThreshold` — each contributes one `// What:` line). Plan 04 must NOT damage any of these blocks. If the post-Plan-01 baseline is narrower than expected, confirm against Plan 01's must_haves and update this count.
  </acceptance_criteria>
  <done>
    All four guards present in code; table-driven tests exercise NaN, ±Inf, and boundary cases; direct-call TverskyScore panic is recoverable via `errors.Is(panicValue.(error), ErrInvalidTverskyParam)`; CHANGELOG entry remains accurate; Plan 01's 4-section sentinel godoc blocks are intact post-edit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → WithThreshold/WithAlgorithm/WithTverskyAlgorithm | Untrusted parameter input — NaN/Inf admittance could destabilise the Scorer reduction (D — DoS via NaN propagation); negative or zero weights could nullify the composite or cause divide-by-zero downstream. |
| caller → TverskyScore direct call | Untrusted parameter input — α+β≤0 produces an undefined Tversky index per Tversky 1977; panic discipline is the locked behaviour per Q2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-07 | D — Denial of service via NaN propagation | scorer reduction loop | mitigate | WithThreshold and WithAlgorithm reject NaN/Inf at option construction time. The Scorer's threshold-first / weight-first validation pipeline (per Phase 8) means invalid construction never reaches the reduction loop. |
| T-08.5-08 | T — Tampering via invalid Tversky parameter | TverskyScore direct call | mitigate | Q2 lock: direct call panics with typed `fmt.Errorf("%w: ...", ErrInvalidTverskyParam, ...)`. Consumers using `recover()` can discriminate via `errors.Is`. The option-time guard prevents Scorer-construction-time admittance entirely. |
| T-08.5-09 | I — Information disclosure (panic message contents) | TverskyScore panic format | accept | The panic message includes the invalid (alpha, beta) values for debugging. These are caller-supplied parameters, not internal state — no information leak risk. |
</threat_model>

<verification>
- Compile-green: `go build ./...` exits 0.
- Test-green: option-test tables and direct-call panic-recovery test all pass.
- Static check: each modified option calls `math.IsNaN` and either `math.IsInf` or relies on the range check for ±Inf rejection.
- CHANGELOG accurate: the `[Unreleased] > Changed` section's Q2 entries match the new behaviour.
</verification>

<success_criteria>
- Q2 lock satisfied: all four guards present + tested.
- Phase 8.5 success criterion 7 (Cosine and Scorer reductions carry the FMA-defeating double-cast — this is Plan 06) is NOT addressed here.
- The api-ergonomics Critical (×2) and security Critical (×2) findings on NaN/Inf admittance close in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-04-SUMMARY.md` documenting:
- exact guards added (one per option + direct-call panic);
- table-driven test rows added with input → expected sentinel;
- the post-edit grep counts for `math.IsNaN`, `alpha+beta`, and panic-wrap;
- agent reviews invoked (api-ergonomics-reviewer, security-reviewer, determinism-reviewer for NaN/Inf rules, code-reviewer).
</output>
