---
phase: 08.5
plan: 17b
type: execute
wave: 6
depends_on: [01, 02, 03, 13, 17a]
files_modified:
  - README.md
  - docs/scorer.md
  - docs/tuning.md
  - docs/best-practices.md
  - docs/faq.md
  - docs/extending.md
  - docs/performance.md
  - docs/scan.md
  - llms.txt
  - llms-full.txt
  - CONTRIBUTING.md
  - doc.go
  - CHANGELOG.md
  - examples/validate-input-quality/main.go
  - examples/validate-input-quality/main_test.go
  - examples/validate-input-quality/go.mod
  - tests/bdd/features/normalisation.feature
  - tests/bdd/features/determinism.feature
  - tests/bdd/steps/normalisation_steps.go
  - tests/bdd/steps/determinism_steps.go
autonomous: true
requirements:
  - VALIDATE-06
  - Q4
  - Q6b
  - Gap3
threat_model_refs: [T-08.5-33]

must_haves:
  truths:
    - "Validate documented across the remaining 4 required surfaces (README Quick Start / Common Patterns, llms.txt + llms-full.txt, user-guide section, runnable example) — surfaces 2 and 3 land in Plan 17a"
    - "README Quick Start refreshed to post-Q1/Q3/Q5 signatures; includes Validate-then-Score Common Pattern"
    - "llms.txt and llms-full.txt include Validate, Warning, WarnKind entries + post-rename signatures"
    - "Gap 3 BDD feature files exist: normalisation.feature + determinism.feature (scan.feature + suppression.feature deferred to Phase 9)"
    - "examples/validate-input-quality/ exists as a runnable example with main + main_test + go.mod"
    - "docs/scorer.md Phase 8 stale references replaced with v1.0 references"
    - "CONTRIBUTING.md wrong §11.2 cross-reference fixed; llms-full.txt Hamming gap filled; doc.go platform list includes darwin/amd64"
    - "American/British mixed spelling normalised to British across all docs"
  artifacts:
    - path: "README.md"
      provides: "Refreshed Quick Start + Common Patterns section including Validate-then-Score"
      contains: "Validate"
    - path: "llms.txt"
      provides: "Updated AI-friendly index with Validate + post-rename signatures"
      contains: "Validate"
    - path: "examples/validate-input-quality/main.go"
      provides: "Runnable Validate example with documented Output"
      contains: "Validate"
  key_links:
    - from: "README Quick Start"
      to: "examples/identifier-similarity/main.go"
      via: "readme_shop_front_test.go meta-test"
      pattern: "identifier-similarity"
---

<objective>
**Plan 17b (the docs-heavy half of the split former-Plan-17, M6 revision 2026-05-17)** — consumer-facing documentation pass that builds on Plan 17a's `docs/algorithms.md` foundation and the 23 per-algorithm godoc cross-refs:

1. **VALIDATE-06 / Q4 — Validate's remaining 4 documentation surfaces** (surfaces 2 and 3 land in Plan 17a):
   - Surface 1: README Quick Start / Common Patterns
   - Surface 4: llms.txt + llms-full.txt
   - Surface 5: User-guide section (docs/best-practices.md NEW or existing equivalent)
   - Surface 6: Runnable examples/validate-input-quality/

2. **README Quick Start refresh** to post-Q1/Q3/Q5 signatures.

3. **Consumer-facing doc updates** from REVIEW-FINDINGS.md: docs/scorer.md "Phase 8" stale refs → v1.0; CONTRIBUTING.md wrong §11.2; llms-full.txt Hamming gap; faq.md "Why no Soft-TFIDF?"; American/British spelling sweep across docs; extending.md wrong §11; doc.go platform list missing darwin/amd64.

4. **Gap 3 BDD feature files**: normalisation.feature + determinism.feature land in Phase 8.5 per CONTEXT.md default; scan.feature + suppression.feature deferred to Phase 9 (with acknowledgement in Plan 18's verification report).

5. **Q6b — CamelCase WarnKind.String() documented** — verify Plan 13's CamelCase output is reflected in the user-facing docs (no code change here).

**Hard dependencies:** Plans 01 (sentinel renames), 02 (MongeElkan rename), 03 (PartialRatioScoreRunes removal), 13 (Validate core exists), 17a (docs/algorithms.md exists with Validate section + per-algorithm content + Q12c panic-surface; README cross-references that section).

Output: a comprehensive consumer-docs pass closing the remaining docs-writer/user-guide-reviewer Important findings + the 4 remaining Validate doc surfaces + the Gap 3 BDD files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@docs/requirements.md
@CHANGELOG.md
@REVIEW-FINDINGS.md
@.claude/skills/documentation-standards/SKILL.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
Plan 17a outputs (consumed here):
- docs/algorithms.md fully populated with per-algorithm sections + Validate section + Q12c panic-surface + Q14a 5 MB note
- 23 algorithm .go files have godoc cross-references to fuzzymatch.Validate
- Q6a SKILL update present

Plan 13 outputs (consumed here):
- Public Validate, Warning, WarnKind surfaces

Documentation-standards SKILL six-surface mandate — Plan 17b covers:
- Surface 1: README Quick Start / Common Patterns
- Surface 4: llms.txt + llms-full.txt
- Surface 5: User-guide section (docs/best-practices.md)
- Surface 6: Runnable examples/validate-input-quality/

Gap 3 BDD feature files:
- normalisation.feature: scenarios covering Normalise's NFC/NFD + diacritic stripping + ASCII fast path
- determinism.feature: scenarios covering cross-platform golden-file byte-identity + no-map-iteration discipline
- (scan.feature + suppression.feature deferred to Phase 9 per CONTEXT.md Gap 3 default)

British English: every doc edit uses British spelling (colour, behaviour, normalise, serialise, artefact).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README Quick Start + llms.txt + llms-full.txt + doc.go + CONTRIBUTING + docs/scorer + docs/faq + docs/extending + docs/best-practices + British English sweep</name>
  <files>README.md, llms.txt, llms-full.txt, doc.go, CONTRIBUTING.md, docs/scorer.md, docs/faq.md, docs/extending.md, docs/best-practices.md, docs/performance.md, docs/scan.md, CHANGELOG.md, docs/tuning.md</files>
  <read_first>
    - README.md in full (Quick Start section + structure)
    - llms.txt + llms-full.txt in full
    - doc.go (platform list — verify darwin/amd64 mention)
    - CONTRIBUTING.md (the wrong §11.2 reference — find via grep)
    - docs/scorer.md (Phase 8 stale references — find via grep)
    - docs/faq.md, docs/extending.md, docs/best-practices.md (or create if absent), docs/performance.md, docs/scan.md
    - Plan 17a SUMMARY (docs/algorithms.md anchors — used as the README cross-reference targets)
    - Plan 13 SUMMARY (Validate surface + WarnKind rule table to embed)
    - REVIEW-FINDINGS.md docs-writer Important findings (the explicit list of doc fixes)
  </read_first>
  <action>
1. **README.md refresh**:
   - Locate the Quick Start section. Update any code blocks referencing:
     - MongeElkanScore — replace with the post-Plan-02 surface (symmetric default + no opts param).
     - HammingDistance — confirm silent-max behaviour (post-Q1 spec catch-up).
     - PartialRatioScoreRunes — replace with PartialRatioScore (post-Plan-03 removal).
   - Add a "Common Patterns" sub-section showing the Validate-then-Score idiom (~30 LOC NEW):
     - Example: read input → call Validate → if any WarnKind.UnequalLength for Hamming, surface a UI warning → call DefaultScorer.Score.
   - Cross-reference docs/algorithms.md Q12c panic-surface section (Plan 17a authored).
   - Verify the README runs through readme_shop_front_test.go's meta-test cleanly.

2. **llms.txt + llms-full.txt updates**:
   - Add entries for Validate, Warning, WarnKind + 5 WarnKind constants in the appropriate section.
   - Refresh signatures post-Q1/Q3/Q5: remove PartialRatioScoreRunes; update MongeElkanScore to symmetric default; update HammingDistance signature note (silent-max).
   - Verify `make verify-llms-sync` (Plan 16) exits 0 after the edit.
   - Close the docs-writer Important "llms-full.txt Hamming gap" finding (add the missing Hamming entry if absent).

3. **doc.go platform list**:
   - Verify the platform list mentions darwin/amd64 (per the Phase 8 docs-writer Important finding). Add if missing.

4. **CONTRIBUTING.md wrong §11.2 reference**:
   - Find the wrong §11.2 reference (likely should be §11.5 or §12.x — verify against docs/requirements.md current state). Fix.

5. **docs/scorer.md "Phase 8" stale references**:
   - Replace "Phase 8" with "v1.0" or remove dev-process references entirely (consumer docs should not reference internal phase numbering).
   - Apply the same "Phase 8" sweep to docs/tuning.md.

6. **docs/faq.md**:
   - Add an entry "Why no Soft-TFIDF?" citing docs/requirements.md §4 (Soft-TFIDF intentionally out of scope; reasoning: complex implementation, alternatives exist via Monge-Elkan + JaroWinkler).
   - Add an entry "What is Validate and when should I use it?" cross-referencing docs/algorithms.md.

7. **docs/extending.md wrong §11 reference**:
   - Fix the wrong §11 cross-reference (likely should be §10 — verify).

8. **docs/best-practices.md** (NEW if absent, or extend if exists):
   - Add a user-guide section on Validate (the 5th of the 6 Validate doc surfaces): when to use, common patterns, examples, what Validate does NOT do (does not modify inputs, does not return errors, never panics).

9. **docs/performance.md, docs/scan.md**: confirm presence (some may still be TBD scaffolds — flag for future Phase 9 work; this plan does NOT fill in scan.md content per Gap 3 deferral).

10. **American/British spelling sweep across docs**: run `make misspell` or `golangci-lint run --enable misspell` across all .md files; convert flagged American spellings to British (`color → colour`, `behavior → behaviour`, `serialize → serialise`, `normalize → normalise`, `artifact → artefact`, `analyze → analyse`). Plan 15b runs the same sweep on .go files; this task covers docs.

11. **Q6b — confirm CamelCase WarnKind.String() output documented**:
   - Verify warn_kind.go (Plan 13) returns CamelCase. No additional doc surface needed beyond Plan 13's tests + the Validate section in docs/algorithms.md (Plan 17a) + the user-guide section in docs/best-practices.md (this task).
  </action>
  <verify>
    <automated>markdownlint docs/*.md README.md CONTRIBUTING.md CHANGELOG.md 2>&1 || true && make verify-llms-sync</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "MongeElkanScoreSymmetric|PartialRatioScoreRunes" README.md llms.txt llms-full.txt | wc -l` returns 0 (no stragglers of pre-rename names in docs).
    - `grep -nE "Validate|WarnKind|Warning" llms.txt llms-full.txt | wc -l` returns at least 6 (Validate documented).
    - `make verify-llms-sync` exits 0.
    - `grep -n "darwin/amd64" doc.go | wc -l` returns at least 1.
    - `grep -nE "Soft-TFIDF" docs/faq.md | wc -l` returns at least 1.
    - `markdownlint docs/*.md README.md CONTRIBUTING.md` exits 0 (or documented `<!-- markdownlint-disable -->` exceptions).
    - `grep -rnE "color\\b|behavior\\b|normalize\\b|serialize\\b|artifact\\b|analyze\\b" docs/ README.md CONTRIBUTING.md | wc -l` returns 0 (British English in docs).
  </acceptance_criteria>
  <done>
    README has refreshed Quick Start + Validate Common Pattern; llms.txt + llms-full.txt + doc.go + CONTRIBUTING + faq + extending + scorer + tuning + best-practices all updated; British English normalised across docs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Runnable example for Validate + Gap 3 BDD feature files (normalisation + determinism)</name>
  <files>examples/validate-input-quality/main.go, examples/validate-input-quality/main_test.go, examples/validate-input-quality/go.mod, tests/bdd/features/normalisation.feature, tests/bdd/features/determinism.feature, tests/bdd/steps/normalisation_steps.go, tests/bdd/steps/determinism_steps.go</files>
  <read_first>
    - examples/scorer-composition/main.go + main_test.go + go.mod (the template)
    - validate.go (Plan 13 surface)
    - tests/bdd/features/scorer.feature (Gherkin structure)
    - tests/bdd/steps/scorer_steps.go (step pattern)
    - normalise.go (the surface scenarios in normalisation.feature drive)
    - .planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md Gap 3 default (normalisation + determinism in 8.5; scan + suppression deferred to Phase 9)
  </read_first>
  <action>
1. **examples/validate-input-quality/ runnable example** — the 6th Validate doc surface:
   - Create directory + main.go + main_test.go + go.mod following the scorer-composition template.
   - main.go: read 2-3 input pairs (hardcoded), call Validate, print warnings to stdout.
   - main_test.go: capture stdout, assert output matches documented expected lines (Go's os.Pipe redirect pattern).
   - go.mod: pin to the local fuzzymatch via replace directive (same as scorer-composition).
   - Add a meta-test in documentation_test.go (Plan 12) ensuring this example compiles via the docs test.

2. **tests/bdd/features/normalisation.feature** — Gap 3:
   - Scenarios covering:
     - Normalise ASCII fast path (NFC/NFD round-trip on `café`).
     - Diacritic stripping (`café → cafe`, `Müller → Mueller`).
     - Combining-mark handling.
     - Property: `Normalise(s)` is idempotent: `Normalise(Normalise(s)) == Normalise(s)`.
   - Step definitions in `tests/bdd/steps/normalisation_steps.go` — Given input, When call Normalise, Then assert output.

3. **tests/bdd/features/determinism.feature** — Gap 3:
   - Scenarios covering:
     - Calling Score(a, b) twice produces byte-identical results.
     - ScoreAll(a, b) produces deterministic key set + values.
     - Golden file byte-identity across 5-platform CI matrix (assertion-style scenario referencing testdata/golden/algorithms.json).
   - Step definitions in `tests/bdd/steps/determinism_steps.go`.

4. Wire both new feature files into the godog runner per existing pattern.

5. **scan.feature + suppression.feature** are NOT created here — deferred to Phase 9 per CONTEXT.md Gap 3 default. The verification-gate report (Plan 18) acknowledges this deferral explicitly.
  </action>
  <verify>
    <automated>cd examples/validate-input-quality && go run . && cd ../.. && cd tests/bdd && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `ls examples/validate-input-quality/main.go examples/validate-input-quality/main_test.go examples/validate-input-quality/go.mod` returns all 3 files.
    - `cd examples/validate-input-quality && go run .` exits 0 (example runs).
    - `cd examples/validate-input-quality && go test ./...` exits 0 (Output-match test green).
    - `ls tests/bdd/features/normalisation.feature tests/bdd/features/determinism.feature` returns both files.
    - `grep -cE "^\\s+Scenario:" tests/bdd/features/normalisation.feature` returns at least 3.
    - `grep -cE "^\\s+Scenario:" tests/bdd/features/determinism.feature` returns at least 3.
    - `cd tests/bdd && go test ./...` exits 0 (new scenarios green).
    - `ls tests/bdd/features/scan.feature tests/bdd/features/suppression.feature 2>/dev/null | wc -l` returns 0 (Phase 9 deferred).
  </acceptance_criteria>
  <done>
    Validate's 6th doc surface (runnable example) landed; Gap 3 BDD feature files for normalisation + determinism present and green; scan + suppression explicitly deferred to Phase 9 (documented in Plan 18 verification report).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README quick-start code → consumer expectations | Stale Quick Start misleads consumers post-Q1/Q3/Q5. |
| Validate documentation → consumer adoption | Plan 17b closes the remaining 4 of 6 Validate doc surfaces (1, 4, 5, 6); Plan 17a closes surfaces 2 and 3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-33 | I — Information disclosure (stale signatures) | README + llms.txt + per-algorithm godoc | mitigate | Plan 16's verify-llms-sync target + readme_shop_front_test.go meta-test (Plan 12) catch stale signatures. British English sweep + markdownlint catch consistency issues. |
</threat_model>

<verification>
- markdownlint exits 0 across all .md files.
- make verify-llms-sync exits 0.
- All 23 algorithm files have Validate cross-references (verified by Plan 17a).
- Runnable example compiles + runs + passes its Output meta-test.
- New BDD feature files green.
- British English normalised in docs.
</verification>

<success_criteria>
- VALIDATE-06 satisfied: surfaces 1, 4, 5, 6 complete (surfaces 2, 3 in Plan 17a).
- Gap 3 default satisfied (normalisation + determinism in 8.5; scan + suppression deferred to Phase 9 — explicit acknowledgement in Plan 18).
- Phase 8.5 success criterion 3 (Validate ships with 6 doc surfaces) fully satisfied once both 17a and 17b land.
- ~30+ docs Important findings close.
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-17b-SUMMARY.md` documenting:
- the total LOC added across all doc edits;
- the 4 Validate surfaces (1, 4, 5, 6) with the file:line where each lands;
- the British English sweep count (misspell hits resolved in docs);
- the Gap 3 deferred items list (scan.feature + suppression.feature → Phase 9);
- agent reviews invoked (docs-writer mandatory; user-guide-reviewer mandatory; bdd-scenario-reviewer for the new feature files).
</output>
