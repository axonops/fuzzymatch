---
phase: 08.5
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - partial_ratio.go
  - dispatch_partial_ratio.go
  - partial_ratio_test.go
  - partial_ratio_fuzz_test.go
  - partial_ratio_bench_test.go
  - example_test.go
  - algoid_test.go
  - export_test.go
autonomous: true
requirements:
  - TOKEN-04
  - Q5
threat_model_refs: [T-08.5-06]

must_haves:
  truths:
    - "PartialRatioScoreRunes does not exist anywhere in the codebase"
    - "PartialRatioScore (byte-only) is the sole partial-ratio surface for token-tier consumers"
    - "All rune-path tests, fuzz harnesses, benchmarks, and the runnable example for PartialRatioScoreRunes are deleted in lockstep"
    - "testdata/fuzz/FuzzPartialRatioScoreRunes/ corpus directory is removed"
  artifacts:
    - path: "partial_ratio.go"
      provides: "Sole PartialRatioScore public function (no rune variant)"
      contains: "func PartialRatioScore("
    - path: "dispatch_partial_ratio.go"
      provides: "Dispatch entry referencing only the byte-path function"
      contains: "PartialRatioScore"
  key_links:
    - from: "algoid_test.go"
      to: "PartialRatio dispatch binding"
      via: "test assertion"
      pattern: "AlgoPartialRatio"
---

<objective>
Q5 — execute the `PartialRatioScoreRunes` removal. This is one of the four breaking pre-v1.0 surface changes. Per Q5 lock: token-tier algorithms operate on the output of `Tokenise`, which is itself rune-aware; the byte-level Indel kernel is correct on post-`Tokenise` byte strings, so a rune-variant is redundant.

Output: atomic commit deleting the rune-path function, its tests, fuzz harness, benchmarks, runnable example, and AlgoID test assertion in lockstep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md
@.planning/phases/08.5-review-remediation-gate/08.5-RESEARCH.md
@.planning/phases/08.5-review-remediation-gate/08.5-PATTERNS.md
@docs/requirements.md
@CHANGELOG.md
@.claude/skills/fuzzymatch-review-protocol/SKILL.md

<interfaces>
<!-- Q5 LOCKED in 08.5-CONTEXT.md — rune-variant removal -->

BEFORE:
- `func PartialRatioScore(a, b string) float64` (byte path)
- `func PartialRatioScoreRunes(a, b string) float64` (rune path — REMOVE)

AFTER:
- `func PartialRatioScore(a, b string) float64` (sole surface)

Touched files per RESEARCH.md §3.2:
- partial_ratio.go (delete function + rune-path implementation)
- dispatch_partial_ratio.go:20, 45 (godoc cleanup)
- partial_ratio_test.go (delete rune-path tests)
- partial_ratio_fuzz_test.go:86-127 (delete FuzzPartialRatioScoreRunes)
- partial_ratio_bench_test.go:107-135 (delete 3 rune-path benchmarks)
- example_test.go:395-408 (delete ExamplePartialRatioScoreRunes)
- algoid_test.go:449 (delete or update rune-variant assertion)
- export_test.go:154 (delete rune-variant export entry)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PartialRatioScoreRunes removal — atomic commit</name>
  <files>partial_ratio.go, dispatch_partial_ratio.go, partial_ratio_test.go, partial_ratio_fuzz_test.go, partial_ratio_bench_test.go, example_test.go, algoid_test.go, export_test.go</files>
  <read_first>
    - partial_ratio.go in full (establishes the rune-path function body to delete)
    - partial_ratio_test.go (every test referencing the rune path must be located and deleted)
    - partial_ratio_fuzz_test.go (FuzzPartialRatioScoreRunes at lines 86-127 to delete)
    - partial_ratio_bench_test.go (3 rune-path benchmarks at lines 107-135 to delete)
    - example_test.go:395-408 (ExamplePartialRatioScoreRunes to delete)
    - dispatch_partial_ratio.go (godoc reference to delete at lines 20 and 45)
    - algoid_test.go:449 (rune-variant test assertion)
    - export_test.go:154 (test-only re-export entry to delete)
    - .planning/phases/08.5-review-remediation-gate/08.5-CONTEXT.md Q5 lock
    - CHANGELOG.md `[Unreleased] > Breaking` (existing Q5 entry)
  </read_first>
  <action>
Atomic deletion across 8 files. Order operations to maintain compile-green if possible (Go's compile-time export check means deletion must be uniform):

1. **In partial_ratio.go** — delete the `PartialRatioScoreRunes` function declaration in full, plus any private helper functions it solely used. Verify by grep that no remaining function in the file references the deleted name. Update the package-level godoc / file header to remove any "byte AND rune surfaces" wording (Q5 — byte-only now).

2. **In dispatch_partial_ratio.go** — at lines 20 (godoc) and 45 (any reference): remove every reference to the rune variant. The dispatch table entry for `AlgoPartialRatio` should remain (it always referenced only the byte path) — verify nothing dispatches against the rune variant.

3. **In partial_ratio_test.go** — delete every test function whose name contains `Runes` or whose body calls `PartialRatioScoreRunes`. Verify no remaining test references the deleted symbol.

4. **In partial_ratio_fuzz_test.go** — delete `FuzzPartialRatioScoreRunes` at lines 86-127 (or equivalent — confirm exact range via grep). The corresponding seed corpus directory `testdata/fuzz/FuzzPartialRatioScoreRunes/` is also deleted (if it exists).

5. **In partial_ratio_bench_test.go** — delete the 3 rune-path benchmarks at lines 107-135 (or equivalent range — confirm via grep).

6. **In example_test.go** — delete `ExamplePartialRatioScoreRunes` at lines 395-408. Verify no doc cross-reference to this example remains in godoc.

7. **In algoid_test.go** — at line 449, audit and remove any test assertion that specifically targeted the rune variant. If a single test loops over byte + rune algorithm pairs, restructure to drop the rune-path branch for PartialRatio.

8. **In export_test.go** — at line 154, remove the test-only re-export entry for `PartialRatioScoreRunes` if present.

9. Delete the seed corpus directory `testdata/fuzz/FuzzPartialRatioScoreRunes/` if it exists (use `git rm -r testdata/fuzz/FuzzPartialRatioScoreRunes/`).

Commit message: `feat!: remove PartialRatioScoreRunes (Q5, breaking pre-v1.0)`.

**Discipline:** verify post-edit that `grep -r "PartialRatioScoreRunes" .` returns zero matches across the entire repo (including docs, llms.txt, llms-full.txt — those will be updated in Plan 15 docs cluster, but flag any references found here for the docs plan).

Per D-XX traceability: this plan implements TOKEN-04 (post-Phase 6 surface refinement) and Q5 (locked breaking-change).
  </action>
  <verify>
    <automated>go build ./... && go test -run 'TestPartialRatio|ExamplePartialRatio|TestAlgoID' -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "PartialRatioScoreRunes" $(git ls-files '*.go') | wc -l` returns 0.
    - `go build ./...` exits 0.
    - `go test -run 'TestPartialRatio' -race -count=1 ./...` exits 0.
    - `go test -run 'ExamplePartialRatio' -race -count=1 ./...` exits 0 (the byte-path example, if any, still runs).
    - `go vet ./...` exits 0.
    - `ls testdata/fuzz/FuzzPartialRatioScoreRunes/` returns "No such file or directory" (corpus dir removed).
    - CHANGELOG.md `[Unreleased] > Breaking` entry for Q5 remains accurate.
  </acceptance_criteria>
  <done>
    The rune-path function and all its supporting tests/benchmarks/fuzz/example/corpus are gone; only `PartialRatioScore` (byte path) remains; repo compiles green; the seed corpus directory is removed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → PartialRatioScore | Post-deletion surface: caller can only invoke the byte-path; rune-aware inputs are accepted via the byte path (UTF-8 bytes are preserved). The token-tier upstream `Tokenise` is rune-aware, so consumers are not regressed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-06 | T — Tampering via incomplete deletion | PartialRatioScoreRunes removal across 8 files | mitigate | Single atomic commit; `grep -r PartialRatioScoreRunes` returns 0 matches post-deletion. CI compile-green is the regression signal. |
</threat_model>

<verification>
- Compile-green: `go build ./...` exits 0.
- Test-green: `go test ./...` exits 0 (no test references the deleted function).
- Static check: `grep -rE "PartialRatioScoreRunes" $(git ls-files '*.go') | wc -l` equals 0.
- Static check (docs, deferred to docs plan): `grep -rE "PartialRatioScoreRunes" README.md docs/ llms.txt llms-full.txt` — any matches flagged for Plan 15 cleanup but DO NOT block this plan.
</verification>

<success_criteria>
- Q5 lock satisfied: `PartialRatioScoreRunes` does not exist anywhere in the codebase.
- Phase 8.5 success criterion 2 (breaking-change items land cleanly with passing tests): PartialRatioScoreRunes removal portion complete.
- CHANGELOG.md entry remains accurate (Q5).
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-review-remediation-gate/08.5-03-SUMMARY.md` documenting:
- exact files touched + line ranges deleted;
- whether the seed corpus directory was present and was deleted;
- any residual references in docs surfaced for Plan 15;
- agent reviews invoked (api-ergonomics-reviewer, code-reviewer, commit-message-reviewer).
</output>
